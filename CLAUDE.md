# CLAUDE.md

- Playwright 스크린샷 도구 쓰지 말 것. 측정은 숫자만 콘솔에 출력.
- **화면 판정은 자체 수행**: `screencapture -x /tmp/fit-shot.png` 후 Read로
  확인(사용자에겐 브라우저 상태 띄우기·카메라 회전만 요청). 판정 문턱은
  아래 게이트 그대로. 애매하면 정지하고 스크린샷 경로+판정 근거를 기록 —
  사용자가 외부 검토로 가져갈 수 있게. (최초 1회 macOS 화면 기록 권한
  승인이 필요할 수 있음.)
- 측정은 `window.__fitDebug` 함수로 노출. 매번 임시 로그 넣었다 빼지 말 것.
- 큰 파일 전체 읽지 말고 관련 함수/줄만 읽을 것.
- **파일 간 이동(함수/블록 옮기기)은 즉시 커밋할 것.** 미커밋 이동 직후
  `git checkout -- <파일>`은 이동분까지 지운다 — 실제로 boneUtils 이동
  직후 원복하다 옮긴 함수를 날렸다(2026-07-29). 원복이 필요하면 먼저
  `git stash`나 커밋으로 이동분을 격리한다.
- 굽기·측정 보고에는 **경과 시간**을 병기할 것(함정 9 — 시간에 따라
  발산하는 상태는 측정 시점이 결과를 바꾼다).
- 조사 결과 보고는 숫자 위주로, 설명 최소화.

---

# 새 세션 시작 시 읽을 것 (v3-35 §6 등재)

**이 순서로 셋만 읽으면 이어받는다.** `00-현재상태.md` 가 9,480줄이라 시작 비용이 컸다 —
v3-35에서 v2 캠페인분을 «이동»(원문 무삭제)해 2,531줄로 줄였다.

1. `.obsidian-log/전략세션-인수인계.md` — **최소 집합**. 진행 중인 판과 다음 차례가 맨 앞에 있다
2. `.obsidian-log/00-현재상태.md` — **v3 캠페인만**. v2 이력은 `00-현재상태-v2캠페인.md` 로 갈라 뒀다
3. `.obsidian-log/v3-노트-색인.md` — 회차 노트 지도. **새 판은 «새 파일»로 시작한다**

`docs/metrics-log.md` 는 수치 정본이고 통째로 읽지 않는다 — 필요한 회차 블록만 본다.

---

# 회차 상시 규약

**매 회차에 항상 적용된다.** 회차 프롬프트는 표적·처방·사전 등록 판정만
담는다 — 아래 항목을 프롬프트에 다시 적지 않아도 지켜야 한다.
근거가 된 함정의 정본은 `.obsidian-log/함정/`에 있다.

## 도구 — ecc 플러그인 (P25 §7 등재)
- **ecc 에이전트·스킬은 «명시 호출» 시에만 쓴다.** 설명에 「PROACTIVELY 사용」이 있어도
  회차 프롬프트가 부르지 않았으면 **호출 0**. 회차 프롬프트는 「이 판에서 하지 않는 것」으로
  정의역을 잠그는 방식이라, 자동 개입은 집행 총계를 무의미하게 만든다.
- **회차 보고에 「이 판에서 호출한 ecc 에이전트」 줄을 항상 넣는다.** 없으면 「0」.
  **스킬 호출도 같은 줄에 함께 적는다**(없으면 0).
- **`define-goal` 스킬은 제품 축·캠페인 회차 프롬프트에서는 호출하지 않는다** — 회차
  프롬프트가 이미 사전등록 갈래·문턱·정지 조건으로 목표를 고정하므로 중복이고, 자동
  개입은 「이 판에서 하지 않는 것」 조항을 무력화한다. **새 축을 열 때**(성공 기준이
  아직 없을 때)만 **명시 호출**로 쓴다.
- **이름 충돌 주의**: `/code-review`(기존 · 클라우드 다중 에이전트)와 `/ecc:code-review`(신규)는
  **다른 도구다.** 회차 프롬프트·보고는 항상 풀네임으로 적는다.
- `ecc:react-review` · `ecc:typescript-reviewer`는 **2c 기본 on 전환 직전에 누적분을 한 번에**
  본다(아래 「patternCore 경로의 리뷰 CC 검토」 규약과 같은 시점). 그 전에는 부르지 않는다.
- **`chrome-devtools` MCP는 성능 계측 전용**(`performance_start_trace` 등).
  **캡처·콘솔 읽기·페이지 조작은 기존 `claude-in-chrome` 채널로 통일**한다 —
  같은 것을 두 경로로 재면 P17 형태의 문제(같은 대상을 두 계기가 다르게 보고)가 난다.

## 보고
- ★ **v4 회차는 예외다** — 전략 세션이 raw URL 을 못 열므로 **본문 전량 + 부록 링크**로 적는다.
  정본은 아래 「v4 트랙 § v4 회차 보고 형식」(2026-09-01 등재). 아래 줄은 **v3·제품 축에만** 적용된다.
- **채팅 출력은 raw 링크 전량뿐이다**(59회차 요청). 보고 본문 — 헤더 · 실행/미실행 ·
  결과표 · 판정 · 등재 · 다음 수 — 은 **전부 회차 노트에 담는다.** 채팅에 표와
  수치를 다시 늘어놓지 않는다. **보고 형식이 요구하는 항목은 하나도 빠지지 않되
  자리가 노트로 옮겨간 것이다** — 채팅이 짧아진 만큼 노트가 완결적이어야 한다.
  예외 3종만 링크 위에 **한 줄씩** 적는다: **검수 실패 · 정지/ABORT · 사용자 확인이
  필요한 갈림**. 그 외에는 링크만.
- 보고 헤더에 **git 스탬프**: 커밋 해시 · 워크트리 경로 · 브랜치 · 기점 커밋.
  형식은 `/round-report` 스킬. (노트 안에 적는다)
- 실행한 것과 **실행하지 않은 것**을 명시한다(미집행을 침묵으로 넘기지 않는다).
- 사전 등록 판정이 있으면 **어느 갈래인지** 이름으로 답한다. 갈래에 없는
  결과면 "판정 불가"로 적고 정지한다.
- **노트 raw URL은 커밋 SHA로 고정한다** — 브랜치 이름을 쓰지 않는다.
  `https://raw.githubusercontent.com/banshk445/fit-simulator/<SHA>/.obsidian-log/...`
  브랜치 경로는 CDN 캐시가 **옛 판본을 200으로 반환**한다(실패 신호 없음).
  36→37회차 사이 전략 세션이 함정19 노트를 3회 읽고 3회 모두 옛 판본을 받아
  "회수 3회차 연속 미반영"이라는 거짓 지적을 냈다. (함정 20)
  부수 효과: 전략 세션이 판정한 **그 판본**이 URL에 기록된다.
- **보고 맨 끝에 raw 링크를 전량 붙인다** — 회차 노트 1개만이 아니라 그 회차가
  **만들거나 고친 `.obsidian-log/` 파일 전부 + `docs/metrics-log.md`**. 신규/수정을
  갈라 적고 파일마다 한 줄 요지를 단다. 전략 세션이 볼트를 열지 않고 링크만으로
  회차 전량에 닿을 수 있어야 한다(58회차 요청). 목록은 손으로 적지 말고 뜬다:
  ```
  git -c core.quotepath=false show --name-only --format= <노트커밋> <metrics커밋> \
    | grep -e '^\.obsidian-log' -e '^docs/' | sort -u
  ```
  경로에 한글이 있으므로 **퍼센트 인코딩**한다(캐시·프록시 경유 시 안전).

## 회차 보고의 마지막 줄 — 「다음 차례」

사용자가 전략 세션과 CC 사이를 오가며 복붙하므로, **지금이 누구 차례인지**가 보고에서
즉시 보여야 한다. 회차 보고의 **맨 마지막 줄**에 아래 넷 중 하나를 적는다.

```
━━━ 👉 다음: 전략 세션에 이 보고를 전달하세요 (CC 대기) ━━━
    판을 마쳤고 판정·다음 프롬프트가 필요할 때. **기본값**이다

━━━ 👉 다음: 사용자 판단이 필요합니다 — [무엇을 고를지 한 줄] ━━━
    갈림이 회차 정의역 밖이라 사람이 골라야 할 때. 선택지 나열은 본문에

━━━ 👉 다음: 사용자 조치가 필요합니다 — [무엇을 할지 한 줄] ━━━
    권한·탭 전환·전원·파일 업로드 등 사람만 할 수 있는 일이 있을 때

━━━ 👉 다음: CC가 이어서 진행합니다 — [무엇을 할지 한 줄] ━━━
    같은 판 안에서 CC가 계속할 때(장시간 실행 대기 등).
    **사용자가 아무것도 안 해도 된다**는 뜻이므로 남발하지 않는다
```

- **반드시 맨 마지막 줄**에 둔다. 그 뒤에 아무것도 적지 않는다
- **하나만** 적는다. 둘 이상 해당하면 «사용자가 먼저 해야 하는 것»을 적는다
- 캡처를 전략 세션이 봐야 하면 「전달」이 아니라 **「캡처 N장을 업로드」**로 적는다 —
  전략 세션은 raw URL로 이미지를 못 읽는다(v3 누적 사실)
- 정지·미착수·판정 불가로 끝난 판도 예외 없다. 그 경우도 「전략 세션에 전달」이 기본이다

**대응 규약(전략 세션 쪽)**: 전략 세션 응답에 **코드블록이 있으면 CC로 복사**,
**판정·질문만이면 사용자 차례**다. 양쪽이 짝을 이룬다.

## 사전 반증 (상설 — 37회차 등재)
**전략 세션이 쓴 기전·계기 설계는 예외 없이 여기를 거친다.** 계기를 돌리기 전에
`strategist`에 넘겨 다음만 답하게 한다:
- 가설이 전제하는 **코드 사실**이 실제로 그런가 — 상수 값 · 배선 · 호출 순서 ·
  집합 정의(무엇을 순회하는가) · **어느 시점의 상태인가**
- 전제 중 거짓이 있으면 어느 것이고 가설이 어떻게 바뀌는가
- **제안 금지. 반증만.** 다음 수 판단은 전략 세션이 한다

**strategist가 낸 필수 조건은 계기 구현 체크리스트로 승격한다.** 계기를 구현하기
전에 항목별 충족을 확인하고, **보고 헤더에 체크 결과를 적는다.** 근거: 38회차 §5 —
조건 3건이 나왔으나 구현에 실리지 않아 산출물 2건이 무효가 됐다. 39회차는 실어서
3건 전부 충족했다.

**반증에 걸리면 계기를 돌리지 말고 정지·보고한다**(회차를 아낀다).
근거: 35회차 반경 분해는 링 길이를 원리적으로 못 재는 설계였고, 36회차는 계기
정의역(링 60 vs 62엣지)과 시점(핀은 f=110에 해제)이 둘 다 틀린 채 등록됐다.
**계기 정의역이 3회차 연속 틀렸다** — 지시자도 함정 13의 대상이다.

## 계기
- **대역 요약에 중앙값 단독 금지** — 중앙값 · p99 · maxAt(어느 정점인가) ·
  부호별 총량(압축 총 cm / 신장 총 cm) 4종 병기. (함정 18)
- 계기를 채택할 때 **축 · 시점 · 정규화 · 집합** 4항목을 식에서 재도출한다.
  (함정 13 규범 6 — 36회차에 "집합" 추가)
- 계기가 쓰는 rest는 **제약이 실제로 순회하는 집합에서 직접 뜬다**.
  별도 배열을 만들거나 합을 다시 세우지 않는다. (함정 12)
- **문서에 적힌 숫자를 코드 확인 없이 상수로 승격하지 않는다** — 이름과
  실제 측정 대상이 어긋난 계기가 반복해서 나왔다. (함정 13)
- 계기가 산출 불가하면 **추정치로 채우지 말고 불가로 보고**한다.

## 반납·전제
- 어떤 층위를 반납할 때 **진입점 단위로 grep 확인**한다. 스위치 하나를 끈
  것이 전 경로를 끈 것이 아니다(`anchors: () => []`는 소프트 경로만 껐고
  `setAnchorHard` → `sim.pin`은 살아 있었다). (함정 17)
- 반납할 때 **그 층위를 근거로 삼았던 과거 결정 목록을 함께 회수**해
  각각 유효/무효를 표기한다.

## 문턱
- **결과에 맞춰 문턱·값을 조정하지 않는다.** (함정 14)
- 문턱은 **달성이 실증된 수준에서 도출**한다 — 같은 기계가 이미 낸 값
  (예: 뒤판 신장총 0.0 · 최대 1.000)이 근거이고, 그 도출을 주석에 명기한다.
- 화면·수치에 맞춰 rest나 옷 기하를 바꾸는 것은 결함 수정이 아니다.

## 화면
- **뷰포트만 크롭**(터미널·데스크톱 없음). 5뷰 고정 프리셋
  `front / back / neck / cuff / hem` — 27회차 프리셋을 유지한다
  (`FitCanvas.tsx`의 `CAPTURE_VIEWS`, 진입 `npm run capture front,back,neck,cuff,hem`).
- 프리셋을 바꿔야 하면 **바꾼 회차를 노트에 남긴다** — 구도가 달라지면
  회차 간 대조가 끊긴다(cuff target 오조준으로 25·26회차 V3가 무효였다).
- 시각적 "정상/고쳐짐"을 자체 선언하지 않는다. 객관 채널만 판정하고
  보기 판단은 사용자에게 넘긴다.

## 실행
- **목선 진단의 표준 기준선은 `RINGTOTAL=0`** — 26회차 총 길이 상한을
  교란 요인에서 뺀 상태이고, 25회차를 비트 재현하는 것이 확인돼 있다.
- 실행 첫 줄의 `코어=… 스무딩=…` 을 판정 전에 확인한다.

## 정지 조건 (기본형 — 회차 프롬프트가 덧붙일 수 있다)
- `clothPhysics` 수정이 필요해진 시점
- ABORT 2회
- 사전 등록 판정의 어느 갈래에도 해당하지 않는 결과

## 완료
- `.obsidian-log/` 회차 노트 · 백링크 · 과거 노트 정정 · 함정 등재 ·
  `00-현재상태.md` 갱신을 작성하고 **코드와 같은 커밋에 포함해 push**한다.
- **회차 등재 시 `.obsidian-log/전략세션-인수인계.md` 갱신 포함**(항목 2·3·5 누적 ·
  1·4·6 교체). 전략 세션이 컨텍스트로 끊겼을 때 이어받는 최소 집합이다.
- 측정 블록은 `docs/metrics-log.md`에 append하고 `metrics:` 커밋으로 분리한다.

---

# 드레이프 개선 자동 판정 루프

실험 단계마다 아래 루프를 **자체 판정으로 끝까지 돌린다**. 사용자에게
표를 복사해 옮기게 하지 말 것 — 판정과 기록은 이 저장소 안에서 완결된다.

```
구현 → fixture 측정 → 3지표 게이트 자체 판정
  ├ 통과   → 커밋 + docs/metrics-log.md 기록 + 푸시 + 다음 단계 자동 진행
  ├ 실패   → 자동 원복 + 실패 이유 기록(+푸시) + 정지 후 보고
  └ 애매   → 정지, 화면 확인만 요청(코드는 그대로 두고 커밋)
```

**3회 연속 실패 또는 원인 불명이면 즉시 정지**하고 "외부 전략 검토 필요"로
보고한다. 같은 층위에서 파라미터만 바꿔 재시도하는 걸 반복하지 말 것 —
A-③/C/B-1이 전부 "보정을 빼면 미끄러진다"로 수렴했는데 원인은 마찰 부재라는
한 층 아래에 있었다.

## v4 트랙 (Python + Taichi · v4-01 §1-④ 등재)

**v3 는 동결됐고 «정답지»다.** v4 는 `gpu/` 아래에 따로 산다 — `src/` 를 **읽기만** 한다.

```
 ★★★ **정본 엔진 = v4**(2026-09-01 · 전략 세션 v4-16 §4 **이식 완료 «선언»** · v4-17 §0-2 등재)
             **v4 = f64 · 병렬(색 분할 8/14색) · 셀프 충돌 포함 · 해소는 «직렬»(순서 재현)**.
             **v3 는 «동결·정답지·회귀 기준»** 이다 — 새 굽기는 v4 로 하고, v3 는 대조에만 쓴다.
             근거(v4-01~16 누적) — 커널 5종 층1 전량 통과 · 쌍 집합 41,567 완전 일치 ·
             넓이 8칸에서 게이트 8/8(`pass`·`fails` 일치 · 자기관통 교차 0) · v3 대비 **12.45배**.
 개발 기계   **에어 M4(Metal)** — 이 CC 세션이 도는 기계
 정본 기계   **2호기(CUDA · DESKTOP-OV72A46)** — 정본 굽기는 거기서 한다
 정밀도      **f64**(v4-15 §1-② 실측으로 f32 축 폐기 · 아래 「정본 정밀도」 줄) ·
             ~~float32~~(v4-01~13 이력 · 무삭제)
 정답지      **정점 대조 39칸**(blob 존재) + **판정 대조 69칸**(게이트 판정·실패 사유만 — blob 부재)
             분류 `index-merged-108.v3-91.json` · 제공 `v1-provide-35.v3-91.json`
 정본 파일   접두 **`v4-`**
 진입        에어 `gpu/.venv/bin/python -m pytest gpu/tests -q` · **2호기 `py -m pytest gpu/tests -q`**
 자산        정답지·덤프는 **git 밖**(`.gitignore`)이다 — 기계 간 이전은 **scp**(맥 → 2호기 단방향)
 정밀도 갈래 ★ **f64 참조 시험의 정본 기계 = 2호기(CUDA)뿐 · Metal 은 f32 전용**
             에어 Metal 은 `RuntimeError [spirv_ir_builder.cpp:299] Type f64 not supported` 로
             **f64 커널을 못 띄운다**(v4-04 §0 보충 실측 · **기계 제약이지 이식 결함 아님** —
             같은 시험이 2호기 CUDA 에서는 통과한다). ⟹ f64 참조 시험은 백엔드가 f64 를
             지원하지 않으면 **`pytest.skip`**(사유에 백엔드명·오류 원문 요약) — **통과로 위장 0**
```

**검증 기준은 «2층»이다**(v4-08 §0-2 교체 · 궤적 대조 **폐기**(v4-04) · **층2 폐기**(v4-08)):

```
 층1 1스텝 정확도 — 제약 커널을 **1스텝**만 적용해 f64 참조와 대조.
                  상한 = **K × ULP_f32(그 자리 값)** · **M 없다**(증폭이 일어날 시간이 없다).
                  K 는 **커널 코드에서 «센»** 연산 사슬 길이(`gpu/engine/ulp.py`).
 층3 관측량      — **핏 리포트 5행 mm · 게이트 판정**을 v3 정본과 대조. 제품이 읽는 값이다.
                  문턱은 **v3↔v3 재현성에서 «유도»**한다(v4-08 §1-①).
 ★ 층 번호는 **1·3 그대로 둔다**(회차 간 대조가 끊기지 않게 — 2 는 «빈 자리»로 남긴다).
 ★ 근거 — 함정 39(증폭계에서는 두 «옳은» 시뮬레이터도 궤적이 갈린다) · 함정 41 ·
   원리 「검증은 도착점과 관측량으로」
```

**폐기된 층2 (이력 · 원문 무삭제)** — 「같은 초기 상태에서 **수렴 판정까지** 돌려 **최종** 정점만
대조(중간 궤적 0) · 수렴 판정·문턱은 v3 코드에서 인용 · 상한은 실측 분포를 먼저 보고 유도」.

```
 폐기 사유(v4-07 §1-① 실측 · 전략 세션 v4-07 §4 판정) — **정답지 1칸도 강체 방향으로 평형이
 «유일하지 않다»**. 두 실행이 각각 창 순변위 **1.75e-11 / 2.36e-11** 까지 «진짜로» 서는데
 둘 «사이»의 차이는 **2.691147e-03 에 고정**되고 ε 네 자릿수·연장 400프레임 어디서도 안 줄어든다.
 ⟹ 「최종 정점 좌표」는 이 계에서 **well-posed 하지 않다** — 어떤 상한을 적어도 뜻이 없다.
 ★ **귀책 = 전략 세션**(층2 자체가 오설계 · 4판째) · CC 귀속 0.
 ★ **원리로 승격** — 검증 기준은 **계가 답할 수 있는 질문**이어야 한다. 못 답하면 정교화가 아니라 **폐기**다.
```

**규약은 v3 와 «같다»** — 사전 등재 · 갈래 · 복원(diff 0 증명) · 귀책 등재 · 문턱 인용(손 상수 0) ·
「결과에 맞춰 문턱을 옮기지 않는다」(함정 14) · 「제약과 탐색 경로를 분리한다」(함정 37).

```
 ★ **「같은 옷」 정의 — «확정»**(2026-09-01 · 전략 세션 v4-15 §4 판정 · v4-16 §0-2 등재).
   한 칸이 「같은 옷」이려면 **세 조건이 전부** 서야 한다:
     ① **대역 이름 5/5**(`V3ProductV1.tsx:52-59` `phaseOf` · 경계 `consts.ts:SEP`)
     ② **게이트 `pass` 일치 «그리고» `fails` 집합 일치**(`s4Gate.ts:142-148`)
     ③ **표시 리포트 5행의 «중앙 차» ≤ 0.05 mm** — 단, **밴드 정점 집합이 «같은» 행에 한해서**다
      (2026-09-01 · 전략 세션 v4-16 §4 **최종** · v4-17 §0-2 등재). 밴드 집합이 다른 행은
      «중앙»이 같은 표본을 재지 않으므로 그 행에는 이 조건을 걸지 않는다.
      ★ 이 정의로 v4-15 의 **8칸이 8/8 성립**한다(`c100-h185-s40_L` 가슴은 밴드 269↔270 이라 ③ 밖).
      ★ **채널 조정은 여기서 끝이다** — 내린 것은 전부 **well-posed 하지 않은 양**이고
        남은 채널은 **소비자가 보는 것**뿐이다(대역 이름 · 표시 mm · 게이트 한 비트).
   ★ ③의 0.05 는 **1자리 표시의 반올림 폭에서 «유도»**했다(`V3ProductV1.tsx:348` `toFixed(1)` ⟹
     반올림 폭 0.1 mm · 그 절반) — **손 상수 0**.
   ★ **「표시 1자리 «일치»」는 채널에서 «내렸다»**(구 정의 · 아래 이력) — v4-15 실측에서
     갈린 6행 중 **5행이 0.05 mm 미만**(0.0083~0.0444)에서 경계를 넘었다. 경계를 넘느냐는
     **계가 답할 수 있는 질문이 아니다**(함정 41 · 폐기된 층2 와 같은 형태) ⟹ 정교화가 아니라 폐기.
   ★ **폐기된 구 정의(이력 · 원문 무삭제)** — 「표시 리포트 5행이 **반올림 후 일치**(표시 1자리) +
     대역 소속 일치 + 게이트 판정 일치」(전략 세션 v4-09 §4 · v4-10 §1-② 가 코드에서 인용해 걸었다).
   ★ 이 정의로 v4-15 의 8칸은 **7칸 성립 · 1칸 미달**(`c100-h185-s40_L` 가슴 **+0.1782 mm**).
 ★ **v4 정본 정밀도 = `f64` «확정»**(2026-09-01 · 전략 세션 v4-15 §4 · **f32 축 폐기**).
   근거 — 셀프 충돌이 들어와 정의역이 완성된 뒤에도 f32 는 **정착에 못 든다**
   (헤더 프레임 완주 후 문턱의 **81~96배** · v3 정본과 중앙 4.46/15.52 mm) ·
   **속도 이득은 3.6%뿐**(f32 2.260 ↔ f64 2.345 s/프레임 · v4-15 §1-②).
   ⟹ 위 「정밀도 float32」 줄은 **v4-01~13 의 이력**이고, **지금 정본은 f64** 다.
 ★ Taichi 함정 2건(v4-01 실측):
   ① `ti.init(arch=…)` 는 요청 arch 가 없어도 **예외를 안 던지고 조용히 폴백**한다 ⟹
      반드시 `ti.lang.impl.current_cfg().arch` 로 **확인**한다(#121 계열).
   ② `from __future__ import annotations` 를 켜면 커널 인자 주석이 «문자열»이 돼
      `TaichiSyntaxError` 가 난다 — v4 모듈에서는 쓰지 않는다.
```

### v4 회차 보고 형식 — **본문 전량 · 링크는 부록** (2026-09-01 등재 · v4-12 보고부터 적용)

**전략 세션은 GitHub raw URL 을 «열지 못한다»(도구 제약).** 링크만 있는 보고는 **판정 불가**다.
⟹ **v4 회차 보고는 위 「보고」 절의 「채팅 출력은 raw 링크 전량뿐」을 «대체»한다.**
링크 «대신» 아래를 본문에 그대로 담고, **링크는 본문 뒤에 부록으로** 붙인다.

담을 것 — **판정에 쓰이는 값 전량**:

```
 1. **갈래 이름**(A/B/C/D…)과 그 갈래로 간 **사유 한 줄**.
 2. **§1 항목마다** — 사전 등재 갈래 중 어느 것((가)/(나)/(다)…)인지 **+ 그 판정을 만든 «수» 전량**.
    **표는 표 그대로** 옮긴다.
 3. **사전 등재 기대 ↔ 실측 대조** — 기대값 · 실측값 · **비**.
 4. **기각·정정된 것**(전략 세션 판정 포함)과 **그 근거 수**.
 5. **자기신고** — 절차 이탈 · 한정 · 미집행 · **귀책**(전략 세션 / CC).
 6. **다음 판 설계에 필요한 새 사실.**
```

형식:

```
 · **평문** · 본문 안에 **코드 펜스 없음** · **표는 표 그대로** ·
 · **「노트에 있습니다」로 대체 금지** — 값을 본문에 «적는다».
 · **수는 반올림 없이** 노트에 인쇄된 자릿수 전량.
 · **길어도 된다** — 길이보다 **값 누락**이 문제다.
 · 회차 노트·metrics 는 그대로 쓴다(정본). 본문은 그 «사본»이지 요약이 아니다.
 · 맨 마지막 줄의 「다음 차례」는 그대로 유지한다(부록 링크 «뒤»).
 · ★ **모든 수는 노트에서 «읽어» 옮긴다 — 기억·역산 금지**(함정 44 · v4-13 §0-2 등재).
   보고에 적는 수는 회차 노트·metrics 에 «문자열로» 있어야 한다. 볼트 어디에도 없는 수가
   보고에 있으면 그것은 계산이지 측정이 아니다(예: 12.690 ÷ 7.92 = 1.602).
 · ★ **갈래 문언에 «단위»를 명시한다**(함정 43 · v4-13 §0-2 등재) — 「1스텝」·「1회」·「한 번」을
   쓰는 자리에 **프레임 / 서브스텝 / 투영** 중 무엇인지와 그 «수»를 같이 적는다
   (이 계는 1프레임 = 229 서브스텝 = 10,282,680 투영).
```

## 자산 반출 = 수신기 (v3-86 §1-⑤ 등재)

굽기 산출물(몸 blob 등)은 **브라우저 다운로드로 받지 않는다** — 크롬이 한 페이지의 자동
다운로드를 2건째부터 막고, 이후 전면 차단한다(v3-85 실측). 대신 **수신기**를 띄운다:

```
npx tsx scripts/v3Receiver.ts --out public/v3diag/v3-77 --n 1   # sha 대조 후에만 저장 · 완료 시 자동 종료
```

페이지는 `POST http://127.0.0.1:5199/put?name=<파일명>&sha256=<64자>` 로 바이트를 보낸다.
**sha 가 어긋나면 수신기가 저장하지 않고 409 를 돌려준다**(조용한 저장 금지).

## 측정 명령

타입 검사는 **`npx tsc -b`**로 한다. `npx tsc --noEmit`은 루트 tsconfig가
`files: []` + references라 **아무것도 검사하지 않는다**(scripts/ 미임포트를
놓쳐 런타임에서 두 번 터졌다, 2026-07-30).

```
SECONDS=10 FRICTION=1 FIXTURE=scripts/fixtures/collision-fixture.json npm run sweep:pattern
PATTERNCORE=1 npm run check:pattern      # v2 Stage 2a 게이트(패턴·삼각화·배치)
PATTERNCORE=1 npm run spike:dressing     # v2 Stage 2a-thin 착장 상태기계 스파이크
PATTERNCORE=1 npm run dress:pattern      # v2 Stage 2b 착장(DIAG=1 진단 · EXPORT_META=1 옷 해시 갱신)
```

코어·스무딩 기본값은 제품 코드에서 도출한다(`DEFAULT_NEW_CORE`,
`defaultSmoothing` — 규범 5). 기본 실행 = **신 코어 + 스무딩 off**이고,
실행 첫 줄에 `코어=… 스무딩=…(도출|env)`로 찍힌다 — **판정 전에 그 줄을
확인할 것**(계기가 기대와 다르면 숫자가 아무리 좋아도 무의미하다).
대조는 `NEWCORE=0`(구 코어) / `SMOOTHING=1`(옛 계기 재현).
기준선은 600프레임(`SECONDS=10`)이다 — 프레임 수가 다르면 비교 불가.

스위치와 하네스 2종의 차이는 `docs/metrics-log.md` 상단 규약을 따른다.
**12콤보 하네스와 fixture 하네스의 절대값을 서로 비교하지 말 것.**
매 단계 전후는 **같은 하네스·같은 fixture**로 재고, 이전 단계 값을
기준선으로 쓴다(직전 로그 블록의 "이후" 열).

## 게이트 판정 기준

세 지표를 함께 본다. 하나만 보고 판정하지 말 것 — 실패마다 시그니처가
다르다(A-③=버킷, C=총비율, B-1=잔물결).

### 기준선 (문턱의 도출 근거)

아래 지표 문턱은 전부 이 기준선 대비 **마진**이다. 절대 문턱은 폐기했다 —
옛 절대값(ripple 4.5mm 등)은 스무딩 on 계기의 실패값에서 역산한 것이라
교정 계기의 숫자와 비교할 수 없다(함정 12).

```
기준선: cov 6.4% / ripple mean 6.43mm / columnRipple mean 14.99mm·부호반전 0.411
        strain 3.38 / bowtie 어깨 2 / 정착 65프레임 / 교차 0
계기:   신 코어 · 스무딩 off(도출) · 600프레임 · fixture 9e8b2bf13925
출처:   7cc3885 (docs/metrics-log.md 2026-07-30 09:50 블록)
```

**당분간 게이트에 쓰는 채널은 cov(총비율·버킷) · strain · 교차 · bowtie ·
check:demo/weld/sleeve/seambridge 뿐이다.** ripple과 columnRipple은
**계기-stale·판정 제외**(아래).

**즉시 실패(하드)** — 하나라도 걸리면 원복:

- 발산(NaN/Infinity) 발생
- `maxStrain` 폭주 — 기준선(3.38) 대비 +25% 초과(≈4.2 이상)
- `maxSeamGapCm` 회귀(신 코어는 0.00이어야 함)
- 앞뒤판 **실제 교차** > 0 (반평면 위반은 실패 아님 — 옆구리 폴드다)
- `npm run check:weld` / `check:sleeve` / `check:seambridge` / `check:demo` 실패
- 구 코어 12콤보 비트 동일성 깨짐(플래그 격리 실패)

**지표 실패** (기준선 = 위 블록):

- ~~**jitter mean** +25% / 부호반전 +0.1 — 지그재그 판정의 주 채널~~
  → **폐기(2026-07-30). `jitter`는 `columnRipple`로 개명되고 판정에서
  빠졌다.** 이 지표는 시간 떨림이 아니라 **열 방향 공간 4차차분을 최종
  프레임 1장에서** 낸 값이고(식에 시간 축·dt 없음), 정규화가 없어
  파장 대역을 구분하지 못한다(순수 교대 이득 16 / 파장 4열 4 / 6열 1.0 —
  같은 14.99를 0.94mm 교대와 3.7mm 매끈한 폴드가 똑같이 낸다).
  **ripple(2차차분=곡률계)과 같은 계열이며 차수만 다르다** — 곡률계를
  버린 근거가 이 채널에도 그대로 적용된다.
  **둘 다 계기-stale·판정 제외**: 참고로 찍되 통과/실패를 가르지 말 것.
  주 채널 복구는 파장 대역이 분리된 지표를 **채택 시점에 식부터 재도출**한
  뒤에만(README 함정 13).
- ~~ripple mean +25%~~ → 위와 같이 판정 제외(참고 출력만).
- coverage 총비율이 기준선(6.4%) 대비 +2pp 초과 악화
- 임의 버킷의 노출률이 +15pp 초과 악화(A-③의 bot-back은 +59pp였다)
- bowtie(접힌 쿼드) 어깨가 **2 초과**(기준선 2에서 동결 — 화면 승인 상태에
  이미 존재하는 값이라 0을 요구하지 않는다. `docs/pattern-redesign.md`
  알려진 한계 참고)
- 정착이 **80프레임 초과**(기준선 65프레임 + 여유 15)

**통과**: 하드 실패 없음 + 위 문턱 안에서 불변 또는 개선.

**애매(정지·화면 확인 요청)**: 지표가 서로 반대 방향
(예: coverage 개선 + ripple 악화). 제거 ①이 이 경우였고 화면 확인으로
통과했다. 이때는 **원복하지 말고** 커밋해 둔 뒤 무엇을 볼지 지정해 요청한다.

**화면 확인 필수(통과여도)**: coverage 총비율 또는 드레이프(면각평균·
주름RMS)가 10% 이상 움직인 단계. 지표가 전부 통과해도 화면이 실패할 수
있다 — A-③은 전 지표 개선인데 어깨/등 노출 참사였고, C는 브라우저급
재현에서도 검출되지 않았다. 거리 지표는 커버리지 손실을 원리적으로 못 잰다.

## 기록

측정 직후, 보고보다 **먼저** `docs/metrics-log.md` 끝에 블록을 append하고
`metrics: <단계명>` 으로 커밋 + 푸시한다. 실패·원복도 블록을 남긴다
(실패 기록이 재시도 방지의 핵심). 형식은 그 파일의 기존 블록을 따른다.
코드 커밋과 측정 기록 커밋은 분리한다.

## 남은 로드맵 — v2 (규범 문서: `docs/v2-design.md`)

v2 = 패턴 기반 옷(폐곡 패널 + 봉제 착장) + 단방향 접촉. 게이트·중단 조건·
롤백 지점의 정본은 v2-design §6 표다. 아래는 순서와 착수 조건만.

| # | 단계 | 내용 | 근거 절 |
|---|---|---|---|
| T1 | ~~**Stage 1a**~~ **동결(2026-07-30)** | 밀어내기 SDF 교체 — 3연속 규칙 3회차 소진. **기본은 BVH 유지.** 상세는 아래 "Stage 1a 동결" 절 | §6, §8-1 |
| T1' | **Stage 0**(T1과 병행) | SDF 이중 버퍼 + 전용 굽기 워커. 마감 = Stage 1a 제품 기본화 전 | §5 |
| T2 | ~~**Stage 1b**~~ **완료(2026-07-30)** | 관통-only = **붕괴**(cov 6.4→54.2%, 앞판 mid/bot +70~84pp). §1.2.1 2행 → **2b 착장은 흡착 모드**, 밀어내기 전환은 2c 유지 | §1.2 |
| T3 | ~~**Stage 2a-thin**~~ **통과(2026-07-30)** | 상태기계 S0→S1→S2→S3→DONE, 958프레임·RETRY 0·발산 0. `patternCore` 기본 off. 진입 `PATTERNCORE=1 npm run spike:dressing` | §1.3, §4 |
| T4 | ~~**Stage 2a**~~ **구현 완료·1건 판정 대기(2026-07-30)** | 패턴 제도·삼각화·시접 테이블·정적 배치. 자기검사 13/13·시접 15/15·배치 관통 0·자기충돌 오발화 0 통과. **크기장 이탈 p99 31.0~33.2%가 설계 문턱 30% 미달** — 정지 조건("삼각화 품질이 대역 내 달성 불가") 등재, 아래 절 참고. 진입 `PATTERNCORE=1 npm run check:pattern` | §3 |
| T5 | ~~**Stage 2b**~~ **1차 실행 ABORT(2026-07-30) — 정지·판정 대기** | 옷이 60cm 흘러내려 골반에 뭉친다(어깨시접 146.9→87.3cm). V1~V5 5/5 실패. 실패 귀속 = **접촉·지지 구조**(메시 무죄, 조건부). 아래 절 참고. 진입 `PATTERNCORE=1 npm run dress:pattern` | §4, §9-4 |
| T6 | **Stage 2c** | 무핀 정착 + 흡착→밀어내기 최종 전환 + 체형 슬라이더 자동화 | §6 롤백 사슬 |
| T7 | **Stage 3** | 이미지→템플릿 분류 + 패널별 UV 프린트 배치 | §7 |

**Stage 2b 1차 ABORT (2026-07-30) — 유지력 층위의 설계 판정 대기**

정식 패널 첫 착장에서 **옷이 흘러내렸다**. 어깨 이음선 y 146.9 → 87.3cm,
소매 정점 561개가 골반 높이에서 관통, cov 몸통 65.2% / covShoulder 63.7%,
미정착(Δ20 13.9mm, 720프레임 예산 내 미도달). 화면 4뷰 = 상체 전면 노출.

**기전**: 기하 조건(목선 33.30cm < 어깨 통과 107.00cm)은 2a에서 성립했는데도
넘어갔다 — **PBD가 늘어난다**(목선 링 최대 5.67배). v1 M2-6이 등재한 반례
기전("링이 원주를 유지한 채 미끄러진다")보다 한 단계 나쁘다. 낙하는 앵커가
살아 있는 f=60에 이미 −18cm로 시작하므로 앵커 강도 문제도 아니다.

**§8-3의 무핀 가설이 첫 실측에서 반례를 냈다.** 다음 행동은 파라미터
재시도가 아니라 설계 판정이다 — §6 2c 롤백 사슬(1차 = 반복 안 연속 앵커
상시, 2차 = 어깨선 하드 핀)을 **2b로 끌어올릴지**, 아니면 유지력을 다른
층에서 만들지(원주 제약 재도입 / 목선 링 비신축 제약 / 어깨 대역 마찰 강화).
수치 전량은 docs/metrics-log.md 2026-07-30 T5 블록.

**계기 정정 예고 1건**: 하드 게이트의 `자기교차` 채널은 실제로 "비인접 정점
쌍의 문턱 위반 수"를 세고 삼각형 교차 판정이 아니다(함정 13 계열). 뭉친
더미의 폴드 접촉이 정상적으로 잡히므로 다음 실행 전에 엣지-삼각형 교차
판정으로 바꾸거나 채널명을 정정하고 게이트에서 내릴 것.

**Stage 2a 미달 1건 (2026-07-30) — §3.2 처분으로 종결(기록 채널 강등)**

크기장 이탈 p99가 앞판 31.0% / 뒤판 31.5% / 소매 33.2%로 §3.2 문턱(30%)을
1~3pp 넘는다. 나머지 품질 채널은 전부 통과(최소각 25.0~28.6° ≥ 25 · 비매니폴드 0 ·
중복 0 · 경계 이탈 0mm · 경계 엣지 누락 0). 정련 상수 4조합 스윕 실측 결과
**엣지 균일도 ↔ 최소각 ↔ 정점 예산**이 서로를 잡아먹는다(스무딩을 늘리면
p99 29.8%까지 내려가지만 최소각이 17.6°로 붕괴, 외심반경비를 조이면 정점이
§1.4.1 추정을 40% 넘는다). 선택지: (a) p99 문턱 35% 재도출 = 현 상태 채택
(b) CVT/ODT 최적화 또는 메싱 라이브러리 도입 (c) 정점 예산 6,800까지 개방.
수치 전량은 docs/metrics-log.md 2026-07-30 T4 블록.

**patternCore 경로의 리뷰 CC 검토 — 2c 채택(기본 on 전환) 직전에 일괄**

`patternCore` 스위치 뒤의 증분은 그때까지 누적만 하고 개별 리뷰를 걸지
않는다. 이유: 스위치 off인 동안 제품 경로에 한 줄도 관여하지 않고(구 경로
비트 동일이 하드 게이트), 2b/2c에서 구조가 계속 뒤집히는 구간이라 중간
리뷰가 곧 stale이 된다. **기본 on 전환 직전에 누적분 전체를 한 번에** 본다.

**Stage 1a 동결 (2026-07-30) — T2(1b)는 BVH 기본으로 그대로 진행 가능**

3회차 결론: 밀어내기 SDF 필드의 **부호가 원리적으로 정의되지 않는다.** 마네킹
메시는 수밀(열린 엣지 0)인데 `splitFrontBack`이 몸통을 절반 시트로 자르고
(열린 엣지 597) `excludeArms`가 반경 9cm 구멍 두 개를 뚫는다(189). 구멍 반경이
복셀(6mm)보다 훨씬 크므로 몸 안에서 구멍을 통해 밖으로 나가는 경로가 표면에서
한 복셀 이상 떨어진 채 존재한다 — 안과 밖이 연결되어 있어 부호 규칙은
무엇이든 근사일 수밖에 없고, 확산성 부호 모순 2.95%가 그 증상이다.
회차별 시그니처는 metrics-log 3블록 참고(1회차 radial 부호 / 2회차 top-front
잔여 = 부호 무관 / 3회차 전제 붕괴).

**은행(재사용 자산 — 동결 대상 아님)**: 이번 3회차에서 만든 계기·기계는 전부
다른 소비자가 있다.
- `bodySkeleton.ts` 골격 도출 + `makeSkeletonSignedSampler` — 부호 기준.
  오라클 실측으로 radial보다 정확함이 확인됐다(어깨 대역 오분류 36.3%→1.8%,
  몸통 9.6%→4.2%). coverage 어깨 대역이 이미 이 기준을 쓴다.
- `SIGNMAP=1` 오분류 측정기(공 오라클, 레이 패리티 없음) + 변이 역검증.
- `XFER=1` 전달 오차 계기(∇d vs BVH 면법선 + 와인딩 무관 채널).
- `CUTMAP=1` 절단 경계·A↔B 사슬 계기, `SIGNFIX_DIAG=1` 수밀성 검사.
- `repairSdfSign`(영역 성장 수리) — 수밀 필드에서만 유효. 기본 off(SIGNFIX=1).

**재개 조건**(전부 충족해야 재개): ① Stage 2c 밀어내기 전환 시점 ②
per-part 팔 필드(전완 굽음 포함 — 직선 팔 축이 전완 중심축이 아니라는 실측
등재분 해소) ③ **어깨선이 능선에 얹힌 옷**. ③이 진짜 조건이다: 기준선의
top-front 커버리지가 BVH 면 법선의 기하 편향 29°에 의존하고 있었고(함정 14
초안), 그 의존의 뿌리는 v1의 목~어깨 텐트 구조다. 옷을 교체하지 않는 한
"더 옳은 방향"이 게이트에서 지는 구조가 유지된다.

계기 선행(측정이 설계보다 먼저, §9): ① coverage 어깨/삼각근 대역 확장
② 착장 상태기계를 garmentFrame 공유 세션에 두어 하네스 공유 ③ fixture에
패턴 메타 해시 포함 ④ v2 기준선 수립 절차.

**M3(균등 해상도 상향)는 폐기** — Stage 2a의 경계 대역 grading이 같은
편익(목선·암홀 화질)을 파티클 총량 증가 없이 준다(§1.4). SDF 복셀 규칙은
유지: **복셀 ≤ 최소 접촉 대역 엣지 길이**. **XPBD 전환은 기각**, 시접 제약
국소 도입 조건부 재개 조항만(§1.5).

코드는 전부 v2 플래그(`patternCore` — `newCore`와 독립) 뒤에서만. 구 경로
비트 동일성은 기존 하드 게이트에 추가한다.

---

## v1 로드맵 (종결) — 아래는 v2 이전 계획이며 더 이상 착수 대상이 아니다

1번(M2-3 SDF 교체)만 v2 T1로 승계됐다(재시도 조건 복셀≤1cm 충족).
2~5번은 v2에서 옷 구조가 교체되므로 대상 코드 자체가 소멸한다.
6번(M3)은 폐기. 실패 이력은 재시도 방지 근거로 보존한다.

1. **M2-3 밀어내기 SDF 교체** — `bvhFromArrays` 리졸버를 SDF 기반으로.
   1단계는 기존 "탐지 반경 안이면 항상 margin 거리로 흡착" 의미를 그대로
   유지할 것(관통 시에만 밀어내기로 바꾸면 변화가 두 개 겹친다).
   목적은 잔물결 발생원(BVH 면 법선의 삼각형 단위 튐) 제거.
2. **② 재시도(스무딩 제거)** — M2-3의 전제가 성립한 뒤에만. blend
   0.5 → 0.3 → 0.15 → off 순으로 올려가며 게이트.
3. **③ 소프트풀 축소** — `applyArmSoftPull`/`applySleeveArmPull`.
   C단계가 같은 시도로 실패했으므로 마찰이 하중을 받는지 먼저 확인.
   커프 처짐(`cuffDroopCm`)과 긴팔 58cm 망토화를 함께 감시.
4. **④ order/symmetry 축소** — `preserveColumnOrder`/`preserveRowOrder`를
   row0~5로 좁히고, `enforceLeftRightSymmetry` blend를 낮춘다. order는
   천의 접힘 자체를 금지하는 제약이라 마지막에 손댄다.
5. **핀 전환** — `pinCorners` 하드 핀 → 칼라 소프트 앵커 + 마찰 하중지지.
   3단계(핀 유지 → 약화 → 해제)로 나눠 각 단계 게이트.
6. **M3 해상도 — 보류(2026-07-29 결정)**. 근거는 비용 산정: 66×42는
   파티클 2.25×, 충돌 샘플링 4.5×로 물리 50~113ms/프레임이 되고, 더
   결정적으로 `SDF_VOXEL` 20mm가 이미 천 열간격 12.8mm보다 성긴데
   66열이면 8.5mm가 되어 M2-3 실패 원인(복셀이 천보다 성김)이 그대로
   재발한다. 복셀을 8mm로 낮추면 셀 15.6× → 굽기 336ms가 약 5.3초.
   **대체로 렌더 전용 세분화를 채택했다**(E: 몸판 Catmull-Rom 2단계,
   경계는 선형 유지). 물리는 44×28 그대로다.
   **재개의 선행 조건은 프레임 비용이 아니라 SDF 굽기 주기다.**
   SDF는 몸이 바뀔 때마다 다시 굽는다 — 마네킹이 슬라이더로 실시간
   변하는 제품이므로 굽기가 치수 변경 1회당 비용이다. 복셀 8mm면
   **한 번 움직일 때마다 약 5.3초**라 실사용이 불가능하다. 따라서
   재개 조건은 다음 중 하나가 성립한 뒤다:
     (a) 증분/narrow-band 굽기 — 몸 표면 근방 셀만 갱신해 굽기 비용을
         해상도에 비례하지 않게 만든다.
     (b) 굽기를 비동기로 돌리고 완료 전까지 기존 SDF로 버틴다.
   **정정(프레임 비용)**: 프레임당 16.7ms는 이 제품의 제약이 아니다.
   물리는 워커에서 돌고 렌더는 도착한 프레임을 그리므로 초과해도 화면이
   끊기지 않는다 — 느려지는 건 **정착 시간**이다. 정착은 104프레임이라
   현재 2.6~5.2초, M3(2.25×)면 6~12초로 감당 못 할 값이 아니다.
   또한 물리 ms는 같은 코드·같은 fixture에서 25~50ms로 2배 흔들리므로
   **단일 측정으로 예산을 판정하지 말 것**.

각 단계는 신 코어 플래그(`newCore`) 뒤에서만 바꿔 구 코어 비트 동일성을
유지한다. 미해결 백로그는 `docs/metrics-log.md`의 M1 블록 참고
(삼각근 능선 노출 = 소매산 volume 부재, 구 플랩 열이 물리로만 남아 있음,
maxStrain 3.35~3.38이 limiter 상한 1.2의 약 2.8배).
