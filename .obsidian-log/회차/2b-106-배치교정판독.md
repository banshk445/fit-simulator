---
tags: [회차, stage2b, 몸통폭, 배치교정, 코드판독]
회차: 106
상태: **본문 확정** · §2 = 사전등록 **«갈래 A 및 C»**(갈래 이름으로 답함 · 회차 문언이 「A «또는» C」 조합을 **미리 허용**) · §1 판독 **7항 완료**(`correctPlacementPenetration` — 정점별 독립 변위 · **변위 상한 없음**) · §3 집행 = **기준선 발화 0정점 / 5244**(`place` 훅 **호출 0**) ⟹ **구조적으로는 상계를 깰 수 있으나 기준선에서는 발화하지 않는다** · §0 ㄴ = 함정32 폭 판본 후보 **«기각» 선언**(전략 세션) · 계기 신설(배치 교정 변위 · 인쇄 전용) · 캡처 **시도 0**(사용자 조치 대기 2건) · 함정 자체 판정 **2건 · 신설 0** · 기준선 **A 유지 · A′ 미수립** · 물리 0줄 · **e 변경 0 · e=3cm 실행 0 · 값 스윕 0 · 처방 0 · 기전 귀속 0 · 새 문턱 0 · 폭 축 종결 등재 0**
층위: 코드 판독 · 배치 교정
---

# 106회차 — `correctPlacementPenetration` 판독 (갈래 A 및 C · 기준선 발화 0)

> **git 스탬프**: 기점 `v2-2b-78-baseline`(9fbd66c) · 브랜치 `v2-stage2a` ·
> 워크트리 `/Users/banshk/projects/fit-simulator` · 착수 시 HEAD `7a57e2a` ·
> 직전 노트 `35a3d4a` · 105 계기 `8036dfd` · 105 metrics `5a2c7bb` ·
> **106 계기 `aacf487`** · **106 metrics `adfb510`** ·
> 노트 커밋 = 이 회차 등재 커밋(**작성 시점 미커밋** — 커밋 금지 지시) ·
> **기점 갱신 0 · 태그 0 · 기준선 A 유지**

본문의 숫자는 **전부 `docs/metrics-log.md` 106회차 블록**
(`## 2026-08-09 · 106회차 — correctPlacementPenetration 판독` · 커밋 `adfb510`)**에서 인용**한
것이고 **이 노트에서 새로 만든 측정 0 · 추정 0 · 처방 0 · 기전 귀속 0 · 새 문턱 0**이다.

코드 범위: 물리 **0줄** · `patternPlacement.ts` **0줄**(호출부 before/after 스냅샷만) ·
`patternGarment`/`garmentFrame`/`torsoCapsule`/`clothPhysics` **0줄** ·
**e 변경 0 · 2단계 재시도 0 · e=3cm 실행 0 · 값 스윕 0 · 처방 0건 · 새 문턱 0 ·
캡처 시도 0**(사용자 조치 대기 2건 미해소).

## 환경 스탬프 · 해시 3종 · 회귀

```
[dress] 해시: pattern 9f7ba80b3497 · fixture 9e8b2bf13925 — 재현하려면 위 env 줄을 그대로 쓴다(함정 26)
```

dress-state md5 `14e50b8919a63a7f9799220b86e508a9` **불변**(재생성 **20회차**) ·
`patternHash 9f7ba80b3497` · `fixture 9e8b2bf13925` 불변 · `npx tsc -b` 통과 ·
**전 채널 비트 동일** — cov **6.8%** · maxStrain **2.661** · maxSeamGap **11.14mm** ·
Δ20 **5.16mm** · 자기교차 **1889** · 관통 **50/5244** · DONE **f=260**.

---

## §0 전략 세션 처분 8건 — 105 §6 갈림 ㄱ~ㅈ (회차 프롬프트 전달분)

```
ㄱ    **폭 축 유지** — **종결 등재 0**. 1단계 성립분은 코드에 그대로 있다
ㄴ    **§0(2) 후보 = «기각» 선언**(전략 세션이 이번에 선언한다) —
      105가 낸 판별 조항의 답 «아니오»는 **갈래 성립 여부와 «독립적으로» 완결된 코드 사실 판정**이고,
      **갈래 미성립이 그 결론을 무효화하지 않는다** ⟹ 「104 2단계 throw가 함정32의 폭 판본이다」는
      **기각**된다. **함정 자체는 폐기하지 않는다** → §4-1 · [[../함정/함정32-소비처부분적용]]
ㄷ    **104 throw의 «자리»를 이 회차가 묻는다** — 105 §1-4가 남긴 유일한 예외
      `correctPlacementPenetration`(`patternPlacement.ts:169-185`)의 **코드 판독**이 정의역이다
ㄹ    **2단계 재질문은 «다음 회차» 몫** — 이 회차는 **e=3cm 실행 0**이고,
      다시 묻는 회차가 **e·문턱·갈래를 실행 «전»에** 확정한다
      ([[../원리/사전등록]] 규범 4·5 · [[../함정/함정14-기준선의존]] §경계선 조건 ②)
ㅁ    **갈래 열거의 완전성은 이 회차 등록문이 «이행»한다** — 갈래를 **구조(A·B)와
      판독 가능성(C·D)** 두 축으로 갈라 잔여 상태를 남기지 않고, **「A «또는» C」 조합을
      실행 «전»에 미리 허용**한다(105 §1의 «빈틈» 재발 방지) → §2 · §4-2
ㅂ·ㅅ  **유지** — `PINDRESS` off ↔ 하드 앵커의 처분 · `g.widthM` 죽은 입력 ·
      `PatternPreview` 슬라이더 무반영. **이 회차 착수 0**(배선 0 · 로그 수정 0 · UI 0)
ㅇ    **캡처 = 사용자 조치 대기 2건 유지**(104 Chrome 자동화 권한 · 105 화면 기록/화면 상태) —
      **이 회차는 촬영을 «시도하지 않았다»**
ㅈ    **화면 서열 = 96 §0-3분 유지**(갱신 0 · 자체 화면 판정 0)
```

---

## §1 판독 — `correctPlacementPenetration`(`patternPlacement.ts:149-186`) 7항

**코드 전량(발췌 · 원문)**

```ts
for (let i = 0; i < count; i++) {
  const x = positions[i*3], y = positions[i*3+1], z = positions[i*3+2];
  if (!inside(x, y, z)) continue;                                    // ① 발화 조건
  const c = mesh.closestPointUnsigned(x, y, z, detectionRadiusM);
  if (!c) continue;
  const dx = c.x - x, dy = c.y - y, dz = c.z - z;
  const l = Math.hypot(dx, dy, dz) || 1;
  let out = marginM;
  for (let k = 0; k < 8; k++) {
    const px = c.x + (dx/l)*out, py = c.y + (dy/l)*out, pz = c.z + (dz/l)*out;   // ② 변위식
    const clash = clashAt(i, px, py, pz);
    positions[i*3] = px; positions[i*3+1] = py; positions[i*3+2] = pz;
    if (!clash) break;
    out += minSepM;                                                   // ④ out만 증가
  }
  corrected++;
}
```

| # | 항목 | 결과 |
|---|---|---|
| **1** | 발화 조건 | **관통 검출 시에만**. `if (!inside(x,y,z)) continue` — `inside` = `insideParity`(레이 패리티 · 호출부 `dressPattern.ts:391`·`:2148`). 추가 필터 `closestPointUnsigned(..., SDF_FAR=300mm)`가 `null`이면 skip |
| **2** | 변위식 | 새 위치 = `c + (c−p)/‖c−p‖ · out`. `c` = 표면 최근접점 · `out` 시작값 = `marginM`(= `PLACE_MARGIN` 15.0mm). ⟹ **변위 크기 = `l + out`**(`l` = 관통 깊이 · p·c·새 위치가 한 직선 위) |
| **3** | 적용 단위 | **정점별 «독립»**(`for i` 루프). 이웃 정점·엣지·시접을 참조하는 항이 **0개**. rigid transform도 uniform scale도 **아니다** |
| **4** | 변위 상한 | **없다.** 루프 8회는 `out`(표면으로부터의 «거리»)만 `minSepM`씩 키운다 — **변위 크기의 상한이 아니다**. `l`의 상한은 `detectionRadiusM = SDF_FAR = 300mm`. ⟹ 변위가 최대 **300mm + (15.0 + 8×3.21)mm** 규모까지 구조적으로 허용된다 |
| **5** | **핵심 판별** | **깬다.** 정점별 독립 변위이므로 이웃 두 정점이 서로 다른 표면점으로 투영되면 그 3D 거리가 임의로 변한다. rest(패턴 2D)는 고정이므로 **신장비가 1을 넘을 수 있다** ⟹ 105 §1-4의 상계를 **구조적으로 깬다** |
| **6** | e=3cm에서 더 쉬운가 | **코드는 말하지 않는다(데이터 의존)**. 배치 평면 z 오프셋은 `body.{front,back}ExtentM + 2·COLLISION_MARGIN`(`patternGarment.ts:363-364`)이라 **폭과 무관**하다 — e를 줄여도 «평면 위치»는 안 바뀌고 패널의 x 범위만 줄어든다. **몸은 x로 넓으므로 x가 줄어드는 것이 몸에 가까워지는 방향이 아니다.** ⟹ 정성적 코드 사실로는 「더 쉬워진다」가 도출되지 않는다 |
| **7** | 호출 시점 | `dressPattern.ts:391`(S0 배치 · `S0FIX` 기본 on) · `:2148`(`place` 훅). `dressingMachine.ts:173 doPlacement()`가 **while 루프 «밖·앞»**이고 `hooks.setAnchorHard`는 `:196`(루프 안 · `session.step` `:221` 전) ⟹ **배치 직후 · 핀 발화 «이전»이 맞다** |

**105 §1-4와 한 쌍이다** — 105는 **배치 «사상»**(`mapTorso`)이 x 항등 · y 1-Lipschitz ·
z 패널 상수라 **상계를 지킨다**를 확정했고, 106은 **같은 단계 안의 배치 «교정»**이
**정점별 독립 변위**라 그 상계를 **깰 수 있다**를 확정했다. **기전 귀속 0** — 확정된 것은
**두 코드 사실**까지다.

---

## §2 판정 — **갈래 A 및 C** (둘 다 §3으로 진행)

| 갈래 | 문언 | 실측 | 판정 |
|---|---|---|---|
| **A** | 5번이 «깬다»(정점별 독립 변위) | 정점별 독립 · 변위 상한 없음 | **성립** |
| **B** | 5번이 «안 깬다»(구조 보존) | — | 불성립 |
| **C** | 발화 조건이 «관통 검출 시에만»이고 코드만으로 e=3cm 발화 여부 불명(데이터 의존) | 1번 = 관통 시에만 · 6번 = 코드가 말하지 않는다 | **성립** |
| **D** | 5번이 코드 판독만으로 불명확 | 변위식이 닫혀 있고 재귀 없음 | 불성립 |

→ **갈래 A 및 C. §3 집행.**

**회차의 갈래 열거가 함정30 재발 방지를 «실제로 이행»했다 (사실 등재)**

```
설계    **A·B는 «구조»**(깬다 / 안 깬다)로, **C·D는 «판독 가능성»**(데이터 의존 / 코드로 불명확)으로
        갈라 **두 축이 서로 다른 물음**을 덮는다 ⟹ 잔여 상태가 남지 않는다
조합    **「A «또는» C」를 회차 문언이 실행 «전»에 미리 허용**했다 —
        두 축이 동시에 성립하는 것이 **설계상 예상된 결과**다
대조    95 §3 · 105 §1은 **갈래 이름으로 답할 수 «없었다»**(값역 포화 / 열거의 빈틈).
        106은 **갈래 이름으로 답했다**
```

**사후 갈래 신설 0 · 갈래 문언 변경 0 · 문턱 사후 변경 0.**

---

## §3 집행 — 기준선 1회 (계기 `aacf487` · 인쇄 전용 · `patternPlacement.ts` 0줄)

계기는 **호출부에서 before/after 스냅샷**만 뜬다(공유 라이브러리 무수정). **두 호출부 전량 배선.**

```
[106 §3·배치 교정 변위] S0 배치 — 교정 반환 0정점 · **실제 이동 0정점 / 5244**
· **발화 0 — 이 배치에서 이 함수는 좌표를 한 점도 안 바꿨다**
· 상한 없음(변위 = 표면까지 거리 + out · out ≤ 15.0 + 8×3.21mm)
[dress] S0 배치 관통: 0 → 교정 0정점 → 0 (패리티 근사)
```

**기준선(halfWidth 27.50cm)에서 이 함수는 «발화 0»이다.**
`place` 훅(재배치)은 이번 실행에서 **호출되지 않았다** — `countPenetrating() = 0`이라
`doPlacement`가 재시도하지 않는다(`dressingMachine.ts:157-171`).

⟹ **이 회차의 실측 결론: 구조적으로는 상계를 깰 수 있으나(갈래 A) 기준선에서는 발화하지 않는다**
(갈래 C의 «데이터 의존»이 **기준선 쪽으로만** 확정됐다).
**e=3cm에서 발화하는지는 이번 회차 정의역 밖이다**(e 재실행 0 · §0 ㄹ대로 다음 회차 몫).

---

## 실행한 것 / 실행하지 않은 것 (미집행을 침묵으로 넘기지 않는다)

**실행한 것**

```
§0   전략 세션 처분 8건 수령(105 §6 갈림 ㄱ~ㅈ) — **ㄴ = 기각 선언**을 함정32에 등재
§1   `correctPlacementPenetration` 판독 **7항** + 코드 원문 발췌
§2   사전 등록 판정 → **갈래 A 및 C**(갈래 이름으로 답함)
§3   계기 신설·실행(`aacf487` · 인쇄 전용 · 호출부 2곳 전량 배선) → **기준선 발화 0정점 / 5244**
회귀  `dress:pattern` 1회 · `npx tsc -b` 통과 · 전 채널 기준선 A 비트 동일
```

**실행하지 않은 것 — 사유 병기**

```
**e=3cm 실행 · e 변경 · 값 스윕**    **0** — 회차 조항(§0 ㄹ) ·
                                   [[../함정/함정14-기준선의존]] §경계선 조건 ②
**`patternPlacement.ts` 수정**      **0줄** — 공유 라이브러리 무수정(계기는 호출부 스냅샷만)
**기전 귀속 · 기전 후보**            **0** — 34게이트 throw의 기전을 묻지 않았다(회차 조항)
**앵커 배선 · 로그 문언 수정**       **0** — §0 ㅂ·ㅅ 유지(105 등재분 그대로)
**`g.widthM` 죽은 입력 · UI 처분**   **0** — 채택 회차 몫
**캡처**                           **시도 0** — §0 ㅇ(사용자 조치 대기 2건 미해소)
**폭 축 종결 등재 · 기준선 A′ 수립 · 태그**  **0**
**MARGIN_ALL 적용 · `COLLISION_MARGIN` 변경 · 물리**  **전부 0**
목선(3순위) · 소매 축(4순위 · 종결) · 와인딩 72건 · 수밀성 감사 · 감사 재계산 6건
                                   **전부 미착수 유지**
```

---

## 산출 불가 / 확인 불가 (metrics 블록의 같은 절 **전량**)

```
**e=3cm에서의 발화 여부·변위 크기**   **실행하지 않았다**(회차 조항 「e=3cm 실행 0」).
        계기는 **이미 배선돼 있으므로** 다음 회차가 `WIDTH_EASE_M`을 그 값으로 두고
        **1회 돌리면 바로 나온다**
**104 throw의 기전**                **귀속하지 않았다.** 이 회차가 확정한 것은
        ① 이 함수가 상계를 **깰 수 있다**(구조) ② **기준선에서는 발화 0**(실측) 두 가지다
**`l`(관통 깊이)의 실제 분포**        발화 0이라 **표본이 없다**
**`place` 훅 경로의 계기 값**        이번 실행에서 **호출 자체가 없었다**
**34게이트 극단값(0.067297~22.668559)을 이 함수가 «실제로» 만들었는가**
        **구조적 가능성까지다.** 실측 연결은 **e=3cm 실행이 있어야 한다**
**캡처**                           **시도 0**(사용자 조치 대기 2건 · 104 Chrome 자동화 권한 ·
        105 화면 기록/화면 상태)
**목선 · 소매 축 · 와인딩 72건 · 수밀성 감사 · 감사 6건 · 고정점 · A′**  전부 **미착수 유지**
기점                               **없음**(기준선 A 유지 · 9fbd66c 그대로 · 태그 갱신 0)
```

---

## §4 함정 판정 — **자체 판정 2건** (회차 프롬프트 위임 · **신설 0 · 수리 0**)

### 4-1 (가) [[../함정/함정30-창폭과창위치]] — **«이행 기록»을 단다 · «사례»로 세지 않는다**

**판정 = 블록을 «단다». 다만 사례 계수는 «불변»이다.**

```
무엇을 다는가   **「규범 ②의 이행 기록 — 열거가 «작동한» 회차」**.
              105가 이 함정의 **사례**(갈래 열거의 «빈틈»)였고, **같은 회차 설계자**가 그것을
              반영해 106 등록문에서 **catch-all(D)를 두고 조합(A·C)을 미리 허용**해
              **판정 불가를 피했다** — 규범 ②(「갈래 술어가 값역을 «분할»하는지 확인한다」)의
              **직접 이행 사례**다
형식에 맞는가   **맞는다.** 이 노트는 이미 **«분류 판정과 이행 기록»** 성격의 블록을 두 종 갖고 있다 —
              「후속 — 이 함정의 사례가 «아닌» 것」 2건(95 · 96)과 **계수 확인 블록**(105 §4-3).
              볼트 선례도 있다: [[../함정/함정14-기준선의존]] 계열의 **「적용 3건째 — 조건 ①② 이행」**
              (104 §5-1)이 «준수 기록»으로 함정 노트에 실렸다
계수 처리      **사례 계수 불변** — 105가 세운 계수는 「실행이 있었고 **갈래 이름으로 답할 수 «없었던»**
              회차」(94·95·105 = 세 번째)이고 **106은 갈래 이름으로 답했다** ⟹ 그 계수에 **넣지 않는다**.
              **계수 신설 0 · 과거 문언 수정 0**
[[../원리/사전등록]] 쪽인가  **아니다 · 갱신 0.** 규범 4·5(도출·문턱은 채택 회차가 실행 «전»에)는
              **이행됐으나** 이번에 자란 것은 **갈래 «열거»의 완전성**이고 그 자리는 105가
              **함정30 규범 ②의 반대 국면**으로 이미 확정했다. **같은 규범의 이행은 같은 노트에 붙는다**
              (규범이 두 군데에서 자라지 않게 한다는 기존 근거)
처분          **규범 ①②③ 문언 수정 0 · 신설 0 · 문턱 0 · 사례 계수 0 · 과거 판정 뒤집기 0**
```

### 4-2 (나) 「한 층은 구조 보존 · 다른 층은 구조 파괴」 — **신설 0 · 사례 등재 0 · «준수 기록»만**

**판정 = 함정을 신설하지 않는다. 기존 함정의 사례로도 올리지 않는다.**
105 §1-4(구조적 상계) + 106 §1(예외 판독)은 **한 쌍으로 완결된 «준수» 기록**이지 결함이 아니다.

```
형태          같은 단계(배치) 안에서 **배치 «사상»은 상계를 지키는데**(x 항등 · y 1-Lipschitz ·
              z 패널 상수) **배치 «교정»은 정점별 독립이라 그것을 깬다**
왜 결함이 아닌가  **105가 그 예외를 «스스로 열거했다»** — 105 §1-4 원문이
              「배치 시점의 예외는 `correctPlacementPenetration`(`patternPlacement.ts:169-185`)
              하나뿐이다」라고 적었고, 106은 그 지목대로 판독했을 뿐이다.
              **놓친 경로가 없고 오염 산출물도 없다**(105 판정에 이 함수의 값이 실린 것 0건)
[[../함정/함정17-반납된전제]] «아님»   시그니처 = **진입점 grep 미이행**(스위치 하나를 끈 것이
              전 경로를 끈 것이 아니다). 이번은 **진입점 열거가 105에서 이미 끝나 있었고**
              예외를 **명시 병기**했다 ⟹ 「모르고 남긴 경로」가 아니다.
              (101이 함정32를 17과 가른 것과 **같은 시험**이다)
[[../함정/함정32-소비처부분적용]] «아님»  대상이 **한 값의 소비처**가 아니다 —
              갈리는 것은 값이 아니라 **한 단계 안의 두 층이 갖는 구조적 성질**이고,
              같은 값이 두 기준으로 갈린 상태가 아니다
[[../함정/함정13-jitter]] 규범 6 «아님»  대상이 **계기가 아니다.** 축·시점·정규화·집합·값역
              어느 항목도 어긋나지 않았고 **채널은 전량 정상 산출**됐다(발화 0은 값이지 결함이 아니다)
[[../함정/함정30-창폭과창위치]] «아님»   창(문턱 폭 · 갈래 술어 · 시점 · 자산 분해능)이 아니다 —
              **갈래가 발화했고**(A 및 C) 창이 답을 덮었다
[[../원리/사전등록]] 규범 15 «대조만»    15는 **원인 열거**의 완전성이다. 여기서 열거된 것은
              원인이 아니라 **한 단계 안의 층**이고, 그 열거는 **이미 완전했다** ⟹ **갱신 0**
처분          **번호 신설 0 · 사례 등재 0 · 규범 문언 수정 0 · 문턱 0.**
              이 회차 노트에 **준수 기록으로만** 남긴다
```

---

## §5 등재

```
[[../함정/함정32-소비처부분적용]]  **§0 ㄴ 기각 선언 블록 추가**(전략 세션 선언 · 「폭 판본」 후보 **기각**) —
                                105의 「단정 0 · 기각 선언 0」 블록은 **원문 무삭제**로 두고 그 «아래»에 잇는다 ·
                                **함정 폐기 0 · 규범 1~4 문언 수정 0 · 사례 1·102·105 블록 수정 0**
[[../함정/함정30-창폭과창위치]]   **이행 기록 블록 추가**(규범 ②가 «작동한» 회차) ·
                                **사례 계수 불변 · 규범 문언 수정 0 · 신설 0**
[[../함정/함정17-반납된전제]]     **갱신 0** — §4-2에서 **대조만**(진입점 열거는 105에서 끝나 있었다)
[[../원리/사전등록]]              **갱신 0** — 규범 4·5·15와 **대조만**(문언 수정 0 · 번호 신설 0)
[[2b-105-폭소비처]]              **§0(2) 후보의 «기각» 선언 결과 + 106 백링크**
                                (105의 「기각 선언 0」 원문 **무삭제** · 판정 뒤집기 0 · 수치 수정 0)
[[2b-104-폭도출채택]]            **106 결과 한 줄**(throw 자리 후보가 **배치 교정**으로 좁혀졌고
                                **기준선 발화 0** · **104 원문 수정 0**)
[[../00-현재상태]]               **몸통 폭 축** — §0(2) 후보 **기각**(축 종결 아님) ·
                                **104 throw의 자리**(배치 사상 배제 → 배치 교정은 구조적으로 가능 ·
                                **기준선 발화 0**) · 계기 신설 · 캡처 **시도 0**(대기 2건) ·
                                서열 **96 §0-3분 유지**
[[../전략세션-인수인계]]          항목 **2·3·5 누적** · **1·4·6 교체** ·
                                **항목 2에 «폭 축»을 넣지 않는다** ·
                                기준선 **「A 유지 · A′ 미수립」 유지** ·
                                raw URL 절은 **106 커밋 뒤 `npm run report`**
[[../규칙/화면판정]]              **갱신 0** — 캡처 시도 0 · 자체 화면 판정 0
감사 노트                        **행 전환 0 · 재계산 0건**(정의역 밖)
```

---

## §6 다음 수 — **갈림만** (제안 금지 · **처방 0 · 기전 후보 0 · 사후 갈래 신설 0 · e 제시 0**)

**이 회차가 도달한 자리: 배치 «교정»은 105의 구조적 상계를 «깰 수 있다»(갈래 A).
그러나 «기준선에서는 한 점도 발화하지 않았다»(발화 0 / 5244 · `place` 훅 호출 0).
따라서 104 throw의 자리는 «후보로 좁혀졌을 뿐 실측으로 연결되지 않았다».**

```
ㄱ  **e=3cm에서 이 함수가 발화하는지 물을 것인가** — **계기는 이미 배선돼 있다.**
    다음 회차가 `WIDTH_EASE_M`을 그 값으로 두고 **1회 돌리면** 발화 정점 수·변위 분포가
    바로 나온다. 다만 그 회차는 **e·문턱·갈래를 실행 «전»에** 정해야 한다
    (규범 4·5 · §0 ㄹ · **함정14 §경계선 조건 ②가 그대로 걸린다**)
ㄴ  **104 throw의 «자리»를 계속 좁힐 것인가** — 배치 사상은 **배제**(105) ·
    배치 교정은 **구조적으로 가능하나 기준선 발화 0**(106). **기전 귀속 0 · 후보 0**
ㄷ  **폭 축을 계속 열어 둘 것인가** — **종결 등재 0**(106도 값을 하나도 바꾸지 않았다)
ㄹ  **`place` 훅 경로를 따로 물을 것인가** — 이번 실행에서 **호출 자체가 없었다**
    (관통 0 ⟹ 재시도 없음). 그 경로의 계기 값은 **산출된 적이 없다**
ㅁ  **갈래 열거 방식의 처분** — 이번 등록문이 **구조 축 + 판독 가능성 축 + 조합 허용**으로
    빈틈을 피했다. 이 형식을 이후 등록문의 관행으로 올릴지는 **갈림**(106 문턱 0 · 신설 0)
ㅂ  **`PINDRESS` off ↔ 하드 앵커 · `g.widthM` 죽은 입력 · `PatternPreview` 무반영** —
    **105 등재 상태 그대로**(106 착수 0)
ㅅ  **캡처 환경 제약 2건** — **사용자 조치 대기**(106은 **시도 0**)
ㅇ  **화면 서열** — **96 §0-3분 유지**(갱신 0) · 자체 화면 판정 0
```

**다음 수의 «선택»은 전략 세션 몫이다.**

### §6 ㄱ 처분 결과 — 107회차 (**106 본문·수치·판정 수정 0**)

```
ㄱ의 답    **물었다 · 답이 나왔다** — 107이 `WIDTH_EASE_M = 0.030`(104와 **같은 값 재사용**)으로
          **1회 실행**했고(코드 커밋 0건 · 즉시 원복 · `git diff` 0),
          **이 함수가 e=3cm에서 «발화한다»**:
          `재배치(오프셋 배수 1.50) — 교정 반환 5정점 · 실제 이동 **5정점 / 4566** ·
           변위(mm) 중앙 15.21 · p99 15.90 · 최대 15.90`
          (S0 배치 자체는 **0정점** · 정점 총수 4566 = **재삼각화**)
대조      **기준선 발화 0정점 / 5244**(106 §3) ↔ **e=3cm 발화 5정점 / 4566**(107 §1) ⟹
          §3의 「기준선 쪽으로만 확정」이 **반대편에서도 값을 얻었다**
규모      §1 항목 2의 식으로 역산 — **변위 = `l` + `out`** · `out` = `PLACE_MARGIN` **15.0mm**
          고정(clash 0회) ⟹ **`l` = 0.21(중앙) ~ 0.90mm(최대)**.
          `COLLISION_MARGIN` 15.0mm의 **0.06배** · `MESH_MARGIN` 3.212mm의 **0.28배** ⟹
          107 사전등록 **«갈래 C»**(발화 > 0이나 규모가 A 기준에 못 미친다)
차수      34게이트 값(**22.669 · 14841.6cm**) ↔ 이 함수의 변위 합(**약 76mm 규모**) —
          **차수가 다르다.** 다만 **구체적 정점/쌍 대응은 107 정의역 «밖»**이다 ·
          **기전 귀속 0**(106·107 둘 다)
§6 ㄹ     `place` 훅 경로가 **107에서 처음 호출됐다**(기준선에서는 호출 0) —
          그 경로의 계기 값이 **처음 산출**됐다(부수 관찰 · 전용 계기 신설 0)
```

→ [[2b-107-e3cm발화실측]] §1 · **§2** · §3 · §4

---

## 링크

- **다음 회차**: [[2b-107-e3cm발화실측]] — **§6 ㄱ 처분 결과**(위 절) ·
  **기준선 발화 0 ↔ e=3cm 발화 5정점** · **갈래 C**(`l` ≤ 0.90mm = 소규모 여유 채널의
  «수 분의 일») · **106 원문·수치·판정 수정 0 · 기전 귀속 0**
- 이전 회차: [[2b-105-폭소비처]] — **§0(2) 후보 기각 선언 결과 + 106 백링크**
  (105 「기각 선언 0」 **원문 무삭제**) · **34게이트 구조적 상계**(106 §1의 짝)
- 관련 회차: [[2b-104-폭도출채택]](**106 결과 한 줄** — throw 자리 후보 · **원문 수정 0**) ·
  [[2b-103-몸통폭도출]](폭 축 재개) · [[2b-101-margin도출]](갈래 C · 규범 4·5 선례)
- 함정: [[../함정/함정32-소비처부분적용]](**폭 판본 «기각» 선언** · 함정 폐기 0) ·
  [[../함정/함정30-창폭과창위치]](**규범 ② 이행 기록** · 사례 계수 불변) ·
  [[../함정/함정17-반납된전제]](**대조만** · 갱신 0) ·
  [[../함정/함정13-jitter]](**대조만** · 갱신 0) ·
  [[../함정/함정14-기준선의존]](§경계선 조건 ② — e 재시도 0) ·
  [[../함정/함정26-기준선재생성]](해시 3종 · dress-state md5 불변 · 재생성 20회차)
- 원리: [[../원리/사전등록]](**갱신 0** — 규범 4·5·15 대조만 · **갈래 이름으로 답함 = A 및 C**)
- 규칙: [[../규칙/화면판정]](**갱신 0** · 캡처 시도 0)
- 관련: [[../00-현재상태]] · [[../전략세션-인수인계]] · `docs/metrics-log.md`(커밋 `adfb510`)
