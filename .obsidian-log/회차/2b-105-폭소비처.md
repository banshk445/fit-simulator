---
tags: [회차, stage2b, 몸통폭, 소비처정리]
회차: 105
상태: **본문 확정** · §1 = 사전등록 **갈래 A·B·C 어디에도 해당하지 않음 → «판정 불가» · 정지**(사후 갈래 신설·문언 변경 0) · 폭 소비처 **전량 정리 완료**(갈림 6곳 · **판정·물리에 값을 넘기는 것 0곳**) · **34게이트 구조적 상계 신규 등재**(104 throw의 값은 배치 사상이 만들 수 없다 · **기전 귀속 0**) · `PINDRESS` off ↔ **하드 앵커 발화** 신규 확정(배선 변경 0) · §2 폭 채널 **㉮ = ㉯ = 27.50cm** · §3 캡처 **0장**(사유 등재 · 사용자 조치 대기 2건) · 함정 자체 판정 **3건 · 신설 0** · 기준선 **A 유지 · A′ 미수립** · 물리 0줄 · **값 변경 0 · e 변경 0 · 처방 0 · 기전 귀속 0 · 새 문턱 0 · 기각 선언 0**
층위: 소비처 정리 · 코드 판독
---

# 105회차 — 폭 소비처 전량 정리 (판정 불가 · 정지)

> **git 스탬프**: 기점 `v2-2b-78-baseline`(9fbd66c) · 브랜치 `v2-stage2a` ·
> 워크트리 `/Users/banshk/projects/fit-simulator` · 착수 시 HEAD `916cee9` ·
> 직전 노트 `bb0f59f` · 104 계기 `5bd5a01` · 104 metrics `05e3c86` ·
> **105 계기 `8036dfd`** · **105 metrics `5a2c7bb`** ·
> 노트 커밋 = 이 회차 등재 커밋(**작성 시점 미커밋** — 커밋 금지 지시) ·
> **기점 갱신 0 · 태그 0 · 기준선 A 유지**

본문의 숫자는 **전부 `docs/metrics-log.md` 105회차 블록**
(`## 2026-08-09 · 105회차 — 폭 소비처 전량 정리` · 커밋 `5a2c7bb`)**에서 인용**한 것이고
**이 노트에서 새로 만든 측정 0 · 추정 0 · 처방 0 · 기전 귀속 0 · 새 문턱 0**이다.

코드 범위: 물리 **0줄** · `patternGarment`/`garmentFrame`/`torsoCapsule`/`clothPhysics` **0줄** ·
**e 변경 0 · 2단계 재시도 0 · fixture 재굽기 0 · 앵커 배선 변경 0 · 값 스윕 0 ·
처방 0건 · 새 문턱 0 · 캡처 0장.**

## 환경 스탬프 · 해시 3종 · 회귀

```
[dress] 해시: pattern 9f7ba80b3497 · fixture 9e8b2bf13925 — 재현하려면 위 env 줄을 그대로 쓴다(함정 26)
[dress] env: RINGTOTAL=0 · … · MARGIN_ALL=미설정 · … · PATTERNCORE=1 · …
```

dress-state md5 `14e50b8919a63a7f9799220b86e508a9` **불변**(재생성 **19회차**) ·
`patternHash 9f7ba80b3497` · `fixture 9e8b2bf13925` 불변 · `npx tsc -b` 통과 · 벽시계 **~44s** ·
**전 채널 비트 동일** — cov **6.8%** · maxStrain **2.661** · maxSeamGap **11.14mm** · Δ20 **5.16mm** ·
자기교차 **1889** · 관통 **50/5244** · DONE **f=260** · 목선 링 **61.80cm** · 밑단 합 **112.57cm** ·
탐지 실패 **2253**.

---

## §0 전략 세션 등재 3건 (프롬프트 원문대로)

**(1) 104 §7 갈림 처분**

```
ㄱ  폭 축 **유지** · 1단계 성립분 **코드 유지**
ㄴ  **ㄷ 뒤로 미룬다** — 소비처 정리 «전»에 2단계를 재시도하면 **101 재연 위험**
ㄷ  **이 회차가 소비처 열거로 답한다**
ㄹ  **미분해 유지**(`check:pattern` 최소각 sleeve 해소의 기전)
ㅁ  **채택 회차 몫 유지**(슬라이더 `garmentSize.width`의 처분)
ㅂ  **사용자 조치 대기**(Chrome 자동화 권한)
ㅅ·ㅇ·ㅈ  **유지**
```

**(2) 전략 세션 후보 등재 — 반증 대상 · «갈래를 만들지 않는다»**

> 「104 2단계 throw는 e 값의 문제가 아니라 **제도 폭과 배치 폭의 소비처 갈림**일 수 있다 —
> **함정32(소비처 부분 적용)의 폭 판본**.」

정황 3건(전략 세션 등재분): §1 검색표 6 — **fixture에 `layout.widthM 0.55`가 절대값으로 박힌다** ·
throw 지점 = **핀 발화 직후** · 최대 신장 정점 = **새 반폭의 모서리 (−24.00, 24.00)**.

**(3)** 이 회차는 **100회차 §1(margin 소비처 8곳 표)의 절차를 «폭»에 적용**한다.
**값 변경 0** · **정리와 실측까지가 정의역**이다.

---

## §1 폭 소비처 전량 (검색 지점 11개 · 규범 18 확장판)

### 1-1 검색 (원문)

```
grep widthM|halfWidth|WIDTH_EASE|WIDTH_CHEST_DIVISOR|chestGirth 전량 ·
fixture JSON 2종 직접 판독 ·
v1 층(buildGarmentSim.ts:112-114 · garmentFitLimits.ts:42,45,63,68 = **이름이 다른 등가 식**) ·
등가 식 재검색(`/ 4` · `chest *` · `girth / 2`) ·
하드코딩 리터럴 `0.55`(checkSleeveSeam.ts:41 = v1 하네스 «별개» 상수) ·
git log -S 'layout.widthM'(6799668 · bca0f65 · 0ba0a18 · c291e22 · b29c76b) ·
git log -S 'halfWidthM'(0ba0a18) ·
git log -S 'widthM' -- patternGarment(4568be2 · 76d6db8) ·
grep layout -- garmentFrame.ts(`:380`·`:383` **2곳뿐**)
```

**지점 수 「11개」는 metrics 블록 헤더 표기 그대로**다(이 노트에서 다시 세지 않았다).
규범 18의 확인 범위 확장분(**v1 층 · 이름이 다른 등가 상수·식 · `git log -S`**)을 전부 이행했다
([[../원리/사전등록]] 규범 18 · 104 §5-3 확장분).

### 1-2 출처 분류표 — ㉮ 1단계 도출(`B/4+e`) / ㉯ fixture `layout.widthM`(0.55 절대값) / ㉰ 슬라이더 직독

| 파일:줄 | 뜻 | 출처 | ㉮·㉯ 갈림 |
|---|---|---|---|
| `patternDraft.ts:231,243,244` `halfWidthM` | 제도 치수(도출 원점) | **㉮** | (원점) |
| `patternDraft.ts:293` `underarmPoint.x` · `:294` `hemOut.x` · `:404` `dims` · `:491-492` 자기검사 | 제도 치수·게이트 | ㉮ | 아니오 |
| `checkPattern.ts:451,452` 코너 `±D.halfWidthM` · `checkBoundary.ts:189` 여유 인쇄 | 게이트 좌표·인쇄 | ㉮ | 아니오 |
| **`patternGarment.ts:454-458` `mapTorso`** | **배치 목표** | **㉮ 경유(`pos2`)** · z는 `body.{front,back}ExtentM + margin·2` | **아니오** |
| `dressPattern.ts:187` `garmentDims.widthM = layout.widthM` → `g.widthM` | 제도 입력(명목) | ㉯ | **예 — 그러나 `patternDraft`가 `g.widthM`을 «한 번도 읽지 않는다»**(사용처 전량 = `shoulderWidthM` · `lengthM` · `sleeveWidthM` · `sleeveLengthM`) ⇒ **죽은 입력** |
| `dressPattern.ts:2105` `frameLayout.widthM` → `session.step` | 프레임 파라미터 | ㉯ | **예 — 소비처는 `garmentFrame.ts:380/383`뿐이고 dress env는 `armSoftPull:false · necklineHug:false`** ⇒ **불활성** |
| `dressPattern.ts:219` `patternMeta.garmentDims` | 메타 인쇄 | ㉯ | 예(인쇄). **`patternHash` 입력이 아니다** — 해시 = `pos2`+`tris`+`seams` |
| `checkPattern.ts:69,76` · `checkBoundary.ts:55` | 인쇄 | ㉯ | 예(인쇄 전용) |
| `PatternPreview.tsx:228` | UI 미리보기 | ㉰ | **예 — `g.widthM`이 죽은 입력이라 슬라이더가 미리보기 폭을 못 바꾼다** |
| `useFitStore.ts:129,167,255-256` · `garmentFitLimits.ts:63,68` | 슬라이더·UI 착용판정 | ㉰ | 예(UI 전용) |
| `Garment.tsx:643` · `buildGarmentSim.ts` · `spikeDressing` · `paramSweep` | v1 경로 | ㉰/㉯ | v2 판정 밖 |

**㉮·㉯가 실제로 갈리는 소비처 = 6곳**
(`dressPattern.ts:187` · `:2105` · `:219` · `checkPattern.ts:69/76` · `checkBoundary.ts:55` ·
`PatternPreview.tsx:228`).
**그중 판정·물리에 값을 넘기는 것은 0곳**이고 **배치·앵커 경로는 포함되지 않는다.**

### 1-3 앵커 36개 — **좌표 기반 · 재삼각화에 취약하지 않다**

`dressPattern.ts:978-1037`:

```ts
return g.seams.filter((sm) => sm.kind === "shoulder")
  .filter((sm) => !(ringVerts.has(sm.a) || ringVerts.has(sm.b)))   // :1024 목점 2쌍 제외
  .map((sm) => { const px = g.pos2[sm.a*2], py = g.pos2[sm.a*2+1];  // :1026 패턴 2D
    const s = Math.hypot(Math.abs(px) - nwHalf, py) / shoulderSeamM; // :1027 호장 비율
    const t = at(Math.sign(px) || 1, s);                             // :1028 능선 곡선 위 점
    return { i: sm.a, x: t.x, y: t.y + COLLISION_MARGIN, z: t.z, … }; });
```

- **목표는 «좌표»다** — `body.ridgePoints` 폴리라인을 **호장 비율로 보간** + `COLLISION_MARGIN`.
  **배치 3D 좌표가 아니다**(`:1046`이 배치 평면 z와 구분해 인쇄).
- **인덱스는 매 실행 `g.seams`에서 재도출**된다(저장된 인덱스 표·하드코딩 목록 **부재**)
  ⇒ **재삼각화가 앵커를 «다른 정점»으로 어긋나게 하는 구조가 아니다.**
  실측 대조: `pattern-meta.json` `seamCounts.shoulder = 38` → 앵커 **36**(= 38−2) ·
  **104의 e=3cm 실행 로그도 「36개」** ⇒ **재삼각화 후에도 기수 동일.**
- **어느 폭도 참조하지 않는다** — 읽는 값 전량 = `neckHalfWidthM` · `shoulderSeamM` ·
  `body.ridgePoints`/`centerX` · `g.pos2` · `COLLISION_MARGIN`. **`halfWidthM`은 어느 항에도 없다.**

**`PINDRESS` off인데 하드 앵커가 발화하는 구조 (함정 17 계열 · 신규 확정)**

```
소프트   dressPattern.ts:1886  anchors: () => (PINDRESS ? anchorList : [])   → **꺼진다**
하드     dressPattern.ts:2051-2093  setAnchorHard(→ sim.pinned[a.i] = 1 · setParticle)
         → **`PINDRESS`를 참조하지 않는다**
로그     `:497` 「착장 앵커 off(기본)」 = **소프트 경로만** 가리킨다
```

**등재만 · 배선 변경 0 · 로그 문언 수정 0**(회차 금지 조항). 판정은 §4-2.

### 1-4 34게이트 — 비교 대상 확정 + **구조적 상계**

`dressPattern.ts:1996-2025` `placementRestGate`:

- **분자** = `sim.positions`의 **현재 3D 정점간 거리**
- **분모** = `sim.constraintPairs[].restLength` = `buildPatternSim.ts:108-119`의
  **패턴 2D(`g.pos2`) 거리**(structural + bend 전량). **시접 쌍은 `:2001`에서 제외**(그것만 3D 갭 등록)
- 대상 = **몸판 한정**(`c.a < g.panelStarts[2]`). 「신장비 1.000±3.33e-5」 =
  ‖3D 현재‖ / ‖패턴 2D‖ · 허용분은 `2·maxCoord·2⁻²⁴ / minRest`
- **핀 목표는 게이트에 들어가지 않는다** — 게이트 호출이 `setAnchorHard` 안(`:2091`)이지만
  그 직전 `setParticle`은 `anchorRampS = 0`이고 `anchorFrom`이 그 프레임의 현재 좌표라 **쓰기가 항등**
- **두 대상이 서로 다른 폭을 읽을 수 있는가 — «아니오».** 분모는 `g.pos2`, 분자는
  `mapTorso(pos2)`로 **같은 `pos2`**에서 나온다. **`layout.widthM`은 어느 쪽에도 없다**
- **시점** — `dressingMachine.ts:196 hooks.setAnchorHard?.(closure <= 0)`는 `session.step`(`:221`)
  **«전»**이고, 게이트는 `:2091 if (anchorRampFrame === 0 && gateArmed)` =
  **배치 뒤 첫 하드 전이 1회**

**구조적 상계 (신규 등재 · 이 회차의 가장 큰 산출)**

배치 상태에서는 **신장비 > 1이 원리적으로 불가능**하다. 게이트 대상 몸판 제약은 전부
**패널 내부** 쌍이고

```
mapTorso: x = centerX + xp        (항등)
          y = topY − clamp(yp, 0, lengthM)   (1-Lipschitz)
          z = 패널 상수
```

⇒ **같은 패널 두 점의 3D 거리 ≤ 2D 거리.** 기준선 실측이 그 상계다 —
**최대 1.000016 / 최소 0.999986 / 신장총 0.0cm**(34·41·42·43회차 전량).

⟹ **104가 인용한 `최대 22.668559 · 최소 0.067297 · 신장총 14841.6cm`는
«배치 사상»이 만들 수 있는 값이 아니다.**
배치 시점의 예외는 `correctPlacementPenetration`(`patternPlacement.ts:169-185`) 하나뿐이다.

**부수 산술**: throw 직전 인쇄가 `seamGap 44.3mm`인데, 배치 직후 앞↔뒤판 시접 갭은
`frontExtentM + backExtentM + 4·COLLISION_MARGIN` = **몸 두께 + 6cm 규모**다
(등재된 배치 평면 z = **+17.07cm**) ⟹ **44.3mm는 배치 상태의 seamGap이 아니다.**

**기전 귀속은 하지 않는다.** 확정된 것은 위 **두 코드 사실**까지다.

---

## §1 판정 — **판정 불가 · 정지** (사전등록 갈래 A·B·C 어디에도 해당하지 않는다)

| 갈래 | 문언 | 실측 | 판정 |
|---|---|---|---|
| **A** | 갈림 소비처 **1곳 이상** **그리고** **그중 배치·앵커 경로가 포함된다** | 갈림 **6곳** · **배치·앵커 미포함** | **불성립**(둘째 연언지 거짓) |
| **B** | 갈림 소비처 **0곳**(배치도 1단계 도출을 읽는다) | 갈림 **6곳** · 괄호 조항은 **참** | **불성립**(주 조항 거짓) |
| **C** | 앵커가 **정점 인덱스 기반**이고 재삼각화에 취약 | **좌표 기반** · 인덱스 **매 실행 재도출** · 기수 **36 동일** | **불성립**(독립 판정 · 「아니오」) |

**갈래 설계가 「갈림은 있으나 전부 죽은 입력 · off · 인쇄」라는 상태를 예상하지 않았다.**
A는 **연언**으로, B는 **계수**로 갈랐는데 **실측이 그 사이에 떨어진다.**

**사후 갈래 신설 0 · 갈래 문언 변경 0 · 문턱 사후 변경 0** ⟹ **판정 불가 · 정지**(상시 규약).

### §0(2) 후보의 «판별 조항»은 답이 나왔다 — 다만 **기각을 «선언하지 않는다»**

```
후보        「throw는 제도 폭과 배치 폭의 «소비처 갈림» — 함정32의 폭 판본」
판별 조항   **「배치·앵커가 ㉯를 읽는가」**
답          **«아니오»** — 배치는 `pos2`(㉮)만, 앵커는 `body.ridgePoints` + `pos2`만 읽는다
상태        **후보는 «지지되지 않는다»**
기각 선언   **하지 않는다** — 갈래가 성립하지 않았으므로 이 회차는 기각을 선언할 자리가 아니다
            (사후 갈래 신설 0 · [[../원리/사전등록]] 규범 4·5)
```

---

## §2 실측 — 폭 채널 (계기 `8036dfd` · 인쇄 전용 · 판정 로직 0줄)

```
[dress] 폭 채널: 제도 반폭(㉮ = body.chestGirthM/4 + WIDTH_EASE_M) **27.50cm** ·
몸통 완성 폐둘레 110.00cm · 몸 가슴둘레 84.01cm · 여유 25.99cm(배율 1.309) ·
**㉯ fixture layout.widthM 55.00cm**(= 반폭 환산 27.50cm) — ㉮ = ㉯(항등 상태) ·
**㉯를 읽는 물리·판정 소비처 0곳** · **안 바꾸는 소비처**: v1 제품(Garment.tsx:643) ·
UI 착용판정(garmentFitLimits) · v1 하네스(buildGarmentSim·spikeDressing·paramSweep)
```

**항등 상태에서 ㉮ = ㉯ = 27.50cm** — **「다르게 나오는 자리」는 없었다.**

**회귀**: 이 계기는 인쇄 전용이고 전 채널이 기준선 A와 **비트 동일**이다(위 헤더 회귀 절).

---

## §3 캡처 — **0장 · 사유 등재**

Chrome 자동화 권한은 **이번에 통과**했으나 **다른 단계에서 실패**했다. 실패 원문:

```
[capture] screencapture 실패(front): could not create image from rect
[capture] 화면 잠김/디스플레이 절전이면 실패한다. 화면 기록 권한도 확인할 것.
```

104회차의 실패 원문은 달랐다:

```
[capture] Chrome 제어 권한이 필요하다 — 시스템 설정 > 개인정보 보호 및 보안 >
자동화 에서 터미널의 Google Chrome 항목을 켤 것.
```

⟹ **두 회차의 사유가 «다르다»** — **104 = 자동화 권한** · **105 = 화면 기록 / 화면 상태**.
⟹ **사용자 조치 대기 2건**(104 §7 ㅂ **유지** + **105 신규**).

현 상태의 dress-state md5가 **기준선 A와 같으므로**
`docs/captures/2b-102-일관적용/R0/` 5장이 **같은 상태의 자산**이다
(**새로 찍은 것이 아니다 · 자체 화면 판정 0**).

### 계기 증분 (인쇄 전용 · 판정 로직 0줄 · 커밋 `8036dfd`)

```
dressPattern.ts  [dress] 폭 채널 — ㉮/㉯ 값과 그 차 · ㉯를 읽는 물리·판정 소비처 0곳 ·
                 안 바꾸는 소비처 3계열을 매 실행 인쇄
```

---

## 실행한 것 / 실행하지 않은 것 (미집행을 침묵으로 넘기지 않는다)

**실행한 것**

```
§0   전략 세션 등재 3건 수령(104 §7 갈림 처분 · 후보 등재 · 100 §1 절차의 폭 적용)
§1   폭 소비처 **전량 정리** — 검색 11지점 · 출처 분류표 ㉮/㉯/㉰ · 갈림 6곳 확정 ·
     앵커 36개 추적 · 34게이트 비교 대상 확정 + **구조적 상계**
§1   사전등록 판정 → **판정 불가 · 정지**(갈래 이름으로 답할 수 없다)
§2   폭 채널 계기 신설·실행(`8036dfd` · 인쇄 전용) — ㉮ = ㉯ = 27.50cm
§3   캡처 **시도** → 실패(사유 등재)
회귀  `dress:pattern` 1회 · `npx tsc -b` 통과 · 전 채널 기준선 A 비트 동일
```

**실행하지 않은 것 — 사유 병기**

```
**2단계 재시도 · e 변경 · 값 스윕**   **0** — §0(1) ㄴ 처분(소비처 정리 «전» 재시도 금지) ·
                                    [[../함정/함정14-기준선의존]] §경계선 조건 ②
**앵커 배선 변경 · 로그 문언 수정**   **0** — 회차 금지 조항(§1-3은 **등재만**)
**`g.widthM` 죽은 입력의 처분**      **0** — **정리까지가 정의역**이다
**fixture 재굽기 · `layout.widthM` 변경**  **0** — 값 변경 0
**기전 귀속 · 기전 후보**            **0** — 34게이트 throw의 기전을 묻지 않았다(회차 조항)
**§0(2) 후보의 «기각» 선언**         **0** — 갈래가 성립하지 않았다(위 §1 판정)
**캡처**                            **0장** — §3(환경 · 사용자 조치 대기)
**폭 축 종결 등재 · 기준선 A′ 수립 · 태그**  **0**
**MARGIN_ALL 적용 · `COLLISION_MARGIN` 변경 · 물리**  **전부 0**
목선(3순위) · 소매 축(4순위 · 종결) · 와인딩 72건 · 수밀성 감사 · 감사 재계산 6건
                                    **전부 미착수 유지**
```

---

## 산출 불가 / 확인 불가 (metrics 블록의 같은 절 **전량**)

```
**§1 갈래 판정**            A·B·C 어디에도 해당하지 않는다. **판정 불가 · 정지**
**§0(2) 후보의 기각 선언**  판별 조항은 「아니오」로 답이 나왔으나 **갈래가 성립하지 않았으므로
                          기각을 선언하지 않는다**(사후 갈래 신설 0)
**104 throw의 기전**       귀속하지 않았다. 확정된 것은 **「배치 사상이 만들 수 있는 값이 아니다」**와
                          **「44.3mm는 배치 상태의 seamGap이 아니다」** 두 **코드 사실**까지다
**캡처**                   **0장**(§3)
**`PINDRESS` off에서 하드 앵커가 발화하는 구조**   **등재만 · 배선 변경 0**(회차 금지 조항)
**`g.widthM` 죽은 입력의 처분**                  하지 않았다(정리까지가 정의역)
**`PatternPreview` 슬라이더가 미리보기 폭을 못 바꾸는 사실**   **등재만**
**목선 · 소매 축 · 와인딩 72건 · 수밀성 감사 · 감사 6건 · 고정점 · A′**   전부 **미착수 유지**
기점                       **없음**(기준선 A 유지 · 9fbd66c 그대로 · 태그 갱신 0)
```

---

## §4 함정 판정 — **자체 판정 3건** (회차 프롬프트 위임 · **신설 0 · 수리 0**)

### 4-1 (가) [[../함정/함정32-소비처부분적용]] — **블록을 «단다» · 문언은 「판별 조항 «아니오»」까지**

**판정 = 추가한다**(「**폭 판본 — 판별 조항 «아니오»**」 블록).

```
왜 다는가   ① 이 함정의 **규범 1(소비처 «전량» 열거 · 바꿀 것/안 바꿀 것을 실행 전에 가른다)을
              이 회차가 «폭»에서 그대로 이행**했고 그 이행 자체가 자산이다(100 §1의 margin 판 →
              105의 폭 판) · ② 102 블록(「전량 적용 결과」)의 선례대로 **이 함정에 붙는 실측을
              같은 자리에 모아 둔다**
문언의 한계  「이 사례에서 **판별 조항이 «아니오»로 나왔다**」까지다.
            **「폭 판본이 아니다」로 단정하지 않는다** — 갈래가 성립하지 않아
            **기각을 선언하지 않았다**(§1 판정)
새 국면      **「갈림은 있는데 «값을 넘기는» 갈림이 0곳」** — 규범 1은 소비처를
            「바꿀 것 / 안 바꿀 것」으로 갈랐고, 여기서는 그 «안 바꾸는» 쪽이
            **죽은 입력 · off · 인쇄 전용**이었다. **사실 등재만 · 규범 문언 수정 0 ·
            규범 승격 0**(102 §5-1의 「승격 보류」 자리와 같은 처리)
안 하는 것   **폐기 0 · 규범 1~4 문언 수정 0 · 기각 선언 0 · 문턱 0 · 신설 0**
```

### 4-2 (나) [[../함정/함정17-반납된전제]] — **사례 등재**(`PINDRESS` off ↔ 하드 앵커 발화)

**판정 = 등재한다.** **33회차 「더 나쁜 두 번째 층」과 같은 시그니처**이고,
**같은 코드 쌍이 v2 착장 경로(`dressPattern.ts`)에서 그대로 살아 있다**는 것이 새 사실이다.

```
사실        소프트 `:1886 anchors: () => (PINDRESS ? anchorList : [])`는 **꺼진다** ·
            하드 `:2051-2093 setAnchorHard`(→ `sim.pinned[a.i] = 1` · `setParticle`)는
            **`PINDRESS`를 참조하지 않는다**
계기 층위    로그 문구 `:497` 「**착장 앵커 off(기본)**」가 **소프트 경로만** 가리킨다 —
            **스위치 하나를 끈 것이 전 경로를 끈 것이 아니다**(함정 17 규범 「진입점 grep」)
[[../함정/함정12-계기하드코딩]] 계열   **부수 대조** — 100회차 규범(「소비처가 갈리면 갈린 값을
            **전부 병기**한다」)의 **앵커 판 미이행**이다(로그가 두 경로 중 **하나만** 인쇄).
            **사례 «후보» · 확정 0 · 오염 산출물 0**(이 회차 판정에 그 로그 줄이 실린 것 0건)
[[../함정/함정13-jitter]] 계열        **대조만** — 「이름이 계산되지 않는 것을 주장한다」 시그니처와
            같은 줄이나, **대상이 계기 채널이 아니라 «상태 인쇄 문구»**다.
            **사례 항목 신설 0 · 규범 문언 수정 0**
처분        **등재만 · 배선 변경 0 · 로그 수정 0 · 규범 신설 0.**
            어느 쪽이 옳은 기본값인지 **판정하지 않는다** — 앵커 층위를 여는 회차 몫이다
```

### 4-3 (다) 「갈래 설계가 실측 상태를 예상하지 못해 판정 불가」 — **[[../함정/함정30-창폭과창위치]] 계열 · 사례 등재 · 신설 0**

**계수부터 볼트로 확인했다 — 프롬프트의 「94·95·96에 이은 네 번째」는 볼트 기록과 갈린다.**

```
94 §2(가)  갈래 술어 **ㄱ·ㄷ 동시 충족**(크기 조건 ∧ 크기 하한 없는 형태 조건) → **판정 불가**
           → **함정30 사례 2 · 규범 ②**로 등재돼 있다
95 §3      값역이 **천장 상수 7.20mm에 포화 · 8/10 동점** → **판정 불가**
           → **함정30 «아님»**으로 판정돼 [[../함정/함정13-jitter]] **규범 6(값역)**에 갔다(95 §9-2)
96 §4      흡착 **평형점 포화** → **갈래 D로 «답했다»**(판정 불가가 아니다) ·
           분류도 **함정30 «아님» · 규범 6(값역)**이다(96 §7-2)
⟹ 계수    「실행이 있었고 갈래 이름으로 답할 수 «없었던»」 회차 = **94 · 95 · 105 = 세 번째** ·
           **함정30 «사례» 계열로는 94 §2에 이은 두 번째**.
           **97·98·99는 실행 자체가 없어 «미발화»**이고 이 계수에 넣지 않는다.
           **과거 문언 수정 0 · 계수 신설 0**(대장 번호 부여는 전략 세션 몫)
```

**분류 판정 = 함정 30.**

```
30의 시그니처   **판별력 0이 «아니다»** — 채널은 정상 산출됐고(갈림 **6곳**이 세어졌고,
                배치·앵커 경로 판정도 **「아니오」로 확정**됐다) 실행마다 다른 값이 난다.
                실패는 **값이 아니라 «창의 설계»**에 있다 → 105가 정확히 이 시그니처다
규범 ②의 «다른 국면»  규범 ②는 「갈래 술어가 값역을 **분할**하는지 확인한다」이고,
                94는 **중첩**(두 갈래 동시 성립) · **105는 «빈틈»**(어느 갈래도 안 덮는 상태)이다.
                분할은 **중첩 없음 ∧ 빠짐 없음**을 둘 다 요구하므로 **같은 규범의 반대쪽 국면**이다 —
                **규범 ①②③ 문언 수정 0 · 규범 신설 0**
왜 값역(규범 6)이 아닌가  **값역 확인을 100% 해도 남는다.** 갈림 소비처 수의 값역은 0~11이고
                실측 6은 그 «안»이며, 배치·앵커 포함 여부는 이산 2값인데 **둘 다 정상 산출**됐다.
                답이 «고정»된 것이 아니라 **답이 나왔는데 갈래가 그 답을 안 덮었다**
[[../원리/사전등록]] 계열 대조
  규범 2·3      **가장 가까우나 대상이 다르다** — 2·3은 「**후보 기전**과 판별 계기의 매핑을
                전부 덮어라」이고, 105에서 빠진 것은 후보가 아니라 **«실측 상태»**다.
                2·3을 100% 지켜도(후보마다 판별자 명시) 이번 빈틈은 남는다
  규범 12       **방향이 반대다** — 12는 갈래 «안»의 조건이 서로를 배제하는 자기모순이고,
                105는 **각 갈래는 만족 가능한데 갈래 «사이»에 빈틈**이 있다
  규범 15       **계열은 같으나 대상이 다르다** — 15는 **원인 열거**의 완전성이고,
                105는 **갈래 열거**의 완전성이다. **규범 신설 0 · 문언 수정 0**(자리만 남긴다)
처분            **함정30에 사례 블록 추가 · 신설 0 · 규범 문언 수정 0 · 문턱 0 ·
                과거 판정 뒤집기 0.** 얼마의 열거면 완전한지는 **그 갈래를 등록하는 회차가
                실행 «전»에** 도출한다(규범 4·5)
```

---

## §5 등재

```
[[../함정/함정32-소비처부분적용]]  **「폭 판본 — 판별 조항 «아니오»」 블록**(기각 선언 0 ·
                                「폭 판본이 아니다」로 단정 0 · 규범 1~4 문언 수정 0)
[[../함정/함정17-반납된전제]]     **사례 추가** — `PINDRESS` off ↔ 하드 앵커 발화 ·
                                로그 문구가 소프트 경로만 가리킨다(**배선 변경 0 · 로그 수정 0**)
[[../함정/함정30-창폭과창위치]]   **사례 추가** — 갈래 열거의 «빈틈»(규범 ② 분할의 반대 국면) ·
                                **계수 확인**(94·95·105 = 세 번째 · 95·96은 이 함정 «아님») ·
                                **신설 0 · 규범 문언 수정 0**
[[../원리/사전등록]]              **갱신 0** — 규범 2·3·12·15와 **대조만**(문언 수정 0 · 번호 신설 0)
[[2b-104-폭도출채택]]             **§7 갈림 처분표**(105 §0(1) 전략 세션 등재분) +
                                **105 결과 백링크**(34게이트 구조적 상계 · throw 자리 재질문 ·
                                **104 원문 수정 0 · 판정 뒤집기 0 · 수치 수정 0**)
[[../00-현재상태]]                **몸통 폭 축** — 소비처 정리 완료 · **갈림 6곳 전부 죽은 입력** ·
                                **판정 불가·정지** · **축 종결 아님** · 34게이트 구조적 상계 ·
                                `PINDRESS` off ↔ 하드 앵커 · **캡처 환경 제약 2건**(사유가 다르다)
[[../전략세션-인수인계]]           항목 **2·3·5 누적** · **1·4·6 교체** ·
                                **항목 2에 «폭 축»을 넣지 않는다** ·
                                기준선 **「A 유지 · A′ 미수립」 유지** ·
                                raw URL 절은 **105 커밋 뒤 `npm run report`**
[[../규칙/화면판정]]              **갱신 0** — 캡처 0장 · 자체 화면 판정 0
감사 노트                        **행 전환 0 · 재계산 0건**(정의역 밖)
```

---

## §6 다음 수 — **갈림만** (제안 금지 · **처방 0 · 기전 후보 0 · 사후 갈래 신설 0 · e 제시 0**)

**이 회차가 도달한 자리: 폭 소비처는 «전량 정리됐다». 갈림은 6곳 실재하나
«판정·물리에 값을 넘기는» 갈림은 0곳이고, 사전등록 갈래는 그 상태를 덮지 않아
«판정 불가»로 정지했다. 축은 닫히지 않았다.**

```
ㄱ  **폭 축을 계속 열어 둘 것인가** — **종결 등재 0.** 1단계 성립분은 코드에 그대로 있다
ㄴ  **§0(2) 후보의 처분** — 판별 조항은 「아니오」다. **기각을 선언할 것인가 · 다른 판별 조항으로
    다시 물을 것인가**가 갈림이다(105는 **선언 0**)
ㄷ  **104 throw의 «자리»를 다시 물을 것인가** — §1-4가 확정한 것은
    **「배치 사상은 그 값을 만들 수 없다」**와 **「44.3mm는 배치 상태의 seamGap이 아니다」**다.
    ⟹ **「배치 단계에서 던졌다」는 라벨이 가리키는 자리가 «배치»가 아닐 수 있다** —
    이것이 다음 질문의 **재료**다. **기전 귀속 0 · 후보 0 · 이 회차는 묻지 않았다**
ㄹ  **2단계를 다시 물을 것인가** — 다시 물면 그 회차가 **e·문턱·갈래를 실행 «전»에** 확정한다
    (규범 4·5) · **함정14 §경계선 조건 ②가 그대로 걸린다**
ㅁ  **갈래 설계의 처분** — 이번 빈틈을 근거로 **다음 등록문이 열거의 완전성을 어떻게 보이는지**가
    갈림이다(**105는 사후 갈래 신설 0** · 문턱도 박지 않았다)
ㅂ  **`PINDRESS` off ↔ 하드 앵커** — 배선을 바꿀지 · 로그 문언을 고칠지 · 그대로 둘지.
    **105는 등재만**(어느 쪽이 옳은 기본값인지 판정 0)
ㅅ  **`g.widthM` 죽은 입력 · `PatternPreview` 슬라이더 무반영** — 처분은 **채택 회차 몫**
ㅇ  **캡처 환경 제약 2건** — **사유가 다르다**(104 자동화 권한 · 105 화면 기록/화면 상태) ·
    **사용자 조치 대기**
ㅈ  **화면 서열** — **96 §0-3분 유지**(갱신 0) · 캡처 0장 · 자체 화면 판정 0
```

**다음 수의 «선택»은 전략 세션 몫이다.**

---

## 링크

- 이전 회차: [[2b-104-폭도출채택]] — **§7 갈림 처분표**(105 §0(1)) · **105 결과 백링크**
  (34게이트 구조적 상계 · throw 자리 재질문 · **104 원문 수정 0**)
- 관련 회차: [[2b-103-몸통폭도출]](폭 축 재개 · 도출 A~E) ·
  [[2b-102-일관적용]](소비처 «일관 적용»의 margin 판) ·
  [[2b-100-margin선행이행]](**소비처 전량 열거 절차의 원형** — 105 §0(3)) ·
  [[2b-101-margin도출]](부분 적용 · 함정32 신설 회차)
- 함정: [[../함정/함정32-소비처부분적용]](**폭 판본 — 판별 조항 «아니오»** · 기각 선언 0) ·
  [[../함정/함정17-반납된전제]](**`PINDRESS` off ↔ 하드 앵커**) ·
  [[../함정/함정30-창폭과창위치]](**갈래 열거의 빈틈** · 규범 ② 반대 국면) ·
  [[../함정/함정12-계기하드코딩]](**사례 후보** — 갈린 소비처 병기의 앵커 판) ·
  [[../함정/함정13-jitter]](**대조만** · 사례 신설 0) ·
  [[../함정/함정14-기준선의존]](§경계선 조건 ② — e 재시도 0) ·
  [[../함정/함정26-기준선재생성]](해시 3종 · dress-state md5 불변)
- 원리: [[../원리/사전등록]](**규범 18 확인 범위 확장분 이행** · 규범 2·3·12·15 **대조만** ·
  **갈래 이름으로 답할 수 없다 = 판정 불가**)
- 규칙: [[../규칙/화면판정]](**갱신 0** · 자체 화면 판정 0)
- 관련: [[../00-현재상태]] · [[../전략세션-인수인계]] · `docs/metrics-log.md`(커밋 `5a2c7bb`)
