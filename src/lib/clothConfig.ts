// GarmentCloth(메인 스레드, 지오메트리/입력 처리)와 clothWorker(물리 연산)
// 양쪽이 동일한 값을 봐야 하므로 한 곳에 모아둔다.
//
// 실제 마네킹 메시(Xbot, BVH) + 자체충돌 기반 아키텍처. 오늘 한때 "웹
// 최적화 게임형"으로 캡슐 2개짜리 근사 충돌 + 자체충돌 끄기로 전면
// 개편했었는데, 캡슐은 완벽한 원통이라 옷이 매끈한 튜브/판초처럼 보이고
// 실제 몸 굴곡을 못 따라가는 근본적 한계가 있었다(실측 확인 — "옷을 입은
// 게 아니라 그냥 걸친 느낌"). CLO3D에 준하는 자연스러운 드레이핑을
// 위해서는 실제 몸 곡면과 자체 접힘/주름이 필요해 다시 이 구조로
// 돌아왔다. 캡슐(torsoCapsule.ts)은 완전히 버리지 않고, BVH가 탐지 반경
// 밖으로 놓쳐 파티클이 몸속에 갇히는 것을 구조하는 보조 안전망으로만
// 남겨둔다.

// 46번(전면 재설계 — 통합 단일 패널): 사용자가 "소매를 별도 원통으로
// 만들지 말고 몸판과 이어진 하나의 넓은 천으로 만들어 달라"고 명시적으로
// 요청했다(AskUserQuestion으로 위험을 설명하고 확답을 받음). 기존에는
// 몸판(앞/뒤)과 소매(좌/우)가 별개 패널이라 그 경계(암홀 시접)가 아무리
// 다듬어도 완전히 안 감춰졌는데, 이제 앞판/뒤판 자체를 어깨 폭보다 훨씬
// 넓게(소매 끝까지 닿도록) 만들어 소매를 "같은 천의 연장"으로 만든다 —
// buildGarmentSim.ts의 layoutTorsoPanels 재작성 참고. 그만큼 가로(COLS)
// 해상도도 더 필요해(같은 열 개수로 훨씬 넓은 폭을 표현해야 목선 곡선이
// 다시 성기게 보이지 않도록) 30→44로 올렸다.
//
// 46번 실측(추가 — 각진 갑옷 모양, 그리고 되돌림): 목선 찢어짐을 고친 뒤
// 실측(스크린샷)해보니, 소매 구간이 큰 평평한 삼각형 몇 개로만 이뤄져
// 있어 각지고 뻣뻣한 "갑옷 어깨보호구"처럼 보였다 — 처음엔 정점 밀도
// (삼각형 크기) 부족이 원인이라 보고 COLS/ROWS를 60/36으로 올렸는데
// (입자 수 약 1.75배), 실측으로 다시 확인해보니 해상도를 올려도 각짐이
// 거의 그대로였다 — 진짜 원인은 밀도가 아니라 형태 수식 자체(ARM_ANGLE_MAX
// 가 너무 넓게 감싸던 것)였다는 게 이후 실측으로 밝혀졌다(buildGarmentSim.ts
// 참고). 즉 이 해상도 증가는 애초에 문제를 해결한 게 아니었는데 물리
// 연산량만 1.75배로 늘려놓은 상태였다 — 프레임 드랍 실측(순수 물리
// 계산만으로 프레임당 17.5ms, 60fps 예산의 대부분을 이미 소진)을 계기로
// 원래 값(44/28)으로 되돌렸었다.
// 46번 실측(재시도 — 순수 밀도 실험, 다시 롤백): 이번엔 형태 수식이
// 아니라 순수 밀도(1.5배, 입자 수 약 2.25배)만 실측해봤는데, 워커에
// 타이머를 심어 재보니 프레임당 89~105ms(약 9~11fps)로 옛날 1.75배
// 실험 때(17.5ms) 실패 사례보다도 훨씬 나빴다 — 이번 세션에 추가한
// 자체충돌/넥라인 안착/토르소 랩핑/어깨 둥글리기 등 여러 물리 로직이
// 전부 해상도에 비례해 같이 늘어난 탓으로 보인다. 60fps 예산(16.67ms)의
// 6배 넘게 초과해 실사용이 불가능한 수준이라 원래 값(44/28)으로
// 되돌린다.
export const COLS = 44;
export const ROWS = 28;
export const PARTICLES_PER_PANEL = COLS * ROWS;

// 46번(전면 재설계): 소매가 더 이상 별도 패널이 아니므로 몸판(앞/뒤) 2개
// 패널만 남는다.
export const PANEL_FRONT = 0;
export const PANEL_BACK = 1;

// 범위 B(소매 재설계 — 별도 패널): 소매를 몸판과 독립된 패널로 되돌린다.
// docs/sleeve-redesign-B.md 결정 참고 — 둘레(cols) 12정점 × 길이(rows)
// 12행, 좌우 소매 동일 해상도.
export const PANEL_SLEEVE_LEFT = 2;
export const PANEL_SLEEVE_RIGHT = 3;
export const SLEEVE_RING_COLS = 12;
export const SLEEVE_RING_ROWS = 12;

// 46번: 0번 행(어깨선) 중 이 비율(중심 기준 절반 폭 대비) 안쪽만 몸/목선에
// 고정(pin)한다 — 그 바깥(소매 쪽으로 뻗은 부분)은 사용자 요청대로 핀을
// 완전히 풀어 중력만 받게 한다. 기존 어깨 핀 폭(SHOULDER_PIN_OUTSET 등
// 반영된 실제 어깨너비)과 맞아떨어지도록 layoutTorsoPanels가 계산하는
// "어깨 실측 반폭 / 전체 반폭" 비율을 그대로 쓴다 — 고정값이 아니라
// buildGarmentSim.ts에서 동적으로 계산된다(TORSO_PIN_FRACTION은 그
// 계산에 쓰는 게 아니라 참고용 문서화 상수일 뿐, 실제로는 shoulderHalfWidth
// 값 자체를 기준으로 판별한다).
export const ARM_ROWS = 9;

export const SUBSTEP_DT = 1 / 60;
// 워커가 한 번에 몰아서 따라잡을 수 있는 서브스텝 상한. 너무 크게 잡으면
// 워커 자신이 느려졌을 때 처리 시간이 계속 늘어나는 악순환에 빠진다.
export const MAX_SUBSTEPS = 1;

// 47번(실측): 이전 값(-2.2)은 근거 없이 낮춰진 값이었다 — 왜 실제 중력
// (-9.81)이 아닌 값을 썼는지 기록이 없었다. 수렴 여부를 직접 재봤다
// (garmentWorker에 임시 계측 코드를 넣어 업로드 직후부터 30초간 1초
// 간격으로 전체 파티클의 프레임 간 평균/최대 |Δp|를 기록, 측정 후 제거):
// 정착 후(3초 이후) 평균 |Δp|가 -9.81에서는 0.1mm대, -2.2에서는 0.2mm대로
// 나왔다 — 실제 중력 쪽이 오히려 더 안정적으로 수렴했다. max|Δp|에 몇 초
// 간격으로 나타나는 스파이크(수 mm~20mm대)는 미수렴 징후가 아니라
// Mannequin.tsx의 8초 주기 팔 오실레이션 애니메이션 때문에 소매 쪽 일부
// 파티클이 그 리듬을 따라 계속 재조정되는 것이다.
// 주의: 이 측정은 "안정성"(값이 발산하지 않고 정착하는지) 검증이지
// "드레이프가 실제로 맞는지" 검증이 아니다 — 별개로 발견된 문제(앞판
// 소매가 팔 캡슐 뒤쪽에 위치하는 것으로 보임)는 여전히 미해결 상태다.
// 근거 없이 이 값을 다시 낮추지 말 것 — 낮추려면 이번처럼 실측 근거를
// 남길 것.
export const GRAVITY_BASE: readonly [number, number, number] = [0, -9.81, 0];

// 메시 충돌 검사(BVH closestPointToPoint)는 구체/캡슐보다 비싸서, 매
// 반복이 아니라 몇 번째 반복마다(+ 마지막 반복엔 항상) 수행한다.
//
// 46번 실측(프레임 드랍): 워커에 직접 타이머를 심어보니 프레임당 88ms 중
// 87ms가 물리+충돌 구간이었고, 그중 상당 부분이 이 BVH 탐색 호출 횟수
// 자체(반복마다 최대 7회/서브스텝 × 서브스텝 2개)에서 나왔다. 소매 열을
// 이 충돌 대상에서 뺀 것(bvhFromArrays.ts의 columnRange)만으로는 부족해,
// 검사 주기 자체도 3→6으로 늘려 호출 횟수를 추가로 줄인다 — 접촉이
// 급격히 바뀌는 상황이 아닌 한(MAX_DISPLACEMENT_PER_SUBSTEP이 이미
// 한 서브스텝당 이동량 자체를 제한해줌) 몇 반복 건너뛰어도 관통이 심하게
// 누적되지 않는다는 게 실측 전제 — 실측으로 침투/찢어짐 재발 여부 확인 필요.
export const COLLISION_EVERY = 6;
// 표면에서 얼마나 떨어뜨려 놓을지(옷감 두께 근사치) — BVH 밀어내는 거리.
export const COLLISION_MARGIN = 0.015;
// BVH closestPointToPoint의 maxThreshold(탐지 반경). COLLISION_MARGIN과
// 분리해 넉넉하게 잡는다 — closestPointToPoint는 maxThreshold보다 먼
// 표면점은 "못 찾은 것"으로 취급해 파티클이 전혀 밀려나지 못하므로,
// 제약/중력이 한 서브스텝 만에 margin 밖으로 밀어넣은 파티클도 다음
// 충돌 검사에서 다시 붙잡아 표면 밖으로 구해낼 여유가 필요하다.
// bvhFromArrays.ts의 PUSH_RELAXATION(under-relaxation)과 짝을 이루는
// 값이라, 탐지 반경을 넓게 잡아도 한 번에 확 스냅되지 않고 여러 반복에
// 걸쳐 서서히 끌려오므로 구조 제약이 저항할 시간을 번다.
export const COLLISION_DETECTION_RADIUS = 0.15;
// 한 서브스텝 동안 파티클이 이동할 수 있는 최대 거리. 충돌 노멀이 절단면
// 근처에서 튀거나 제약끼리 힘겨루기를 해도, 이 이상 한 번에 움직이지
// 못하게 강제로 잘라내(clamp) 순간적인 반발력으로 옷감이 찢어지듯 튀는
// 현상을 막는 안전장치. 60fps 기준 5cm/프레임은 초속 3m로, 정상적인
// 처짐 속도보다 훨씬 빨라 진짜 발산 상황만 걸러낸다.
export const MAX_DISPLACEMENT_PER_SUBSTEP = 0.05;
// 앞판/뒤판 파티클끼리 이보다 가까워지면 밀어낸다. 옆선 시접(SEAM_REST_LENGTH,
// 6mm)보다 반드시 작아야 시접 제약과 자체충돌이 서로 힘겨루기를 하지
// 않는다 — 요청받은 1.5~2cm는 이 제약보다 커서(시접 목표 거리 6mm를
// 자체충돌이 계속 "너무 가깝다"며 밀어내려 함) 그대로 못 쓴다. 시접보다
// 확실히 작은 선에서 기존(4mm)보다만 올린다.
export const SELF_COLLISION_MIN_DIST = 0.0055;
// 옆선(사이드심) 시접의 목표 간격.
export const SEAM_REST_LENGTH = 0.006;

// 드레이프 개선 A안(A-②): 제약 종류별 목표 강성. step()이 iteration 수로
// 보정(k'=1-(1-k)^(1/n))해 적용하므로 "반복이 많으면 유효 강성이 도로
// 1에 붙는" 문제 없이 이 값이 곧 프레임당 유효 강성이다(clothPhysics.ts
// setKindTargetStiffness 참고). 표준 천 시뮬은 bend를 stretch 대비
// 0.01~0.1로 두는데 기존엔 셋 다 1.0이라 2칸 거리 제약이 "펴진 상태"를
// 100% 강도로 유지 — 44×28 셀(~2.5×3cm)보다 작은 곡률의 접힘이 아예
// 불가능해 큰 평면 폴리곤이 각지게 굳는 원인이었다(A-① 조사).
// 1 초과 금지 — docs/pattern-redesign.md 13번(stiffness>1 카오스적 불안정).
export const STIFFNESS_STRUCTURAL = 1.0;
// A-③(0.6) 원복: 12콤보 전 지표 개선/불변이었는데 화면은 참사 — 네 각도
// 모두 어깨 천이 몸에서 떨어져 등판 노출(두 조합 실측 동일). 전단 저항이
// 약해지면 천이 어깨 곡면을 감싸는 형태를 유지 못 하고 미끄러진다.
// 이 실패가 bodyGapMm 지표(drapeMetrics.ts) 신설 계기 — A-② 128mm대 vs
// A-③ 124~160mm 대조 참고. 다시 낮추려면 bodyGapMm 회귀 확인 필수.
export const STIFFNESS_SHEAR = 1.0;
export const STIFFNESS_BEND = 0.1;
export const STIFFNESS_SEAM = 1.0;

// M2(SDF 마찰). 복셀 2cm — 몸통 굴곡(어깨 캡 돔 반경 수 cm)을 담을 만큼
// 촘촘하면서 굽기 비용(수만 회 BVH 질의, rebuild 시 1회)이 감당되는 선.
// 팔은 여전히 해석적 캡슐이 담당한다(4.56cm 반경 팔은 2cm 복셀로 부정확).
export const SDF_VOXEL = 0.02;
export const SDF_FAR = 0.3;
// 접촉 판정 폭 — COLLISION_MARGIN(1.5cm)보다 살짝 넓게 잡아, 밀어내기가
// 목표 거리에 안착한 파티클도 접촉으로 계속 인식되게 한다.
export const FRICTION_CONTACT_BAND = 0.02;
// 계수는 실측이 아니라 눈대중 초기값 — 면(cotton) 대 인체 마찰이 대략
// 0.3~0.6대라는 통념에서 시작. 게이트 수치·화면 판정으로 조정될 값이다.
export const FRICTION_MU_STATIC = 0.6;
// M2-3: SDF 밀어내기의 under-relaxation — bvhFromArrays.ts의
// PUSH_RELAXATION(0.4)과 같은 값·같은 이유(한 번에 다 풀면 구조 제약이
// 저항하기 전에 옷이 표면으로 수축한다).
export const SDF_PUSH_RELAXATION = 0.4;
export const FRICTION_MU_KINETIC = 0.4;
// M2-5(반복 안 마찰) 전용 μ — 서브스텝 말미 속도 패스의 μ(위)와 별개.
// 반복 안 운동 감쇠는 반복 수(18)만큼 누적되므로 훨씬 작아야 한다.
// 스윕 실측(0.02/0.05/0.1): 0.05가 coverage 9.2%(역대 최저)·maxStrain
// 3.57(-15%)·jitter 12.67(기준선 12.37 동등)로 세 기준 동시 충족.
// 0.1은 strain이 더 내려가나(3.33) jitter가 오르기 시작(14.23, +15%).
export const FRICTION_MU_ITER = 0.05;
// 핀 전환(화면 판정 대기): 1=하드 핀. 반복 안 마찰(μ_iter)이 하중을 받게
// 된 뒤의 재스윕에서 0.5가 후보 — bodyGap 어깨 mean -21.3→-10.6(절반),
// 면각평균 15.2→19.5(+28%), maxStrain 4.155(기준 4.2 이하 유지),
// jitter 13.26(동등). 비용: coverage 10.1→25.5%(+15.4pp — 이전 핀
// 스윕의 +25.6pp보다 크게 줄었지만 여전히 문턱 밖이라 화면 판정 필요).
// 원복(화면 판정 실패): PIN 0.5에서 등 상단 양쪽(견갑골) 노출 —
// A-③/C 실패 부위 재현(/tmp/fit-shot-pin05-back.png). 앞면 몸통 드레이프는
// 역대 최고였으나(면각 +28% 실재 확인) 목선 지그재그·소매캡 분리도 동반.
// 재시도 조건: 어깨 mean 고원(-10.5)의 원인인 COLLISION_MARGIN(15mm)을
// 먼저 낮춰 천이 실제로 어깨에 앉을 수 있게 한 뒤.
export const PIN_STRENGTH = 1;
// 겨드랑이 밑부터 밑단까지만 옆선을 잇고, 그 위(어깨~겨드랑이)는 트여 있는
// 암홀(진동)로 남겨둔다 — 별도 소매 패널 없이도 최소한의 팔 통로를 만든다.
// 이 구간이 넓을수록 앞/뒤판이 서로 안 붙잡아 주는 구간도 넓어져서, 뒤판이
// 중력만으로 몸통에서 옆으로 벌어져 뻣뻣한 판처럼 떠 보이는 문제가 실측으로
// 확인됐다 — 실제 티셔츠 암홀 깊이 근사치보다 조금 타이트하게 잡아 상체
// 위쪽에서도 앞뒤가 서로 붙들어 주게 한다. (테이퍼 구간을 늘려 오프숄더
// 처짐을 완화해보려 했지만 실측상 효과가 없어 원래 값으로 되돌렸다 — 단,
// 그때는 halfWidthAtRow의 클램프 버그(buildGarmentSim.ts 참고, shoulderHalfWidth를
// fullHalfWidth로 항상 깎아버리던 버그)가 아직 안 고쳐진 상태라 테이퍼 구간을
// 늘려도 애초에 늘어날 폭 자체가 없었다 — 이번엔 그 클램프를 먼저 없앤 뒤
// 다시 실측해, 몸판 바깥쪽 열이 짧은 소매 이음매 링에서 얼마나 떨어져
// 있는지(Playwright evaluate로 두 메시의 실제 정점 좌표 직접 대조) 재보니
// 0.1(3행, 수직으로 약 7.8cm)일 땐 암홀 시작행(3번)에서 거리가 최대 6cm까지
// 벌어졌다 — 소매 길이(어깨핀에서 손목 방향으로 약 11cm)보다 이 구간이
// 짧아서, 몸판이 소매보다 먼저 가슴 폭으로 좁아져버리는 게 원인이었다.
// 이 구간을 소매 길이에 맞춰 늘리면(0.1→0.18, 5행, 약 13cm) 몸판이 좁아지기
// 전에 소매 전체 길이와 겹칠 시간을 벌어준다.
export const ARMHOLE_ROW_FRACTION = 0.18;
// 초기 배치 시 앞판/뒤판을 중심선에서 이만큼씩 앞/뒤로 벌려 놓는다(대략적인
// 몸통 두께 절반). 물리가 시작되면 충돌/중력으로 실제 몸 표면에 맞게 정착한다.
// 너무 넓게 벌려두면 옆선 시접이 끌어당겨야 할 거리가 길어져 옆판이 몸통을
// 따라 굽지 못하고 뻣뻣하게 뜬 채로 남는 문제가 있어, 몸통 두께 절반보다
// 조금 더 좁게 잡았다.
export const FRONT_BACK_HALF_GAP = 0.05;
// 상단 몇 행에 걸쳐 이 값을 점진 적용하는 램프를 시도했으나,
// 메시 충돌이 row1~5에 상시 작동하는 현재 구조에서는 정착 결과에
// 영향이 없었다(켬/끔 차이 전 구간 1mm 미만, 실측). 다시 시도할
// 이유가 생기면 그 전제부터 확인할 것.

// 몸 실측이 바뀐 뒤 충돌 메시를 다시 굽기까지의 디바운스 시간(ms). 슬라이더를
// 드래그하는 동안 매 틱마다 다시 굽지 않기 위함.
export const REBUILD_DEBOUNCE_MS = 200;

// 46번(전면 재설계): 소매를 별도 원통 패널로 만들지 않으므로 위
// SLEEVE_* 계열 상수(반지름/테이퍼/플레어/행렬 해상도)는 전부 필요
// 없어졌다 — 이제 "소매"는 몸판(앞/뒤) 패널 자체의 넓은 바깥쪽 열이
// 팔 방향으로 뻗어나가는 부분일 뿐이라, 굵기는 반지름 상수가 아니라
// 실제 팔에 대한 충돌(garmentWorker.ts의 팔 캡슐)로 결정된다. 원단별
// 감쇠/반복 횟수는 그대로 fabricPresets.ts를 공유한다.
// 마네킹 팔을 근사하는 충돌 캡슐의 반지름 — 옷감이 팔에 얹힐 표면 두께다.
//
// 46번 실측(실험 — 이 반경 자체는 원인이 아니었음, 단 이 결론은 더 이상
// 신뢰할 수 없음): 소프트 타겟/가중치를 전부 손봐도 어깨 뽕이 픽셀
// 단위로 안 바뀌어서, 이 반경을 1.5cm까지 낮춰 하드 제약 자체를
// 격리해본 적이 있다 — 그때는 반경을 바꿔도 증상이 그대로였다. 다만 이
// 실험은 `SHOULDER_PIN_OUTSET`이 11.5cm(Z-fighting을 위치로 우회하던
// 값 — shoulderPin.ts 47번 항목 참고)였던 시절에 이뤄졌다. 그 뒤
// OUTSET을 6.5cm로 되돌리고 어깨너비 클램프 버그까지 고치면서 핀
// 위치 자체가 달라졌으므로, "1.5cm도 영향 없었다"는 이 결론이 지금도
// 그대로 성립하는지는 알 수 없다 — 재실험 없이는 참고용으로만 남긴다.
//
// 47번(실측): 그동안 "마네킹 이두 대략 굵기(4~4.5cm)"라는 추정치를 써왔다
// — 실제로 잰 적이 없었다. 브라우저에서 실제 앱이 쓰는 BVH(Garment.tsx의
// neckSurfaceBvh, 팔 제외 없는 몸 전체 메시)로 어깨~팔꿈치 중간 높이에서
// 원주 방향 24방향 레이캐스팅을 돌려 직접 쟀다(Node에서 GLB 스키닝을
// 재구현하는 경로는 좌표계 불일치로 이전에 실패했다 — 이 측정 방식은
// 그 문제를 피해간다). 결과: 왼팔 평균 반경 4.063cm, 오른팔 평균 반경
// 4.062cm(사실상 대칭, 신뢰할 만함) — 단 각도별 편차가 커서(최소
// ~2.0cm, 최대 ~5.9cm) 단면이 완전한 원이 아니거나 겨드랑이 쪽 레이가
// 몸통에 맞는 등의 영향이 섞여 있을 수 있다. 평균(4.0625cm)에 5mm
// 여유를 더한 값으로 교체한다 — 옛 6.5cm는 실측치보다 컸던 추정값이었다.
export const ARM_COLLISION_RADIUS = 0.0456;
