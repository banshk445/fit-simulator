// GarmentCloth(메인 스레드, 지오메트리/입력 처리)와 clothWorker(물리 연산)
// 양쪽이 동일한 값을 봐야 하므로 한 곳에 모아둔다.
//
// 실제 마네킹 메시(Xbot, BVH) + 자체충돌 기반 아키텍처. 오늘 한때 "웹
// 최적화 게임형"으로 캡슐 2개짜리 근사 충돌 + 자체충돌 끄기로 전면
// 개편했었는데, 캡슐은 완벽한 원통이라 옷이 매끈한 튜브/판초처럼 보이고
// 실제 몸 굴곡을 못 따라가는 근본적 한계가 있었다(실측 확인 — "옷을 입은
// 게 아니라 그냥 걸친 느낌"). CLO3D에 준하는 자연스러운 드레이핑을
// 위해서는 실제 몸 곡면과 자체 접힘/주름이 필요해 다시 이 구조로
// 돌아왔다. 캡슐(torsoCapsule.ts)은 완전히 버리지 않고, BVH가 탐지 반경
// 밖으로 놓쳐 파티클이 몸속에 갇히는 것을 구조하는 보조 안전망으로만
// 남겨둔다.

// 그리드 해상도: 클수록 주름이 부드럽지만 파티클/제약조건 수가 제곱으로
// 늘어난다. BVH 메시 충돌 + 자체충돌은 비용이 커서, 게임형 아키텍처
// 시절 올렸던 32x32는 성능 여유가 없다 — 오늘 아침 이 조합으로 60fps를
// 실측 확인했던 해상도로 되돌린다.
export const COLS = 22;
export const ROWS = 28;
export const PARTICLES_PER_PANEL = COLS * ROWS;

export const SUBSTEP_DT = 1 / 60;
// 워커가 한 번에 몰아서 따라잡을 수 있는 서브스텝 상한. 너무 크게 잡으면
// 워커 자신이 느려졌을 때 처리 시간이 계속 늘어나는 악순환에 빠진다.
export const MAX_SUBSTEPS = 2;

export const GRAVITY_BASE: readonly [number, number, number] = [0, -2.2, 0];

// 메시 충돌 검사(BVH closestPointToPoint)는 구체/캡슐보다 비싸서, 매
// 반복이 아니라 3번째 반복마다(+ 마지막 반복엔 항상) 수행한다.
export const COLLISION_EVERY = 3;
// 표면에서 얼마나 떨어뜨려 놓을지(옷감 두께 근사치) — BVH 밀어내는 거리.
export const COLLISION_MARGIN = 0.015;
// BVH closestPointToPoint의 maxThreshold(탐지 반경). COLLISION_MARGIN과
// 분리해 넉넉하게 잡는다 — closestPointToPoint는 maxThreshold보다 먼
// 표면점은 "못 찾은 것"으로 취급해 파티클이 전혀 밀려나지 못하므로,
// 제약/중력이 한 서브스텝 만에 margin 밖으로 밀어넣은 파티클도 다음
// 충돌 검사에서 다시 붙잡아 표면 밖으로 구해낼 여유가 필요하다.
// bvhFromArrays.ts의 PUSH_RELAXATION(under-relaxation)과 짝을 이루는
// 값이라, 탐지 반경을 넓게 잡아도 한 번에 확 스냅되지 않고 여러 반복에
// 걸쳐 서서히 끌려오므로 구조 제약이 저항할 시간을 번다.
export const COLLISION_DETECTION_RADIUS = 0.15;
// 한 서브스텝 동안 파티클이 이동할 수 있는 최대 거리. 충돌 노멀이 절단면
// 근처에서 튀거나 제약끼리 힘겨루기를 해도, 이 이상 한 번에 움직이지
// 못하게 강제로 잘라내(clamp) 순간적인 반발력으로 옷감이 찢어지듯 튀는
// 현상을 막는 안전장치. 60fps 기준 5cm/프레임은 초속 3m로, 정상적인
// 처짐 속도보다 훨씬 빨라 진짜 발산 상황만 걸러낸다.
export const MAX_DISPLACEMENT_PER_SUBSTEP = 0.05;
// 앞판/뒤판 파티클끼리 이보다 가까워지면 밀어낸다. 옆선 시접(SEAM_REST_LENGTH)
// 보다 반드시 작아야 시접 제약과 자체충돌이 서로 힘겨루기를 하지 않는다.
export const SELF_COLLISION_MIN_DIST = 0.004;
// 옆선(사이드심) 시접의 목표 간격.
export const SEAM_REST_LENGTH = 0.006;
// 겨드랑이 밑부터 밑단까지만 옆선을 잇고, 그 위(어깨~겨드랑이)는 트여 있는
// 암홀(진동)로 남겨둔다 — 별도 소매 패널 없이도 최소한의 팔 통로를 만든다.
// 이 구간이 넓을수록 앞/뒤판이 서로 안 붙잡아 주는 구간도 넓어져서, 뒤판이
// 중력만으로 몸통에서 옆으로 벌어져 뻣뻣한 판처럼 떠 보이는 문제가 실측으로
// 확인됐다 — 실제 티셔츠 암홀 깊이 근사치보다 조금 타이트하게 잡아 상체
// 위쪽에서도 앞뒤가 서로 붙들어 주게 한다. (테이퍼 구간을 늘려 오프숄더
// 처짐을 완화해보려 했지만 실측상 효과가 없어 원래 값으로 되돌렸다 — 단,
// 그때는 halfWidthAtRow의 클램프 버그(buildGarmentSim.ts 참고, shoulderHalfWidth를
// fullHalfWidth로 항상 깎아버리던 버그)가 아직 안 고쳐진 상태라 테이퍼 구간을
// 늘려도 애초에 늘어날 폭 자체가 없었다 — 이번엔 그 클램프를 먼저 없앤 뒤
// 다시 실측해, 몸판 바깥쪽 열이 짧은 소매 이음매 링에서 얼마나 떨어져
// 있는지(Playwright evaluate로 두 메시의 실제 정점 좌표 직접 대조) 재보니
// 0.1(3행, 수직으로 약 7.8cm)일 땐 암홀 시작행(3번)에서 거리가 최대 6cm까지
// 벌어졌다 — 소매 길이(어깨핀에서 손목 방향으로 약 11cm)보다 이 구간이
// 짧아서, 몸판이 소매보다 먼저 가슴 폭으로 좁아져버리는 게 원인이었다.
// 이 구간을 소매 길이에 맞춰 늘리면(0.1→0.18, 5행, 약 13cm) 몸판이 좁아지기
// 전에 소매 전체 길이와 겹칠 시간을 벌어준다.
export const ARMHOLE_ROW_FRACTION = 0.18;
// 초기 배치 시 앞판/뒤판을 중심선에서 이만큼씩 앞/뒤로 벌려 놓는다(대략적인
// 몸통 두께 절반). 물리가 시작되면 충돌/중력으로 실제 몸 표면에 맞게 정착한다.
// 너무 넓게 벌려두면 옆선 시접이 끌어당겨야 할 거리가 길어져 옆판이 몸통을
// 따라 굽지 못하고 뻣뻣하게 뜬 채로 남는 문제가 있어, 몸통 두께 절반보다
// 조금 더 좁게 잡았다.
export const FRONT_BACK_HALF_GAP = 0.05;

// 몸 실측이 바뀐 뒤 충돌 메시를 다시 굽기까지의 디바운스 시간(ms). 슬라이더를
// 드래그하는 동안 매 틱마다 다시 굽지 않기 위함.
export const REBUILD_DEBOUNCE_MS = 200;

// 소매(팔을 감싸며 늘어지는 원통형 천). 몸판(앞판+뒤판)만으로는 암홀
// 구멍만 뚫려 있고 실제로 팔을 덮는 천이 없어서, 어깨 핀을 아무리 정확히
// 맞춰도 민소매/탱크탑처럼 보이는 문제가 실측(사용자 확인)으로 드러났다
// — 그래서 별도의 원통형 소매 패널을 추가한다. 몸판 시뮬레이션과는 완전히
// 분리된 별도 워커/컴포넌트로 만든다 — 오늘 몸판 쪽에서 겪은 안정성
// 문제들(순서 역전, 잔물결 등)을 고려하면, 소매를 몸판 시뮬레이션에
// 얽어 넣었다가 어렵게 안정화한 몸판 쪽까지 함께 위험해지는 것보다,
// 독립된 작은 시뮬레이션으로 안전하게 분리하는 편이 낫다.
// 팔 둘레 방향 파티클 수(닫힌 원통이라 마지막 열이 첫 열과 다시 이어짐).
// 처음엔 14로 잡았는데, 사용자가 확대 화면에서 "소매가 딱딱한 상자처럼
// 렌더링된다"고 지적했다 — 실측해보니 어깨-소매 사이 "틈"은 이미
// 해결된 상태였고(핀 좌표 대조로 확인), 진짜 남은 문제는 어깨 각도
// 문제가 아니라 소매 자체의 해상도였다: 원통 둘레를 14각형으로만
// 근사하면 각 면이 평평하고 각져서, 옷감이 매끄럽게 접히는 게 아니라
// 저해상도 폴리곤 상자처럼 보인다. 24로 늘려 원통 곡면을 훨씬
// 부드럽게 근사한다(파티클 수는 늘지만 소매는 몸판과 완전히 분리된
// 가벼운 시뮬레이션이라 성능 여유가 충분함 — 실측 60fps 유지 확인).
export const SLEEVE_COLS = 24;
// 반팔은 어깨~위팔 중간까지 짧게, 긴팔은 어깨~손목 근처까지 길게 늘어져야
// 해서 행 수를 다르게 잡는다 — 반팔과 같은 행 수로 긴 길이를 늘리면
// 한 칸 간격이 너무 넓어져(제약이 성기게) 처짐이 뚝뚝 끊겨 보인다.
// SLEEVE_COLS를 늘린 김에 세로 방향도 비례해서 늘려 전체적으로
// 매끈해 보이게 한다.
export const SLEEVE_ROWS_SHORT = 7;
export const SLEEVE_ROWS_LONG = 13;
// 몸판의 암홀(팔 구멍)은 원형이 아니라 어깨 핀 한 점에서 각지게 트여
// 있는 모양이라, 소매 윗단(원형 링)이 정확히 그 각진 경계와 맞아떨어지긴
// 어렵다 — 반지름을 넉넉하게 잡아 소매가 암홀 가장자리보다 살짝 더
// 넓게 겹치도록 해서, 둘 사이에 마네킹 피부가 비쳐 보이는 틈을 가린다.
// 실측(측면 각도로 확인)해보니 암홀 구간(옆선 시접이 아직 시작 안 된
// 0~2번 행)에서 뒤판이 초기 배치 간격(FRONT_BACK_HALF_GAP)만큼 아직
// 충돌로 안쪽에 완전히 당겨지지 않은 채로 남아있는 경우가 있어(앞판
// Z≈0인데 뒤판 Z≈-0.05로 거의 그대로), 그 뒤판 모서리가 소매 반지름
// 밖으로 살짝 튀어나와 보이는 문제가 있었다 — 반지름을 더 넉넉하게
// 잡아 이 여유를 흡수한다.
// 큰 재작업 중 실측(회전된 카메라로 소매 전체를 넓게 잡은 스크린샷)으로
// 발견: 0.1(반지름 10cm=지름 20cm)은 SLEEVE_LENGTH_SHORT(0.14=14cm)와
// 비슷하거나 더 커서, 반팔 소매가 팔을 따라 늘어진 원통이 아니라 어깨
// 위에 얹힌 뚱뚱한 원판/상자처럼 보이는 문제의 실제 원인이었다(이음매
// 블렌딩을 완전히 꺼도 똑같이 보여 blendSeamRing 버그가 아니라 순수
// 반지름/길이 비율 문제로 확인). 0.065로 줄여 팔 굵기(마네킹 이두 기준
// 약 4~4.5cm)에 여유분을 더한 체감 사이즈로 되돌렸다.
//
// 그 직후 사용자가 탑다운(위에서 내려다보는) 각도에서 "여전히 어깨에
// 피부가 비쳐 보인다"고 재지적해, 반지름을 0.08·0.1까지 다시 키워보며
// 재현·비교했지만 틈 크기가 전혀 줄지 않았다(가장자리 블렌딩 강도
// PULL_RADIUS를 0.15까지, 심지어 weight=1 완전 스냅까지 실측해봐도
// 마찬가지 — 자세한 진단은 garmentStitch.ts의 PULL_RADIUS 주석 참고).
// 즉 이 반지름은 그 틈과 무관하다는 게 최종 확인됐다 — 근본 원인은
// 몸판이 평평한 패널 2장이라 마네킹의 둥근 어깨를 애초에 감쌀 수 없다는
// 구조적 한계이고, 반지름을 키우는 건 그 틈을 "가리는" 임시방편일 뿐
// 실제로 닫지 못한다(가려지는 정도도 실측상 무시할 만한 수준이었다).
// 그러니 이 값은 오직 팔 굵기 비례(정면/측면 각도에서 자연스러운 소매
// 두께로 보이는가)만 기준으로 정한다 — 0.065가 그 기준에 가장 부합해
// 최종값으로 확정한다.
export const SLEEVE_RADIUS = 0.065; // 소매 통 반지름(팔 굵기 + 여유분 근사치, 어깨 쪽 기준)
// 이음매(0번 행) 반지름. 몸판 어깨 핀은 "점 하나"인데(평평한 패널의
// 모서리라 그 자체로는 폭이 없다) 소매는 그 점을 완전히 감싸는 원이라,
// 좁은 암홀 위에 넓은 링이 붕 뜬 것처럼 보이지 않으려면 가늘게 잡아야
// 한다. (한때 "몸판과 소매 사이에 피부가 비쳐 보이는 틈"을 이 반지름
// 탓으로 보고 0.045, 0.065, 0.06까지 여러 차례 키워봤지만 거의 변화가
// 없었다 — 그 틈의 진짜 원인은 SHOULDER_PIN_OUTSET 부족 문제였을 때도
// 있었고, 탑다운 각도에서는 몸판 평면-패널 구조 자체의 한계였을 때도
// 있었다. 둘 다 이 반지름과 무관하니 원래 의도대로 가늘게 되돌린다.)
export const SLEEVE_RADIUS_SEAM = 0.02;
// 긴팔은 손목 쪽으로 갈수록 팔이 가늘어지는 만큼 반지름도 줄인다. 처음엔
// 0.048(SLEEVE_RADIUS 대비 3cm 차이)로 잡았는데, 실측(화면 확인)해보니
// 그 정도 차이는 이미지 텍스처 위에서 거의 안 보여서 소매가 손목까지
// 거의 통짜로 굵게 이어지는 것처럼 보였다 — 확 좁아지는 걸 눈으로도
// 알아볼 수 있게 차이를 더 크게 벌려 0.036까지 줄였었다.
//
// 그런데 그렇게 좁힌 값이 실제 마네킹 팔뚝 두께보다 얇아서, 마네킹 팔
// 메시가 얇아진 소매 천을 뚫고 앞에 그려지는(오클루전) 문제가 실측(마네킹을
// 잠깐 숨겨서 소매가 원래 정확한 길이까지 제대로 나오는 것을 확인)으로
// 드러났다 — 반지름/길이 계산 자체는 처음부터 맞았고, 손목 쪽 절반이
// 마네킹 팔에 가려 안 보였을 뿐이었다. 어깨 핀 때(SHOULDER_PIN_OUTSET)와
// 같은 교훈 — 실제 표면 두께와 너무 딱 맞추지 말고 확실한 여유를 둬야
// 한다. 0.05로 늘려 마네킹 팔뚝을 여유 있게 감싸게 한다.
export const SLEEVE_RADIUS_WRIST = 0.05;
export const SLEEVE_LENGTH_SHORT = 0.14; // 어깨에서 소매 밑단까지 길이(반팔)
// 긴팔은 고정 길이가 아니라 실제 팔 길이(어깨~손목) 슬라이더에 비례해야
// 팔 길이를 바꿔도 소매가 항상 손목 근처에서 끝난다 — SleeveCloth에서
// 실측 팔 길이 * 이 비율로 계산한다(손 앞까지 다 덮으면 손등을 뚫고
// 나가 보일 수 있어 살짝 못 미치게 잡는다).
export const SLEEVE_LENGTH_LONG_FRACTION = 0.92;
// 밑단으로 갈수록 살짝 벌어지게(플레어) — 완전한 원기둥이면 압박대처럼
// 보여 자연스러운 소매 느낌이 덜하다. 긴팔은 손목 쪽이라 플레어를 거의
// 주지 않는다(오히려 좁아짐 — SLEEVE_RADIUS_WRIST로 이미 반영).
export const SLEEVE_FLARE_SHORT = 0.12;
export const SLEEVE_ITERATIONS = 14;
export const SLEEVE_DAMPING = 0.97;
