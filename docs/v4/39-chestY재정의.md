# v4-39 — `chestY` 재정의(겨드랑이 합류 사건 · 포즈 불변) · 신구 대조 · A포즈 5행 재산출

**물리 0줄 · `src/` diff = `bodyLevels.ts` 의 `chestY` 유도 «한정» · 굽기 0 · 캡처 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-39-chest-armpit` |
| 기점 | **`342b0dd`**(= v4-38 §1~§4 + 볼트 + 보고) |
| §0 커밋 | **`88a6ba5`**(집행 «전» · 새 규칙 문언 포함) |
| §1~§4 커밋 | (집행 후 기입) |
| 보고 파일 | `docs/v4/보고/39.md` |

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v4-39-chest-armpit 342b0dd → Switched to a new branch 'v4-39-chest-armpit'
       $ git branch --show-current → v4-39-chest-armpit · HEAD 342b0dd
 2호기 (§0 커밋을 push 한 «뒤» 같은 자리로 전환 — §0-6 에 인쇄)
```

---

## §0 수령·사무

### §0-1 판정문 (전략 세션 · v4-38 §4 수령 · 원문)

```
 「갈래 A 수령 · ★ ② 일반화 수용(창 상한 · 근거가 코드에서 · T 6/6) ·
   ★★★ ③ 사실 병기가 제품 결함을 드러냄 — A포즈 chestY(102.95) < waistY(107.59) ·
     「가슴」 이 손끝 높이 ⟹ 이대로 108칸이면 핵심 행이 허구 ·
   ★★ 처분(전략 세션): chestY 의미 = «겨드랑이 사건»으로 재정의 —
     팔이 몸통 고리에 합류하는 높이(CC 가 값으로 지목: A 1.357→1.367 · T 1.311→1.321) ·
     근거 = 의류 실측 관행(가슴둘레는 겨드랑이 높이) + 포즈 불변성 ·
     현행 규칙은 «팔 최저점»을 잡는 규칙이었고 T포즈에서만 우연히 겨드랑이 근처였다 ·
   ★★ T포즈 값 변화 «허용»(의미 정정 · 함정 14 아님 — 위치의 의미를 제품 정의에 맞추는 것 · 값 보기 전 등재) ·
     조건: v3 동결·기존 산출물 불변(이력) · 새 규칙은 v4 경로만 · 신구 차이 값 등재」
```

### §0-2 바꾸지 않는 것 (회차 프롬프트 원문)

```
 물리 **0줄** · `garmentScene`·`dressRun`·워커 굽기 경로 **0** · 정본·blob·문턱 **0** ·
 게이트 채널·문턱 **0** · v4-38 의 허리 창(`yTop`) **유지** · v3 화면·제공 37칸 산출물 **불변**
```

### §0-3 ★ 새 `chestY` 규칙 — **값 보기 «전» 등재**(§1-① 이 이 문언대로 집행된다)

```
 ㄱ **사건** — 「팔이 몸통 고리에 «흡수»되는 높이」. 몸통 고리 = 축점 (x=0, z=AXIS_Z) 을 품는 단면 성분
    (`containsAxis` · 기존 정의 그대로). 겨드랑이 «밑»에서는 그 고리가 몸통만이고, 겨드랑이를 지나면
    같은 고리가 **팔 표면까지 감싼다** ⟹ 그 고리의 볼록껍질 둘레 `C(y)` 가 **불연속으로 뛴다**.
 ㄴ **형식화(손 상수 0)** — 1cm 격자에서 `C` 가 유한한 이웃 쌍 (yᵢ, yᵢ₊₁) 의 «상대 증가»
    `C(yᵢ₊₁)/C(yᵢ)` 가 **최대**인 전이 하나를 고른다(문턱 0 · 극값 규칙 · 허리 최소와 같은 성격).
 ㄷ **이분법** — 그 전이의 두 끝 둘레를 `C_lo`·`C_hi` 라 하고 **그 둘의 중점**(몸에서 뜬 값 · 손 상수 0)을
    술어로 쓴다: `P(y) := C(y) ≥ (C_lo + C_hi)/2`. `P` 가 거짓인 쪽과 참인 쪽 사이를 `TOL_SELF` 까지 좁힌다.
 ㄹ **어느 끝을 쓰는가** — **`chestY` = 거짓 쪽 끝**(= 흡수 «직전» · 겨드랑이 밑) ·
    **`Y_ARM` = 참 쪽 끝**(= 흡수 직후). 기존 코드의 짝(아래 = `chestY` · 위 = `Y_ARM`)을 **그대로** 유지한다.
    `C_chest = at(chestY).C` 이므로 **가슴 둘레는 팔을 포함하지 않는다**.
 ㅁ **못 찾으면** 던진다 — 기존 형식대로 「… — 갈래 B」 문언.
 ㅂ **근거** — ① 의류 실측 관행: 가슴둘레는 «겨드랑이 높이»에서 잰다. ② 포즈 불변: 이 사건은 팔의 각도와
    무관하게 «팔이 몸통에 붙는 자리»다. 구 규칙(성분 1→>1 첫 전이)은 실은 **팔 표면의 최저점**을 집었고,
    T포즈에서만 우연히 겨드랑이 근처였다(v4-38 §1-① 실측: T 전이 성분의 |x| 754.38~754.94 mm).
 ㅅ **바꾸지 않는 것** — `Y_LOW`·`waistY`(v4-38 창) · `armFirst`·`grid`(진단) · 리포트 식 · 게이트.
```

### §0-4 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · 단위 명시)

**★ 단위** — 높이는 **m**(세계 y) · 둘레·리포트는 **mm** · 「구간 안」은 **m 구간**(A 1.357~1.367 · T 1.311~1.321).

| 항 | 갈래 |
|---|---|
| **①** `chestY` 재정의 | **(가)** 두 포즈 산출 성립 + A포즈 `chestY > waistY` + 값이 지목 구간 안 / **(나)** 일부 불성립(값·사유) / **(다)** 산출 불가 |
| **②** 신구 대조(T포즈 M) | 값 표(구·신 · 차이 mm · 대역 소속 변동 정점 수) · 가슴 «행만» 바뀌는지 확인 |
| **③** A포즈 5행 재산출 | v4-38 ③ 표와 나란히 · 눌림·NaN 0 · 게이트 불변 · 워커 결과 JSON 재생성 |

**§4** — A = ①(가)+②③ ⟹ 상신 / B = ①(나) ⟹ 값 상신(판정은 전략 세션) / C = ①(다) ⟹ 정지 / D = 그 외 ⟹ 정지.

### §0-5 사전 결정 3건 (값 보기 «전» · CC 집행 설계)

```
 ㄱ **구 규칙의 산출은 «지운다»가 아니라 «남긴다»** — v4-38 의 T·A 산출 JSON 은 그대로 두고,
    새 규칙 산출은 **새 태그**로 낸다(신구 대조가 파일 대 파일로 서게).
 ㄴ **「가슴 행만 바뀐다」의 확인 방법** — 같은 상태·같은 칸에서 신구 리포트를 내고 **5행 9필드**를 비교한다.
    목선·허리·밑단·소매 4행이 한 필드라도 다르면 **그 사실을 값으로 적는다**(기대 이탈 · 은폐 0).
 ㄷ **대역 소속 변동 정점 수** — 가슴 대역은 `|y − chestY| ≤ d`(d = 9 mm)이므로 구·신 대역의
    **표본 수 차이**와 **교집합 크기**를 함께 적는다(어느 정점이 들어오고 나갔는지의 수).
```

### §0-6 커밋·전환 인쇄 (집행 «전»)

```
 §0 선커밋 **`88a6ba5`** — 새 `chestY` 규칙(§0-3)을 **값 보기 «전»** 에 담아 push 했다
 2호기 $ git fetch -q origin && git checkout -q v4-39-chest-armpit && git pull -q
        $ git branch --show-current → v4-39-chest-armpit
        $ git log --oneline -1     → 88a6ba5 v4-39 §0 — 수령·사무 + 새 chestY 규칙 «값 보기 전» 등재
 ★ 전환 전에 2호기의 v4-38 작업 잔재를 지웠다 — `scripts/v4Levels.ts`(미추적 사본) 삭제 ·
   `src/v3/bodyLevels.ts` 는 `git checkout --` 로 원복(그 판본은 이미 v4-38 커밋에 있다).
```

---

## §1-① `chestY` 재정의 — **(가) 두 포즈 성립 + 구간 안 + A포즈 `chestY > waistY` 복귀**

### diff 원문 (`src/v3/bodyLevels.ts` · +18 / −6 · 주석 8줄 포함 · 이 파일 밖 0줄)

```
 삭제  /* `Y_ARM` — `Y_LOW` «위»로 올라가며 성분이 1 → >1 로 바뀌는 «첫» 전이. */
       let yC = NaN, yPure = NaN;
       for (let y = Y_LOW + GRID_M; y <= yTop; y += GRID_M) {
         const a = at(y);
         if (a.nComp === 1 && a.hasTorso) { yPure = y; continue; }
         if (a.nComp > 1) { yC = y; break; }
       }
       if (!Number.isFinite(yC) || !Number.isFinite(yPure)) throw new Error('성분 1 → >1 첫 전이를 못 찾는다 — 갈래 B');
       const [lowSide, hiSide] = bisect(yPure, yC, (y) => at(y).nComp === 1 && at(y).hasTorso);

 신설  const cGrid: { y: number; C: number }[] = [];
       for (let y = Y_LOW + GRID_M; y <= yTop; y += GRID_M) {
         const a = at(y);
         if (Number.isFinite(a.C)) cGrid.push({ y, C: a.C });
       }
       let jump = -1, best = 1;
       for (let i = 0; i + 1 < cGrid.length; i++) {
         const r = cGrid[i + 1].C / cGrid[i].C;
         if (r > best) { best = r; jump = i; }
       }
       if (jump < 0) throw new Error('팔이 몸통 고리에 흡수되는 전이를 못 찾는다 — 갈래 B');
       const cMid = (cGrid[jump].C + cGrid[jump + 1].C) / 2;
       const [lowSide, hiSide] = bisect(cGrid[jump].y, cGrid[jump + 1].y, (y) => at(y).C < cMid);

 유지  const chestY = lowSide, Y_ARM = hiSide;   ← 아래 = chestY · 위 = Y_ARM (짝 불변)
       const C_chest = at(chestY).C;
```

**§0-3 등재 문언과 1:1** — ㄱ 사건(고리가 팔을 흡수) · ㄴ 상대 증가 최대 전이(문턱 0) ·
ㄷ 두 끝 둘레의 **중점** 술어로 이분법 · ㄹ 아래 끝 = `chestY` · 위 끝 = `Y_ARM` · ㅁ 못 찾으면 던짐.
**새 상수 0 · 새 문턱 0** — `GRID_M`·`TOL_SELF`·`yTop` 은 이미 이 함수가 쓰던 것이다.

### 산출

| 채널 | **T포즈 몸** | **A포즈 몸** |
|---|---|---|
| `chestY` m | **1.3205274248123169** | **1.3664153099060057** |
| 지목 구간(§0-4) | 1.311 ~ 1.321 ⟹ **안** ✅ | 1.357 ~ 1.367 ⟹ **안** ✅ |
| `Y_ARM` m(흡수 직후) | 1.320605549812317 | 1.3664934349060056 |
| `C_chest` mm | **1199.4280547998462** | **981.5558742962282** |
| `waistY` m(v4-38 규칙 · 불변) | 1.0322641174463794 | 1.0758755126227961 |
| `chestY > waistY` | 예(+0.2882633073659375 m) | **예(+0.2905397972832096 m)** ✅ 복귀 |
| `Y_LOW` m | 0.7938086748123163 | 0.8283684349060052 |
| `C_waist` mm | 655.3116208934264 | 680.3875783475349 |

★ **두 포즈의 값은 같지 않다**(사실만) — `chestY` T 1.32053 ↔ A 1.36642(차 **45.8878850936888 mm**).
같은 몸의 두 판본인데 **A포즈 몸 전체가 위로 올라가 있다**: `Y_LOW` 차 **34.5597600936889 mm** ·
`Y_TOP` 차 **46.434 mm**(1.410606 → 1.457040). 바닥 기준 상대 높이 `chestY − Y_LOW` 로 보면
T **527.7187500000006 mm** ↔ A **538.0468750000004 mm**(차 **10.3281249999998 mm**).
**해석·처분 0** — 포즈 불변성의 판정은 전략 세션 몫이다.

★ `armFirst`(진단 전용)도 뜻이 바뀌었다 — 이제 **흡수 직후 높이**의 잔여 비몸통 성분이다:
T 두 덩이(각 49 선분 · |x| 712.61~751.11 mm = 손) · A 한 덩이(62 선분 · |x| 177.59~338.13 mm).

## §1-② 신구 대조 (T포즈 M · v3 정본 blob · 같은 상태)

| 채널 | **구 규칙**(v4-38) | **새 규칙**(v4-39) | 차이 |
|---|---|---|---|
| `chestYCm` | 129.9746174812317 | **132.0527424812317** | **+2.078025 cm**(20.78025 mm) |
| `yArmCm` | 129.9824299812317 | **132.0605549812317** | +2.078125 cm |
| `cChestCm` | 95.54291364543998 | **119.94280547998461** | +24.39989183454463 cm |
| `waistYCm`·`cWaistCm`·`yLowCm` | 103.22641174463794 · 65.53116208934264 · 79.38086748123163 | **같음** | 0 |

**리포트 5행 — 「가슴」 한 행만 바뀐다**(§0-5ㄴ 확인 · 나머지 4행 9필드 전부 불변)

| 행 | 구 규칙 | 새 규칙 |
|---|---|---|
| 목선 | 중앙 1.0640620622862236 · p25 0.998538122408768 · p75 1.87614198962685 · 0/41/13 · 54 | **불변** |
| **가슴** | 중앙 **4.466004035258341** · p25 1.000000117994877 · p75 16.800410221227867 · 밀착/여유 **111/155** · 표본 **266** | 중앙 **2.0957157526560173** · p25 1.0019395501042934 · p75 7.011987799387334 · 밀착/여유 **121/129** · 표본 **250** |
| 허리 | 중앙 26.120323046509828 · … · 0/0/226 · 226 | **불변** |
| 밑단 | 중앙 18.37433614917702 · … · 0/8/102 · 110 | **불변** |
| 소매 | 중앙 4.61769505265601 · … · 0/319/599 · 918 | **불변** |

가슴 행 중앙 차 **−2.3702882826023238 mm** · 표본 **266 → 250**(−16).

**대역 소속 변동(§0-5ㄷ)** — 대역은 `|y − chestY| ≤ d`, `d = 9 mm`(대역 반폭 · 리포트 인쇄값).
두 대역 중심 거리 **20.78025 mm > 2d = 18 mm** ⟹ **두 대역의 교집합은 0**(도출 · 새 측정 0).
즉 가슴 행의 표본 **266개가 전부 빠지고 250개가 새로 들어왔다**.

**게이트 불변 확인** — `c100-h185-s40_L`(v4-27 회귀1 산출 · 같은 상태)에서 새 규칙으로 다시 내
저장본과 **전 필드 동일**(`pass` true↔true · `fails` []↔[]). 게이트 경로는 `deriveLevels` 를 부르지 않는다
(`scripts/v4ProductGate.ts` 에 `bodyLevels` 참조 0 · grep 확인) — 그 사실과 실측이 일치한다.

## §1-③ A포즈 5행 재산출 (v4-37 정착 산출 · 굽기 0 · 층3만)

| 행 | **v4-38 ③**(구 `chestY` 102.95 cm) | **v4-39 ③**(새 `chestY` 136.64 cm) |
|---|---|---|
| 목선 | 중앙 1.0692841106160207 · p25 0.9999999849952131 · p75 2.6847060798192777 · 0/40/16 · 56 | **불변** |
| **가슴** | 중앙 **42.081225636607314** · p25 29.341033081181116 · p75 49.131216918777085 · 밀착/여유 **0/228** · 표본 **228** | 중앙 **2.0790** · p25 **1.0016** · p75 **5.4760** · 밀착/여유 **123/128** · 표본 **251** |
| 허리 | 중앙 48.41965459456567 · p25 29.736365093853223 · p75 61.6885133359544 · 0/0/228 · 228 | **불변** |
| 밑단 | 중앙 19.334009181588545 · p25 1.5331311849830729 · p75 33.207306744300446 · 0/31/81 · 112 | **불변** |
| 소매 | 중앙 7.394977060365645 · p25 1.724933944953111 · p75 16.944983507437183 · 0/276/744 · 1020 | **불변** |

레벨(새 규칙) — `yLowCm` **82.83684349060051** · `chestYCm` **136.64153099060056** ·
`yArmCm` **136.64934349060056** · `waistYCm` **107.58755126227962** ·
`cChestCm` **98.15558742962283** · `cWaistCm` **68.0387578347535**.

- **가슴 대역이 겨드랑이 높이로 이동했다** — 대역 중심 y **1.0295403099060051 → 1.3664153099060057 m**
  (**+336.8750000000006 mm**) · `C_chest` **71.09024830975724 → 98.15558742962283 cm**.
- **눌림 0 · NaN 0**(다섯 행 전부 `pressN = 0`) · 가슴 행이 T포즈의 자릿수(중앙 2.0957 mm)와 **같은 대역**으로 들어왔다.
- **게이트 불변** — `pass: true` · `fails` **[]**(v4-37·v4-38 과 같은 판정 · 재굽기 0).
- 워커 결과 JSON 재생성 — `gpu/bake/results/v4-37-Aramp/fit-Aaxi_M.json`·`gate-Aaxi_M.json`·`Aaxi_M.done`
  를 새 규칙 산출로 덮었다 ⟹ **성공 기록 유지**.

---

## §2 자기검사

| 항 | 값 |
|---|---|
| `src/` diff vs 기점 `342b0dd` | **`src/v3/bodyLevels.ts` +18 / −6 한 파일뿐**(`chestY`·`Y_ARM` 유도 구간 한정 · 위 diff 원문) · 물리 **0줄** |
| `garmentScene`·`dressRun`·워커·`gpu/engine/` | **diff 0** · 정본·blob·문턱 **0** · 게이트 채널·문턱 **0** · v4-38 허리 창(`yTop`) **유지** |
| v3 기존 산출물 | **불변** — 이 판은 `public/v3diag/`·정답지 blob·제공 37칸 산출물을 **쓰기 0**(읽기만) |
| 회귀 성격 | T포즈 «가슴 행만» 바뀐다(의미 정정 · 판정문이 값 변화를 «허용» · 값 보기 «전» §0-3 등재) · 나머지 4행·게이트 **불변** |
| 이 판이 **신설**한 것 | 산출 `l3-levels-v4-39-{T,A}.json` · `fit-…-v4-39-Tnew.json` · `l3-gate-…-v4-39-Lgate.json` · A포즈 층3 재생성 3건 |
| 굽기 · 캡처 | **0 · 0**(v4-37 산출 재사용) |
| pytest(2호기) | **40 passed · 2 xfailed · 1 xpassed**(58.50 s) |
| `npx tsc -b` | **통과**(종료 0) |
| 브랜치 인쇄 | §0 머리에 에어·2호기 둘 다 · §0 은 집행 **«전»** 커밋(`88a6ba5` · 자기기입 `da3c0ef`) |
| ecc 에이전트 / 스킬 | **0 / 0** |

## §4 회차 갈래 — **A**(①(가) + ②③)

**넘기는 것 6건**

1. ★★★ **새 규칙이 두 포즈에서 겨드랑이를 집는다** — `chestY` T **1.3205274248123169** · A **1.3664153099060057** ·
   둘 다 지목 구간 안 · A포즈에서 **`chestY`(136.64 cm) > `waistY`(107.59 cm)** 로 복귀했다.
2. ★★★ **A포즈 「가슴」 행이 제자리로 왔다** — 중앙 **42.081225636607314 → 2.0790 mm** ·
   밀착/여유 0/228 → **123/128** · 대역 중심이 **+336.875 mm** 올라갔다(손끝 → 겨드랑이).
3. ★★ **T포즈는 「가슴」 한 행만 바뀐다** — 중앙 4.466004035258341 → **2.0957157526560173 mm** ·
   표본 266 → 250 · 구·신 대역 중심 거리 20.78025 mm > 2d(18 mm) ⟹ **교집합 0** · 게이트 전 필드 불변.
4. ★★ **`C_chest` 의 뜻이 바뀌었다** — T 95.54291364543998 → **119.94280547998461 cm**.
   새 `chestY` 는 흡수 «직전»이지만 그 높이의 몸통 고리는 이미 삼각근까지 감싼다(1cm 격자에서 C 99.0 → 207.1 cm 사이).
   **둘레 값의 처분(가슴둘레로 쓸 것인지)은 이 판이 판단하지 않는다.**
5. ★★ **두 포즈 값이 같지 않다** — 차 45.8878850936888 mm. 다만 A포즈 몸 전체가 위로 올라가 있어
   (`Y_LOW` 차 34.5597600936889 mm) 바닥 기준으로는 차가 **10.3281249999998 mm** 다(사실만).
6. ★ **`armFirst` 의 뜻이 따라 바뀌었다**(진단 전용) — 이제 «흡수 직후» 남은 성분(T = 손 · A = 팔 한 덩이)이다.

**이 판이 하지 않은 판단** — `C_chest` 의 처분 · 두 포즈 값 차의 처분 · 108칸 확장 · 화면.
