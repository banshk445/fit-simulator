# v4-34 — SDF 포화 사실(참깊이 재측정) · 변환 경로 위 «위반 0 경계» 측정 · 처방 재료

**물리 0줄 · `src/` diff 0(계기·판정 스크립트만) · 굽기 0 · 캡처 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-34-frontier` |
| 기점 | **`e274504`**(= v4-33 §1~§4) |
| §0 커밋 | (집행 «전» · 이 커밋) |
| §1~§4 커밋 | (집행 후 기입) |
| 보고 파일 | `docs/v4/보고/34.md` |

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v4-34-frontier e274504 → Switched to a new branch 'v4-34-frontier'
       $ git branch --show-current → v4-34-frontier · HEAD e274504
 2호기 (§0 커밋을 push 한 «뒤» 같은 자리로 전환 — §0-5 에 인쇄)
```

---

## §0 수령·사무

### §0-1 판정문 (전략 세션 · v4-33 §4 수령 · 원문)

```
 「갈래 C 수령 · ★ 강체 6자유도 소진 확정(회전 포함 중앙 31.6 · 위반 291→148 · 0 아님) ·
   28°(손 각도 0)는 방향 정합·양 부족의 값 ·
   ★★ 계기 유보 승격 — 깊이 10.5054 가 세 판에서 동일 = SDF 밴드(헤더 ≈8.6mm) 포화 후보:
     (a) 실깊이 미지 (b) 밴드 밖 관통은 솔버도 못 민다(기울기 소실) ⟹ 측정 자부터 확인 ·
     함정 후보 「포화된 계기로 잰 깊이를 참값으로 안다」 ·
   ★★ 강체 불가 ≠ 기하 불가 — 변환 경로 t∈[0,1] 위 «위반 0 최대 t» 미측정:
     그 점의 봉제 중앙 ≤ 217.0112(T포즈 램프 실적)이면 «부분 정합 + 램프» 처방이 손 상수 0 으로 선다
     (t 는 탐색값 · fitSleeve R 과 같은 성격) · 밖이면 암홀(패턴) 질문이 정당 ·
   이 판 = 측정 · 처방 0」
```

### §0-2 바꾸지 않는 것 (회차 프롬프트 원문)

```
 물리 **0줄** · `src/` **0** · 정본·blob·문턱 **0** · 워커 **diff 0** ·
 격자 용량 **262144** · 계기(v4-28~33) **유지**.
```

### §0-3 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · 단위 명시)

**★ 단위** — ①은 **mm**(깊이) · ②의 `t` 는 **무차원**(0~1 · 강체 변환 경로의 매개변수) · 거리 **mm**.

| 항 | 갈래 |
|---|---|
| **①** SDF 포화 사실 | **(가)** 클램프 확인(실깊이 > 10.5) / **(나)** 클램프 아님(실깊이 ≈ SDF 값) / **(다)** 그 외 |
| **②** 위반 0 경계 | **(가)** `t*` 의 봉제 중앙 **≤ 217.0112** ⟹ 「부분 정합 + 램프」 재료 성립 / **(나)** 초과(값) ⟹ 강체 경로 불가 확정 · 암홀 질문 상신 / **(다)** `t* ≈ 0`(값) |
| **③** 처방 재료 정리 | 판단 0 — ②의 갈래별 사실 목록만 |

**§4** — A = ①②③ 전량 ⟹ 상신 / B = ①(다) 또는 ② 측정 불가 ⟹ 정지 / C = 그 외 ⟹ 정지.

### §0-4 사전 결정 5건 (값 보기 «전» · CC 집행 설계)

```
 ㄱ **참깊이 자는 «있는 것»을 쓴다** — `src/v3/instruments.ts` `makeBodyDistance().exactBodyDist`
    (몸 «메시»까지의 정확 무부호 거리 · v3-37 이래 게이트가 쓰는 자). SDF 는 **부호**에만 쓴다.
    ⟹ 부호 있는 여유 = (안이면 −, 밖이면 +) × 정확 거리. **새 계산 0 · 새 문턱 0.**
 ㄴ **①의 판정 기준을 지금 고정한다** — v4-33 의 148 정점에서 **참깊이 중앙이 10.5054 를 «넘으면» (가)**,
    **±0.01 mm 안에서 같으면 (나)**, 그 밖이면 (다).
 ㄷ **②의 경로를 지금 정의한다** — 강체 변환을 `t` 로 보간한다:
      `p(t) = R(축, 각도·t)·(p − s̄) + s̄ + t·(d̄ − s̄)`   (t=0 항등 · t=1 은 v4-33 의 (R, Δ))
    회전은 **같은 축에서 각도만** 줄이고 평행이동은 선형이다. 좌·우 각각 자기 (축, 각도, 중심)을 쓴다.
 ㄹ **`t*` 의 정의** — 「참깊이 자로 위반 0(모든 소매 정점의 부호 있는 여유 ≥ SEP)인 **최대 t**」.
    이분 탐색 40회 · **손 상수 0**(문턱은 SEP 그대로).
 ㅁ **이 판은 처방을 고르지 않는다** — ③은 «다음 판이 쓸 사실 목록»만 적는다(문안 초안 포함 · 적용 0).
```
