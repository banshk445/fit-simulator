# v4-47 — ③a·교차 문턱의 A포즈 재도출(도출 규칙 동형) · 108칸 재판정 · 편입 집계

**물리 0줄 · `src/` diff = 게이트 문턱 «값» 한정(도출 산출물로) · 재굽기 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-47-gate-rederive` |
| 기점 | **`08a9c5a`**(= v4-46 보고·부록 · 캡처 `ad3d9a7` 포함) |
| §0 커밋 | (아래 §0-5 에 자기 기입) |
| §1~§4 커밋 | (집행 후 기입) |
| 보고 파일 | `docs/v4/보고/47.md` |

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v4-47-gate-rederive 08a9c5a → v4-47-gate-rederive · HEAD 08a9c5a
 2호기 (§0 커밋을 push 한 «뒤» 같은 자리로 — §0-5 에 인쇄)
```

---

## §0 수령·사무

### §0-1 판정문 (전략 세션 · v4-46 집계 + 캡처 8장 화면 판정 수령 · 원문)

```
 「갈래 B 수령 · 층3 96/96 완승 · 화면 = 4칸 전부 옷(L 가슴 미세 관통점 = 경계 fail 의 실물 —
   물러나면 안 보임 · XL 극단은 매칭이 거를 부류) ·
   ★★ 처분: ③a·교차 문턱을 A포즈 분포에서 «같은 도출 규칙»으로 재도출한다 —
     함정 14 아님(결과 맞춤 이동이 아니라 도출 절차의 동형 재적용 · v4-23 계열) ·
     조건: T포즈 도출 경위(어느 판 · 어느 분포 · 어느 규칙)를 먼저 인용하고
     그 규칙을 «그대로» A포즈 96칸 분포에 적용 · 규칙이 재현 불가면 재도출 포기하고 사실 상신 ·
     교차는 「A포즈 상시 접촉」 성격을 값으로 확인(교차 위치가 겨드랑이 대역인지) 후 같은 원칙」
```

### §0-2 바꾸지 않는 것

```
 물리 **0줄** · 굽기 **0**(v4-46 산출 재사용) · 정본·blob **0** · 몸 **0** ·
 `src/` 는 **문턱 «값»만**(그것도 ②의 도출이 값을 바꿀 때에 한해)
```

### §0-3 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · 단위 명시)

**★ 단위** — 관통은 **mm** · 교차는 **개**(삼각형 교차 수) · 칸은 **칸** · 비율은 **무차원**.

| 항 | 갈래 |
|---|---|
| **①** T포즈 도출 경위 | **(가)** 규칙 재현 가능 / **(나)** 불가 ⟹ 사실 상신 · ②③ 착수 0 |
| **②** A포즈 재도출 | 같은 규칙 적용 → 새 문턱(값·근거) · 교차는 **위치 분류 먼저** |
| **③** 재판정 | 새 문턱으로 96칸 재산출(재굽기 0) · 편입/보류/착용불가 목록 초안 |
| **④** 편입 재료 | 색인 JSON 초안(T포즈 37칸 규약 동형 · 제품 배선은 다음 판) |

**§4** — A = ①(가)+②③④ ⟹ 상신 / B = ①(나) ⟹ 사실 상신 / C = 그 외 ⟹ 정지.

### §0-4 사전 결정 5건 (값 보기 «전» · CC 집행 설계)

```
 ㄱ **도출 «규칙»을 먼저 문자로 확정한다** — 코드 주석·볼트 원문에서 인용하고, 그 규칙이
    **무엇의 함수인지**(옷 상수인지 · 몸 분포인지)를 적는다. 규칙이 분포의 함수가 «아니면»
    재도출해도 값이 안 변한다 — 그 경우 **「값이 안 변한다」를 그대로 결론으로 적는다**
    (문턱을 움직일 구실을 만들지 않는다 · 함정 14).
 ㄴ **교차는 «위치»부터** — 각 fail 칸에 v4-24 계기(`scripts/v4Armpit.ts`)를 걸어
    **대역(겨드랑이) 교차**와 **전체 교차**를 함께 잰다. 대역 밖 교차가 있으면 그 사실이 우선한다.
 ㄷ **재판정은 «재굽기 0»** — v4-46 의 정착 산출(`results/v4-46-A108/<칸>.bin`)을 그대로 읽는다.
 ㄹ **편입 규약은 T포즈 것을 인용한다** — 새 등급을 만들지 않는다(편입/보류/착용불가).
 ㅁ **판단 0** — 문턱을 실제로 바꿀지는 전략 세션이 정한다. 이 판은 규칙·값·재판정 결과만 낸다.
```

### §0-5 커밋·전환 인쇄 (집행 «전»)

(§0 커밋 sha 자기 기입 · 2호기 전환 인쇄는 아래에 붙인다)


---

## §1-① T포즈 문턱 도출 경위 — **(가) 규칙 재현 가능**

### ③a 관통 ≤ 0.5 mm

| 자리 | 원문 |
|---|---|
| `src/v3/s4Gate.ts:48` | `penMaxM: 5e-4,        // ③a 절대 관통 ≤ 0.5mm (v3-29 「두께의 절반」 도출분)` |
| `.obsidian-log/00-현재상태.md:3671`(v3-29 절) | 「**③a 물리 안전**(최대 절대 관통 ≤ 두께×0.5 = **0.5mm** · 도출: **옷 중면이 몸 표면에서 두께만큼 떨어진 곳을 접촉면으로 잡으므로 두께가 한계이고 절반이 여유** · **새 상수 0**)」 |
| `src/v3/consts.ts:6` | `export const THICK = 1e-3;` ⟹ 두께 **1.0 mm** |

**규칙 = `penMax ≤ THICK × 0.5`** — **옷 상수의 함수**다. 「어느 분포에서 뽑았다」가 **아니다**
(v3-29 는 분포를 «검증»에만 썼다: 「0.3588~0.4133mm 전부 ③a 통과」).

### 자기관통 교차 = 0

| 자리 | 원문 |
|---|---|
| `src/v3/s4Gate.ts:49` | `crossings: 0,         // 자기관통 삼각형 교차 0` |
| `src/v3/s4Gate.ts:143` | ``if (mp.hits !== S4_THRESHOLD.crossings) fails.push(`자기관통 교차 ${mp.hits} ≠ 0`);`` |

**규칙 = 「옷이 자기 자신을 통과하지 않는다」** — 0 은 분포에서 «뽑은 수»가 아니라 **정의**다
(비교 대상이 없다 · v3-40 의 목선 초과비 1 과 같은 성격: 「1 은 고른 수가 아니라 «정의»다」).

⟹ **①(가)** — 두 규칙 다 문자로 재현된다.

## §1-② A포즈 재도출 — **값이 바뀌지 않는다**(그리고 교차 완화의 근거는 값으로 기각)

### ③a — 같은 규칙을 그대로 적용

```
 규칙        penMax ≤ THICK × 0.5
 A포즈 THICK **1e-3 m**(`consts.ts:6` · 옷 상수 · 몸·포즈와 무관)
 ⟹ 재도출값 **0.5 mm** — T포즈와 «같다».
 ★ 규칙이 **분포의 함수가 아니므로**, A포즈 96칸 분포를 넣어도 값이 움직일 자리가 없다.
   §0-4ㄱ 대로 **「값이 안 변한다」를 그대로 결론으로 적는다**(분포에 맞춰 옮기면 그것이 함정 14다).
 참고(판정 아님) — A포즈 96칸 관통 최대 분포: 중앙 **0.40976730400947536** · p90 **1.6509985656884503** ·
   최대 **32.45656730295164 mm**. 문턱 0.5 아래가 **68칸**, 위가 **28칸**이다.
```

### 교차 — 위치부터(§0-4ㄴ) · 「A포즈 상시 접촉」 가설 **기각**

교차로 fail 한 **49칸** 각각에 v4-24 계기(`scripts/v4Armpit.ts`)를 걸어 **대역(겨드랑이) 교차**와
**전체 교차**를 함께 쟀다(재굽기 0 · 새 계기 0 · 산출 `l3ap-xloc-v4-47.json`).

| 채널 | 값 |
|---|---|
| 대상 칸 | **49** |
| 전체 교차 합 | **639**(칸당 중앙 **12** · 최대 **32**) |
| **대역(겨드랑이) 교차 합** | **22** |
| **대역 비율** | **0.034429**(3.44 %) |
| 대역 교차 > 0 인 칸 | **10 / 49**(대역 교차 최대 **5**) |
| 팔↔몸통 최소 간격(봉제쌍 제외) 최소 | **0.9845 mm** |

⟹ **교차의 96.6 %가 겨드랑이 대역 «밖»에서 난다.** 판정문이 조건으로 건 「A포즈 상시 접촉(겨드랑이)」
성격이 **값으로 성립하지 않으므로**, 「대역 내 교차 허용 · 대역 밖 0」 동형 규칙 후보는 **근거가 없다**
⟹ 형식화하지 않았다(§0-4ㄴ 「대역 밖 교차가 있으면 그 사실이 우선한다」). **교차 문턱 0 유지.**

## §1-③ 재판정 — 문턱이 그대로이므로 판정도 그대로(재굽기 0)

새 문턱(③a **0.5 mm** · 교차 **0** · 목선 초과비 1 · 정착 TOL · pinned 0)으로 v4-46 산출
(`results/v4-46-A108/`)을 **다시 판정**했다(재굽기 0 · 게이트 값 재사용 · 색인 스크립트 `idx47.py`).

| 등급(T포즈 규약 인용) | 칸 |
|---|---|
| **편입**(gate pass) | **33** |
| **보류**(게이트 미통과) | **63** |
| **착용불가**(옷 제도 갈래 D) | **12** |
| 합 | **108** |

| 사이즈 | 편입 | 보류 | 착용불가 |
|---|---|---|---|
| S | **11** | 11 | 5 |
| M | **9** | 14 | 4 |
| L | **6** | 19 | 2 |
| XL | **7** | 19 | 1 |

## §1-④ 편입 재료 — 색인·제공 목록 «초안»(제품 배선 0)

T포즈 규약과 **같은 형식**으로 두 파일을 냈다(형식 출처: `public/v3diag/v3-77/index-merged-108.json` ·
`v1-provide-35.json`).

```
 gpu/oracle/export/l3ap-index-merged-108-v4-47.json
   칸마다 {status: 편입|보류|착용불가, f, sec, sha(편입만), gate, reason(보류/착용불가), at, host, os, node}
 gpu/oracle/export/l3ap-provide-v4-47.json
   {메타, provide: [편입 33칸]}  ← 메타에 「문턱은 v4-47 §1-② 재도출분(값 불변)」과 몸 출처(그리드 정본 · 80944b2)를 적었다
```

**편입 33칸** — `c100-h155-s45_S` · `c100-h155-s50_S` · `c100-h170-s40_S` · `c100-h170-s45_L` ·
`c100-h170-s45_M` · `c100-h170-s45_S` · `c100-h170-s50_L` · `c100-h170-s50_M` · `c100-h170-s50_XL` ·
`c100-h185-s40_M` · `c100-h185-s45_S` · `c122.5-h155-s40_M` · `c122.5-h155-s40_S` · `c122.5-h155-s45_S` ·
`c122.5-h155-s50_M` · `c122.5-h155-s50_XL` · `c122.5-h170-s40_S` · `c122.5-h170-s45_L` ·
`c122.5-h170-s45_XL` · `c122.5-h170-s50_L` · `c122.5-h170-s50_XL` · `c122.5-h185-s40_L` ·
`c122.5-h185-s40_M` · `c122.5-h185-s40_S` · `c122.5-h185-s45_XL` · `c87.5-h155-s40_L` ·
`c87.5-h155-s40_XL` · `c87.5-h155-s45_M` · `c87.5-h155-s45_XL` · `c87.5-h170-s50_M` ·
`c87.5-h170-s50_S` · `c87.5-h185-s50_M` · `c87.5-h185-s50_S`.

★ 참고(판정 아님) — T포즈 제공 목록은 **35칸**이었다(`v1-provide-35.json`). A포즈 편입은 **33칸**이다.
