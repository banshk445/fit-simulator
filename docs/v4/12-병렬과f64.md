# v4-12 — f64 결착 · 발산 시작점 주사 · «진짜» 병렬 커널

**집행 기계 = 2호기(CUDA · 정본 기계) · `src/` diff 0 · 굽기 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-12-parallel` |
| 기점 | **`1cc186b`** (= v4-11 HEAD) |
| 워크트리 | `C:\Users\banba\fit-simulator` |
| 기계 | **`DESKTOP-OV72A46`** (win32 · CUDA · GTX 1660 Ti · Taichi 1.7.4 · llvm 15.0.1 · py 3.12.10) |
| §0 커밋 | **`600809f`** — 집행 «전» 별도 커밋(이 파일의 §0 만) |
| §1~§4 커밋 | **`dce37b2`** (코드·산출물·본문) · 볼트 등재 **`2baef64`** · metrics **`43db01e`** |
| 호출 ecc 에이전트/스킬 | **0 / 0** (`strategist` 서브에이전트도 0) |

---

## §0 수령 (집행 «전» 별도 커밋)

### §0-1 판정문 (전략 세션 · v4-11 §4 수령 · 원문)

```
 「갈래 B 수령 · ★ 전략 세션 판정 «3건 기각» 수용:
   (1) 직렬성 가설 기각(비트 동일 · serialize 존재 · IR 태스크 1 · 각인 10만/10만)
   (2) 「병렬화로 안 빨라진다」 기각 — 함정 42 로 색분할판은 «한 번도 병렬로 돈 적이 없다»
       (if 안 최상위 for 는 병렬 태스크가 아니다) ⟹ 병렬 실익 «미측정»
   (3) 「launch 오버헤드 지배」 기각(오버헤드:계산 ≈ 0:1)
   ★★ 새 사실 = v4 는 grid 1 × block 1 로 돈다 — CUDA 코어 1,536개 중 «하나»만 쓴다
      ⟹ 이식은 실패한 게 아니라 시작되지 않았다
   ★ 미수렴 후보 재개방: f32 가 유일한 유력 후보이나 v4-09 의 양자 논증은 «1스텝 양자»이지
     «반복 솔버 극한 사이클»이 아니다 · f64 검사는 3판 연속 미완 — 이 판에서 결착
   ★ 귀책 = 전략 세션(v4-10 ④ 해석 · v4-11 가설 3건)」
```

### §0-2 바꾸지 않는 것 (회차 프롬프트 원문)

```
 `src/` · 정본 · blob · v3 동결 · `full.py` 물리 식 0줄(신설 병렬 경로는 «새 파일»)
 ⟹ 이 판이 만드는 것은 **새 파일**뿐이다(`gpu/engine/full_par.py` · 실행기·계기 신설분).
 ★ `engine/full.py` · `full_color.py` · `full_color2.py` · 커널 4종 **바이트 불변**.
 ★ v4-02~11 산출물 바이트 불변(이 판 산출은 새 접두 `l3f64-` · `l3fd-` · `l3par-` 로 판다).
```

### §0-3 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0)

**① f64 결착** — 같은 칸·같은 초기 상태·같은 제약 집합으로 v4 를 **f64** 60프레임.
arch 실측 등재(CUDA f64 가 느리면 x64 로 떨어져도 그대로 기록 · 예산 초과 시 **30프레임에서 정지**하고 그 사실 등재).

| 갈래 | 조건 |
|---|---|
| **(가)** | 순변위가 v3 처럼 «감소 추세» ⟹ 미수렴 원인 = **f32 확정** |
| **(나)** | 여전히 진동(값) ⟹ **f32 아님** · 원인 미상으로 남긴다 |
| **(다)** | 실행 불가(사유) |

**② 발산 시작점 주사** — v3(f64·Node)와 v4(f32·CUDA)를 «같은 초기 상태»에서 각각 10프레임 돌려
**프레임마다** 최대 좌표차·중앙 좌표차를 표로.

| 갈래 | 조건 |
|---|---|
| **(가)** | 차이가 f=1 에서 ULP 급이고 이후 «매끄럽게» 커진다 ⟹ 정밀도 증폭 |
| **(나)** | f=1 에서 이미 ULP 를 크게 넘는다(값) ⟹ **1스텝 단위의 논리 차이**가 있다(자리 후보 등재 · 처방 0) |
| **(다)** | 그 외 |

**③ «진짜» 병렬 커널**(함정 42 우회) — 색 루프를 `if` «밖» 최상위 `range_for` 로 재구성한 새 파일
(`full_par.py` · 물리 식은 `full.py` 의 `@ti.func` 를 그대로 물려받는다 · 색 분할은 v4-10 의 8색·14색 그대로).

```
 ㄱ 병렬 확인(실행 «전») — 프로파일러 태스크 수 = 코드에서 센 수 · grid/block > 1 · IR 에 range_for 존재.
    셋 다 성립해야 ㄴ 착수(아니면 값 상신 · ㄴ 착수 0).
 ㄴ 속도·정확도 — 60프레임 실행 · s/프레임 · 직렬판 12.690 과 배수 · 순변위 궤적 모양 · 리포트 5행(값만 · 판정 0).
```

| 갈래 | 조건 |
|---|---|
| **(가)** | ㄱ 성립 + 배수 **> 2** |
| **(나)** | ㄱ 성립 + 배수 **≤ 2**(값) |
| **(다)** | ㄱ 불성립(사유) |
| **(라)** | 발산·NaN |

**§4 회차 갈래** — A = ①②③ 전량 ⟹ 상신 / B = ①(다) ⟹ ②③ 집행·상신 /
C = ③(다)(라) ⟹ ①② 상신 · ③ 사유 등재 / D = 그 외 ⟹ 정지. **사후 갈래 신설 0 · 처방·최적화 착수 0.**

### §0-4 집행 «전» 사전 결정 6건 (값을 보기 «전»에 적는다)

```
 ㄱ **① 「감소 추세」의 정의** — v3 의 모양(f10 1.600e-02 → f60 2.531e-04 = 1/63)에서 «유도»한다.
    (가) = ㉠ net(f60) ≤ net(f10)/10 **그리고** ㉡ f40·f50·f60 이 모두 net(f10) 미만이고 «단조 비증가».
    (나) = ㉠㉡ 중 하나라도 불성립. **30프레임에서 정지한 경우**는 ㉠' net(f30) ≤ net(f10)/3 ·
    ㉡' f20·f30 단조 비증가 로 같은 규칙을 축약 적용하고 «축약했다는 사실»을 등재한다.
    창 순변위 식·N_WIN(10)·창 정의는 v4-10/11 과 **같은 것**을 쓴다(새 수 0).

 ㄴ **① arch 선택 규약** — CUDA f64 와 x64 f64 를 각각 **1프레임** 재고 60프레임 벽시계를 추정한다.
    더 빠른 쪽으로 60프레임을 돌린다. 둘 다 60프레임 추정이 **2시간**을 넘으면 **30프레임**으로 자르고
    그 사실을 등재한다(회차 프롬프트 ★). 폴백이면 `current_cfg().arch` 를 그대로 인쇄해 등재(함정 1).
    ★ f64 실행이 **CPU(x64)** 로 가면 GPU 를 다투지 않으므로 ②③ 과 «동시»에 돌 수 있다 —
      그 경우에만 동시 실행을 허용하고, CUDA 로 가면 v4-11 대로 **줄 세운다**.

 ㄷ **② 대조의 정의** — v3 는 f64 위치를 프레임마다 덤프(`scripts/v4FrameDump.ts` 신설 · 물리 경로 diff 0 ·
    `v4CellConverge.ts` 의 `prepare`·제약 집합 `all`·`params` 를 그대로 인용) · v4 는 같은 blob 을 f32 로
    캐스팅해 **1프레임씩** 덤프. 프레임 f 마다 ㉠ 최대 좌표차(성분 절대차 max · mm) ㉡ 최대 정점 거리(mm)
    ㉢ **중앙 정점 거리**(mm) 를 인쇄한다. **f=0(캐스팅만) 도 인쇄한다** — 이것이 ULP 급의 «척도»다.
    (가)/(나) 의 가름 = **f=1 의 최대 좌표차 ≤ 10 × f=0 의 최대 좌표차** 면 「ULP 급」(가), 넘으면 (나).
    참고 상수(계기 아님): f32 ULP(1.0 m) = 2^-23 = 1.192e-07 m = **1.192e-04 mm**.

 ㄹ **③ 「코드에서 센 수」** — 예측 1 + λ초기화 3 + 늘어남 8색 + 굽힘 14색 + 봉제 1 + 몸충돌 1 +
    속도갱신 1 = **29 / 서브스텝**(v4-10 이 센 수와 같은 정의). ㄱ 은 이 29 와 프로파일러 레코드를
    «같은 판 안에서» 대조한다(함정 42 규범 ①).

 ㅁ **③ 정확도 채널** — 색 순서가 v3 배열 순서와 다르므로 직렬판과 «갈릴 수 있다»(v4-10 §0 등재).
    따라서 ㄴ 은 **판정 0** — 궤적 모양과 리포트 5행을 «값으로만» 적는다. 배수는 **v4-11 §1-① 의
    12.703/12.795 가 아니라 회차 프롬프트가 지정한 12.690**(v4-10 직렬)로 나눈다.

 ㅂ **정지** — ③ㄱ 이 불성립이면 ㄴ 착수 0(회차 프롬프트). 발산·NaN 이면 (라) 로 적고 그 프레임을 등재한다.
```

### §0-5 예산 · 실행 규약

```
 실행 예산 = 각 60프레임 이하. 전부 **배경 실행 + 로그 파일**.
 예상: ① f64 60프레임(arch 미정 · ㄴ 규약으로 자른다) · ② v4 f32 10프레임 ≈ 130s + v3 10프레임 ·
      ③ㄱ 물리 0~1프레임(분 단위) · ③ㄴ 60프레임(배수에 따라 다름).
 ★ GPU 동시 실행 0(①이 x64 로 가는 경우만 예외 · §0-4ㄴ).
```

### §0-6 귀책 등재 (전략 세션 · 판정문이 스스로 적은 3건)

| # | 무엇 | 어디서 났나 | 어디서 값으로 뒤집혔나 |
|---|---|---|---|
| 1 | 「v4 의 직렬 경로가 실은 병렬이라 쓰기 경쟁이 있다」 | v4-10 §4 판정문 | v4-11 §1-①(비트 동일) · §1-②(serialize · IR · 각인) |
| 2 | 「병렬화로는 안 빨라진다」 + 굽힘 Amdahl 상한 | v4-10 §4 · v4-10 §1-④ 해석 | v4-11 §1-④ㄹ · 함정 42(색분할판은 병렬로 돈 적이 없다) |
| 3 | 「55 ms/서브스텝은 커널 던지는 비용이 지배」 | v4-10 §4 판정문 | v4-11 §1-④ㄷ(커널/벽시계 98~100% · 오버헤드 상한 4.61%) |

★ 세 건 모두 **CC 귀속 0** — 판정문이 스스로 기각을 수용했고(§0-1), 이 표는 그 수용을 볼트에 «남기는» 것이다.

---

## §1 집행

### §1-① f64 결착 — **(가) 감소 추세** ⟹ 미수렴 원인 = **f32 확정**

계기 = `gpu/l3f64_run.py`(신설). `engine/full.py` 를 **그대로** 부른다(물리 식 0줄 · 바이트 불변).
같은 칸 `c100-h170-s45_M` · 초기 상태 = 정착 blob 의 **f64 원본**(캐스팅 0) · 제약 `all` 44,920
(늘어남 18,016 + 굽힘 26,648 + 봉제 256) · substeps 229 · N_WIN 10 · **60프레임**.

#### ㄱ arch — **실측으로 골랐다**(§0-4ㄴ · 값을 보기 «전»에 등재한 규약)

| arch | 1프레임 probe | 60프레임 추정 | 채택 |
|---|---|---|---|
| **CUDA f64** (GTX 1660 Ti) | **69.280 s/프레임** | 4,157 s ≈ **69분** | — |
| **x64 f64** (CPU · 단일 스레드) | 5.878 s/프레임(JIT 포함) | 353 s ≈ **6분** | **★ 채택** |

```
 ★ 폴백이 아니다 — **요청한 arch 로 각각 떴다**(`current_cfg().arch` = `Arch.cuda` / `Arch.x64` · assert 통과).
 ★ 두 arch 의 f=1 창 순변위가 **9.471112e-03 으로 같다**(인쇄 자릿수 전량) ⟹ f64 결과는 arch 에 의존하지 않는다.
 ★ 두 probe 는 **서로 동시에** 돌았다(하나는 GPU · 하나는 CPU) — 추정용이고 **본 60프레임 실행은 단독**이다.
 ★ 본 실행의 실측은 **0.963 s/프레임**(JIT 이 1프레임 probe 에 다 들어갔던 것) · 60프레임 **57.8 s** · degenerate 0.
```

#### ㄴ 창 순변위 궤적 — **v3 와 인쇄 자릿수 전량 일치**

| f | 10 | 20 | 30 | 40 | 50 | 60 |
|---|---|---|---|---|---|---|
| **v4 f64**(이 판 · x64) | **1.599926e-02** | **3.992115e-03** | **1.181904e-03** | **7.531095e-04** | **5.404642e-04** | **2.530865e-04** |
| **v3**(정답지 · v4-10 §1-①ㄴ) | 1.600e-02 | 3.992e-03 | — | 7.531e-04 | — | **2.531e-04** |
| v4 **f32**(직렬 · v4-11 §1-①) | 2.651729e-02 | 2.349800e-02 | 2.299182e-02 | 3.003855e-02 | 1.937818e-02 | 2.705761e-02 |

```
 ★ 사전 등재 판정식(§0-4ㄱ · 값을 보기 «전»에 적었다):
   ㉠ net(f60) ≤ net(f10)/10 → 2.530865e-04 ≤ 1.599926e-03 **성립**(실제 비 **1/63.2**)
   ㉡ f40·f50·f60 이 net(f10) 미만이고 단조 비증가 → 7.53e-04 ≥ 5.40e-04 ≥ 2.53e-04 **성립**
   ⟹ **갈래 (가)**. 30프레임 축약 규칙은 **쓰지 않았다**(60프레임 완주).
 ★★ v4 f64 는 v3 의 감소 곡선을 **인쇄 자릿수 전량**으로 재현한다(f10·f20·f40·f60 네 점 모두).
   같은 코드가 f32 에서는 2.7e-02 에서 «진동»한다 ⟹ **바뀐 것은 정밀도 하나뿐이다.**
 ★ 이로써 v4-09 §1-① 이 남긴 미결(「1스텝 f32 양자로는 관측 크기를 40~100배 못 채운다」)은
   «반복 솔버 극한 사이클» 쪽으로 정리된다 — **판정은 (가) 하나이고, 기전의 단정은 여기서 하지 않는다.**
 ★ 참고(사실만 · 처방 0) — **f64 x64 0.963 s/프레임 이 f32 CUDA 직렬 12.690 보다 13.2배 «빠르다»**.
   grid 1 × block 1 에서는 CPU 한 스레드가 CUDA 코어 하나보다 빠르다(v4-11 §1-④ㄹ 의 따름).
```

#### ㄷ 리포트 5행 (참고 · 판정 0 · `scripts/v4FitReport.ts` · 물리 0프레임)

| 행 | v4 **f64** f=60 | v4 f32 직렬 f=60(v4-10 §1-④ㄹ) |
|---|---|---|
| 목선 | **1.0650** | 1.59 |
| 가슴 | **4.0274** | 4.89 |
| 허리 | **26.7097** | 27.32 |
| 밑단 | **19.1641** | 22.96 |
| 소매 | **4.4996** | 4.68 |

★ **이 자리도 «정착 상태»가 아니다**(f=60 창 순변위 2.53e-04 m > 문턱 1e-4) — v4-10 이 등재한 주의가 그대로 산다.
  v3 의 f=60 위치 덤프는 이 판에 **없다**(만들지 않았다 · §2-1) ⟹ **v3 와의 리포트 대조는 하지 않았다.**

### §1-② 발산 시작점 주사 — **(나)**(등재 정의역 f=1) · 보조 채널은 «매끄러운 증폭»을 보인다

계기 = `scripts/v4FrameDump.ts`(신설 · v3 f64 Node) + `gpu/l3fd_run.py`(신설 · v4 f32 CUDA).
같은 초기 상태(v3 f=0 이 정착 blob 원본과 **바이트 동일**함을 assert 로 막았다) · 같은 제약 집합
(v3 실측 `m 44,920 {inplane 18,016 · bend 26,648 · dist 256}` = v4 의 mi/mb/ms) · 같은 substeps 229.

| f | 최대 좌표차(mm) | 최대 정점거리(mm) | **중앙 정점거리(mm)** | p99(mm) | maxAt |
|---|---|---|---|---|---|
| **0**(f32 캐스팅만) | **5.959267e-05** | 6.102575e-05 | 2.435580e-05 | 5.897532e-05 | 8769 |
| **1** | **2.022914e+00** | 2.524552e+00 | 6.263487e-01 | 1.320057e+00 | 3189 |
| 2 | 5.169993e+00 | 5.767791e+00 | 2.188878e+00 | 3.917043e+00 | 3749 |
| 3 | 8.985106e+00 | 1.027353e+01 | 4.650792e+00 | 6.636137e+00 | 3679 |
| 4 | 1.399178e+01 | 1.744707e+01 | 7.296900e+00 | 9.813248e+00 | 3679 |
| 5 | 1.793181e+01 | 2.073712e+01 | 9.754361e+00 | 1.311435e+01 | 3679 |
| 6 | 2.212723e+01 | 2.493305e+01 | 1.208034e+01 | 1.620478e+01 | 3735 |
| 7 | 2.486921e+01 | 2.726489e+01 | 1.430543e+01 | 1.886059e+01 | 3624 |
| 8 | 2.686068e+01 | 2.893866e+01 | 1.611794e+01 | 2.122366e+01 | 3569 |
| 9 | 2.764148e+01 | 2.971718e+01 | 1.742208e+01 | 2.307831e+01 | 3569 |
| 10 | 2.783666e+01 | 2.954862e+01 | 1.826581e+01 | 2.415485e+01 | 3569 |

프레임별 배수 — **33,945.68** · 2.56 · 1.74 · 1.56 · 1.28 · 1.23 · 1.12 · 1.08 · 1.03 · 1.01

```
 ⟹ 사전 등재 규칙(§0-4ㄷ)대로 **f=1 의 최대 좌표차 2.022914e+00 mm > 10 × 5.959267e-05 mm**
   ⟹ **갈래 (나)**. 참고 상수 f32 ULP(1.0 m) = 1.192093e-04 mm.
 ★ f=0 은 «캐스팅만»이고 5.96e-05 mm = **0.5 ULP** — 계기의 바닥이 제자리에 있다.
 ★ 최대 자리(maxAt)가 f=3 부터 3679 → 3735 → 3624 → 3569 로 **옮겨 다닌다** — 한 정점의 사고가 아니다.
```

#### ★ 주의 — 「f=1」은 「1스텝」이 아니다 (갈래 이름과 정의역의 어긋남 · **함정 43** 등재)

한 프레임 = **229 서브스텝**이고, 한 서브스텝 = **44,920개 제약의 순차 투영**이다.
따라서 (나) 의 문구 「**1스텝** 단위의 논리 차이」를 f=1 값으로 읽으면 **10,282,680회의 투영**을 「1스텝」이라
부르게 된다. 이 판은 그 자리를 **값으로 가른다** — 프레임 «밑»으로 내려간 보조 채널을 새로 만들었다
(`scripts/v4SubstepDump.ts` + `gpu/l3sub_run.py` · 양쪽 다 `dt = DT/229 · substeps = 1` 규약 ·
`solver.ts:1050` 이 `h = dt/substeps` 이므로 **정확히 한 서브스텝**이다).

| 서브스텝 s | 0 | **1** | 2 | 4 | 8 | 16 | 32 | 64 | 128 | 229(= f 1) |
|---|---|---|---|---|---|---|---|---|---|---|
| 최대 좌표차(mm) | 5.959e-05 | **6.529e-04** | 1.851e-03 | 5.820e-03 | 1.585e-02 | 4.609e-02 | 1.460e-01 | 3.638e-01 | 1.295e+00 | 1.960e+00 |
| 중앙 거리(mm) | 2.436e-05 | 6.546e-05 | 1.739e-04 | 4.801e-04 | 1.359e-03 | 4.664e-03 | 1.833e-02 | 6.638e-02 | 2.295e-01 | 6.273e-01 |
| 직전 값 대비 배수 | — | **10.96** | 2.84 | 3.14 | 2.72 | 2.91 | 3.17 | 2.49 | 3.56 | 1.51 |

```
 ★ **s=1(한 서브스텝) 의 최대 좌표차 = 6.529e-04 mm = f32 ULP(1 m) 의 5.5배**(투영 44,920회 뒤의 값).
   f=0 대비 비는 **10.96** — 사전 등재 문턱 10 을 «간신히» 넘는다.
 ★ 이후 구간을 배로 늘릴 때마다 **2.5~3.6배**로 «매끄럽게» 커진다 — 어느 자리에도 «점프»가 없다.
   (있었다면 그 서브스텝 구간이 논리 차이의 자리 후보였을 것이다. 없다.)
 ★ **두 읽기를 둘 다 적는다**(v4-11 §1-③ 과 같은 처분):
   ㉠ **등재 갈래대로 = (나)** — 정의역 f=1(프레임)에서 비 33,945.68.
   ㉡ **보조 채널(서브스텝) = (가) 의 서명** — 시작이 ULP 급(5.5 ULP)이고 매끄럽게 커진다.
   어느 쪽으로 셀지는 «갈래를 쓴» 전략 세션 몫이다. **자리 후보 등재 0 · 처방 0 · 단정 0.**
 ★ 이 보조 채널의 h 는 `DT/229` 로 본 실행과 «같은 식»이고, v4 쪽은 그 값이 f32 로 반올림된다(≤1 ULP).
 ★ ①(f64 가 v3 를 자릿수 전량 재현)과 ㉡ 은 **같은 방향**이다 — 그러나 이 판은 그것을 «판정»으로 잇지 않는다.
```

### §1-③ «진짜» 병렬 커널 — **(가)** ㄱ 성립 + 배수 **57.34 > 2**

신설 = `gpu/engine/full_par.py`. `full_color2.FullColor2` 를 **상속**한다 —
투영 산술 4종은 `full.Full` 의 `@ti.func` 를 **그대로** 물려받고(물리 식 0줄), 색 분할(8색/14색)과
순열도 `FullColor2.__init__` 의 것을 그대로 쓴다. **바뀐 것은 런타임 `if` 를 없앤 것 하나**다
(색 루프가 커널의 «최상위»에 선다 · `use_*` 갈래는 제품 정의역 `all` 하나로 고정하고 그 밖은 예외를 던진다).

#### ㄱ 병렬 확인 — **셋 다 성립**(실행 «전» · `gpu/par_probe.py` 신설)

| 조건 | 실측 | 성립? |
|---|---|---|
| ㉠ 프로파일러 태스크/호출 == 코드에서 «센» 수 | **29.00 == 29**(§0-4ㄹ) · 종류 29 · 호출 5회 | **성립** |
| ㉡ 색 루프 태스크의 grid/block > 1 | `range_for` **28종** · block **128** · grid 최대 **209**(굽힘 λ) · **grid>1 인 태스크 26종** | **성립** |
| ㉢ IR 에 `range_for` 존재 | 최종 IR offloaded **29개**(그중 `range_for` **28개**) · 정본 `docs/v4/12-ir-par-offload.txt` | **성립** |
| (대조군 · 함정 42 규범 ③) `FullColor2.substep` | **6.00 태스크/호출** — v4-11 실측 6.00 **재현** · 그중 `…_serial` 1개(grid 1 × block 1) | 계기 살아 있음 |

```
 ★ IR 의 색 구간이 «값으로» 맞는다 — 늘어남 8구간 합 **18,016** · 굽힘 14구간 합 **26,648**
   (제약 수와 일치 · 예: `range_for(2978, 5930)` · `range_for(26590, 26648)`).
 ★ 봉제만 `offloaded`(range_for 없음) 1개 — `ti.loop_config(serialize=True)` 를 그대로 뒀다(정의역 밖).
 ★ v4-11 대조 — `full.py:run` 의 최종 IR 은 offloaded **1개** · `range_for` **없음** · grid/block 표기 **없음**.
 ★ 색 루프 태스크의 grid 는 **17~24**(색당 제약 2,110~2,952 ÷ 128) — 코드의 색 크기와 1:1로 맞는다.
```

#### ㄴ 속도 · 정확도 (60프레임 · JIT 27.2 s 는 벽시계 «밖» · 단독 실행)

| 채널 | 값 |
|---|---|
| 벽시계 | **13.3 s / 60프레임** = **0.221 s/프레임** |
| **직렬 12.690 대비 배수**(§0-4ㅁ 분모) | **★ 57.34배** |
| 발산 · NaN · degenerate | **0 / 0 / 0**(전 정점 유한) |
| 창 순변위 궤적 f10~f60 | 2.712393e-02 · 2.310992e-02 · 1.889386e-02 · 2.789009e-02 · 1.759260e-02 · **3.062315e-02** |
| 궤적 «모양» | **직렬 f32 와 같은 진동**(감소 아님) — ① 이 말한 대로 f32 인 한 색 분할과 무관하다 |
| 병렬 ↔ 직렬 좌표차(f=60) | 중앙 **0.642** · p99 **7.169** · 최대 **16.248** mm(정점 8301) · 평균 변위 (−0.200, −0.139, −0.019) mm |
| (대조) v4-10 색분할2 ↔ 직렬 | 중앙 1.013 mm |
| (대조) v4-11 직렬 ↔ v4-10 직렬 | **0.000 mm**(중앙·p99·최대 전부) — 회차 간 비트 재현 |

리포트 5행(f=60 · `scripts/v4FitReport.ts` · **값만 · 판정 0**)

| 행 | **병렬**(이 판) | 직렬(v4-10) | f64(이 판 §1-①ㄷ) |
|---|---|---|---|
| 목선 | **1.2804** | 1.59 | 1.0650 |
| 가슴 | **4.5207** | 4.89 | 4.0274 |
| 허리 | **27.8677** | 27.32 | 26.7097 |
| 밑단 | **20.8629** | 22.96 | 19.1641 |
| 소매 | **4.7606** | 4.68 | 4.4996 |

```
 ⟹ **갈래 (가)** — ㄱ 성립 + 배수 **57.34 > 2**. (라) 아님(발산·NaN 0).
 ★ 「병렬화로는 안 빨라진다」(v4-10 §4)는 이 값으로 **57배 차이로** 뒤집힌다 — 함정 42 대로
   **그때까지 병렬로 돈 적이 없었다.**
 ★ 배수의 «출처»는 두 겹이다(사실만): 한 스레드(grid 1×block 1) → 26개 태스크가 block 128 로 퍼졌고,
   커널 호출당 태스크가 6 → 29 로 늘었다. **분해는 하지 않았다**(이 판의 물음이 아니다).
 ★ 정확도는 **판정 0**(§0-4ㅁ) — 색 순서가 v3 배열 순서와 다르고, f=60 은 **정착 자리가 아니다**.
```

---

## §2 자기검사

### §2-1 집행 / 미집행 총계

**집행한 것**

| # | 무엇 | 계기(신설) | 실행 |
|---|---|---|---|
| ① | f64 arch probe 2건(CUDA · x64 · 각 1프레임) | `gpu/l3f64_run.py` | 69.3 s(GPU) + 5.9 s(CPU) · **서로 동시** |
| ① | **f64 60프레임**(x64 · 단독) | 〃 | **57.8 s** |
| ① | f64 f=60 리포트 5행 | `scripts/v4FitReport.ts`(diff 0 · 인용) | 물리 0프레임 |
| ② | v3 f64 **프레임별** 덤프 10프레임 | `scripts/v4FrameDump.ts` | 95.3 s(9,525 ms/프레임) |
| ② | v4 f32 CUDA 10프레임 + 대조표 | `gpu/l3fd_run.py` | 127.3 s(12.730 s/프레임) |
| ②보조 | v3·v4 **서브스텝 경계** 10점 대조 | `scripts/v4SubstepDump.ts` · `gpu/l3sub_run.py` | 9.3 s + 13.5 s |
| ③ㄱ | 태스크 수·grid/block(신설판 + 대조군) | `gpu/par_probe.py tasks` | 1분 미만(5호출) |
| ③ㄱ | 최종 IR(`print_ir`) | `gpu/par_probe.py ir` | 1분 미만(1호출) |
| ③ㄴ | **병렬 60프레임** | `gpu/l3par_run.py` · `gpu/engine/full_par.py` | **13.3 s**(+ JIT 27.2 s) |
| ③ㄴ | 병렬 f=60 리포트 5행 · 좌표차 대조 | `scripts/v4FitReport.ts` · numpy | 물리 0프레임 |

**실행하지 «않은» 것**(침묵으로 넘기지 않는다)

```
 v3 의 **f=60 위치 덤프 0** ⟹ f64 f=60 리포트를 v3 와 «대조하지 않았다»(§1-①ㄷ 에 그대로 적었다).
 f64 × 병렬 결합 실행 **0**(= `full_par` 를 f64 로 돌리기) — 등재 갈래에 없다. **사후 갈래 신설 0.**
 셀프 충돌 이식 **0** · 최적화·튜닝(block_dim·색 순서·`if` 밖 이동 외 어떤 변경도) **0** ·
 `src/` 수정 **0** · `engine/` 기존 7파일 수정 **0** · 굽기 **0** · 캡처 **0**(§3) ·
 정본/blob 재생성 **0** · v3 재실행은 **이 판의 덤프 2건뿐**(정본 재계산 0) · 180프레임 **0** ·
 30프레임 축약 **0**(60프레임 완주) · **ecc 에이전트 0 / ecc 스킬 0** · `strategist` 호출 **0**.
```

### §2-2 정밀도 · arch · grid/block · 태스크 · 시간

| 채널 | 값 |
|---|---|
| 기계 | **`DESKTOP-OV72A46`** · win32 · GTX 1660 Ti · Taichi **1.7.4** · llvm 15.0.1 · py **3.12.10** |
| arch (`current_cfg().arch`) | **`Arch.cuda`** 5건 · **`Arch.x64`** 2건 — **전부 assert 통과**(v4-01 함정 1 · 조용한 폴백 0) |
| 정밀도 | ① **f64** · ②③ **f32**(`default_fp`) |
| 태스크/호출 | 병렬 **29.00**(코드 29) · 색분할2 **6.00**(v4-11 6.00) · 직렬 **1**(v4-11) |
| grid × block | 병렬 = `range_for` 28종 **block 128** · grid **2~209**(색 루프 17~24) + 봉제 serial 1×1 ·<br>직렬 = **1 × 1**(v4-11) |
| s/프레임 | 병렬 f32 **0.221** · 직렬 f32 12.690(v4-10) · f64 x64 **0.963** · f64 CUDA 69.280(1프레임 probe) |
| 이 판의 총 실행(벽시계 합) | 약 **7분**(GPU 약 4분 · CPU 약 3분) — 예산 60프레임 상한 안 |
| `py -m pytest gpu/tests -q` | **40 passed · 2 xfailed · 1 xpassed** (59.57 s) — v4-10·v4-11 과 **같은 값** |
| `npx tsc -b` | **통과**(exit 0) |

### §2-3 경계 · diff

| 채널 | 값 |
|---|---|
| `src/` diff(기점 `1cc186b` ↔ 이 판) | **0** (`git diff --stat 1cc186b HEAD -- src/` = 빈 출력) |
| `engine/` 물리 7파일 blob 동일 | `full.py` `a631992832dd` · `full_color.py` `7c7a49217664` · `full_color2.py` `1ea95c3af689` · `stretch.py` `7b3d8b6bf8a5` · `bending.py` `6e1972f589d0` · `seam.py` `7abfd6074dc1` · `collide.py` `4d748bb65870` — **7/7 기점과 같은 blob** |
| 정본 diff | **0** — 추적 중인 파일 중 «수정»으로 잡힌 것 0(이 판 산출은 전부 **신규 파일**) |
| 이 판이 «만든» 코드 | `gpu/engine/full_par.py` · `gpu/par_probe.py` · `gpu/l3f64_run.py` · `gpu/l3fd_run.py` · `gpu/l3sub_run.py` · `gpu/l3par_run.py` · `scripts/v4FrameDump.ts` · `scripts/v4SubstepDump.ts` (전부 **신규**) |
| 이 판이 «만든» 산출물 | `l3f64-*`(cuda-f1 · x64-f1 · x64-f60) · `l3fd-v3-*` · `l3fd-cmp-*` · `l3sub-v3-*` · `l3sub-cmp-*` · `l3par-*` · `fit-*-v4-12-*` · `docs/v4/12-ir-par-offload.txt` (전부 새 접두) |
| 굽기 / 캡처 / ecc | **0 / 0 / 0** |

### §2-4 물리 diff 0 의 «바이트» 증명

```
 ① f64 판(`l3f64_run.py`)은 `engine/full.py` 를 그대로 부른다 — 같은 커널이 f32 에서 낸 궤적이
   v4-11 §1-①(두 실행)·v4-10 §1-④ 와 **인쇄 자릿수 전량 동일**함은 v4-11 이 이미 등재했다.
 ② 이 판에서 새로 재현된 것 — **v4-11 직렬 f=60 위치 ↔ v4-10 직렬 f=60 위치 = 0.000 mm**
   (중앙·p99·최대 전부 · 다른 회차 · 다른 프로세스). 회차 사이의 비트 재현이 살아 있다.
 ③ `full_par` 는 `full.Full` 의 `@ti.func` 4종을 **상속으로** 물려받는다 — 투영 산술을 다시 적은 줄 **0**
   (`full_par.py` 에 `_p_inplane` · `_p_bend` · `_p_seam` · `_p_collide` **정의 없음** · 호출만).
```

### §2-5 사무 1건(값 아님)

```
 콘솔 기본 인코딩이 **cp949** 라 한글·«»·— 이 섞인 인쇄가 `UnicodeEncodeError` 로 죽었다(par_probe 1차).
 **`PYTHONIOENCODING=utf-8` 로 실행**해 고쳤다 — 코드·물리 무관 · 산출물 영향 0 · 재실행 1건(1분 미만).
```

---

## §3 화면

**캡처 0.** 이 판은 궤적·태스크·시간만 묻는다(회차 프롬프트 §3).

---

## §4 회차 갈래 — **A**(①②③ 전량) ⟹ 상신

| 항 | 갈래 | 한 줄 |
|---|---|---|
| ① f64 결착 | **(가)** | f64 가 v3 의 감소 곡선을 **자릿수 전량 재현**(f60 2.530865e-04 ↔ v3 2.531e-04) ⟹ **미수렴 원인 = f32** |
| ② 발산 시작점 | **(나)** | f=1 최대 좌표차 **2.023 mm** = f=0 의 33,946배(문턱 10) · **다만 f=1 = 229 서브스텝**이고<br>서브스텝 채널은 s=1 **6.53e-04 mm(5.5 ULP)** 에서 배가마다 2.5~3.6배로 «매끄럽게» 증폭 |
| ③ 진짜 병렬 | **(가)** | ㄱ 셋 다 성립(태스크 29 = 코드 29 · grid 최대 209 · IR range_for 28) · **57.34배**(0.221 s/프레임) · 발산 0 |

**넘기는 것 6건**

```
 1. **f32 가 원인이다**(①) — v4-09~v4-11 이 세 판에 걸쳐 못 닫은 미수렴 후보가 닫혔다.
    바뀐 것은 정밀도 하나뿐이고 코드·정의역·초기 상태는 같다.
 2. **병렬은 «되었고» 57배다**(③) — 「병렬화로 안 빨라진다」(v4-10 §4)가 값으로 뒤집혔다.
    v4 는 이제 grid 1×block 1 이 아니다(색 루프 26개 태스크가 block 128).
 3. **②의 두 읽기** — 등재 갈래는 (나)지만 서브스텝 채널은 (가)의 서명이다. 갈래를 쓴 쪽이 정한다.
 4. **아직 «안 한» 결합** — f64 × 병렬(= `full_par` 를 f64 로)은 **실행 0**. 등재 갈래에 없었다.
    참고 재료: f64 는 CUDA 직렬에서 69.280 s/프레임 · f32 는 병렬로 57배 빨라졌다.
 5. **속도의 새 지형**(사실만) — f64 **x64 0.963 s/프레임** 이 f32 CUDA 직렬 12.690 보다 13.2배 빠르다.
 6. **정착 자리의 대조는 여전히 없다** — f=60 은 v3 문턱(1e-4)으로 아직 «서지 않은» 자리다.
    v3 f=60 위치 덤프도 이 판에 없다.
```

**이 판이 «하지 않은» 판단** — 원인 기전의 단정(왜 f32 가 극한 사이클을 만드는가) · ②의 자리 후보 지목 ·
병렬판 채택 여부 · f64 채택 여부 · 다음 판의 정의역. **처방 0 · 최적화 0 · 사후 갈래 신설 0.**

---

## §5 인용 대조 1건 (사후 · 집행 0 · 새 실행 0 · **노트 값 수정 0**)

**2026-09-01 · 사용자 요구.** 같은 SHA(`3fb2c33`)를 인용한 **두 보고의 값이 어긋났다.**
이 절은 **정본을 고치지 않고**(함정 14) 어긋난 쪽이 «어떻게 생겼는지»만 값으로 가른다.
새 측정 0 · 새 실행 0 ⟹ **`docs/metrics-log.md` 에는 아무것도 적지 않는다**(측정이 아니다).

### §5-1 정본 자기대조 — **어긋남 0**

| 채널 | 노트(`dce37b2` 본문) | metrics(`43db01e`) | 일치 |
|---|---|---|---|
| ① 프레임 수 · 축약 | 60프레임 완주 · 축약 **0** | 「갈래 (가) · 30프레임 축약 0」 | **일치** |
| ① s/프레임 · arch | **0.963** · **x64** | **57.8 s = 0.963 s/프레임** · x64 채택 | **일치** |
| ② f=1 최대 좌표차 | **2.022914e+00 mm** | **2.022914e+00 mm** | **일치** |
| ③ㄴ s/프레임 · 배수 | **0.221** · **57.34** | **0.221** · **57.34** | **일치** |

⟹ **정본 귀책 0.** 두 정본은 인쇄 자릿수 전량 같다.

### §5-2 어긋난 값 3종의 «출처» — 전부 **v4-12 밖**

| 어긋난 값 | v4-12 에 있나 | 실재하는 곳 | 무엇인가 |
|---|---|---|---|
| **30프레임 축약을 썼다** | **없다**(반대가 적혀 있다) | 이 노트 §0-3:46 · §0-4ㄱ:87 · §0-4ㄴ:92 | **조건절**(「예산 2시간 초과 «시»」)이지 결과가 아니다.<br>결과는 §1-①ㄴ:170 「쓰지 않았다」 · §2-1:333 「축약 **0**」 |
| **5.837018e-07** | **0건**(grep) | `docs/v4/02-늘어남.md:242`<br>(인용 v4-03:133 · v4-04:112 · v4-06:173) | **v4-02** 층1 **늘어남 1스텝** 최대 좌표차(**m**) ↔ 상한 4.768372e-07 · 비 1.224 |
| **1.602 s/프레임 · 7.92배** | **0건**(grep) | `7.9배` 만 `docs/v4/02-늘어남.md:303` §4-1 ㉣ | **v4-02** 「직렬 이식이 v3(tsx)보다 **7.9배 느리다**」(6.512s ↔ 825ms).<br>**방향이 반대인 비**이고 병렬 가속 배수가 아니다 |

```
 ★ 세 값이 서로 무관하지 않다 — ②③ 이 **한 판(v4-02 늘어남)** 으로 수렴한다.
 ★ 혼입이 쉬운 «자리»가 값으로 있다: v4-02:244 의 「f64→f32 캐스팅만 **5.959267e-08 m**」 은
   이 판 §1-② 의 f=0 **5.959267e-05 mm** 와 **같은 수**다. 두 판의 «바닥»이 동일하므로
   그 옆줄(5.837018e-07)을 이 판의 f=1 로 오인하기 좋다. 게다가 그 값은 v4-02 에서 **진짜 1스텝**
   (투영 18,016회)이라 **함정 43 이 지적한 단위 혼동과 정확히 겹친다.**
 ★★ **1.602 는 인용이 아니라 «역산»이다** — 12.690 ÷ 7.92 = 1.60227… 이고 볼트 전량 grep 0건이다.
   읽어서는 나올 수 없는 수 ⟹ **그 SHA 를 열지 않았다는 값 증거**다(역산은 열람의 반대 증거).
```

### §5-3 기전 · 귀책

```
 기전 = **값 재활용(v4-02 → v4-12)** + **조건절의 결과화**(§0 규약을 §1 결과로 읽음) + **역산**.
       인용 SHA 는 3fb2c33 으로 «맞게» 적혔으나 그 SHA 를 실제로 열지 않았다 —
       열었다면 :170·:333 과 §1-③ㄴ 표를 지나칠 수 없고, 볼트에 없는 1.602 를 만들 수 없다.
 ⟹ **기억 서술**(다른 판의 값을 기억으로 채우고 SHA 만 이 판 것으로 붙였다).

 귀책 = **어긋난 보고를 쓴 쪽(인용 주체).** 정본 귀책 **0**.
       볼트는 그 보고의 작성자를 기록하지 않는다 ⟹ 이름은 사용자가 확정한다
       (전략 세션이면 전략 세션 · 다른 CC 세션이면 CC). **이 절은 이름을 단정하지 않는다.**
 처분 = **노트·metrics 값 수정 0**(함정 14) · 회수한 판정 **0**(어긋난 값이 볼트에 등재된 적 없다) ·
       새 함정 **1건** 등재([[함정44-인용SHA와값의출처]]).
```
