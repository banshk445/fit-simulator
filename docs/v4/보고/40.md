# v4-40 — `C_chest` 소비자 사실 · A포즈 그리드 몸 27 브라우저 하네스 준비(생성 0)

**물리 0줄 · `src/` diff = 하네스 «신설 파일» 한정(기존 파일은 최소 배선 · 원문 등재) · 굽기 0 · 몸 생성 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-40-grid-prep` |
| 기점 | **`3f5e7bd`**(= v4-39 §1~§4 + 볼트 + 보고) |
| §0 커밋 | **`75c10c8`**(집행 «전») |
| §1~§4 커밋 | **`161cf5f`**(metrics `1cbf397` · 볼트 갱신 포함) |
| 보고 파일 | `docs/v4/보고/40.md` · 절차서 `docs/v4/40-실행절차.md` |

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v4-40-grid-prep 3f5e7bd → Switched to a new branch 'v4-40-grid-prep'
       $ git branch --show-current → v4-40-grid-prep · HEAD 3f5e7bd
 2호기 (§0 커밋을 push 한 «뒤» 같은 자리로 전환 — §0-5 에 인쇄)
```

---

## §0 수령·사무

### §0-1 판정문 (전략 세션 · v4-39 §4 수령 · 원문)

```
 「갈래 A 수령 · ★★★ chestY 재정의 성립(두 포즈 지목 구간 안 · A 가슴 행 겨드랑이 복귀 ·
   중앙 42.08→2.08 · 밀착/여유 123/128) · A포즈 기본몸 M = 게이트·화면·층3 «제품 의미 완결» ·
   ★ T포즈 가슴 한 행만 변화(교집합 0 도출) · 게이트 불변 · v3 산출물 쓰기 0 — 등재대로 ·
   ★★ C_chest 처분 보류: 새 높이의 볼록껍질은 삼각근 포함(119.9cm) — 줄자 관행과 다름 ·
     소비자 사실부터(grep) · 소비 0 이면 진단값으로 등재만 ·
   ★ 두 포즈 chestY 차 45.9mm = A export 전역 y 오프셋 지배(바닥 기준 10.3mm) · 사실 등재로 종결 ·
   ★★ 다음 = 그리드 몸 27(백로그 0번 · 브라우저 경로) — CC 준비 · 승혁 실행 판」
```

### §0-2 바꾸지 않는 것 (회차 프롬프트 원문)

```
 물리 **0줄** · `bodyLevels`(v4-39 상태) **0** · `garmentScene`·`dressRun`·워커 **0** ·
 정본·blob·문턱 **0** · `Mannequin.tsx` 본체 **0**(하네스는 «호출»만)
```

### §0-3 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · 단위 명시)

**★ 단위** — 몸 수는 **칸**(27) · 각도는 **도**(35°) · 수렴 판정은 **m**(연속 프레임 정점 최대 변위) ·
소비처는 **파일:줄**.

| 항 | 갈래 |
|---|---|
| **①** `C_chest` 소비자 | **(가)** 소비 0(진단·표시뿐) ⟹ 「의미 변경 등재」 종결 문안 초안 / **(나)** 소비 존재 ⟹ 소비처별 신구 영향(값) · 처분은 전략 세션 |
| **②** 브라우저 하네스 | **(가)** 완성(승혁 실행 절차서 포함) / **(나)** 막힘(코드 사실·사유) |
| **③** 절차서 | ②(가) 전제 · 단계마다 「실패 시 보낼 것」 명시 |

**§4** — A = ①+②(가)+③ ⟹ 상신(다음 판 = 승혁 실행) / B = ②(나) ⟹ 사실 상신 / C = 그 외 ⟹ 정지.

### §0-4 사전 결정 4건 (값 보기 «전» · CC 집행 설계)

```
 ㄱ **소비 판정의 정의를 지금 고정한다** — 「소비」 = 그 값이 **판정·문턱·매칭·물리·산출 파일**에 들어가는 것.
    **화면 인쇄·JSON 기록·로그**는 «표시/진단»으로 분류한다. grep 은 이름 3종
    (`C_chest`·`cChestCm`·`C_waist`/`cWaistCm`)으로 전수하고 **파일:줄을 전부 적는다**.
 ㄴ **하네스는 «신설 파일»이 원칙** — 기존 파일 수정이 불가피하면 **줄 수를 최소로** 하고
    **diff 원문을 노트에 싣는다**. `Mannequin.tsx` 본체·`garmentScene`·물리는 **건드리지 않는다**.
 ㄷ **수렴 판정은 «몸에서» 유도한다**(손 상수 0) — A포즈 회전이 안정됐는지는
    **연속 프레임 정점 최대 변위**로 재고, 문턱은 **이미 이 저장소가 쓰는 값**을 인용한다
    (`consts.ts` 의 `TOL_SELF` 계열 · 어느 상수를 인용했는지 노트에 적는다).
 ㄹ **몸 생성은 이 판에서 «하지 않는다»** — 하네스와 검증기와 절차서까지만.
    산출 27칸은 **승혁이 실행**하고, 검증은 다음 판이 한다.
```

### §0-5 커밋·전환 인쇄 (집행 «전»)

```
 §0 선커밋 **`75c10c8`** — v4-40 §0 수령·사무(집행 «전») · push 완료
 2호기 $ git checkout -- src/v3/bodyLevels.ts && git checkout -q v4-40-grid-prep && git pull -q
        $ git branch --show-current → v4-40-grid-prep
        $ git log --oneline -1     → 75c10c8 v4-40 §0 — 수령·사무(집행 «전» 선커밋)
```

---

## §1-① `C_chest` 소비자 사실 — **(가) 소비 0**(표시·진단뿐)

이름 4종(`C_chest`·`cChestCm`·`C_waist`·`cWaistCm`)을 `src`·`scripts`·`gpu` 전수 grep 했다(코드 0줄).

| 파일:줄 | 용도 | 분류(§0-4ㄱ) |
|---|---|---|
| `src/v3/bodyLevels.ts:11` | `Levels` 타입 선언 | — (선언) |
| `src/v3/bodyLevels.ts:143` | `const C_chest = at(chestY).C` — 산출 | — (산출) |
| `src/v3/bodyLevels.ts:179` | `return { … C_chest, C_waist … }` — 반환 | — (산출) |
| `src/v3/fitReport.ts:38` | `levels` 타입에 `cChestCm`·`cWaistCm` | — (선언) |
| `src/v3/fitReport.ts:175` | `cChestCm: L.C_chest * 100` — **JSON 기록** | **표시/진단** |
| `src/components/FitReportTable.tsx:32` | 화면에 「둘레 …cm」 인쇄 | **표시** |
| `scripts/v3Sensitivity.ts:72` | 콘솔 인쇄 | **진단** |
| `scripts/v3FitTable.ts:23` | 콘솔 인쇄 | **진단** |
| `scripts/v3BodyLevels.ts:23-24` | 콘솔 인쇄 | **진단** |
| `scripts/v3BodyLevels.ts:29` | 「물리 정합 `C(chestY) > C(waistY)`」 **인쇄** — 성립/불성립 문자열 | **진단**(반환·throw 0) |
| `scripts/v3BodyLevels.ts:31` | 결정성 서명 문자열에 포함 | **진단** |
| `scripts/v4Levels.ts:49` | v4-38 계기 인쇄 | **진단** |

**판정·문턱·매칭·물리·산출 파일에 들어가는 자리 = 0건.**
`scripts/v4ProductGate.ts` 는 `bodyLevels` 를 **부르지 않는다**(v4-39 §1-② 확인 · grep 0) ·
`buildFitReport` 의 대역은 `chestY`·`waistY`(**높이**)로만 잡히고 `C_chest` 는 쓰이지 않는다
(`src/v3/fitReport.ts:107-108`) · 사이즈 매칭은 `body.chestGirthM` 라는 **다른 채널**을 쓴다.

⟹ **(가)**. 판정문이 요구한 **「의미 변경 등재」 종결 문안 초안**(문안일 뿐 · 처분 0):

```
 「`C_chest`(= `cChestCm`)는 **진단·표시 값**이다 — 판정·문턱·매칭·물리·산출 어디에도 들어가지 않는다.
   v4-39 에서 `chestY` 가 «팔이 몸통 고리에 흡수되는 높이»(겨드랑이)로 바뀌면서 그 높이의
   **볼록껍질 둘레는 삼각근을 포함**한다(T포즈 M: 95.54 → 119.94 cm). 줄자 관행의 가슴둘레와 다르므로
   **이 값을 「가슴둘레」로 읽지 않는다** — 이름은 `C_chest`(그 높이의 단면 볼록껍질 둘레)로 두고
   화면·로그에 그대로 인쇄하되 제품 수치로 승격하지 않는다. 승격이 필요해지면 그때
   «줄자 규약»(팔 제외 몸통 둘레)을 새 채널로 «따로» 만든다 — 이 판은 만들지 않는다.」
```

## §1-② 브라우저 하네스 — **(가) 완성**

### ㄱ 기존 그리드 몸 생성 경로 사실(v3-77·83·85 · 코드에서 읽음)

| 자리 | 사실 |
|---|---|
| `src/components/V3Panel.tsx:413 bakeGrid` | T포즈 27칸 하네스. 목록은 순수 모듈 `src/v3/grid.ts` `bodies()`·`bodyIdOf()`·`FIXED` |
| 같은 함수 `:437-441` | 칸마다 슬라이더 3축(가슴·키·어깨) · **팔·다리는 `FIXED`** 로 고정 |
| 같은 함수 `:443-448` | 수렴 = `poseStopped() && maxScaleResidual <= POSE_SETTLE_EPS` **연속 2프레임** · 상한 **600** |
| 같은 함수 `:457` | 굽기 = `bakeBodyVerts(glb, root, "tpose")` ⟹ **여기가 T포즈 전용인 부분** |
| `src/components/bodyInjectBake.ts:82-105` | `"tpose"` 면 `userData.__p27Bind` 로 되돌린다 · `"apose"` 면 **지금 자세 그대로** 굽는다 |
| `src/components/Mannequin.tsx:228-283` | 제품 A포즈는 `outwardDown = (sign·0.6, −1, 0)` — **v4 의 35° 규칙과 다른 자세** |
| `scripts/v4Apose.ts:75-85` | v4 A포즈 규칙 = 팔 축을 뼈에서 읽어 **월드 z 둘레 35° 내림** · Node 라 **기본 몸 하나**뿐 |
| `scripts/v3Receiver.ts` | 반출 규약 — `POST /put?name=&sha256=` · **sha 대조 뒤에만 저장** · `--n` 채우면 자동 종료 |

⟹ **막힌 곳은 하나** — 27칸의 «치수»는 `Mannequin.tsx` 뼈 배율 lerp 로만 걸려 Node 로는 못 만든다.
그래서 v3-83 하네스의 절차를 그대로 쓰고 **포즈만** 바꾼다.

### ㄴ A포즈 파라미터와 수렴 판정(손 상수 0 · 전부 인용)

```
 각도   DEG = **35°**(v4 규약 · `?apodeg=` 로만 바뀐다) · 회전축 = **월드 z** · 좌우는 부호만 —
        `scripts/v4Apose.ts:79-85` 와 «같은 줄»
 시작   제품 A포즈(0.6 outward)가 이미 걸려 있으므로 **바인드로 되돌린 뒤** 규칙을 적용한다
        (되돌리는 세 줄 = `bodyInjectBake.ts:97-105` 의 `__p27Bind` 사본 · **뼈 배율은 손대지 않는다**
         ⟹ 슬라이더가 만든 치수는 남는다)
 수렴   `poseStopped() && mannequinPoseRef.maxScaleResidual <= POSE_SETTLE_EPS` **연속 2프레임** ·
        상한 **600 프레임**(도달 = 그 칸 실패 · 저장 0) — v3-83 과 같은 절 ·
        `POSE_SETTLE_EPS` 는 `src/lib/mannequinRef` 의 **기존 상수**(이 판이 정한 수 0)
 순서   ① 슬라이더 → ② 수렴 대기 → ③ 바인드 복원 → ④ 35° 적용 → ⑤ `bakeBodyVerts(…, "apose")`
        ★ ④와 ⑤ 사이에 **프레임을 전진시키지 않는다** — 전진하면 제품 A포즈 루프가 자세를 덮는다
```

### ㄷ 하네스 화면 — `src/components/V4AposeGrid.tsx`(**신설**)

버튼 하나(「A포즈 그리드 몸 굽기(27칸)」) · 진행 표시(`3/27 · <칸>`) ·
칸별 표(결과 · 전진 프레임 · 잔차 · 높이 m · 반출 수 · 사유) · 완료 표시(`완료 — 통과 n/27`).
산출은 **수신기 경유**(자동 다운로드 0) · 칸마다 3개 + 색인 1 = **82개**.

**기존 파일 수정은 배선 2곳뿐**(diff 원문 · `src/components/V3Panel.tsx` +4 / −0):

```
 @@ import { bodies, bodyIdOf, FIXED } from "../v3/grid.ts";
 +/* v4-40 §1-② — **A포즈 그리드 하네스**(신설 파일 · 이 줄과 아래 한 줄이 배선의 전부다). */
 +import { V4AposeGrid } from "./V4AposeGrid.tsx";
 @@         <span className="opacity-60">결과는 콘솔 `[v3-71]`</span>
          </div>
 +      {/* v4-40 §1-② — A포즈 그리드 몸 27칸(승혁 실행 · 절차서 `docs/v4/40-실행절차.md`). */}
 +      <div className="mb-1"><V4AposeGrid /></div>
```

★ `bakeGrid`(T포즈 27칸)·`Mannequin.tsx`·`garmentScene`·물리는 **한 줄도 건드리지 않았다**.

### ㄹ 산출 검증기 — `scripts/v4GridVerify.ts`(**신설** · 판정만)

진입 `[DIR=gpu/oracle/export/grid27] [BASEDIR=gpu/oracle/export] [DEG=35] npx tsx scripts/v4GridVerify.ts`.
채널 8종 — ㉠ 파일 셋 존재 · ㉡ `4×3n` 모양과 칸 간 `n` 일치 · ㉢ 비유한 0 · ㉣ 높이와 목표 차 mm ·
㉤ **층3 성립**(`deriveLevels` 가 `Y_LOW`·`chestY`·`waistY`·`C_*` 를 내는가 · `chestY > waistY` 포함) ·
㉥ 팔 축 단위벡터·`sin(35°)` 기대차 · ㉦ 피벗 유한·좌우 |x|·y 차 ·
㉧ **기본 몸 교차 확인**(브라우저 산출 ↔ Node 산출 `l3ap-body-c100-h170-s45-a35.bin` 의 sha·최대 좌표차 mm).

**계기 스모크(산출 «전» · 이 판에서 실행)**

```
 $ DIR=gpu/oracle/export BASEDIR=gpu/oracle/export npx tsx scripts/v4GridVerify.ts
 칸 27 · 통과 **1** · 나머지 26 «없음»(아직 굽지 않았다 — 이 판은 몸 생성 0)
 기준칸 c100-h170-s45 — n **15882** · 높이 목표차 **+65.05167828872804 mm** · 층3 레벨 **성립**
 교차확인 — 같은 파일을 맞댄 자기 대조(기준 sha c5ef87759711da60f3bd68066814cc706a1db245c75fe047a1fbc7e1c7786c81 ·
 최대 좌표차 0) ⟹ 승혁 산출이 들어오면 «진짜» 대조가 된다
```

★ **높이 목표차 +65.05167828872804 mm 는 사실로 남긴다** — A포즈는 팔이 내려와 `ymax − ymin` 이 키와 다르다.
그 차의 처분(높이 채널을 A포즈에 어떻게 걸 것인가)은 **이 판이 판단하지 않는다**.

## §1-③ 승혁 실행 절차서 — `docs/v4/40-실행절차.md`

8단계(준비 → 수신기 → dev 서버 → `?v3=1` → 클릭 → 완료 확인 → 검증기 → push → 통보) ·
**단계마다 「막히면 보낼 것」**(출력 전문 · 스크린샷 · 콘솔) 명시 · 옵션 3종(`apodry`·`apoonly`·`apodeg`) ·
산출 이름 규약 표. **몸 생성은 이 판에서 하지 않았다**(§0-4ㄹ).

---

## §2 자기검사

| 항 | 값 |
|---|---|
| `src/` diff vs 기점 `3f5e7bd` | **`src/components/V3Panel.tsx` +4 / −0**(배선 두 자리 · 위 diff 원문) + **신설 파일** `src/components/V4AposeGrid.tsx` · 물리 **0줄** |
| `scripts/` 신설 | `scripts/v4GridVerify.ts`(검증기 · 판정만) |
| `bodyLevels`·`garmentScene`·`dressRun`·워커·`gpu/engine/` | **diff 0**(`git diff 3f5e7bd -- src gpu` 출력이 `V3Panel.tsx` 한 줄뿐) |
| **기존 T포즈 그리드 경로 불변** | `git diff -U0` 의 훅 위치가 **`@@ -32,0` (import 절)** 과 **`@@ -794,0` (JSX 꼬리)** 둘뿐이다 — `bakeGrid` 본문(413~560)에 **변경 0줄** ⟹ v3-83 경로는 **바이트 그대로** |
| `Mannequin.tsx` | **0줄**(하네스는 «호출»만 · §0-2) |
| 굽기 · 몸 생성 · 캡처 | **0 · 0 · 0**(§0-4ㄹ 대로 — 27칸 생성은 승혁 실행 판) |
| 검증기 스모크 | 실행함 — 칸 27 · 통과 1(기준 몸) · 나머지 「없음」 · 층3 레벨 성립 확인 |
| `npx tsx scripts/v4GridVerify.ts` | 종료 0 · 산출 `gpu/oracle/export/l3ap-grid-verify-a35.json` |
| `npx tsc -b` | **통과**(종료 0 · 하네스·검증기 포함) |
| pytest(2호기) | ★ **미집행** — 2호기 ssh 이름 해석 실패(`ssh: Could not resolve hostname desktop-ov72a46`) 3회. **이 판은 `gpu/` 변경 0 · 굽기 0** 이라 회귀 대상이 아니지만, **돌리지 못한 사실을 그대로 적는다**(은폐 0) |
| 브랜치 인쇄 | §0 머리에 에어·2호기 둘 다 · §0 은 집행 **«전»** 커밋(`75c10c8` · 자기기입 `f365d5c`) |
| ecc 에이전트 / 스킬 | **0 / 0** |

## §4 회차 갈래 — **A**(① + ②(가) + ③)

**넘기는 것 6건**

1. ★★★ **`C_chest` 소비 0** — 12개 자리 전부 선언·산출·표시·진단이다. 판정·문턱·매칭·물리·산출 파일 **0건** ⟹
   판정문이 말한 「진단값으로 등재만」이 성립한다. 종결 문안 초안은 §1-① 에 있다(처분은 전략 세션).
2. ★★★ **하네스 완성** — 신설 파일 하나(`V4AposeGrid.tsx`) + 배선 **4줄** · 문턱은 전부 인용(새 수 0) ·
   포즈 규칙은 `scripts/v4Apose.ts:79-85` 와 **같은 줄** · 순서에 **바인드 복원**을 넣어 제품 A포즈(0.6 outward)와
   섞이지 않게 했다(④ ⑤ 사이 프레임 전진 금지가 이 하네스의 핵심 제약이다).
3. ★★ **검증기 8채널 + 교차 확인** — 브라우저 산출과 Node 기준 몸을 **sha·최대 좌표차**로 맞댄다.
   같은 규칙이면 기본 칸은 «같은 몸»이어야 한다(승혁 산출이 오면 그 값이 첫 판정 재료다).
4. ★★ **A포즈 높이 사실** — 기준 몸에서 `ymax − ymin` 이 목표 키보다 **+65.05167828872804 mm** 다.
   v3-83 은 높이를 **기록 열**로만 뒀는데(게이트 아님), A포즈에서는 그 차가 더 크다는 사실을 병기한다.
5. ★★ **절차서** — 8단계 · 단계마다 「막히면 보낼 것」 · 옵션 3종 · 파일 82개 규약(27×3 + 색인 1).
   수신기는 `--out gpu/oracle/export/grid27 --n 82` 로 띄운다(**기존 기준 몸을 덮지 않게 폴더를 가른다**).
6. ★ **pytest 미집행**(2호기 이름 해석 실패) — `gpu/` 변경 0 이라 대상은 아니지만 사실로 남긴다.

**이 판이 하지 않은 판단** — `C_chest` 처분 · A포즈 높이 채널의 처분 · 27칸 산출의 품질(다음 판) · 화면.

---

## 부록 — raw 링크 (커밋 `b20f165` 고정)

**수정** — `src/components/V3Panel.tsx`(+4 배선) · `.obsidian-log/전략세션-인수인계.md`(v4 블록 교체) ·
`.obsidian-log/00-현재상태.md` · `.obsidian-log/v4-노트-색인.md` · `docs/metrics-log.md`(`metrics:` `1cbf397`)

**신규** — `src/components/V4AposeGrid.tsx`(하네스) · `scripts/v4GridVerify.ts`(검증기) ·
`docs/v4/40-그리드준비.md`(정본 노트) · `docs/v4/40-실행절차.md`(**승혁 실행 절차서**) · `docs/v4/보고/40.md`

- https://raw.githubusercontent.com/banshk445/fit-simulator/b20f165/docs/v4/%EB%B3%B4%EA%B3%A0/40.md
- https://raw.githubusercontent.com/banshk445/fit-simulator/b20f165/docs/v4/40-%EA%B7%B8%EB%A6%AC%EB%93%9C%EC%A4%80%EB%B9%84.md
- https://raw.githubusercontent.com/banshk445/fit-simulator/b20f165/docs/v4/40-%EC%8B%A4%ED%96%89%EC%A0%88%EC%B0%A8.md
- https://raw.githubusercontent.com/banshk445/fit-simulator/b20f165/docs/metrics-log.md
- https://raw.githubusercontent.com/banshk445/fit-simulator/b20f165/.obsidian-log/%EC%A0%84%EB%9E%B5%EC%84%B8%EC%85%98-%EC%9D%B8%EC%88%98%EC%9D%B8%EA%B3%84.md
- https://raw.githubusercontent.com/banshk445/fit-simulator/b20f165/.obsidian-log/00-%ED%98%84%EC%9E%AC%EC%83%81%ED%83%9C.md
- https://raw.githubusercontent.com/banshk445/fit-simulator/b20f165/.obsidian-log/v4-%EB%85%B8%ED%8A%B8-%EC%83%89%EC%9D%B8.md
