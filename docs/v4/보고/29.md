# v4-29 보고 (전략 세션 제출본 · 터미널 출력 전량)

**2026-09-04 · 집행 완료 · 브랜치 `v4-29-pivot-anchor` · 갈래 B**

> CLAUDE.md 「v4 회차 보고 «저장»」 규약 — 이 파일이 전략 세션 전달본이다.
> 집행 정본 노트는 `docs/v4/29-피벗배치.md` · 캡처는 `docs/v4/캡처/29-1-front.png` · `29-2-side.png`.

---

# v4-29 — 축선 오프셋 사실 · 피벗 배치 가상 실험 · (성립 시) 원점 분리 처방 + 재탐침

**물리 0줄 · `src/` diff = ③ 성립 시 `axPoint` 원점 분리 «한정» · 굽기 = ③ 성립 시 1칸 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-29-pivot-anchor` |
| 기점 | **`8507d92`**(= v4-28 §1~§4) |
| §0 커밋 | **`db3623f`**(집행 «전») |
| §1~§4 커밋 | (집행 후 기입) |
| 보고 파일 | `docs/v4/보고/29.md` |

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v4-29-pivot-anchor 8507d92 → Switched to a new branch 'v4-29-pivot-anchor'
       $ git branch --show-current → v4-29-pivot-anchor · HEAD 8507d92
 2호기 (§0 커밋을 push 한 «뒤» 같은 자리로 전환 — §0-5 에 인쇄)
```

---

## §0 수령·사무

### §0-1 판정문 (전략 세션 · v4-28 §4 수령 · 원문)

```
 「갈래 A 수령 · ★★ 값 3건이 «자리» 문제를 가리킨다(크기 아님):
   몸SDF 단독 지배 · 61.5 관이 팔을 10.5mm 관통 · RMIN 에서 이미 −7.7 ·
   ★★ 산술 사실 등재: 관 축선(C 출발)과 팔 축선(피벗 출발)의 수직 오프셋 ≈ 102mm
     (d = 피벗 − C · |d − (d·AX)AX| · 보고 수에서 계산) —
     v4-26 의 「axPoint 도 같은 원점」 승인이 대역 측정과 원통 배치라는 두 의미를 한 변수 C 에 실었다
     (함정 37 의 기하판) · 귀책 = 전략 세션(6건째) ·
   ★ 확정은 실험이 한다(5회 기각 전과) — 피벗 배치 가상 실험이 가르고, 성립 시에만 처방 ·
   ★ CC v4-27 읽기 자기 정정 수용」
```

**★ 귀책 등재(전략 세션 · 여섯째)** — v4-26 의 「`axPoint` 도 같은 원점 의미」 승인이 **대역 측정**과
**원통 배치**라는 두 의미를 **한 변수 C** 에 실었다. 판정문이 이를 자기 귀책으로 등재했고, 본 회차는
그 등재를 **그대로 옮겨 적는다**(CC 판단 0).

### §0-2 예외 3건째 «정정» 승인 문언 (③ 성립 시에만 적용 · 원문)

```
 「armAxis 전달 분기에서 원점을 «분리»한다: axDot(대역 길이) = C 기준 «유지» ·
   axPoint(원통 중심) = 어깨 피벗 P(투영 전 원좌표 · 몸에서) 기준 · sgn 규칙은 v4-26 그대로 ·
   미전달 분기 불변 · 물리 0줄 · T포즈 82칸 비트 동일」
```

### §0-3 바꾸지 않는 것 (회차 프롬프트 원문)

```
 물리 **0줄** · `dressRun` **0** · `fitSleeve` **탐색 로직·RMIN·문턱 0** ·
 정본·blob·문턱 **0** · 워커 **diff 0** · 계기(v4-28) **유지**.
```

### §0-4 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · 단위 명시)

**★ 단위** — ①은 **mm**(길이) · ②는 **표본점**(510개 중) · ③은 **칸**(T포즈 82) · ④는 **칸 1개**.

| 항 | 갈래 |
|---|---|
| **①** 오프셋 실측 | **(가)** A포즈 ~102 mm · T포즈 한 자릿수 mm / **(나)** 다름(값) |
| **②** 피벗 배치 가상 실험 | **(가)** 위반이 무너진다(0 또는 팔 위반 소멸 · 값) / **(나)** 잔존 ⟹ **가설 6번째 기각 등재 · ③④ 착수 0** |
| **③** 원점 분리 처방 | **(가)** 82/82 + `SLV_R` 자릿수 복귀 / **(나)** 82/82 인데 이탈(값) / **(다)** 회귀 불일치 ⟹ 즉시 복원 |
| **④** A포즈 재탐침 + 캡처 | **(가)** 게이트 `pass` / **(나)** fail(채널·값 · 처방 0) / **(다)** 발산·미정착 |

**§4** — A = ①(가)+②(가)+③(가)+④(가) / B = ④(나)(다) 또는 ③(나) / **C = ②(나) ⟹ 기각 등재 · ③④ 착수 0** / D = ③(다) ⟹ 복원 / E = 그 외 ⟹ 정지.

### §0-5 사전 결정 4건 (값 보기 «전» · CC 집행 설계)

```
 ㄱ **②는 «코드를 고치기 전»에 판정만 한다** — `src/` 를 건드리지 않고, 계기 스크립트 안에서
    관의 중심만 **P(어깨 피벗 원좌표)** 로 옮겨 같은 표본점을 다시 잰다.
    쓰는 조각은 전부 «있는 것»이다: `P.S.PROBE.sleeve`(내보내진 표본) · `P.bodyG`(몸 격자) ·
    `sampleSdf`(`src/v3/bodySdf.ts`) · `sleeveTrace.dims`(v4-28 계기가 인쇄한 CAP_H·SLV_X0 등).
    축의 직교 보완(AU·AV)은 `garmentScene.ts` 의 **같은 규칙**을 그대로 옮겨 쓴다(값 창작 0).
 ㄴ **②에서 «몸판 기둥» 항은 평가하지 않는다(사유 등재)** — `distToSurface` 는 `garmentScene` 내부라
    밖에서 못 부른다. v4-28 실측에서 A포즈 49회차 «전부» 몸SDF 가 지배했고 기둥 항은 수렴점에서
    +26.1980 mm 로 멀었다 ⟹ **몸SDF 만으로 (가)/(나)를 가른다.** 이 한정을 값 보기 전에 등재한다.
 ㄷ **②(가)의 뜻을 지금 고정한다** — 「R = 61.510 mm 에서 **팔 위반이 0**」이면 (가) ·
    「팔 위반이 남으면」 (나). 부위 판정은 v4-28 §0-4ㄷ 의 **같은 규칙**(옷 치수에서)을 그대로 쓴다.
 ㄹ **③은 ②(가)일 때만** 착수하고, ④는 ③(가)일 때만 착수한다. 갈래를 값 보고 고르지 않는다.
```

---

## §1-① 축선 오프셋 실측 — **(가)** A포즈 **102.0632 mm** · T포즈 **8.8453 mm**

산술만이다(코드 0 · 굽기 0). `d = 피벗 P − C` · 평행 성분 `(d·AX)AX` · **수직 성분 = d − 평행**.
오른팔 기준으로 x 는 `|x|`(코드의 `sgn` 규약과 같은 쪽)를 쓴다.

| | A포즈(35°) | T포즈(0°) |
|---|---|---|
| 피벗 P | (−0.18685160239983742, 1.4321119511472187, −0.02806145821212186) | 같음 |
| C(중심선투영) | (0, 1.4321119511472187, −0.02806145821212186) | 같음 |
| AX | (0.837637993, −0.545228797, 0.032987130) | (0.998878891, 0.032578749, 0.034345104) |
| \|d\| | **186.8516 mm** | **186.8516 mm** |
| d·AX | 156.5140 mm | 186.6421 mm |
| 수직 성분 | (55.7495, 85.3359, −5.1629) mm | (0.4187, −6.0806, −6.4102) mm |
| **\|수직\|** | **102.0632 mm** | **8.8453 mm** |

⟹ 판정문의 산술 사실(≈102 mm)이 **값으로 확인**됐고, T포즈는 **한 자릿수 mm** 다 ⟹ **①(가)**.
★ T포즈에서 오프셋이 작은 이유는 축이 거의 +x 라 `d`(x 방향)가 축과 거의 나란하기 때문이다(값 그대로).

---

## §1-② 피벗 배치 가상 실험 — **(가) 위반이 무너진다**(69 → **0**)

`src/` 를 고치기 «전»에, 계기 스크립트(`scripts/v4PivotProbe.ts`)가 **관의 중심만** 옮겨 같은 표본을 다시 쟀다.
R 은 **61.510 mm 강제** · `x0 = 0.215` · 표본 **510** · **몸SDF 항 한정**(§0-5ㄴ 등재).

| 중심 | min 몸SDF | 위반(< SEP 2 mm) | 부위별 | 깊이 mm(최소/p25/중앙/p75/최대) |
|---|---|---|---|---|
| **C**(현행 · 중심선투영) `(0, 1.432112, −0.028061)` | **−8.5054 mm** | **69** | 팔 48 · 겨드랑이 21 | 1.1600 / 5.1675 / **9.4451** / 10.4487 / **10.5054** |
| **P**(어깨 피벗 원좌표) `(0.186852, 1.432112, −0.028061)` | **+7.2720 mm** | **0** | — | — |

★ **계기 충실도** — C 배치의 수(위반 69 · 팔 48 · 겨드랑이 21 · 깊이 분포 5개)가 **v4-28 §1-③ 과 완전히 같다**.
독립 경로(스크립트 안에서 재구성한 축·직교보완)로 같은 수가 났다 ⟹ 이 실험의 P 배치 값도 같은 자로 잰 것이다.

⟹ **②(가)** — 팔 위반 **0**(§0-5ㄷ 이 값 보기 «전»에 고정한 기준: 「R = 61.510 에서 팔 위반이 0」).

---

## §1-③ 원점 분리 처방 — **(가) 82/82 + `SLV_R` 자릿수 복귀**

### diff 원문 (`src/v3/garmentScene.ts` · 전달 분기 안 · §0-2 승인 문언대로)

```diff
   armAxis?: { left; right;
               origin?: { left; right };
+              /* v4-29 §1-③ — `pivot` = **원통 «중심»의 자리 P**(어깨 피벗 원좌표 · 투영 «전»).
+               * 넘기지 «않으면» `origin` 을 그대로 쓴다(= v4-26 거동 보존). */
+              pivot?: { left; right } };
```
```diff
   const AO = cfg.armAxis?.origin ? [origin.right…] : null;      // ← axDot(대역 길이) = C 유지
+  /* v4-29 §1-③ — **원점 «분리»**: axPoint(원통 중심)만 P 로. pivot 이 없으면 AO 로 떨어진다. */
+  const AP = cfg.armAxis?.pivot ? [|pivot.right.x|, pivot.right.y, pivot.right.z] : AO;
-  const axPoint = AO ? (t,R,ph,sgn) => [ sgn*(AO[0] + …), AO[1] + …, AO[2] + … ] : …옛 식…;
+  const axPoint = AP ? (t,R,ph,sgn) => [ sgn*(AP[0] + …), AP[1] + …, AP[2] + … ] : …옛 식…;
```

`git diff --stat` — `src/v3/garmentScene.ts` **+16/−5** · `scripts/armAxisEnv.ts` **+8/−3**(피벗 읽기) ·
`dressRun.ts` **0줄** · `fitSleeve` 탐색 로직·`RMIN`·문턱 **0줄**.

### 회귀 — T포즈 108칸 조립 드라이런(**2호기** · 전/후)

| 채널 | 값 |
|---|---|
| 행 대조 | **108/108 동일** · 불일치 **0** |
| 조립 성공 칸 | **82 ↔ 82** ⟹ **82/82 비트 동일** |
| 벽시계 | 594897 ms ↔ 606033 ms |

### A포즈 조립 — `SLV_R` 이 **56.750 mm** 로 돌아왔다

| 채널 | v4-24(축 없음) | v4-26·27·28(C 배치) | **v4-29(P 배치)** | T포즈 정본 |
|---|---|---|---|---|
| `SLV_R` | 178.741 mm | **140.765 mm** | **56.750 mm** | 61.510 mm |
| `PLACE_SIG` | — | `…/0.140765` | **`0.024131/0.002000/0.215000/0.056750`** | `…/0.061510` |
| 탐색 회차 | — | 49 | **43** | 44 |
| **실패항 분포** | — | **몸SDF 49 / 기둥 0** | **소매자기쌍 34 · 몸SDF 9** | 몸SDF 23 · 기둥 21 |
| 몸SDF 최소점 부위 | — | 겨드랑이 39 · 팔 10 · 몸통 0 | **팔 43**(전부) | 몸통 39 · 겨드랑이 3 · 팔 2 |

⟹ **③(가)** — 82/82 이고 `SLV_R` 이 **RMIN(56.431 mm) 바로 위**로 내려왔다(정본 61.510 과 같은 자릿수).
★ 지배항이 **몸SDF → 소매 자기쌍**으로 바뀌었다 — 관이 더 이상 몸에 걸려 커지지 않고,
**감김이 겹치지 않는 최소 크기**에서 멈춘다(값 그대로 · 단정 0).

---

## §1-④ A포즈 재탐침 + 캡처 — **(나) 게이트 fail** · 그러나 **네 채널이 함께 좋아졌다**

```
 job v4-29-Aramp — cell Apiv_M(asm-Apiv_M.bin) · ramp true · frames 400(상한)
 ★ 첫 시도는 **셀프 충돌 격자 용량**에 걸려 16.4 s 만에 실패했다:
   RuntimeError: 격자 셀 131220 > 용량 131072 — 상자가 너무 넓다
   ⟹ **호출부 용량만** 올렸다(`cellCap.max_cells` 131072 → **262144** · `cell_cap` 192 그대로 ·
      `gpu/engine/` 바이트 **불변** · v4-20 이 세운 규약 그대로).
 정착 **f280** · convNet **9.71591965417345e-05** ≤ TOL 1e-4 · 2.494 s/프레임 · 벽시계 743.2 s · rampN 98
 셀프 쌍/서브스텝 47796.53246753247 · 해소/서브스텝 623.1298701298701
 궤적 (10, 0.18445353857457722) (20, 0.12287365764583981) … (270, 0.00017908854511565237) (280, 0.0000971591965417345)
 ★ 워커는 이 칸도 **실패로 기록**한다 — 층3 `v4FitReport`(`deriveLevels`)가 던지기 때문(별도 판 · v4-24 이래 같음).
```

### 게이트 5채널 — `pass: false` · `fails` **2줄**

| 채널 | v4-24(축 없음) | v4-26·27(C 배치) | **v4-29(P 배치)** | 문턱 | 판정 |
|---|---|---|---|---|---|
| ③a 관통 최대 | 0.3252650778712598 mm | 1.303184307796152 mm | **1.0751871724985371 mm**(관통정점 726) | ≤ 0.5 | ❌ |
| 자기관통 교차 | 0 | 23 | **4** | 0 | ❌ |
| 최소 쌍거리 | 1.9950490699507553 mm | 0.00953771812990775 mm | **0.3812271157100771 mm** | — | — |
| 목선 초과비 | 0.9899308031171294 | 0.9958305945976433 | **0.9956222974475711**(링 94.28537231439918 / 허용 94.69994048557776 cm) | ≤ 1 | ✅ |
| 정착 창 순변위 | 0.09830835343189877 mm | 0.09911050178924592 mm | **0.09715919654173451 mm** | ≤ 0.1 | ✅ |
| 봉제 간극 중앙/최대 | 2.046798 / 4.849906 mm | 2.369618 / **123.257174** mm | 3.047334337518693 / **84.70459071455284** mm | — | — |
| λ 최대 | 1.3628108891987687 | 3.9447492035938305 | **3.244273246537137** | — | — |
| 정착 f | 250 | 240 | **280** | — | — |

`fails` = `["③a 관통 1.0752mm > 0.5mm", "자기관통 교차 4 ≠ 0"]`

### 겨드랑이 채널

| 채널 | v4-24 | v4-26·27 | **v4-29** |
|---|---|---|---|
| 팔↔몸통 최소 간격(봉제쌍 제외) | 2.3232970008230542 mm | 1.836195044026062 mm | **3.148867381130144 mm**(정점쌍 9610↔8232) |
| 대역 자기관통 교차 | 0 | 0 | **0** |
| 대역 최소 쌍거리 | 1.9950490699507553 mm | 0.08621082613974851 mm | **0.9318270697338343 mm** |
| 대역 관통 최대 | 0.3252650778712598 mm | 0.23105439707030453 mm | **0.9248128275014629 mm**(정점 8623) |
| 전체 교차 / 관통 최대 | 0 / 0.3253 mm | 23 / 1.3032 mm | **4 / 1.0752 mm**(관통정점 726) |

### 캡처

`docs/v4/캡처/29-1-front.png` · `docs/v4/캡처/29-2-side.png`(900×1200 · `front`/`sideXplus` · v4-24·26 과 **같은 프리셋**).
★ **화면 판정 0** — 「벗겨짐이 사라졌는가」와 관찰 3건은 **전략 세션이 본다**. 대조군은 `26-*.png`(C 배치)와 `24-*.png`(축 없음)다.

---

## §2 자기검사

### 워커 3경로 회귀 — **셋 다 비트 동일**

| 경로 | job | sha256(앞 16) | 등재값 | 판정 |
|---|---|---|---|---|
| 정착-blob | `v4-29-회귀1` | `3d8fac29f90fbfe4` | 동일 | **동일** |
| 조립(램프 없음) | `v4-29-회귀2` | `0d833601326922e2` | 동일 | **동일** |
| 조립+램프 | `v4-29-회귀3` | `24b0d3a11e94757e` | 동일 | **동일** |

층3 산출(`fit-*.json`·`gate-*.json`) **6/6 동일**(태그·경로 제외).

| 항 | 값 |
|---|---|
| `src/` diff vs 기점 | `garmentScene.ts` **+16 / −5**(전달 분기 안 · `pivot` 인자 + `AP` + `axPoint` 원점) |
| `scripts/armAxisEnv.ts` | **+8 / −3**(피벗 읽기 · 체제 유지) |
| `dressRun.ts` · `fitSleeve` 탐색·`RMIN`·문턱 | **0줄** · 물리 **0줄** · `gpu/engine/`·`worker.py` **diff 0** |
| 정본·blob·문턱 | **0** · 굽기 = 탐침 **1칸**(+ 회귀 3칸) |
| 이 판이 **신설**한 것 | `scripts/v4PivotProbe.ts`(가상 실험 계기) · job 4종 · 산출 5종(`asm-Apiv_M.bin` 등) · 캡처 2장 |
| pytest(2호기) | **40 passed · 2 xfailed · 1 xpassed**(63.53 s) |
| `npx tsc -b` | **통과**(종료 0) |
| 브랜치 인쇄 | §0 머리에 에어·2호기 둘 다 · §0 은 집행 **«전»** 커밋(`db3623f`) |
| ecc 에이전트 / 스킬 | **0 / 0** |

---

## §4 회차 갈래 — **B**(④(나) ⟹ 값 상신 · 판정은 전략 세션)

**넘기는 것 8건**

1. ★★★ **원점 분리가 «자리» 문제를 닫았다** — 가상 실험에서 위반 **69 → 0**(min 몸SDF −8.5054 → **+7.2720 mm**),
   실제 조립에서 `SLV_R` **140.765 → 56.750 mm**(정본 61.510 과 같은 자릿수 · RMIN 56.431 바로 위).
2. ★★★ **오프셋이 값으로 확인됐다** — A포즈 **102.0632 mm** · T포즈 **8.8453 mm**(판정문 ≈102 와 일치).
3. ★★★ **지배항이 바뀌었다** — A포즈 R 탐색의 실패항이 **몸SDF 49/49 → 소매자기쌍 34 · 몸SDF 9**,
   몸SDF 최소점 부위는 **전부 팔(43)**. 관이 몸에 걸려 커지지 않고 **감김이 겹치지 않는 최소 크기**에서 멈춘다.
4. ★★★ **게이트는 아직 fail** — 관통 **1.0752 mm**(> 0.5) · 교차 **4**(≠ 0). **그러나 네 채널이 함께 좋아졌다**:
   관통 1.3032 → 1.0752 · 교차 23 → 4 · 봉제 간극 최대 123.257 → **84.705 mm** · 팔↔몸통 간격 1.836 → **3.149 mm**.
5. ★★ **T포즈는 한 톨도 안 움직였다** — 108/108 행 동일 · **82/82 비트 동일** · 워커 3경로 비트 동일 · 층3 6/6 동일.
6. ★★ **셀프 충돌 격자 용량**을 한 번 넘겼다(131,220 > 131,072) — **호출부 용량만** 262,144 로 올렸다(엔진 바이트 불변).
7. ★ **계기 충실도가 값으로 확인됐다** — 독립 경로로 재구성한 가상 실험이 v4-28 §1-③ 의 수(위반 69 · 팔 48 · 겨드랑이 21 · 깊이 5개)를 **정확히 재현**했다.
8. ★ A포즈 층3(`deriveLevels`)은 **여전히 별도 판** — 워커가 A포즈 칸을 실패로 기록하는 원인도 그것이다.

**이 판이 하지 않은 판단** — 남은 관통 1.0752 mm·교차 4 의 처분 · 화면(벗겨짐·관찰 3건) · A포즈 층3 · 좌우 대칭(왼팔) 검증.
