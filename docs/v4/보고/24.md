# v4-24 보고 (전략 세션 제출본 · 터미널 출력 전량)

**2026-09-03 · 집행 완료 · 브랜치 `v4-24-apose` · 갈래 C**

> CLAUDE.md 「v4 회차 보고 «저장»」 규약 — 이 파일이 전략 세션 전달본이다.
> 집행 정본 노트는 `docs/v4/24-A포즈탐침.md`. 캡처는 `docs/v4/캡처/24-1-front.png` · `24-2-side.png`.

---

# v4-24 — A포즈 준비 4건 해소 · A포즈 굽기 탐침 · 캡처

**물리 0줄 · `src/` diff 0 · 정본·blob·문턱 0 · 워커 3경로 diff 0 · 굽기 = 탐침 1칸 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-24-apose` |
| 기점 | **`8c7792d`**(= v4-23 HEAD) |
| §0 커밋 | **`78646e1`**(집행 «전») |
| §1~§4 커밋 | (집행 후 기입) |
| 보고 파일 | `docs/v4/보고/24.md` |

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v4-24-apose 8c7792d → Switched to a new branch 'v4-24-apose'
       $ git branch --show-current → v4-24-apose · HEAD 8c7792d
 2호기 (§0 커밋을 push 한 «뒤» 같은 자리로 전환 — v4-21~23 과 같은 순서 · §0-5 에 인쇄)
```

---

## §0 수령·사무

### §0-1 판정문 (전략 세션 · v4-23 §4 수령 · 원문)

```
 「①(가/나 기계별) 수용 · ★ 새 원리 「정본은 기계 좌표를 갖는다」(v3 정본 = 맥 · v4 정본 = 2호기 ·
   맥 재굽기 바이트 동일 = 정본 맥산 확인) · ★★ 사전 규칙 (b) 적용 —
   실비교(v4@2호기 ↔ 정본@맥)의 자연 변동 = v3 기계 간 갈림(중앙 0.777) ·
   v4 잔여 0.632/0.139 는 그 안 ⟹ 「같은 옷」 «성립 선언» ·
   0.05mm 채널은 «같은 기계» 전용으로 존치 · T포즈 재생 검증 트랙 종료 ·
   ② 남은 3축 단독 귀속 불가 수용 · 추가 조사 0 ·
   ★ A포즈 = 비교 대상 부재 — 판정은 게이트 + 겨드랑이 채널 + 화면(전략 세션) ·
   v4 가 A포즈 첫 정본 생산자 ·
   ★ 사무 종결: v4-18 캡처 = 원본 없음(헤드리스 · §3 캡처 0 이 정본 · 복사 지시는 전략 세션 착오) —
   대기 항목에서 제거」
```

**★ 트랙 종결 등재** — 「T포즈 재생 검증」 트랙은 판정문이 **종결**했다(「같은 옷」 성립 선언 · 0.05 mm 채널은
같은 기계 전용 존치). 본 회차는 그 선언을 **옮겨 적을 뿐** 재판정하지 않는다(CC 판단 0).

### §0-2 사무 3건 (이 §0 커밋에 «함께» 든다)

| # | 사무 | 이행 |
|---|---|---|
| 1 | 원리 노트 「정본은 기계 좌표를 갖는다」 신설 | `.obsidian-log/원리/정본은-기계-좌표를-갖는다.md` — 근거 표(같은 기계 = 바이트 동일 / 기계 사이 = 중앙 0.777 mm)와 따르는 것 4항 |
| 2 | CLAUDE.md **캡처 규약 전문 등재** | `CLAUDE.md` v4 트랙 절에 「캡처 규약 — git 경유가 유일한 경로」 신설 · **판정문 문언 그대로** |
| 3 | 인수인계에서 **캡처 대기 항목 제거** | ★ **인수인계에 v4-18 캡처 대기 항목이 «없다»** — 그 대기는 대화 안에만 있었고 볼트에 등재된 적이 없다(grep `v4-18` → 예외 대장 1줄뿐). ⟹ **제거할 항목 0** · 이 줄이 그 사실의 기록이다. 인수인계에 남아 있는 캡처 대기는 **v3 트랙**(v3-85·91·92 업로드)뿐이라 **손대지 않았다** |

### §0-3 바꾸지 않는 것 (회차 프롬프트 원문)

```
 물리 **0줄** · `src/` diff **0** · 정본·blob·문턱 **0** ·
 워커 **3경로 diff 0**(A포즈는 기존 조립+램프 경로를 «사용»한다).
```

### §0-4 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · 단위 명시)

**★ 단위** — ①은 **항목**(4건) · ②는 **칸 1개** · 간격·관통 **mm** · 프레임 **1/60 s**.

| 항 | 갈래 |
|---|---|
| **①** 준비 4건 해소 | 항목별 「원문 인용 → 해소 방법(있는 것에서 유도 · 손 상수 0) → 해소 증거」 · 넷 다 닫히지 않으면 **그 사실과 사유** |
| **②** A포즈 굽기 탐침 | **(가)** 게이트 `pass` / **(나)** fail(채널·값 · 처방 0) / **(다)** 발산·미정착(값) |
| **③** 캡처 | 앞·옆 2장 → `docs/v4/캡처/24-1-front.png` · `24-2-side.png` · 불가면 **캡처 0 · 사유** · **화면 판정 0**(전략 세션 몫) |

**§4** — A = ①4/4 + ②(가) / B = ②(나)(다) ⟹ 값 상신 / **C = ① 미해소 항목 존재 ⟹ 사유 상신 · ②는 해소분으로 가능하면 집행** / D = 그 외 ⟹ 정지.

### §0-5 사전 결정 4건 (값 보기 «전» · CC 집행 설계)

```
 ㄱ **①의 「해소」 기준을 지금 고정한다** — 「그 항목이 막던 «다음 한 걸음»이 실제로 서면 해소」다.
    문서로 「없음을 설명한 것」은 해소가 아니다. 각 항목에 **파일·값·명령** 중 하나를 증거로 붙인다.
 ㄴ **`src/` 를 고쳐야만 닫히는 항목은 «미해소»로 적는다** — §0-3 이 `src/` diff 0 이고,
    v3 동결 «예외» 대장에는 `garmentScene.ts` 조립 코드 한정 1건뿐이다(`dressRun.ts` 는 없다).
    그 경우 **필요한 승인 문언과 정확한 diff 를 «제안»으로만** 적는다(적용 0).
 ㄷ **②의 몸은 회차 프롬프트가 지정한 것** — v4-19 A포즈 조립 상태(**기본 마네킹** A포즈 · 그리드 몸 아님).
    ①의 그리드 몸 항목이 닫히든 말든 **②의 입력은 바뀌지 않는다**(같은 판에서 두 몸을 섞지 않는다).
 ㄹ **리포트 5행은 «값 등재»로만 적는다** — A포즈에는 비교 대상이 없고,
    대역 정의역은 **T포즈 그리드 몸**에서 뜬다(`v4FitReport.ts` 가 `body-<bodyId>.bin` 을 읽는다) ⟹
    **A포즈 상태에 T포즈 대역을 얹은 수**임을 값 옆에 명시한다. 판정에 쓰지 않는다.
```

---

## §1-① A포즈 준비 4건 — **2/4 해소 · 2/4 미해소(사유)**

기준은 §0-5ㄱ 그대로다 — 「그 항목이 막던 **다음 한 걸음**이 실제로 서면 해소」.

### ①-1 A포즈 조립 export 일습 — **해소**

> 원문(v4-23 §1-③) — 「**A포즈 조립 export 일습** — `asm-Aasm_M.bin` · `scene-Aasm_M.bin` · `scene-bend-…` ·
> `scene-seam-…` · `sdf-Aasm.bin` … `gpu/oracle/export/` 에 `*Tasm_M*` 5종만 있다」

**해소 방법** — 이미 있던 인자(`v4AsmExport.ts:26` `BODY_BIN`)에 v4-19 A포즈 몸을 넣었다. **새 코드 0줄.**

```
 (2호기) $ CELL=c100-h170-s45_M BODY_BIN=gpu/oracle/export/l3ap-body-c100-h170-s45-a35.bin \
           TAG=Aasm_M npx tsx scripts/v4AsmExport.ts
 n 9644 · m(늘어남) 18520 · mb(굽힘) 27400 · ms(봉제) 258 · substeps 231 · sdf 416×479×84
 산출 scene-Aasm_M.bin · scene-bend-Aasm_M.bin · scene-seam-Aasm_M.bin · sdf-Aasm.bin · asm-Aasm_M.bin
```

**증거** — 위 5개 파일 · **②가 그 파일들로 실제로 구워졌다**(다음 한 걸음이 섰다).
★ 산출 기계 = **2호기**(원리 「정본은 기계 좌표를 갖는다」 — v4 정본은 2호기산이다).

### ①-2 A포즈 정답지 부재 — **해소**(판정 «체제»로)

> 원문 — 「v3 정본은 **T포즈 108칸뿐**이다 ⟹ A포즈에서는 「같은 옷」 대조가 **원리적으로 불가**」

**해소 방법** — 없는 정답지를 만드는 것이 아니라, 판정문이 **판정 체제를 갈아 끼웠다**:
「A포즈 = 비교 대상 부재 — 판정은 **게이트 + 겨드랑이 채널 + 화면**(전략 세션) · **v4 가 A포즈 첫 정본 생산자**」.
⟹ 「대조 불가」는 **막는 것이 아니게** 됐다.

**증거** — ②가 그 체제로 판정됐다(게이트 5채널 · 겨드랑이 채널 · §3 캡처). 리포트 5행은 §0-5ㄹ 대로 **값 등재만**.

### ①-3 A포즈 «그리드» 몸 — **미해소**(사유 · §0-5ㄴ)

> 원문 — 「기본 마네킹 A포즈 — c100-h170-s45 «아님»(본 스케일 미적용)」

**막는 것(코드 사실)** — 몸 파라미터(가슴·키·어깨)를 뼈 배율로 바꾸는 자리가 **React 컴포넌트 안**이다:

```
 src/components/Mannequin.tsx:357  const chestMultiplier = bodySize.chest / DEFAULT_BODY_SIZE.chest
                          :396-410 bone.scale[axis] = to(bone.scale[axis], …)   ← shoulder·torsoGirth·역상쇄
 그 `to()` 는 **프레임 delta 에 의존하는 lerp**(같은 파일 P26 §2 주석) — 값이 «프레임 루프에서 수렴»한다
 ⟹ 헤드리스로 부를 수 있는 **순수 함수가 없다**(grep: `applyBody*`·`setBody*` 부재).
```

**해소 경로 둘**(둘 다 이 회차 예산 밖) — ㉠ 그 배율 규칙을 순수 함수로 «추출»(= `src/` 수정 ⟹ **v3 동결
예외 승인 필요**) · ㉡ **브라우저 경로**로 A포즈 그리드 몸을 굽는다(= 백로그 0번 「A포즈 그리드 몸 27 생성」).
**이 판에서 실행 0** — §0-3 이 `src/` diff 0 이고, 회차 예산은 「굽기 = 탐침 1칸」이다.

★ **②에는 영향이 없다** — §0-5ㄷ 대로 ②의 몸은 회차 프롬프트가 지정한 **기본 마네킹 A포즈**다.

### ①-4 팔 축(`armAxis`) 배선 — **미해소**(사유 · 제안 diff 만)

> 원문 — 「`garmentScene.ts:51·631-636` 은 `cfg.armAxis` 를 받지만, `dressRun.prepare` 에는 그 인자가 **없다**」

**막는 것** — 배선 자리가 `src/v3/dressRun.ts` 다. **v3 동결 «예외» 대장에는 `garmentScene.ts` 조립 코드
한정 1건뿐**이고(`dressRun.ts` 는 없다), 이 회차는 `src/` diff 0 이다 ⟹ **적용 0**.

**제안 diff(적용하지 않았다 · 승인 대상)**

```diff
  export type RunInput = {
    glb: ArrayBuffer;
    bodyScale?: [number, number, number];
+   /** 팔 축 [단위벡터] — 하네스가 A포즈 축을 넘길 때 쓴다(기본 = +x · garmentScene.ts:631-636) */
+   armAxis?: { left: [number, number, number]; right: [number, number, number] };
    bodyVerts?: Float32Array;
```
```diff
    const S = createScene({
      body: prim0, bodyIdx, bodyG, sdfSpec,
+     armAxis: inp.armAxis,
      L: gd.L, W: gd.W, SW: gd.SW, SLEN: gd.SLEN, ARM_G: gd.ARM_G,
```

**필요한 승인 문언(예)** — 「v3 동결 «예외» 2번: `src/v3/dressRun.ts` — `armAxis` **인자 전달 한정** ·
물리 0줄 · **T포즈 조립 회귀가 비트 동일**이 조건」. ⟹ 승인 전까지 **A포즈 조립도 T포즈 축으로 소매를 놓는다**
(그 상태가 ②의 입력이다 — 값은 ②에 있다).

---

## §1-② A포즈 굽기 탐침 — **(가) 게이트 `pass`**(A포즈 몸으로 잰 값)

**입력** — v4-19 A포즈 조립 상태(**기본 마네킹** A포즈 · §0-5ㄷ) · 경로 = 기존 **「조립 → 램프 → 정착」**
(워커 3경로 중 세 번째 · **워커 diff 0**).

```
 job gpu/bake/jobs/v4-24-Aramp.json — fp f64 · arch cuda · cell Aasm_M(asm-Aasm_M.bin) ·
     reportCell c100-h170-s45_M · frames 400(상한) · cellCap {192, 131072} · ramp true
 [12:12:00] 램프 — RAMP_N 110 · rest0 2.971874e-03~2.992267e-01 · SEP 2.000000e-03
 [12:21:24] Aasm_M · **정착 f250** · 2.189 s/프레임 · JIT 53.5s · ti.init 0.4s · 자식 벽시계 621.3s
```

| 채널 | 값 |
|---|---|
| 정착 | **f=250**(상한 400) · `convNet` **9.830835343189877e-05** ≤ TOL **1e-4** · 발산 **false** · NaN **0** |
| 계 크기 | n **9,644** · substeps **231** · 셀프 쌍/서브스텝 **40,387.0** · 해소/서브스텝 **438.61038961038963** |
| 궤적(창별 순변위 m) | (10, 0.16175803901929833) · (20, 0.13652227688534865) · (30, 0.053482078241938125) · (40, 0.05542085163684637) · (50, 0.03272089390074125) … (210, 0.00022404747641120227) · (220, 0.000123521641521168) · (230, 0.00012746801331496314) · (240, 0.00010152144642139298) · (250, 0.00009830835343189877) |

### 게이트 5채널 — **`pass` · `fails` 빈 집합**(몸 = **A포즈 몸**)

| 채널 | 값 | 문턱 | 판정 |
|---|---|---|---|
| ③a 관통 최대 | **0.3252650778712598 mm**(관통정점 1,045) | ≤ 0.5 mm | ✅ |
| 자기관통 교차 | **0** | 0 | ✅ |
| 목선 초과비 | **0.9899308031171294**(링 **56.051364593625884 cm** / 허용 **56.62149760077103 cm**) | ≤ 1 | ✅ |
| 정착 창 순변위 | **0.09830835343189877 mm** | ≤ 0.1 mm | ✅ |
| 보조 장치(invMass=0) | **0** | 0 | ✅ |
| (부수) 봉제 간극 | 중앙 **2.046798414582496 mm** · 최대 **4.849906213559389 mm** · λ 최대 **1.3628108891987687** | — | — |

### ★★ 계기 사고 1건 — 워커 층3 기본 경로는 **T포즈 몸으로** 쟀다(값 등재 · 원인 코드 사실)

워커의 층3(`worker.py:174-181`)은 `CELL=<reportCell>` 로 `v4FitReport`·`v4ProductGate` 를 부른다.
두 계기는 그 칸의 **`body-<bodyId>.bin`(T포즈 그리드 몸)**을 읽고, 위치 파일은 **앞에서부터 n×3 개만**
가져갔다 ⟹ A포즈 상태 **9,644** 정점이 T포즈 **9,388** 로 **잘린 채** 다른 몸에 얹혔다.

| 채널 | 워커 기본(**T포즈 몸 · 잘린 위치**) | 이 판 재산출(**A포즈 몸 · 전량**) |
|---|---|---|
| 게이트 | **fail** | **pass** |
| ③a 관통 최대 | 36.69058075722288 mm | **0.3252650778712598 mm** |
| 자기관통 교차 | 5,689 | **0** |
| 목선 초과비 | 1.0287792554346198 | **0.9899308031171294** |
| 봉제 간극 중앙 | 265.3144527544343 mm | **2.046798414582496 mm** |

**처분(이 판)** — 두 계기에 **`BODY_BIN` 인자**를 열었다(기본값은 **그대로** ⟹ 기존 호출은 바이트 불변) ·
`v4FitReport` 의 길이 검사를 **「짧으면」에서 「다르면」으로** 고쳤다(**조용한 절단 0**). `src/` **0줄**.
★ 이 값들의 **판정 채널은 A포즈 몸 쪽**이다 — 다른 몸으로 잰 수는 **옷의 상태가 아니다**.

### ★ 리포트 5행 — **산출 불가**(A포즈에서 대역 도출이 서지 않는다)

```
 $ CELL=c100-h170-s45_M BODY_BIN=…l3ap-body-c100-h170-s45-a35.bin POS=…Aasm_M.bin npx tsx scripts/v4FitReport.ts
 Error: C 최소가 구간 «끝»에 붙는다(내부 극값 없음) — 갈래 B
     at deriveLevels (src/v3/bodyLevels.ts:151)
```
⟹ **A포즈 몸에서는 허리 레벨이 «내부 극값»으로 뜨지 않는다**(팔이 내려와 몸통 단면 프로파일이 바뀐다).
계기가 **추정치로 채우지 않고 던진다**(상시 규약 「산출 불가면 불가로 보고」) — **값 0 · 처방 0.**
§0-5ㄹ 이 상정한 「T포즈 대역을 얹은 수」조차 **A포즈 몸에서는 나오지 않는다**는 것이 이 판의 사실이다.

### 겨드랑이 채널 (대역은 **옷 구조에서 유도** · 손 상수 0)

계기 = `scripts/v4Armpit.ts`(새 물리 0줄 · `minPairDist` · `makeBodyDistance` 를 부른다).
대역 정의 — 소매↔몸통 **봉제쌍 정점**(씨앗 206) ∪ 그 1-링 삼각형(**406** 삼각형 · **418** 정점).

| 채널 | 값 |
|---|---|
| 팔↔몸통 최소 간격(**봉제쌍 제외**) | **2.3232970008230542 mm**(정점쌍 9133 ↔ 7223) |
| 〃 (봉제 포함 · 설계상 SEP 로 붙는 쌍) | 2.004678078188115 mm |
| 대역 **자기관통 교차** | **0** |
| 대역 최소 쌍거리 | 1.9950490699507553 mm(SEP 2.0) |
| 대역 **관통 최대** | **0.3252650778712598 mm**(정점 7783) · 대역 몸거리 최소 0.6747349221287402 mm |
| 전체(대조) | 교차 **0** · 최소 쌍거리 1.9950490699507553 mm · 관통 최대 0.3252650778712598 mm(관통정점 1,045) |

★ 소매 정점 **1,020** · 몸통 정점 **8,624** · 전체 **9,644**.

---

## §1-③ 캡처 — **2장**(`docs/v4/캡처/24-1-front.png` · `24-2-side.png`)

계기 = `scripts/v4AposeRender.ts`(오프스크린 · 정사영 · z버퍼 · `src/v3/raster.ts` 를 부른다 · 새 렌더 0줄).
몸 = A포즈 몸 · 옷 = ②의 정착 위치(`Aasm_M.bin`) · 900×1200 · 시점 `front` / `sideXplus`(`raster.ts:18-22` 고정 프리셋).
경계(두 메시 전량) lo **[−0.7646207213401794, 0.004112526308745146, −0.13799095698395883]** ·
hi **[0.7644810676574707, 1.7691642045974731, 0.18803229860278586]**.

★ **화면 판정 0** — 상시 규약대로 CC 는 보기 판단을 하지 않는다. 두 PNG 는 이 회차 커밋에 들어 있고
(캡처 규약 §0-2), 판정은 전략 세션 몫이다.

---

## §2 자기검사

### 워커 3경로 회귀 — **셋 다 비트 동일**(이 판의 계기 수정이 기존 호출을 **한 바이트도** 바꾸지 않았다)

| 경로 | job | 결과 | sha256(앞 16) | 기존 | 판정 |
|---|---|---|---|---|---|
| 정착-blob 모드 | `v4-24-회귀1`(`c100-h185-s40_L`) | 정착 f150 · `pass` | `3d8fac29f90fbfe4` | `3d8fac29f90fbfe4`(v4-22 등재) | **동일** |
| 조립 모드(램프 없음) | `v4-24-회귀2`(`Tasm_M` · 200프레임) | 정착 f190 | `0d833601326922e2` | `0d833601326922e2`(v4-22 등재) | **동일** |
| 조립+램프 | `v4-24-회귀3`(`Tasm_M` · 260프레임) | 정착 **f180** · `pass True` | `24b0d3a11e94757e` | `24b0d3a11e94757e`(v4-21 산출) | **동일** |

층3 산출도 대조했다 — `fit-*.json` · `gate-*.json` 이 **태그·경로 필드를 뺀 전 항목에서 동일**(3경로 × 2파일 = 6/6).

| 항 | 값 |
|---|---|
| `src/` diff | **0**(`git diff --stat 78646e1 -- src/` 무출력) · 물리 **0줄** |
| `gpu/engine/` · `gpu/bake/worker.py` | **diff 0** — A포즈는 **기존 조립+램프 경로에 job 만** 줬다 |
| 정본·blob·문턱 | **0** |
| 이 판이 **신설**한 것 | `scripts/v4Armpit.ts`(겨드랑이 채널) · `scripts/v4AposeRender.ts`(오프스크린 렌더) · job 4종 · 캡처 2장 · 원리 노트 1 · 함정 후보 1 |
| 이 판이 **고친** 것 | `scripts/v4FitReport.ts`·`scripts/v4ProductGate.ts` — **`BODY_BIN` 인자 추가**(기본값 그대로) + `v4FitReport` 길이 검사 「짧으면」→**「다르면」** · **회귀로 무영향 증명**(위 표) |
| pytest(2호기) | **40 passed · 2 xfailed · 1 xpassed**(65.96 s) |
| `npx tsc -b` | **통과**(종료 0) |
| 브랜치 인쇄 | §0 머리에 에어·2호기 둘 다 · §0 은 집행 **«전»** 커밋(`78646e1`) |
| ecc 에이전트 / 스킬 | **0 / 0** |

---

## §4 회차 갈래 — **C**(① 미해소 항목 존재 ⟹ 사유 상신 · ②는 해소분으로 집행했다)

갈래 규칙 그대로다 — ① 이 **2/4**라 A 가 아니고, ② 는 **(가) `pass`** 라 B 도 아니다.

**넘기는 것 7건**

1. ★★★ **A포즈 1칸이 게이트를 통과했다**(A포즈 몸으로 잰 값) — 정착 **f=250** · 관통 **0.3253 mm** ·
   교차 **0** · 목선 초과비 **0.98993** · 정착 순변위 **0.09831 mm** · `fails` **빈 집합**.
   ⟹ **v4 가 A포즈 첫 정본을 냈다**(판정문의 「첫 정본 생산자」가 값으로 성립).
2. ★★★ **계기 사고 1건** — 워커 층3 기본 경로가 A포즈 상태(9,644)를 **T포즈 몸**(9,388)으로 재고
   **조용히 잘랐다**. 그 판에서는 관통 **36.69 mm** · 교차 **5,689** 로 fail 이었다.
   처분 = `BODY_BIN` 인자 + 길이 검사 「다르면 던진다」 · 3경로 회귀로 **무영향 증명** ·
   함정 후보 「다른 몸으로 잰 수를 그 옷의 값으로 안다」 등재.
3. ★★★ **A포즈에서는 리포트 5행이 «산출 불가»다** — `bodyLevels.deriveLevels` 가
   「C 최소가 구간 «끝»에 붙는다(내부 극값 없음) — 갈래 B」로 **던진다**.
   ⟹ A포즈 층3은 **게이트 + 겨드랑이 채널**만으로 서고, **대역 기반 5행은 새 도출이 필요하다**.
4. ★★ **겨드랑이 채널이 섰다**(v4-17 이래 미완이던 계기) — 팔↔몸통 최소 간격 **2.3233 mm**(봉제쌍 제외) ·
   대역 교차 **0** · 대역 관통 최대 **0.3253 mm**. 대역은 **옷 구조(소매↔몸통 봉제쌍의 1-링)**에서 뜬다.
5. ★★ **①-3 미해소** — A포즈 «그리드» 몸: 배율 규칙이 `Mannequin.tsx:357·396-410`(프레임 의존 lerp) 안에 있어
   **헤드리스 순수 함수가 없다**. 해소는 ㉠ 추출(= `src/` 수정 · **예외 승인 필요**) 또는 ㉡ 브라우저 경로(백로그 0번).
6. ★★ **①-4 미해소** — `dressRun.prepare` 의 `armAxis` 배선: **제안 diff 2곳**을 적어 뒀고 **적용 0**.
   승인 문언 예시도 노트에 있다. ⟹ 지금 A포즈 조립은 **T포즈 축으로 소매를 놓는다**(그 상태로 게이트는 통과했다).
7. ★ **캡처 2장**이 저장소에 있다(`docs/v4/캡처/24-1-front.png` · `24-2-side.png`) — **화면 판정 0**.

**이 판이 하지 않은 판단** — 화면 · A포즈 5행 대역의 새 도출 · 그리드 몸 27 생성 착수 ·
`armAxis` 예외 승인 · A포즈 정본의 정본화(색인 등재 여부).
