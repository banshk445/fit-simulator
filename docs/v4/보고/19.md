# v4-19 보고 (전략 세션 제출본 · 터미널 출력 전량)

**2026-09-02 · 집행 완료 · 브랜치 `v4-19-apose-probe`**

> CLAUDE.md 「v4 회차 보고 «저장»」 규약 — 이 파일이 전략 세션 전달본이다.
> 집행 정본 노트는 `docs/v4/19-A포즈탐침.md`.

---

# v4-19 — 팔 축 인자화 재시도(기저 = v3 유도 규칙) · A포즈 조립·굽기 탐침

**집행 기계 = 2호기 · 물리 0줄 · `Mannequin.tsx`·`dressRun.ts` 0줄 · 정본·blob·문턱 0 · 굽기 0 · 캡처 0 · ecc 0/0**

### ★ 브랜치 전환 인쇄

```
 에어  Switched to a new branch 'v4-19-apose-probe' · show-current = v4-19-apose-probe
       rev-parse HEAD = 2a8e7916daed4c03bd9e6362d92e4e724629befe
 2호기 Switched to a new branch 'v4-19-apose-probe' · show-current = v4-19-apose-probe
```

---

## §0-2 ★ 인용 대조 — **판정문의 v4-18 서술 3건이 정본과 어긋난다**

정본 = `docs/v4/18-A포즈몸.md`(`ff999d3` · sha 기입 `43c94fb`) · `docs/v4/보고/18.md` ·
metrics v4-18 블록 · 산출 `l3ap-body-c100-h170-s45-a35.json`. **새 실행 0 · 정본 수정 0.**

| # | 판정문 | **정본** | 어긋남 |
|---|---|---|---|
| 1 | ③(나) — **복원 적법** | ③ = **(가)** · T포즈 조립 **82/82 비트 동일** · **되돌림 커밋 0** | 갈래가 다르다 |
| 2 | ②(가) **성립** · 축 (0.8095, −0.5860, 0.0234) · 높이 **1.7000** · 겨드랑이 **41.03 mm** | ② = **ㄱ만 성립 · ㄴㄷㄹ 미집행** · 축 **(0.8375, −0.5453, 0.0359)** · 높이 **1.765052 m** · 겨드랑이 **미산출** | 세 수가 전부 다르고 게이트·높이는 **돌린 적이 없다** |
| 3 | ① JIT 이탈 **−27~−39%** | **9.3~10.8 s ↔ 48.6~63.0 s = −78~−85%** | 자릿수가 다르다 |

★ **저장소에 높이 1.7000 인 몸도, 겨드랑이 41.03 을 낸 계기도 없다**(grep 0건). 있는 A포즈 몸은
`l3ap-body-c100-h170-s45-a35.bin` **하나**이고 그것은 **기본 마네킹**(1.765051678288728 m)이다.
★ **이름은 단정하지 않는다**(v4-12 §5 와 같은 처분) — 사용자가 확정한다.

**귀책 등재 2건**(판정문 지정) — 손 upVec 스케치 · ±10% 대칭 문턱 = **둘 다 전략 세션**.

---

## §1-① 팔 축 기저 = **v3 유도 규칙의 벡터 일반형** — **(가)**

| 채널 | 값 |
|---|---|
| 인용 | `src/lib/bodyGeodesic.ts:281-289` — `seed = |n.x| < 0.9 ? [1,0,0] : [0,1,0]` · `u = norm(seed × n)` · `v = n × u` |
| 이 판 | `w = norm(seed × a)` → **`AU = a × w`** → **`AV = a × AU`** · **손 벡터 0**(v4-18 up 벡터 걷어냄) |
| `a = +x` 축소(기계 확인) | seed (0,1,0) → w (0,0,−1) → **AU (−0,1,0)** · **AV (0,−0,1)** ⟹ 옛 식과 **정확히** 같다 |
| **T포즈 108칸 회귀** | 칸 108 · 조립 성공 **82** · **비트 동일 82/82** · **불일치 0** · after19 604 s |

⟹ **v3 동결 예외 1번의 조건(물리 0줄 · T포즈 비트 동일) 유지.**

## §1-② A포즈 조립 탐침 — **(가) 조립 통과**

★ 몸 정체 = **기본 마네킹 A포즈**(1.765052 m · `c100-h170-s45` 아님) ⟹ **게이트·높이 판정 0**.

| 채널 | 값 |
|---|---|
| **조립** | **통과** · 옷 정점 **9,644** · 제약 **46,178** · substeps **231** |
| **옷 ↔ 몸 최소 간격** | **2.000000000060054 mm**(정점 9109) · `SEP` 2 mm ⟹ **문턱에 붙어 선다** |
| 옷 자기 최소 쌍거리 | **2.785006 mm** |
| 소매 `R`/`x0` | **미노출**(씬 밖으로 안 나온다 · 자리 등재) |

★★ **팔 축 인자를 못 넘겼는데도 통과했다** — `dressRun.prepare` 가 `armAxis` 를 안 받고 `dressRun.ts` 는
예외 대장 범위 **밖**이라, **기본 축 ±x** 로 35° 내려간 팔을 감았는데 `fitSleeve` 가 SEP 를 만족했다.

## §1-③ A포즈 굽기 탐침 — **착수 불가**(사유 2건)

```
 ① A포즈 칸에 «정착 blob» 이 없다 — 워커는 `load.py:45-55` 로 `settled-<cell>.bin` 을 읽는다.
 ② 만들려면 **정본 blob 디렉터리에 쓰거나 워커 입력 규약을 바꿔야** 하는데 둘 다 이 판의 금지다.
 ⟹ 갈래 (가)(나)(다) 어디로도 판정하지 않는다 · §3 **캡처 0**.
```

---

## §2 자기검사

물리 diff **0줄** · `src/` 변경 = **`garmentScene.ts` 기저 유도부 하나** · `Mannequin.tsx`·`dressRun.ts` **0줄** ·
`public/` 무변경(정본·blob 0) · pytest **40 passed · 2 xfailed · 1 xpassed**(63.22 s) · `npx tsc -b` **통과**.

---

## §4 회차 갈래 — **등재 갈래 밖**(①(가) + ②(가) + ③ 착수 불가) ⟹ **값·사유 상신**

**넘기는 것 6건**

1. ★★★ **판정문 ↔ 정본 어긋남 3건**(§0-2) — 특히 「높이 1.7000 · 겨드랑이 41.03」 은 **저장소에 없다**.
2. ★★★ **기저를 v3 규칙으로 갈아도 비트가 흔들리지 않는다**(82/82) — 손 벡터가 사라졌다.
3. ★★★ **A포즈 몸에서도 조립이 «선다»**(옷↔몸 2.000 mm) — x축 소매로도 SEP 를 만족했다.
4. ★★ **다음 관문 = `dressRun.prepare` 에 `armAxis` 흘려보내기** — 예외 범위 확장 여부는 전략 세션 몫.
5. ★★ **③은 «워커 입력 규약»에 막혔다** — 「조립 직후 상태 입력 모드」를 열지가 다음 결정이다.
6. ★ 소매 `R`/`x0` 미노출(자리 등재 · 안 고쳤다).

**이 판이 하지 않은 판단** — 판정문 어긋남의 이름 · A포즈 «정본» 몸 만들기 · 예외 범위 확장 ·
워커 규약 변경 · 없는 몸의 게이트 판정. **처방 0 · 문턱 이동 0 · 사후 갈래 신설 0.**
