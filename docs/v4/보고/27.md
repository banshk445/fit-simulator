# v4-27 보고 (전략 세션 제출본 · 터미널 출력 전량)

**2026-09-03 · 집행 완료 · 브랜치 `v4-27-band-radius` · 갈래 B**

> CLAUDE.md 「v4 회차 보고 «저장»」 규약 — 이 파일이 전략 세션 전달본이다.
> 집행 정본 노트는 `docs/v4/27-반경조건.md` · 캡처는 `docs/v4/캡처/27-1-front.png` · `27-2-side.png`
> (둘 다 v4-26 캡처와 **바이트 동일**이다).

---

# v4-27 — 대역 반경 조건(예외 4건째) · A포즈 재탐침 · 캡처

**물리 0줄 · `src/` diff = `garmentScene.ts` `armProfile` 반경 조건 «한정»(전달 분기 안) · `dressRun.ts` 0줄 · 정본·blob·문턱 0 · 워커 3경로 diff 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-27-band-radius` |
| 기점 | **`e07b624`**(= v4-26 §1~§4) |
| §0 커밋 | **`62f245d`**(집행 «전») |
| §1~§4 커밋 | (집행 후 기입) |
| 보고 파일 | `docs/v4/보고/27.md` |

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v4-27-band-radius e07b624 → Switched to a new branch 'v4-27-band-radius'
       $ git branch --show-current → v4-27-band-radius · HEAD e07b624
 2호기 (§0 커밋을 push 한 «뒤» 같은 자리로 전환 — §0-5 에 인쇄)
```

---

## §0 수령·사무

### §0-1 판정문 (전략 세션 · v4-26 §4 + 화면 수령 · 원문)

```
 「갈래 B 수령 · ★ 원점 일반화 성립(NaN 0 · 82/82) · NaN 가드 성립 ·
   ★★ 화면 판정 = 새 결함 지배: 옷이 어깨에서 벗겨짐(오프숄더 사선 · 소매 분리 · v4-24 에 없던 현상) —
     봉제 간극 123mm·관통 1.30·교차 23 과 정합 · 관찰 3건 자연 실험은 판정 불가(보류 유지) ·
   ★★ 인과 사슬 확정 방향: 반경 조건 부재 → 대역 2,509(다리까지) → SLV_R 140.8 →
     소매 헐거운 관 → 램프 못 닫음 → 벗겨짐 ·
   ★ 예외 대장 4건째 «승인»: armProfile 선별 반경 조건(armAxis 전달 분기 한정) —
     반경 문턱은 몸에서 유도(유도 규칙은 CC 가 정하고 근거 등재 · 손 상수 0) · 미전달 분기 밖 ·
     82칸 비트 동일 · 물리 0줄 · 사전 기대: 대역 정점 493 자릿수 복귀 · SLV_R 61.5mm 자릿수 복귀 ·
   ★ 층3 deriveLevels 는 별도 판 유지」
```

### §0-2 바꾸지 않는 것 (회차 프롬프트 원문)

```
 물리 **0줄** · `dressRun` **0** · 정본·blob·문턱 **0** · 워커 **3경로 diff 0** ·
 `armAxisEnv`·NaN 가드 **체제 유지**.
```

### §0-3 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · 단위 명시)

**★ 단위** — ①은 **칸**(T포즈 108칸 중 성공 82칸) · ②는 **칸 1개** · 반경·간격 **mm** · 프레임 **1/60 s**.

| 항 | 갈래 |
|---|---|
| **①** 반경 조건 + 회귀 | **(가)** 82/82 **그리고** 대역·`SLV_R` 이 기대 자릿수 / **(나)** 82/82 인데 대역·`SLV_R` 이탈(값) / **(다)** 회귀 불일치 ⟹ 즉시 복원 · diff 0 |
| **②** A포즈 재탐침 | **(가)** 게이트 `pass` / **(나)** fail(채널·값 · 처방 0) / **(다)** 조립 실패(사유) |
| **③** 캡처 | 앞·옆 2장 → `27-1-front.png` · `27-2-side.png` · **화면 판정 0** |

**§4** — A = ①(가)+②(가) / B = ②(나)(다) 또는 ①(나) / C = ①(다) ⟹ 복원 / D = 그 외 ⟹ 정지.

### §0-4 사전 결정 5건 (값 보기 «전» · CC 집행 설계)

```
 ㄱ **반경 문턱의 «유도 규칙» = 「몸이 자른다」(손 상수 0 · 문턱 «수»를 사람이 쓰지 않는다).**
    대역 후보를 **축선까지의 반경 r 로 정렬**해, 이웃 r 사이의 **가장 큰 «틈»(gap)** 에서 자른다.
    근거 — 팔은 축선에 붙은 **조밀한 군집**이고 몸통·다리는 **멀리 떨어진 다른 군집**이다.
    ⟹ 자르는 «자리»를 사람이 아니라 **분포가 정한다**. 나온 값(틈의 크기·자른 반경·남은 정점 수)을 §1-①에 적는다.
 ㄴ **★ 값 보기 «전»에 등재하는 코드 사실 — ①이 ②를 못 바꿀 수 있다.**
    `armProfile` 의 산출 `ARM.yc/zc` 는 **미전달 분기의 `axPoint`(`:694-695`)에서만** 읽힌다.
    v4-26 이 연 전달 분기의 `axPoint` 는 중심으로 **C** 를 쓰므로 `ARM` 을 **참조하지 않는다**.
    ⟹ 반경 조건이 대역을 고쳐도 **A포즈 조립·`SLV_R` 은 그대로일 수 있다**.
    그러면 판정문의 인과 사슬(「대역 → SLV_R → 벗겨짐」)은 **첫 고리에서 끊긴다**.
    **이 판은 그 여부를 «값으로» 가른다** — 조립 blob 이 v4-26 과 **바이트 동일한지**로 본다(사후 해석 0).
 ㄷ **①의 갈래 판정 기준을 지금 고정한다** — 「82/82 이고 **대역이 기대 자릿수(수백)** 이며 `SLV_R` 이
    **61.5 mm 자릿수**」면 (가) · 「82/82 인데 둘 중 하나라도 이탈」이면 **(나)**.
 ㄹ **②는 ①의 갈래와 무관하게 «돌린다»** — 조립이 바뀌지 않아도 굽기를 다시 돌려
    **v4-26 과 비트로 같은지**까지 값으로 남긴다(결정성 재확인 · 사후 갈래 신설 0).
 ㅁ **회귀는 같은 기계 전/후**(2호기) · ssh 가 끊기면 에어로 옮기고 등재한다.
```

---

## §1-① 반경 조건 — **(나) 82/82 는 섰고, 대역·`SLV_R` 은 기대에서 이탈**

### diff 원문 (`src/v3/garmentScene.ts` `armProfile` · **전달 분기 안**)

```diff
   function armProfile(x0: number, x1: number) {
+    if (!AO) {                                    // ← 미전달 경로: 아래 블록은 **옛 코드 그대로**
       let y0 = Infinity, …
       for (…) { const px = axDot(…); if (px < x0 || px >= x1) continue; …bbox… }
       return { yc: (y0 + y1) / 2, zc: (z0 + z1) / 2 };
+    }
+    /* v4-27 §1-① 선별 «반경» 조건 — 문턱을 사람이 쓰지 않는다. «몸이 자른다»(§0-4ㄱ):
+     * 후보를 축선까지의 반경 r 로 정렬해 **이웃 r 사이의 가장 큰 «틈»**에서 자르고 안쪽만 남긴다. */
+    const rs = [], ys = [], zs = [];
+    for (…) { const t = axDot(…); if (t < x0 || t >= x1) continue;
+              r = |p − C − t·AX| ; rs.push(r); ys.push(py0); zs.push(pz0); }
+    const ord = rs.map((_, i) => i).sort((a, b) => rs[a] − rs[b]);
+    …가장 큰 틈에서 cut… → cut 앞쪽만으로 bbox → { yc, zc }
   }
```

`git diff --stat src/v3/garmentScene.ts` = **1 file changed, 36 insertions(+), 5 deletions(-)** · `dressRun.ts` **0줄** · 물리 **0줄**.

### 회귀 — T포즈 108칸 조립 드라이런(**2호기** · 전/후)

| 채널 | 값 |
|---|---|
| 행 대조 | **108/108 동일** |
| 조립 성공 칸 | **82 ↔ 82** ⟹ **82/82 비트 동일 · 패턴해시 동일** · 불일치 **0** |
| 벽시계 | before **599687 ms** ↔ after **800141 ms** |

### 검증값 — **사전 기대와 어긋난다**

| 채널 | 사전 기대(판정문) | **실측** |
|---|---|---|
| 대역 정점 수 | **493 자릿수 복귀** | **3** (버린 정점 2,506) |
| 대역 y 범위 | (팔) | 1.2808 ~ 1.2854 |
| `ARM.yc` / `ARM.zc`(코드 인쇄값) | — | **1.3003407716751099** / **−0.030687931925058365** |
| `SLV_R` | **61.5 mm 자릿수 복귀** | **140.765 mm** — v4-26 과 **한 자리도 다르지 않다** |

**왜 3개인가(값으로)** — 대역 후보 2,509개의 축선 반경 분포는
**36.742 ~ 986.403 mm**(1% 49.5 · 5% 68.3 · 25% 277.1 · 50% 443.2 · 75% 639.9)로 **바깥이 «연속»**이다.
⟹ 「가장 큰 틈」이 **팔 «안»에서** 났다: 틈 **7.972 mm**(38.950 → 46.922 mm) ⟹ 자른 반경 38.950 mm · 남은 정점 **3**.
사전 등재한 유도 규칙(§0-4ㄱ)이 **이 몸에서는 서지 않는다**. **규칙을 값 보고 바꾸지 않았다**(함정 14).

**참고 — 문턱을 어디로 두면 어떻게 되는지(값만 · 이 판의 «선택» 0)**

| 반경 문턱 mm | 남는 정점 | y 범위 |
|---|---|---|
| 20 | 0 | — |
| 40 | 3 | 1.2808~1.2854 |
| 60 | 98 | 1.2481~1.3604 |
| 80 | 165 | 1.2441~1.3733 |
| 100 | 236 | 1.2238~1.3836 |
| 120 | 304 | 1.2034~1.3987 |
| **150** | **412** | 1.1718~1.4263 |
| **200** | **477** | 1.1241~1.4263 |
| 300 | 691 | 0.9927~1.4263 |

⟹ **기대한 「493 자릿수」는 문턱 150~200 mm 대에 있다**(412 / 477). 그 자리를 **이 판이 고르지 않는다** — 처방 0.

### ★★★ 사전 등재 예측(§0-4ㄴ)이 **값으로 맞았다** — ①은 ②를 바꾸지 못한다

`ARM.yc/zc` 는 **미전달 분기의 `axPoint`(`:725-726`)에서만** 읽힌다. 전달 분기 `axPoint` 는 중심으로 **C** 를 쓴다.
`ARM` 은 씬 객체로 내보내지지만(`:914`) **조립 경로의 소비자가 없다**(grep: `S.ARM` 소비 0 · v3S4 의 `ARM` 은 그 파일의 지역값).

```
 asm-Aori_M.bin(v4-26 · 반경 조건 «전») ↔ asm-Arad_M.bin(이 판 · 반경 조건 «후»)
   헤더 차이 = `cell` 태그 하나뿐("Aori_M" ↔ "Arad_M")
   **페이로드 462,912 바이트 «전량 동일»** · 페이로드 sha256 앞16 = `8ad460577caef5ec` (양쪽 같다)
   PLACE_SIG 0.024131/0.002000/0.215000/**0.140765** (양쪽 같다) · RAMP_N 98 · n 9644
```

⟹ **판정문의 인과 사슬은 첫 고리에서 끊긴다** — 「반경 조건 부재 → 대역 2,509 → SLV_R 140.8」이 아니다.
대역은 `SLV_R` 에 닿지 않는다. `SLV_R` 은 `fitSleeve()`(`:713-730`)가 **몸 SDF(`sampleSdf`)와 몸판 기둥(`distToSurface`)에서
SEP 이상 떨어질 때까지 R 을 키우는** 해에서 나온다. **이 판은 그 자리를 건드리지 않았다**(단정 0 · 처방 0).

---

## §1-② A포즈 재탐침 — **(나) 게이트 fail · 그리고 v4-26 과 «비트 동일»**

§0-4ㄹ 대로 조립이 바뀌지 않아도 **굽기를 다시 돌렸다**(결정성 재확인).

```
 job v4-27-Aramp — fp f64 · arch cuda · cell Arad_M(asm-Arad_M.bin) · frames 400(상한) · ramp true
 램프 rampN 98 · 정착 **f240** · convNet **9.911050178924592e-05** ≤ TOL 1e-4 · 2.775 s/프레임 · 벽시계 689.0s
 셀프 쌍/서브스텝 42897.393939393936 · 해소/서브스텝 736.5757575757576
 ★ 워커는 이 칸도 **실패로 기록**한다 — 층3 `v4FitReport` 의 `deriveLevels` 가 던진다(v4-24·26 과 같은 자리 · 별도 판).
```

**v4-26 대비 — 위치 sha256(앞 16) `2789d42e882ed094` ↔ `2789d42e882ed094` · 궤적 배열 «전량 동일» · 정착 f·convNet 동일.**

### 세로 대조 (같은 A포즈 몸 · 축·원점·반경 조건만 다르다)

| 채널 | v4-24(축 없음) | v4-26(축+원점) | **v4-27(축+원점+반경)** |
|---|---|---|---|
| 조립 pos NaN | 0 | 0 | **0** |
| `SLV_R` | 178.741 mm | 140.765 mm | **140.765 mm** |
| RAMP_N | 110 | 98 | **98** |
| 정착 f | 250 | 240 | **240** |
| convNet | 9.830835343189877e-05 | 9.911050178924592e-05 | **9.911050178924592e-05** |
| 게이트 | **pass** | fail | **fail** |
| ③a 관통 최대 | 0.3252650778712598 mm | 1.303184307796152 mm | **1.303184307796152 mm** |
| 자기관통 교차 | 0 | 23 | **23** |
| 목선 초과비 | 0.9899308031171294 | 0.9958305945976433 | **0.9958305945976433** |
| 봉제 간극 중앙/최대 | 2.046798 / 4.849906 mm | 2.369618 / 123.257174 mm | **2.369618 / 123.257174 mm** |
| λ 최대 | 1.3628108891987687 | 3.9447492035938305 | **3.9447492035938305** |
| 팔↔몸통 최소 간격 | 2.3232970008230542 mm | 1.836195044026062 mm | **1.836195044026062 mm** |
| 대역 교차 / 대역 최소 쌍거리 | 0 / 1.9950490699507553 mm | 0 / 0.08621082613974851 mm | **0 / 0.08621082613974851 mm** |

`fails` = `["③a 관통 1.3032mm > 0.5mm", "자기관통 교차 23 ≠ 0"]` — **v4-26 과 같은 두 줄**.

---

## §1-③ 캡처 — **2장 · v4-26 과 «바이트 동일»**

`docs/v4/캡처/27-1-front.png`(sha256 앞16 **`8a1fa2668d81f7be`**) · `27-2-side.png`(**`8eafe1a09df4f027`**) —
**v4-26 의 `26-1-front.png`·`26-2-side.png` 와 각각 같은 해시**다(같은 프리셋·같은 상태).
★ **화면 판정 0** — 「벗겨짐이 사라졌는가」는 전략 세션이 본다. 이 판이 말할 수 있는 것은
**화면이 바뀌지 않았다는 사실**뿐이다(파일이 비트로 같다).

---

## §2 자기검사

### 워커 3경로 회귀 — **셋 다 비트 동일**

| 경로 | job | sha256(앞 16) | 등재값 | 판정 |
|---|---|---|---|---|
| 정착-blob | `v4-27-회귀1` | `3d8fac29f90fbfe4` | 동일 | **동일** |
| 조립(램프 없음) | `v4-27-회귀2` | `0d833601326922e2` | 동일 | **동일** |
| 조립+램프 | `v4-27-회귀3` | `24b0d3a11e94757e` | 동일 | **동일** |

층3 산출(`fit-*.json`·`gate-*.json`) **6/6 동일**(태그·경로 제외).

| 항 | 값 |
|---|---|
| `src/` diff | **`garmentScene.ts` 한 파일 · +36/−5**(주석 8줄 포함 · `armProfile` 전달 분기 안) |
| `dressRun.ts` | **0줄** · 물리 **0줄** · `gpu/engine/`·`worker.py` **diff 0** |
| 정본·blob·문턱 | **0** · `armAxisEnv`·NaN 가드 **체제 유지**(수정 0) |
| 이 판이 **신설**한 것 | job 4종(`v4-27-Aramp` · `v4-27-회귀1~3`) · 산출 3종(`asm-Arad_M.bin` 등) · 캡처 2장 |
| pytest(2호기) | **40 passed · 2 xfailed · 1 xpassed**(64.44 s) |
| `npx tsc -b` | **통과**(종료 0) |
| 브랜치 인쇄 | §0 머리에 에어·2호기 둘 다 · §0 은 집행 **«전»** 커밋(`62f245d`) |
| ecc 에이전트 / 스킬 | **0 / 0** |

---

## §4 회차 갈래 — **B**(①(나) ⟹ 값 상신 · 판정은 전략 세션)

**넘기는 것 7건**

1. ★★★ **판정문의 인과 사슬이 첫 고리에서 끊긴다** — 반경 조건을 넣어도 A포즈 조립 페이로드가
   **462,912 바이트 전량 동일**(`8ad460577caef5ec`) · `SLV_R` **140.765 mm 불변** · 굽기 결과 **비트 동일**
   (`2789d42e882ed094`) · **캡처까지 바이트 동일**. ⟹ 「대역 → SLV_R → 벗겨짐」의 첫 화살표가 **없다**.
   ★ 이 결론은 **값 보기 «전»에 §0-4ㄴ 로 등재한 예측**이고, 값이 그대로 맞았다.
2. ★★★ **`SLV_R` 의 진짜 자리는 `fitSleeve()`(`:713-730`)다** — 소매 관이 **몸 SDF** 와 **몸판 기둥**
   (`distToSurface`)에서 SEP 이상 떨어질 때까지 R 을 키운다. A포즈에서는 팔이 몸통에 붙어 그 조건이
   R 을 키우는 쪽으로 작동하는 것으로 «보인다» — **이 판은 그 자리를 건드리지 않았고 단정도 하지 않는다**.
3. ★★★ **사전 등재한 유도 규칙(「가장 큰 틈」)이 이 몸에서 서지 않는다** — 남는 정점 **3**(기대 493 자릿수).
   반경 분포가 팔 밖에서 **연속**이라 가장 큰 틈(7.972 mm)이 **팔 안**에서 났다. **규칙을 값 보고 바꾸지 않았다.**
4. ★★ **문턱을 두면 어디에 있는지는 값으로 있다** — 150 mm → 412정점 / 200 mm → 477정점(기대 자릿수).
   **선택은 이 판이 하지 않았다.**
5. ★★ **회귀는 그대로다** — T포즈 82/82 비트 동일 · 워커 3경로 비트 동일 · pytest 40/2/1 · tsc 통과.
6. ★ **커밋된 반경 조건은 «무해»하다** — 전달 분기에서 `ARM` 을 소비하는 곳이 없다(조립 경로 소비자 0).
   ⟹ 되돌릴지(diff 0), 문턱 규칙을 갈아 끼울지는 **전략 세션의 선택**이다.
7. ★ **A포즈 층3(`deriveLevels`)은 여전히 별도 판**이다 — 워커가 A포즈 칸을 실패로 기록하는 원인도 그것이다.

**이 판이 하지 않은 판단** — 반경 문턱의 선택 · `fitSleeve` 의 처분 · 벗겨짐의 원인 귀속 · 화면.
