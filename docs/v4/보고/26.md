# v4-26 보고 (전략 세션 제출본 · 터미널 출력 전량)

**2026-09-03 · 집행 완료 · 브랜치 `v4-26-band-origin` · 갈래 B**

> CLAUDE.md 「v4 회차 보고 «저장»」 규약 — 이 파일이 전략 세션 전달본이다.
> 집행 정본 노트는 `docs/v4/26-대역원점.md` · 캡처는 `docs/v4/캡처/26-1-front.png` · `26-2-side.png`.

---

# v4-26 — 대역 원점 일반화(예외 3건째) · NaN 가드 · A포즈 재탐침 · 캡처

**물리 0줄 · `src/` diff = `garmentScene.ts` 대역 원점 «한정»(예외 대장 3건째) · `dressRun.ts` 0줄 · 정본·blob·문턱 0 · 워커 3경로 diff 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-26-band-origin` |
| 기점 | **`f55953d`**(= v4-25 §1~§4) |
| §0 커밋 | **`817db6b`**(집행 «전») |
| §1~§4 커밋 | **`e07b624`** — 본문 · 보고 · 캡처 2장 · 볼트 4종 · metrics · 코드·계기 **한 커밋** |
| 보고 파일 | `docs/v4/보고/26.md` |

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v4-26-band-origin f55953d → Switched to a new branch 'v4-26-band-origin'
       $ git branch --show-current → v4-26-band-origin · HEAD f55953d
 2호기 (§0 커밋을 push 한 «뒤» 같은 자리로 전환 — v4-21~25 와 같은 순서 · §0-5 에 인쇄)
```

---

## §0 수령·사무

### §0-1 판정문 (전략 세션 · v4-25 §4 수령 · 원문)

```
 「갈래 B 수령 · ★ 배선 성립(82/82 · 사전 결정 ㄱ 옳음 · T포즈 실측 축 ≠ ±x 증명) ·
   ★★ NaN 자리 판독 수용 — axDot(원점 투영) ↔ 대역 경계(어깨 길이)의 의미 불일치 ·
   암묵 가정 = 원점이 몸 중심선 위 · 민감도 표가 기전 증명 ·
   ★ 예외 대장 3건째 «승인»: garmentScene.ts 대역 산정 원점 일반화 한정 —
     armAxis 전달 시에만 axDot(p) = AX·(p − C) · C = 어깨 피벗(몸에서 · A포즈 json 어깨 관절 · 손 상수 0) ·
     axPoint(원통 중심)도 같은 원점 의미 · 미전달 경로는 분기 자체를 안 탄다(T포즈 비트 동일 구성 보장) ·
     조건 = 물리 0줄 · 82칸 비트 동일 ·
   ★ 계기 확장 승인 — v4AsmExport 유한성 검사(NaN 이면 던진다) ·
     함정 후보 「배치 산출의 조용한 NaN 통과」 ·
   ★ SLV_R 56.4mm(정본 61.5 근접) = 축이 관찰 ①에 닿는 정황(단정 0) · 화면 자연 실험은 이 판 캡처에서」
```

### §0-2 바꾸지 않는 것 (회차 프롬프트 원문)

```
 물리 **0줄** · `dressRun`(v4-25 상태) **0** · 정본·blob·문턱 **0** ·
 워커 **3경로 diff 0** · `armAxisEnv` **체제 유지**.
```

### §0-3 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · 단위 명시)

**★ 단위** — ①은 **칸**(T포즈 조립 108칸 중 성공 82칸) · ③은 **칸 1개** · 간격·관통 **mm** · 프레임 **1/60 s**.

| 항 | 갈래 |
|---|---|
| **①** 대역 원점 일반화 + 회귀 | **(가)** 82/82 비트 동일 · 패턴해시 동일 / **(나)** 불일치 ⟹ **즉시 복원 · diff 0 · 값 상신** |
| **②** NaN 가드 | `v4AsmExport` 유한성 검사 · **v4-25 의 NaN 조립 재현 시 던지는지**(값) |
| **③** A포즈 재탐침 | **(가)** 게이트 `pass` / **(나)** fail(채널·값 · 처방 0) / **(다)** 조립 실패(사유·값) |
| **④** 캡처 | 앞·옆 2장 → `docs/v4/캡처/26-1-front.png` · `26-2-side.png` · **화면 판정 0** |

**§4** — A = ①(가)+②+③(가) / B = ③(나)(다) ⟹ 값 상신 / C = ①(나) ⟹ 복원·상신 / D = 그 외 ⟹ 정지.

### §0-4 사전 결정 5건 (값 보기 «전» · CC 집행 설계)

```
 ㄱ **C 는 «몸 중심선 위» 점으로 넘긴다** — 판정문이 스스로 적은 「암묵 가정 = 원점이 몸 중심선 위」를
    값으로 지킨다: `C = (0, 어깨 피벗 y, 어깨 피벗 z)`.
    근거(값으로 확인 가능) — 대역 경계 `[SW/2+CAP_H, SW/2+SLEN]` 은 **몸 중심선에서 잰 길이**다.
    C 를 어깨 피벗 «그대로»(x≈0.17) 넘기면 대역이 팔의 «바깥 절반»(팔꿈치 너머)을 고른다.
    ⟹ x 를 0 으로 둔 투영점을 넘기고, **그 사실을 여기 등재**한다(호출부가 넘기는 값이지 코드 안 상수 0).
 ㄴ **어깨 피벗은 «뼈에서» 읽는다** — A포즈 몸 json 에는 «축»만 있고 «관절 좌표»가 없다(사실).
    ⟹ `scripts/v4ArmOrigin.ts` 를 새로 세워 v4-17/18 과 **같은 뼈 경로**로 팔 뿌리 뼈의 월드 좌표를 뜬다
    (몸 blob 은 **다시 굽지 않는다** · 손 상수 0).
 ㄷ **`dressRun` 은 0줄이다** — 축 인자 «객체» 가 그대로 건네지므로(구조적 타이핑) `origin` 은
    `armAxis` 안에 실어 보낸다. `dressRun` 의 타입에는 안 보이지만 값은 통과한다.
    ⟹ **타입 표기 부채**로 §4 에 남긴다(다음 판에서 반영 권고 · 이 판 적용 0).
 ㄹ **좌우 거울** — 원통은 **x 만 뒤집는다**(`sgn`). 기울어진 축에서 옛 식(`tt = sgn·t`)을 그대로 쓰면
    왼팔의 y 가 «위»로 간다 ⟹ 전달 분기에서는 `x` 성분에만 `sgn` 을 건다.
    이것을 승인 문언 「axPoint 도 같은 원점 의미」 «안»으로 읽고 집행하며, **읽은 사실을 여기 등재**한다.
 ㅁ **회귀는 같은 기계 전/후** — 2호기에서 뜨되 ssh 가 끊기면 에어로 옮기고 그 사실을 적는다(v4-25 와 같은 규약).
```

---

## §1-① 대역 원점 일반화 — **(가) 82/82 비트 동일**

### diff 원문 (`src/v3/garmentScene.ts` **전달 분기 한정**)

```diff
-  armAxis?: { left: [number, number, number]; right: [number, number, number] };
+  /* v4-26 §1-① — `origin` = **대역 산정 원점 C**(어깨 피벗 · 호출부가 몸에서 읽어 넘긴다).
+   * 넘기지 «않으면» 아래 `axDot`·`axPoint` 는 **옛 식 그대로**다(분기 자체를 안 탄다). */
+  armAxis?: { left: [number, number, number]; right: [number, number, number];
+              origin?: { left: [number, number, number]; right: [number, number, number] } };
```
```diff
-  const axDot = (px, py, pz) => AX[0] * px + AX[1] * py + AX[2] * pz;
-  const axPoint = (t, R, ph, sgn) => {
-    const c0 = Math.cos(ph), s0 = Math.sin(ph), tt = sgn * t;
-    return [ 0 + tt*AX[0] + R*(c0*AU[0]+s0*AV[0]),
-             ARM.yc + tt*AX[1] + R*(c0*AU[1]+s0*AV[1]),
-             ARM.zc + tt*AX[2] + R*(c0*AU[2]+s0*AV[2]) ];
-  };
+  const AO = cfg.armAxis?.origin ? [origin.right…] : null;          // C
+  const axDot = AO
+    ? (px,py,pz) => AX[0]*(px−AO[0]) + AX[1]*(py−AO[1]) + AX[2]*(pz−AO[2])
+    : (px,py,pz) => AX[0]*px + AX[1]*py + AX[2]*pz;                 // ← 미전달 = 옛 식 «그대로»
+  const axPoint = AO
+    ? (t,R,ph,sgn) => [ sgn*(AO[0] + t*AX[0] + R*(c0*AU[0]+s0*AV[0])),   // sgn 은 x 성분에만
+                        AO[1] + t*AX[1] + R*(c0*AU[1]+s0*AV[1]),
+                        AO[2] + t*AX[2] + R*(c0*AU[2]+s0*AV[2]) ]
+    : (t,R,ph,sgn) => …옛 식 그대로…;
```

`git diff --stat src/v3/garmentScene.ts` = **1 file changed, 38 insertions(+), 11 deletions(-)**(주석 14줄 포함) ·
`dressRun.ts` **0줄**(§0-4ㄷ — `origin` 은 `armAxis` 객체에 실려 구조적 타이핑으로 통과한다) · 물리 **0줄**.

### 회귀 — T포즈 108칸 조립 드라이런(**2호기** · 전/후)

```
 $ TAG=v4-26-before npx tsx scripts/v4AssembleHash.ts   → 칸 108 · 성공 82 · 600858 ms
 $ TAG=v4-26-after  npx tsx scripts/v4AssembleHash.ts   → 칸 108 · 성공 82 · 789545 ms
```

| 채널 | 값 |
|---|---|
| 행 대조 | **108/108 동일**(`posSha`·`uvSha`·`n`·`m`·`sub`) |
| 조립 성공 칸 | **82 ↔ 82** ⟹ **82/82 비트 동일 · 패턴해시 동일** |
| 불일치 | **0**(다른 것은 `tag`·`ms` 벽시계뿐) |

★ 회귀는 **2호기에서 전/후** 떴다(§0-4ㅁ 충족 · 이번엔 ssh 가 끊기지 않았다).

### 어깨 피벗 C (뼈에서 · `scripts/v4ArmOrigin.ts` · 굽기 0)

| | 피벗(월드) | **넘긴 C**(중심선투영) | 방향 |
|---|---|---|---|
| 왼팔 `mixamorig1LeftArm` | (0.18685…, 1.4321118440203853, −0.029804377681745885) | **(0, 1.4321118440203853, −0.029804377681745885)** | (0.837455482475954, −0.5453251468113814, 0.03589984855277742) |
| 오른팔 `mixamorig1RightArm` | (−0.18685160239983742, 1.4321119511472187, −0.02806145821212186) | **(0, 1.4321119511472187, −0.02806145821212186)** | (−0.8376379933700414, −0.5452287971924666, 0.03298713044607542) |

★ `x = 0` 으로 투영해 넘긴 근거는 §0-4ㄱ(대역 경계가 **몸 중심선에서 잰 길이**라서다) — **호출부의 값**이지 코드 안 상수가 아니다.

---

## §1-② NaN 가드 — **선다**(쓰기 «전»에 던진다 · 산출 0건)

`scripts/v4AsmExport.ts` 에 유한성 검사를 넣었다(패널별 `pos`·`uv` 전 정점 · **`prepare` 직후 · 첫 쓰기 앞**).

**재현 검증** — v4-25 의 NaN 조건(축만 넘기고 **원점 없음**)으로 다시 돌렸다:

```
 $ ARM_AXIS_JSON=…a35.json (ARM_ORIGIN_JSON 없음) TAG=Anan_M npx tsx scripts/v4AsmExport.ts
 Error: 조립 산출에 NaN/Inf 가 있다 — sleeveR: pos 510 · uv 0 / 510 | sleeveL: pos 510 · uv 0 / 510
     at scripts/v4AsmExport.ts:91
 $ dir gpu/oracle/export/*Anan*   → **산출 0건**(쓰기 «전»에 던졌다)
```

★ 첫 판은 검사를 `scene-*.bin` **뒤**에 두어 부분 산출 3개가 났다 — **가드를 `prepare` 직후로 옮기고 재검증**했다(위 결과).
★ 함정 후보 「배치 산출의 조용한 NaN 통과」는 이 판에서 등재한다(`.obsidian-log/함정/`).

---

## §1-③ A포즈 재탐침(실측 축 + 어깨 피벗) — **(나) 게이트 fail**

### 대역 — **NaN 은 사라졌다. 그러나 «팔만» 고르지는 않는다**

| 축·원점 | axDot 범위 | 대역 `[0.2150, 0.4200]` 안 정점 | 대역 y 범위 |
|---|---|---|---|
| +x · 원점 없음(v4-24) | −0.7646 ~ +0.7645 | **493** | (팔) |
| 실측 축 · 원점 없음(v4-25) | −1.2045 ~ +0.1929 | **0** ⟹ NaN | — |
| **실측 축 · C 적용(이 판)** | **−0.4227 ~ +0.9747** | **2,509** | **0.3763 ~ 1.4263** |

⟹ **NaN 은 원점 일반화로 닫혔다**(정점 0 → 2,509 · `ARM.yc/zc` 유한 · pos NaN **0/9,644**).
**그러나 기대(「+x 493 과 같은 자릿수」)와 다르다** — 대역이 **다리·몸통까지** 걸린다(y 하한 **0.3763 m**).
사실만 적는다: 축이 아래로 기운 만큼, **축 위 거리 [0.215, 0.420]** 이라는 조건 하나로는 팔을 고르지 못한다
(**반경 조건이 없다**). **처방 0** — 이 판의 승인 범위는 «원점»까지다.

### 조립 (NaN 가드 통과)

| 채널 | v4-24(축 없음) | v4-25(축만) | **v4-26(축+원점)** |
|---|---|---|---|
| pos NaN | 0 | **2,040 좌표**(소매 1,020 정점) | **0** |
| `SLV_R` | 178.741 mm | 56.431 mm | **140.765 mm** |
| `PLACE_SIG` | `0.024131/0.002000/0.215000/0.178741` | `…/0.056431` | **`0.024131/0.002000/0.215000/0.140765`** |
| RAMP_N | 110 | (산출 불가) | **98** |
| n · 제약 · substeps | 9644 · 46178 · 231 | 〃 | 〃 |

### 굽기 · 정착

```
 job v4-26-Aramp — fp f64 · arch cuda · cell Aori_M(asm-Aori_M.bin) · frames 400(상한) · ramp true
 정착 **f240** · convNet **9.911050178924592e-05** ≤ TOL 1e-4 · 2.758 s/프레임 · JIT 13.9s · 벽시계 686.0s
 셀프 쌍/서브스텝 42897.393939393936 · 해소/서브스텝 736.5757575757576
 궤적 (10, 0.10945639818491054) (20, 0.0632254576355345) (30, 0.04253160164346694) …
      (220, 0.0003020188138679121) (230, 0.0001311755656634213) (240, 0.00009911050178924592)
 ★ 워커는 이 칸을 **실패로 기록**한다 — 층3 `v4FitReport` 가 **던지기** 때문이다(아래).
```

### 층3 리포트 — **여전히 산출 불가**(v4-24 와 같은 자리)

`scripts/v4FitReport.ts` → `bodyLevels.deriveLevels` 가 A포즈 몸에서 「C 최소가 구간 «끝»에 붙는다
(내부 극값 없음) — 갈래 B」로 **던진다**. ⟹ 워커 층3은 **칸 실패**로 남고, **게이트는 따로** 냈다(아래).

### 게이트 5채널 — **`pass: false`**

| 채널 | 값 | 문턱 | 판정 |
|---|---|---|---|
| ③a 관통 최대 | **1.303184307796152 mm**(관통정점 899) | ≤ 0.5 | ❌ |
| 자기관통 교차 | **23** | 0 | ❌ |
| 목선 초과비 | **0.9958305945976433**(링 96.45275475334883 / 허용 96.85658913936031 cm) | ≤ 1 | ✅ |
| 정착 창 순변위 | **0.09911050178924592 mm** | ≤ 0.1 | ✅ |
| 보조 장치(invMass=0) | **0** | 0 | ✅ |
| (부수) 봉제 간극 | 중앙 **2.369617708279929** · 최대 **123.25717358951295 mm** · λ 최대 **3.9447492035938305** | — | — |

`fails` = `["③a 관통 1.3032mm > 0.5mm", "자기관통 교차 23 ≠ 0"]`

### 겨드랑이 채널

| 채널 | v4-24(축 없음) | **v4-26(축+원점)** |
|---|---|---|
| 팔↔몸통 최소 간격(봉제쌍 제외) | 2.3232970008230542 mm | **1.836195044026062 mm** |
| 대역 자기관통 교차 | 0 | **0** |
| 대역 최소 쌍거리 | 1.9950490699507553 mm | **0.08621082613974851 mm** |
| 대역 관통 최대 | 0.3252650778712598 mm | **0.23105439707030453 mm**(정점 9610) |
| 대역 몸거리 최소 | 0.6747349221287402 mm | **0.7689456029296955 mm** |
| 전체 교차 / 관통 최대 | 0 / 0.3252650778712598 mm | **23 / 1.303184307796152 mm** |

★ 대역(소매↔몸통 봉제쌍 1-링) 크기는 두 판이 같다(정점 418 · 삼각형 406 · 소매 1,020 · 몸통 8,624).

---

## §1-④ 캡처 — **2장**

`docs/v4/캡처/26-1-front.png` · `docs/v4/캡처/26-2-side.png`(900×1200 · 정사영 `front`/`sideXplus` ·
몸 = A포즈 몸 · 옷 = ③의 정착 위치 `Aori_M.bin` · v4-24 와 **같은 프리셋**).
★ **화면 판정 0** — 관찰 3건(①가슴 텐트 ②밑단 A라인 ③목선 V)의 소멸/잔존은 **전략 세션이 본다**.
대조군은 v4-24 의 `24-1-front.png` · `24-2-side.png` 다(같은 몸 · 축만 다르다).

---

## §2 자기검사

### 워커 3경로 회귀 — **셋 다 비트 동일**(원점 일반화 «뒤»의 값이다)

| 경로 | job | 결과 | sha256(앞 16) | 등재값 | 판정 |
|---|---|---|---|---|---|
| 정착-blob | `v4-26-회귀1`(`c100-h185-s40_L`) | — | `3d8fac29f90fbfe4` | 동일 | **동일** |
| 조립(램프 없음) | `v4-26-회귀2`(`Tasm_M` · 200프레임) | — | `0d833601326922e2` | 동일 | **동일** |
| 조립+램프 | `v4-26-회귀3`(`Tasm_M` · 260프레임) | 정착 **f180** · 425.6 s | `24b0d3a11e94757e` | 동일 | **동일** |

층3 산출(`fit-*.json`·`gate-*.json`)도 **6/6 동일**(태그·경로 제외).
★ 회귀2 실행 중 ssh 가 한 번 끊겼으나(`Read from remote host … Operation timed out`) **2호기 부모 프로세스는 살아
끝까지 돌았다** — 위 값은 그 완주분을 회수한 것이다(사실 등재).

| 항 | 값 |
|---|---|
| `src/` diff | **`garmentScene.ts` 한 파일 · +38/−11**(주석 14줄 포함 · 전달 분기 한정) |
| `dressRun.ts` | **0줄**(§0-4ㄷ · `origin` 은 `armAxis` 객체에 실려 통과) · 물리 **0줄** |
| 정본·blob·문턱 | **0** · `gpu/engine/`·`worker.py` **diff 0** |
| 이 판이 **신설**한 것 | `scripts/v4ArmOrigin.ts`(어깨 피벗 · 굽기 0) · 함정 후보 노트 1 · job 4종 |
| 이 판이 **고친** 것 | `scripts/armAxisEnv.ts`(`origin` 읽기 · 체제 유지) · `scripts/v4AsmExport.ts`(**유한성 가드** · 첫 쓰기 앞) |
| pytest(2호기) | **40 passed · 2 xfailed · 1 xpassed**(60.81 s) |
| `npx tsc -b` | **통과**(종료 0) |
| 브랜치 인쇄 | §0 머리에 에어·2호기 둘 다 · §0 은 집행 **«전»** 커밋(`817db6b`) |
| ecc 에이전트 / 스킬 | **0 / 0** |

---

## §4 회차 갈래 — **B**(③(나) ⟹ 값 상신 · 판정은 전략 세션)

**넘기는 것 7건**

1. ★★★ **원점 일반화가 NaN 을 닫았다** — 대역 정점 **0 → 2,509** · pos NaN **2,040 → 0** ·
   T포즈 조립 **82/82 비트 동일**(승인 조건 충족).
2. ★★★ **그러나 대역이 «팔만» 고르지 않는다** — 대역 y 가 **0.3763~1.4263 m**(다리까지) ·
   기대(「+x 493 과 같은 자릿수」)의 **5배**인 2,509. **축 위 거리 조건 하나로는 부족하다 — 반경 조건이 없다.**
3. ★★★ **③ 게이트 fail** — 관통 **1.303184307796152 mm**(> 0.5) · 자기관통 교차 **23**(≠ 0).
   목선(0.99583)·정착(0.09911)은 통과. 봉제 간극 최대 **123.25717358951295 mm** 가 남는다.
4. ★★ **소매 반지름이 «가운데»로 왔다** — `SLV_R` 178.741(축 없음) → 56.431(축만·NaN) → **140.765 mm**(축+원점).
   대역이 다리를 포함해 부푼 만큼 반지름도 부푼 것으로 읽힌다(**단정 0**).
5. ★★ **NaN 가드가 선다** — 재현 시 패널·개수와 함께 던지고 **산출 0건**. 함정 후보 등재.
6. ★★ **A포즈 층3 리포트는 여전히 산출 불가**(`deriveLevels` 갈래 B) — v4-24 와 같은 자리 · 이 판에서 손대지 않았다.
7. ★ **캡처 2장**이 저장소에 있다(같은 프리셋 · v4-24 와 대조 가능) — **화면 판정 0**.

**이 판이 하지 않은 판단** — 대역에 반경 조건을 넣을지 · A포즈 대역 재도출 · 관통 1.3 mm 의 처분 · 화면.
