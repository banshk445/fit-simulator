# v4-34 보고 (전략 세션 제출본 · 터미널 출력 전량)

**2026-09-05 · 집행 완료 · 브랜치 `v4-34-frontier` · 갈래 A**

> CLAUDE.md 「v4 회차 보고 «저장»」 규약 — 이 파일이 전략 세션 전달본이다.
> 집행 정본 노트는 `docs/v4/34-경계측정.md`. **캡처 0**(측정 판).

---

# v4-34 — SDF 포화 사실(참깊이 재측정) · 변환 경로 위 «위반 0 경계» 측정 · 처방 재료

**물리 0줄 · `src/` diff 0(계기·판정 스크립트만) · 굽기 0 · 캡처 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-34-frontier` |
| 기점 | **`e274504`**(= v4-33 §1~§4) |
| §0 커밋 | **`5355ce0`**(집행 «전») |
| §1~§4 커밋 | **`69707f6`** — 본문 · 보고 · 볼트 3종 · metrics · 계기 1종 · 함정 후보 **한 커밋**( diff 0) |
| 보고 파일 | `docs/v4/보고/34.md` |

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v4-34-frontier e274504 → Switched to a new branch 'v4-34-frontier'
       $ git branch --show-current → v4-34-frontier · HEAD e274504
 2호기 (§0 커밋을 push 한 «뒤» 같은 자리로 전환 — §0-5 에 인쇄)
```

---

## §0 수령·사무

### §0-1 판정문 (전략 세션 · v4-33 §4 수령 · 원문)

```
 「갈래 C 수령 · ★ 강체 6자유도 소진 확정(회전 포함 중앙 31.6 · 위반 291→148 · 0 아님) ·
   28°(손 각도 0)는 방향 정합·양 부족의 값 ·
   ★★ 계기 유보 승격 — 깊이 10.5054 가 세 판에서 동일 = SDF 밴드(헤더 ≈8.6mm) 포화 후보:
     (a) 실깊이 미지 (b) 밴드 밖 관통은 솔버도 못 민다(기울기 소실) ⟹ 측정 자부터 확인 ·
     함정 후보 「포화된 계기로 잰 깊이를 참값으로 안다」 ·
   ★★ 강체 불가 ≠ 기하 불가 — 변환 경로 t∈[0,1] 위 «위반 0 최대 t» 미측정:
     그 점의 봉제 중앙 ≤ 217.0112(T포즈 램프 실적)이면 «부분 정합 + 램프» 처방이 손 상수 0 으로 선다
     (t 는 탐색값 · fitSleeve R 과 같은 성격) · 밖이면 암홀(패턴) 질문이 정당 ·
   이 판 = 측정 · 처방 0」
```

### §0-2 바꾸지 않는 것 (회차 프롬프트 원문)

```
 물리 **0줄** · `src/` **0** · 정본·blob·문턱 **0** · 워커 **diff 0** ·
 격자 용량 **262144** · 계기(v4-28~33) **유지**.
```

### §0-3 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · 단위 명시)

**★ 단위** — ①은 **mm**(깊이) · ②의 `t` 는 **무차원**(0~1 · 강체 변환 경로의 매개변수) · 거리 **mm**.

| 항 | 갈래 |
|---|---|
| **①** SDF 포화 사실 | **(가)** 클램프 확인(실깊이 > 10.5) / **(나)** 클램프 아님(실깊이 ≈ SDF 값) / **(다)** 그 외 |
| **②** 위반 0 경계 | **(가)** `t*` 의 봉제 중앙 **≤ 217.0112** ⟹ 「부분 정합 + 램프」 재료 성립 / **(나)** 초과(값) ⟹ 강체 경로 불가 확정 · 암홀 질문 상신 / **(다)** `t* ≈ 0`(값) |
| **③** 처방 재료 정리 | 판단 0 — ②의 갈래별 사실 목록만 |

**§4** — A = ①②③ 전량 ⟹ 상신 / B = ①(다) 또는 ② 측정 불가 ⟹ 정지 / C = 그 외 ⟹ 정지.

### §0-4 사전 결정 5건 (값 보기 «전» · CC 집행 설계)

```
 ㄱ **참깊이 자는 «있는 것»을 쓴다** — `src/v3/instruments.ts` `makeBodyDistance().exactBodyDist`
    (몸 «메시»까지의 정확 무부호 거리 · v3-37 이래 게이트가 쓰는 자). SDF 는 **부호**에만 쓴다.
    ⟹ 부호 있는 여유 = (안이면 −, 밖이면 +) × 정확 거리. **새 계산 0 · 새 문턱 0.**
 ㄴ **①의 판정 기준을 지금 고정한다** — v4-33 의 148 정점에서 **참깊이 중앙이 10.5054 를 «넘으면» (가)**,
    **±0.01 mm 안에서 같으면 (나)**, 그 밖이면 (다).
 ㄷ **②의 경로를 지금 정의한다** — 강체 변환을 `t` 로 보간한다:
      `p(t) = R(축, 각도·t)·(p − s̄) + s̄ + t·(d̄ − s̄)`   (t=0 항등 · t=1 은 v4-33 의 (R, Δ))
    회전은 **같은 축에서 각도만** 줄이고 평행이동은 선형이다. 좌·우 각각 자기 (축, 각도, 중심)을 쓴다.
 ㄹ **`t*` 의 정의** — 「참깊이 자로 위반 0(모든 소매 정점의 부호 있는 여유 ≥ SEP)인 **최대 t**」.
    이분 탐색 40회 · **손 상수 0**(문턱은 SEP 그대로).
 ㅁ **이 판은 처방을 고르지 않는다** — ③은 «다음 판이 쓸 사실 목록»만 적는다(문안 초안 포함 · 적용 0).
```

---

## §1-① SDF 포화 사실 — **(가) 클램프 확인**(참깊이가 SDF 값의 2~4배)

### ㄱ 코드 사실

```
 src/v3/bodySdf.ts:41-42  /** |d|를 이 값으로 «자른다» — 띠 밖은 «부호»만 유효하다 */  band: number;
 src/v3/bodySdf.ts:24-25  「격자 밖은 «밖(자유 공간)»으로 간주 — 외삽하지 않는다」
 src/v3/bodySdf.ts:124    const band = thickness + 2 * h;      ← 밴드의 정의(손 상수 0)
 이 장면의 값             h = **3.7527 mm** · band = **8.5054 mm**(= 1.0 + 2×3.7527) · THICK 1.0 · SEP 2.0
```

⟹ SDF 로 잰 깊이의 상한은 **SEP + band = 2.0 + 8.5054 = 10.5054 mm** 다.
**세 판(v4-32·33·34)에서 되풀이된 10.5054 는 «포화값»이었다** — 깊이가 아니라 **자의 끝**이다.

### ㄴ 참깊이 재측정 (v4-33 의 위반 **148** 정점 · 자 = `exactBodyDist`)

| 자 | 최소 | p25 | **중앙** | p75 | **최대** |
|---|---|---|---|---|---|
| SDF 깊이 | 0.5893 | 9.2444 | **10.5054** | 10.5054 | **10.5054** |
| **참깊이(정확 거리)** | 0.5937 | 9.4055 | **19.3403** | 29.2328 | **41.3362** |

⟹ **(가)** — 참깊이 중앙이 **19.3403 mm** 로 클램프값 10.5054 를 **넘는다**(§0-4ㄴ 기준). 최대는 **41.3362 mm**.
**실제 관통은 SDF 가 보여준 것의 약 2~4배**다. 함정 후보 「포화된 계기로 잰 깊이를 참값으로 안다」를 등재한다.
★ 판정문이 든 두 번째 함의(밴드 밖은 기울기가 없어 솔버가 못 민다)는 **이 판이 확인하지 않았다**(단정 0).

---

## §1-② 위반 0 경계 — **(다) `t*` = 0**(현행에서 조금도 못 움직인다)

| 채널 | 값 |
|---|---|
| t = 0 에서 위반 0? | **아니다** — 최소 여유 **1.9797 mm** < SEP **2.0**(부족 **0.0203 mm**) |
| t = 1 에서 위반 0? | 아니다(참깊이 최대 41.3362 mm) |
| **`t*`(위반 0 최대 t)** | **0** |
| 봉제쌍 거리 mm(t*=0) | 최소 197.2746 · p25 206.6353 · **중앙 234.7710** · p75 261.8464 · 최대 267.8725 |
| (대조) 봉제쌍 거리 mm(t=1) | 최소 11.4109 · p25 22.5786 · **중앙 31.6355** · p75 37.2595 · 최대 76.9355 |
| 중앙 ≤ 문턱 217.0112(t*) | **아니다**(234.7710) |
| 소매 y중앙 m(t=0 / t* / t=1) | 1.238413 / 1.238413 / 1.277675 |
| 비봉제 최소 쌍거리 mm(t*) | 172.8786 |

⟹ **(다)** — 강체 경로 위에 「위반 0」 구간이 **없다**. **출발점(t=0)부터 이미 SEP 를 0.0203 mm 어긴다.**
★ **자가 바뀌자 출발점이 바뀌었다** — v4-32·33 이 「이동 전 위반 0」이라고 적은 것은 **SDF 자 기준**이었고,
참깊이 자로 보면 t=0 도 경계 위(1.9797 mm)다. 두 판의 「전 = 0」은 **그 자의 값**으로 읽어야 한다(정정 사실).

---

## §1-③ 처방 재료 정리 (판단 0 · 이 판은 아무것도 고르지 않는다)

②가 (다)이므로 **「부분 정합 + 램프」 재료는 서지 않는다**. 남는 것은 판정문이 (나)에 걸어 둔 방향이다.

**다음 판이 쓸 «사실» 목록(측정 대상 · 값은 아직 없다)**

1. 암홀 링(몸판 쪽 봉제 정점 52×2)의 **중심·법선** — A포즈 몸의 **팔 축(AX)·어깨 피벗(P)** 과의 각도·거리.
2. 그 링 위 각 점의 **몸 표면까지 정확 거리**(참깊이 자) — 링 자체가 몸 «안»에 있는 구간이 있는가.
3. 소매 캡 링(소매 쪽 52×2)의 같은 값 — 두 링의 **둘레·지름·평면성** 비교.
4. T포즈에서 같은 네 값(대조군 · 재굽기 0 — `asm-Tasm_M.bin` 에서 뜬다).
5. 이 판의 확정 사실 두 개: **band = 8.5054 mm**(SDF 자의 끝) · **t=0 최소 여유 1.9797 mm**.

**이미 확보된 재료(이 판 산출)** — 참깊이 분포(중앙 19.3403 · 최대 41.3362) · 포화 상수(10.5054 = SEP + band) ·
강체 경로의 봉제쌍 거리 양끝(234.7710 ↔ 31.6355).

---

## §2 자기검사

| 항 | 값 |
|---|---|
| `src/` diff vs 기점 | **0**(`git diff --stat 5355ce0 -- src/` 무출력) · 물리 **0줄** |
| 워커 · `gpu/engine/` | **diff 0** · 정본·blob·문턱 **0** · 격자 용량 **262144 유지** · 계기(v4-28~33) **수정 0** |
| 이 판이 **신설**한 것 | `scripts/v4DepthFrontier.ts`(참깊이·경계 계기) · 산출 `l3ap-frontier-v4-34-A.json` · 함정 후보 노트 1 |
| 굽기 · 캡처 | **0 · 0** |
| pytest(2호기) | **40 passed · 2 xfailed · 1 xpassed**(199.42 s) |
| `npx tsc -b` | **통과**(종료 0) |
| 브랜치 인쇄 | §0 머리에 에어·2호기 둘 다 · §0 은 집행 **«전»** 커밋(`5355ce0`) |
| ecc 에이전트 / 스킬 | **0 / 0** |

---

## §4 회차 갈래 — **A**(①②③ 전량 · ①=(가) · ②=(다))

**넘기는 것 7건**

1. ★★★ **10.5054 는 깊이가 아니라 «자의 끝»이었다** — `band = thickness + 2h = 8.5054 mm` ·
   SDF 깊이 상한 = SEP + band = **10.5054 mm**. 세 판(v4-32·33·34)의 같은 수가 이것으로 설명된다.
2. ★★★ **참깊이는 2~4배 깊다** — 중앙 **19.3403** · p75 29.2328 · 최대 **41.3362 mm**(148 정점).
3. ★★★ **`t*` = 0** — 강체 경로 위에 「위반 0」 구간이 **없다**. 게다가 **t=0(현행)부터** 최소 여유가
   **1.9797 mm** 로 SEP 2.0 을 **0.0203 mm** 어긴다.
4. ★★★ **자가 바뀌자 «출발점»이 바뀌었다** — v4-32·33 의 「이동 전 위반 0」은 **SDF 자 기준**이었다.
   그 문장을 이 판이 값으로 정정한다(함정 후보 등재).
5. ★★ **「부분 정합 + 램프」 재료는 서지 않는다** — t* 에서 봉제 중앙이 **234.7710 mm**(문턱 217.0112 초과).
   판정문이 (나)에 걸어 둔 방향(**암홀 자리 = 몸판 쪽 질문**)이 남는다.
6. ★★ **다음 판이 잴 것 5가지**를 §1-③에 목록으로 남겼다(링 중심·법선 · 링의 참깊이 · 두 링의 둘레·평면성 ·
   T포즈 대조군 · 이 판의 두 상수).
7. ★ **측정만 했다** — `src/` 0줄 · 굽기 0 · 캡처 0 · 처방 0.

**이 판이 하지 않은 판단** — 암홀 자리의 처분 · 밴드 밖 기울기 소실이 솔버에 미치는 영향(미확인) · 화면.
