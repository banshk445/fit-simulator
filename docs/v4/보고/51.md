# v4-51 — 작은몸×L 칸 대조(제품 표시 ↔ 굽기 산출) · 「어깨 걸림」 재료 · 잔여 UX 등재

**물리 0줄 · `src/` diff 0(계기·렌더만) · 굽기 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-51-smoke-followup` |
| 기점 | **`8c0fa22`**(= v4-50 metrics·볼트) |
| §0 커밋 | **`46a36ef`**(집행 «전») |
| §1~§4 커밋 | 캡처 `40d5871` · 본문은 아래 커밋 |
| 보고 파일 | `docs/v4/보고/51.md` |

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v4-51-smoke-followup 8c0fa22 → v4-51-smoke-followup · HEAD 8c0fa22
 2호기 (§0 커밋 push 뒤 같은 자리로 — §0-5 에 인쇄)
```

---

## §0 수령·사무

### §0-1 판정문 (전략 세션 · 스모크 3차 수령 · 원문)

```
 「제품 화면 성립 — 기본 몸 M 합격(A포즈·핏맵·5행·각주) ·
   관찰 1: 작은몸(c87.5-h155-s40)×L 폴백이 오프숄더 —
     게이트가 어깨 걸림을 안 재는 틈(v4-24 계보)의 제품 노출 후보 vs 그 조합의 진짜 상태 — 대조가 가른다 ·
   관찰 2: 전 사이즈 미제공 몸의 빈 화면(몸 표시 UX · 등재만)」
```

### §0-2 바꾸지 않는 것

```
 물리 **0줄** · `src/` **0줄** · 자산·정본 **0** · 굽기 **0** · 문턱·규약 **0**(재료만 낸다)
```

### §0-3 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · 단위 명시)

**★ 단위** — 정점 수는 **개** · 높이는 **m** · sha 는 **hex 64자** · 칸은 **칸**.

| 항 | 갈래 |
|---|---|
| **①** 대조 | **(가)** 같은 산출 + 렌더도 오프숄더(= 진짜 상태) / **(나)** 산출은 정상인데 제품 표시가 다름 / **(다)** 그 외 |
| **②** 어깨 걸림 재료 | 편입 33칸에 **v4-30 정의 그대로** 「어깨선 위 옷 정점 수」 · **분포만**(문턱 제안 0) |
| **③** UX 등재 | 관찰 2(몸 단독 표시) · 렌더 미학(핏맵 얼룩) — **코드 0** |

**§4** — A = ①~③ ⟹ 상신 / B = 막힘 ⟹ 사실 상신.

### §0-4 사전 결정 4건 (값 보기 «전» · CC 집행 설계)

```
 ㄱ **「같은 산출인가」는 sha 로 가른다** — 제품이 읽는 blob(`v4-a35/settled-<칸>.bin`)의 본문 sha 와
    굽기 산출(`results/v4-46-A108/<칸>.bin`)의 «위치 블록» 을 맞대어 **바이트로** 확인한다.
 ㄴ **렌더는 기존 프리셋 그대로**(`scripts/v4AposeRender.ts` · v4-24 이후 같은 구도) — 새 구도 0.
 ㄷ **어깨선 정의는 v4-30 을 인용한다** — `gpu/bake/slip_probe.py:58` 「`(p[:,1] > Y_TOP).sum()`」 ·
    `Y_TOP` 은 그 칸 장면의 어깨선 높이(세계 m). **새 정의·새 문턱 0.**
 ㄹ **화면 판정 0** — 「오프숄더로 보이는가」는 전략 세션·사용자가 본다. CC 는 **수와 렌더 산출물**만 낸다.
```

### §0-5 커밋·전환 인쇄 (집행 «전»)

```
 §0 선커밋 **46a36ef** — 집행 «전» · push 완료 · 2호기는 렌더만 수행(굽기 0)
```


---

## §1-① 대조 — **(가) 같은 산출이다**(제품이 읽는 blob = 굽기 산출 · 바이트로 확인)

대상 칸 **`c87.5-h155-s40_L`**(판정문 관찰 1의 그 조합 · 작은 몸 × L 폴백).

| 채널 | 값 |
|---|---|
| 제품이 읽는 blob | `public/v3diag/v4-a35/settled-c87.5-h155-s40_L.bin` · n **10,050** · frame **200** |
| 그 blob 본문 sha | **95b01d29bb6d13c7…** |
| 색인 등재 sha | **95b01d29bb6d13c7…** ⟹ **일치**(그래서 화면이 그린다) · status **편입** · gate **pass** |
| blob 의 **위치 블록** sha | **c0a4caaa665a37a5…** |
| 굽기 산출 `results/v4-46-A108/c87.5-h155-s40_L.bin` | **241,200 B** = 정점 **10,050** · sha **c0a4caaa665a37a5…** |
| 속도 블록 | **전부 0**(v4-49 재포장 규약대로) |

⟹ **제품이 표시하는 좌표는 굽기 산출과 «같은 바이트»다**(위치 블록 sha 동일). 표시 경로가 다른 것을
보여주는 것이 아니다 ⟹ **(가)** — 화면에 보이는 상태가 **그 조합의 진짜 정착 상태**다.

**표준 프리셋 렌더**(같은 산출을 CC 가 다시 그린 것 · 구도는 v4-24 이후와 동일):
`docs/v4/캡처/51-1-front.png` · `51-2-side.png`(900×1200 · `front`/`sideXplus` ·
몸 = 그 칸의 그리드 정본 몸 · 옷 = `results/v4-46-A108/c87.5-h155-s40_L.bin`).
★ **화면 판정 0** — 「오프숄더로 보이는가」의 판단은 전략 세션·사용자가 한다. CC 는 산출물과 수만 낸다.

## §1-② 어깨 걸림 재료 — 편입 33칸 「어깨선 위 옷 정점 수」(v4-30 정의 그대로 · 분포만)

계기 신설 `scripts/v4ShoulderCount.ts` — 정의는 `gpu/bake/slip_probe.py:58` 「`(p[:,1] > Y_TOP).sum()`」 ·
`Y_TOP` 은 그 칸 장면의 어깨선 높이(`P.S.Y_TOP` · `scripts/v4SlipIdx.ts:45` 와 같은 출처).
위치는 **제품이 읽는 그 blob**에서 읽었다(화면이 보는 바이트와 동일). **새 정의·새 문턱 0.**

| 분포(33칸) | 값 |
|---|---|
| 최소 | **0** |
| p25 | 550 |
| **중앙** | **694** |
| p75 | 773 |
| 최대 | **1,129** |
| 평균 · 표준편차 | 623 · 276.76 |
| **0인 칸** | **3** |

| 칸(하위) | 어깨선 위 정점 | 비(정점 대비) | 옷 최고점 − 어깨선 mm | n |
|---|---|---|---|---|
| `c87.5-h155-s40_XL` | **0** | 0.000000 | **−10.03** | 11,050 |
| `c87.5-h155-s45_M` | **0** | 0.000000 | **−8.02** | 9,184 |
| `c87.5-h155-s45_XL` | **0** | 0.000000 | **−6.18** | 10,948 |
| **`c87.5-h155-s40_L`**(관찰 1) | **1** | 0.000100 | **+1.72** | 10,050 |
| `c122.5-h185-s40_S` | 343 | 0.034417 | +27.76 | 9,966 |
| `c122.5-h185-s40_M` | 398 | 0.037854 | +30.70 | 10,514 |

| 칸(상위) | 어깨선 위 정점 | 비 | 옷 최고점 − 어깨선 mm | n |
|---|---|---|---|---|
| `c122.5-h155-s40_M` | 921 | 0.095480 | +49.21 | 9,646 |
| `c122.5-h155-s50_XL` | 940 | 0.081315 | +49.41 | 11,560 |
| `c122.5-h170-s45_XL` | **1,129** | 0.095971 | **+60.86** | 11,764 |

★ **값이 갈린다** — 0~1 인 칸이 **4개**(전부 작은 몸 `c87.5-h155-s4x` 계열)이고, **그 다음 칸이 343**이다.
사이에 **342 의 빈 구간**이 있다. 「옷 최고점 − 어깨선」으로 봐도 4칸은 **−10.03 ~ +1.72 mm**,
나머지는 **+27.76 mm 이상**이다.
★ **문턱 제안 0 · 판단 0**(§0-2·§0-4ㄹ) — 이 표는 「편입 규약에 화면 채널을 더할 것인가」라는
**다음 결정의 재료**일 뿐이다. 게이트 5채널은 **한 줄도 바뀌지 않았다**.

## §1-③ 잔여 UX 등재(코드 0)

```
 ㉠ **전 사이즈 미제공 몸의 빈 화면**(판정문 관찰 2) — 지금은 제공 0칸이면 `landingSize` 가 `null` 을 내고
    (`src/v3/provide.ts:78-79`) 화면은 씬을 세우지 않는다(`V3ProductV1.tsx:151` 에서 `scened` 를 내린다).
    ⟹ **몸도 옷도 안 보인다.** 후보(구현 0 · 등재만): 「옷 없이 **몸만** 표시」 ·
    그러려면 몸 정점만으로 그리는 경로가 필요하다(`renderProduct` 는 몸·옷을 함께 받는다 —
    옷을 빈 배열로 넘기는 길이 있는지는 **확인하지 않았다**).
 ㉡ **핏맵 얼룩**(렌더 미학) — 정점 색(`fit.color.rgb`)을 그대로 칠하므로 대역 경계에서 얼룩이 보인다.
    후보(구현 0 · 등재만): 대역 경계 스무딩 · 색 스케일 재설계. **판정 채널 아님**(표시층).
 ㉢ 두 건 다 **백로그 재료**이고 이 판에서 코드는 한 줄도 건드리지 않았다.
```


---

## §2 자기검사

| 항 | 값 |
|---|---|
| `src/` diff vs 기점 `8c0fa22` | **0**(무출력) · 물리 **0줄** |
| 자산·정본 | **0 변경**(blob·색인·제공 목록 그대로 · `public/` diff 0) |
| `gpu/engine`·워커 | **diff 0** · 굽기 **0** |
| 이 판이 **신설**한 것 | `scripts/v4ShoulderCount.ts`(측정만) · 산출 `l3ap-shoulder-v4-51.json` · 캡처 2장 |
| 게이트 채널·문턱 | **0**(재료만 냈다 · 문턱 제안 0) |
| `npx tsc -b` | **통과**(종료 0) |
| pytest(2호기) | **40 passed · 2 xfailed · 1 xpassed**(63.61 s) |
| 캡처 커밋 | `40d5871`(에어 · 2호기 렌더분을 회수해 커밋) |
| 화면 판정 | **0** |
| 브랜치 인쇄 | §0 머리 · §0 은 집행 **«전»** 커밋(`46a36ef`) |
| ecc 에이전트 / 스킬 | **0 / 0** |

★ **절차 사실 1건** — 2호기가 v4-46 자리에서 캡처를 커밋해(`cc562fe`) push 가 거절됐다.
그 파일 2장을 에어로 회수해 **이 브랜치에 다시 커밋**했다(`40d5871`) · 렌더 산출물 자체는 같다.

## §4 회차 갈래 — **A**(①(가) + ②③)

**넘기는 것 6건**

1. ★★★ **①(가) — 제품이 보는 것이 «진짜 상태»다** — `c87.5-h155-s40_L` 의 제품 blob 위치 블록 sha
   **c0a4caaa665a37a5…** = 굽기 산출 파일 sha(241,200 B · 정점 10,050)와 **동일** ·
   본문 sha **95b01d29bb6d13c7…** 는 색인 등재값과 일치(gate pass · status 편입).
   ⟹ 표시 경로의 문제가 아니라 **그 조합의 정착 결과**다.
2. ★★★ **②의 분포가 값으로 갈린다** — 편입 33칸의 「어깨선 위 옷 정점 수」는 중앙 **694**
   (p25 550 · p75 773 · 최대 1,129 · 평균 623 · 표준편차 276.76)인데,
   **0~1 인 칸이 4개**(전부 작은 몸 `c87.5-h155-s4x`)이고 **그 다음 칸이 343** — 사이에 **342 의 빈 구간**이 있다.
3. ★★★ 「옷 최고점 − 어깨선」으로 봐도 같은 갈림 — 그 4칸은 **−10.03 ~ +1.72 mm**, 나머지는 **+27.76 mm 이상**.
4. ★★ **관찰 1의 칸은 그 4칸 중 하나**(`c87.5-h155-s40_L` · 어깨선 위 **1점** · 최고점 **+1.72 mm**).
5. ★★ **게이트는 이 채널을 안 본다** — 5채널(관통·교차·목선·정착·pinned)에 어깨 걸림이 없다(v4-24 계보).
   **문턱 제안 0 · 규약 변경 0** — 이 표는 다음 결정의 재료다.
6. ★ **UX 2건 등재**(코드 0) — 제공 0칸 몸의 빈 화면(몸만 표시 후보) · 핏맵 얼룩(표시층).

**이 판이 하지 않은 판단** — 화면(오프숄더 여부) · 편입 규약에 화면 채널을 더할지 · UX 구현.


---

## 부록 — raw 링크 (커밋 `127bf4b` 고정)

**신규** — `docs/v4/51-대조.md` · `docs/v4/보고/51.md` · `scripts/v4ShoulderCount.ts`(측정 계기) ·
`gpu/oracle/export/l3ap-shoulder-v4-51.json`(33칸 산출) · 캡처 `docs/v4/캡처/51-1-front.png` · `51-2-side.png`(커밋 `40d5871`)
**수정** — `.obsidian-log/전략세션-인수인계.md` · `.obsidian-log/00-현재상태.md` ·
`.obsidian-log/v4-노트-색인.md` · `docs/metrics-log.md`

- https://raw.githubusercontent.com/banshk445/fit-simulator/127bf4b/docs/v4/%EB%B3%B4%EA%B3%A0/51.md
- https://raw.githubusercontent.com/banshk445/fit-simulator/127bf4b/docs/v4/51-%EB%8C%80%EC%A1%B0.md
- https://raw.githubusercontent.com/banshk445/fit-simulator/127bf4b/docs/metrics-log.md
- https://raw.githubusercontent.com/banshk445/fit-simulator/127bf4b/.obsidian-log/%EC%A0%84%EB%9E%B5%EC%84%B8%EC%85%98-%EC%9D%B8%EC%88%98%EC%9D%B8%EA%B3%84.md
- 캡처 https://raw.githubusercontent.com/banshk445/fit-simulator/127bf4b/docs/v4/%EC%BA%A1%EC%B2%98/51-1-front.png · https://raw.githubusercontent.com/banshk445/fit-simulator/127bf4b/docs/v4/%EC%BA%A1%EC%B2%98/51-2-side.png
