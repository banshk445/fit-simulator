# v4-52 — 편입 규약에 「어깨 걸림」 채널 추가 · 제공 목록 재발행(29칸) · 제품 반영 확인

**물리 0줄 · `src/` diff = 편입 산출 스크립트 «한정» · 게이트 5채널·문턱 불변 · 제품 로더 0 · 굽기 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-52-shoulder-channel` |
| 기점 | **`858b20a`**(= v4-51 최종 HEAD) |
| §0 커밋 | **`58a23f8`**(집행 «전» · N 유도 포함) |
| §1~§4 커밋 | (집행 후 기입) |
| 보고 파일 | `docs/v4/보고/52.md` |

### ★ 기점 사실 병기(다툼 0)

```
 회차 프롬프트는 기점을 **40d5871** 로 적었다. 그 sha 는 v4-51 의 **캡처 커밋**이고,
 그 회차의 **최종 HEAD 는 858b20a**(보고 부록까지)다. 캡처 커밋에서 뗄 경우 v4-51 의 노트·볼트·metrics 가
 트리에서 빠지므로 **최종 HEAD 에서 뗐다**. 두 커밋은 같은 계보이고 40d5871 은 858b20a 의 조상이다.
 (판단은 전략 세션 몫 · 이 판은 사실만 적는다.)
```

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v4-52-shoulder-channel 858b20a → v4-52-shoulder-channel · HEAD 858b20a
 2호기 (§0 커밋 push 뒤 같은 자리로 — §0-5 에 인쇄)
```

---

## §0 수령·사무

### §0-1 판정문 (전략 세션 · v4-51 §4 수령 · 원문)

```
 「갈래 A 수령 · ①(가) 오프숄더 = 진짜 상태(sha 사슬 확정 · 표시 무죄) ·
   ★★ ② 분포 이봉: 0~1 ↔ 343(빈 구간 342) · 보조 채널 동일 갈림 ⟹
   처분: 편입 규약에 「어깨 걸림」 채널 신설
     (함정 14 아님 — 새 채널 · 근거 = 화면 관찰 + 이봉 분포 · 문턱 자리 = 빈 구간) ·
   게이트 5채널 불변(굽기 품질의 자와 화면 채널의 자를 가른다) ·
   4칸 편입→보류 강등 ⟹ 제공 29 · 보류 67 · 착용불가 12 · UX 2건 백로그 수용」
```

### §0-2 바꾸지 않는 것

```
 물리 **0줄** · **게이트 5채널·문턱 0**(굽기 품질의 자) · 제품 로더·매칭·분류 코드 **0** ·
 굽기 **0** · 정본 blob 바이트 **0** · v3 구판 경로 **불변**
```

### §0-3 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · 단위 명시)

**★ 단위** — 어깨 채널은 **정점 개수**(좌우 «합») · 칸은 **칸** · sha 는 **hex 64자**.

| 항 | 갈래 |
|---|---|
| **①** 규약 문언 | 편입 조건에 「어깨선 위 옷 정점 수 ≥ N」 추가 · **N 과 그 유도**를 등재 · 좌우 합/각각 명시 |
| **②** 재발행 | 제공 **29칸** · 강등 4칸은 **보류 + 사유(값)** · 제품 색인 갱신 · sha 규약(v4-50) 유지 |
| **③** 확인 | 제공 0칸 몸에서 화면이 어떤 경로를 타는지 **코드 사실**로(화면은 승혁) · `?tpose=1` 불변 |

**§4** — A = ①~③ ⟹ 승혁 확인 스모크(작은몸 1입력) / B = 막힘 ⟹ 사실 상신.

### §0-4 사전 결정 4건 (값 보기 «전» · CC 집행 설계)

```
 ㄱ **N 은 «빈 구간»에서 유도한다** — 관측된 두 끝(미달 최대 **1** · 성립 최소 **343**)의 **중점**을 쓴다:
    `N = (1 + 343) / 2 = 172`(정수). 두 끝에서 «가장 멀다» ⟹ 재굽기·재측정에서 흔들릴 여지가 최소다.
    **어느 칸의 값도 아니고**(결과에 맞춘 수가 아니다) 분포의 «틈»에서 나온다.
 ㄴ **채널 정의는 v4-30 을 그대로 인용한다** — `(p[:,1] > Y_TOP).sum()` · `Y_TOP` = 그 칸 장면의 어깨선 높이 ·
    **좌우 «합»**(v4-30 계기가 좌우를 가르지 않는다) · 위치는 **제품이 읽는 blob**에서 읽는다.
 ㄷ **게이트와 «자»를 가른다** — 이 채널은 **편입 규약**(제공 여부)이지 게이트가 아니다.
    `s4Gate.ts` 는 **한 글자도 안 고친다**(§0-2).
 ㄹ **강등 칸은 지우지 않는다** — 색인에 **보류 + 사유(값 포함)** 로 남긴다(T포즈 규약 동형).
```

### §0-5 커밋·전환 인쇄 (집행 «전»)

```
 §0 선커밋 **58a23f8** — 집행 «전»(N = 172 의 유도까지 담았다) · push 완료
 2호기 자산 동기화만 수행(굽기 0 · 코드 0)
```


---

## §1-① 규약 문언 — 편입 조건에 「어깨 걸림」 추가

### 새 편입 정의(둘 다 서야 한다)

```
 편입(= 「제공한다」) ⟺ ㉠ **게이트 `pass`**(굽기 품질의 자 · `src/v3/s4Gate.ts` 5채널·문턱 **그대로**)
                    **그리고**
                    ㉡ **어깨 걸림** — 「어깨선 위 옷 정점 수」 **≥ 172**(화면 채널의 자 · v4-52 신설)

 ㉡의 정의(v4-30 계기 «그대로» 인용 · 새 정의 0)
   값  = `(p[:,1] > Y_TOP).sum()`            ← `gpu/bake/slip_probe.py:58`
   Y_TOP = 그 칸 장면의 어깨선 높이(세계 m)  ← `P.S.Y_TOP`(`scripts/v4SlipIdx.ts:45` 와 같은 출처)
   집계 = **좌우 «합»**(v4-30 계기가 좌우를 가르지 않는다 · 단위 = **정점 개수**)
   위치 = **제품이 읽는 주입 blob 의 위치 블록**(= 화면이 보는 바이트)
```

### N = 172 의 유도(§0-4ㄱ · 값 보기 «전» 등재분 그대로)

```
 v4-51 §1-② 의 **이봉 분포** — 미달 무리의 최대 **1**(`c87.5-h155-s40_L`) ↔ 성립 무리의 최소 **343**
 (`c122.5-h185-s40_S`) · 그 사이 **342 의 빈 구간**.
 N = (1 + 343) / 2 = **172** — **빈 구간의 중점**이다. 두 무리에서 «가장 멀어» 재측정·재굽기에서
 분류가 흔들릴 여지가 최소이고, **어느 칸의 값도 아니다**(결과에 맞춘 수가 아니다 · 함정 14 회피).
 ★ 이 판 재산출에서도 같은 자리가 확인됐다 — **편입 칸의 최소 `shoulderAbove` = 343** ·
   강등 칸은 **0 · 0 · 0 · 1** ⟹ N 을 172 로 두든 그 구간 어디로 두든 **같은 분류**다.
```

★ **게이트와 자를 가른다**(판정문 문언) — 이 채널은 **편입 규약**(제공 여부)이고 **게이트가 아니다**.
`s4Gate.ts` 는 **한 글자도 고치지 않았다**(`git status` 무출력).

## §1-② 재발행 — 제공 **29칸** · 강등 4칸은 보류로 남긴다

산출 스크립트 신설 `scripts/v4Provide.ts`(편입 산출 «한정» · 게이트는 읽기만).

| 등급 | v4-47/50 | **v4-52** |
|---|---|---|
| 편입(= 제공) | 33 | **29** |
| 보류 | 63 | **67** |
| 착용불가 | 12 | **12** |
| 합 | 108 | **108** |

**강등 4칸**(색인에 **보류 + 사유(값)** 로 남겼다 · 삭제 0)

| 칸 | 어깨선 위 정점 | 색인 사유 |
|---|---|---|
| `c87.5-h155-s40_L` | **1** | 「어깨 걸림 미달 — 어깨선 위 옷 정점 1개 < 172(v4-52 §1-① · v4-30 계기 정의)」 |
| `c87.5-h155-s40_XL` | **0** | 같은 문언(0개) |
| `c87.5-h155-s45_M` | **0** | 같은 문언(0개) |
| `c87.5-h155-s45_XL` | **0** | 같은 문언(0개) |

```
 ★ 강등 칸의 `sha` 는 지웠다(T포즈 규약 동형 — 보류 칸에는 sha 를 두지 않는다) ·
   blob 파일은 **지우지 않았다**(바이트 변경 0).
 ★ 편입 후보 전 칸에 **`shoulderAbove` 값을 기록**했다(사실 병기 · 판정 근거가 색인에 남는다).
 ★ sha 규약(v4-50) 유지 — 제공 29칸 **전부** 「blob 헤더 제외 본문 sha256 = 색인 sha」 **일치(29/29 · 불일치 0)**.
```

## §1-③ 확인 — 작은 몸에서 L 폴백이 사라진다(코드 사실)

정본 2종을 그대로 읽어 **제품과 같은 함수**(`src/v3/provide.ts`)로 재현했다(화면 판정 0 · 코드 수정 0).

| 입력(가슴·키·어깨) | 매칭 몸 | 착지 | 버튼 |
|---|---|---|---|
| **88 · 155 · 40** | `c87.5-h155-s40` | **`{size: null, fallback: false}`** | — |
| 100 · 170 · 45 | `c100-h170-s40` | `{size: "S", fallback: true}` | 활성(「입어본 결과 있음」) |
| 122 · 185 · 50 | `c122.5-h185-s40` | `{size: "M", fallback: false}` | 활성 |

`landing.size === null` 이면 화면은 **`V3ProductV1.tsx:287-288`** 의
「**이 몸에 맞는 준비된 사이즈가 아직 없습니다**」를 띄우고 씬을 세우지 않는다 ⟹
**작은 몸의 L 폴백(오프숄더)이 사라지고 큰 몸과 같은 정직한 처리**가 된다(판정문 문언 그대로).
★ 그 몸의 제공 칸은 **0개**다(`c87.5-h155-s40_*` · `c87.5-h155-s45_*` 모두).
★ `?tpose=1` 구판은 **불변**(구판 자산 쓰기 0 · 코드 0).


---

## §2 자기검사

| 항 | 값 |
|---|---|
| `src/` diff vs 기점 `858b20a` | **0**(무출력) — 제품 로더·매칭·분류 코드 **0** |
| `src/v3/s4Gate.ts` | **불변**(`git status` 무출력) — **게이트 5채널·문턱 0**(§0-2) |
| `gpu/engine`·워커 | **diff 0** · 굽기 **0** · 정본 blob 바이트 **0** |
| 이 판이 **신설**한 것 | `scripts/v4Provide.ts`(편입 산출 · 게이트는 읽기만) |
| 자산 갱신 | `v4-a35/v1-provide.json`(29칸) · `v4-a35/index-merged-108.json`(강등 4칸 보류 + `shoulderAbove` 기록) · **2호기 동기화** |
| sha 규약(v4-50) | 제공 29칸 **29/29 일치 · 불일치 0** |
| v3 구판 자산·경로 | **불변**(쓰기 0) |
| `npx tsc -b` | **통과**(종료 0) |
| pytest(2호기) | **40 passed · 2 xfailed · 1 xpassed**(59.93 s) |
| 화면 판정 | **0**(승혁 확인 스모크 대기) |
| 브랜치 인쇄 | §0 머리 · §0 은 집행 **«전»** 커밋(`58a23f8`) · **기점 사실 병기**(프롬프트 `40d5871` = v4-51 캡처 커밋 · 실제 HEAD `858b20a` 에서 뗐다) |
| ecc 에이전트 / 스킬 | **0 / 0** |

## §4 회차 갈래 — **A**(①~③ ⟹ 승혁 확인 스모크)

**넘기는 것 6건**

1. ★★★ **편입 규약이 둘로 늘었다** — 게이트 `pass` **그리고** 「어깨선 위 옷 정점 ≥ **172**」.
   두 자를 가른다(굽기 품질 ↔ 화면 채널) · **게이트는 한 글자도 안 바뀌었다**.
2. ★★★ **N = 172 는 빈 구간의 중점** — (1 + 343) / 2 · 어느 칸의 값도 아니다.
   재산출에서도 같은 자리 확인(편입 최소 **343** ↔ 강등 **0·0·0·1**) ⟹ 구간 안 어디로 둬도 **같은 분류**.
3. ★★★ **제공 33 → 29** · 보류 63 → **67** · 착용불가 12(불변) · 합 108.
4. ★★ **강등 4칸은 사유와 값을 달고 색인에 남는다**(삭제 0 · blob 바이트 0) ·
   편입 후보 전 칸에 `shoulderAbove` 기록 ⟹ 판정 근거가 자산 안에 남는다.
5. ★★ **작은 몸의 L 폴백이 사라진다** — 88/155/40 → `c87.5-h155-s40` → **착지 `null`** ⟹
   화면은 「이 몸에 맞는 준비된 사이즈가 아직 없습니다」(`V3ProductV1.tsx:287-288`) · **씬 미표시**.
6. ★ **sha 규약 유지** — 제공 29칸 전부 색인 sha 와 일치(29/29) · `?tpose=1` 구판 불변.

**이 판이 하지 않은 판단** — 화면(승혁) · 보류 67칸의 품질 축 · UX 2건 구현 · 배포.


---

## 부록 — raw 링크 (커밋 `260f12e` 고정)

**신규** — `docs/v4/52-어깨채널.md` · `docs/v4/보고/52.md` · `scripts/v4Provide.ts`(편입 산출)
**수정** — `.obsidian-log/전략세션-인수인계.md` · `.obsidian-log/00-현재상태.md` ·
`.obsidian-log/v4-노트-색인.md` · `docs/metrics-log.md` ·
자산 `public/v3diag/v4-a35/v1-provide.json`(29칸) · `index-merged-108.json`(강등 4칸 · `shoulderAbove` 기록)
— 자산은 **git 밖 로컬**이고 에어·2호기 양쪽에 동기화돼 있다.

- https://raw.githubusercontent.com/banshk445/fit-simulator/260f12e/docs/v4/%EB%B3%B4%EA%B3%A0/52.md
- https://raw.githubusercontent.com/banshk445/fit-simulator/260f12e/docs/v4/52-%EC%96%B4%EA%B9%A8%EC%B1%84%EB%84%90.md
- https://raw.githubusercontent.com/banshk445/fit-simulator/260f12e/docs/metrics-log.md
- https://raw.githubusercontent.com/banshk445/fit-simulator/260f12e/.obsidian-log/%EC%A0%84%EB%9E%B5%EC%84%B8%EC%85%98-%EC%9D%B8%EC%88%98%EC%9D%B8%EA%B3%84.md
- https://raw.githubusercontent.com/banshk445/fit-simulator/260f12e/.obsidian-log/00-%ED%98%84%EC%9E%AC%EC%83%81%ED%83%9C.md
