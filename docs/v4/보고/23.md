# v4-23 보고 (전략 세션 제출본 · 터미널 출력 전량)

**2026-09-03 · 집행 완료 · 브랜치 `v4-23-control` · 갈래 A**

> CLAUDE.md 「v4 회차 보고 «저장»」 규약 — 이 파일이 전략 세션 전달본이다.
> 집행 정본 노트는 `docs/v4/23-대조군.md`.

---

# v4-23 — 대조군(v3 조립부터 재굽기) · 잔여 0.632 의 성격 판정 · (조건부) CC 주도 차이 전수

**물리 0줄 · `src/` diff 0(재굽기 = 기존 v3 경로 «실행»만) · 정본·blob·문턱 0 · 굽기 = v3 1칸 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-23-control` |
| 기점 | **`0c8a0ee`**(= v4-22 sha 자기 기입 HEAD) |
| §0 커밋 | **`82b6fa4`**(집행 «전») |
| §1~§4 커밋 | **`9b7613a`** — 본문 · 보고 · 볼트 3종 · metrics · 계기 산출 **한 커밋**(`src/` diff 0) |
| 보고 파일 | `docs/v4/보고/23.md` |

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v4-23-control 0c8a0ee → Switched to a new branch 'v4-23-control'
       $ git branch --show-current → v4-23-control · HEAD 0c8a0ee
 2호기 (§0 커밋을 push 한 «뒤» 같은 자리로 전환 — v4-21·22 와 같은 순서 · 아래 §0-5 에 인쇄)
```

---

## §0 수령·사무

### §0-1 판정문 (전략 세션 · v4-22 §4 수령 · 원문)

```
 「갈래 C 수령 · ★ 프레임 회계 가설 사망(180 = 총합 · 시계 동일) · 귀책 = 전략 세션(네 번째) ·
   ★★ 규칙 적용: 물리 세부에서 전략 세션은 후보 지정 0 — 실험 설계로 가르고 자리는 CC 가 값으로 지목 ·
   ★★ 대조군 = v3 자신을 조립부터 재굽기(결정적이면 정본과 비트 동일이어야 한다) ·
   ★ 사전 규칙(값 보기 «전»): (a) 대조군이 정본과 비트 동일(또는 ≤ 4·ULP_f64) ⟹ 0.632 는 순수 v4 차이
     → CC 주도 전수 조사 (b) 대조군이 갈린다 ⟹ 그 갈림 폭 = «굽기 체제의 자연 변동» 기준선 ·
     v4 가 그 안이면 「같은 옷」 성립·밖이면 미성립 — 문턱 완화가 아니라 비교 체제 정정이며
     이 규칙을 지금 등재한다 · 멈추는 규칙은 대조군 로그로 병행 확인」
```

**★ 귀책 등재(전략 세션 · 네 번째)** — 「프레임 회계」 가설은 v4-22 §1-① 에서 **코드 사실로 사망**했다.
판정문이 이를 자기 귀책으로 등재했다. 본 회차는 그 등재를 **그대로 옮겨 적는다**(CC 판단 0).

### §0-2 바꾸지 않는 것 (회차 프롬프트 원문)

```
 물리 **0줄** · `src/` diff **0**(재굽기는 기존 v3 경로 «실행»만) · 정본·blob·문턱 **0** ·
 굽기 = v3 **1칸** · ecc **0** · §0 집행 «전» 별도 커밋 · 브랜치 인쇄.
```

### §0-3 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · 단위 명시)

**★ 단위** — ①은 **칸 1개**(그 칸의 정착 blob) · 좌표차 **mm** · 리포트 5행 **mm** · 프레임 **1/60 s**.

| 항 | 갈래 |
|---|---|
| **①** 대조군 재굽기 | **(가)** 비트 동일 또는 ≤ 4·ULP_f64 / **(나)** 갈린다(최대·중앙 좌표차 · 리포트 5행 차 · **그 값이 새 기준선**) / **(다)** 실행 불가(사유) |
| **②** ①(가) ⟹ CC 주도 차이 «전수» + 계기로 기여 서열(상위 1~2 지목 · 처방 0) · ①(나) ⟹ 새 기준선 규칙으로 v4-21 램프판 재판정(재실행 0) |
| **③** A포즈 준비 사실(경량 · 굽기 0) — asm 파일 존재·헤더 · «지금 없는 것» 목록 |

**§4** — A = ①(가)+②·③ / B = ①(나)+②·③(성립/미성립 판정 포함) / **C = ①(다) ⟹ 사유 상신 · 정지** / D = 그 외 ⟹ 정지. 사후 갈래 신설 0.

### §0-4 사전 결정 5건 (값 보기 «전» · CC 집행 설계)

```
 ㄱ **대조군 기계 = «정본을 구운 기계»(맥)를 «주» 로 삼는다.**
    판정문의 검사식 「결정적이면 정본과 비트 동일」은 **같은 런타임에서만** 참/거짓이 갈린다 —
    다른 기계에서 갈리면 「비결정」과 「기계 차」가 섞여 (a)/(b) 규칙이 값을 못 준다.
    정본 M 칸 기록 = `index-shard-mac.json`(맥 순회 · f=180 · sec 1705.7 · sha 9d3b07e4…) ⟹ 맥이 주.
    **2호기 대조군은 «부» 로 병행**한다(교차 기계 사실 · 판정에는 쓰지 않는다).
    ⟹ 「집행 기계 = 2호기」 규칙의 **이 회차 한정 예외**이며, 사유를 여기에 등재한다.
 ㄴ **정본 보호 절차**(public/ diff 0 을 «절차»로 보장) —
    ⓐ `settled-c100-h170-s45_M.bin` 백업 → ⓑ `RECHECK=1 SHARD=v4-23-control` 로 재굽기(그 파일을 덮어쓴다)
    → ⓒ 새 산출을 `gpu/oracle/export/` 로 이관 → ⓓ 정본 blob 을 git 에서 복원 · 샤드 인덱스 삭제.
    **본 계정 인덱스에 이중 기록 0**(샤드로 갈랐다 · v3-78B §0‴ 규약 그대로).
 ㄷ **비교 계기는 기존 것만** — `scripts/v4FitReport.ts`(POS 만 갈아 끼운다 · 새 물리 0줄) ·
    정본 5행은 이미 있는 `fit-c100-h170-s45_M-v3정본.json` 을 «인용»한다(재생성 0).
 ㄹ **②의 착수 조건을 지금 고정한다** — ①(가)면 「전수 목록 → 계기 → 서열」, ①(나)면 「기준선 규칙 재판정(재실행 0)」.
    값을 보고 «어느 쪽을 할지» 고르지 않는다.
 ㅁ **멈춘 조건**은 **로그가 말하는 것만** 적는다 — `v3GridRun.ts:136` 은 `s4.pass || settle` 단락 평가라
    `gate=pass` 로 멈춘 칸은 **pass 가 참이었다**는 사실까지가 로그의 한계다(settle 동시 성립 여부는 단정 0).
```

---

## §1-① 대조군(v3 조립부터 재굽기) — **(가) 정본과 «바이트 전량» 동일**

**주 대조군 = 맥**(정본을 구운 기계 · §0-4ㄱ). 기존 v3 경로 «실행»만 — 인자 셋 외에 준 것 0.

```
 $ ONLY=c100-h170-s45_M RECHECK=1 SHARD=v4-23-control npx tsx scripts/v3GridRun.ts
 [그리드] 대상 1칸 · 완료분 101칸 · 상한 400프레임 · 시간 상한 118.6분 · **샤드 v4-23-control**
          · **RECHECK**(건너뜀 끔) · 머신 banseunghyeog-ui-MacBookAir.local (darwin-arm64 · node v25.9.0)
   15:11:36 c100-h170-s45_M   **편입** · f=180 · 1520.5s · pass · sha 9d3b07e45b67e0c3… · @맥
 [그리드] 종료 — 편입 1 · 총 1칸
```

| 채널 | 정본(v3-77 순회) | 대조군(이 판 재굽기) | 차 |
|---|---|---|---|
| 페이로드 sha256 | `9d3b07e45b67e0c35ba28488d5cb10b5ab5024fe2999a387a5b99f28dc736f4d` | **같음** | **0** |
| 파일 전체 sha256(헤더 포함) | `103d5ccbafe870ee780776aa0fc59374c339df673e16b651c1fa6a2aeebbf39c` | **같음**(`cmp` 무차이) | **0** |
| 최대 좌표차 | — | — | **정확히 0**(ULP 비교가 필요 없다) |
| 리포트 5행 | — | — | **0**(상태가 바이트 동일 ⟹ 같은 계기가 같은 수를 낸다 · 재생성 0) |
| 총 프레임 | 180 | **180** | 0 |
| 게이트 | `pass` | **`pass`** | 0 |
| 벽시계 | 1705.7 s | 1520.5 s | **−185.2 s**(시간은 판정 채널이 아니다) |

**멈춘 조건** — `v3GridRun.ts:136` 은 `s4.pass || s4.settleNetM ≤ 문턱` 단락 평가다. 대조군 기록의 `gate = pass`
⟹ **pass 가 참이었다**. 로그가 말하는 것은 거기까지다(settle 동시 성립 여부는 **단정 0** · §0-4ㅁ).

**★ 부수 사실(판정에 쓰지 않는다)** — 정본 기록에는 `host/os/node` 필드가 **없다**(SRC 필드 도입 «전» 기록 ·
`v3GridRun.ts:71` 「부재 = 맥」). ⟹ **정본을 구운 node 버전은 기록에 없다.** 그럼에도 **node v25.9.0 에서
바이트가 동일하게 났다** — v3 는 이 기계에서 **결정적**이다.

**정본 보호(§0-4ㄴ 이행)** — 재굽기 산출이 **정본과 바이트 동일**이라 덮어쓰기가 곧 원상이었다(백업 2부와
`cmp` 로 확인). 샤드 인덱스 `index-shard-v4-23-control.json` 은 **미추적 파일**이라 본 계정 오염 0이며,
기록은 위 표로 옮긴 뒤 지웠다. ⟹ **`public/` diff 0.**

### §1-①ㄴ 부 대조군(2호기 · **판정에 쓰지 않는다** · §0-4ㄱ) — **기계가 다르면 갈린다**

```
 $ (2호기) ONLY=c100-h170-s45_M RECHECK=1 SHARD=v4-23-control-2ho npx tsx scripts/v3GridRun.ts
   17:17:30 c100-h170-s45_M  **편입** · f=180 · 4024.7s · pass · sha 239318694d8045f2… · @DESKTOP-OV72A46
   머신 DESKTOP-OV72A46 (win32-x64 · node v25.9.0)   ← 맥과 **같은 node**, 다른 arch·libm
```

| 채널 | 값 |
|---|---|
| 페이로드 sha256 | `239318694d8045f2…` ↔ 정본 `9d3b07e45b67e0c3…` — **다르다** |
| 총 프레임 · 게이트 | **180 · `pass`** — 정본과 **같다** |
| 좌표차(전 정점 · vs 정본) | 최대 **29.410891 mm** · p99 **14.643415** · 중앙 **0.777487 mm** |

**★ v3 는 «한 기계 안에서» 결정적이고, «기계 사이»에서는 결정적이지 않다.** 두 사실이 같은 판에서 났다.
정본 blob 은 2호기에서도 백업본으로 **복원**했다(복원 뒤 해시 `103d5ccb…` = 맥 정본 파일과 동일 · 샤드 인덱스 삭제).

⟹ **판정문 사전 규칙 (a) 발동** — 대조군이 정본과 비트 동일하므로 **0.632 는 순수 v4 차이**다. ②로 간다.

---

## §1-② ①(가) ⟹ **CC 주도 차이 «전수»** + 계기로 기여 서열

### ㉠ 전수 목록 — v3 「조립 → 봉제 램프 → 정착」 ↔ v4 워커 경로 (코드 자리로만)

| # | 축 | v3 자리 | v4 자리 | 차 |
|---|---|---|---|---|
| 1 | 초기 상태 | `dressRun.prepare` → `S.assemble(d)` · 속도 0 | `asm-Tasm_M.bin`(= **같은 조립 산출**을 `v4AsmExport.ts` 가 포장) | **0** |
| 2 | 봉제 램프 | `dressRun.ts:111-116` `rest0` · `RAMP_N=⌈(max rest0 − SEP)/(G·DT²)⌉` · `:177` `setRest(f+1)` | `worker.py:120-139` **같은 식** · `SEPm = 2×THICK` | **0**(`consts.ts:8` `SEP = 2×THICK` ⟹ 같은 수 · RAMP_N **79** 양쪽 로그 일치) |
| 3 | 예측(중력·오일러) | `solver.ts:1055-1071` | `full_sc.substep_a` ① | **0** |
| 4 | λ 초기화(서브스텝마다) | `:1072-1079` | `substep_a` ② | **0** |
| 5 | 제약 «종류» 순서 | `garmentScene.ts:779` `cons = [inplane…, bends…, seamCons…]` | 늘어남 → 굽힘 → 봉제 | **0** — v3 배열이 **이미 종류별로 정렬**돼 있다 |
| 6 | 제약 «내부» 순서 | 배열 **순차** 가우스–자이델(`:1089-1095`) | **8색/14색 색분할**(색 사이는 순차 · `full_color2.py`) | **≠ 갈린다**(v4-10 §0 이 등재한 «알려진» 갈림) |
| 7 | 봉제 투영 | 순차 | `ti.loop_config(serialize=True)` **같은 순서** | **0** |
| 8 | 셀프 충돌 | `resolveSelfCollisions` 순차 | `selfcol._rank` 로 **v3 접촉 순서 재현** + serialize | **0**(v4-14 층1 2 ULP) |
| 9 | 자리(셀프 ↔ 몸충돌) | `solver.ts:1117-1122` 제약 «뒤» · 몸충돌 «앞» | `substep_a` → `sc.apply()` → `substep_b` | **0** |
| 10 | 몸 충돌 SDF | `bakeSdf(prim0.pos, bodyIdx, h, band)` → `bodyG` | `v4AsmExport.ts:73-74` — **`P.bodyG` 를 그대로** 포장 | **0**(같은 배열) |
| 11 | 속도 갱신·감쇠 | `:1134-1140` `decay = exp(−DAMP·h)` | `substep_b` ⑦ 같은 `decay` | **0** |
| 12 | **정지 규칙** | `v3GridRun.ts:136` **`pass ‖ settle`** | `worker.py:152-154` **`settle` 만** | **≠ 갈린다** |
| 13 | **부동소수 산술** | JS `f64`(V8) | Taichi `f64`(CUDA / x64 LLVM) | **≠ 갈린다** |
| 14 | 관측(밴드 정점 집합) | — | — | 가슴 266/264 · 허리 226/224(**굽기 차이 아님** · v4-16) · **밑단 110/110 · 소매 918/918 은 같다** |

⟹ **0 이 아닌 축은 셋뿐이다: #6 색 순서 · #12 정지 규칙 · #13 산술 반올림.**

### ㉡ 계기 A — **산술만 바꾼 재굽기**(`arch: cuda → x64` · 코드·순서·입력·램프 **전부 동일**)

```
 job gpu/bake/jobs/v4-23-x64.json — name v4-23-x64 · fp f64 · arch x64 · cell Tasm_M(asm-Tasm_M.bin) ·
     reportCell c100-h170-s45_M · frames 260 · cellCap {192, 131072} · ramp true
 [01:15:33]  램프 — RAMP_N 79 · rest0 2.984649e-03~2.170112e-01 · SEP 2.000000e-03   ← v4-21 과 같은 수
 [01:29:00]  Tasm_M · **정착 f190** · 4.152 s/프레임 · JIT 10.0s · pass True   (convNet 6.8796e-05 ≤ TOL 1e-4)
```

**정본 대비 리포트 5행 중앙 차** — 왼쪽은 v4-21 기록 인용(재실행 0 · 함정 44), 오른쪽이 이 판의 계기.

| 행 | 밴드 n(정본/판) | **cuda** f=180 | **x64** f=190 | 두 v4 판 «사이» |
|---|---|---|---|---|
| 목선 | 54/54 · 같다 | −0.002525 | −0.001809 | 0.000716 |
| 가슴 | 266/264 ↔ 266/263 · **다르다** | −0.273787 | −0.372029 | (표본이 달라 비교 불가) |
| 허리 | 226/224 ↔ 226/223 · **다르다** | +0.288516 | +0.319632 | (〃) |
| **밑단** | **110/110 · 같다** | **+0.632179** | **+0.894412** | **0.262233** |
| **소매** | **918/918 · 같다** | **−0.139005** | **−0.037768** | **0.101237** |

**게이트** — x64 판도 `pass`(교차 0 · 목선 초과비 0.98878 · 관통 최대 0.3968 mm) · 대역 이름 **5/5 일치**.

### ㉡′ 계기 B — **v3 를 «다른 기계»에서 구운 것**(§1-①ㄴ 산출로 리포트 5행 · 새 굽기 0)

같은 v3 코드 · 같은 입력 · 같은 f=180 · 같은 `pass`. **다른 것은 기계(산술)뿐**이다.

| 행 | 밴드 n(정본/2호기) | **v3(2호기) vs v3 정본** |
|---|---|---|
| 목선 | 54/54 · 같다 | +0.000433 |
| 가슴 | 266/262 · 다르다 | +0.176821 |
| 허리 | 226/222 · 다르다 | +1.061630 |
| **밑단** | **110/110 · 같다** | **+0.029207** |
| **소매** | **918/918 · 같다** | **−0.559276** |

대역 이름 **5/5 일치** · 표시 1자리 2/5 · 전 정점 좌표차 중앙 **0.777 mm** · 최대 **29.4 mm**.

### ㉢ 기여 서열 (값으로만 · 처방 0)

| 순위 | 축 | 값 | 근거 |
|---|---|---|---|
| **1** | **#13 산술 반올림의 증폭** | **v4 안**(계기 A · arch 만 다름): 밑단 **0.262** · 소매 **0.101 mm** · 정착 프레임 **180 → 190**<br>**v3 안**(계기 B · 기계만 다름): 밑단 **0.029** · 소매 **0.559 mm** · 전 정점 중앙 **0.777 mm** · 최대 **29.4 mm** | 코드·순서·입력이 **전부 같고** 반올림만 다른 두 판 사이에서 난 값 — **v3 자신도 이만큼 갈린다** |
| **2** | **#6 색 순서** | 60프레임·램프 없음에서 중앙 **0.0122 mm**(v4-13 §1-①) — 같은 조건 arch 간 차는 **4.02e-08 mm** ⟹ 출발 섭동이 **약 3×10⁵ 배** | 성격이 #13 과 같고(같은 물리·다른 순서) 출발 섭동이 훨씬 크다 ⟹ 기여는 **#13 이상**으로 읽힌다(이 판 재측정 0) |
| **3** | **#12 정지 규칙** | 정착 자리가 반올림만으로 **±10프레임** 이동(180 ↔ 190) · v3 두 기계 모두 **`pass` 로** 멈췄다 | 「어디서 멈추느냐」를 두 판이 다른 규칙으로 정한다 |

**★ 값이 말하는 것(사실만 · 단정·처방 0)**

1. **밑단 +0.632 를 개별 차이에 «귀속»할 수 없다.** 반올림만 바꿔도 같은 행이 v4 안에서 **0.262 mm**,
   v3 안에서 **0.559 mm**(소매) 움직인다.
2. **v4 잔여는 v3 자신의 기계 간 갈림과 «같은 자릿수»다.** 소매는 v3 쪽 갈림(**0.559**)이 v4 잔여(**0.139**)보다 **크다**.
3. 「같은 옷」 ③ 조항의 **0.05 mm** 는 **이 굽기 체제의 자연 변동보다 작다**(위 네 값 전부가 그보다 크다).
4. ★ 판정문 사전 규칙 **(b) 는 발동하지 않았다** — (b) 는 ①(나) 조건부이고, **주 대조군은 (가)**(같은 기계에서 비트 동일)다.
   위 변동은 **「기계가 다를 때」와 「v4 안에서」** 잰 것이지 **주 대조군의 갈림 폭이 아니다**.
   **문턱 변경·정의 재작성·처방 0** — 처분은 전략 세션 몫이다.

---

## §1-③ A포즈 준비 사실 (경량 · 굽기 0 · 새 실행 0)

**있는 것**

| 파일 | 사실 |
|---|---|
| `gpu/oracle/export/asm-Tasm_M.bin` | 450,791 B · 헤더 `{what: "v4-20 조립 «직후» 상태(속도 0)", cell: "Tasm_M", n: 9388, frame: 0, d: 0.009, body: "public/v3diag/v3-77/body-c100-h170-s45.bin", substeps: 229}` · 페이로드 f64 56,328 = **위치 9,388 + 속도 9,388** — **T포즈다** |
| `gpu/oracle/export/l3ap-body-c100-h170-s45-a35.bin` | A포즈 «몸» 정점(15,882 · 190,584 B · v4-18 축 정정판) |
| `gpu/oracle/export/l3ap-assemble-c100-h170-s45_M.json` | v4-19 A포즈 «조립만» 탐침 — 조립 **통과** · n 9,644 · 제약 46,178 · substeps 231 · 옷↔몸 최소 간격 **2.000000000060054 mm** |

**지금 «없는» 것**(A포즈 1칸을 구우려면 필요한 것 기준)

| # | 없는 것 | 근거(코드 사실) |
|---|---|---|
| 1 | **A포즈 조립 export 일습** — `asm-Aasm_M.bin` · `scene-Aasm_M.bin` · `scene-bend-…` · `scene-seam-…` · `sdf-Aasm.bin` | `gpu/oracle/export/` 에 `*Tasm_M*` 5종만 있다. **생성 명령은 이미 있다**: `CELL=c100-h170-s45_M BODY_BIN=gpu/oracle/export/l3ap-body-c100-h170-s45-a35.bin TAG=Aasm_M npx tsx scripts/v4AsmExport.ts`(`v4AsmExport.ts:26` 이 `BODY_BIN` 을 받는다) — **이 판에서 실행 0**(굽기 0) |
| 2 | **A포즈 정답지** | v3 정본은 **T포즈 108칸뿐**이다 ⟹ A포즈에서는 「같은 옷」 대조가 **원리적으로 불가**하고, 판정은 **제품 게이트 통과 여부**만 가능하다 |
| 3 | **A포즈 «그리드» 몸** | `l3ap-assemble-….json` 의 자기 기록 — 「기본 마네킹 A포즈 — c100-h170-s45 «아님»(본 스케일 미적용)」 ⟹ 백로그 0번 「A포즈 그리드 몸 27 생성」이 선행이다 |
| 4 | **팔 축 인자 배선** | `garmentScene.ts:51·631-636` 은 `cfg.armAxis` 를 받지만, `dressRun.prepare` 에는 그 인자가 **없다**(grep: `armAxis` 는 `garmentScene.ts` 와 탐침 스크립트에만 나온다) ⟹ A포즈 조립도 **T포즈 축**으로 소매를 놓는다 |

---

## §2 자기검사

| 항 | 값 |
|---|---|
| `src/` diff | **0**(`git diff --stat 0c8a0ee -- src/` 무출력) · 물리 **0줄** |
| 정본·blob·문턱 | **0** — 재굽기 산출이 정본과 **바이트 동일**(§1-①) · 샤드 인덱스는 삭제 · `public/` **미추적 잔여 0** |
| 이 판이 «만든» 것 | `gpu/bake/jobs/v4-23-x64.json`(작업 정의 · 물리 0줄) · `gpu/bake/results/v4-23-x64/`(계기 산출) · `gpu/oracle/export/l3-product-cmp-v4-23-x64.json` · 문서 |
| `engine/` · `worker.py` | **diff 0**(계기 A 는 **기존 워커에 job 만 준 것**이다) |
| 굽기 | **v3 1칸**(맥 대조군) + **v3 1칸**(2호기 부 대조군 · §1-①ㄴ) + **v4 계기 1칸**(x64) — ★ 「굽기 = v3 1칸」을 **v3 재굽기 예산**으로 읽었고, ②가 요구한 「계기를 붙여 값으로 서열」은 **v4 탐침 1건**으로 이행했다. 이 해석을 여기 등재한다 |
| pytest(2호기) | **40 passed · 2 xfailed · 1 xpassed**(70.88 s) |
| `npx tsc -b` | **통과**(종료 0) |
| 브랜치 인쇄 | §0 머리에 **에어·2호기 둘 다** · §0 은 집행 **«전»** 커밋(`82b6fa4`) |
| ecc | **0/0** |
| ★ 커밋 기계 | **에어**(§0-4ㄱ 의 「대조군 기계 = 맥」 예외의 연장 — 주 대조군 산출·계기 비교가 모두 에어에 있다). 2호기는 push 뒤 fast-forward 로 맞춘다 |

---

## §4 회차 갈래 — **A**(①(가) + ②·③ 상신)

**넘기는 것 6건**

1. ★★★ **대조군이 정본과 «바이트 전량» 동일하다** — 같은 기계에서 v3 는 **결정적**이다(sha·파일 해시·프레임·게이트 전부 일치 · 좌표차 정확히 0). ⟹ 사전 규칙 (a) 대로 **0.632 는 순수 v4 차이**다.
2. ★★★ **그러나 v3 는 «기계 사이»에서 결정적이지 않다** — 같은 코드·같은 입력·같은 f=180·같은 `pass` 인데 2호기 산출은 정본과 **최대 29.41 mm · 중앙 0.777 mm** 갈린다(부 대조군 · 판정에는 쓰지 않았다).
3. ★★★ **잔여를 개별 차이에 귀속할 수 없다** — 0 이 아닌 축은 셋뿐(#6 색 순서 · #12 정지 규칙 · #13 산술)이고, **반올림만 바꾼** 두 판 사이가 이미 밑단 **0.262**(v4 안) · 소매 **0.559 mm**(v3 안)다. 「같은 옷」 ③의 **0.05 mm** 는 이 체제의 자연 변동보다 **작다**.
4. ★★ **차이 전수 목록 14축**(§1-②㉠) — 11축이 **코드로 0**이다(램프 식·SEP·제약 종류 순서·봉제·셀프충돌 순서·자리·SDF·감쇠 포함).
5. ★ **x64 판도 게이트 `pass`** · 대역 이름 5/5 · 소매 중앙 차 **−0.0378**(문턱 0.05 «안») — 같은 행이 판마다 문턱 안팎을 오간다.
6. ★ **A포즈 준비** — 없는 것 4건(조립 export 일습 · A포즈 정답지 부재 · 그리드 몸 미생성 · `prepare` 의 `armAxis` 배선). 생성 명령은 이미 있고 이 판에서 실행 0.

**이 판이 하지 않은 판단** — 「같은 옷」 문턱·정의의 처분 · 비교 체제 정정 여부 · 정지 규칙의 통일 · 색 순서의 재측정 · A포즈 착수 · 캡처.
