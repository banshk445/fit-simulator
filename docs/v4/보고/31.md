# v4-31 보고 (전략 세션 제출본 · 터미널 출력 전량)

**2026-09-05 · 집행 완료 · 브랜치 `v4-31-sleeve-toffset` · 갈래 C(기각)**

> CLAUDE.md 「v4 회차 보고 «저장»」 규약 — 이 파일이 전략 세션 전달본이다.
> 집행 정본 노트는 `docs/v4/31-t오프셋.md`. **캡처 0**(③④ 착수 0).

---

# v4-31 — 소매 축 구간 오프셋 사실 · 가상 조립 실험 · (성립 시) t 원점 정합 처방 + 재탐침

**물리 0줄 · `src/` diff = ③ 성립 시 `axPoint` **t 오프셋 한정** · 굽기 = ③ 성립 시 1칸 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-31-sleeve-toffset` |
| 기점 | **`5626cae`**(= v4-30 §1~§4) |
| §0 커밋 | **`fb61d93`**(집행 «전») |
| §1~§4 커밋 | **`cd0561c`** — 본문 · 보고 · 볼트 3종 · metrics · 계기 1종 **한 커밋**( diff 0) |
| 보고 파일 | `docs/v4/보고/31.md` |

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v4-31-sleeve-toffset 5626cae → Switched to a new branch 'v4-31-sleeve-toffset'
       $ git branch --show-current → v4-31-sleeve-toffset · HEAD 5626cae
 2호기 (§0 커밋을 push 한 «뒤» 같은 자리로 전환 — §0-5 에 인쇄)
```

---

## §0 수령·사무

### §0-1 판정문 (전략 세션 · v4-30 §4 + 화면 수령 · 원문)

```
 「갈래 A 수령 · ★★★ ①(나) 수용 + 재해석: 램프는 무죄 —
   T포즈에서 몸판을 끌어올리던 그 힘이 A포즈에선 «아래에 있는 소매» 쪽으로 끌었다 ·
   f0 화면 = 소매가 몸판에서 분리된 채 팔꿈치 부근(암홀과 뻥 뚫림) —
   수(첫 프레임 −62mm · T포즈 +59.7mm 상승)와 정합 ·
   ★★ 산술 사실 등재: SLV_X0(0.215) 는 «중심선에서 잰 길이»인데 v4-29 axPoint 는 원점을 P 로 옮기며
     t 를 그대로 썼다 — P 는 중심선에서 축 방향으로 d·AX = 156.514mm 나간 점 ⟹ 소매가 ~156mm 팔 아래 배치 ·
     함정 37 «세 번째»(t 가 두 원점을 오감) · 귀책 = 전략 세션(v4-29 문언이 t 원점 미지정 · 7건째) ·
   ★ 확정은 실험이(전례 3단: 산술→가상→처방) · 좌우 대칭·계기 비트 불변 수용」
```

**★ 귀책 등재(전략 세션 · 일곱째)** — v4-29 의 승인 문언이 **`t` 의 원점을 지정하지 않았다**.
`axPoint` 의 원점만 P 로 옮기고 `t = SLV_X0 + (CAP_H − py)`(중심선 기준 길이)를 그대로 두어
소매가 축 방향으로 **dAX 만큼** 밀려났다. 판정문이 이를 자기 귀책으로 등재했고, 본 회차는 **그대로 옮겨 적는다**.

### §0-2 예외 3건째 «재정정» 승인 문언 (③ 성립 시에만 · 원문)

```
 「전달 분기 axPoint 의 축 매개변수를 중심선 기준으로 정합한다:
   axPoint(t) = P + (t − dAX)·AX + R(…) · dAX = (P − C)·AX (몸에서 계산 · 손 상수 0) ·
   또는 동치 형태(원점을 축선 위 중심선 최근점으로) — 형태는 CC 가 고르고 동치 증명 한 줄 ·
   sgn·미전달 분기·axDot 불변 · 물리 0줄 · T포즈 82칸 비트 동일」
```

### §0-3 바꾸지 않는 것 (회차 프롬프트 원문)

```
 물리 **0줄** · `dressRun`·`fitSleeve` 로직 **0** · 정본·blob·문턱 **0** ·
 워커 **diff 0** · 격자 용량 **262144** · 계기(v4-28·30) **유지**.
```

### §0-4 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · 단위 명시)

**★ 단위** — ①은 **mm** · ②는 **봉제쌍**(어깨 이음) · ③은 **칸**(T포즈 82) · ④는 **칸 1개**.

| 항 | 갈래 |
|---|---|
| **①** 오프셋 실측 | **(가)** 어긋남 ≈ dAX 자릿수 / **(나)** 다름(값) |
| **②** 가상 조립 실험 | **(가)** 어깨 이음 봉제쌍 거리가 현행 대비 **자릿수로 닫힌다** / **(나)** 잔존 ⟹ **기각 등재 · ③④ 착수 0** |
| **③** 처방 | **(가)** 82/82 + ② 재현 / **(나)** 이탈(값) / **(다)** 회귀 불일치 ⟹ 즉시 복원 |
| **④** A포즈 재탐침 + 캡처 | **(가)** 게이트 `pass` / **(나)** fail(채널·값 · 처방 0) / **(다)** 발산·미정착 |

**§4** — A = ①(가)+②(가)+③(가)+④(가) / B = ④(나)(다) 또는 ③(나) / **C = ②(나) ⟹ 기각·착수 0** / D = ③(다) ⟹ 복원 / E = 그 외 ⟹ 정지.

### §0-5 사전 결정 5건 (값 보기 «전» · CC 집행 설계)

```
 ㄱ **형태는 «원점 이동»으로 고른다**(승인 문언의 둘째 형태) — `C₂ = P − dAX·AX` 를 두고
    `axPoint(t) = C₂ + t·AX + R(…)`. **동치 증명 한 줄**:
      `P + (t − dAX)·AX = (P − dAX·AX) + t·AX = C₂ + t·AX` (결합법칙 · 항 재배열뿐).
    ⟹ 첫째 형태와 **같은 점**이고, `C₂` 는 「축선 위에서 중심선(C)에 가장 가까운 점」이다
      (`C₂ = C + perp(P − C)` — 수직 성분만 남는다 · v4-29 가 고친 102.06 mm 는 그대로 유지된다).
 ㄴ **①의 «어긋남»을 지금 정의한다** — 조립 f0 상태에서
    `t(p) = (p − P)·AX` 를 소매 정점과 **암홀(몸판 쪽) 정점**에 각각 재고 **중앙값 차**를 어긋남으로 본다.
    기대는 **+dAX 자릿수**(소매가 팔 아래로 나가 있다).
 ㄷ **②는 조립만 한다**(굽기 0) — `t` 를 dAX 만큼 되돌린 관으로 **조립만** 돌려 f0 지표를 잰다:
    어깨 이음(암홀 4줄) 봉제쌍 거리 분포 · 소매 y중앙 · NaN 수 · 재탐색된 `SLV_R`.
 ㄹ **②(가)의 뜻을 지금 고정한다** — 「어깨 이음 봉제쌍 거리 **중앙값**이 현행의 **1/10 이하**」면 (가).
    (현행 값은 이 판에서 처음 재므로, 판정은 **같은 판 안의 두 수의 비**로만 한다.)
 ㅁ **③은 ②(가)일 때만**, ④는 ③(가)일 때만 착수한다. 갈래를 값 보고 고르지 않는다.
```

---

## §1-① 오프셋 실측 — **(가) 자릿수는 같다**(다만 **2.24배**)

조립 f0(현행 v4-29 배치) 위에서 `t(p) = (p − P)·AX` 로 쟀다(왼쪽은 x 를 거울로 본다 · `sgn` 규약과 같은 쪽).

| 채널 | 값 |
|---|---|
| **dAX = (P − C)·AX** | **156.514 mm**(판정문 산술과 일치) |
| 소매 정점 t 중앙 | **355.1933 mm** · 범위 **[215.0, 420.0]** = `[SLV_X0, SLV_X0+SLEN]` **그대로** |
| 암홀(몸판 쪽) t 중앙 | **4.3526 mm** · 범위 [−348.8976, 100.6923] |
| **어긋남**(소매 − 암홀) | **350.8406 mm** |
| 어긋남 ÷ dAX | **2.2416** |

⟹ 소매의 t 구간이 **패턴 값 그대로**(215~420 mm) 쓰이는 것이 값으로 확인됐고, 암홀은 t ≈ 0 근처에 있다.
**어긋남은 dAX 와 «같은 자릿수»(10² mm)이지만 그 2.24배**다 — dAX 하나로 설명되지 않는다.

---

## §1-② 가상 조립 실험 — **(나) 잔존**(중앙 비 **0.4185** > 1/10)

`t` 를 **dAX 만큼 되돌린**(= 소매 패널을 −dAX·AX 로 평행이동) 가상 배치에서 **같은 봉제쌍**을 다시 쟀다.
암홀 봉제쌍 **104쌍** · NaN **0**.

| 거리(mm) | 최소 | p25 | **중앙** | p75 | 최대 |
|---|---|---|---|---|---|
| **현행**(v4-29 배치) | 197.2746 | 206.6353 | **234.7710** | 261.8464 | 267.8725 |
| **가상**(t 되돌림 156.514 mm) | 54.4598 | 72.6476 | **98.2506** | 126.4229 | 141.9180 |

**중앙 비(가상/현행) = 0.4185** — §0-5ㄹ 이 값 보기 «전»에 고정한 (가) 기준(**1/10 이하**)에 **미달** ⟹ **(나)**.
소매 y중앙은 1.238413 → **1.323749 m** 로 올라가지만 암홀(몸판) y중앙 **1.354483 m** 에 **30.7 mm 못 미친다**.

### 참고 — 축 방향 이동량 훑기(값만 · 이 판의 «선택» 0)

| 이동 mm | 봉제쌍 거리 중앙 | 최소 | 최대 |
|---|---|---|---|
| 0(현행) | 234.7710 | 197.2746 | 267.8725 |
| 100 | 142.8915 | 101.5767 | 181.1594 |
| **156.514**(dAX) | **98.2506** | 54.4598 | 141.9180 |
| **200** | **75.6210**(최소) | **40.7104** | 122.2723 |
| 250 | 82.6725 | 67.9097 | 116.8639 |
| 300 | 114.0341 | 99.0059 | 131.7718 |
| 350.84(어긋남) | 155.2993 | 134.2626 | 167.1082 |
| 400 | 199.3369 | 175.2888 | 214.0897 |
| 450 | 241.9783 | 220.3402 | 263.0245 |

⟹ **어떤 축 방향 평행이동으로도 암홀이 닫히지 않는다** — 최선(200 mm)에서도 중앙 **75.6 mm**·최소 **40.7 mm** 다.
「t 원점 하나」로 설명되는 문제가 **아니다**(값 그대로 · 원인 단정 0 · 처방 0).

---

## §1-③④ — **착수 0**(§0-4 · §0-5ㅁ 대로)

②가 **(나)** 이므로 사전 등재 갈래에 따라 **③(처방)과 ④(재탐침·캡처)에 착수하지 않았다**.
`src/` **한 줄도 고치지 않았다** · 굽기 **0** · 캡처 **0**.

---

## §2 자기검사

| 항 | 값 |
|---|---|
| `src/` diff vs 기점 | **0**(③ 미착수) · 물리 **0줄** |
| `gpu/bake/worker.py` · `gpu/engine/` | **diff 0** · 정본·blob·문턱 **0** · 격자 용량 **262144 유지** · 계기(v4-28·30) **수정 0** |
| 이 판이 **신설**한 것 | `scripts/v4TOffsetProbe.ts`(계기 · 판정만) · 산출 `l3ap-toffset-v4-31-A.json` |
| 굽기 · 캡처 | **0 · 0** |
| pytest(2호기) | **40 passed · 2 xfailed · 1 xpassed**(58.27 s) |
| `npx tsc -b` | **통과**(종료 0) |
| 브랜치 인쇄 | §0 머리에 에어·2호기 둘 다 · §0 은 집행 **«전»** 커밋(`fb61d93`) |
| ecc 에이전트 / 스킬 | **0 / 0** |

---

## §4 회차 갈래 — **C**(②(나) ⟹ 기각 등재 · ③④ 착수 0)

**넘기는 것 6건**

1. ★★★ **「t 원점 하나」 가설은 값으로 기각됐다** — dAX 되돌림의 중앙 비 **0.4185**(기준 1/10),
   그리고 **어떤 축 방향 이동으로도** 최선이 중앙 **75.6210 mm**(이동 200 mm)다.
2. ★★★ **어긋남은 350.8406 mm 로 dAX(156.514)의 2.24배** — 남는 194 mm 는 축 방향 이동이 아닌 곳에서 온다.
3. ★★★ **소매 t 구간은 패턴 값 그대로다**(215.0~420.0 mm = `[SLV_X0, SLV_X0+SLEN]`) —
   즉 소매는 «어깨에서 팔 길이만큼» 나간 자리에 놓이는데, **암홀은 t ≈ 4.35 mm**(어깨 옆)에 있다.
4. ★★ **평행이동만으로는 y 도 못 맞춘다** — 소매 y중앙 1.238413 → 1.323749 ↔ 암홀 y중앙 **1.354483 m**(−30.7 mm).
5. ★★ **가설 기각은 이번이 처음이 아니다** — v4-27(반경) · v4-31(t 원점) 두 번은 «가상 실험»이 먼저 걸러
   코드를 고치지 않고 끝났다(굽기 0 · `src/` 0).
6. ★ **다음 질문은 «소매를 어디에 놓아야 하는가»** — t 구간·반지름·축 셋 중 무엇을 다시 볼지는 **전략 세션 몫**이다.

**이 판이 하지 않은 판단** — 처방 · 원인 귀속 · A포즈 굽기 · 화면.
