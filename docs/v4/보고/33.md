# v4-33 보고 (전략 세션 제출본 · 터미널 출력 전량)

**2026-09-05 · 집행 완료 · 브랜치 `v4-33-cap-kabsch` · 갈래 C(기각)**

> CLAUDE.md 「v4 회차 보고 «저장»」 규약 — 이 파일이 전략 세션 전달본이다.
> 집행 정본 노트는 `docs/v4/33-강체정합.md`. **캡처 0**(③④ 착수 0).

---

# v4-33 — 캡↔암홀 강체 정합(회전 포함 · 대응에서 유도) · 가상 실험 · (성립 시) 처방 + 재탐침

**물리 0줄 · `src/` diff = ③ 성립 시 «소매 강체 변환(R+Δ)» 한정 · 굽기 = ③ 성립 시 1칸 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-33-cap-kabsch` |
| 기점 | **`b4b6cba`**(= v4-32 §1~§4) |
| §0 커밋 | **`24eb44d`**(집행 «전») |
| §1~§4 커밋 | **`e274504`** — 본문 · 보고 · 볼트 3종 · metrics · 계기 1종 **한 커밋**( diff 0) |
| 보고 파일 | `docs/v4/보고/33.md` |

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v4-33-cap-kabsch b4b6cba → Switched to a new branch 'v4-33-cap-kabsch'
       $ git branch --show-current → v4-33-cap-kabsch · HEAD b4b6cba
 2호기 (§0 커밋을 push 한 «뒤» 같은 자리로 전환 — §0-5 에 인쇄)
```

---

## §0 수령·사무

### §0-1 판정문 (전략 세션 · v4-32 §4 수령 · 원문)

```
 「갈래 C 수령 · ★★ 기각의 정보: 평행이동(자유도 3)은 «닫으면 뚫고 안 뚫으면 안 닫힌다» —
   두 조건 동시 만족 해 없음 · 남은 자유도 = 회전 · 값이 가리킴 · 가상 실험 3번째 차단(src 0 · 굽기 0) ·
   ★★ 회전은 손 각도가 아니라 «대응에서 유도»한다: 봉제쌍 52(좌우 각각)의 소매 점집합을
     암홀 점집합에 앉히는 최소제곱 강체 변환(Kabsch · 교과서 절차 · 매개변수 0) —
     v4-32 의 Δ 는 회전 없는 특수해였다 ·
   ★ 판정 규칙 유지(거리 중앙 ≤ 217.0112 «그리고» 새 위반 0 · 후≤전) ·
   ★ 강체 정합으로도 뚫리면 = 이 암홀 위치·모양의 기하 불가 ⟹ 몸판 쪽 질문(암홀 자리)으로 —
     그 갈래 사전 등재」
```

### §0-2 예외 3건째 «최종 형태» 승인 문언 (③ 성립 시에만 · 원문)

```
 「전달 분기에서 소매 패널 배치 «후» 강체 변환(회전 R + 평행이동 Δ)을 적용한다:
   (R, Δ) = 어깨 이음 봉제쌍 대응(소매 쪽 → 암홀 쪽)의 최소제곱 강체 정합(Kabsch · 반사 금지 det(R)=+1) ·
   좌우 각각 · 조립 자신에서 계산(손 상수 0 · 손 각도 0) ·
   axDot·axPoint·fitSleeve·sgn 불변 · 미전달 분기 불변 · 물리 0줄 · T포즈 82칸 비트 동일」
```

### §0-3 바꾸지 않는 것 (회차 프롬프트 원문)

```
 물리 **0줄** · `dressRun`·`fitSleeve` 로직 **0** · 정본·blob·문턱 **0** ·
 워커 **diff 0** · 격자 용량 **262144** · 계기(v4-28·30·31·32) **유지**.
```

### §0-4 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · 단위 명시)

**★ 단위** — ①②는 **mm**(거리)·**도**(회전각) · ③은 **칸**(T포즈 82) · ④는 **칸 1개**.

| 항 | 갈래 |
|---|---|
| **①** 강체 정합 산출 | **(가)** 성립(det(R)=+1 · NaN 0) / **(나)** 불가(사유) |
| **②** 가상 실험 | **(가)** 거리 중앙 **≤ 217.0112** **그리고** 몸SDF 위반 **후 ≤ 전** / **(나)** 거리 미달(값) / **(다)** 위반 잔존(값·부위) — **(나)(다) 모두 기각 등재 · ③④ 착수 0 · 「암홀 자리」 질문을 다음 후보로 상신** |
| **③** 처방 | **(가)** 82/82 + ② 재현 / **(나)** 이탈(값) / **(다)** 회귀 불일치 ⟹ 즉시 복원 |
| **④** A포즈 재탐침 + 캡처 | **(가)** 게이트 `pass` / **(나)** fail(채널·값 · 처방 0) / **(다)** 발산·미정착 |

**§4** — A = ①(가)+②(가)+③(가)+④(가) / B = ④(나)(다) 또는 ③(나) / **C = ②(나)(다) ⟹ 기각·착수 0** / D = ③(다) ⟹ 복원 / E = 그 외 ⟹ 정지.

### §0-5 사전 결정 5건 (값 보기 «전» · CC 집행 설계)

```
 ㄱ **Kabsch 는 교과서 절차 그대로 쓴다**(매개변수 0) — 대응은 v4-32 와 «같은 봉제쌍»(좌우 각 52) ·
    소스 = 소매 쪽 점, 타깃 = 암홀 쪽 점 · 중심 이동 → 상관행렬 → **회전은 사원수 최대 고유벡터**(Horn 형태)로 뽑는다.
    ⟹ 사원수에서 나온 회전은 **항상 det = +1**(반사 불가) — 승인 문언의 「반사 금지」가 구성으로 보장된다.
    회전각·축은 그 사원수에서 그대로 읽어 등재한다(손 각도 0).
 ㄴ **판정 문턱은 v4-32 것을 그대로 쓴다** — 거리 중앙 **≤ 217.0112 mm**(T포즈 f0 최대) ·
    몸SDF 위반 **후 ≤ 전**(v4-32 §0-5ㄷ 정의). **새 문턱 0.**
 ㄷ **②에 «비봉제 최소 쌍거리»를 하나 더 본다** — 강체 변환은 소매를 몸판 쪽으로 당기므로
    옷↔옷 겹침이 생길 수 있다. 소매 정점 ↔ 몸판 정점(봉제쌍 제외) 최소 거리를 이동 전/후로 잰다(값만).
 ㄹ **③의 형태** — 배치(`place`) «뒤» 소매 패널 정점에 `p ← R·(p − s̄) + ā` 를 적용한다.
    `axDot`·`axPoint`·`fitSleeve`·`sgn` 은 한 글자도 건드리지 않는다. (R, Δ)는 그 조립 «안»에서 계산한다.
 ㅁ **③은 ②(가)일 때만**, ④는 ③(가)일 때만 착수한다. 갈래를 값 보고 고르지 않는다.
```

---

## §1-① 강체 정합 산출 — **(가) 성립**(det(R) = +1 · NaN 0)

대응은 v4-32 와 **같은 봉제쌍**(좌·우 각 **52**). 소스 = 소매 쪽, 타깃 = 암홀 쪽.

| 채널 | 오른쪽 R | 왼쪽 L |
|---|---|---|
| **회전각** | **28.0246°** | **28.0789°** |
| 회전축 | (−0.2180, −0.3252, −0.9202) | (−0.2161, +0.3254, +0.9205) |
| **det(R)** | **1.0000000000** | **1.0000000000** |
| **Δ (mm)** | (−739.9594, +382.5142, +22.9986) | (+740.8463, +383.4053, +21.7820) |
| **잔차 RMS** | **36.8539 mm** | **36.7715 mm** |

★ 좌우가 **부호만 뒤집힌 같은 변환**이다(각도 차 **0.0543°** · RMS 차 0.0824 mm).
★ 회전각 **28.02°** 는 A포즈 팔 내림(35°)과 **같은 자릿수**다 — 다만 **손 각도는 한 번도 쓰지 않았다**(대응에서 유도).
★ Δ 는 v4-32 의 회전 없는 특수해(축 −220.6 · 수직 65.4 mm)와 **다른 벡터**다 — 회전 중심이 다르기 때문이다.

---

## §1-② 가상 실험 — **(다) 위반 잔존**(거리는 통과 · 관통은 남는다)

| 거리(mm) | 최소 | p25 | **중앙** | p75 | 최대 |
|---|---|---|---|---|---|
| 현행 | 197.2746 | 206.6353 | **234.7710** | 261.8464 | 267.8725 |
| **강체 정합 후** | **11.4109** | 22.5786 | **31.6355** | 37.2595 | **76.9355** |
| (참고) v4-32 평행이동만 | 6.6145 | 33.5864 | 43.0171 | 53.4817 | 90.9742 |

* **중앙 비(후/전) = 0.13475** · **중앙 31.6355 ≤ 문턱 217.0112** ✅ — 평행이동만(43.0171)보다 **더 닫힌다**.
* **몸SDF 위반 정점 0 → 148** ❌(「후 ≤ 전」 거짓) — v4-32 의 **291 에서 절반으로 줄었지만 0 이 아니다**.
  깊이 최소 **0.5893** · p25 9.2444 · 중앙 **10.5054** · 최대 **10.5054 mm**.
* 비봉제 최소 쌍거리 **172.8786 → 2.1758 mm**(SEP 2.0 바로 위 — 옷↔옷 겹침은 아직 없다).
* 소매 y중앙 1.238413 → **1.277675 m** · NaN **0**.

⟹ 사전 등재 갈래의 (가)는 「거리 **그리고** 위반 후 ≤ 전」이다. 뒤가 깨졌다 ⟹ **(다)**.
**최소제곱 강체 변환(자유도 6)으로도 «닫으면서 안 뚫는» 자리가 없다.**

★ **계기 한 줄 유보** — 위반 깊이의 p75·최대가 **10.5054 mm 로 같은 값에 몰린다**(v4-32 도 같은 수).
`sampleSdf` 격자 밴드의 하한에 닿았을 가능성이 있다(**단정 0** · 이 판은 그 자리를 확인하지 않았다).

---

## §1-③④ — **착수 0**(사전 등재대로)

②가 **(다)** 이므로 처방(③)과 재탐침·캡처(④)에 착수하지 않았다.
`src/` **한 줄도 고치지 않았다** · 굽기 **0** · 캡처 **0**.
판정문이 미리 등재한 갈래대로 **「암홀 자리」(몸판 쪽) 질문을 다음 후보로 상신**한다.

---

## §2 자기검사

| 항 | 값 |
|---|---|
| `src/` diff vs 기점 | **0**(③ 미착수) · 물리 **0줄** |
| `worker.py` · `gpu/engine/` | **diff 0** · 정본·blob·문턱 **0** · 격자 용량 **262144 유지** · 계기(v4-28·30·31·32) **수정 0** |
| 이 판이 **신설**한 것 | `scripts/v4KabschProbe.ts`(판정만 계기 · 사원수 Kabsch) · 산출 `l3ap-kabsch-v4-33-A.json` |
| 굽기 · 캡처 | **0 · 0** |
| pytest(2호기) | **40 passed · 2 xfailed · 1 xpassed**(147.37 s) |
| `npx tsc -b` | **통과**(종료 0) |
| 브랜치 인쇄 | §0 머리에 에어·2호기 둘 다 · §0 은 집행 **«전»** 커밋(`24eb44d`) |
| ecc 에이전트 / 스킬 | **0 / 0** |

---

## §4 회차 갈래 — **C**(②(다) ⟹ 기각 등재 · ③④ 착수 0)

**넘기는 것 7건**

1. ★★★ **강체 정합은 «성립»했다** — 회전각 **28.02° / 28.08°** · det **+1** · 잔차 RMS **36.85 / 36.77 mm** ·
   좌우 대칭(각도 차 0.0543°). **손 각도 0**(대응에서 유도).
2. ★★★ **거리는 더 닫혔다** — 봉제쌍 중앙 **234.7710 → 31.6355 mm**(v4-32 평행이동 43.0171 보다 작다).
3. ★★★ **그래도 뚫는다** — 몸SDF 위반 **0 → 148**(v4-32 291 의 절반) · 깊이 중앙 **10.5054 mm**.
   ⟹ **자유도 6(회전+평행이동)으로도 두 조건을 동시에 만족하는 자리가 없다.**
4. ★★★ **판정문이 사전 등재한 결론으로 간다** — 「강체 정합으로도 뚫리면 = 이 암홀 위치·모양의 기하 불가」
   ⟹ 다음 질문은 **몸판 쪽(암홀 자리)** 이다.
5. ★★ 회전각 **28°** 가 팔 내림 **35°** 와 같은 자릿수라는 사실(손 각도 없이 나온 수) — 방향은 맞는데 **양이 모자란다**.
6. ★★ **가상 실험이 네 번째로** 코드 수정을 막았다(v4-27 · v4-31 · v4-32 · v4-33 · 넷 다 `src/` 0 · 굽기 0).
7. ★ **계기 유보 1건** — 위반 깊이가 10.5054 mm 에 몰린다(SDF 밴드 하한 가능성 · 단정 0).

**이 판이 하지 않은 판단** — 암홀 자리의 처분 · 비강체(변형) 정합 · A포즈 굽기 · 화면.
