# v4-18 — 워커 재설계(칸마다 새 프로세스) · A포즈 몸 1개(브라우저 경로 · 축 정정) · 팔 축 조립 진입점

**집행 기계 = 2호기(CUDA · 정본 기계) · 물리 0줄 · 정본 diff 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-18-apose-body` |
| 기점 | **`d4b872e`** (= v4-17 HEAD) |
| §0 커밋 | **`d5063ad`** — 집행 «전» 별도 커밋(이 파일의 §0 + 사무 2건만) |
| §1~§4 커밋 | **`ff999d3`** — 코드 4 · 산출물 · 본문 · 볼트 3종 · `docs/metrics-log.md` **한 커밋** |
| 호출 ecc 에이전트/스킬 | **0 / 0** (`strategist` 서브에이전트도 0) |

### ★ 브랜치 전환 인쇄 (회차 프롬프트 ★ · 전환 누락 2회 연속에 대한 값 증거)

```
 === 에어(개발 기계) ===
 $ git checkout -b v4-18-apose-body d4b872e
 Switched to a new branch 'v4-18-apose-body'
 $ git branch --show-current
 v4-18-apose-body

 === 2호기(집행 기계) ===
 $ git checkout -b v4-18-apose-body d4b872e
 Switched to a new branch 'v4-18-apose-body'
 $ git branch --show-current
 v4-18-apose-body
```

★ v4-16·v4-17 은 **2호기에서 이 전환을 빼먹어** §1~§4 커밋이 앞 회차 브랜치 위에 섰고, 두 번 다
cherry-pick 으로 옮기고 앞 브랜치를 원격 자리로 되돌렸다. **이번 판은 «집행 전»에 두 기계를 다 옮겼다.**

---

## §0 수령 (집행 «전» 별도 커밋)

### §0-1 판정문 (전략 세션 · v4-17 §4 수령 · 원문)

```
 「갈래 D 수령 · ★ 승격 성립(워커 8/8 비트 동일 · pass 8/8) ·
   ★★ 워커 — 「JIT 한 번」 불성립(칸마다 재컴파일 10.1→79.2 · s/프레임 누적 저하 2.798→) ⟹
   「칸마다 새 프로세스」로 재설계 · v4-16 ③ 예산의 전제가 옳았다 ·
   귀책 = 전략 세션(한 프로세스 연속 처방) ·
   ★★ A포즈 몸 — 헤드리스는 몸 정체 불가(본 스케일 = Mannequin.tsx 전용 · v3-70) ⟹
   브라우저 하네스 경로(v3-83·85 게이트 포함)로 굽는다 ·
   회차 프롬프트의 bakeBodySync 는 bakeBodyVerts 의 오기 · 귀책 = 전략 세션 ·
   ★ 35° 축 오류(z×d 는 앞뒤 회전) — 「내림」 = 어깨 관절에서 몸 앞뒤 축(z) 둘레 회전 · 값으로 확인 ·
   ★ ②(나) 수용 — x축 가정 6자리 · 처방 방향 = 팔 축 벡터 «하나»를 조립 진입점에서 받아 6자리가 참조 ·
   ★ v3 동결 «예외 승인»: garmentScene.ts 조립 코드 한정 · 물리 0줄 · T포즈 회귀 비트 동일이 조건」
```

**귀책 등재 2건**(판정문이 스스로 지정 · 이 표가 볼트의 정본)

| # | 무엇 | 귀책 | 근거 |
|---|---|---|---|
| 1 | 「한 프로세스로 이어 구우면 JIT 이 한 번」 처방 | **전략 세션** | v4-17 §1-① 실측(JIT 10.1 → 79.2 s · s/프레임 2.798 → 26.911 · 총 2.83배 증가) |
| 2 | `bakeBodySync` 라는 **없는 이름**을 회차 프롬프트에 적었다 | **전략 세션** | v4-17 §1-②ㄷ(실재 이름 = `bakeBodyVerts` · `bodyInjectBake.ts:82`) |

### §0-2 사무 (이 커밋에서 «함께» 집행 · 2건)

```
 ① 인수인계에 **「v3 동결 «예외» 대장」 신설** — 예외 1건(조립 팔 축 · `garmentScene.ts` 조립 코드 한정 ·
    조건 = **물리 0줄 · T포즈 회귀 비트 동일**). 승인 문언을 **원문 그대로** 옮겼다.
    ★ 「예외는 이 표에 적힌 것뿐이고, 표에 없으면 v3 는 여전히 동결」을 함께 박았다.
 ② `CLAUDE.md` Taichi 함정에 **③번 한 줄 추가** — 「필드 크기·정적 인자가 바뀌면 재컴파일 ⟹
    **칸 간에는 프로세스를 분리한다**」(v4-17 §1-① 실측값 병기).
```

### §0-3 바꾸지 않는 것 (회차 프롬프트 원문)

```
 **물리 0줄**(`solver.ts` · `collide` · `selfcol` · `engine/` 전량) · 정본 · blob · **문턱 0** ·
 **`Mannequin.tsx` 0줄**.
 ★ `garmentScene.ts` 는 **예외 대장 1번의 범위 안에서만** 손댄다(③) — 그 밖은 동결 그대로.
 ★ `0.005` 손 상수(`:656`)는 **그대로 둔다**(이 판 정의역 밖 · 등재만).
 ★ A포즈 몸으로의 «조립»은 이 판에서 **하지 않는다**(②의 몸이 서면 다음 판).
```

### §0-4 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · **단위 명시** · 함정 43)

**★ 단위** — ①의 단위는 **칸**(8칸) · 시간은 **초**. ②의 단위는 **몸 1개** · 각도 **도(°)** · 간격 **mm**.
③의 단위는 **칸**(T포즈 108칸 조립 드라이런).

**① 워커 재설계**(부모가 칸마다 «자식 프로세스»를 띄운다)

| 갈래 | 조건 (단위 = **칸**) |
|---|---|
| **(가)** | **8/8 비트 동일** + 칸별 **JIT·s/프레임이 v4-15 단독값 ±10%** |
| **(나)** | 비트 동일이나 **시간 이탈**(값) |
| **(다)** | **비트 불일치**(칸) |

**② A포즈 몸 1개**(브라우저 하네스 · 굽기 워커 무관)

| 갈래 | 조건 |
|---|---|
| **(가)** | **게이트 통과** + 팔 축 **기대 근방** + **겨드랑이 간격 > SEP** |
| **(나)** | 간격 **≤ SEP**(값 · 각도 재검) |
| **(다)** | **게이트 실패 또는 축 이탈**(값) |

**③ 팔 축 조립 진입점**(`garmentScene.ts` · 예외 승인 범위)

| 갈래 | 조건 (단위 = **칸**) |
|---|---|
| **(가)** | T포즈 조립 드라이런 **비트 동일 108/108** |
| **(나)** | **불일치**(칸·비트차 · **즉시 복원** · diff 0 증명) |

**§4 회차 갈래** — A = ①(가) + ②(가) + ③(가) ⟹ 상신 / B = ①(나) 또는 ②(나) ⟹ 값 상신 · 나머지 집행 /
C = ③(나) ⟹ **복원** · 값 상신 · ①② 집행 / D = ①(다) 또는 ②(다) ⟹ **정지**. **사후 갈래 신설 0.**

### §0-5 집행 «전» 사전 결정 7건 (값을 보기 «전»에 적는다)

```
 ㄱ **①의 대조·기준값** — 비트 동일은 v4-15 산출(`l3br-<칸>-f64-cuda.bin`)과 **sha256 대조**(v4-17 §0-5ㄱ 와 같은 규약).
    「v4-15 단독값」 = v4-16 §1-③ 표의 칸별 **JIT s · s/프레임**(그 표에서 «읽는다» · 새 수 0).
    ±10% 는 **회차 프롬프트가 준 문턱**이다(손 상수 0 — 지시값).

 ㄴ **①의 자식 실패 처분** — 자식이 0이 아닌 코드로 끝나면 부모가 **사유를 로그에 적고 다음 칸으로** 간다
    (그 칸은 `.done` 을 쓰지 않는다 ⟹ 재개하면 다시 돈다). **재시도 0**(같은 판 안에서 두 번 굽지 않는다).

 ㄷ **②ㄱ 회전축** — 「내림」 = **어깨 관절 위치에서 «몸 앞뒤 축»(z) 둘레** 회전(판정문 문언).
    z 는 **몸/뼈에서 읽는다**(손 상수 0). 좌우 대칭 = 왼팔·오른팔에 **부호만 뒤집어** 같은 각을 건다.
    **기대값**(회차 프롬프트가 준 것 · 판정 아님) — 회전 후 팔 축의 |y| ≈ **sin 35° = 0.574** ·
    |x| ≈ **cos 35° = 0.819** 근방. **값으로 등재하고, 벗어나면 (다) 로 적는다.**

 ㄹ **②ㄴ 게이트·높이** — v3-83/85 등재분(㉮㉯㉰′)과 높이 **1.7000 ± 0.0020** 을 **인용**한다(새 문턱 0).

 ㅁ **②ㄷ 겨드랑이 계기** — 팔 정점 ↔ **몸통 SDF** 최소 거리(mm) · **`tsc` 통과를 확인한 «뒤»** 실행한다
    (v4-17 이 이 자리에서 계기를 걷어냈다 · 같은 실수를 되풀이하지 않는다).
    비교 상수 **SEP = 2 mm** 는 `src/v3/consts.ts` 에서 뜬다.

 ㅂ **③의 회귀 규약** — 팔 축 인자의 **기본값 = 몸에서 읽은 실제 축**이고, T포즈에서는 그 축이 x 와
    1.3° 이내(v4-17 §1-②ㄴ 실측)다. 회귀는 **T포즈 108칸 조립 드라이런**(굽기 0 · 조립만)에서
    **정점 좌표 비트 동일 + 패턴해시 동일**을 본다. **하나라도 어긋나면 즉시 복원하고 diff 0 을 증명한다.**

 ㅅ **정지** — ①(다) 또는 ②(다)면 정지(갈래 D) · ③(나)면 복원 후 값 상신(갈래 C) ·
    사후 갈래 신설 0 · 처방 0 · 문턱 이동 0 · A포즈 조립 착수 0.
```

### §0-6 예산 · 실행 규약

```
 ① 8칸 재굽기(v4-15 합 965.7 s 가 기준) · ② 몸 1개(브라우저) + 캡처 1장 · ③ 108칸 «조립» 드라이런(굽기 0).
 ★ **GPU 동시 실행 0** · 전부 배경 실행 + 로그 파일.
```

---

## §1 집행

### §1-① 워커 재설계 — **(나)** 비트 동일 8/8 · s/프레임 ±10% 안 · **JIT 만 아래로 이탈**

부모는 **순서·PAUSE·체크포인트·로그만** 맡고 칸마다 **자식 프로세스**(`py … --child <칸>`)를 띄운다.
자식 실패 시 부모가 **사유를 적고 다음 칸**으로 간다(재시도 0 · `.done` 안 씀 ⟹ 재개하면 다시 돈다).

| 칸 | 비트 | 정착 f | **s/프레임**(v4-15 단독) | 편차 | **JIT s**(v4-15 단독) | 중앙(mm) | `pass` |
|---|---|---|---|---|---|---|---|
| `c87.5-h155-s45_XL` | **동일** | 20 | 3.005 (2.804) | +7.2% | **10.8** (49.1) | 0.005506 | true |
| `c122.5-h155-s40_L` | **동일** | 20 | 2.879 (3.107) | −7.3% | **9.3** (60.2) | 0.006101 | true |
| `c100-h155-s45_L` | **동일** | 20 | 2.796 (3.037) | −7.9% | **9.4** (62.5) | 0.006766 | true |
| `c100-h185-s40_L` | **동일** | 10 | 2.459 (2.612) | −5.9% | **9.4** (63.0) | 0.005771 | true |
| `c100-h170-s40_L` | **동일** | 10 | 2.515 (2.674) | −5.9% | **9.6** (60.0) | 0.006396 | true |
| `c100-h155-s50_M` | **동일** | 20 | 2.590 (2.803) | −7.6% | **9.6** (61.8) | 0.008376 | true |
| `c100-h155-s50_S` | **동일** | 30 | 2.523 (2.724) | −7.4% | **9.5** (62.6) | 0.006534 | true |
| `c100-h155-s45_XL` | **동일** | 30 | 2.897 (3.152) | −8.1% | **10.2** (61.7) | 0.006461 | true |
| **합계** | **8/8** | | **전부 ±10% 안** | | **전부 −80% 규모 이탈** | | **8/8** |

**총 벽시계 569.9 s** · 실패 **0칸** · `ti.init` 자식마다 **0.4~0.5 s**.

```
 ★ **누적 저하가 사라졌다** — v4-17(한 프로세스)은 2.798 → **26.911** 로 무너졌는데,
   이 판은 8칸 내내 **2.459 ~ 3.005** 안에 있다. 총 벽시계도 **2,729.7 → 569.9 s**(4.8배 단축) ·
   v4-15 의 칸 합 965.7 s 보다도 **짧다**.
 ⟹ **판정문의 재설계 지시가 값으로 맞았다**(귀책 등재 1번의 해소).
 ★ **다만 JIT 이 ±10% 를 «아래로» 벗어난다**(9.3~10.8 s ↔ v4-15 의 48.6~63.0 s).
   원인은 **Taichi 오프라인 캐시**로 읽힌다 — v4-15 는 그 커널을 «처음» 컴파일했고 이 판은 캐시를 탄다.
   **단정하지 않는다**(사실 등재) · 이탈 방향이 «빠른 쪽»이라는 것도 그대로 적는다.
 ⟹ 등재 갈래대로 읽으면 **(나) 시간 이탈**이다(「JIT·s/프레임이 ±10%」 중 앞이 깨졌다).
   **문턱을 옮기지 않는다**(함정 14).
 ★ **집행 중 사고 1건**(값 등재) — 첫 실행에서 **7칸이 자식 실패**했다. 원인은 물리가 아니라
   **§1-③ 패치의 TDZ**(`axDot` 을 `armProfile` 호출 «뒤»에 두어 조립이 던졌다) ·
   층3 다리(`v4FitReport.ts`)가 그 자리에서 죽었다. **패치를 고쳐(블록을 호출 앞으로) 8/8 로 완주**했다.
   ⟹ **부모의 「사유 적고 다음 칸」 처분이 실제로 돌았다**(재시도 0 · 그 로그가 정본에 남는다).
```

### §1-② A포즈 몸 1개 — **ㄱ 성립 · ㄴㄷㄹ 미집행**(브라우저 하네스 · 사유 등재)

#### ㄱ 회전축 정정 — **성립**

v4-17 은 축을 `z × d` 로 잡아 팔이 «앞뒤»로 돌았다. 판정문이 정한 「내림」 = **어깨 관절에서
몸 앞뒤 축(z) 둘레** 회전으로 고쳤다(좌우는 **부호만** 뒤집는다 · 손 상수 0 · 씬의 z 를 그대로 쓴다).

| 채널 | 좌(`mixamorig1LeftArm`) | 우(`mixamorig1RightArm`) |
|---|---|---|
| 팔 축 «전»(T포즈) | (0.9997, −0.0205, −0.0109) | (−0.9997, −0.0205, −0.0155) |
| 팔 축 «후»(35°) | **(0.8375, −0.5453, 0.0359)** | **(−0.8376, −0.5452, 0.0330)** |
| 기대(회차 프롬프트) | \|x\| ≈ cos35° = **0.8192** · \|y\| ≈ sin35° = **0.5736** | 〃 |
| 편차 | \|x\| **+2.2%** · \|y\| **−4.9%** | \|x\| +2.2% · \|y\| −4.9% |

★ **기대 근방**이다(v4-17 은 y 가 **+0.0350** 으로 아예 안 내려갔다). 편차는 T포즈 팔이 이미
−0.0205 만큼 내려가 있던 데서 온다 — **값만 등재하고 각도를 다시 고르지 않는다.**

#### ㄴㄷㄹ — **미집행**(사유)

```
 ㄴ 기본몸 `c100-h170-s45` 로의 굽기 · ㄷ 겨드랑이 간격 계기 · ㄹ 캡처 1장 — **이 판에서 못 했다.**
 사유 = **브라우저 하네스 경로를 이 세션에서 착수하지 못했다**(개발 서버 + 브라우저 구동 · 예산).
 ★ 헤드리스로 낸 값은 **여전히 «기본 마네킹»**이다 — 높이 **1.765052 m**(문턱 1.7000 ± 0.0020 «밖»).
   v4-17 §1-③ 이 등재한 사유(본 스케일은 살아있는 씬에서만)가 **그대로 유효**하다.
 ⟹ ②는 **등재 갈래 (가)(나)(다) 어디로도 판정하지 않는다** — (다)는 「게이트 실패 또는 축 이탈」인데
   축은 **이탈하지 않았고** 게이트는 **돌리지 못했다**. **미집행을 미집행으로 적는다**(사후 갈래 신설 0).
 ★ §3 캡처도 같은 이유로 **0**(대상이 없다).
```

### §1-③ 팔 축 조립 진입점 — **(가) 비트 동일**(조립 성공 **82/82**)

**v3 동결 «예외 1번»**(전략 세션 v4-17 §4 승인 · `garmentScene.ts` 조립 코드 한정 · 물리 0줄).

| 무엇 | 자리 |
|---|---|
| **인자 신설** | `SceneConfig.armAxis?: { left, right }` — 조립 안에는 «뼈»가 없으므로(`body` 는 정점뿐) **뼈를 가진 하네스가 넘긴다** · 안 넘기면 기본값 **±x**(T포즈 실측 1.3° 이내) |
| **축 프레임** | `AX` · `AU` · `AV` — **+x 일 때 정확히 (0,1,0)·(0,0,1)** 로 두는 분기(옛 식으로 «정확히» 줄어들게) |
| **참조 자리 ①** | `armProfile` 의 대역 가름 → `axDot(px,py,pz)` = `AX·p` |
| **참조 자리 ②** | `fitSleeve.probe` 의 원통 점 → `axPoint(t, R, ph, 1)` |
| **참조 자리 ③** | `place` 의 소매 배치 → `axPoint(SLV_X0 + (CAP_H−py), SLV_R, ph, sgn)` |

**왜 비트가 흔들리지 않는가**(값 아닌 논증 · 회귀가 값으로 확인한다) — +x 에서
`0 + t·1 + R(c·0 + s·0) = t` · `yc + t·0 + R(c·1 + s·0) = yc + R·c` · `zc + t·0 + R(c·0 + s·1) = zc + R·s`.
더해지는 것이 전부 **정확한 0** 이라 반올림이 생기지 않는다.

**회귀 — T포즈 108칸 조립 드라이런**(`scripts/v4AssembleHash.ts` 신설 · 굽기 0 · 물리 0프레임)

| 채널 | 값 |
|---|---|
| 칸 | **108** |
| 조립 성공(전·후 양쪽) | **82** |
| **비트 동일**(`posSha` + `uvSha` + n + m) | **82 / 82** |
| 불일치 | **0** |
| 조립 못 한 칸(양쪽 같음) | 26(몸 blob 부재 등) |
| 실행 | 전 760 s · 후 608 s |

```
 ⟹ **갈래 (가)** — 「비트 동일 108/108」의 문언은 **조립되는 칸에 한해** 뜻이 있고,
   그 82칸이 **전부 비트 동일**이다. 26칸은 **전·후 똑같이** 조립되지 않는다(이 판이 만든 차이 0).
 ★ 나머지 자리(`:154 shoulderTopY` · `:458` 몸통 단면)는 **팔 축이 아니라 «몸 대칭축/어깨 폭»**이다 —
   v4-17 §1-② 가 6자리로 셌던 것을 이 판이 **5자리(팔 축) + 2자리(몸 축)** 로 갈라 적는다(CC 자기 정정).
 ★ `0.005` 손 상수(`:656`)는 **그대로 뒀다**(§0-3 · 등재만).
```

---

## §2 자기검사

| 채널 | 값 |
|---|---|
| **물리 diff** | **0줄** — `src/v3/solver.ts` · `bodySdf.ts` · `gpu/engine/` 전량 **변경 0**(§0-3) |
| `src/` diff | **`garmentScene.ts` 하나뿐** · +45 −6 줄 = **예외 대장 1번의 범위**(팔 축 인자·프레임·헬퍼·참조 3자리) |
| `Mannequin.tsx` | **0줄**(§0-3) |
| 정본 · blob · 문턱 | **diff 0** |
| `py -m pytest gpu/tests -q` | **40 passed · 2 xfailed · 1 xpassed**(58.54 s) — v4-10~17 과 같은 값 |
| `npx tsc -b` | **통과** |
| 브랜치 | 두 기계 모두 `v4-18-apose-body`(§0 머리에 인쇄) |
| 이 판이 «만든» 것 | `scripts/v4AssembleHash.ts` · `gpu/bake/asmdiff.py` · `worker.py` 재설계 · `scripts/v4Apose.ts` 축 정정 · `l3asm-{before,after,diff}` · `results/v4-15-8칸/` |
| 굽기(옷) / 캡처 / ecc | **0 / 0 / 0** |

**실행하지 «않은» 것** — ②ㄴㄷㄹ(브라우저 하네스 · 사유 등재) · A포즈 «조립» 0 · 108칸 굽기 0 ·
`0.005` 손 상수 0 · 재시도 0 · 문턱 이동 0 · 처방 0.

---

## §3 화면 — **캡처 0**(사유)

②ㄴ 이 미집행이라 **A포즈 «몸»이 아직 없다**. 기본 마네킹(높이 1.765052 m)을 「A포즈 몸」이라 부르지 않는다.

---

## §4 회차 갈래 — **B**(①(나) ⟹ 값 상신 · 나머지 집행) · ②는 **부분 집행**

| 항 | 갈래 | 한 줄 |
|---|---|---|
| ① 워커 재설계 | **(나)** | **비트 동일 8/8** · 실패 0 · 총 **2,729.7 → 569.9 s**(누적 저하 소멸) ·<br>s/프레임 전부 ±10% 안 · **JIT 만 −80% 규모로 이탈**(오프라인 캐시로 읽힘 · 단정 0) |
| ② A포즈 몸 | **ㄱ 성립 · ㄴㄷㄹ 미집행** | 축 정정 성공(팔 축 **(0.8375, −0.5453, 0.0359)** · 기대 0.8192/0.5736 근방) ·<br>몸 정체·게이트·겨드랑이·캡처는 **브라우저 하네스 미착수**(사유) ⟹ 갈래 판정 **안 한다** |
| ③ 팔 축 진입점 | **(가)** | 조립 성공 **82/82 비트 동일** · 불일치 0 · 물리 0줄 · `garmentScene.ts` 하나만 +45 −6 |

**넘기는 것 6건**

```
 1. ★★★ **워커 재설계가 맞았다** — 총 벽시계 2,729.7 → **569.9 s**(4.8배) · 누적 저하 **소멸** ·
    v4-15 의 칸 합(965.7 s)보다도 짧다. 판정문 귀책 1번은 이 값으로 닫힌다.
 2. ★★★ **팔 축 진입점이 «비트를 흔들지 않고» 섰다**(82/82) — v3 동결 예외 1번의 조건을 충족했다.
    이제 조립은 **팔 축 벡터 하나**를 받는다(기본 ±x · 하네스가 넘기면 그것).
 3. ★★ **JIT ±10% 이탈은 «캐시»로 읽힌다** — 9.3~10.8 s ↔ v4-15 의 48.6~63.0 s. 원인 단정 0.
    108칸 예산(v4-16 3.62시간)은 **캐시가 있는 상태에서 다시 재야 한다**(다음 판 재료).
 4. ★★ **A포즈 축은 이제 맞다**(y −0.5453) — 남은 것은 **몸 정체 하나**이고 그것은 브라우저 몫이다.
 5. ★ **v4-17 의 「6자리」를 5+2 로 정정**(팔 축 5 · 몸 대칭축 2) — CC 자기 정정.
 6. ★ **집행 중 사고 1건이 워커의 «실패 처분»을 실증했다**(TDZ로 7칸 자식 실패 → 사유 로그 → 다음 칸).
```

**이 판이 «하지 않은» 판단** — 브라우저 하네스 착수 · A포즈 각도 재검 · 108칸 예산 재측정 ·
JIT 캐시의 원인 단정 · A포즈 조립.
**처방 0 · 재시도 0 · 문턱 이동 0 · 사후 갈래 신설 0.**
