# v4-03 — 오차 상한 재유도(연산 사슬 K) · 늘어남 재판정 · 굽힘 제약 이식

**집행 기계 = 2호기(CUDA · 정본 기계) · `src/` diff 0 · 굽기 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-03-bending` |
| 기점 | **`5766d95`** (= v4-02 HEAD · push 완료 · `origin/v4-02-stretch` 에 있음) |
| 워크트리 | `C:\Users\banba\fit-simulator` |
| 기계 | **`DESKTOP-OV72A46`** (win32 · CUDA) |
| §0 커밋 | **`b627e5f`** — 집행 «전» 별도 커밋(sha 만 그 뒤 한 줄로 채웠다) |
| 호출 ecc 에이전트/스킬 | **0** |

---

## §0 수령 (집행 «전» 별도 커밋)

### §0-1 판정문 (전략 세션 · v4-02 §4 수령 · 원문)

```
 「갈래 C 수령 · 이식 정확 확인(f64 에서 2.22e-16) · 초과 1.224배 = f32 폭 ·
   ★ 귀책 = 전략 세션(상한이 스텝 수만 세고 스텝 «안» 연산 사슬을 안 셈 · 함정 36 이웃) ·
   처분 = 상한 «재유도»(완화 아님): 정점당 부동소수 연산 사슬 길이 K 를 커널 코드에서 세어
   K × ULP_f32(그 자리 값) × M · K 는 실측을 보고 고르지 않는다(코드에서 센 K 를 먼저 등재 후 대조) ·
   문턱 이동 0 · push 는 fine-grained 토큰(Contents R/W)으로 해결」
```

### §0-1′ 귀책 등재 — **전략 세션**(v4-02 상한)

```
 v4-02 §0-4 ③ 의 상한 **M × 4·ULP_f32** 는 **M(스텝 수)만 세고 «스텝 안에서 도는 연산 사슬»을
 세지 않았다.** ㄴ 는 1 스텝 안에서 제약 **18,016 개**를 가우스–자이델로 투영하고, 한 정점은
 자기가 속한 삼각형 수만큼 «거듭» 갱신된다 — 그런데 상한은 M=1 로 **4 ULP** 만 허용했다.
 ⟹ 초과(1.224배)는 **이식의 결함이 아니라 상한식의 결함**이었다(f64 재현 2.22e-16 이 증명).
 **귀책 = 전략 세션.** CC 는 이 판에서 **문턱을 만들지 않고**, 처분이 지정한 **K 를 코드에서 센다**.
 함정 36(최대값을 문턱으로 삼지 않는다)의 이웃 — 여기서는 **「연산 사슬을 안 센 상한」**이다.
```

### §0-2 사무 (이 커밋에서 집행)

| # | 항목 | 자리 |
|---|---|---|
| ㄱ | **「2호기 push = fine-grained PAT(Contents R/W) · `origin` URL 에 토큰」** 한 줄 | `00-현재상태.md` 원격 굽기 인프라 블록 |
| ㄴ | **「정답지 자산은 git 밖 · 기계 간 이전은 scp · 2호기 경로」** 등재 | 〃 (같은 블록) |

### §0-3 바꾸지 않는 것

```
 `src/` · 정본 · blob · v3 동결 · **v4-02 커널 로직 diff 0**(상한 «정의»만 바꾼다)
```

### §0-4 사전 등재 — **K 는 실측을 보기 «전»에 센다**

```
 절차를 순서로 못 박는다(사후 맞춤 방지):
   ① 커널 코드에서 **정점 위치 갱신 경로의 부동소수 연산을 «세어» K 를 등재한다**
   ② 그 «뒤»에 v4-02 의 실측값(5.837018e-07)을 새 상한과 대조한다
 나눗셈·`sqrt` 는 **각 1** 로 센다(IEEE-754 에서 정확히 반올림되는 기본 연산이므로 —
 그 사실을 여기 명시한다). 곱셈·덧셈·뺄셈도 각 1.
```

### §0-5 갈래 (집행 «전» 등재 · 원문)

| 갈래 | 조건 | 처분 |
|---|---|---|
| **A** | ①②③④ 전량 + ①(가) + ③ 상한 안 | 상신 |
| **B** | ① **(나)** — 재유도 상한도 초과 | 값 상신 · **③ 착수 0** |
| **C** | ③ 상한 초과 | 값 상신 · **정지** |
| **D** | 그 외 | 정지 |

**①의 하위 갈래** — (가) 실측 5.837e-07 ≤ 새 상한 ⟹ 늘어남 이식 **통과** /
(나) 초과 ⟹ 값 상신 · 정지(**귀속 0 · 상한 재조정 금지**).

**사후 갈래 신설 0.**

### §0-6 착수 «전» 알려진 사실

```
 · push 는 **해결됐다** — `origin` URL 의 fine-grained PAT 로 `--dry-run` 이 통과한다(집행 전 확인).
 · 정답지 자산 39 blob · 몸 27 · 인덱스 정본은 **이 기계에 이미 있다**(v4-02 에서 scp 도착분).
 · v4-02 가 남긴 조립 덤프(`gpu/oracle/export/`)가 커밋돼 있어 **ㄴ 의 입력이 재현 가능**하다.
 · ★ **굽힘 제약은 v4-02 에서 «만든 적이 없다»** — v4-02 덤프는 `inplane` 만 담았다(`v4Export.ts`).
   ⟹ ③ 은 덤프 형식을 **넓혀야** 한다(굽힘 제약 배열 추가). 그것은 v3 를 읽는 창의 확장이고
   `src/` 는 여전히 **0줄**이다.
```

---

## §1 집행

### §1-① 오차 상한 «재유도» — K 를 코드에서 세고, 그 «뒤» 늘어남을 재판정

#### 세는 규칙 (§0-4 등재분 그대로)

```
 나눗셈 · sqrt · atan2 = 각 **1**(IEEE-754 기본 연산은 «정확히 반올림»된다) ·
 곱셈 · 덧셈 · 뺄셈 · 부호반전 = 각 **1**.
 둘을 «둘 다» 센다 — **깊이**(갱신된 좌표가 거치는 의존 사슬의 길이 · 엄격) ·
 **총량**(그 갱신이 의존하는 연산의 총수 · 느슨). **판정은 «깊이»로 한다**(함정 14).
```

#### 늘어남 커널 K — `gpu/engine/stretch.py:_project`(v4-02 커널 · 이 판에서 **로직 diff 0**)

| 자리 | 식 | 연산 수 | 깊이 |
|---|---|---|---|
| `:98-99` | `e1 = pos[i1]−pos[i0]` · `e2 = pos[i2]−pos[i0]` | 3 + 3 | 1 |
| `:100-101` | `xu = a·e1 + b·e2` · `xv = c·e1 + d·e2` | 9 + 9 | 3 |
| `:107` (G00) | `C = (xu·xu − 1)/2` | 5+1+1 = **7** | 8 |
| `:109-110` | `g1 = a·xu` · `g2 = b·xu` | 3 + 3 | 4 |
| `:121` | `g0 = −(g1+g2)` | 3+3 = **6** | 6 |
| `:122` | `denomW = Σ wᵢ\|gᵢ\|²` | 15+3+2 = **20** | 12 |
| `:124` | `at = 1/(area·stiff)/h²` | **3** | 3 |
| `:125` | `dl = (−C − at·λ)/(denomW+at)` | **5** | 14 |
| `:126` | `λ += dl` | 1 | 15 |
| `:127-129` | `pos += w·dl·g` (한 좌표) | **3** | **17** |
| | **성분 G00 합** | **75** | **17** |
| 〃 | 성분 G11(같은 꼴) | 75 | 17 |
| `:117-120` | 성분 G01 — `C = (xu·xv)/2` (6) · `g1·g2 = (a·xv+c·xu)/2` (12+12) | **92** | **19** |
| | **제약 하나 = 성분 3개(«차례로»)** | **242** | **53** |

#### 새 상한 · 늘어남 재판정 — **(가) 통과**

```
 상한 = **K × deg × ULP_f32(최대|좌표|) × M**
   `deg` = 한 정점이 속한 제약 수 — **메시의 «구조»값**이지 측정값이 아니다(함정 36 아님).
   한 스텝 안에서 그 정점 좌표가 deg 번 갱신되므로 사슬이 deg 배로 는다.
 기본몸 M — deg 분포 {1:8, 2:8, 3:736, **6:8636**} ⟹ **deg 최대 6** ·
   최대|좌표| 1.449586255 ⟹ **ULP_f32 = 1.192093e-07** · M = **1**(「1스텝」)
```

| 채널 | K | 상한 | 실측 | 실측/상한 | 판정 |
|---|---|---|---|---|---|
| **깊이**(엄격 · 판정용) | 53 × 6 = 318 | **3.790855e-05** | **5.837018e-07** | **0.0154** | **(가) 통과** |
| 총량(느슨 · 참고) | 242 × 6 = 1452 | 1.730919e-04 | 〃 | 0.0034 | 통과 |
| ★ **바닥 사례**(deg = 1 · K = 53) | 53 | **6.318092e-06** | 〃 | **0.0924** | 통과 |

```
 ★ **실측은 이 세션에서 «다시» 쟀다** — 5.837018e-07 로 v4-02 등재값과 **자릿수 전량 동일**하다
   (재현 확인 · 인용이 아니라 재측정).
 ★★ **판정이 K 선택에 «둔감»하다.** 가장 작게 잡을 수 있는 K(제약 하나의 깊이 53 · deg 1)로도
   실측이 상한의 **9.24%** 다. ⟹ 「K 를 통과하도록 골랐다」는 의심이 **구조적으로 성립하지 않는다.**
 ★ v4-02 의 `xfail(strict)` 시험은 **통과 시험으로 바뀌었다** — 이력은 시험 docstring 에 남겼다
   (지우지 않는다). 상한을 바꾼 것은 **전략 세션의 처분**이고 CC 는 K 를 «세기만» 했다.
```

### §1-② v4-02 ①② 재확인 — **값 그대로**

| 항목 | v4-02 | **v4-03 재실행** | 일치 |
|---|---|---|---|
| GPU | NVIDIA GeForce GTX 1660 Ti | 〃 | ✅ |
| VRAM / 드라이버 / CUDA | 6144 MiB / 610.88 / 13.3 | 〃 | ✅ |
| Python / Taichi | 3.12.10 / 1.7.4 | 〃 | ✅ |
| `ti.init(arch=cuda)` → 실제 | `Arch.cuda` **요청대로** | 〃 | ✅ |
| `ti.init(arch=metal)` → 실제 | `Arch.x64` **폴백** | 〃 | ✅ |
| 벤치 1e7 · cuda | 0.728 ms | **0.719 ms** | 값 흔들림 |
| 벤치 1e7 · cpu(x64) | 11.469 ms | **7.136 ms** | ★ **1.6배 흔들림** |
| settled 파일 / 표 | 39 / 39칸 | 〃 | ✅ |
| 인덱스 sha 일치 | 36/36 | 〃 | ✅ |
| blob n ↔ 조립 n 불일치 | 2칸(전부 sha 미등재) | 〃 | ✅ |
| 몸 sha | 27/27 | 〃 | ✅ |
| 원단 / k / d | gray 단일 / 69 / 0.009 | 〃 | ✅ |

```
 ★ **시간 채널만 흔들린다** — cpu 벤치가 11.469 → 7.136 ms(1.6배). arch·모델·정점·sha 는 **전량 불변**.
   ⟹ **단일 측정으로 성능을 판정하지 않는다**(v1 M3 절의 「물리 ms 는 2배 흔들린다」와 같은 사실).
   이 판의 갈래는 시간에 걸려 있지 않으므로 판정에 영향 0.
```

### §1-③ 굽힘 제약 이식

**v3 코드 사실 — 손으로 적지 않고 «인용»한다**

| 자리 | 원문 / 사실 |
|---|---|
| `src/v3/solver.ts:73-88` | `BendConstraint` — 공유 엣지 `p0`·`p1` · 날개 `p2`·`p3` · `restAngle` · `ke`[N·m] · `shape` |
| `src/v3/solver.ts:80` | 「정지 이면각 — **평면 제도이므로 0이어야 한다**(S2 완료 조건 ①)」 |
| `src/v3/solver.ts:842` | `makeBend` — 엣지 맵에서 **날개가 «둘»인 엣지만** 힌지(`wings.length !== 2` 는 경계) |
| `src/v3/solver.ts:869` | `shape: (4 * a) / (l * l)` · `a` = 두 삼각형 **정지** 면적 합 · `l` = 정지 UV 의 엣지 길이 |
| `src/v3/garmentScene.ts:733` | `makeBend(tris, uv, MAT.B)` ⟹ **ke = 원단 B**(gray **2.3191698e-5**) |
| `src/v3/solver.ts:955-958` | `n1 = e × d2` · `n2 = d3 × e` (평면일 때 두 법선이 **같은** 방향 ⟹ θ=0) |
| `src/v3/solver.ts:971-972` | `∇p2θ = −n1·\|e\|/\|n1\|²` — `kA = −le/(l1*l1)` |
| `src/v3/solver.ts:979-983` | `tA = (d2·e)/\|e\|²` → `b0 = (tA−1)b2 + (tB−1)b3` · `b1 = −tA·b2 − tB·b3` |
| `src/v3/solver.ts:915` | `dihedral` — `atan2(sin, cos)` · `sin = ((â1×â2)·e)/\|e\|` |
| `src/v3/solver.ts:1002` | `at = c.shape / c.ke / h2` ⟹ **α = 4a/(ke·l²)** |
| `src/v3/solver.ts:1084` | `iters = p.iterations ?? 1` ⟹ **반복수 1** · λ 는 서브스텝마다 0 |
| `src/v3/garmentScene.ts:799` | 서브스텝은 `substepsForBending` 로 **도출**(손 상수 0) |

#### 굽힘 커널 K′ — `gpu/engine/bending.py:_project`

| 자리 | 식 | 연산 수 | 깊이 |
|---|---|---|---|
| e · d2 · d3 | `pos[pk] − pos[p0]` | 3+3+3 | 1 |
| `n1` · `n2` | 외적 | 9+9 | 3 |
| `l1` · `l2` | `sqrt(n·n)` | 6+6 | 7 |
| `le2` · `le` | `e·e` · `sqrt` | 5+1 | 5 |
| `kA` · `kB` | `−le/(l·l)` | 3+3 | 9 |
| `b2` · `b3` | `k·n` | 3+3 | 10 |
| `tA` · `tB` | `(d·e)/le2` | 6+6 | 5 |
| `b0` · `b1` | 위 식 | 11+10 | 12 |
| `a1` · `a2` · `cr` | 단위 법선 · 외적 | 3+3+9 | 10 |
| `sin` · `cos` · `atan2` | 이면각 | 6+5+1 | 14 |
| `C = θ − restAngle` | | 1 | 15 |
| `denomW = Σ wᵢ\|bᵢ\|²` | 4항 | 20+4+3 = **27** | 19 |
| `at = shape/ke/h²` | | 2 | 2 |
| `dl` | `(−C − at·λ)/(denomW+at)` | 5 | 21 |
| `λ += dl` | | 1 | 22 |
| `pos += w·dl·b` (한 좌표) | | 3 | **24** |
| | **제약 하나** | **156** | **24** |

```
 ★ 총량은 처음 **121** 로 적었다가 «전 항목 합산»으로 **156** 으로 고쳤다 — **산술 정정**이다.
   판정에 쓰는 것은 **깊이 24**이고 그 값은 **불변** ⟹ 갈래 판정에 영향 **0**.
 ★★ **v3 와 «다를 수밖에 없는» 자리 2곳 — 숨기지 않고 값으로 등재한다**
   ① `Math.hypot` — v3 는 `l1`·`l2` 와 `dihedral` 의 `le` 에 `hypot` 을 쓰는데 **Taichi 에는 없다** ⟹
      이식은 `sqrt(x²+y²+z²)` 를 쓴다. 두 함수의 상대차 **2.584e-16**(≈1.16 ULP_f64 · 실측) —
      **f32 의 ULP(1.19e-07)보다 9 자릿수 작다** ⟹ f32 판정에 관여하지 않는다.
      (v3 자신도 `bendGradients` 의 `le` 는 `sqrt`, `dihedral` 의 `le` 는 `hypot` 으로 **갈라 쓴다** —
       이식은 그 갈림을 그대로 옮겼다.)
   ② 퇴화 분기 — v3 는 `l1<1e-14 ‖ l2<1e-14 ‖ le<1e-14` 면 0 을 돌려주는데 **b0..b3 는 «직전
      제약의 값»이 남는다**(모듈 전역 버퍼). 이식은 그 자리에서 기울기를 **0** 으로 둔다.
      **발화 계수기를 달아 두 계기 모두 «0회»임을 확인**했다 ⟹ 두 구현은 **구별되지 않는다**.
```

#### ㄱ 합성 「두 삼각형 힌지」 — **상한 초과**

정의(집행 «중» 고정 · 근거를 함께 적는다): 정점 4 · 삼각형 2 · 공유 엣지 1 ⟹ 굽힘 제약 **정확히 1개** ·
정지 UV 는 간격 d 평면 ⟹ `restAngle = 0` · **한쪽 삼각형(0·1·2) 고정** · 초기 = 날개 3 을 공유 엣지
둘레로 **90° 접은** 상태 · **중력 0** · 제약은 `bend` 만.

```
 ★ **왜 중력을 뺐는가** — 처음엔 중력을 넣었는데, 늘어남 제약이 없으면 날개 정점을 «자리에» 잡아
   두는 것이 아무것도 없어 600 프레임에 **14.35 m 를 떠내려갔다**(실측). 그 좌표 크기가 ULP 상한을
   지배해 **굽힘을 재는 계기가 되지 못한다.** 중력을 빼면 남는 힘이 굽힘 하나뿐이다.
   ★ 이 재정의는 **v3 단독 실행의 진단**을 보고 한 것이고, **f32↔v3 대조 결과를 본 뒤가 아니다.**
     대조 결과를 보고 정의를 다시 바꾸지 «않았다».
```

| 항목 | 값 |
|---|---|
| 규모 | n 4 · 삼각형 2 · 굽힘 제약 **1** · 서브스텝 **13**(`substepsForBending` 도출) · M **600** |
| `shape` / `ke` / `restAngle` | 4.000000000000 / 2.3191698e-05 / 0 |
| 이면각 | **1.570796e+00 → −1.165241622460e-15** rad(정지각으로 이완) |
| `make_bend` 이식 대조 | 제약 수 · 정점 순서 `[0,1,2,3]` · `shape` **v3 와 일치** |
| 최대 \|좌표\| | 0.339313748 m ⟹ ULP_f32 **2.980232e-08** · deg_bend **1** |
| **v4 f32 — 최대 좌표차** | **1.292866e-02** ⟹ ★ **초과** |
| **v4 f64 — 최대 좌표차** | **1.110223e-16** ⟹ 이식 **정확** |
| 퇴화 분기 발화 | **0**(f32 · f64 모두) |
| 상한 깊이(24×1×ULP×600) | **4.291534e-04** · 비 **30.126** |
| 상한 총량(156×1×ULP×600) | 2.789497e-03 · 비 **4.635** |

**★ 초과의 «모양» — 선형 누적이 아니라 «증폭»이다** (같은 커널 · 정밀도만 다르게 · f32 ↔ f64)

| M(프레임) | f32↔f64 최대차 | 최대\|좌표\| | 상한(깊이 24) | 실측/상한 |
|---|---|---|---|---|
| 1 | 1.709838e-08 | 0.025606 | 4.470348e-08 | **0.382** 통과 |
| 10 | 7.383113e-07 | 0.206158 | 3.576279e-06 | **0.206** 통과 |
| 60 | 7.256765e-05 | 0.338367 | 4.291534e-05 | 1.691 초과 |
| 200 | 2.855478e-03 | 0.339314 | 1.430511e-04 | 19.961 |
| 600 | 1.292866e-02 | 0.339314 | 4.291534e-04 | 30.126 |

```
 M 이 600배 늘 때 오차는 **7.6e5 배** 늘었다 — 선형이면 600배여야 한다 ⟹ **1,260배가 선형 밖**이다.
 ★★ **원인은 이식이 아니라 «계»다.** f64 궤적이 v3 와 1.1e-16 안에서 같으므로 **궤적 자체가 v3 것**이고,
   그 궤적이 **초기 섭동을 지수적으로 키운다**(자유 날개 하나 + 각도 제약 하나 = 약감쇠 진동계).
   f32 가 심는 1.7e-08 짜리 섭동이 600 프레임에 1.3e-02 로 자란다.
 ⟹ **`K × ULP × M` 은 «선형 누적»을 전제한 형식이다.** 증폭계에서는 K 를 어떻게 세도 성립할 수 없다
   (총량 156 을 써도 4.6배 초과). **문턱을 옮기지 않는다** — 형식을 바꿀지는 전략 세션 몫이다.
```

#### ㄴ 정답지 1칸 · 「굽힘만 1스텝」 — **통과**

정의는 v4-02 ㄴ 와 «같다»: 정착 위치 · 속도 0 · 중력 0 · 감쇠 0 · substeps 1 · dt = DT/SUB ⟹
`bend` 투영이 **정확히 1회**. v3 쪽은 `step()`·`projectBend` 를 **그대로** 부른다.

| 항목 | 값 |
|---|---|
| 칸 | `c100-h170-s45_M` · n **9,388** · **굽힘 제약 26,648** · 서브스텝 229 · h 7.278020378457e-05 |
| deg_bend | 중앙 **12** · 평균 11.354 · **최대 12** |
| v3 투영 변위 | 최대 **5.511010e-06** m · 중앙 3.072e-09 |
| 최대 \|좌표\| | 1.449586255 ⟹ ULP_f32 1.192093e-07 |
| **v4 f32 — 최대 좌표차** | **4.114550e-07** · 중앙 5.744e-09 · p99 1.424e-07 |
| **v4 f64 — 최대 좌표차** | **3.388132e-21** ⟹ 이식 **정확** |
| 퇴화 분기 발화 | **0** |
| **상한 깊이**(24 × deg 12 × ULP × 1) | **3.433228e-05** ⟹ **통과** · 비 **0.0120** |
| 상한 총량(156 × deg 12) | 2.231598e-04 ⟹ 통과 · 비 0.0018 |

```
 ★ **실제 옷에서는 통과하고 합성 계기에서만 초과한다.** 차이는 «구속»이다 — 정답지 칸의 정점은
   늘어남·봉제 제약이 함께 잡고 있고 스텝도 1 이라 증폭이 일어날 자리가 없다.
   합성 힌지는 자유 날개 하나를 600 프레임 굴린다.
```

### §1-④ 「같은 옷」 기준 후보 — **미집행**

```
 §0-5 **C = 「값 상신 · 정지」** ⟹ ④ 는 **착수 0**(v4-02 와 같은 처분). **등재 0 · 문턱 0.**
```

---

## §2 자기검사

| # | 항목 | 결과 |
|---|---|---|
| ㉠ | ①③ 표 | 위 §1 |
| ㉡ | **pytest(CUDA) 전량** | **22 passed · 1 xfailed** — xfail = ㄱ 사전등록 상한 초과(갈래 C · strict) |
| ㉢ | `src/` diff | **0 파일**(기점 `5766d95` 대비) |
| ㉣ | 정본 diff | **0** — `public/v3diag` 쓰기 0 |
| ㉤ | blob sha | **36/36 인덱스와 일치** · 몸 27/27 |
| ㉥ | **v4-02 커널 로직 diff** | **0** — `stretch.py` 의 `_project`·`run` 은 한 줄도 안 바뀌었다 |
| ㉦ | **v4-02 산출물 바이트 불변** | `scene-*.bin` · `cellstep-*.bin` · `strip-v3.bin` **변경 0**(굽힘은 «새 파일»로) |
| ㉧ | `npx tsc -b` | **통과**(exit 0 · 결함 1건 수정 — 쓰지 않는 `G` 임포트) |
| ㉨ | 굽기 | 착장 `runFrames` **0회** · 조립 덤프만 |
| ㉩ | 호출 ecc 에이전트 / 스킬 | **0 / 0** |

```
 ★ 부수 발견 1건 — **덤프 파일의 sha 는 재현되지 않는다.** 헤더에 **경과 시간(`ms`)** 이 들어 있어
   재실행마다 헤더 길이가 바뀐다. **물리 페이로드(위치·속도)는 비트 동일**임을 값으로 확인했다
   (재실행 위치 차 **0.000e+00**). 형식을 고칠지는 넘긴다 — v4-02 산출물을 건드리지 않으려고 두었다.
```

**§3 캡처 — 0장**(사전 등재 그대로).

---

## §4 판정 — **갈래 C**

```
 §0-5 **C = 「③ 상한 초과 ⟹ 값 상신 · 정지」**.
 ① **(가) 통과**(5.837018e-07 ≤ 3.790855e-05 · 비 0.0154) · ② 재확인 전량 일치 ·
 ③ㄴ **통과**(4.114550e-07 ≤ 3.433228e-05 · 비 0.0120) · **③ㄱ 초과**(1.292866e-02 ↔ 4.291534e-04 · 비 30.126)
 ⟹ **C** 다(A 는 「③ 상한 안」을 요구하므로 불성립 · B 는 ①이 (가)라 불성립).
 상신하는 값: **ㄱ f32 1.292866e-02 · f64 1.110223e-16 · 성장 곡선(M 1→600 에서 비 0.382→30.126)**.
 **사후 갈래 신설 0 · 문턱 이동 0 · 기대 사후 변경 0 · v3 diff 0 · v4-02 커널 diff 0.**
```

### §4-1 넘기는 것

| # | 항목 | 몫 |
|---|---|---|
| **㉠** | **상한 «형식»의 재검토** — `K × ULP × M` 은 **선형 누적** 전제다. 합성 힌지는 **증폭계**라 어떤 K 로도 성립하지 않는다(총량 156 으로도 4.6배 초과). ① 형식을 바꿀지(예: 리아푸노프 계수를 계기에서 «재고» 곱할지) ② 아니면 **증폭계를 계기에서 배제**할지(구속이 있는 계기만 쓴다). **CC 는 문턱을 만들지 않는다** | 전략 세션 |
| ㉡ | **굽힘 이식은 «정확하다»** — f64 에서 ㄱ 1.110223e-16 · ㄴ 3.388132e-21. 실제 옷(ㄴ)에서는 f32 도 상한의 **1.2%** 다 ⟹ 다음 층(자기충돌·봉제) 진행에 굽힘이 걸림돌이 아니다 | 전략 세션 |
| ㉢ | `Math.hypot` **부재** — Taichi 이식은 `sqrt(Σx²)` 를 쓴다(차 2.584e-16). f64 대조를 «정확히» 하려면 hypot 이식이 필요하다. f32 판정에는 무관 | 전략 세션 |
| ㉣ | v3 `bendGradients` 의 **퇴화 분기가 직전 제약의 기울기를 남긴다**(전역 버퍼) — 이 판에서는 **발화 0**이라 무해했으나 **v3 쪽 잠재 결함**이다(v3 는 동결 ⟹ 등재만) | 전략 세션 |
| ㉤ | **덤프 sha 비재현**(헤더의 경과 시간) — 페이로드는 비트 동일. 형식을 고칠지 | 전략 세션 |
| ㉥ | **Metal 재실행**(v4-02 §4-1 ㉤ 이월 · 아직 미도착) — 맥에서 `py -m pytest gpu/tests -q` | 사용자(승혁) |
