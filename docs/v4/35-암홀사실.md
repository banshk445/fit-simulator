# v4-35 — 암홀 링의 자리 사실(측정만 · 처방 0 · 굽기 0)

**물리 0줄 · `src/` diff 0(계기만) · 굽기 0 · 캡처 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-35-armhole-facts` |
| 기점 | **`69707f6`**(= v4-34 §1~§4) |
| §0 커밋 | **`5ea201c`**(집행 «전») |
| §1~§4 커밋 | (집행 후 기입) |
| 보고 파일 | `docs/v4/보고/35.md` |

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v4-35-armhole-facts 69707f6 → Switched to a new branch 'v4-35-armhole-facts'
       $ git branch --show-current → v4-35-armhole-facts · HEAD 69707f6
 2호기 (§0 커밋을 push 한 «뒤» 같은 자리로 전환 — §0-5 에 인쇄)
```

---

## §0 수령·사무

### §0-1 판정문 (전략 세션 · v4-34 §4 수령 · 원문)

```
 「갈래 A 수령 · ★★★ 10.5054 = SEP + band(자의 끝) 확정 · 참깊이 중앙 19.34 · 최대 41.34 —
   함정 등재 수용 · v4-32·33 「이동 전 위반 0」은 SDF 자의 문장이었다는 정정 수용 ·
   측정 판이 잘못된 처방(부분 정합)을 막았다 ·
   ★★★ t* = 0 — 강체 경로 «완전 폐쇄» · 소매 무죄 확정 · 질문은 몸판(암홀 링)으로 ·
   t=0 의 0.0203mm 는 경계 사건(등재만) ·
   ★ 다음 판 = CC 목록 5측정 그대로 · 전략 세션 후보 지정 0 · 갈래 형태만:
     (가) 링이 몸 «안» 구간 존재(상류 = 몸판 배치가 팔을 침범) /
     (나) 밖이지만 두 링 기하 불일치(패턴 질문) / (다) 그 외」
```

### §0-2 바꾸지 않는 것 (회차 프롬프트 원문)

```
 물리 **0줄** · `src/` **0** · 정본·blob·문턱 **0** · 워커 **diff 0** · 격자 용량 **262144** ·
 계기(v4-28~34) **유지** · 자 = `exactBodyDist`(참깊이 · v4-34 와 동일) + SDF 는 **부호만**.
```

### §0-3 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · 단위 명시)

**★ 단위** — 길이 **mm** · 각도 **도** · 부위는 **점 분류**(v4-28 규칙: 팔 = |x| > W/2 · 겨드랑이 = |y − Y_ARM| ≤ ARM_D · 몸통 = 나머지).

| 항 | 갈래 |
|---|---|
| **①** 암홀 링 사실 | **(가)** 링에 **부호 음(몸 안)** 구간 존재(점 수·깊이·부위) / **(나)** 전부 밖이나 기하 문제(값으로) / **(다)** 그 외 |
| **②** 소매 캡 링 사실 | 값 등재(중심·법선·둘레·지름·평면 RMS · 두 링 차) |
| **③** T포즈 대조군 | 같은 채널 전량(재굽기 0) · **「T에서 성립하던 관계 중 A에서 깨진 것」을 값으로 지목**(해석 최소) |

**§4** — A = ①②③ 전량 ⟹ 상신 / B = 측정 불가 항목(사유) ⟹ 나머지 상신 / C = 그 외 ⟹ 정지.

### §0-4 사전 결정 4건 (값 보기 «전» · CC 집행 설계)

```
 ㄱ **링의 정의** — 암홀 링 = 어깨 이음 봉제쌍의 **몸판 쪽** 정점(좌·우 각 52) ·
    캡 링 = 같은 쌍의 **소매 쪽** 정점. v4-24·31~34 와 **같은 가름**(한쪽이 소매·다른 쪽이 몸판).
 ㄴ **법선은 최소제곱 평면에서 뜬다**(손 벡터 0) — 점들의 공분산 행렬에서 **가장 작은 고윳값의 고유벡터**.
    부호는 **팔 축(AX)과 예각이 되도록** 맞춘다(부호 규약만 · 값 창작 0).
 ㄷ **여유의 자는 v4-34 와 같다** — 크기 = `exactBodyDist`(정확 거리) · 부호 = SDF.
    위반 = 부호 있는 여유 < SEP(2.0 mm). **새 문턱 0.**
 ㄹ **①의 갈래 판정 기준을 지금 고정한다** — 링 점 중 **부호가 음(몸 안)인 점이 1개라도 있으면 (가)** ·
    전부 밖이면 **(나)** · 계산 불가면 (다). 「기하 문제」의 판단은 **하지 않는다**(값만 적는다).
```
