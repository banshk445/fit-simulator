# v4-35 — 암홀 링의 자리 사실(측정만 · 처방 0 · 굽기 0)

**물리 0줄 · `src/` diff 0(계기만) · 굽기 0 · 캡처 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-35-armhole-facts` |
| 기점 | **`69707f6`**(= v4-34 §1~§4) |
| §0 커밋 | **`5ea201c`**(집행 «전») |
| §1~§4 커밋 | **`1d5b6fa`** — 본문 · 보고 · 볼트 3종 · metrics · 계기 1종 **한 커밋**( diff 0) |
| 보고 파일 | `docs/v4/보고/35.md` |

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v4-35-armhole-facts 69707f6 → Switched to a new branch 'v4-35-armhole-facts'
       $ git branch --show-current → v4-35-armhole-facts · HEAD 69707f6
 2호기 (§0 커밋을 push 한 «뒤» 같은 자리로 전환 — §0-5 에 인쇄)
```

---

## §0 수령·사무

### §0-1 판정문 (전략 세션 · v4-34 §4 수령 · 원문)

```
 「갈래 A 수령 · ★★★ 10.5054 = SEP + band(자의 끝) 확정 · 참깊이 중앙 19.34 · 최대 41.34 —
   함정 등재 수용 · v4-32·33 「이동 전 위반 0」은 SDF 자의 문장이었다는 정정 수용 ·
   측정 판이 잘못된 처방(부분 정합)을 막았다 ·
   ★★★ t* = 0 — 강체 경로 «완전 폐쇄» · 소매 무죄 확정 · 질문은 몸판(암홀 링)으로 ·
   t=0 의 0.0203mm 는 경계 사건(등재만) ·
   ★ 다음 판 = CC 목록 5측정 그대로 · 전략 세션 후보 지정 0 · 갈래 형태만:
     (가) 링이 몸 «안» 구간 존재(상류 = 몸판 배치가 팔을 침범) /
     (나) 밖이지만 두 링 기하 불일치(패턴 질문) / (다) 그 외」
```

### §0-2 바꾸지 않는 것 (회차 프롬프트 원문)

```
 물리 **0줄** · `src/` **0** · 정본·blob·문턱 **0** · 워커 **diff 0** · 격자 용량 **262144** ·
 계기(v4-28~34) **유지** · 자 = `exactBodyDist`(참깊이 · v4-34 와 동일) + SDF 는 **부호만**.
```

### §0-3 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · 단위 명시)

**★ 단위** — 길이 **mm** · 각도 **도** · 부위는 **점 분류**(v4-28 규칙: 팔 = |x| > W/2 · 겨드랑이 = |y − Y_ARM| ≤ ARM_D · 몸통 = 나머지).

| 항 | 갈래 |
|---|---|
| **①** 암홀 링 사실 | **(가)** 링에 **부호 음(몸 안)** 구간 존재(점 수·깊이·부위) / **(나)** 전부 밖이나 기하 문제(값으로) / **(다)** 그 외 |
| **②** 소매 캡 링 사실 | 값 등재(중심·법선·둘레·지름·평면 RMS · 두 링 차) |
| **③** T포즈 대조군 | 같은 채널 전량(재굽기 0) · **「T에서 성립하던 관계 중 A에서 깨진 것」을 값으로 지목**(해석 최소) |

**§4** — A = ①②③ 전량 ⟹ 상신 / B = 측정 불가 항목(사유) ⟹ 나머지 상신 / C = 그 외 ⟹ 정지.

### §0-4 사전 결정 4건 (값 보기 «전» · CC 집행 설계)

```
 ㄱ **링의 정의** — 암홀 링 = 어깨 이음 봉제쌍의 **몸판 쪽** 정점(좌·우 각 52) ·
    캡 링 = 같은 쌍의 **소매 쪽** 정점. v4-24·31~34 와 **같은 가름**(한쪽이 소매·다른 쪽이 몸판).
 ㄴ **법선은 최소제곱 평면에서 뜬다**(손 벡터 0) — 점들의 공분산 행렬에서 **가장 작은 고윳값의 고유벡터**.
    부호는 **팔 축(AX)과 예각이 되도록** 맞춘다(부호 규약만 · 값 창작 0).
 ㄷ **여유의 자는 v4-34 와 같다** — 크기 = `exactBodyDist`(정확 거리) · 부호 = SDF.
    위반 = 부호 있는 여유 < SEP(2.0 mm). **새 문턱 0.**
 ㄹ **①의 갈래 판정 기준을 지금 고정한다** — 링 점 중 **부호가 음(몸 안)인 점이 1개라도 있으면 (가)** ·
    전부 밖이면 **(나)** · 계산 불가면 (다). 「기하 문제」의 판단은 **하지 않는다**(값만 적는다).
```

---

## §1-① 암홀 링 사실 — **(나) 전부 몸 «밖»**(음 점 0 · 다만 경계 위반 1점/측)

| 채널(A포즈 f0) | R | L |
|---|---|---|
| 점 수 | 52 | 52 |
| **음(몸 안) 점** | **0** | **0** |
| **위반(< SEP 2.0) 점** | **1** | **1** |
| 여유 mm | 최소 **1.8640** · p25 22.138 · 중앙 **24.083** · p75 25.787 · 최대 37.619 | 최소 **1.8780** · p25 22.052 · 중앙 24.083 · p75 25.787 · 최대 37.640 |
| 둘레 mm | **763.24** | 763.04 |
| 최대 점쌍(지름 대용) mm | **247.56** | 247.58 |
| 평면 이탈 RMS mm | **8.91** | 8.86 |
| 법선↔팔축 deg | **22.11** | 45.19 ※ |
| 중심↔어깨 피벗 mm | **85.83** | 395.35 ※ |

⟹ **①(나)** — 링에 몸 «안»인 점은 **없다**. 다만 **한 점씩(좌·우)** SEP 를 아슬아슬하게 어긴다
(**1.8640 / 1.8780 mm** · v4-34 가 t=0 에서 본 **1.9797 mm** 와 같은 경계 사건의 링 쪽 얼굴).
※ **계기 한정** — 왼쪽 값은 **x 거울을 적용하지 않은 채** 오른팔 축(AX)·오른쪽 피벗(P)과 견준 수다.
좌·우 비교에는 쓰지 말고 **오른쪽 값만** 읽어야 한다(값은 그대로 등재).

---

## §1-② 소매 캡 링 사실 · 두 링 차 (A포즈)

| 채널 | R | L |
|---|---|---|
| 캡 둘레 mm | **611.08** | 611.08 |
| 캡 지름 mm | **167.53** | 167.53 |
| 캡 평면 이탈 RMS mm | **0.13** | 0.13 |
| **둘레 차(암홀 − 캡)** | **+152.16 mm** | +151.96 |
| **지름 차** | **+80.03 mm** | +80.06 |
| **법선 사이 각** | **26.64°** | 26.70° |
| **중심 거리** | **230.08 mm** | 230.02 |

---

## §1-③ T포즈 대조군 (재굽기 0 · `asm-Tasm_M.bin`)

| 채널 | **T포즈** | **A포즈** | 변화 |
|---|---|---|---|
| 암홀 둘레 mm | 784.22 | 763.24 | −20.98 |
| 캡 둘레 mm | 624.51 | 611.08 | −13.43 |
| **둘레 차(암홀 − 캡)** | **159.70** | **152.16** | −7.54(**거의 같다**) |
| 암홀 지름 mm | 241.73 | 247.56 | +5.83 |
| 캡 지름 mm | 181.27 | 167.53 | −13.74 |
| **지름 차** | **60.46** | **80.03** | **+19.57** |
| 암홀 평면 RMS mm | 8.77 | 8.91 | +0.14 |
| 캡 평면 RMS mm | 2.71 | **0.13** | −2.58 |
| **두 링 법선 사이 각** | **62.26°** | **26.64°** | **−35.62°**(A가 더 «나란») |
| **두 링 중심 거리 mm** | **109.12** | **230.08** | **+120.96(2.11배)** |
| 암홀 여유 최소 mm | **2.0020** | **1.8640** | −0.1380(**SEP 미달로 전환**) |
| 암홀 여유 중앙 mm | 26.604 | 24.083 | −2.521 |
| 음(몸 안) 점 | 0 | 0 | — |
| 위반(< SEP) 점 | **0** | **1**(좌·우 각 1) | +1 |

### 「T에서 성립하던 관계 중 A에서 깨진 것」(값으로만 · 해석 최소)

1. **두 링 중심 거리** — 109.12 → **230.08 mm**(2.11배). 이 판의 채널 중 **가장 크게 벌어진 것**.
2. **암홀 여유의 하한** — 2.0020(= SEP 바로 위) → **1.8640 mm**(SEP 미달) · 위반 점 0 → 1.
3. **지름 차** — 60.46 → **80.03 mm**(캡이 상대적으로 더 작아졌다: 캡 지름 181.27 → 167.53).

### 「깨지지 않은 것」

4. **둘레 차는 거의 같다** — 159.70 ↔ **152.16 mm**(둘 다 캡이 ~150 mm 짧다) ⟹ **A포즈 고유 문제가 아니다**.
5. **법선 사이 각은 오히려 «좋아졌다»** — 62.26° → **26.64°**(A에서 두 링이 더 나란하다).
6. **음(몸 안) 점은 두 포즈 모두 0** — 링 자체가 몸을 파고들지는 않는다.

---

## §2 자기검사

| 항 | 값 |
|---|---|
| `src/` diff vs 기점 | **0**(무출력) · 물리 **0줄** |
| 워커 · `gpu/engine/` | **diff 0** · 정본·blob·문턱 **0** · 격자 용량 262144 유지 · 계기(v4-28~34) **수정 0** |
| 이 판이 **신설**한 것 | `scripts/v4RingFacts.ts`(링 계기) · 산출 `l3ap-ring-v4-35-A.json` |
| 굽기 · 캡처 | **0 · 0** |
| pytest(2호기) | **40 passed · 2 xfailed · 1 xpassed**(59.11 s) |
| `npx tsc -b` | **통과**(종료 0) |
| 브랜치 인쇄 | §0 머리에 에어·2호기 둘 다 · §0 은 집행 **«전»** 커밋(`5ea201c`) |
| ecc 에이전트 / 스킬 | **0 / 0** |
| ★ 계기 한정 2건 | ㉠ 왼쪽 링 값은 x 거울 미적용(오른쪽 값만 유효) · ㉡ `Y_ARM(세계)`·부위 분류는 `P.S` 에 해당 필드가 없어 **산출 불가**(음 점이 0이라 판정에 쓰이지 않았다) |

---

## §4 회차 갈래 — **A**(①②③ 전량 · ① = **(나)**)

**넘기는 것 7건**

1. ★★★ **①(나)** — 암홀 링에 **몸 «안»인 점은 0**(A·T 모두). 「몸판 배치가 팔을 침범한다」는 상류 가설은 **서지 않는다**.
2. ★★★ **가장 크게 깨진 관계 = 두 링 «중심 거리»** — T **109.12** → A **230.08 mm**(**2.11배**).
3. ★★★ **둘레 차는 포즈와 무관하다** — T 159.70 ↔ A 152.16 mm(둘 다 캡이 ~150 mm 짧다) ⟹
   「캡이 짧다」는 **A포즈 고유 문제가 아니다**(패턴의 상수 성질).
4. ★★ **법선 각은 오히려 좋아졌다** — 62.26° → 26.64°. 방향이 아니라 **거리**가 문제다.
5. ★★ **경계 사건의 링 쪽 얼굴** — A포즈 암홀 여유 최소 **1.8640 / 1.8780 mm**(위반 1점씩) ·
   T포즈는 **2.0020 mm**(위반 0). v4-34 의 t=0 **1.9797 mm** 와 같은 자리다.
6. ★★ **지름 차 확대** — 60.46 → 80.03 mm(캡 지름 181.27 → 167.53).
7. ★ **계기 한정 2건**을 §2 에 적었다(왼쪽 거울 미적용 · `Y_ARM` 산출 불가).

**이 판이 하지 않은 판단** — 중심 거리 230 mm 의 처분 · 둘레 차 150 mm 의 처분 · 패턴 질문 · 화면.
