# v4-22 — 프레임 회계 사실 · 회계 맞춘 재실행 · A포즈 탐침 · 캡처

**집행 기계 = 2호기 · 물리 0줄 · 워커 diff = 프레임 회계 «옵션»만 · 정본·blob·문턱 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-22-frames` |
| 기점 | **`29df357`**(= v4-21 §1~§4) + **`58ba8f2`** cherry-pick(= v4-21 sha 자기 기입 · **v4-21 과 같은 규칙**) |
| §0 커밋 | **`e3d8f5a`** |
| §1~§4 커밋 | **`645b1fa`** — 본문 · 보고 파일 · 볼트 3종 · metrics · 회귀 산출 **한 커밋**(코드 diff 0) |
| 보고 파일 | `docs/v4/보고/22.md` |

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v4-22-frames 29df357 → show-current = v4-22-frames · HEAD 29df357
       $ git cherry-pick 58ba8f2 → 2 files changed(문서 sha 기입) · HEAD d7076ed
 2호기 (§0 커밋을 push 한 «뒤» 같은 자리로 전환 — v4-21 과 같은 순서)
```

---

## §0 수령

### §0-1 판정문 (전략 세션 · v4-21 §4 수령 · 원문)

```
 「갈래 C 수령 · ★★ 램프 적중(교차 8→0 · 목선 0.9891 · 게이트 완전 일치 · 중앙 차 10.34→0.63) ·
   ★ 남은 두 행(밑단 +0.632 · 소매 −0.139)의 유력 후보 = «프레임 회계»
   (v4 는 램프 79 «포함» f=180 에서 순변위 9.29e-05 로 멈춤 ↔ v3 정본 정착 순변위 5.87e-05 ·
   dressRun :177 startFrame 이 축 분리를 시사) · 후보이지 단정 0 · 코드 사실로 가른다 ·
   밴드 n 갈림은 v4-16 문제의 재출현 · ③ 적용 밖 유지 · 워커 2모드 보존·§0 선커밋 수용」
```

### §0-2 바꾸지 않는 것 (회차 프롬프트 원문)

```
 물리 **0줄** · `garmentScene` **0** · 정본·blob·문턱 **0** ·
 워커 **기존 3경로**(정착-blob · 조립 · 조립+램프) **diff 0** — 회계는 «옵션»이다.
```

### §0-3 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · 단위 명시)

**★ 단위** — ①은 **프레임**(1/60 s) · ②③은 **칸 1개**.

| 항 | 갈래 |
|---|---|
| **①** 프레임 회계(코드만) | **(가)** 180 = 램프 «뒤» 정착 프레임(총 = 램프 + 180) / **(나)** 180 = 총합(v4 와 같은 회계) / **(다)** 그 외(사실) |
| **②** 회계 맞춘 재실행 | **(가)** 「같은 옷」 성립(③ 적용 행 전부 ≤ 0.05) / **(나)** 여전히 초과(행·값 · 처방 0) / **(다)** 미정착·발산 |
| **③** A포즈 탐침 + 캡처 | **(가)** pass / **(나)** fail(채널·값 · 처방 0) / **(다)** 발산·미정착 |

**§4** — A = ②(가)+③(가) / B = ③(나)(다) / **C = ②(나)(다) ⟹ ③ 착수 0** / D = 그 외 ⟹ 정지.

### §0-4 사전 결정 4건 (값 보기 «전»)

```
 ㄱ **①은 «코드만»이다** — `dressRun.ts` 와 **정본 blob 을 «쓴» 경로**를 파일:줄로 인용한다.
    실행 0 · 추정 0 · 「180 이 무엇인지」를 **쓰는 자리에서** 읽는다.
 ㄴ **②의 「같은 회계」** — ①이 (가)면 v4 를 **램프 프레임을 «빼고»** 세어 v3 와 같은 자리에서
    정착 판정을 시작한다(총 = 램프 + 정착). (나)면 지금 회계가 이미 같으므로 **재실행 없이**
    그 사실을 등재하고 ②는 「회계로 안 닫힌다」로 간다.
 ㄷ **회계는 «옵션»** — 칸 항목에 `"settleFrom": <프레임>` 이 있을 때만 판정 시작점이 옮겨진다.
    없으면 v4-21 의 세 경로가 **한 줄도 안 바뀐 채** 돈다(§2 가 3경로 회귀로 확인).
 ㄹ **정지** — ②(나)(다)면 ③ 착수 0(갈래 C) · 사후 갈래 신설 0 · 처방 0 · 문턱 이동 0.
```

### §0-5 §0 커밋 sha

| | |
|---|---|
| §0 커밋 | **`e3d8f5a`** — 집행 «전» 별도 커밋(이 파일의 §0 만) |

---

## §1 집행

### §1-① 프레임 회계 «사실» — **(나) 180 = 총합**(v4 회계와 같다 · 코드만 · 실행 0)

| # | 물음 | **코드 사실**(파일:줄) |
|---|---|---|
| 1 | 정착 판정 카운터는 어디서 시작하나 · 램프 «중»에도 판정하나 | `scripts/v3GridRun.ts:128` **`let frame = 0`** — 램프 구간을 **포함**해 0부터 센다.<br>`:130-136` 창(`N_WIN`)마다 `runS4Gate(P, before)` 를 부르고 `s4.pass \|\| s4.settleNetM ≤ S4_THRESHOLD.settleNetM` 이면 **break** ⟹ **첫 판정이 f=10** 이고 **램프 중에도 판정한다** |
| 2 | `startFrame` 의 의미 | `:132` `runFrames(P, step, …, frame)` — 넘기는 것은 **누적 프레임**이고, `src/v3/dressRun.ts:177` 이 `setRest(startFrame + f + 1)` 로 쓴다 ⟹ **램프의 «시계»가 창 재시작에도 되감기지 않게** 하는 인자다(`:169-171` 주석 원문: 「이어받는 프레임 번호 — 램프가 되감기지 않게」) |
| 3 | 정본 헤더의 「180」은 무엇인가 | `:144` **`stateBlob(P, frame, P.S.PLACE_SIG)`** — 헤더에 싣는 것은 그 루프의 **누적 `frame`** 이다(램프 포함). 「램프 뒤 프레임」이 아니다 |
| 4 | v3 정본 기본몸 M 의 실제 총 프레임 | 정본 인덱스 `public/v3diag/v3-77/index-shard-mac.json` → `c100-h170-s45_M` = **`{status: 편입, f: 180, gate: pass}`** |

```
 ⟹ **갈래 (나)** — 「180」은 **총합**이고 **램프 79 프레임을 포함**한다.
   v4-21 의 회계(램프 79 포함 · f=180 에서 창 순변위 9.291560e-05 로 정착)와 **같다.**
 ★ **부수 사실 1건(축이 다르다 · 단정 0)** — v3 의 «멈추는 규칙»은 `s4.pass || settleNetM ≤ tol` 인데
   v4 는 **`settle` 하나뿐**이다(`:136` ↔ 워커). 정본 인덱스가 `gate: pass` 이므로 v3 가 **pass 로**
   멈췄을 가능성이 «값으로» 열려 있다. **프레임 회계와는 다른 축이고, 이 판은 그것을 판정하지 않는다.**
```

### §1-② 회계 맞춘 재실행 — **(나) 회계로는 안 닫힌다**(재실행 0 · §0-4ㄴ 등재대로)

①이 **(나)** 이므로 사전 결정 ㄴ 이 정한 대로 **재실행하지 않았다** — 「(나)면 지금 회계가 이미 같으므로
재실행 없이 그 사실을 등재하고 ②는 「회계로 안 닫힌다」로 간다」.

| 채널 | v3 정본 | v4-21 램프판 | 판정 |
|---|---|---|---|
| 총 프레임 | **180**(램프 79 포함) | **180**(램프 79 포함) | **같다** |
| 판정 시작 | 창 경계 f=10부터 | 창 경계 f=10부터 | **같다** |
| 램프 시계 | `setRest(startFrame+f+1)` 연속 | `t = (f+1)/RAMP_N` 연속 | **같다** |
| 남은 중앙 차(③ 적용 행) | — | **밑단 +0.632179** · **소매 −0.139005** | **문턱 0.05 초과 유지** |

```
 ⟹ **갈래 (나)** — 프레임 회계는 **이미 같았고**, 남은 두 행은 **그것 때문이 아니다**.
   판정문이 「유력 후보」로 든 축이 **값으로 닫혔다**(후보 제거 · 원인 단정 0 · 처방 0).
 ★ 남은 후보는 이 판이 «세우지 않는다» — 부수 사실(멈추는 규칙 `pass` ↔ `settle`)과
   v4-16 이 등재한 **밴드 경계 정점** 문제가 둘 다 열려 있다. 어느 쪽으로 갈지는 전략 세션 몫이다.
```

### §1-③ A포즈 탐침 + 캡처 — **착수 0**(갈래 C · §0-3)

②가 **(나)** 이므로 §4 의 C 대로 **착수하지 않았다**. **캡처 0**(찍을 대상 부재 · 사후 갈래 신설 0).

---

## §2 자기검사

| 채널 | 값 |
|---|---|
| **워커 diff** | **0** — ①이 (나)라 **회계 옵션을 «넣지 않았다»**(`git diff 29df357 -- gpu/bake/worker.py` = 빈 출력) ⟹ **3경로는 정의상 보존** |
| 3경로 회귀(값) | 정착-blob 모드 `3d8fac29f90fbfe4` **비트 동일** · 조립 모드(램프 없음) `0d833601326922e2` **비트 동일** · 램프 경로 = v4-21 §1 산출 그대로(같은 파일·같은 워커) |
| 물리 diff | **0줄** |
| `src/` diff · 정본·blob·문턱 | **0** |
| `py -m pytest gpu/tests -q` | **40 passed · 2 xfailed · 1 xpassed**(59.06 s) |
| `npx tsc -b` | **통과** |
| 브랜치 · §0 | 두 기계 `v4-22-frames` · **§0 선커밋 `e3d8f5a`**(sha 자기 기입 `8fb13dc`) — **집행 «전»** |

---

## §4 회차 갈래 — **C**(②(나) ⟹ 값 상신 · ③ 착수 0)

| 항 | 갈래 | 한 줄 |
|---|---|---|
| ① 프레임 회계 | **(나)** | **180 = 총합**(램프 79 포함) — `v3GridRun.ts:128·132·136·144` + 정본 인덱스 `f: 180` |
| ② 회계 맞춘 재실행 | **(나)** | 회계가 **이미 같다** ⟹ 재실행 0 · 남은 **밑단 +0.632179 · 소매 −0.139005** 는 회계 탓이 아니다 |
| ③ A포즈 탐침·캡처 | **착수 0** | 갈래 C |

**넘기는 것 4건**

```
 1. ★★★ **「프레임 회계」 후보가 값으로 닫혔다** — v3 의 180 은 램프를 «포함한» 총합이고
    판정 시작점(f=10)·램프 시계까지 v4 와 같다. 남은 두 행은 **다른 데서 온다**.
 2. ★★ **부수 사실 — 멈추는 «규칙»이 다르다**(축이 다르다 · 단정 0) — v3 는 `pass || settle`,
    v4 는 `settle` 뿐이다. 정본 인덱스가 `gate: pass` 라 v3 가 **pass 로 멈췄을 수 있다**.
    같은 프레임(180)에 멈췄으니 회계 문제는 아니지만, **어느 조건이 먼저 걸렸는지는 열려 있다.**
 3. ★★ **밴드 경계 문제가 여전히 두 행을 ③ 적용 밖으로 민다**(가슴 266/264 · 허리 226/224 · v4-16).
 4. ★ **워커 diff 0** — 회계 옵션을 넣지 않아 3경로가 그대로 보존됐다(넣을 이유가 값으로 사라졌다).
```

**이 판이 하지 않은 판단** — 남은 0.632/0.139 mm 의 원인 · 멈추는 규칙의 처분 · 밴드 경계 처분 ·
A포즈 탐침 · 캡처 경로. **처방 0 · 문턱 이동 0 · 사후 갈래 신설 0.**
