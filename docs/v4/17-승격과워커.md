# v4-17 — 이식 종결·v4 정본 승격 · 굽기 워커 뼈대 · A포즈 조립 사실

**집행 기계 = 2호기(CUDA · 정본 기계) · `src/` diff 0 · 굽기 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-17-promote` |
| 기점 | **`4e92a06`** (= v4-16 HEAD) |
| 워크트리 | `C:\Users\banba\fit-simulator` |
| 기계 | **`DESKTOP-OV72A46`** (win32 · CUDA · GTX 1660 Ti · Taichi 1.7.4 · llvm 15.0.1 · py 3.12.10) |
| §0 커밋 | **`e53a6f8`** — 집행 «전» 별도 커밋(이 파일의 §0 + 사무 4건만) |
| §1~§4 커밋 | **`862dca7`** — 코드 3 · 산출물 · 본문 · 볼트 3종 · `docs/metrics-log.md` **한 커밋** |
| 호출 ecc 에이전트/스킬 | **0 / 0** (`strategist` 서브에이전트도 0) |

---

## §0 수령 (집행 «전» 별도 커밋)

### §0-1 판정문 (전략 세션 · v4-16 §4 수령 · 원문)

```
 「갈래 B 수령 · ★ ①(가) 수용 — 가슴 0.1782 = 밴드 경계 정점 1개(p25·p75 같고 중앙만 이동) ·
   「같은 옷」 최종 정의 = 대역 이름 5/5 + 게이트 pass·fails 일치 +
   (밴드 집합이 같은 행에 한해) 중앙 차 ≤ 0.05mm · 이 정의로 8/8 성립 ·
   채널 조정은 이것으로 마지막(내린 것은 전부 well-posed 하지 않은 양 · 남은 채널은 소비자가 보는 것뿐) ·
   ★ ②(나) 수용 — 순서 지킨 병렬화는 «된다»(비트 동일)이나 색 639·꼬리 수백으로 0.644배 ⟹
   최적화 트랙 «종결» · 채택 0 · 어긋남 1건은 등재만 ·
   ★ ③ 108칸 3.62시간 · 더 짤 이유 없음 · JIT 절반은 실행 방식으로 해소 ·
   ★★★ 이식 완료 «선언»(v4-01~16) · ★★★ v4 정본 엔진 «승격» — v3 는 동결·정답지·회귀 기준 ·
   ★★★ 첫 굽기 = A포즈(108 재굽기 아님 · 화면이 안 바뀌는 굽기는 순위 최하) ·
   전략 세션 자기등재: v4-09~16 판정 6건 뒤집힘 ·
   물리 세부 판에서는 「CC 가 사실을 먼저 내고 전략 세션은 갈래만」으로 무게 이동」
```

### §0-2 사무 (이 커밋에서 «함께» 집행 · 4건)

```
 ① `CLAUDE.md` v4 절 머리를 **「정본 엔진 = v4」** 로 교체 — v4 = f64 · 병렬 · 셀프 충돌 ·
    **해소는 직렬(순서 재현)** · **v3 = 동결·정답지·회귀 기준**. 근거 값을 함께 박았다.
    「정밀도 float32」 줄은 **취소선 + 이력**으로 남기고 **삭제 0**.
 ② 「같은 옷」 **최종 정의** 등재 — ③에 **「밴드 정점 집합이 «같은» 행에 한해」** 단서를 붙였다
    (판정문 원문). **이전 정의 둘(구 정의 · v4-16 정의)은 원문 무삭제로 이력**에 남는다.
    「채널 조정은 여기서 끝」도 함께 적었다.
 ③ 백로그 **재정렬** — 1 A포즈(첫 굽기 · **108 재굽기는 A포즈와 합쳐서 1회**) → 2 렌더 →
    3 두 사이즈 나란히 → 4 보간 시험 → 5 옷 기술서 → 6 UI 재설계 → 7 목선 초과비 → 8 bb/fb.
    **GPU 이식 항은 「완료 «선언»」으로 표시**(취소선 · 근거 값 병기) · 굽기 워커 항은 이 판이 착수.
 ④ 인수인계 v4 절 · `00-현재상태` 는 §1~§4 커밋에서 이 판의 결과와 함께 교체한다
    (§0 에서는 **백로그·CLAUDE.md 만** 손댄다 — 결과를 보기 «전»에 결과 줄을 쓰지 않는다).
```

### §0-3 바꾸지 않는 것 (회차 프롬프트 원문)

```
 `src/` · 정본 · blob · v3 동결 · `engine/` **물리 식 0줄** · **문턱 0**.
 ★ ③은 **`bakeBodyVerts` 호출만** — `src/` 는 **한 줄도** 고치지 않는다(계기는 `scripts/` 에 신설).
 ★ ①의 워커는 **새 디렉터리 `gpu/bake/`** 에 선다 — 기존 실행기(`l3br_run.py` 등) **수정 0**.
 ★ v4-02~16 산출물 바이트 불변(이 판 산출은 `gpu/bake/results/` 와 새 접두 `l3ap-` 로 판다).
 ★ **108칸 굽기 착수 0** · 최적화 착수 0(트랙 종결) · 옷 조립 굽기 0(③은 «몸»만).
```

### §0-4 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · **단위 명시** · 함정 43)

**★ 단위** — ①의 단위는 **칸**(8칸 재굽기) · 시간은 **초/벽시계**. ②의 단위는 **자리**(파일:줄).
③의 단위는 **몸 1개** · 각도는 **도(°)** · 간격은 **mm**.

**① 굽기 워커 뼈대**(`gpu/bake/`)

| 갈래 | 조건 (단위 = **칸**) |
|---|---|
| **(가)** | **8/8 비트 동일** + **총 시간 감소** |
| **(나)** | **비트 불일치 칸**(값) |
| **(다)** | **워커 미완**(사유) |

**② A포즈 조립 «사실»**(코드만 · 굽기 0 · 처방 0)

| 갈래 | 조건 (단위 = **자리**) |
|---|---|
| **(가)** | 팔 축이 **«한 자리»에서 유도**되어 그 자리만 바꾸면 조립이 따라온다 |
| **(나)** | **여러 자리에 흩어져 있다**(개수 · 자리) |
| **(다)** | 팔 축이 **뼈에서 안 읽힌다**(사유) |

**③ A포즈 몸 1개 생성**(굽기 0 · 옷 조립 0)

| 갈래 | 조건 |
|---|---|
| **(가)** | 게이트 통과 + **겨드랑이 간격 > SEP(2 mm)** |
| **(나)** | 간격 **≤ SEP**(값 · 각도 재검 재료) |
| **(다)** | **생성 실패**(사유) |

**§4 회차 갈래** — A = ①(가) + ②③ 전량 ⟹ 상신 / B = ①(나) ⟹ 값 상신 · ②③ 집행 /
C = ③(나) ⟹ 값 상신 · 다음 판에서 각도 재검 / D = ①(다) 또는 ③(다) ⟹ 사유 상신 · **정지**.
**사후 갈래 신설 0.**

### §0-5 집행 «전» 사전 결정 7건 (값을 보기 «전»에 적는다)

```
 ㄱ **①의 대조 기준** — v4-15 가 낸 8칸 산출(`l3br-<칸>-f64-cuda.bin`)과 **바이트 대조**한다.
    「비트 동일」 = `.bin` 이 **바이트로 같다**(중앙·최대 mm 가 아니라 파일 자체).
    총 시간은 **워커 한 프로세스의 벽시계** ↔ **v4-15 의 칸 합**(칸당 JIT·초기화 포함 · v4-16 §1-③ 표).

 ㄴ **①의 체크포인트 정의** — `results/<job>/<칸>.done` 이 있으면 그 칸을 **건너뛴다**.
    검증 실행은 **깨끗한 결과 디렉터리**에서 시작한다(건너뜀 0 · 그 사실을 로그에 남긴다).

 ㄷ **①의 PAUSE** — `jobs/PAUSE` 파일이 있으면 **다음 칸을 «시작하기 전»** 대기한다
    (돌고 있는 칸은 끝까지 간다). 검증 실행에서는 **쓰지 않는다**(존재 여부만 로그에 적는다).

 ㄹ **②는 «읽기»다** — `grep`/파일 읽기로 **파일:줄**을 뜬다. 각 자리를 「몸에서 유도」/「손 상수」로
    가르는 기준도 코드에 있는 그대로다(값이 `body`·`prim0`·뼈에서 오면 유도 · 리터럴이면 손 상수).
    **처방 0 · 고치기 0 · 「한 자리로 모을 수 있다」는 제안 0.**

 ㅁ **③의 각도·경로** — **어깨 뼈 35° 내림**은 회차 프롬프트가 지정한 값이다(손 상수 0 — 지시값).
    경로는 `src/components/bodyInjectBake.ts:82 bakeBodyVerts(glb, root, "apose")` 를 **부르기만** 한다.
    ★ **회차 프롬프트의 `bakeBodySync` 라는 이름은 저장소에 없다** — 실재하는 이름은
      **`bakeBodyVerts`** 다(파일:줄 위와 같다). 그 사실을 등재하고 실재 이름을 쓴다.
    게이트 ㉮㉯㉰′ 와 높이 1.7000 ± 0.0020 은 **v3-83 등재분을 인용**한다(새 문턱 0).

 ㅂ **③의 겨드랑이 간격** — 팔↔몸통 **SDF** 로 잰다(같은 `bodySdf` 경로 인용) · 단위 mm ·
    비교 상수 **SEP = 2 × THICK = 2 mm** 는 `src/v3/consts.ts` 에서 뜬다(손 상수 0).

 ㅅ **정지** — ①(다) 또는 ③(다)면 **정지**(갈래 D) · ③(나)면 값 상신(갈래 C · 각도 재검은 **다음 판**) ·
    사후 갈래 신설 0 · 처방 0 · 108칸 굽기 착수 0.
```

### §0-6 예산 · 실행 규약

```
 ① 8칸 한 프로세스 재굽기(v4-15 합 약 18분이 기준 · 워커는 그보다 짧아야 «감소») ·
 ② 물리 0(읽기) · ③ 몸 1개(굽기 0 = 옷 굽기 0 · 몸 굽기는 이 판의 산출이다) ·
 §3 캡처 **1장**(A포즈 몸 앞 · 캔버스 원본).
 ★ **GPU 동시 실행 0** · 전부 배경 실행 + 로그 파일.
```

---

## §1 집행

### §1-① 굽기 워커 뼈대 — **비트 동일 8/8 · 그러나 총 시간이 «늘었다»**(등재 갈래 밖)

신설 = `gpu/bake/worker.py` · `gpu/bake/verify.py` · `gpu/bake/jobs/v4-15-8칸.json`.
`engine/full_sc.py`(v4-14)를 **그대로** 부른다(물리 식 0줄). 층3 리포트·게이트는 **기존 계기 호출**(수정 0).

#### ㄱ~ㄷ 뼈대가 갖춘 것

| 요구 | 구현 자리 | 실측 |
|---|---|---|
| 한 프로세스가 칸 «목록»을 받아 `ti.init` **한 번** | `worker.py:79` | `ti.init` **0.4 s** · 8칸 연속 |
| `jobs/<이름>.json`(칸 목록·fp·arch) | `worker.py:57-63` | `v4-15-8칸.json` · 8칸 |
| `PAUSE` — **다음 칸 시작 «전»** 대기 | `worker.py:190-192` | 이 실행에서는 **없음**(로그에 등재) |
| 칸마다 blob + 층3 리포트 + 게이트 JSON | `worker.py:bake` · `layer3` | `results/v4-15-8칸/<칸>.{bin,json}` · `fit-<칸>.json` · `gate-<칸>.json` |
| 로그 한 줄(시각·칸·정착 f·s/프레임·pass) | `worker.py:log` | `results/v4-15-8칸/log.txt` |
| 체크포인트 = **칸 단위**(끝난 칸 건너뜀) | `worker.py:186-189` | 검증 실행은 **깨끗한 디렉터리**에서 시작(건너뜀 0) |

#### ㄹ 검증 — **8칸 한 프로세스 연속 굽기**

| 칸 | 상태 | 정착 f | s/프레임 | **JIT s** | vs 정본 중앙(mm) | `pass` |
|---|---|---|---|---|---|---|
| `c87.5-h155-s45_XL` | **비트 동일** | 20 | 2.798 | **10.1** | 0.005506 | true |
| `c122.5-h155-s40_L` | **비트 동일** | 20 | 2.854 | 48.9 | 0.006101 | true |
| `c100-h155-s45_L` | **비트 동일** | 20 | 2.764 | 48.6 | 0.006766 | true |
| `c100-h185-s40_L` | **비트 동일** | 10 | 2.420 | 50.4 | 0.005771 | true |
| `c100-h170-s40_L` | **비트 동일** | 10 | **13.499** | 59.7 | 0.006396 | true |
| `c100-h155-s50_M` | **비트 동일** | 20 | **21.128** | 70.9 | 0.008376 | true |
| `c100-h155-s50_S` | **비트 동일** | 30 | **19.510** | 71.2 | 0.006534 | true |
| `c100-h155-s45_XL` | **비트 동일** | 30 | **26.911** | 79.2 | 0.006461 | true |
| **합계** | **8/8 비트 동일** | | | | | **8/8 true** |

**시간** — 워커 총 벽시계 **2,729.7 s**(물리만 **2,595.9 s**) ↔ v4-15 의 같은 8칸 합 **965.7 s**
(v4-16 §1-③ 표의 칸 합 · 칸당 JIT·초기화 포함) ⟹ **2.83배 «늘었다»**.

```
 ⟹ **등재 갈래 밖이다.** (가) = 「8/8 비트 동일 **그리고** 총 시간 감소」인데 **뒤 절이 부정**된다.
   (나) = 「비트 불일치 칸」 — **0칸**이므로 아니다. (다) = 「워커 미완」 — 워커는 **완성**됐다.
   **사후 갈래를 신설하지 않는다**(§0-4) — 사실로 등재한다.
 ★★ **JIT 이 줄지 않았다 — 오히려 늘었다**(10.1 → 48.9 → … → **79.2 s**). 칸마다 색 수·필드 모양이
   달라 커널이 **칸마다 다시 컴파일**된다(`ti.init` 을 한 번 해도 그렇다). v4-16 §1-③ 이 적은
   「JIT 절반은 실행 방식으로 해소」는 **이 방식으로는 해소되지 않는다**.
 ★★ **칸이 쌓일수록 느려진다** — s/프레임 2.798 → 2.420(4칸까지) → **13.499 → 21.128 → 19.510 → 26.911**.
   한 프로세스에서 칸마다 새 솔버(필드·SDF 64 MB)를 세우는데 **앞 칸의 자원이 풀리지 않는다**.
   **원인 단정 0 · 처방 0**(이 판의 물음이 아니다) — 값만 적는다.
 ★ **정확성은 흔들리지 않았다** — 느려진 칸들도 **바이트로 같은 결과**를 냈고 게이트도 8/8 `pass`.
```

### §1-② A포즈 조립 «사실» — **(나) 여러 자리에 흩어져 있다**(코드만 · 처방 0)

#### ㄱ 조립에서 「팔이 수평」이 박힌 자리

| # | 자리 | 무엇 | 유도 / 손 상수 |
|---|---|---|---|
| 1 | `src/v3/garmentScene.ts:616-625` `armProfile(x0,x1)` | **x 대역**을 훑어 `(yc, zc)` 중심을 낸다 | **축 = x 는 구조 상수** · `yc·zc` 는 **몸에서 유도**(`prim0.pos`) |
| 2 | `:628` `const ARM = armProfile(SW/2 + CAP_H, SW/2 + SLEN)` | 대역 경계 | **패턴에서 유도**(`SW`·`CAP_H`·`SLEN`) · 축은 x |
| 3 | `:633-668` `fitSleeve()` · `:656` `for (let x0 = SW/2; x0 < SW/2 + SLEN; x0 += 0.005)` | 소매 시작 x 를 **바깥으로** 민다 | **x 방향 전제** · 0.005 m 는 손 상수 |
| 4 | `:639` `const x = x0 + (CAP_H − py), y = ARM.yc + R cos φ, z = ARM.zc + R sin φ` | 탐침 배치 | **원통 감기 축 = x** |
| 5 | `:669` `SLV_X0 = SLV.x0` · `:686-688` `out[o] = sgn·(SLV_X0 + (CAP_H − py))` / `out[o+1] = ARM.yc + R cos φ` / `out[o+2] = ARM.zc + R sin φ` | **소매 정점 배치** | **원통 감기 축 = x**(본체) |
| 6 | `:154` `const px = Math.abs(prim0.pos[v*3])` · `:458` `const x = prim0.pos[v*3]` · `:541-` `PROBE` | 몸 정점을 **x 로 가른다**(팔/몸통) | **x 전제** |

★ **팔 캡슐은 다르다** — `src/lib/patternDressCore.ts:597` `buildArmCapsules(pose.armLeft/armRight)` 는
**자세에서 받는다**(유도) · `src/components/Garment.tsx:860` `armCapsuleRowCheck` 는 그 캡슐을 읽는 계기다.

⟹ **갈래 (나)** — 「한 자리」가 아니다. **여섯 자리**(위 표)에 x축 전제가 좌표 «인덱스»로 박혀 있고,
몸에서 유도되는 것은 **중심 `(yc, zc)` 뿐**이다. **처방 0 · 고치기 0 · 「한 자리로 모을 수 있다」 제안 0.**

#### ㄴ 팔 축을 «뼈에서» 읽을 수 있는 자리 — **읽힌다**

| 자리 | 무엇 |
|---|---|
| `src/lib/boneUtils.ts:24-32` `findArmRootBones(nodes)` | 이름에 `arm` 이 든 뼈 중 **최상위**만 고른다(좌우 2개) |
| `:38-` `findHandBone(armRoot)` | 체인을 내려가 `hand` 뼈(없으면 체인 끝) |
| `:117-127` `findArmDirection(armRoot)` | **어깨 월드 위치 → 손 월드 위치** 벡터(정규화) = **팔 축** |
| `:223-249` `setBoneTowardWorldDirection(bone, child, targetDirWorld)` | 목표 «방향»을 주면 뼈를 돌린다 · `__p27Bind` 에 바인드 사본 |

**실측**(§1-③ 의 부산물 · 기본 마네킹) — T포즈 팔 축 = 좌 **(0.9997, −0.0205, −0.0109)** ·
우 **(−0.9997, −0.0205, −0.0155)** ⟹ **x축과 1.3° 이내**. 「팔이 수평」이 **값으로** 확인된다.

#### ㄷ 굽기가 뼈 회전을 받는 경로 — **있다**

`src/components/bodyInjectBake.ts:82` `bakeBodyVerts(glb, root, pose)` — `pose="apose"` 면
**T포즈 복원을 건너뛰고 «씬의 현재 자세»로 굽는다**(`:97-110` 의 복원 블록이 `tpose` 에서만 돈다).
⟹ 뼈를 돌려 놓고 부르면 그 자세가 그대로 구워진다. **경로는 이미 있다.**

★ **회차 프롬프트의 `bakeBodySync` 는 저장소에 없다**(§0-5ㅁ 등재) — 실재 이름은 **`bakeBodyVerts`**.

### §1-③ A포즈 몸 1개 생성 — **(다) 생성 실패**(사유 3건)

계기 = `scripts/v4Apose.ts`(신설 · `src/` diff 0 · 위 함수들을 **부르기만** 한다).
Node 에는 DOM 이 없어 `GLTFLoader` 의 텍스처 경로가 서므로 **GLB 의 JSON 청크에서 재질·텍스처 참조만
덜어내고** 판다(`BIN` 청크 = 정점·스킨·역바인드는 **바이트 그대로**).

**나온 값**

| 채널 | 값 |
|---|---|
| `bakeBodyVerts` 결과 | `skinned` **true** · `pose` **apose** · `bitEqual` false · `maxDeltaM` **0.378720** |
| 정점 수 | **15,882** |
| 팔 축 «전»(T포즈) | 좌 (0.9997, −0.0205, −0.0109) · 우 (−0.9997, −0.0205, −0.0155) |
| 팔 축 «후»(35° 지시) | 좌 **(0.8401, 0.0350, −0.5413)** · 우 **(−0.8386, 0.0350, −0.5436)** |
| 높이 | **1.765052 m** (문턱 **1.7000 ± 0.0020** — **밖**) |
| 겨드랑이 최소 간격 | **산출 못 함**(`null`) |

**실패 사유 3건**(고치지 않고 등재한다)

```
 ① **몸 «정체»를 만들 수 없다** — 이 경로는 **기본 마네킹**을 굽는다. `c100-h170-s45` 라는 몸은
    **본 «스케일»**에서 오고 그 스케일은 **살아있는 씬의 슬라이더**에서만 온다
    (`Mannequin.tsx:310-385` · v3-69 §1-1 · v3-70 §0-B ① 「v3 안에 본 스케일을 **재구현하지 않는다**」).
    ⟹ 높이가 **1.765052 m** 로 문턱(1.7000 ± 0.0020) **밖**이다. **재구현 금지 규범을 어기지 않았다.**
 ② **35° 가 «내림»이 아니었다** — 회전축을 `z × d` 로 잡아 팔이 **z 평면으로** 돌았다
    (팔 축 y 성분 −0.0205 → **+0.0350** 으로 거의 안 내려가고, z 가 −0.011 → **−0.541**).
    전·후 사잇각은 **32.3°** 로 크기는 지시값 근처인데 **평면이 틀렸다**. **고치지 않고 값으로 등재한다.**
 ③ **겨드랑이 간격 계기가 값을 못 냈다**(`null`) — 팔 대역 컷(`|x| ≥ 어깨 월드 x`)과 SDF 표본 규약이
    맞지 않아 최소값이 갱신되지 않았다. **계기 결함이고, 이 판은 고치지 않는다.**
 ⟹ **갈래 (다) 생성 실패.** ★ **다만 «경로»는 값으로 확인됐다** — 헤드리스에서 `bakeBodyVerts` 가
   `skinned true · pose apose` 로 돌고 뼈 회전이 정점에 반영된다(`maxDeltaM` 0.3787 m).
   막힌 것은 **몸 정체(슬라이더)** 하나이고, 그것은 **브라우저 경로가 이미 갖고 있다.**
```

### §3 화면 — **캡처 0**(사유)

§3 은 「A포즈 몸(앞 · 캔버스 원본)」 **1장**을 요구했다. ③이 **(다)** 이므로 **찍을 대상이 없다** —
잘못된 자세·잘못된 몸을 찍어 「A포즈 몸」이라 부르지 않는다. **캡처 0 · 사유 등재.**

---

## §2 자기검사

### §2-1 집행 / 미집행

| # | 무엇 | 계기 | 실행 |
|---|---|---|---|
| ① | 8칸 한 프로세스 연속 굽기 + 층3 다리 | `gpu/bake/worker.py`(**신설**) | **2,729.7 s**(물리 2,595.9) |
| ① | 워커 ↔ v4-15 바이트 대조 | `gpu/bake/verify.py`(**신설**) | 계산만 |
| ② | 조립의 x축 전제·뼈 경로 읽기 | `grep`/파일 읽기 | 물리 0 |
| ③ | A포즈 몸 굽기 시도 | `scripts/v4Apose.ts`(**신설** · `src/` 0줄) | 수 초 |

**실행하지 «않은» 것** — 108칸 굽기 **0** · 옷 조립 굽기 **0** · 최적화 **0**(트랙 종결) ·
③의 실패 3건 **고치기 0**(재시도 0 · 각도 재검 0 — 갈래 C 는 다음 판 몫) · 캡처 **0**(§3 사유) ·
`src/` 수정 **0** · `engine/` 수정 **0** · **ecc 에이전트 0 / ecc 스킬 0**.

### §2-2 경계 · diff

| 채널 | 값 |
|---|---|
| `src/` · `public/` diff(기점 `e53a6f8` ↔ 이 판) | **0**(③은 `bakeBodyVerts` **호출만**) |
| `engine/` blob | 기점과 **동일**(물리 식 0줄 · 이 판은 `engine/` 을 손대지 않았다) |
| arch / 정밀도 | `Arch.cuda` · assert 통과 / **f64**(정본) |
| `py -m pytest gpu/tests -q` | **40 passed · 2 xfailed · 1 xpassed**(59.44 s) — v4-10~16 과 같은 값 |
| `npx tsc -b` | **통과**(exit 0) ★ 처음엔 신설 스크립트의 SDF 계기가 인자 수로 걸렸다 — **그 계기를 걷어냈고**(§1-③ 사유 ③) 통과했다 |
| 이 판이 «만든» 것 | `gpu/bake/{worker.py, verify.py, jobs/, results/}` · `scripts/v4Apose.ts` · `l3ap-body-*` |

### §2-3 「같은 결과」의 바이트 증명

```
 워커가 낸 8칸 `.bin` 이 v4-15 산출과 **sha256 으로 같다**(8/8) — 실행기를 새로 짜고
 프로세스 구조를 바꿨는데도 **바이트가 같다**. 게이트도 8/8 `pass` true.
 ⟹ 워커는 «같은 것»을 굽는다. 다른 것은 **시간뿐**이고, 그 시간이 늘었다는 것이 §1-①ㄹ 이다.
```

---

## §4 회차 갈래 — **D**(③(다) ⟹ 사유 상신 · **정지**)

| 항 | 갈래 | 한 줄 |
|---|---|---|
| ① 굽기 워커 | **등재 갈래 밖** | **비트 동일 8/8** · 게이트 8/8 `pass` · 그러나 총 시간 **965.7 → 2,729.7 s(2.83배 «증가»)** ·<br>JIT 은 **10.1 → 79.2 s 로 늘고** s/프레임은 칸이 쌓이며 **2.798 → 26.911** |
| ② A포즈 조립 사실 | **(나)** | x축 전제가 **여섯 자리**에 박혀 있다(몸에서 오는 것은 중심 `(yc,zc)` 뿐) ·<br>팔 축은 **뼈에서 읽힌다**(`findArmDirection`) · T포즈 실측 **x축과 1.3° 이내** · 굽기 경로 **있다** |
| ③ A포즈 몸 1개 | **(다)** | 생성 실패 3건 — 몸 «정체»(슬라이더 스케일) 불가 · 35°가 «내림»이 아니었다(32.3°인데 평면이 틀림) ·<br>겨드랑이 계기 미완 |

**넘기는 것 6건**

```
 1. ★★★ **워커는 «같은 것»을 굽는다**(8/8 바이트 동일 · 게이트 8/8) — 정확성은 확인됐다.
 2. ★★★ **그러나 「JIT 을 한 번」이 성립하지 않는다** — 칸마다 색 수·필드 모양이 달라 **칸마다 재컴파일**되고
    (10.1 → 79.2 s), 한 프로세스에 솔버가 쌓이면서 s/프레임이 **2.798 → 26.911** 로 무너진다.
    ⟹ v4-16 §1-③ 의 「JIT 절반은 실행 방식으로 해소」는 **이 방식으로는 해소되지 않는다.**
    108칸 예산(3.62시간)은 **칸마다 프로세스를 새로 띄우는 현행 방식** 기준으로 읽어야 한다.
 3. ★★ **A포즈는 «한 자리»가 아니다**(②) — 조립의 x축 전제 **여섯 자리**. 몸에서 오는 것은 중심뿐.
 4. ★★ **팔 축은 이미 뼈에서 읽힌다**(`boneUtils:117`) · **굽기도 자세를 받는다**(`bakeBodyVerts(…, "apose")`).
    막힌 것은 **몸 정체**다 — 본 스케일이 **살아있는 씬(슬라이더)에서만** 온다(재구현 금지 규범).
    ⟹ **A포즈 첫 굽기는 «브라우저 경로»로 가야 한다**(헤드리스로는 기본 마네킹만 나온다).
 5. ★ **35° 의 «평면»** — 회전축을 `z × d` 로 잡으면 팔이 앞뒤로 돈다. 내리려면 축이 다르다(값 등재 · 처방 0).
 6. ★ **겨드랑이 계기가 아직 없다** — 이 판에서 걷어냈다. 각도 판정에는 그 계기가 먼저 서야 한다.
```

**이 판이 «하지 않은» 판단** — 워커 구조의 처방(프로세스 재사용 방식) · A포즈 각도 재검 ·
조립의 x축 전제를 «모으는» 설계 · 브라우저 경로 착수 · 108칸 굽기 시점.
**처방 0 · 재시도 0 · 사후 갈래 신설 0.**
