# v4-17 — 이식 종결·v4 정본 승격 · 굽기 워커 뼈대 · A포즈 조립 사실

**집행 기계 = 2호기(CUDA · 정본 기계) · `src/` diff 0 · 굽기 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-17-promote` |
| 기점 | **`4e92a06`** (= v4-16 HEAD) |
| 워크트리 | `C:\Users\banba\fit-simulator` |
| 기계 | **`DESKTOP-OV72A46`** (win32 · CUDA · GTX 1660 Ti · Taichi 1.7.4 · llvm 15.0.1 · py 3.12.10) |
| §0 커밋 | 집행 «전» 별도 커밋(이 파일의 §0 + 사무 4건만) |
| §1~§4 커밋 | (집행 후 자기 기입) |
| 호출 ecc 에이전트/스킬 | (집행 후 기입) |

---

## §0 수령 (집행 «전» 별도 커밋)

### §0-1 판정문 (전략 세션 · v4-16 §4 수령 · 원문)

```
 「갈래 B 수령 · ★ ①(가) 수용 — 가슴 0.1782 = 밴드 경계 정점 1개(p25·p75 같고 중앙만 이동) ·
   「같은 옷」 최종 정의 = 대역 이름 5/5 + 게이트 pass·fails 일치 +
   (밴드 집합이 같은 행에 한해) 중앙 차 ≤ 0.05mm · 이 정의로 8/8 성립 ·
   채널 조정은 이것으로 마지막(내린 것은 전부 well-posed 하지 않은 양 · 남은 채널은 소비자가 보는 것뿐) ·
   ★ ②(나) 수용 — 순서 지킨 병렬화는 «된다»(비트 동일)이나 색 639·꼬리 수백으로 0.644배 ⟹
   최적화 트랙 «종결» · 채택 0 · 어긋남 1건은 등재만 ·
   ★ ③ 108칸 3.62시간 · 더 짤 이유 없음 · JIT 절반은 실행 방식으로 해소 ·
   ★★★ 이식 완료 «선언»(v4-01~16) · ★★★ v4 정본 엔진 «승격» — v3 는 동결·정답지·회귀 기준 ·
   ★★★ 첫 굽기 = A포즈(108 재굽기 아님 · 화면이 안 바뀌는 굽기는 순위 최하) ·
   전략 세션 자기등재: v4-09~16 판정 6건 뒤집힘 ·
   물리 세부 판에서는 「CC 가 사실을 먼저 내고 전략 세션은 갈래만」으로 무게 이동」
```

### §0-2 사무 (이 커밋에서 «함께» 집행 · 4건)

```
 ① `CLAUDE.md` v4 절 머리를 **「정본 엔진 = v4」** 로 교체 — v4 = f64 · 병렬 · 셀프 충돌 ·
    **해소는 직렬(순서 재현)** · **v3 = 동결·정답지·회귀 기준**. 근거 값을 함께 박았다.
    「정밀도 float32」 줄은 **취소선 + 이력**으로 남기고 **삭제 0**.
 ② 「같은 옷」 **최종 정의** 등재 — ③에 **「밴드 정점 집합이 «같은» 행에 한해」** 단서를 붙였다
    (판정문 원문). **이전 정의 둘(구 정의 · v4-16 정의)은 원문 무삭제로 이력**에 남는다.
    「채널 조정은 여기서 끝」도 함께 적었다.
 ③ 백로그 **재정렬** — 1 A포즈(첫 굽기 · **108 재굽기는 A포즈와 합쳐서 1회**) → 2 렌더 →
    3 두 사이즈 나란히 → 4 보간 시험 → 5 옷 기술서 → 6 UI 재설계 → 7 목선 초과비 → 8 bb/fb.
    **GPU 이식 항은 「완료 «선언»」으로 표시**(취소선 · 근거 값 병기) · 굽기 워커 항은 이 판이 착수.
 ④ 인수인계 v4 절 · `00-현재상태` 는 §1~§4 커밋에서 이 판의 결과와 함께 교체한다
    (§0 에서는 **백로그·CLAUDE.md 만** 손댄다 — 결과를 보기 «전»에 결과 줄을 쓰지 않는다).
```

### §0-3 바꾸지 않는 것 (회차 프롬프트 원문)

```
 `src/` · 정본 · blob · v3 동결 · `engine/` **물리 식 0줄** · **문턱 0**.
 ★ ③은 **`bakeBodyVerts` 호출만** — `src/` 는 **한 줄도** 고치지 않는다(계기는 `scripts/` 에 신설).
 ★ ①의 워커는 **새 디렉터리 `gpu/bake/`** 에 선다 — 기존 실행기(`l3br_run.py` 등) **수정 0**.
 ★ v4-02~16 산출물 바이트 불변(이 판 산출은 `gpu/bake/results/` 와 새 접두 `l3ap-` 로 판다).
 ★ **108칸 굽기 착수 0** · 최적화 착수 0(트랙 종결) · 옷 조립 굽기 0(③은 «몸»만).
```

### §0-4 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · **단위 명시** · 함정 43)

**★ 단위** — ①의 단위는 **칸**(8칸 재굽기) · 시간은 **초/벽시계**. ②의 단위는 **자리**(파일:줄).
③의 단위는 **몸 1개** · 각도는 **도(°)** · 간격은 **mm**.

**① 굽기 워커 뼈대**(`gpu/bake/`)

| 갈래 | 조건 (단위 = **칸**) |
|---|---|
| **(가)** | **8/8 비트 동일** + **총 시간 감소** |
| **(나)** | **비트 불일치 칸**(값) |
| **(다)** | **워커 미완**(사유) |

**② A포즈 조립 «사실»**(코드만 · 굽기 0 · 처방 0)

| 갈래 | 조건 (단위 = **자리**) |
|---|---|
| **(가)** | 팔 축이 **«한 자리»에서 유도**되어 그 자리만 바꾸면 조립이 따라온다 |
| **(나)** | **여러 자리에 흩어져 있다**(개수 · 자리) |
| **(다)** | 팔 축이 **뼈에서 안 읽힌다**(사유) |

**③ A포즈 몸 1개 생성**(굽기 0 · 옷 조립 0)

| 갈래 | 조건 |
|---|---|
| **(가)** | 게이트 통과 + **겨드랑이 간격 > SEP(2 mm)** |
| **(나)** | 간격 **≤ SEP**(값 · 각도 재검 재료) |
| **(다)** | **생성 실패**(사유) |

**§4 회차 갈래** — A = ①(가) + ②③ 전량 ⟹ 상신 / B = ①(나) ⟹ 값 상신 · ②③ 집행 /
C = ③(나) ⟹ 값 상신 · 다음 판에서 각도 재검 / D = ①(다) 또는 ③(다) ⟹ 사유 상신 · **정지**.
**사후 갈래 신설 0.**

### §0-5 집행 «전» 사전 결정 7건 (값을 보기 «전»에 적는다)

```
 ㄱ **①의 대조 기준** — v4-15 가 낸 8칸 산출(`l3br-<칸>-f64-cuda.bin`)과 **바이트 대조**한다.
    「비트 동일」 = `.bin` 이 **바이트로 같다**(중앙·최대 mm 가 아니라 파일 자체).
    총 시간은 **워커 한 프로세스의 벽시계** ↔ **v4-15 의 칸 합**(칸당 JIT·초기화 포함 · v4-16 §1-③ 표).

 ㄴ **①의 체크포인트 정의** — `results/<job>/<칸>.done` 이 있으면 그 칸을 **건너뛴다**.
    검증 실행은 **깨끗한 결과 디렉터리**에서 시작한다(건너뜀 0 · 그 사실을 로그에 남긴다).

 ㄷ **①의 PAUSE** — `jobs/PAUSE` 파일이 있으면 **다음 칸을 «시작하기 전»** 대기한다
    (돌고 있는 칸은 끝까지 간다). 검증 실행에서는 **쓰지 않는다**(존재 여부만 로그에 적는다).

 ㄹ **②는 «읽기»다** — `grep`/파일 읽기로 **파일:줄**을 뜬다. 각 자리를 「몸에서 유도」/「손 상수」로
    가르는 기준도 코드에 있는 그대로다(값이 `body`·`prim0`·뼈에서 오면 유도 · 리터럴이면 손 상수).
    **처방 0 · 고치기 0 · 「한 자리로 모을 수 있다」는 제안 0.**

 ㅁ **③의 각도·경로** — **어깨 뼈 35° 내림**은 회차 프롬프트가 지정한 값이다(손 상수 0 — 지시값).
    경로는 `src/components/bodyInjectBake.ts:82 bakeBodyVerts(glb, root, "apose")` 를 **부르기만** 한다.
    ★ **회차 프롬프트의 `bakeBodySync` 라는 이름은 저장소에 없다** — 실재하는 이름은
      **`bakeBodyVerts`** 다(파일:줄 위와 같다). 그 사실을 등재하고 실재 이름을 쓴다.
    게이트 ㉮㉯㉰′ 와 높이 1.7000 ± 0.0020 은 **v3-83 등재분을 인용**한다(새 문턱 0).

 ㅂ **③의 겨드랑이 간격** — 팔↔몸통 **SDF** 로 잰다(같은 `bodySdf` 경로 인용) · 단위 mm ·
    비교 상수 **SEP = 2 × THICK = 2 mm** 는 `src/v3/consts.ts` 에서 뜬다(손 상수 0).

 ㅅ **정지** — ①(다) 또는 ③(다)면 **정지**(갈래 D) · ③(나)면 값 상신(갈래 C · 각도 재검은 **다음 판**) ·
    사후 갈래 신설 0 · 처방 0 · 108칸 굽기 착수 0.
```

### §0-6 예산 · 실행 규약

```
 ① 8칸 한 프로세스 재굽기(v4-15 합 약 18분이 기준 · 워커는 그보다 짧아야 «감소») ·
 ② 물리 0(읽기) · ③ 몸 1개(굽기 0 = 옷 굽기 0 · 몸 굽기는 이 판의 산출이다) ·
 §3 캡처 **1장**(A포즈 몸 앞 · 캔버스 원본).
 ★ **GPU 동시 실행 0** · 전부 배경 실행 + 로그 파일.
```
