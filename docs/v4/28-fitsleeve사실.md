# v4-28 — 반경 조건 철회(복원) · fitSleeve A포즈 사실(계기만 · 굽기 0)

**물리 0줄 · `src/` diff = `armProfile` v4-26 상태 복원 + `fitSleeve` 계기 인쇄만 · 굽기 0 · 캡처 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-28-fitsleeve-probe` |
| 기점 | **`077148d`**(= v4-27 §1~§4) |
| §0 커밋 | **`62b41e2`**(집행 «전») |
| §1~§4 커밋 | **`8507d92`** — 본문 · 보고 · 볼트 3종 · metrics · 코드(복원+계기) · 계기 산출 **한 커밋** |
| 보고 파일 | `docs/v4/보고/28.md` |

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v4-28-fitsleeve-probe 077148d → Switched to a new branch 'v4-28-fitsleeve-probe'
       $ git branch --show-current → v4-28-fitsleeve-probe · HEAD 077148d
 2호기 (§0 커밋을 push 한 «뒤» 같은 자리로 전환 — §0-5 에 인쇄)
```

---

## §0 수령·사무

### §0-1 판정문 (전략 세션 · v4-27 §4 + 화면 수령 · 원문)

```
 「갈래 B 수령 · ★★ 전략 세션 인과 사슬 «기각»(첫 화살표 부재 · ARM 소비자 0 · 조립·굽기·캡처 비트 동일) —
   CC 의 §0-4ㄴ 사전 예측이 값으로 맞았다 · 귀책 = 전략 세션(확정 방향 오지정 · 5건째) ·
   ★ 반경 조건 «철회» — 유도 규칙 불성립(3 정점 · 분포 연속) + 죽은 코드 + 전제 기각 ·
     armProfile 을 v4-26 상태로 복원(파일 구간 해시로 v4-26 판과 동일 증명 · 82/82 는 v4-26 실측 인용 ·
     재드라이런 불요) · 문턱 150~200 선택은 손 상수라 기각 · 예외 대장 4건째 = 철회로 이력 ·
   ★★ 다음 표적 = fitSleeve(:713-730) — CC 지목 수용 · 전략 세션 후보 지정 0 · 사실 먼저 ·
   ★ 화면 = v4-26 판정 유효(벗겨짐 잔존) · 봉제 123mm 정황 유지(단정 0)」
```

**★ 귀책 등재(전략 세션 · 다섯째)** — 「반경 조건 부재 → 대역 → SLV_R → 벗겨짐」의 **확정 방향 지정**이
v4-27 에서 **값으로 기각**됐다(조립 페이로드·굽기·캡처 전부 비트 동일). 판정문이 이를 자기 귀책으로 등재했고,
본 회차는 그 등재를 **그대로 옮겨 적는다**(CC 판단 0).

### §0-2 바꾸지 않는 것 (회차 프롬프트 원문)

```
 물리 **0줄** · `dressRun` **0** · **`fitSleeve` 로직 0줄**(계기 인쇄만) ·
 정본·blob·문턱 **0** · 워커 **diff 0** · `armAxisEnv`·NaN 가드 **유지**.
```

### §0-3 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · 단위 명시)

**★ 단위** — ②의 갈래 단위는 **탐색 회차**(= `probe` 호출) · 좌표·간격 **m/mm** · 부위는 **표본점 분류**.

| 항 | 갈래 |
|---|---|
| **①** 반경 조건 철회 | 복원 증명(구간 문자 동일) · 재드라이런 **불요 사유 등재** |
| **②** fitSleeve A포즈 사실 | **(가)** 실패 표본점이 **몸통**에 몰린다(비율·좌표) / **(나)** **팔**에 몰린다 / **(다)** 혼재·그 외(분포 그대로) |
| **③** 가상 배치 조사(R = 61.5 mm 강제) | 위반 항 · 부위 분포 · 위반 깊이(mm) — **처방 0** |

**§4** — A = ①~③ 전량 ⟹ 상신 / B = ② 계기 부착 불가(코드 사실) ⟹ 사유 상신·정지 / C = 그 외 ⟹ 정지.

### §0-4 사전 결정 4건 (값 보기 «전» · CC 집행 설계)

```
 ㄱ **철회는 «파일 되돌리기»로 한다** — v4-27 이 고친 것은 `armProfile` 하나뿐이므로
    `git checkout e07b624 -- src/v3/garmentScene.ts` 로 되돌리면 **v4-26 판과 파일 전체가 문자 동일**이다.
    그 상태의 `git diff e07b624` 가 **빈 출력**인 것을 증명으로 싣는다(구간 해시보다 강한 증명).
    계기는 **그 «뒤»에** 붙이고, 남는 diff 가 **계기뿐**임을 원문으로 보인다.
 ㄴ **계기는 «값을 만들지 않는다»** — `Math.min` 사슬을 건드리지 않고 **같은 값을 한 번 더 읽어** 기록만 한다.
    ⟹ `SLV_R` 은 계기를 켜도 꺼도 **같은 수**여야 한다. 그 사실을 §1-② 에서 값으로 확인한다(불변 확인).
 ㄷ **부위 분류는 «옷 치수»에서 뜬다**(손 상수 0) — `W/2`(몸통 반폭) · 겨드랑이 높이 `Y_ARM` · 암홀 깊이 `ARM_D`.
    팔 = |x| > W/2 · 겨드랑이 = |x| ≤ W/2 이고 |y − Y_ARM(세계)| ≤ ARM_D · 몸통 = 나머지.
    이 규칙을 값 보기 «전»에 등재한다.
 ㄹ **③은 판정만 돌린다** — R 을 61.5 mm 로 강제한 «가상 관»에 같은 검사를 걸어 위반을 «재기»만 한다.
    조립·굽기·정본에 손대지 않는다(굽기 0 · 캡처 0). **처방은 고르지 않는다.**
```

---

## §1-① 반경 조건 철회 — **복원 완료(문자 동일)**

```
 $ git show e07b624:src/v3/garmentScene.ts → 그대로 덮어씀(v4-26 판)
 $ git diff e07b624 -- src/v3/garmentScene.ts
   (빈 출력)                        ← **v4-26 판과 파일 «전체»가 문자 동일**
 $ git diff --stat 077148d -- src/v3/garmentScene.ts
   src/v3/garmentScene.ts | 41 +++++------------------------------------
   1 file changed, 5 insertions(+), 36 deletions(-)      ← v4-27 반경 조건 블록이 사라졌다
```

**재드라이런 «불요» 사유(등재)** — 파일이 v4-26 판과 **문자 동일**이므로 T포즈 조립 산출도 v4-26 과 같다.
그 판의 실측이 **82/82 비트 동일**(v4-26 §1-① · 108/108 행 동일)이고, 그 뒤 v4-27 의 반경 조건도
**82/82 였다**(v4-27 §1-①). ⟹ 되돌린 상태의 82/82 는 **이미 두 번 실측된 값**이다.
★ 계기는 이 복원 «뒤»에 붙였고, v4-26 대비 남는 diff 는 **추가 36줄뿐 · 삭제 0줄**이다(`git diff e07b624` 확인).

---

## §1-② fitSleeve A포즈 사실 — **(다) 혼재·그 외**(몸통 0 · 겨드랑이 39 · 팔 10)

**계기가 값을 만들지 않았다(§0-4ㄴ 확인)** — A포즈 `PLACE_SIG` = `0.024131/0.002000/0.215000/`**`0.140765`**,
T포즈 = `0.027419/0.002000/0.215000/`**`0.061510`** — 둘 다 **계기 없던 판과 같은 수**다.

### R 탐색 궤적 (A포즈 기본몸 M · 축+원점 · 탐색 회차 **49**)

| 회차 | x0 | R | min | 실패항 | 몸SDF | 몸판기둥 | 소매자기쌍 | 몸SDF 최소점 부위 |
|---|---|---|---|---|---|---|---|---|
| 1 | 0.215 | **56.431 mm**(=RMIN) | **−7.6912 mm** | 몸SDF | −7.6912 | +6.9354 | 0.0000 | **팔** |
| 2 | 0.215 | 64.896 mm | −8.5054 mm | 몸SDF | −8.5054 | +0.7440 | 51.7092 | **팔** |
| 3 | 0.215 | 74.630 mm | −8.5054 mm | 몸SDF | −8.5054 | +1.2535 | 103.4867 | **팔** |
| … | … | … | … | … | … | … | … | … |
| 47~49 | 0.215 | **140.765 mm** | **+2.0000 mm** | 몸SDF | +2.0000 | +26.1980 | 267.9931 | **겨드랑이** |

**분포(49회차)**

| 채널 | 값 |
|---|---|
| **실패항**(min 을 만든 항) | **몸SDF 49 / 49** · 몸판기둥 **0** · 소매자기쌍 **0** |
| 몸SDF 최소점 **부위** | **겨드랑이 39** · **팔 10** · 몸통 **0** |
| (참고) 몸판기둥 최소점 부위 | 팔 40 · 겨드랑이 9 |

⟹ 사전 등재 갈래 중 **(가) 몸통에 몰린다**도 **(나) 팔에 몰린다**도 아니다 ⟹ **(다)**.
값 그대로 — **몸통 0** · 다수는 **겨드랑이(39)** · 나머지가 **팔(10)** 이고, **막는 항은 전부 «몸 SDF»**다.

### 대조군 — T포즈 기본몸 M (같은 계기 · 축 미전달)

| 채널 | T포즈 | A포즈 |
|---|---|---|
| 수렴 `SLV_R` | **61.510 mm**(정본과 같다) | **140.765 mm** |
| 탐색 회차 | 44 | 49 |
| **실패항 분포** | **몸SDF 23 · 몸판기둥 21**(섞인다) | **몸SDF 49 · 기둥 0**(한 항이 전부) |
| 몸SDF 최소점 부위 | **몸통 39** · 겨드랑이 3 · 팔 2 | 겨드랑이 39 · 팔 10 · **몸통 0** |
| 마지막 회차 | R 61.5101 mm · min **+1.0278 mm**(실패항 **몸판기둥**) · 몸SDF +6.9291 | R 140.765 mm · min **+2.0000 mm**(실패항 **몸SDF**) · 기둥 +26.1980 |

★ **T포즈에서 R 을 정하는 것은 «몸판 기둥»이고, A포즈에서는 «몸 SDF»다** — 지배하는 항이 바뀐다(값만 · 단정 0).

---

## §1-③ 가상 배치 조사 — R 을 **61.510 mm** 로 강제(판정만 · 굽기 0 · 처방 0)

```
 sleeveTrace.probe(x0 = 0.215, R = 0.061510)  →  min = **−8.505401201546192 mm**
```

| 채널 | 값 |
|---|---|
| 표본 | **510** |
| 위반(< SEP 2 mm) | **69**(13.53%) |
| **위반 항** | **몸SDF 69** · 몸판기둥 **0** |
| **위반 부위** | **팔 48** · **겨드랑이 21** · 몸통 **0** |
| 위반 깊이 mm | 최소 **1.1599972288426852** · p25 **5.1675077559987415** · 중앙 **9.44508353035266** · p75 **10.44871852158759** · 최대 **10.505401201546192** |
| 위반 표본 앞3 | (0.3819, 1.2475, −0.0443) 팔 · 깊이 4.5905 mm<br>(0.3834, 1.2502, −0.0379) 팔 · 깊이 9.6506 mm<br>(0.3845, 1.2523, −0.0313) 팔 · 깊이 10.5037 mm |

⟹ **T포즈 정본 크기의 관(61.5 mm)은 A포즈에서 «팔»을 최대 10.5 mm 파고든다** — 몸통이 아니라 팔이다.
**어떤 처방도 이 판에서 고르지 않는다**(§0-4ㄹ).

---

## §2 자기검사

| 항 | 값 |
|---|---|
| `garmentScene.ts` diff vs **v4-26**(`e07b624`) | **+35 / −0** — **계기 «추가»만**(`sleeveTrace` 선언 · `probe` 뒤 기록 블록 · 손잡이 2줄) |
| 〃 vs 기점(`077148d`) | +40 / −36 (= 반경 조건 철회 −36 + 계기 +35, 문맥 줄 포함) |
| 물리 diff | **0줄** — `probe` 의 `Math.min` 사슬·`fitSleeve` 브래킷·`RMIN`·문턱 **한 글자도 안 건드렸다** |
| 값 불변 확인 | `PLACE_SIG` A포즈 **0.140765** · T포즈 **0.061510** — 계기 없던 판과 **같은 수** |
| `dressRun.ts` · 워커 · `engine/` | **0** · 정본·blob·문턱 **0** · `armAxisEnv`·NaN 가드 **유지** |
| 굽기 · 캡처 | **0 · 0**(회차 프롬프트대로) |
| 이 판이 **신설**한 것 | `scripts/v4SleeveTrace.ts`(계기 스크립트) · 산출 `l3ap-sleeve-v4-28-A.json` · `l3ap-sleeve-v4-28-T.json` |
| `npx tsc -b` | **통과**(종료 0) |
| pytest(2호기) | **40 passed · 2 xfailed · 1 xpassed**(59.30 s) |
| 브랜치 인쇄 | §0 머리에 에어·2호기 둘 다 · §0 은 집행 **«전»** 커밋(`62b41e2`) |
| ecc 에이전트 / 스킬 | **0 / 0** |

---

## §4 회차 갈래 — **A**(①~③ 전량 · ②는 **(다)**)

**넘기는 것 7건**

1. ★★★ **A포즈에서 R 을 키우는 것은 «몸 SDF» 하나다** — 49회차 **전부**(몸판 기둥 0 · 소매 자기쌍 0).
   T포즈는 **몸SDF 23 · 몸판기둥 21** 로 섞이고 마지막 회차의 지배항은 **몸판 기둥**이다. **지배항이 바뀐다.**
2. ★★★ **막는 자리는 «겨드랑이»와 «팔»이다 — 몸통이 아니다**(겨드랑이 39 · 팔 10 · 몸통 0).
   ⟹ v4-27 §4 에서 CC 가 적은 「몸통 기둥이 R 을 키운다」는 **읽기가 값으로 틀렸다**(CC 귀책 · 자기 정정).
3. ★★★ **T포즈 크기의 관(61.510 mm)은 A포즈에서 팔을 파고든다** — 위반 **69/510** · **전부 몸SDF** ·
   부위 **팔 48 · 겨드랑이 21** · 깊이 중앙 **9.445 mm** · 최대 **10.505 mm**.
4. ★★ **첫 회차부터 이미 뚫려 있다** — R = RMIN(56.431 mm)에서 몸SDF **−7.6912 mm**(부위 **팔**).
   ⟹ 「관을 키워 팔을 피한다」가 아니라 **「관이 팔을 비켜 놓이지 못한다」**로 읽힌다(값만 · 단정 0).
5. ★★ **수렴점의 성격** — 140.765 mm 에서 몸SDF 가 **정확히 +2.0000 mm**(= SEP)에 닿고, 그때 최소점은 **겨드랑이**다.
6. ★ **반경 조건 철회 완료** — 파일이 v4-26 판과 **문자 동일** · 재드라이런 불요(82/82 는 두 번 실측된 값).
7. ★ **계기는 값을 만들지 않았다** — 두 포즈의 `PLACE_SIG` 가 계기 이전 판과 같다.

**이 판이 하지 않은 판단** — 처방(관의 축·시작점·SEP 조건 어디를 고칠지) · 벗겨짐의 원인 귀속 · A포즈 층3.
