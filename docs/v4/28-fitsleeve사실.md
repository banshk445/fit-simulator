# v4-28 — 반경 조건 철회(복원) · fitSleeve A포즈 사실(계기만 · 굽기 0)

**물리 0줄 · `src/` diff = `armProfile` v4-26 상태 복원 + `fitSleeve` 계기 인쇄만 · 굽기 0 · 캡처 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-28-fitsleeve-probe` |
| 기점 | **`077148d`**(= v4-27 §1~§4) |
| §0 커밋 | **`62b41e2`**(집행 «전») |
| §1~§4 커밋 | (집행 후 기입) |
| 보고 파일 | `docs/v4/보고/28.md` |

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v4-28-fitsleeve-probe 077148d → Switched to a new branch 'v4-28-fitsleeve-probe'
       $ git branch --show-current → v4-28-fitsleeve-probe · HEAD 077148d
 2호기 (§0 커밋을 push 한 «뒤» 같은 자리로 전환 — §0-5 에 인쇄)
```

---

## §0 수령·사무

### §0-1 판정문 (전략 세션 · v4-27 §4 + 화면 수령 · 원문)

```
 「갈래 B 수령 · ★★ 전략 세션 인과 사슬 «기각»(첫 화살표 부재 · ARM 소비자 0 · 조립·굽기·캡처 비트 동일) —
   CC 의 §0-4ㄴ 사전 예측이 값으로 맞았다 · 귀책 = 전략 세션(확정 방향 오지정 · 5건째) ·
   ★ 반경 조건 «철회» — 유도 규칙 불성립(3 정점 · 분포 연속) + 죽은 코드 + 전제 기각 ·
     armProfile 을 v4-26 상태로 복원(파일 구간 해시로 v4-26 판과 동일 증명 · 82/82 는 v4-26 실측 인용 ·
     재드라이런 불요) · 문턱 150~200 선택은 손 상수라 기각 · 예외 대장 4건째 = 철회로 이력 ·
   ★★ 다음 표적 = fitSleeve(:713-730) — CC 지목 수용 · 전략 세션 후보 지정 0 · 사실 먼저 ·
   ★ 화면 = v4-26 판정 유효(벗겨짐 잔존) · 봉제 123mm 정황 유지(단정 0)」
```

**★ 귀책 등재(전략 세션 · 다섯째)** — 「반경 조건 부재 → 대역 → SLV_R → 벗겨짐」의 **확정 방향 지정**이
v4-27 에서 **값으로 기각**됐다(조립 페이로드·굽기·캡처 전부 비트 동일). 판정문이 이를 자기 귀책으로 등재했고,
본 회차는 그 등재를 **그대로 옮겨 적는다**(CC 판단 0).

### §0-2 바꾸지 않는 것 (회차 프롬프트 원문)

```
 물리 **0줄** · `dressRun` **0** · **`fitSleeve` 로직 0줄**(계기 인쇄만) ·
 정본·blob·문턱 **0** · 워커 **diff 0** · `armAxisEnv`·NaN 가드 **유지**.
```

### §0-3 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · 단위 명시)

**★ 단위** — ②의 갈래 단위는 **탐색 회차**(= `probe` 호출) · 좌표·간격 **m/mm** · 부위는 **표본점 분류**.

| 항 | 갈래 |
|---|---|
| **①** 반경 조건 철회 | 복원 증명(구간 문자 동일) · 재드라이런 **불요 사유 등재** |
| **②** fitSleeve A포즈 사실 | **(가)** 실패 표본점이 **몸통**에 몰린다(비율·좌표) / **(나)** **팔**에 몰린다 / **(다)** 혼재·그 외(분포 그대로) |
| **③** 가상 배치 조사(R = 61.5 mm 강제) | 위반 항 · 부위 분포 · 위반 깊이(mm) — **처방 0** |

**§4** — A = ①~③ 전량 ⟹ 상신 / B = ② 계기 부착 불가(코드 사실) ⟹ 사유 상신·정지 / C = 그 외 ⟹ 정지.

### §0-4 사전 결정 4건 (값 보기 «전» · CC 집행 설계)

```
 ㄱ **철회는 «파일 되돌리기»로 한다** — v4-27 이 고친 것은 `armProfile` 하나뿐이므로
    `git checkout e07b624 -- src/v3/garmentScene.ts` 로 되돌리면 **v4-26 판과 파일 전체가 문자 동일**이다.
    그 상태의 `git diff e07b624` 가 **빈 출력**인 것을 증명으로 싣는다(구간 해시보다 강한 증명).
    계기는 **그 «뒤»에** 붙이고, 남는 diff 가 **계기뿐**임을 원문으로 보인다.
 ㄴ **계기는 «값을 만들지 않는다»** — `Math.min` 사슬을 건드리지 않고 **같은 값을 한 번 더 읽어** 기록만 한다.
    ⟹ `SLV_R` 은 계기를 켜도 꺼도 **같은 수**여야 한다. 그 사실을 §1-② 에서 값으로 확인한다(불변 확인).
 ㄷ **부위 분류는 «옷 치수»에서 뜬다**(손 상수 0) — `W/2`(몸통 반폭) · 겨드랑이 높이 `Y_ARM` · 암홀 깊이 `ARM_D`.
    팔 = |x| > W/2 · 겨드랑이 = |x| ≤ W/2 이고 |y − Y_ARM(세계)| ≤ ARM_D · 몸통 = 나머지.
    이 규칙을 값 보기 «전»에 등재한다.
 ㄹ **③은 판정만 돌린다** — R 을 61.5 mm 로 강제한 «가상 관»에 같은 검사를 걸어 위반을 «재기»만 한다.
    조립·굽기·정본에 손대지 않는다(굽기 0 · 캡처 0). **처방은 고르지 않는다.**
```
