# v4-46 — A포즈 108칸 본굽기(그리드 27몸 × 4사이즈) · 집계 · 대표 캡처

**물리 0줄 · `src/`·워커·engine diff 0(jobs·실행만) · 굽기 = 108칸 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-46-bake108` |
| 기점 | **`cbbfc0f`**(= v4-45 §1~§4 + 볼트 + 보고 + 캡처) |
| §0 커밋 | (아래 §0-5 에 자기 기입) |
| §1~§4 커밋 | (집행 후 기입) |
| 보고 파일 | `docs/v4/보고/46.md` |

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v4-46-bake108 cbbfc0f → Switched to a new branch 'v4-46-bake108'
       $ git branch --show-current → v4-46-bake108 · HEAD cbbfc0f
 2호기 (§0 커밋을 push 한 «뒤» 같은 자리로 전환 — §0-5 에 인쇄)
```

---

## §0 수령·사무

### §0-1 판정문 (전략 세션 · v4-45 §4 + 화면 수령 · 원문)

```
 「갈래 A 수령 · 브라우저 몸 게이트 pass + 층3 워커 첫 성공 기록 + 둘레 비 = 배율(규칙 스케일 자동 추종 확인) ·
   화면 합격(v4-37 동급 · 재발 0) ⟹ 108칸 발주 · 시간 25.9h 수용
   (순서 = M 사이즈 9몸 먼저 → S → L → XL · 중간 점검 지점) ·
   던짐 예상 4칸(+α)은 T포즈 규약대로 «판정»으로 집계」
```

### §0-2 바꾸지 않는 것

```
 물리 **0줄** · `src/` **0줄** · 워커·`gpu/engine/` **diff 0** · 정본·blob·문턱 **0** · 게이트 채널·문턱 **0**
```

### §0-3 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · 단위 명시)

**★ 단위** — 칸은 **칸**(108) · 시간은 **초/프레임**과 **시간(h)** · 게이트 값은 **mm** · 정착은 **프레임**.

| 항 | 갈래 |
|---|---|
| **①** 본굽기 | 순서 M → S → L → XL · `.done` 체크포인트 · M 9몸 시점 **중간 집계 1줄** metrics append(대기 0) |
| **②** 집계 | pass / fail(채널·값) / 던짐(규칙·부류) · 층3 성립 수 · `chestY > waistY` 전 칸 · 정착 f·s/프레임 분포 · 총 벽시계 |
| **③** 대표 캡처 | pass 칸 4개(작은 S · 기본 M · 큰 L/XL · 극단 1) 앞·옆 · **화면 판정 0** |

**§4** — A = pass 다수 + 층3 전 칸 성립 ⟹ 집계·캡처 상신 / B = 구조적 fail 부류 존재 ⟹ 값 상신 /
C = 실행 중단(사유) ⟹ **재개 절차와 함께** 상신.

### §0-4 사전 결정 5건 (값 보기 «전» · CC 집행 설계)

```
 ㄱ **한 칸 = 한 job** 으로 쪼갠다 — 판정문의 «사이즈 순서»(M → S → L → XL)를 지키려면
    「한 job = 한 몸(4칸)」이 아니라 **칸 단위**여야 한다. 워커는 어차피 **칸마다 자식 프로세스**를 띄우므로
    (`worker.py:11` 계열) 쪼개도 JIT 비용은 같다. `BODY_BIN` 은 job 을 띄우는 **환경**에 준다.
 ㄴ **드라이버는 «신설 파일» 하나** — `scripts/bake108.py`(실행 도구 · 물리 0줄 · 워커 0줄).
    하는 일은 셋뿐이다: ① 조립 export 가 없으면 실행 ② job json 을 쓴다 ③ 워커를 부른다.
    이미 `.done` 이 있으면 **건너뛴다**(재개 · T포즈 규약 인용).
 ㄷ **ssh 끊김 대비** — 배치를 나눠 돌리고, 끊기면 **같은 명령을 다시 부른다**(`.done` 이 이어붙인다).
    `PYTHONIOENCODING=utf-8` 를 항상 준다(v4-37 의 cp949 사고 재발 방지).
 ㄹ **중간 집계는 «한 줄»** — M 9몸이 끝나면 metrics 에 한 줄 append 하고 **계속 진행**한다(대기 0).
 ㅁ **fail·던짐은 값만** — 처방 0. 던짐은 T포즈 규약대로 «판정»으로 집계한다(칸이 사라지지 않는다).
```

### §0-5 커밋·전환 인쇄 (집행 «전»)

(§0 커밋 sha 자기 기입 · 2호기 전환 인쇄는 아래에 붙인다)

---

## §1-① 본굽기 — **108/108 처리**(굽기 96 + 옷 던짐 12)

드라이버 `scripts/bake108.py`(신설 · 실행 도구 · 물리 0줄 · 워커 0줄)로 **한 칸 = 한 job** 을 세워
M → S → L → XL 순으로 돌렸다. 몸·축·원점은 **그리드 정본**(`gpu/oracle/export/grid27/` · v4-45 등재)에서만 왔다.

```
 job 이름 v4-46-A108 · 칸마다 job json(`gpu/bake/jobs/v4-46-A108-<칸>.json`) ·
 결과 `gpu/bake/results/v4-46-A108/<칸>.{bin,json,done}` + `fit-<칸>.json` · `gate-<칸>.json`
 총 굽기 **61,737.1 s(≈17.1 시간)** · s/프레임 중앙 **2.6786** · 최대 5.9642
 정착 프레임 — 최소 **160** · 중앙 **200** · 최대 **400**(상한)
```

### ★ 실행 중 사고 2건(값·처분 그대로)

```
 ㉠ **옛 산출 재사용** — 드라이버가 「`scene-<칸>.bin` 이 있으면 조립을 건너뛴다」로 돼 있어
    **T포즈 시절 동명 파일**(`scene-c100-h155-s50_M.bin` 2026-09-01 · `scene-c100-h170-s45_M.bin` 2026-08-30)을
    재사용했고, `asm-*.bin` 이 없어 워커가 **즉시 실패**했다(1.3 s · returncode 1 · 조용한 오굽기 0).
    처분 — 조립은 **6 초**뿐이므로 «항상» 다시 만들도록 고쳤다(드라이버 주석에 사고 기록) · 두 칸 재실행 완료.
    ★ 그중 `c100-h170-s45_M` 은 정착 **f350** · convNet **4.391290028753105e-05** 로
      v4-45 의 `Bwsr_M`(같은 몸·같은 칸 · 다른 태그) 값을 **그대로 재현**했다.
 ㉡ **ssh 끊김 반복** — `client_loop: send disconnect: Broken pipe` 가 배치마다 났다.
    처분 — **자동 재접속 루프**(끊기면 같은 명령을 다시 부른다)로 돌렸고, `.done` 체크포인트가 이어붙였다.
    **재굽기·중복 0**(칸 단위 스킵).
```

## §1-② 집계

| 채널 | 값 |
|---|---|
| 처리 | **108/108** — 굽기 **96** · 옷 던짐 **12** |
| 게이트 | **pass 33 / fail 63**(굽힌 96칸 기준) |
| 층3 5행 | **96/96 성립** · **`chestY > waistY` 96/96** |
| 정착 | f 최소 160 · 중앙 200 · 최대 400 · **미정착 7칸**(창 순변위 > TOL) |
| 시간 | 총 **61,737.1 s** · s/프레임 중앙 **2.6786** · 최대 **5.9642** |

### 사이즈별

| 사이즈 | pass | fail | 던짐 |
|---|---|---|---|
| S | **11** | 11 | 5 |
| M | **9** | 14 | 4 |
| L | **6** | 19 | 2 |
| XL | **7** | 19 | 1 |

### fail 채널 분포(중복 포함 · 굽힌 96칸)

| 채널 | 칸 수 |
|---|---|
| **자기관통 교차 ≠ 0** | **49** |
| **③a 관통 > 0.5 mm** | **28** |
| 정착 창 순변위 > 0.1 mm | **7** |
| 목선 초과비 > 1 | **1** |

**관통 최대 mm 분포**(96칸) — 최소 **0.2244297351141325** · 중앙 **0.40976730400947536** ·
p90 **1.6509985656884503** · 최대 **32.45656730295164**(`c87.5-h155-s40_M`).
**자기관통 교차 분포** — 교차 0인 칸 **47** · 중앙 **3** · 최대 **32**.

### 미정착 7칸(창 순변위 · 400프레임 상한 도달)

```
 c100-h170-s45_XL 1.3200065122537942e-03 · c100-h185-s50_M 1.1633504641107067e-04 ·
 c100-h185-s50_XL 1.5492974012250326e-04 · c122.5-h155-s40_XL 1.5898971244578816e-04 ·
 c122.5-h170-s40_L 4.986315832406059e-04 · c122.5-h170-s40_M 6.50259561638087e-04 ·
 c122.5-h170-s40_XL 2.267550019637961e-04       (문턱 1e-4 · 발산·NaN 0 · degenerate 0)
```

### 옷 던짐 12칸(제도 단계 갈래 D · 몸 읽기 «전» · v4-44 §1-② 와 같은 규칙)

```
 c122.5-h155-s45_M · c122.5-h155-s50_L · c122.5-h155-s50_S · c122.5-h170-s45_S · c122.5-h170-s50_M ·
 c122.5-h170-s50_S · c122.5-h185-s45_M · c122.5-h185-s45_S · c122.5-h185-s50_M · c122.5-h185-s50_S ·
 c122.5-h185-s50_XL · **c87.5-h185-s50_L**
 ⟹ 11칸이 c122.5 계열이고 1칸이 c87.5-h185-s50_L 이다. T포즈 108칸의 던짐은 **26칸**이었다
   (SEP 15 · 소매산 9 · 목 밑동 2) — **A포즈에서는 12칸**이다(사실 · 해석 0).
```

## §1-③ 대표 캡처 — 8장(pass 칸 4개 × 앞·옆) · **화면 판정 0**

| 파일 | 칸 | 관통 최대 mm |
|---|---|---|
| `46-1-작은몸S-1-front.png` · `-2-side.png` | **c87.5-h170-s50_S**(작은 몸 · S) | 0.4088 |
| `46-2-기본M-1-front.png` · `-2-side.png` | **c100-h170-s45_M**(기본 · M) | 0.3309 |
| `46-3-큰몸L-1-front.png` · `-2-side.png` | **c122.5-h185-s40_L**(큰 몸 · L) | 0.4253 |
| `46-4-극단XL-1-front.png` · `-2-side.png` | **c122.5-h155-s50_XL**(가슴 최대 · 키 최소 · XL) | 0.3996 |

900×1200 · 정사영 `front`/`sideXplus` · 몸 = **그리드 정본 몸** · 옷 = 각 칸의 정착 위치 · v4-24 이후와 같은 프리셋.
**보기 판단은 전략 세션·사용자가 한다.**


---

## §2 자기검사

| 항 | 값 |
|---|---|
| `src/` diff vs 기점 `cbbfc0f` | **0**(무출력) · 물리 **0줄** |
| 워커 · `gpu/engine/` | **diff 0** |
| 이 판이 **신설**한 것 | `scripts/bake108.py`(드라이버) · job 96종 · 결과 폴더 `results/v4-46-A108/` · 캡처 8장 |
| **3경로 회귀**(굽기 «전» 1회) | `c100-h185-s40_L` 3회 — 정착 **f10** · `lastNet` **9.012449069698124e-05** · vs 정본 중앙 **0.005771083432066226 mm** · `pass true` · `fails []` — **3/3 동일** |
| `npx tsc -b` | **통과**(종료 0) |
| pytest(2호기) | **40 passed · 2 xfailed · 1 xpassed**(81.20 s) — ★ 아래 사고 복구 «뒤» 값 |
| 캡처 커밋 | `ad3d9a7`(2호기 · git 경유) |
| 브랜치 인쇄 | §0 머리 · §0 은 집행 **«전»** 커밋(`7dbe754`) |
| ecc 에이전트 / 스킬 | **0 / 0** |

### 자기신고 — 정본 산출물 오염(CC 귀책)

```
 무엇 — 드라이버 태그를 `<몸>_<사이즈>`(= 칸 이름)로 두어 조립 산출물이 **T포즈 정본과 같은 이름**이 됐고
        추적 파일 **30개**(`scene-*`·`scene-bend-*`·`scene-seam-*`)와 `sdf-<몸>.bin` **27개**를 덮었다.
 발견 — §2 의 `pytest` 가 **20 failed**(`test_bending`·`test_collide` — 그 파일들을 읽는다).
 처분 — (1) `git checkout -- gpu/oracle/export` 로 추적분 **30개 원복**(diff 0 확인)
        (2) `sdf-*` 는 git 밖이라 **기본(T포즈) 몸으로 조립 export 재실행**해 재생성 — **25몸 성공** ·
            **2몸 실패**(`c122.5-h185-s45`·`c122.5-h185-s50` — T포즈에서 네 사이즈 모두 옷이 던진다 ⟹
            그 몸의 T포즈 SDF 는 **원래 만들 수 없다** · 현 파일은 A포즈판 · 사실 등재)
        (3) 재검사 **pytest 40 passed** 회복 (4) 드라이버 머리주석에 사고·처분 등재(재발 방지)
 ★ 굽기 결과(`results/v4-46-A108/`)는 별도 폴더라 **오염 0**이다.
```

## §4 회차 갈래 — **B**(구조적 fail 부류 존재 ⟹ 값 상신)

층3은 **96/96 성립** · `chestY > waistY` **96/96** 인데 게이트는 **pass 33 / fail 63** 이고
fail 이 **두 채널에 몰려 있다**(자기관통 교차 49 · ③a 관통 28) ⟹ 사전 등재 **B**.

**넘기는 것 8건**

1. ★★★ **108/108 처리** — 굽기 96 · 옷 던짐 12 · 총 **61,737.1 s(≈17.1 h)** · s/프레임 중앙 **2.6786**.
2. ★★★ **층3은 전 칸 선다**(5행 96/96 · `chestY > waistY` 96/96) — v4-38·39 규칙이 27몸 × 4사이즈를 따라갔다.
3. ★★★ **게이트 pass 33 / fail 63** — 자기관통 교차 ≠ 0 **49칸** · ③a 관통 > 0.5 mm **28칸** ·
   정착 초과 **7** · 목선 초과 **1**. 관통 최대 중앙 **0.40976730400947536** · p90 **1.6509985656884503** ·
   최대 **32.45656730295164 mm**(`c87.5-h155-s40_M`).
4. ★★ **사이즈별 pass/fail/던짐** — S **11/11/5** · M **9/14/4** · L **6/19/2** · XL **7/19/1**.
5. ★★ **교차 0인 칸 47개**(중앙 3 · 최대 32).
6. ★★ **미정착 7칸**(400프레임 상한 · 발산 0) — 1.1633504641107067e-04 ~ 1.3200065122537942e-03.
7. ★ **옷 던짐 12칸**(A포즈) ↔ T포즈 **26칸** — 같은 규칙인데 수가 다르다(사실).
8. ★★ **자기신고** — 정본 산출물 30 + SDF 27 오염 후 복구(위 §2 · pytest 회복).

**이 판이 하지 않은 판단** — fail 두 부류의 처분 · 미정착 7칸 · 화면 · 다음 축.
