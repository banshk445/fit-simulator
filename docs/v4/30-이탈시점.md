# v4-30 — 어깨 이탈의 «시점» 사실(계기만 · 처방 0)

**물리 0줄 · `src/` diff = 계기 인쇄만 · 워커 diff 0 · 굽기 1칸(v4-29 조립 재사용) · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-30-slip-timing` |
| 기점 | **`3d8d83a`**(= v4-29 §1~§4) |
| §0 커밋 | **`4819fa2`**(집행 «전») |
| §1~§4 커밋 | (집행 후 기입) |
| 보고 파일 | `docs/v4/보고/30.md` |

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v4-30-slip-timing 3d8d83a → Switched to a new branch 'v4-30-slip-timing'
       $ git branch --show-current → v4-30-slip-timing · HEAD 3d8d83a
 2호기 (§0 커밋을 push 한 «뒤» 같은 자리로 전환 — §0-5 에 인쇄)
```

---

## §0 수령·사무

### §0-1 판정문 (전략 세션 · v4-29 §4 + 화면 수령 · 원문)

```
 「갈래 B 수령 · ★★★ 원점 분리 성립(산술 102.06 → 가상 위반 69→0 → SLV_R 56.75 ·
   6번째 가설 첫 생존 · 함정 37 기하판 확정) · 지배항 전이 수용(몸SDF → 소매자기쌍 = 건강한 꼴) ·
   격자 용량 호출부 처분·계기 충실도·T포즈 무영향 수용 ·
   ★★ 화면 판정: 관찰① 가슴 텐트 «소멸» — T포즈 축 잔재 원인 확정(자연 실험 성립) ·
   벗겨짐 «잔존» — 성격 변화(비대칭 구겨짐 → 좌우 대칭 오프숄더 · 천은 매끈 ·
   어깨에 걸리지 않고 흘러내림) · 이것이 지배 결함(게이트 잔여 관통 1.08·교차 4 보다 위) ·
   밑단 A라인 잔존 · 목선 V 판정 불가(목선 자체 하강) ·
   ★★ 다음 질문 = 옷이 «언제» 흘러내리는가(조립부터 / 램프 중 / 정착 중) — 셋은 처방이 다르다 ·
   후보 지정 0 · 프레임 궤적이 가른다」
```

### §0-2 바꾸지 않는 것 (회차 프롬프트 원문)

```
 물리 **0줄** · `garmentScene`·`dressRun`·`fitSleeve`(v4-29 상태) **0** ·
 정본·blob·문턱 **0** · 워커 **diff 0**(계기는 스크립트/호출부) · 격자 용량 **262144 유지**.
```

### §0-3 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · 단위 명시)

**★ 단위** — ①의 갈래 단위는 **구간**(프레임 구간) · 높이 **mm** · 프레임 **1/60 s**.

| 항 | 갈래 |
|---|---|
| **①** 시점 계기 | **(가)** f0 부터 이미 낮다(조립 기하 · T포즈 f0 대비 차 mm) / **(나)** 램프 구간(f0~rampN)에서 급락(구간·낙폭) / **(다)** 램프 뒤 정착 중 서서히 미끄러짐(궤적) / **(라)** 혼재(구간별 값) |
| **②** 프레임 스냅 | 이탈 구간 전·중·후 3~4장 → `docs/v4/캡처/30-<n>-f<프레임>.png` · **화면 판정 0** |
| **③** 좌우 대칭 | 정착 상태에서 좌/우 소매·어깨선 y 중앙의 대칭성(값만) |

**§4** — A = ① 갈래 판정 성립 + ②③ ⟹ 상신 / B = ① 계기 부착 불가(사유) ⟹ 정지 / C = 그 외 ⟹ 정지.

### §0-4 사전 결정 5건 (값 보기 «전» · CC 집행 설계)

```
 ㄱ **워커를 고치지 않는다** — §0-2 대로 `gpu/bake/worker.py` 는 **바이트 불변**이고,
    계기는 **새 스크립트**(`gpu/bake/slip_probe.py`)가 진다. 그 스크립트는 워커의 굽기 설정을
    **같은 순서로** 세우고(같은 `engine/full_sc` · 같은 램프 식 · 같은 `cellCap`), 창마다 채널만 «읽어» 담는다.
 ㄴ **계기가 값을 만들지 않았음을 «비트»로 증명한다** — 이 판의 최종 위치가 v4-29 의 `Apiv_M.bin` 과
    **바이트 동일**이어야 한다. 다르면 그 사실을 그대로 적고 ①의 판정을 «보류»한다.
 ㄷ **정점 집합은 씬에서 «내보내» 쓴다**(손 상수 0) — 목선 링(`dressRun` 의 `neckF`·`neckB`) ·
    어깨 봉제선(`sc.seams` 중 어깨) · 패널 상단. TS 스크립트가 인덱스를 json 으로 내고 파이썬이 읽는다.
 ㄹ **T포즈 대조군은 «기존 산출»을 쓴다**(재굽기 0) — 조립 `asm-Tasm_M.bin`(f0)과
    v4-29 §2 회귀3 의 정착 산출(램프 포함 f180)에서 **같은 채널**을 계산한다. 재굽기 여부를 §1-①에 등재한다.
 ㅁ **②의 스냅 프레임은 ①의 값이 고른다** — 낙폭이 큰 구간의 «전·중·후»를 값으로 집고 렌더한다.
    화면 판정은 하지 않는다(전략 세션 몫).
```
