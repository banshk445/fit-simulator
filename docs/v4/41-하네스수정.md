# v4-41 — 그리드 하네스 실패 원인(코드 사실) · 수정 · 1칸 드라이런 검증

**물리 0줄 · `src/` diff = `V4AposeGrid.tsx`(신설 하네스) «한정» · `Mannequin.tsx`·`bakeGrid`·`garmentScene` 0 · 굽기 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-41-grid-fix` |
| 기점 | **`b20f165`**(= v4-40 §1~§4 + 볼트 + 보고) |
| §0 커밋 | (아래 §0-5 에 자기 기입) |
| §1~§4 커밋 | (집행 후 기입) |
| 보고 파일 | `docs/v4/보고/41.md` |

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v4-41-grid-fix b20f165 → Switched to a new branch 'v4-41-grid-fix'
       $ git branch --show-current → v4-41-grid-fix · HEAD b20f165
 2호기 (§0 커밋을 push 한 «뒤» 같은 자리로 전환 — §0-5 에 인쇄 · 2호기 ssh 이름 해석 상태도 함께 적는다)
```

---

## §0 수령·사무

### §0-1 판정문 (전략 세션 · 승혁 실행 결과 수령 · 원문)

```
 「승혁 실행 = 절차 준수 · 결과 = 27/27 실패 «상한 600 도달» · 잔차 1.13e-1~4e-2 대 정체 ·
   귀책 = 전략 세션(1칸 드라이런 갈래 없이 27칸 일괄을 설계 · 10건째) ·
   후보 둘(단정 0): (a) A포즈 회전 상태에서 maxScaleResidual 이 안 떨어짐(제품 A포즈 루프·회전이 잔차 생산)
                    (b) 하네스 순서가 매 프레임 자세를 되감아 안정 불가 — 코드 사실로 가른다」
```

**★ 귀책 등재(전략 세션 · 열째)** — 「1칸 드라이런 갈래 없이 27칸 일괄을 설계」. 판정문이 자기 귀책으로
등재했고 본 회차는 **그대로 옮겨 적는다**(CC 판단 0).
★ **사실 병기(다툼 0)** — 그 27칸 일괄 하네스와 **절차서를 쓴 것은 CC**(v4-40 §1-②③)이고,
v3-83 이 절차로 요구한 **「가시화 계기 1회 → 루트 생성 → 동기 굽기」 개시 시퀀스**를
CC 의 절차서가 **빠뜨렸다**(v4-40 `docs/v4/40-실행절차.md` 3단계). 이 사실은 §1-① 의 판정 재료다.

### §0-2 바꾸지 않는 것 (회차 프롬프트 원문)

```
 물리 **0줄** · `Mannequin.tsx` 본체 **0** · `bakeGrid`(T포즈 27) **0** ·
 `bodyLevels`·`garmentScene`·워커 **0** · 정본 **0**
```

### §0-3 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · 단위 명시)

**★ 단위** — 잔차는 **무차원 배율**(스케일 lerp 목표 대비) · 프레임은 **프레임**(1/60 s 전진) ·
좌표차는 **mm** · 칸은 **칸**.

| 항 | 갈래 |
|---|---|
| **①** 원인(코드 사실) | **(가)** 자리·기전 특정 / **(나)** 불가(사유) |
| **②** 수정 | ①의 자리에 최소 수정(하네스 파일 안 원칙) · diff 원문 · 근거 |
| **③** 1칸 드라이런 | **(가)** 1칸 통과 + 교차 확인 / **(나)** 승혁 1칸 실행 필요(절차·판정 기준) / **(다)** 여전히 실패(값) |

**§4** — A = ①(가)+②+③(가) ⟹ 상신(승혁 27칸 재실행 안내) / B = ③(나) ⟹ 절차 상신 /
C = ①(나) 또는 ③(다) ⟹ 값 상신 · 정지 / D = 그 외 ⟹ 정지.

### §0-4 사전 결정 4건 (값 보기 «전» · CC 집행 설계)

```
 ㄱ **후보 (a)·(b)는 «코드»로 먼저 가른다** — 실행 없이 가를 수 있으면 실행하지 않는다.
    각 후보를 「그 기전이 성립하려면 코드의 어느 줄이 그래야 하는가」로 바꿔 적고, 그 줄을 읽어 판정한다.
 ㄴ **정체값은 «식»과 맞춰 본다** — 잔차의 정의는 `Mannequin.tsx` 의 `|현재 배율 − 목표 배율|` 이다.
    슬라이더 목표로부터 **|1 − 배율|** 을 계산해 관측 정체값(1.13e-1 · 4e-2 대)과 대조한다.
    맞으면 「프레임이 «한 번도» 안 돌았다」가 되고, 안 맞으면 다른 자리를 본다.
 ㄷ **CC 가 스스로 확인할 수 있는 최대까지 간다** — 이 저장소의 dev 서버를 띄우고
    브라우저 채널(콘솔·DOM 텍스트)로 **수치만** 읽는다. **화면 판정 0**(보기 판단 0 · 스크린샷 판정 0).
    브라우저 채널이 안 서면 그 사실을 적고 ③(나)로 간다.
 ㄹ **수정은 하네스 파일 «안»에서** — `Mannequin.tsx`·`bakeGrid` 는 0줄.
    프레임이 실제로 도는지 **자가진단**을 넣어, 안 돌면 27칸을 태우지 않고 **즉시 멈추고 사유를 낸다**.
```

### §0-5 커밋·전환 인쇄 (집행 «전»)

(§0 커밋 sha 자기 기입 · 2호기 전환 인쇄는 아래에 붙인다)
