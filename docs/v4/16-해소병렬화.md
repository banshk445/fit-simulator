# v4-16 — 가슴 0.1782 진단 · 해소 커널 병렬화 · 108칸 예산 실측

**집행 기계 = 2호기(CUDA · 정본 기계) · `src/` diff 0 · 굽기 0(정본 재생성 0) · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-16-resolve` |
| 기점 | **`8ee1e86`** (= v4-15 HEAD) |
| 워크트리 | `C:\Users\banba\fit-simulator` |
| 기계 | **`DESKTOP-OV72A46`** (win32 · CUDA · GTX 1660 Ti · Taichi 1.7.4 · llvm 15.0.1 · py 3.12.10) |
| §0 커밋 | **`2b13e85`** — 집행 «전» 별도 커밋(이 파일의 §0 + `CLAUDE.md` 사무 2건만) |
| §1~§4 커밋 | **`aaa4ada`** — 코드 5 · 산출물 · 본문 · 볼트 3종 · `docs/metrics-log.md` **한 커밋** |
| 호출 ecc 에이전트/스킬 | **0 / 0** (`strategist` 서브에이전트도 0) |

---

## §0 수령 (집행 «전» 별도 커밋)

### §0-1 판정문 (전략 세션 · v4-15 §4 수령 · 원문)

```
 「갈래 B 수령 · ★★★ 게이트 넓이 완전 — 8/8 pass · fails 빈 집합 · 교차 0 · 정착 8/8 ·
   ★★★ 「같은 옷」 «정의 확정»(전략 세션 몫) = 대역 이름 5/5 + 게이트 pass·fails 일치 +
   표시값 «중앙 차» ≤ 0.05mm — 표시 1자리 «일치»는 채널에서 «내린다»(갈린 6행 중 5행이
   0.05mm 미만에서 경계를 넘었고, 경계 통과 여부는 well-posed 하지 않다 · 함정 41) ·
   0.05 는 1자리 표시의 반올림 폭에서 유도 · 손 상수 0 · 이 정의로 8칸 중 7칸 성립 · 1칸 미달 ·
   ★★ 유일한 실체적 차이 = c100-h185-s40_L 가슴 +0.1782mm(다른 39행은 ≤0.05) · 귀속 0 ·
   ★★ f32 축 «폐기 선언» — 셀프 충돌 뒤에도 미도달(81~96배) · 이득 3.6% ⟹ v4 정본 정밀도 = f64 확정 ·
   ★ 최적화 상계 = 해소 36.65% · +광역 57.36% · 순서 재현 비용 1.09%(싸다) · 보류 2칸은 등재만」
```

### §0-2 사무 (이 커밋에서 «함께» 집행 · 2건 · `CLAUDE.md` v4 절)

```
 ① **「같은 옷」 정의 교체 등재** — 세 조건(대역 이름 5/5 · 게이트 `pass`·`fails` 일치 ·
    표시 5행 «중앙 차» ≤ 0.05 mm). **0.05 는 `toFixed(1)` 의 반올림 폭 0.1 의 절반에서 유도**(손 상수 0).
    **구 정의(표시 1자리 «일치»)는 폐기 사유와 함께 «원문 무삭제»로 이력에 남겼다**
    (사유 = 경계 통과 여부가 well-posed 하지 않다 · 함정 41 · 폐기된 층2 와 같은 형태).
 ② **f32 축 폐기 등재** — v4 정본 정밀도 = **f64 확정**. 「정밀도 float32」 줄은 v4-01~13 의 «이력»으로
    표시하고 근거(미도달 81~96배 · 이득 3.6%)를 붙였다. **줄 삭제 0**.
```

### §0-3 바꾸지 않는 것 (회차 프롬프트 원문)

```
 `src/` · 정본 · blob · v3 동결 · **물리 식 0줄** · **문턱 0**.
 ★ 병렬화는 **«순서 재현»을 지키는 범위 안에서만** 한다 — v4-14 가 세운 원리
   [[../../.obsidian-log/원리/순서도-이식의-조건이다]] 를 이 판이 «시험대»에 올린다.
 ★ `engine/` **기존 11파일 바이트 불변** — 병렬 해소는 **새 파일**로 판다
   (`selfcol_col.py` · `full_sc_col.py`). 기존 `selfcol.py` · `full_sc.py` 는 대조군으로 남는다.
 ★ v4-02~15 산출물 바이트 불변(이 판 산출은 새 접두 `l3rp-` 로 판다) · 108칸 «굽기» 착수 0 · 캡처 0.
```

### §0-4 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · **단위 명시** · 함정 43)

**★ 단위** — ①의 단위는 **행**(리포트 5행 중 한 행). ②의 단위는 **프레임**(1프레임 = 그 칸의 substeps).
③의 단위는 **칸**(108칸) · 시간은 **시간(h)**.

**① 가슴 0.1782 진단**(칸 `c100-h185-s40_L` · **사실만 · 처방 0 · 귀속 0**)

| 갈래 | 조건 (단위 = **행**) |
|---|---|
| **(가)** | **밴드 정점 집합이 v3·v4 에서 다르다**(개수 · 차집합) |
| **(나)** | 집합은 같은데 **좌표차가 그 밴드에서만 크다**(값) |
| **(다)** | 그 밴드도 다른 밴드와 같은 크기인데 **«중앙» 계산이 갈린다**(값) |
| **(라)** | 그 외 |

**② 해소 커널 병렬화** — 접촉이 공유하는 **정점**으로 그래프를 만들어 **탐욕 컬러링** ·
색 «안»에서만 동시 갱신 · **색 순서는 v3 순서를 따르는 배열로 고정**.

| 갈래 | 조건 (단위 = **프레임**) |
|---|---|
| **(가)** | ㄱㄴ 성립 + 「같은 옷」(**새 정의**) 성립 + **배수 > 1.3** |
| **(나)** | 성립하나 **배수 ≤ 1.3**(값) |
| **(다)** | **「같은 옷」 깨짐**(값 · 즉시 등재 · **채택 0**) |
| **(라)** | **색칠 불가**(사유) |

**③ 108칸 예산 실측** — ②가 (가)면 그 판으로, 아니면 현행 판으로. v4-15 의 **8칸 실측**에서 산출.
**병렬 굽기 착수 0 · 추정 0 — 실측한 것만 적는다.**

**§4 회차 갈래** — A = ①②③ 전량 + ②(가) ⟹ 상신 / B = ②(나) ⟹ 값 상신 · ③ 현행 판으로 집행 /
C = ②(다)(라) ⟹ 값·사유 상신 · **채택 0** · ③ 현행 판으로 / D = ①(라) 또는 그 외 ⟹ 정지.
**사후 갈래 신설 0.**

### §0-5 집행 «전» 사전 결정 8건 (값을 보기 «전»에 적는다)

```
 ㄱ **①의 밴드 정의는 코드에서 인용한다**(새 수 0) — `src/v3/fitReport.ts:80-86` `bandOf` =
    **몸통 패널 · `|y − cy| ≤ d`(9 mm)** · 중앙/p25/p75 는 `fitReport.ts` 가 내는 값을 그대로 읽는다.
    비교 대상은 **같은 칸의 다른 4행 밴드**와 **다른 칸의 가슴 밴드**다.

 ㄴ **①ㄹ 비트 재현** — 같은 칸을 **같은 실행기로 두 번** 돌려 위치를 바이트 대조한다.
    재현되면 「결정적 차이」 · 아니면 **그 사실 자체가 결과**다(v4-11 과 같은 처분).

 ㄷ **②의 컬러링 규약**(값 보기 «전») — 우선순위 = **v3 접촉 순서(`ckey` 오름차순)** ·
    같은 색 안의 접촉은 **정점을 하나도 공유하지 않는다**(기계검증) ·
    색은 **오름차순으로 차례차례** 적용한다. ⟹ 결과 순서 = **「v3 순서 우선 탐욕 컬러링」**이다.
    ★ **이것이 v3 의 순차 결과와 «비트로» 같다고 주장하지 않는다** — 색 사이에서 순서가 접힌다.
      **얼마나 갈리는지를 값으로 재고**, 판정은 **「같은 옷」(새 정의)로만** 건다(회차 프롬프트 ②ㄷ).
      이 판은 v4-14 원리의 「순서를 바꾸면 더 이상 같지 않다」를 **제품 문턱에서 시험**한다.

 ㄹ **②ㄴ 병렬 확인**(함정 42) — 프로파일러 태스크/호출 · **grid/block > 1** · IR 의 `range_for` ·
    대조군(직렬 해소판)도 **같은 판에서** 재서 계기가 살아 있음을 보인다.

 ㅁ **②ㄷ 배수의 분모** — **v4-15 §1-② 의 기준선 칸 실측 2.345 s/프레임**(같은 칸 · 같은 실행기 계열).
    분자는 이 판 병렬 해소판의 같은 칸 실측. **새 수 0.**

 ㅂ **②의 「같은 옷」 판정은 «새 정의»로만** 건다(§0-2 ①) — 대역 이름 5/5 · 게이트 `pass`·`fails` 일치 ·
    표시 5행 중앙 차 ≤ 0.05 mm. 표시 1자리 일치는 **병기만** 하고 판정에 걸지 않는다.

 ㅅ **③의 산출 규약** — 칸별 **s/프레임 × 정착 프레임 수**를 v4-15 8칸 실측에서 «읽고»,
    108칸은 그 **분포로** 산출한다(중앙·최소·최대 시나리오를 **값으로** 적는다 · 단일 수 하나로 말하지 않는다).
    **조립·SDF 내보내기 시간을 포함할지 여부를 표에 명시**한다(v4-15 에서 실측된 것만 쓴다).
    ★ **병렬 굽기(한 GPU 에 여러 칸) 착수 0 · 추정 0** · 108칸 실제 굽기 착수 **0**.

 ㅇ **정지** — ②(다)(라)면 채택 0(갈래 C) · ①(라)면 정지(갈래 D) · 사후 갈래 신설 0 · 처방 0 ·
    문턱 이동 0 · 「채택」 선언은 ②(가)일 때만 «후보»로 적고 채택 여부는 전략 세션 몫이다.
```

### §0-6 예산 · 실행 규약

```
 ① 물리 0~1칸 재실행(비트 재현 2회) · ② 1칸 정착까지 + 컬러링 계기 · ③ 물리 0(산출).
 ★ **GPU 동시 실행 0** · 전부 배경 실행 + 로그 파일 · 캡처 0(§3) · 108칸 굽기 0.
```

---

## §1 집행

### §1-① 가슴 0.1782 진단 — **(가) 밴드 «정점 집합»이 다르다**(269 ↔ 270 · 정점 **1개**)

칸 `c100-h185-s40_L` · 값은 **v4-15 가 이미 낸 리포트 JSON 자신**에서 읽었다(새 계기 0 · 함정 44).

| 행 | n (v3 / v4) | domain (v3 / v4) | 중앙 v3 | 중앙 v4 | **중앙 차(mm)** |
|---|---|---|---|---|---|
| 목선 | 60 / 60 | 60 / 60 | 1.104873 | 1.103351 | −0.001522 |
| **가슴** | **269 / 270** ❌ | **269 / 270** ❌ | 4.040645 | 4.218848 | **+0.178203** |
| 허리 | 237 / 237 | 237 / 237 | 32.990236 | 33.003297 | +0.013061 |
| 밑단 | 116 / 116 | 116 / 116 | 19.004228 | 18.990206 | −0.014022 |
| 소매 | 1122 / 1122 | 1122 / 1122 | 4.988757 | 4.978811 | −0.009946 |

```
 ★ **밴드 다섯 중 «가슴»만 정점 수가 다르다**(269 → 270). 정점 대역 소속도 그 한 정점에서만
   갈린다 — 여유 **154 → 155**(눌림 0/0 · 밀착 115/115 동일).
 ★ **p25·p75 는 사실상 같다** — v3 1.000794 ~ 15.242251 ↔ v4 1.000457 ~ 15.245865
   (양끝 차 3.4e-04 · 3.6e-03 mm). **분포의 양끝이 같은데 «중앙»만 0.178 움직였다.**
 ★ 다른 네 행의 중앙 차는 **0.0015 ~ 0.0140 mm**(전부 새 정의 문턱 0.05 안) —
   **가슴만 한 자릿수 이상 크다**(0.178203).
 ★ 밴드 정의는 «현재 위치»로 정해진다(`fitReport.ts:80-86` — 몸통 패널 중 |y − chestY| ≤ 9 mm) ⟹
   **상태가 미세하게 다르면 경계의 정점이 들어오거나 나간다.** 이 칸의 특성(키 최대 185 · 어깨 최소 40)이
   그 경계에 정점을 몇 개 얹는지는 **이 판이 재지 않았다**(계기 폐기 · §2-5).
 ⟹ **갈래 (가)** — 「밴드 정점 집합이 v3·v4 에서 다르다(개수 269↔270 · 차집합 1)」.
   (다)의 전제(「집합은 같은데」)가 **깨졌으므로** (다)는 부정된다.
 ★ **원인 단정 0 · 귀속 0 · 처방 0**(회차 프롬프트 ①). 「정점 하나가 중앙값의 자리를 민다」는
   **읽기**이지 이 판의 판정이 아니다 — 값은 위 표가 전부다.
```

#### ㄹ 비트 재현 — **재현된다**(결정적 차이)

같은 칸을 **같은 실행기로 다시** 돌렸다(v4-15 이후 다른 프로세스 · 다른 시각):

| 채널 | v4-15 | 이 판 재실행 | 같은가 |
|---|---|---|---|
| `c100-h185-s40_L` 정착 · net | f10 · 9.012449e-05 | f10 · 9.012449e-05 | **인쇄 자릿수 전량 동일** |
| 〃 vs 정본 중앙 / 최대 | 0.005771 / 0.090124 | 0.005771 / 0.090124 | **동일** |
| `c100-h170-s45_M` 정착 · net | f10 · 9.000416e-05 | f10 · 9.000416e-05 | **동일** |
| 〃 vs 정본 중앙 / 최대 | 0.006600 / 0.090004 | 0.006600 / 0.090004 | **동일** |

⟹ 가슴 +0.1782 는 **결정적 차이**다(시행 잡음 아님). v4-15 §2-5 가 등재한 `cell` 흔들림은 이 칸들에서 결과를 바꾸지 않았다.

### §1-② 해소 커널 병렬화 — **(나)** ㄱㄴ 성립 · 「같은 옷」 성립 · **배수 ≤ 1.3**

신설 = `gpu/engine/selfcol_col.py`(`SelfCol` 상속) · `gpu/engine/full_sc_col.py`(`FullSC` 상속) ·
`gpu/l3rp_probe.py` · `gpu/l3rp_run.py`. **기존 `engine/` 11파일 바이트 불변**(§2-3).

#### ㄱ 색 분할 — **정점 공유 0**(전수 기계검증) · 색 수 **639**

우선순위 = **v3 접촉 순서**(`crank`) · 승자 = 「이웃(정점 공유) 중 내가 가장 이르다」 ⟹
**v3 순서 «우선» 탐욕 컬러링**. 기본몸 M · 한 서브스텝 · 접촉 **2,085**:

| 채널 | 값 |
|---|---|
| **색 수**(라운드 1024 · 완주) | **639** |
| 색을 못 받은 접촉(`left`) | **0** |
| **같은 색 안 정점 공유**(O(N²) 전수 검사) | **0** |
| 색별 접촉 수 | 앞 [36, 26, 24, 23, 22, 21, 18, 18, 18, 18] … 뒤 [1 × 10] (합 2,085) |

```
 ★ **꼬리가 길다** — 앞 색은 20~36개를 담는데 뒤로 갈수록 1개짜리 색이 수백 개다.
   색 수 639 = 「v3 순서 우선」이 만드는 «사슬 길이»이지 그래프의 채색수가 아니다.
 ★ 그래서 **라운드를 자르고 «꼬리는 직렬»** 로 처리하는 판을 함께 세웠다(`_tail_serial`).
   **자르고도 정확하다** — 승자 규칙상 「색을 받은 접촉의 «먼저» 이웃은 전부 색을 받았다」이므로
   (먼저 a · 나중 b · 이웃)의 네 경우가 모두 순서를 지킨다(b 만 색 있는 경우는 **불가능**).
```

#### ㄴ 병렬 확인 (함정 42 — 세서 본다 · 대조군을 같은 판에서 같이 잰다)

| 커널 | 종류 | grid × block | 호출/서브스텝 |
|---|---|---|---|
| **색 라운드**(청구·승자·해소 한 커널) | **`range_for`** | **768 × 128** | 라운드 수만큼 |
| (대조군) 직렬 해소 `_resolve …_serial` | `serial` | **1 × 1** | 1 |

⟹ 색 해소는 **`if` 밖 최상위 `range_for`** 로 섰다(grid > 1 · block 128). 대조군은 여전히 1 × 1 이다.

#### ㄷ 정확도 — **1서브스텝 «비트 동일»**

같은 초기 상태에서 직렬 해소판 ↔ 색 해소판, `substep_a` → 셀프 충돌 → `substep_b` **한 서브스텝**:

| 라운드 상한 | 색 수 | `left` | 정점 공유 | 해소 횟수(색 / 직렬) | **위치차 최대 성분(m)** |
|---|---|---|---|---|---|
| 32 | 32 | 1,513(꼬리로 처리) | 0 | **550 / 550** | **0.0** |
| 64 | 64 | 1,168(〃) | 0 | **550 / 550** | **0.0** |
| 128 | 128 | 931(〃) | 0 | **550 / 550** | **0.0** |
| 1024 | **639** | **0** | 0 | **550 / 550** | **0.0** |

★★ **순서 재현이 «깨지지 않았다»** — 네 설정 전부에서 위치가 **비트로 같다**.
v4-14 원리(「순서도 이식의 조건이다」)를 지키면서 병렬화가 **가능하다**는 것이 값으로 섰다.

#### ㄹ 속도 · 「같은 옷」 (기본몸 M · 정착까지 · 분모 = v4-15 기준선 **2.345 s/프레임**)

| 판 | s/프레임 | **배수** | 정착 | 도달 net | vs 정본 중앙 / 최대(mm) |
|---|---|---|---|---|---|
| 직렬 해소(기준선) | **2.345** | 1.000 | f10 | 9.000416e-05 | 0.006600 / 0.090004 |
| 색 해소 `rounds=16` | **3.643** | **0.644** | f10 | 8.990014e-05 | 0.006581 / 0.089900 |
| 색 해소 `rounds=64` | **7.523** | **0.312** | f10 | 8.994779e-05 | 0.006593 / 0.089948 |

「같은 옷」(**새 정의**) — 색 해소판(`rounds=16`) ↔ v3 정본: **대역 이름 5/5** · **게이트 `pass` true ·
`fails` []** · 표시값 1자리 **5/5**(병기) · 정점 대역 소속 5/5 · 대역 n 5/5 ⟹ **성립**.

```
 ⟹ **갈래 (나)** — ㄱㄴ 성립 + 「같은 옷」 성립인데 **배수가 1.3 이하**(0.644 · 0.312)다.
   색 분할은 **정확하지만 이 접촉 그래프에서는 «빠르지 않다»**.
 ★ 이유가 값에 있다 — 색 수 **639** 에 **1개짜리 색이 수백 개**다. 라운드마다 커널을 한 번 띄우므로
   라운드 수가 곧 비용이고, 라운드를 자르면 그만큼이 **직렬 꼬리**로 되돌아온다.
   (r16: 1,168개가 꼬리 · r64: 1,168 → 더 적지만 라운드 비용이 는다 ⟹ 둘 다 느리다.)
 ★ **채택 0** — 「후보」로도 적지 않는다(배수 < 1). 채택 여부는 전략 세션 몫이고 이 판은 값만 낸다.
 ★ **어긋남 1건(원인 미상 · 귀속 0)** — 1서브스텝은 «비트 동일»인데 10프레임 실행의 도달 net 은
   직렬 9.000416e-05 ↔ 색 8.990014e-05(r16) · 8.994779e-05(r64) 로 **갈린다**(상대 1.2e-03).
   1서브스텝 대조는 네 설정 전부 0.0 이었고, 직렬판은 회차를 넘어 **비트 재현**된다(§1-①ㄹ).
   **이 판은 원인을 단정하지 않는다** — 두 사실을 그대로 등재한다.
```

### §1-③ 108칸 예산 실측 — **현행 판**(②가 (나)이므로 · §0-4 ③)

v4-15 의 **8칸 + 기준선 칸 실측**에서 «읽어» 산출한다(새 실행 0 · 추정 규칙은 §0-5ㅅ).

| 칸 | 정착 f | s/프레임 | 실행 s | **JIT s** | 초기화 s | **칸 합 s** |
|---|---|---|---|---|---|---|
| `c100-h155-s45_L` | 20 | 3.037 | 60.7 | 62.5 | 2.7 | **125.9** |
| `c100-h155-s45_XL` | 30 | 3.152 | 94.6 | 61.7 | 2.7 | **158.9** |
| `c100-h155-s50_M` | 20 | 2.803 | 56.1 | 61.8 | 2.7 | **120.6** |
| `c100-h155-s50_S` | 30 | 2.724 | 81.7 | 62.6 | 2.7 | **147.0** |
| `c100-h170-s40_L` | 10 | 2.674 | 26.7 | 60.0 | 2.6 | **89.4** |
| `c100-h170-s45_M` | 10 | 2.345 | 23.5 | 9.5 | 1.5 | **34.4** |
| `c100-h185-s40_L` | 10 | 2.612 | 26.1 | 63.0 | 2.7 | **91.9** |
| `c122.5-h155-s40_L` | 20 | 3.107 | 62.1 | 60.2 | 2.4 | **124.7** |
| `c87.5-h155-s45_XL` | 20 | 2.804 | 56.1 | 49.1 | 2.2 | **107.3** |

**108칸 산출**(칸 합의 분포로 · 단일 수 하나로 말하지 않는다 · §0-5ㅅ)

| 시나리오 | 칸당 s | **108칸** |
|---|---|---|
| 최소(가장 빠른 칸) | 34.4 | **3,716 s = 1.03 시간** |
| **중앙** | **120.6** | **13,020 s = 3.62 시간** |
| 평균 | 111.1 | 12,001 s = 3.33 시간 |
| 최대(가장 느린 칸) | 158.9 | 17,165 s = 4.77 시간 |

```
 ★ **포함/미포함을 명시한다**(§0-5ㅅ) —
   **포함** = 장면 로드 + Taichi 초기화 + **JIT** + 물리 실행(정착까지).
   **미포함** = **조립·SDF 내보내기**(`scripts/v4Export.ts`) — v4-15 에서 **칸별로 실측되지 않았다**
   (여러 칸을 한 호출로 묶어 돌렸다) ⟹ **이 표에 넣지 않고 «없다»고 적는다.**
   **미포함** = 층3 다리(리포트·게이트 · 물리 0프레임 · v4-15 에서 칸당 약 30 s 규모였으나 개별 미실측).
 ★ **JIT 이 칸 시간의 절반이다** — 60 s 안팎이 여덟 칸에서 되풀이된다(기준선 칸만 9.5 s 인데
   그 실행은 같은 프로세스에서 두 번째로 선 커널이었다). **사실만 적는다 — 방안 0 · 추정 0.**
 ★ **정착 프레임 수가 10~30 으로 흩어진다**(10 셋 · 20 넷 · 30 둘) — 칸 시간의 나머지 절반이 여기서 온다.
 ★ **병렬 굽기(한 GPU 에 여러 칸) 착수 0 · 추정 0** · 108칸 실제 굽기 **착수 0**(회차 프롬프트 ③).
```

---

## §2 자기검사

### §2-1 집행 / 미집행 총계

| # | 무엇 | 계기 | 실행 |
|---|---|---|---|
| ① | 리포트 5행 밴드 수·중앙 대조 | v4-15 산출 JSON **읽기만** | 물리 0 |
| ①ㄹ | 비트 재현 재실행 2칸 | `gpu/l3br_run.py`(v4-15 · **diff 0**) | 24.1 + 24.0 s |
| ② | 색 분할 확인 4설정(32·64·128·1024 라운드) | `gpu/l3rp_probe.py`(**신설**) | 각 1~3분 |
| ② | 색 해소판 정착 실행 2설정(r16 · r64) | `gpu/l3rp_run.py`(**신설**) + `engine/selfcol_col.py`·`full_sc_col.py`(**신설**) | 36.4 + 75.2 s |
| ② | 「같은 옷」 판정(리포트·제품 대조·게이트) | 계기 3종(**diff 0**) | 물리 0프레임 |
| ③ | 108칸 예산 산출 | v4-15 산출 JSON **읽기만** | 물리 0 |

**실행하지 «않은» 것**

```
 108칸 굽기 **0** · 병렬 굽기 착수 0 · 추정 0 · 색 분할 «채택» 0 · 라운드 수 최적화 훑기는
 **네 설정 실측**으로 끝(문턱·규약 변경 0) · `src/`·`public/` 수정 0 · `engine/` 기존 11파일 수정 0 ·
 v3 재실행 0 · 굽기(정본 재생성) 0 · 캡처 0 · ①의 원인 단정·귀속·처방 **0** ·
 ②의 어긋남 1건 원인 단정 **0** · **ecc 에이전트 0 / ecc 스킬 0**.
```

### §2-2 arch · 태스크 · grid/block · 시간

| 채널 | 값 |
|---|---|
| 기계 | **`DESKTOP-OV72A46`** · win32 · GTX 1660 Ti · Taichi **1.7.4** · llvm 15.0.1 · py **3.12.10** |
| arch | **`Arch.cuda`** 전량 · assert 통과(조용한 폴백 0) |
| 정밀도 | **f64**(정본 · §0-2 ②) |
| grid × block | 색 라운드 커널 **768 × 128**(`range_for`) ↔ 직렬 해소 **1 × 1**(`serial`) |
| s/프레임(기본몸 M) | 직렬 **2.345** · 색 r16 **3.643** · 색 r64 **7.523** |
| degenerate / NaN / 발산 | **0 / 0 / 0**(이 판의 4개 정착 실행 전부) |
| `py -m pytest gpu/tests -q` | **40 passed · 2 xfailed · 1 xpassed** (58.02 s) — v4-10~15 와 **같은 값** |
| `npx tsc -b` | **통과**(exit 0) |

### §2-3 경계 · diff

| 채널 | 값 |
|---|---|
| `src/` · `public/` diff(기점 `2b13e85` ↔ 이 판) | **0** |
| `engine/` **기존 11파일** blob | **11/11 기점과 동일**(`full.py` · `full_color.py` · `full_color2.py` · `full_par.py` · `full_sc.py` · `selfcol.py` · `stretch.py` · `bending.py` · `seam.py` · `collide.py` · `ulp.py`) |
| 계기 diff 0 | `l3br_run.py` · `v4FitReport.ts` · `v4ProductCmp.ts` · `v4ProductGate.ts` |
| 이 판이 «만든» 코드 | `gpu/engine/selfcol_col.py` · `gpu/engine/full_sc_col.py` · `gpu/l3rp_probe.py` · `gpu/l3rp_run.py` · `scripts/v4BandDump.ts`(**폐기 · §2-5**) |
| 이 판이 «만든» 산출물 | `l3rp-probe-*` · `l3rp-<칸>-f64-cuda-r{16,64}` · `l3rp-band-*`(폐기분) · `fit-…-v4-16-col-r16` · `l3-gate-…-v4-16-col-r16` · `l3-product-cmp-col-r16` |
| 굽기 / 캡처 / ecc | **0 / 0 / 0** |

### §2-4 «순서 재현»의 바이트 증명

```
 ① 색 해소판은 `SelfCol` 을 **상속**하고 광역·협역·방출·순서(`_rank`)를 **한 줄도 다시 쓰지 않는다** —
    바뀐 것은 `_resolve`(직렬) → `_round`(색) + `_tail_serial`(꼬리) 둘이다.
 ② **1서브스텝 위치차가 네 설정 전부 0.0**(비트 동일) · 해소 횟수도 **550 = 550** ⟹
    「색 안 동시 갱신 = 그 색 안 순차 갱신」과 「색 사이 순서 = v3 순서」가 값으로 확인됐다.
 ③ 같은 색 안 **정점 공유 0**(O(N²) 전수 검사) — 동시 갱신의 전제가 기계로 확인됐다.
```

### §2-5 계기 사실 1건 — **밴드 덤퍼 폐기**(값 아님 · 처방 0)

```
 ①ㄱㄷ 를 위해 `scripts/v4BandDump.ts` 를 신설해 `fitReport.ts:77-92` 의 정의역 식을 인용했는데,
 그 계기가 낸 밴드 크기가 **리포트 자신의 `domain` 을 재현하지 못했다**
 (기본몸 M 가슴 **236** ↔ 리포트 **266** · 허리 240 ↔ 226).
 ⟹ **그 계기로 얻은 수는 이 판에서 «쓰지 않았다»**(폐기 · 파일은 이력으로 남긴다).
 ①은 **v4-15 가 낸 리포트 JSON 자신의 `n`·`domain`·중앙값**으로만 적었다(위 §1-① 표).
 ★ 어긋남의 원인은 **찾지 않았다**(이 판의 물음이 아니다) — 「계기가 정본을 재현하지 못하면
   그 계기의 수는 쓰지 않는다」는 규범(함정 12 계열)만 적용했다.
```

---

## §3 화면

**캡처 0.**

---

## §4 회차 갈래 — **B**(②(나) ⟹ 값 상신 · ③ 현행 판으로 집행) ⟹ 상신

| 항 | 갈래 | 한 줄 |
|---|---|---|
| ① 가슴 0.1782 | **(가)** | **가슴 밴드만 정점 수가 다르다**(269 ↔ 270 · 여유 154 → 155) · p25·p75 는 사실상 같고 **중앙만** 0.178 · 다른 네 행 0.0015~0.0140 · **비트 재현됨**(결정적) |
| ② 해소 병렬화 | **(나)** | ㄱㄴ 성립(색 639 · 정점 공유 0 · `range_for` 768×128) · **1서브스텝 비트 동일** · 「같은 옷」 성립 ·<br>**배수 0.644(r16) / 0.312(r64) ≤ 1.3** ⟹ 정확하지만 **빠르지 않다** · **채택 0** |
| ③ 108칸 예산 | 사실 | 칸 합 **34.4 ~ 158.9 s** ⟹ 108칸 **1.03 ~ 4.77 시간**(중앙 **3.62**) · **JIT 이 칸 시간의 절반** |

**넘기는 것 7건**

```
 1. ★★★ **가슴 0.1782 의 자리가 «밴드 경계»다** — 다섯 밴드 중 가슴만 정점 수가 269 → 270 이고
    분포의 양끝(p25·p75)은 같은데 중앙만 0.178 움직인다. **원인 단정 0** — 값이 그렇게 생겼다.
 2. ★★★ **순서를 지키면서 병렬화하는 것이 «가능»하다** — 색 분할 해소가 1서브스텝에서 직렬판과
    **비트 동일**이다(네 설정 전부). v4-14 원리는 「병렬화 못 한다」가 아니라 「순서를 지켜야 한다」였고,
    이 판이 지키는 방법을 값으로 보였다(v3 순서 우선 탐욕 + 꼬리 직렬).
 3. ★★★ **그런데 «빠르지 않다»** — 색 수 **639** 에 1개짜리 색이 수백 개다. 라운드가 곧 비용이라
    배수 0.644 / 0.312. **최적화 상계(v4-15 §1-③ 해소 36.65%)는 이 경로로는 회수되지 않는다.**
 4. ★★ **어긋남 1건**(원인 미상 · 귀속 0) — 1서브스텝은 비트 동일인데 10프레임 도달 net 이
    9.000416e-05 ↔ 8.990014e-05 / 8.994779e-05 로 갈린다. 직렬판 자신은 회차를 넘어 비트 재현된다.
 5. ★★ **108칸은 «시간»이 문제가 아니다** — 중앙 시나리오 **3.62 시간**(최대 4.77). 굽기 결정은
    시간이 아니라 다른 축(정답지 결손 2칸 · 조립·SDF 시간 미실측)에 걸린다.
 6. ★ **JIT 이 칸 시간의 절반**(60 s 안팎 × 칸마다) — 사실만 등재(방안 0).
 7. ★ **계기 하나를 폐기했다**(밴드 덤퍼가 리포트의 `domain` 을 재현하지 못했다) — 그 수는 쓰지 않았다.
```

**이 판이 «하지 않은» 판단** — 가슴 밴드 차이의 원인·처방 · 색 분할 채택 · 라운드 수 «최적» 선택 ·
②의 어긋남 원인 · 108칸 굽기 여부와 시점 · 밴드 덤퍼 어긋남의 원인.
**처방 0 · 채택 0 · 사후 갈래 신설 0.**
