# v4-13 — f64 × 병렬 «결합» 실측 · 정착 자리 확보 · 층3 첫 판정

**집행 기계 = 2호기(CUDA · 정본 기계) · `src/` diff 0 · 굽기 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-13-combine` |
| 기점 | **`f2d220c`** (= v4-12 HEAD · `09c7645`(§5 인용 대조) 다음 커밋 = 보고 규약 등재) |
| 워크트리 | `C:\Users\banba\fit-simulator` |
| 기계 | **`DESKTOP-OV72A46`** (win32 · CUDA · GTX 1660 Ti · Taichi 1.7.4 · llvm 15.0.1 · py 3.12.10) |
| §0 커밋 | **`7bd170f`** — 집행 «전» 별도 커밋(이 파일의 §0 + `CLAUDE.md` §0-2 두 줄만) |
| §1~§4 커밋 | (집행 후 자기 기입) · 볼트 등재 · metrics |
| 호출 ecc 에이전트/스킬 | **0 / 0** (`strategist` 서브에이전트도 0) |

---

## §0 수령 (집행 «전» 별도 커밋)

### §0-1 판정문 (전략 세션 · v4-12 §4 수령 · 원문)

```
 「갈래 A 수령 · ★ ①(가) 수용 — f64 가 v3 정답지를 f10·f20·f40·f60 인쇄 자릿수 전량 재현 ·
   두 arch 동일(9.471112e-03) ⟹ 이식 정확 «완결» · ★ 미수렴 원인 = f32 확정 ·
   ★ ③(가) 수용 — 병렬 57.34배(grid 17~209 × block 128 · IR range_for 28 · 물리 식 0줄) ·
   v4-10 「병렬화로 안 빨라진다」 완전 기각 ·
   ★ ② 두 읽기 = 전략 세션이 «(가) 정밀도 증폭»으로 확정(f=0 0.5 ULP · s=1 5.5 ULP ·
     배가마다 2.5~3.6배 점프 0 · maxAt 이동 · ①과 독립 합류) ⟹ 다음 판은 «자리 찾기» 안 간다 ·
     갈래 문언 「1스텝」 = 전략 세션 귀책(함정 43) ·
   ★ 함정 44 = CC 귀책(노트 미개봉 · 기억/역산 보고) — 정본 수정 0 · 회수 0 ·
     보고 규약에 「수는 노트에서 읽어 옮긴다」 추가 ·
   ★★ 이식 «존속» — 정확성(f64)과 속도(병렬)가 처음으로 동시에 섰다 · 남은 문제는 결합 하나」
```

**귀책 등재 2건**(판정문이 지정한 대로 · 이 표가 볼트의 정본이다)

| # | 무엇 | 귀책 | 근거 |
|---|---|---|---|
| 1 | 갈래 ②의 문언 「**1스텝** 단위의 논리 차이」가 계의 단위와 어긋났다(f=1 = 229 서브스텝 = 10,282,680 투영) | **전략 세션** | 함정 43 · v4-12 §0-3 이 쓴 문언 · v4-12 §1-② 보조 채널이 값으로 드러냄 |
| 2 | v4-12 를 인용한 보고가 노트를 열지 않고 **기억·역산**으로 값을 채웠다(5.837018e-07 = v4-02 값 · 1.602 = 12.690 ÷ 7.92) | **CC** | 함정 44 · v4-12 §5 · 정본 수정 0 · 회수 0 |

### §0-2 사무 (이 커밋에서 «함께» 집행)

`CLAUDE.md` 의 v4 보고 규약에 두 줄을 더한다:

```
 ① **모든 수는 노트에서 읽어 옮긴다 — 기억·역산 금지**(함정 44).
 ② **갈래 문언에 «단위»를 명시한다**(프레임 / 서브스텝 / 투영 · 함정 43).
```

### §0-3 바꾸지 않는 것 (회차 프롬프트 원문)

```
 `src/` · 정본 · blob · v3 동결 · `engine/` **물리 식 0줄**(바꿔도 되는 것은 **dtype·arch 스위치**뿐).
 ⟹ 이 판이 만드는 것은 **새 파일**뿐이다(결합 실행기 · 수렴 실행기 · v3 프레임 지정 덤프).
 ★ `full.py` · `full_color.py` · `full_color2.py` · `full_par.py` · 커널 4종 **바이트 불변**.
 ★ v4-02~12 산출물 바이트 불변(이 판 산출은 새 접두 `l3cb2-` · `l3conv-` 로 판다).
 ★ 셀프 충돌 이식 **0** · 혼합 정밀도 **0** · 최적화·튜닝 **0**(block_dim · 색 순서 등 어떤 것도).
```

### §0-4 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · **단위 명시** · 함정 43)

**① 결합 실측 3종** — 같은 칸 `c100-h170-s45_M` · 같은 초기 상태(정착 blob) · **60프레임**.
ㄱ `full_par` × **f64** × **CUDA** / ㄴ `full_par` × **f64** × **x64** / ㄷ `full_par` × **f32** × **CUDA**.

**★ 단위** — 「프레임」 = **1/60 s = 229 서브스텝 = 229 × 44,920 = 10,282,680 투영**.
「s/프레임」의 분모도 이 프레임이다. 창 순변위의 창 = **10프레임**(N_WIN).

| 갈래 | 조건 (단위 = **프레임**) |
|---|---|
| **(가)** | ㄱ 또는 ㄴ 이 «감소 추세»(아래 ㉠㉡) **이고** 그 판의 **s/프레임 ≤ 2.0** |
| **(나)** | 감소하나 느리다(= 감소 성립 · s/프레임 > 2.0 · 값) |
| **(다)** | 병렬 f64 가 **진동**한다(값 · 「색 순서 × 정밀도」 결합 효과로 등재) |
| **(라)** | 실행 불가(사유) |

**② 정착 자리 확보** — ①에서 고른 판 하나를 v3 문턱에 «도달할 때까지» 돌린다(상한 **400프레임**).
같은 프레임까지 v3 도 돌려 위치 덤프를 낸다. 두 최종 상태의 좌표차(최대·중앙·p99).

**③ 층3 첫 판정** — ②의 두 최종 상태로 리포트 5행을 내고 **제품 문턱**으로 판정한다.

| 갈래 | 조건 |
|---|---|
| **(가)** | 5행 **표시값·대역·게이트 전부 일치** ⟹ 「같은 옷」 **첫 성립** |
| **(나)** | **대역·게이트는 일치**하나 표시값 일부 불일치(행·값) |
| **(다)** | **대역 또는 게이트 불일치** |

**④** 셀프 충돌 이식 착수 **0** · 혼합 정밀도 착수 **0** · 최적화 **0**.

**§4 회차 갈래** — A = ①②③ 전량 + ①(가) + ③(가)(나) ⟹ 상신 /
B = ①(나) ⟹ 값 상신 · ②③ 은 «가장 빠른 감소판»으로 집행 / C = ①(다)(라) ⟹ 값·사유 상신 · ②③ 착수 0 /
D = ③(다) ⟹ 값 상신 · **정지** / E = 그 외 ⟹ 정지. **사후 갈래 신설 0.**

### §0-5 집행 «전» 사전 결정 8건 (값을 보기 «전»에 적는다)

```
 ㄱ **① 「감소 추세」의 정의 = v4-12 §0-4ㄱ 를 «그대로»** (새 수 0 · 단위 = 프레임):
    ㉠ net(f60) ≤ net(f10)/10  그리고  ㉡ f40·f50·f60 이 net(f10) 미만이고 «단조 비증가».
    창 순변위 식·N_WIN(10)·창 정의는 v4-10/11/12 와 **같은 것**을 쓴다.
    「진동」(다) = ㉠㉡ 중 하나라도 불성립. **f32 판(ㄷ)은 진동이 «기대값»이다**(v4-12 ①에서 확정) ⟹
    (다) 판정은 **f64 판(ㄱㄴ)에만** 건다.

 ㄴ **① s/프레임 문턱 2.0** — 회차 프롬프트가 지정한 값. JIT 은 벽시계 «밖»(v4-12 와 같은 처분:
    1프레임 예열 후 상태를 되감는다). 배수의 분모는 v4-12 와 같은 **12.690**(v4-10 직렬)로 고정.

 ㄷ **② 고르는 규칙**(값 보기 «전») — ①에서 **감소 성립**한 판 중 **s/프레임 최소**.
    소수 셋째 자리까지 동률이면 **CUDA 우선**(GPU 를 쓰는 것이 이 축의 목적이므로).
    감소 성립이 **하나도 없으면** ② 착수 0(갈래 C).

 ㄹ **② 수렴 문턱은 «읽는다»**(손 상수 0) — v3 산출물 헤더의 `tol` 필드
    (`gpu/oracle/export/cellconv7-c100-h170-s45_M-all-e0-x0.json`)에서 뜬다. 그 값은
    `s4Gate.ts:settleNetM` = `TOL_SELF` 가 v3 실행 시점에 새긴 것이다. 판정식 = 창 순변위 ≤ tol.
    ★ 미도달이면 **400프레임에서 멈추고** 그 사실과 최종 net 을 등재한다(갈래 없음 · 사실 등재).

 ㅁ **② v3 쪽의 «프레임 F»** — F = v4 가 문턱에 도달한 프레임(미도달이면 400).
    v3 는 **같은 F** 까지 돌린다. 신설 `scripts/v4FrameAt.ts` 가 `v4CellConverge.ts` 의
    물리 경로(`prepare` · `sc.cons` = all · `params`)를 **그대로 인용**한다(물리 식 0줄).
    ★ **미리 읽어 둔 사실(이 판의 실측 아님)** — v4-07 산출물
      `cellconv7-…-all-e0-x0.json` 이 v3 의 `convFrame` **80** · `convNet`
      **8.066670907700614e-05** 를 이미 등재하고 있고 그 프레임의 위치 덤프(`…-x0.bin`)도 있다.
      **F = 80 이면** 그 산출물을 «대조군»으로 함께 인용하고 신설 실행분과 **비트 동일**인지 본다
      (같으면 회차 간 재현 · 다르면 그 사실을 등재). **F ≠ 80 이면 신설 실행분만 쓴다.**

 ㅂ **③ 판정 규칙은 코드에서 «인용»**(v4-10 §1-② 경로 재사용 · 새 규칙 0 · 손 상수 0):
    리포트 = `scripts/v4FitReport.ts`(→ `src/v3/fitReport.ts:buildFitReport`) ·
    대조 = `scripts/v4ProductCmp.ts`(표시 반올림 `V3ProductV1.tsx:348` · 대역 `phaseOf` ·
    경계 `consts.ts:SEP`) · 게이트 = `scripts/v4ProductGate.ts`(`runS4Gate` · `S4_THRESHOLD`).
    ★ 세 파일 모두 **이 판에서 수정 0**(인용만). 물리 0프레임.

 ㅅ **③ 게이트의 정착 채널** — `v4ProductGate.ts` 의 `NET=` 규약대로 그 판의 **마지막 창 순변위**를
    넣는다(v4 는 이 판의 실행 로그 · v3 는 신설 실행분의 net). 문턱은 `S4_THRESHOLD` 에서 뜬다.

 ㅇ **①ㄷ(f32 CUDA) 는 «재실행»한다** — v4-12 값 재사용이 허용됐으나, 신설 실행기가 v4-12 의
    `l3par_run.py` 와 같은 것을 계산하는지 **비트로 확인**하는 데 쓴다(13.3 s · 싸다).
    기대 = v4-12 산출 `l3par-…-f32-f60.bin` 과 **좌표차 0.000 mm**. 다르면 그 사실을 등재하고
    ①의 배수·궤적은 **신설 실행분**으로 적는다.

 ㅈ **정지** — ③(다)면 갈래 D 로 **정지**(처방 0). 발산·NaN 이면 그 프레임을 등재하고 (라)/(다).
    사후 갈래 신설 0 · 처방 0 · 최적화 0 · 「같은 옷」 선언은 ③(가)일 때만 하고 그 밖에는 하지 않는다.
```

### §0-6 예산 · 실행 규약

```
 ① 60프레임 × 3종. 추정 — ㄷ f32 CUDA 0.221 s/프레임(v4-12 실측) · ㄱㄴ 은 미지(f64 는 직렬에서
   CUDA 69.280 ↔ x64 0.963 이었다 · **곱해서 추정하지 않는다** · 실측만 적는다).
 ② 상한 400프레임(v4) + 같은 F 프레임(v3 · v3 는 9,525 ms/프레임 실측 = v4-12 §2-1).
 ③ 물리 0프레임(리포트·대조·게이트 전부 읽기).
 ★ **GPU 동시 실행 0** — CUDA 를 쓰는 실행은 줄 세운다. x64 실행만 CPU 이므로 v3(Node)와 동시 가능.
 ★ 전부 배경 실행 + 로그 파일. 캡처 0(§3).
```

---

## §1 집행

### §1-① 결합 실측 3종 — **(가)** ㄱㄴ 둘 다 «감소 추세» · s/프레임 0.355 / 0.699 ≤ 2.0

계기 = `gpu/l3cb2_run.py`(신설) + `gpu/par_probe2.py`(신설). 둘 다 `engine/full_par.py` 를 **그대로**
부른다(물리 식 0줄 · blob 불변 · §2-3). 같은 칸 `c100-h170-s45_M` · 같은 초기 상태(정착 blob ·
f64 판은 **f64 원본**, f32 판은 f32 캐스팅 — v4-12 와 «같은» 처분) · 제약 `all` 44,920
(늘어남 18,016 + 굽힘 26,648 + 봉제 256) · substeps 229 · N_WIN 10 · **60프레임**.

#### ㄱ 병렬 확인 (실행 «전» · `py gpu/par_probe2.py <arch> <fp>` · 정본 `docs/v4/13-par-tasks.txt`)

| 판 | 태스크/호출 | 코드에서 «센» 수 | range_for | serial | grid>1 | grid 범위 | block |
|---|---|---|---|---|---|---|---|
| **ㄱ** f64 × CUDA | **29.00** | 29 | **28종** | 1종 | **26종** | **1~209** | **128** |
| **ㄴ** f64 × x64 | **29.00** | 29 | **28종** | 1종 | 0종 | **0~0** | **0** |
| **ㄷ** f32 × CUDA | **29.00** | 29 | **28종** | 1종 | **26종** | **1~209** | **128** |

```
 ★ 세 판 모두 **태스크/호출 29.00 == 코드에서 센 29**(§0-5 · v4-12 §0-4ㄹ 와 같은 정의).
 ★ **x64 의 grid/block 이 0 인 것은 «폴백»이 아니다** — `current_cfg().arch` 는 `Arch.x64` 로
   assert 를 통과했고(§2-2), CPU 프로파일러가 grid/block 을 «내지 않는» 것이다(레코드는 29개 그대로).
   ⟹ x64 의 병렬 여부는 grid 로 못 세고 **태스크 수 29 와 벽시계로만** 읽는다(사실 등재 · 단정 0).
 ★ f64 CUDA 의 최상위 비용은 **봉제 serial 커널 26.643 ms**(5호출 누적 · grid 1 × block 1) —
   나머지 28개 range_for 의 합보다 크다. **분해·처방은 하지 않는다**(이 판의 물음이 아니다 · §0-3).
```

#### ㄴ 속도 · 궤적 (60프레임 · JIT 은 벽시계 «밖» · 단독 실행 · GPU 동시 실행 0)

| 채널 | **ㄱ** f64 × CUDA | **ㄴ** f64 × x64 | **ㄷ** f32 × CUDA |
|---|---|---|---|
| `current_cfg().arch` (assert) | **`Arch.cuda`** ✅ | **`Arch.x64`** ✅ | **`Arch.cuda`** ✅ |
| 벽시계 / 60프레임 | **21.286 s** | 41.913 s | 13.468 s |
| **s/프레임** | **★ 0.355** | 0.699 | 0.224 |
| 직렬 12.690 대비 배수 | **35.77배** | 18.17배 | 56.53배 |
| JIT(벽시계 밖) · 초기화 | 29.408 s · 1.299 s | 28.376 s · 0.763 s | 5.168 s · 1.812 s |
| **degenerate / NaN / 발산** | **0 / 0 / 0** | **0 / 0 / 0** | **0 / 0 / 0** |
| 전 정점 유한(`finite`) | true | true | true |

창 순변위 궤적(단위 = **m** · 창 = **10프레임**)

| f | 10 | 20 | 30 | 40 | 50 | 60 |
|---|---|---|---|---|---|---|
| **ㄱ** f64 CUDA | **1.549149e-02** | 2.349863e-03 | 1.208024e-03 | 7.782157e-04 | 5.641285e-04 | **2.684565e-04** |
| **ㄴ** f64 x64 | **1.549149e-02** | 2.349863e-03 | 1.208024e-03 | 7.782157e-04 | 5.641285e-04 | **2.684565e-04** |
| **ㄷ** f32 CUDA | 2.712393e-02 | 2.310992e-02 | 1.889386e-02 | 2.789009e-02 | 1.759260e-02 | **3.062315e-02** |
| (인용) v3 정답지 | 1.599926e-02 | 3.992115e-03 | 1.181904e-03 | 7.531095e-04 | 5.404642e-04 | 2.530865e-04 |
| (인용) v4-12 f64 **직렬** x64 | 1.599926e-02 | 3.992115e-03 | 1.181904e-03 | 7.531095e-04 | 5.404642e-04 | 2.530865e-04 |

#### ㄷ 사전 등재 판정식 적용 (§0-5ㄱ · 값을 보기 «전»에 적은 식 그대로)

```
 **ㄱ f64 × CUDA**
   ㉠ net(f60) ≤ net(f10)/10 → 2.684565e-04 ≤ 1.549149e-03  **성립**(실제 비 **1/57.71**)
   ㉡ f40·f50·f60 < net(f10) 이고 단조 비증가 → 7.78e-04 ≥ 5.64e-04 ≥ 2.68e-04  **성립**
   s/프레임 **0.355 ≤ 2.0**  **성립**  ⟹ **(가)**
 **ㄴ f64 × x64**
   ㉠ 2.684565e-04 ≤ 1.549149e-03 **성립**(비 **1/57.71** · ㄱ 과 인쇄 자릿수 전량 같다)
   ㉡ **성립** · s/프레임 **0.699 ≤ 2.0** **성립**  ⟹ **(가)**
 **ㄷ f32 × CUDA** — 진동(2.71e-02 ~ 3.06e-02 · 감소 아님). **(다) 판정을 걸지 않는다**(§0-5ㄱ:
   f32 의 진동은 v4-12 ①에서 이미 «기대값»으로 확정됐고, (다)는 **f64 판에만** 거는 갈래다).
 ⟹ **① 갈래 = (가)** — 「병렬 f64 가 진동한다」(다)는 **값으로 부정**됐다.
   ★ 이 판이 처음 보인 것: **색 분할(병렬) × f64 의 결합에서도 감소 추세가 그대로 산다.**
     v4-12 가 「하지 않았다」고 남긴 결합(§4 넘기는 것 4번)이 여기서 값이 됐다.
```

#### ㄹ 대조 3건 (`gpu/l3cb2_cmp.py` 신설 · 물리 0프레임 · 읽기만)

| 대조 | 중앙 | p99 | 최대 정점거리 | 최대 좌표차 | maxAt | 바이트 동일 |
|---|---|---|---|---|---|---|
| **ㄷ(이 판 f32) ↔ v4-12 `l3par` f32** | **0.000 mm** | 0.000 | **0.000 mm** | 0.000 | — | **true** |
| **ㄱ(f64 CUDA) ↔ ㄴ(f64 x64)** | 2.615072e-10 mm | 6.264924e-09 | **4.024419e-08 mm** | 3.424513e-08 | 7746 | false |
| **v4-12 f64 «직렬» x64 ↔ ㄱ f64 «병렬» CUDA** | **0.012199 mm** | 0.435998 | **4.188471 mm** | 4.085628 | 7693 | false |

```
 ★ 1행 — 사전 결정 ㅇ 의 «기대»가 그대로 맞았다: 신설 실행기가 v4-12 `l3par_run.py` 와
   **바이트 동일**한 결과를 낸다(재실행 13.5 s). ⟹ ①의 배수·궤적은 두 판이 **같은 것을 잰다**.
 ★ 2행 — 두 arch 의 f64 결과 차이는 **최대 4.02e-08 mm**(중앙 2.6e-10 mm). v4-12 ①이 f=1 에서
   본 「두 arch 동일(9.471112e-03)」이 **60프레임 뒤에도** 자릿수 규모로 유지된다(사실 등재).
 ★ 3행 — **같은 f64 인데 «색 순서»만 다르면** 60프레임 뒤 중앙 **0.0122 mm** 갈린다.
   대조: f32 에서 병렬↔직렬은 중앙 **0.642 mm**(v4-12 §1-③ㄴ) ⟹ f64 에서 **약 53배 작다**.
   **판정 0**(§0-3 · 색 순서 차이는 v4-10 §0 이 이미 등재한 «알려진» 갈림이다).
```

### §1-② 정착 자리 확보 — **F = 80프레임에서 문턱 도달**(v4 · v3 «둘 다»)

고른 판 = **ㄱ f64 × CUDA**. 규칙은 §0-5ㄷ(값 보기 «전» 등재) 그대로 —
감소 성립한 판 ㄱ·ㄴ 중 **s/프레임 최소**(0.355 < 0.699). 동률 아님 ⟹ CUDA 우선 규칙은 쓰지 않았다.

문턱은 **읽었다**(§0-5ㄹ · 손 상수 0) — `cellconv7-c100-h170-s45_M-all-e0-x0.json` 의 `tol`
= **1e-4**(= `s4Gate.ts:settleNetM` = `TOL_SELF`). 상한 400프레임.

| 채널 | **v4** (f64 × 병렬 × CUDA · `l3cb2_run.py`) | **v3** (f64 · Node · `scripts/v4FrameAt.ts` 신설) |
|---|---|---|
| 도달 프레임 | **80** | **80** |
| 도달 시 창 순변위 | **8.538170e-05** (≤ 1e-4) | **8.066671e-05** (≤ 1e-4) |
| 벽시계 | **28.404 s** (0.355 s/프레임) | **749.677 s** (9,370.96 ms/프레임) |
| degenerate / 발산 | 0 / 없음 | — / 없음 |
| 상한 400 도달 | **아니오**(80에서 정지) | — (F=80 못 박음) |

창 순변위 궤적 (m · 창 10프레임)

| f | 10 | 20 | 30 | 40 | 50 | 60 | **70** | **80** |
|---|---|---|---|---|---|---|---|---|
| **v4** f64 병렬 | 1.549149e-02 | 2.349863e-03 | 1.208024e-03 | 7.782157e-04 | 5.641285e-04 | 2.684565e-04 | **1.353730e-04** | **8.538170e-05** |
| **v3** | 1.599926e-02 | 3.992115e-03 | 1.181904e-03 | 7.531095e-04 | 5.404642e-04 | 2.530865e-04 | **1.341130e-04** | **8.066671e-05** |

```
 ★ **v3 도 정확히 f=80 에서 처음 문턱을 넘는다** — 이 판이 새로 돌린 v3(신설 계기)의 `convFrame`
   **80** · `convNet` **8.066670907700614e-05** 는 v4-07 산출물 `cellconv7-…-all-e0-x0.json` 이
   등재한 값과 **같은 수**다(§0-5ㅁ 가 «미리 읽어 둔» 사실).
 ★ **대조군 = 바이트 동일** — v4-07 의 `cellconv7-…-e0-x0.bin` ↔ 이 판 신설 실행분
   `l3conv-v3-…-f80.bin` 의 좌표차 **0.000 mm**(중앙·p99·최대 전부 · `bytesIdentical` true).
   ⟹ 신설 스크립트가 `v4CellConverge.ts` 와 **같은 물리 경로**임이 값으로 확인됐다(회차 간 재현).
 ★★ **v4 가 «정착 자리»에 처음 섰다.** v4-10 게이트에서 v4 의 정착 채널은 14.2843 mm(문턱 0.1 mm)
   로 실패했었다 — 이 판의 v4 는 **0.0854 mm** 로 통과한다(§1-③ 게이트 표).
```

두 최종 상태(f=80)의 좌표차 — `gpu/l3cb2_cmp.py`

| 채널 | 값 |
|---|---|
| **중앙 정점거리** | **0.011974 mm** |
| **p99 정점거리** | **0.439469 mm** |
| **최대 정점거리** | **4.186409 mm** (정점 **7693**) |
| 최대 좌표차(성분) | 4.088415 mm |
| 평균 변위 (x, y, z) | (+4.274e-04, −4.467e-03, +4.057e-03) mm |

### §1-③ 층3 첫 판정 — 규칙은 코드에서 «인용»(v4-10 §1-② 경로 · 새 규칙 0 · 손 상수 0)

계기 = `scripts/v4FitReport.ts` · `scripts/v4ProductCmp.ts` · `scripts/v4ProductGate.ts` —
**세 파일 다 이 판에서 수정 0**(§0-5ㅂ · §2-3). 물리 **0프레임**. 입력은 §1-② 의 두 `.bin` 뿐이다.

#### ㄱ 리포트 5행 (`buildFitReport` · 같은 계산 경로에 «상태만» 갈아 끼웠다)

| 행 | v3@80 중앙 | **v4@80 중앙** | 표시 1자리 (v3 / v4) | 같은가 | 대역 이름 | 같은가 | 정점 대역 [눌림,밀착,여유] | 같은가 | 대역 n |
|---|---|---|---|---|---|---|---|---|---|
| 목선 | 1.064980 | **1.064658** | **1.1mm / 1.1mm** | ✅ | 밀착 / 밀착 | ✅ | [0,41,13] / [0,41,13] | ✅ | 54 / 54 ✅ |
| 가슴 | 4.034180 | **4.061852** | **4.0mm / 4.1mm** | ❌ | 여유 / 여유 | ✅ | [0,111,150] / [0,111,**151**] | ❌ | **261 / 262** ❌ |
| 허리 | 26.731447 | **26.731787** | **26.7mm / 26.7mm** | ✅ | 여유 / 여유 | ✅ | [0,0,228] / [0,0,228] | ✅ | 228 / 228 ✅ |
| 밑단 | 19.143135 | **19.169818** | **19.1mm / 19.2mm** | ❌ | 여유 / 여유 | ✅ | [0,8,102] / [0,8,102] | ✅ | 110 / 110 ✅ |
| 소매 | 4.504774 | **4.512979** | **4.5mm / 4.5mm** | ✅ | 여유 / 여유 | ✅ | [0,323,595] / [0,323,595] | ✅ | 918 / 918 ✅ |
| **합계** | | | **3/5** | | **5/5** | | **4/5** | | **4/5** |

진단 2자리(`FitReportTable.tsx:46-47` `toFixed(2)`) — **0/5 일치**. 어긋난 자리는 전부 p25·p75 다:
목선 `1.06 (1.00~1.93)` ↔ `1.06 (1.00~1.89)` · 가슴 `4.03 (1.00~15.24)` ↔ `4.06 (1.00~15.29)` ·
허리 `26.73 (15.69~70.54)` ↔ `26.73 (15.69~70.53)` · 밑단 `19.14 (7.05~39.01)` ↔ `19.17 (7.08~38.99)` ·
소매 `4.50 (1.02~16.32)` ↔ `4.51 (1.02~16.28)`. **중앙 2자리는 목선·허리 2행이 같다.**

행별 중앙 차(mm · B−A): 목선 **−3.220e-04** · 가슴 **+2.767e-02** · 허리 **+3.405e-04** ·
밑단 **+2.668e-02** · 소매 **+8.205e-03**.

```
 ★ **불일치 2행의 실체는 «반올림 경계»다** — 가슴 4.034180 ↔ 4.061852 (차 0.0277 mm) 가
   4.0 ↔ 4.1 로 갈리고, 밑단 19.143135 ↔ 19.169818 (차 0.0267 mm) 가 19.1 ↔ 19.2 로 갈린다.
   **문턱을 옮기지 않는다**(함정 14) — 규칙은 `V3ProductV1.tsx:348` 의 `toFixed(1)` 그대로다.
 ★ **대역 정의역 n 의 어긋남도 정점 «한 개»다** — 가슴 261 ↔ 262. 대역 정의역은
   `fitReport.ts:80-86` 의 `|y − cy| ≤ 9 mm` 이고, 그 경계에 정점 하나가 v4 쪽에서만 들어왔다.
   정점 대역 소속의 유일한 어긋남(여유 150 ↔ 151)도 **같은 정점 하나**다(눌림·밀착은 전 행 일치).
```

#### ㄴ 게이트 판정 (`runS4Gate` · 물리 0스텝 · 문턱은 `S4_THRESHOLD` 에서 뜬다)

| 채널 | 문턱 | **v3@80** | **v4@80** |
|---|---|---|---|
| ③a 관통 최대 | ≤ 0.5 mm | 0.4195 (963정점) ✅ | **0.6599 (967정점) ❌** |
| **자기관통 교차** | = 0 | **430** ❌ | **428** ❌ |
| 목선 초과비 | ≤ 1 | 0.98914 ✅ | 0.98916 ✅ |
| 보조 장치(invMass=0) | = 0 | 0 ✅ | 0 ✅ |
| 발산 | 없음 | 없음 ✅ | 없음 ✅ |
| **정착 창 순변위**(인용) | ≤ 0.1 mm | **0.0807 mm** ✅ | **0.0854 mm** ✅ |
| 최소 쌍거리 | (참고) | 0.0018001 mm | 0.0003509 mm |
| 봉제 간극 중앙 / 최대 | (참고) | 2.0188 / 5.1787 mm | 2.0204 / 5.1758 mm |
| λ 최대 | (참고) | 1.443553 | 1.442836 |
| **`pass`** | — | **false** | **false** |
| **`fails`** | — | 교차 **1건** | 관통 + 교차 **2건** |

```
 ★ **정착 채널이 v3·v4 둘 다 처음 통과한다**(0.0807 · 0.0854 ≤ 0.1 mm).
   v4-10 의 v4@180 은 이 자리에서 14.2843 mm 로 실패했었다 — 그때 v4 는 정착한 적이 없었다.
 ★ 이 정의역(셀프 충돌 이식 0)에서는 **v3 도 게이트를 통과하지 못한다**(교차 430) — v4-10 과 같다.
 ★ v4 만 실패한 항 = **③a 관통 0.6599 mm > 0.5 mm**(v3 는 0.4195). 정점 수는 963 ↔ 967 로 가깝다.
   **원인 단정·처방 0**(§0-3 · 이 판의 물음이 아니다).
```

#### ㄷ 갈래 판정 — **두 읽기를 둘 다 적는다**(CC 가 고르지 않는다 · v4-10 §1-② · v4-12 §1-② 와 같은 처분)

```
 (가) = 「5행 표시값·대역·게이트 **전부** 일치」 — 표시값 **3/5** 이므로 **부정**.

 ㉠ **「대역」 = 대역 «이름» · 「게이트 판정」 = `pass` 값** 으로 읽으면
    → 대역 이름 **5/5 일치** · 게이트 `pass` **false == false 일치** · 표시값만 **3/5**
    ⟹ **(나) = 대역·게이트는 일치하나 표시값 일부 불일치**(가슴 4.0↔4.1 · 밑단 19.1↔19.2).

 ㉡ **「대역」 = 정점 «대역 소속» · 「게이트 판정」 = `fails` 집합** 으로 읽으면
    → 대역 소속 **4/5**(가슴 여유 150↔151 = 정점 1개) · `fails` **1건 ↔ 2건 불일치**
    ⟹ **(다) = 대역 또는 게이트 불일치.**

 ★ 두 읽기의 «가름»은 규칙 문언에 있고 값에 있지 않다 — 회차 프롬프트 ③은 채널을
   「표시 반올림 후 일치 n/5 · **대역 소속** 일치 n/5 · **게이트 판정** 일치 여부」로 적었고,
   v4-10 §1-② 는 같은 자리에서 **대역 «이름»**과 **`pass` 값**을 나란히 등재했다.
   **어느 쪽으로 셀지는 규칙을 쓴 전략 세션 몫이다**(v4-12 ②와 같은 처분 · CC 선택 0).
 ★ 어느 읽기든 **바뀌지 않는 값**: 표시값 3/5 · 대역 이름 5/5 · 대역 소속 4/5 ·
   대역 정의역 n 4/5 · 진단 2자리 0/5 · `pass` 둘 다 false · `fails` 1건 ↔ 2건 ·
   정착 채널 **둘 다 통과**.
 ★ 「같은 옷」 **선언 0** — ③(가)가 아니므로 선언하지 않는다(§0-5ㅈ).
```

### §1-④ 착수 0 (회차 프롬프트 ④)

```
 셀프 충돌 이식 **0** · 혼합 정밀도 **0** · 최적화·튜닝 **0**(block_dim · 색 순서 · 봉제 컬러링 ·
 `serialize` 해제 — 어떤 것도 건드리지 않았다. §1-①ㄱ 이 봉제 serial 26.643 ms 를 «보았지만»
 그것은 값 등재이고 처방이 아니다).
```

---

## §2 자기검사

### §2-1 집행 / 미집행 총계

**집행한 것**

| # | 무엇 | 계기 | 실행(벽시계) |
|---|---|---|---|
| ①ㄱ | 병렬 확인 3종(f64 CUDA · f64 x64 · f32 CUDA) | `gpu/par_probe2.py`(**신설**) | 각 1분 미만(5호출 · 물리 5서브스텝) |
| ①ㄴ | **f64 × 병렬 × CUDA 60프레임** | `gpu/l3cb2_run.py`(**신설**) | **21.286 s**(+ JIT 29.408 s) |
| ①ㄴ | **f64 × 병렬 × x64 60프레임** | 〃 | **41.913 s**(+ JIT 28.376 s) |
| ①ㄴ | **f32 × 병렬 × CUDA 60프레임**(재실행) | 〃 | **13.468 s**(+ JIT 5.168 s) |
| ①ㄹ | 대조 3건(f32 비트 재현 · arch 간 · v4-12 f64직렬 간) | `gpu/l3cb2_cmp.py`(**신설**) | 물리 0프레임 |
| ② | **f64 × 병렬 × CUDA 수렴 실행**(상한 400 · f80 도달) | `l3cb2_run.py … 400 conv` | **28.404 s**(+ JIT 4.228 s) |
| ② | **v3 를 F=80 까지** + 위치 덤프 | `scripts/v4FrameAt.ts`(**신설**) | **749.677 s**(9,371 ms/프레임) |
| ② | 대조 2건(v4-07 대조군 · v3↔v4 최종 상태) | `gpu/l3cb2_cmp.py` | 물리 0프레임 |
| ③ | 리포트 5행 2건(v3@80 · v4@80) | `scripts/v4FitReport.ts`(**diff 0** · 인용) | 물리 0프레임 |
| ③ | 제품 문턱 대조 | `scripts/v4ProductCmp.ts`(**diff 0** · 인용) | 물리 0프레임 |
| ③ | 게이트 2건 | `scripts/v4ProductGate.ts`(**diff 0** · 인용) | 물리 0프레임 |

물리 벽시계 합 = **854.7 s ≈ 14.2분**(그중 v3 가 749.7 s) · JIT 합 67.2 s(벽시계 «밖»).

**실행하지 «않은» 것**(침묵으로 넘기지 않는다)

```
 400프레임 상한까지 돌리기 **0**(80에서 문턱 도달 · 상한은 쓰이지 않았다) ·
 셀프 충돌 이식 **0** · 혼합 정밀도 **0** · 최적화·튜닝 **0**(block_dim · 색 순서 · 봉제 컬러링 ·
 `serialize` 해제 · IR 재인쇄도 **0** — v4-12 `12-ir-par-offload.txt` 가 정본이고 코드가 안 바뀌었다) ·
 `src/` 수정 **0** · `engine/` 8파일 수정 **0** · v4-02~12 산출물 수정 **0** · 굽기 **0** · 캡처 **0** ·
 정본/blob 재생성 **0** · v3 재실행은 **이 판의 F=80 덤프 하나뿐**(정본 재계산 0) ·
 「같은 옷」 선언 **0** · 처방 **0** · 사후 갈래 신설 **0** ·
 **ecc 에이전트 0 / ecc 스킬 0**(`strategist` 서브에이전트도 0).
```

### §2-2 정밀도 · arch · grid/block · 태스크 · 시간

| 채널 | 값 |
|---|---|
| 기계 | **`DESKTOP-OV72A46`** · win32 · GTX 1660 Ti · Taichi **1.7.4** · llvm 15.0.1 · py **3.12.10** |
| arch (`current_cfg().arch`) | **`Arch.cuda`** 6건 · **`Arch.x64`** 2건 — **전부 assert 통과**(v4-01 함정 1 · 조용한 폴백 0) |
| 정밀도 | **f64** 6건(①ㄱㄴ · ② · probe 2건) · **f32** 2건(①ㄷ · probe 1건) |
| 태스크/호출 | **29.00 == 29** (세 판 전부 · 코드에서 «센» 수와 대조 · 함정 42 규범 ①) |
| grid × block | CUDA(f64·f32) = `range_for` **28종 · block 128 · grid 1~209** + 봉제 serial **1×1** ·<br>x64 = 레코드 29개 · grid/block **0**(CPU 프로파일러 미표기 · 폴백 아님) |
| s/프레임 | **f64 병렬 CUDA 0.355** · f64 병렬 x64 0.699 · f32 병렬 CUDA 0.224 ·<br>(인용) f32 직렬 CUDA 12.690 · f64 직렬 x64 0.963 · f64 직렬 CUDA 69.280 |
| degenerate / NaN / 발산 | **0 / 0 / 0** (4개 실행 전부 · 전 정점 유한) |
| 이 판의 총 실행(물리 벽시계) | **854.7 s ≈ 14.2분** — 예산(① 60프레임 ×3 · ② 상한 400) 안 |
| `py -m pytest gpu/tests -q` | **40 passed · 2 xfailed · 1 xpassed** (57.94 s) — v4-10·v4-11·v4-12 와 **같은 값** |
| `npx tsc -b` | **통과**(exit 0) |

### §2-3 경계 · diff

| 채널 | 값 |
|---|---|
| `src/` diff(기점 `f2d220c` ↔ 이 판) | **0** (`git diff --stat f2d220c HEAD -- src/` = 빈 출력) |
| `engine/` **8파일** blob 동일 | `full.py` `a631992832dd` · `full_color.py` `7c7a49217664` · `full_color2.py` `1ea95c3af689` · `full_par.py` `09a6517973bd` · `stretch.py` `7b3d8b6bf8a5` · `bending.py` `6e1972f589d0` · `seam.py` `7abfd6074dc1` · `collide.py` `4d748bb65870` — **8/8 기점과 같은 blob** |
| 계기 3종 diff 0 | `scripts/v4FitReport.ts` · `v4ProductCmp.ts` · `v4ProductGate.ts` — **인용만**(수정 0) |
| 정본 diff | **0** — 추적 중인 파일 중 «수정»으로 잡힌 것 0(이 판 산출은 전부 **신규 파일**) |
| 이 판이 «만든» 코드 | `gpu/l3cb2_run.py` · `gpu/l3cb2_cmp.py` · `gpu/par_probe2.py` · `scripts/v4FrameAt.ts` (전부 **신규**) |
| 이 판이 «만든» 산출물 | `l3cb2-*`(f64-cuda-f60 · f64-x64-f60 · f32-cuda-f60 · f64-cuda-conv · cmp 5건) ·<br>`l3conv-v3-…-f80` · `fit-…-v3-f80` · `fit-…-v4-13-f64par-f80` · `l3-gate-…` 2건 ·<br>`l3-product-cmp-v3f80-vs-v4f64parf80` · `docs/v4/13-par-tasks.txt` (전부 새 접두) |
| 굽기 / 캡처 / ecc | **0 / 0 / 0** |

### §2-4 물리 diff 0 의 «바이트» 증명

```
 ① `full_par.py` 는 blob `09a6517973bd` 로 기점과 **같다** — 이 판이 바꾼 것은 **호출하는 쪽**의
   `arch`·`default_fp` 두 인자뿐이다(`l3cb2_run.py` 는 `FullPar` 를 그대로 세운다).
 ② **f32 판이 v4-12 와 바이트 동일**(§1-①ㄹ 1행) — 같은 커널이 같은 입력에서 같은 결과를 낸다.
   ⟹ 신설 실행기가 v4-12 `l3par_run.py` 와 «같은 것»을 잰다는 것이 **값으로** 섰다.
 ③ **v3 쪽도 바이트 동일**(§1-② 대조군) — 신설 `v4FrameAt.ts` ↔ v4-07 `cellconv7-…-e0-x0.bin`
   좌표차 **0.000 mm**. ⟹ v3 물리 경로 인용이 정확하다(회차·프로세스가 다른데도 비트 재현).
```

### §2-5 정본 대조 1건 (사후 · 새 실행 0 · **노트 값 수정 0** · 함정 14)

```
 이 판의 f32 CUDA 재실측이 낸 grid 범위는 **1~209**(§1-①ㄱ · `13-par-tasks.txt`)다.
 v4-12 노트는 같은 채널을 두 자리에 적었는데 **서로 어긋난다**:
   `12-병렬과f64.md:260` — 「range_for 28종 · grid 최대 209 · **grid>1 인 태스크 26종**」
   `12-병렬과f64.md:344` — 「grid **2~209**(색 루프 17~24)」
 28종 중 grid>1 이 26종이면 나머지 **2종은 grid 1** 이므로 :344 의 하한 「2」는 :260 과 맞지 않는다.
 이 판의 재실측(같은 기계·같은 코드·같은 정의역)은 **1~209 · grid>1 26종** 으로 :260 쪽과 맞는다.
 ⟹ **v4-12 노트는 고치지 않는다**(함정 14 · 값 수정 0 · 회수 0). 이 절은 «어긋남이 있다»는
   사실만 값으로 남긴다. 처분은 볼트를 쓰는 쪽 몫이다.
```

---

## §3 화면

**캡처 0.** 이 판은 궤적·시간·리포트 수만 묻는다(회차 프롬프트 §3).

---

## §4 회차 갈래

| 항 | 갈래 | 한 줄 |
|---|---|---|
| ① 결합 실측 3종 | **(가)** | f64 × 병렬이 **감소 추세를 유지**(f60 2.684565e-04 = f10 의 1/57.71) ·<br>**0.355 s/프레임**(CUDA · 문턱 2.0) · 두 arch 궤적 자릿수 전량 동일 · 발산·NaN 0 |
| ② 정착 자리 확보 | (갈래 없음 · 사실) | **v4·v3 둘 다 f=80 에서 문턱 도달**(8.538170e-05 · 8.066671e-05 ≤ 1e-4) ·<br>두 최종 상태 좌표차 **중앙 0.011974 · p99 0.439469 · 최대 4.186409 mm** |
| ③ 층3 첫 판정 | **㉠(나) / ㉡(다)** | 표시값 **3/5** · 대역 이름 **5/5** · 대역 소속 **4/5** · 진단 2자리 0/5 ·<br>게이트 `pass` **둘 다 false**(일치) · `fails` **1건 ↔ 2건**(불일치) · 정착 채널 **둘 다 통과** |

**회차 갈래** — §0-4 의 문언대로:
`A = ①②③ 전량 + ①(가) + ③(가)(나) ⟹ 상신` / `D = ③(다) ⟹ 값 상신 · 정지`.
①②③ **전량 집행**됐고 ①은 **(가)**다. ③이 **두 읽기**로 갈리므로 회차 갈래도 **A(㉠) 또는 D(㉡)** 로 갈린다 —
**어느 쪽이든 이 판은 여기서 멈춘다**(처방 0 · 다음 판 정의역 0 · 사후 갈래 신설 0). ⟹ **값 상신.**

**넘기는 것 6건**

```
 1. **결합이 섰다**(①) — f64(정확)와 병렬(속도)이 «동시에» 선다. v4-12 §4 가 「안 한 결합」으로
    넘긴 자리가 값이 됐다: **0.355 s/프레임 · 감소 추세 유지 · 발산 0**.
    비용 지형 — f32 병렬 0.224 → f64 병렬 0.355(**1.58배**) · f64 직렬 CUDA 69.280 → 병렬 0.355(**195배**).
 2. **v4 가 «정착 자리»에 처음 섰다**(②) — 창 순변위 **0.0854 mm ≤ 0.1 mm**. v4-10 의 v4@180 은
    14.2843 mm 였다. 게이트의 정착 항이 v4 에서 **처음 통과**한다.
 3. **v3 와 «같은 프레임»에서 만난다**(②) — 둘 다 f=80 에서 문턱을 넘는다(v3 8.067e-05 · v4 8.538e-05).
    수렴 «속도»가 맞는다는 뜻이고, 이 자리가 층3 대조의 **첫 정당한 자리**다.
 4. **층3 판정의 두 읽기**(③) — 「대역」과 「게이트 판정」을 어느 채널로 셀지에 따라 (나)/(다)로 갈린다.
    갈리는 실체는 작다: 표시 반올림 경계 **2행**(차 0.027 mm · 0.027 mm) · 대역 정의역 정점 **1개**(261↔262) ·
    게이트 fails 에 v4 만 있는 **③a 관통 0.6599 mm**(문턱 0.5).
 5. **남은 간극의 크기**(사실만) — 두 최종 상태 중앙 **0.012 mm** · p99 **0.44 mm** · 최대 **4.19 mm**(정점 7693).
    같은 f64 에서 «색 순서»만 다른 갈림(§1-①ㄹ 3행)이 중앙 0.0122 mm 로 **거의 같은 크기**다.
 6. **v4-12 노트의 grid 표기 어긋남 1건**(§2-5) — :260「grid>1 26종」 ↔ :344「grid 2~209」.
    이 판 재실측은 **1~209**. 정본 수정 0 · 처분은 볼트를 쓰는 쪽 몫.
```

**이 판이 «하지 않은» 판단** — 「같은 옷」 선언 · ③의 읽기 선택 · ③a 관통 초과의 원인 귀속 ·
f64 채택 여부 · 병렬판 채택 여부 · 다음 판의 정의역 · 봉제 serial(26.643 ms) 의 처분.
**처방 0 · 최적화 0 · 사후 갈래 신설 0.**
