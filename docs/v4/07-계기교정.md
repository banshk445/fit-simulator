# v4-07 — 판별력 계기 «교정» · 층2·층3 첫 통과

**집행 기계 = 2호기(CUDA · 정본 기계) · `src/` diff 0 · 굽기 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-07-gauge` |
| 기점 | **`173b5fc`** (= v4-06 HEAD) |
| 워크트리 | `C:\Users\banba\fit-simulator` |
| 기계 | **`DESKTOP-OV72A46`** (win32 · CUDA · 12코어 · GTX 1660 Ti 6144MiB) |
| §0 커밋 | **`3258c2f`** — 집행 «전» 별도 커밋(sha 만 그 뒤 한 줄로 채웠다) |
| 호출 ecc 에이전트/스킬 | **0** |

---

## §0 수령 (집행 «전» 별도 커밋)

### §0-1 판정문 (전략 세션 · v4-06 §4 수령 · 원문)

```
 「갈래 C 수령 · ① 봉제 층1 통과(f64 차 0 · 첫 비트 동일) + 결합 1스텝(순서 포함) 검증 ⟹
   커널 4종 «옮겨적기» 종결 · 남은 이식 = 셀프 충돌 하나 ·
   ② 봉제 = 층2 선행 조건 확정(80프레임 수렴 · 중앙 0.8mm) ·
   ★ 귀책 = 전략 세션(판별력 섭동 1e-7 을 근거 없이 골랐다 — 수렴 판정이 순변위 1e-4 에서
   서니 섭동이 줄어들 시간이 없다 · 보존비 5.2·33,637 은 발산이 아니라 «계기 눈금이 굵다»는 뜻) ·
   처분 = 계기 교정(섭동을 수렴 문턱에서 유도 · 손 상수 0) · 문턱 이동 0 · CC 귀속 0」
```

### §0-1′ 귀책 등재 — **전략 세션**(v4-06 판별력 섭동 1e-7 을 근거 없이 골랐다)

```
 v4-04~v4-06 세 판에서 판별력 섭동은 **1e-7 m** 로 고정돼 있었고, 그 값의 «근거»는 어디에도
 등재되지 않았다. v4-06 이 값으로 드러낸 것 —
   수렴은 **창 순변위 ≤ 1e-4 m**(v3 `s4Gate.ts:settleNetM`)에서 선언되는데
   선언 시점의 실제 창 순변위가 **8.067e-05 = 섭동(1e-7)의 807배**다
 ⟹ 1e-7 짜리 섭동은 «줄어들 시간»을 못 갖고 계에 실려 간다. 보존비 5.2278 · 33,637.78 은
 계가 발산한다는 뜻이 «아니라» **계기 눈금이 수렴 판정보다 3자릿수 가늘다**는 뜻이다.
 **귀책은 전략 세션**(계기 설계)이고 **CC 귀속 0**이다.
 ★ 처분은 「값을 바꿔 본다」가 아니라 **「섭동을 수렴 문턱에서 «유도»한다 · 손 상수 0」**이다.
   그래서 이 판의 §1-① 은 v3↔v3 «훑기»이고, §1-② 는 그 실측에서 **규칙**을 뽑는다.
```

### §0-2 바꾸지 않는 것

```
 `src/` · 정본 · blob · v3 동결 ·
 **v4 커널 4종 로직 diff 0**(늘어남 · 굽힘 · 몸 충돌 · 봉제) · **결합 로직(`full.py`) diff 0** ·
 **v3 수렴 판정 규칙 0**(`N_WIN` = 10 · `settleNetM` = 1e-4 m · 창 순변위 식 그대로) ·
 적격 기준 **0.01**(v4-05 §0-5 문면 그대로 · **이동 0**)
```

### §0-3 시험계의 «정의역»을 집행 전에 못 박는다 (사후 해석 금지)

```
 시험계 = **정답지 1칸 «전량»** — 늘어남 + 굽힘 + 봉제 + 몸 충돌 (+ 중력) · 칸 `c100-h170-s45_M`.
 이것은 v4-06 §1-② 의 시험계 **ㄷ** 와 «같다»(회차 간 대조가 끊기지 않게 유지한다).
 ★ **셀프 충돌은 뺀다** — v4 에 이식이 없다(정의역 일치). v3 쪽도 뺀다.
   그 결과 정답지 blob 은 이 정의역에서 «완전한 평형이 아니다»(v4-06 실측 최대 16.5 mm).
 섭동 정의 = **자유 정점 전량의 x 에 +ε m**(강체 병진 · v4-04~06 과 «같은 형태» · 크기 ε 만 훑는다).
 보존비 = **(최종 좌표차 최대) / ε**. 적격 = **보존비 ≤ 0.01**.
 ★ ①은 **v3↔v3 만** 쓴다 — **v4 는 한 줄도 돌리지 않는다**(계기 교정에 피검체를 넣지 않는다).
```

### §0-4 사전 등재 갈래 (원문)

**①의 하위 갈래**

| 갈래 | 조건 |
|---|---|
| **(가)** | ㄱ 에서 보존비가 «단조 감소하며 어떤 ε 이상에서 0.01 이하로 안정»되는 영역이 있다(영역 등재) |
| **(나)** | ㄴ 에서 연장 프레임이 늘수록 보존비가 0.01 이하로 내려간다(필요 프레임 수 등재) |
| **(다)** | 둘 다 |
| **(라)** | 둘 다 아님 → 값 상신 · 정지 |

**회차 갈래**

| 갈래 | 조건 | 처분 |
|---|---|---|
| **A** | ①~④ 전량 + ② 적격 | 상신 |
| **B** | ① **(라)** | 값 상신 · **②③④ 착수 0** · 층2 설계 재검 |
| **C** | ③ 또는 ④ 에서 v3·v4 가 «육안으로 다른 형상» | 값·좌표 상신 · 정지 |
| **D** | 그 외 | 정지 |

**사후 갈래 신설 0.**

### §0-5 착수 «전» 알려진 사실 · 예산

```
 · v3 는 이 계에서 **11.0~15.4 초/프레임**(v4-06 실측 · 6프로세스 경합 시 15.3) ·
   v4 결합 커널은 **12.6 초/프레임**(CUDA · f32 · 직렬 · 비용 측정만).
 · 기본(섭동 0) 실행은 **80프레임**에 수렴했고 그 시점 창 순변위 **8.066671e-05 m**.
 · ㄱ 은 «기본 1 + 섭동 6» = **7 프로세스**를 병렬로 돌린다(12코어). 경합 계수는 실측해 적는다.
 · ㄴ 의 연장 훑기는 **한 실행이 0·50·100·200·400 을 «전부 지난다»** ⟹
   기본·섭동 각 **1회**만 돌리고 지나는 자리마다 위치를 «스냅»한다(중복 실행 0).
 · 계기 코드(`scripts/v4CellConverge.ts`)에 **ε 를 파일명에 싣고 · 연장 스냅을 넣는다**.
   물리 경로(`step` 호출·제약 집합·판정식)는 **한 줄도 바꾸지 않는다**.
 · ③ 의 v3 쪽은 ㄴ 기본 실행의 «연장 P 스냅»을 **그대로 쓴다**(재실행 0).
```

### §0-6 이 판에서 «하지 않는» 것

```
 · 문턱(0.01) 이동 · 층2 상한 등재 · 게이트 판정(셀프 충돌 미이식) · 이산 계수 대조(발화 수 등)
 · 궤적 대조(폐기 · 함정 39) · 캡처 · 굽기 · `src/` 수정 · 커널 4종·결합 로직 수정
 · 사후 갈래 신설 · 결과를 보고 c·P 를 다시 고르기(함정 14)
```
