# v4-11 — v4 직렬성 검사 · 비용 분해 · 이식 존속 판정 재료

**집행 기계 = 2호기(CUDA · 정본 기계) · `src/` diff 0 · 굽기 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-11-serial` |
| 기점 | **`7272eb0`** (= v4-10 HEAD) |
| 워크트리 | `C:\Users\banba\fit-simulator` |
| 기계 | **`DESKTOP-OV72A46`** (win32 · CUDA · GTX 1660 Ti · Taichi 1.7.4 · llvm 15.0.1 · py 3.12.10) |
| §0 커밋 | **`11b7d12`** — 집행 «전» 별도 커밋 |
| §1~§4 커밋 | **`beaea29`** (코드·산출물·본문·과거 노트 정정) · 볼트 등재는 뒤이은 커밋 |
| 호출 ecc 에이전트/스킬 | **0 / 0** |

---

## §0 수령 (집행 «전» 별도 커밋)

### §0-1 판정문 (전략 세션 · v4-10 §4 수령 · 원문)

```
 「갈래 A 수령 · ★ ①(나) — 정의역 가설 «사망»: 진동 정점 근접쌍 소속률 0/100(대조군 14%) ·
   그리고 v3 를 같은 정의역(셀프 충돌 제외)으로 돌리면 6.935e-07 로 «수렴»(진동 0) ⟹
   같은 정의역에서 v3 는 서고 v4 는 안 선다 ⟹ 남은 설명은 v4 자체의 동작 차이 ·
   ★★ ④ 가 자리를 가리킨다 — 색 분할판과 「직렬」판의 궤적이 «같고» 둘 다 진동 ⟹
   가설: v4 의 직렬 경로가 실은 병렬이다(Taichi 최상위 루프 기본 병렬 · v3 가 발산 실측한
   Jacobi 계열) · 단정 0 · 검사로 가른다 ·
   ★ ④ 속도 — 색 분할이 1.08배 «느리다» ⟹ 병렬화로는 안 빨라진다 ·
   55ms/서브스텝은 커널 던지는 비용이 지배함을 시사(단정 0) ·
   ② 유보(서지 않은 옷의 리포트) · 대역 5/5 는 층3 방향의 신호로 등재 ·
   이 판은 «이식 존속 판정 재료»를 낸다 — 셀프 충돌 이식 0 · 최적화 착수 0」
```

### §0-2 바꾸지 않는 것 (회차 프롬프트 §0-2 원문)

```
 `src/` · 정본 · blob · v3 동결 · **물리 식 0줄**
 ⟹ 이 판의 변경은 «루프 실행 방식»과 «계기»뿐이다.
 ★ `engine/full.py` · `full_color.py` · `full_color2.py` · 커널 4종(늘어남·굽힘·봉제·몸 충돌)
   **바이트 불변**. 이 판이 만드는 것은 **새 파일**뿐이다.
 ★ v4-02~10 산출물 바이트 불변(이 판 산출은 새 접두 `l3det-` · `l3prof-` 로 판다).
```

### §0-3 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0)

**① 결정성 검사** — v4 「직렬」판으로 같은 칸·같은 초기 상태를 «두 번» 60프레임 실행해 최종 정점 비트 대조

| 갈래 | 조건 |
|---|---|
| **(가)** | 비트 동일 ⟹ 경쟁 없음(직렬성 가설 «기각») |
| **(나)** | 다름(최대 좌표차 등재) ⟹ 경쟁 있음 = **병렬 쓰기 확정** |
| **(다)** | 그 외 |

**② 병렬성 코드 사실** — 갈래 없음. `full.py` 제약 투영 커널의 «최상위 루프»가 무엇인지 ·
`ti.loop_config(serialize=True)` 유무를 **파일:줄**로 등재 · Taichi 가 실제로 그 루프를
병렬화했는지 확인 가능한 경로(컴파일 정보 · 문서 근거) 등재. **판정 0 · 처방 0.**

**③ 「진짜 직렬」판** — 선행 조건: ①이 (나) 이거나 ②에서 serialize «부재»가 확인될 것.
물리 식 0줄 · `serialize=True` 만 · 60프레임 · ㄱ 순변위 궤적(v3 와 나란히) ㄴ s/프레임 ㄷ 리포트 5행

| 갈래 | 조건 |
|---|---|
| **(가)** | 수렴한다 ⟹ 진동 원인 확정 |
| **(나)** | 여전히 진동(값 등재) |
| **(다)** | 실행 불가(사유) |

**④ 비용 분해** — 60프레임 실행에서 서브스텝당 커널 실행 «횟수» + 각 커널 누적 시간
(`ti.profiler` 또는 동등 계기) · 커널 실행 오버헤드 «추정치» vs 실제 계산 시간의 비

| 갈래 | 조건 |
|---|---|
| **(가)** | 오버헤드가 지배(비 등재) |
| **(나)** | 계산이 지배 |
| **(다)** | 판별 불가 |

**§4 회차 갈래** — A = ①~④ 전량 ⟹ 상신 / B = ③(다) ⟹ ①②④ 상신 /
C = ④(다) ⟹ ①②③ 상신 / D = 그 외 ⟹ 정지. **사후 갈래 신설 0 · 처방·최적화·이식 착수 0.**

### §0-4 집행 «전» 사전 결정 5건 (값을 보기 «전»에 적는다)

```
 ㄱ **① 「비트 대조」의 정의** — 두 실행의 최종 `pos` 필드를 **원시 바이트**로 비교한다
   (`pos.to_numpy()` = f32 · `tobytes()` 의 sha256 일치 + `np.array_equal`). `vel` 도 같이 센다.
   보조로 **최대 좌표차 · 다른 정점 수**를 항상 인쇄한다(같아도 0 으로 인쇄된다).
   ★ 두 실행은 **같은 프로세스 · 같은 컴파일 산출**로 돌린다 — 이 판이 묻는 경쟁은
   «커널 «안»의 쓰기 경쟁»이지 컴파일 재현성이 아니다. 상태는 같은 `pos0/vel0` 로 되감는다.

 ㄴ **② 「병렬화됐는가」의 확인 경로 3종** — 셋 다 적는다.
   ㉠ 코드 인용(파일:줄) ㉡ Taichi 문서 근거(최상위 루프만 병렬 · `loop_config`)
   ㉢ **기계 검사** — 「실행 순서 각인」 커널을 만들어 `serialize` 유무로 각각 돌린다.
      `ord[i] = cnt[None]; cnt[None] += 1` 이면 «진짜 직렬»일 때만 `ord == [0..N-1]` 이다.
      `full.py:233-234` 와 **같은 중첩 모양**(최상위 1회전 + 안쪽 range) 도 같이 잰다.

 ㄷ **④ 계기** — `ti.init(kernel_profiler=True)` + `ti.profiler.get_kernel_profiler()`.
   커널 «횟수»는 ㉠ 프로파일러 레코드 ㉡ 코드에서 센 수(직렬 = 창당 1 · 색분할 = 29×서브스텝)
   **둘 다** 적는다. 어긋나면 어긋난 채로 적는다.
   **오버헤드 추정치** = 같은 프로세스에서 «빈 커널»을 10만 회 띄운 벽시계 / 횟수
   (계산 0 인 커널이므로 이 값이 launch 하한이다). 비 = (추정 launch 총비용) / (벽시계).

 ㄹ **④ 는 직렬판·색분할판 «둘 다»** 60프레임으로 잰다 — 배수 대조가 채널이기 때문이다.
   프로파일러가 시간을 바꿀 수 있으므로 **v4-10 의 비프로파일 벽시계와 나란히** 적는다.

 ㅁ **③ 선행 조건 불성립 시** — ①이 (가) «그리고» ② 에서 `serialize=True` 가 확인되면
   「진짜 직렬판」은 **만들 대상이 없다**(이미 그것이다). 그 경우 ③ 은 **(다) 실행 불가**로 적고
   사유에 «선행 조건 미충족»을 쓴다. **사후 갈래 신설 0** — 이 결정은 값을 보기 «전»에 적는다.
```

### §0-5 예산 · 실행 규약

```
 실행 예산 = **60프레임 이하로 전부 자른다**(회차 프롬프트 ★).
 v4-10 실측 직렬 12.690 s/프레임 ⟹ 60프레임 ≈ 13분. 이 판의 실행 건수:
   ① 2건(직렬 60f ×2 · 같은 프로세스) · ④ 2건(직렬·색분할 각 60f · 프로파일러 on)
   [+ ③ 이 성립하면 1건] ⟹ **약 50~65분**.
 ★ 전부 **배경 실행 + 로그 파일** · **GPU 를 다투지 않게 줄 세운다**(동시 실행 0).
```

---

## §1 집행

### §1-① 결정성 검사 — **(가) 비트 동일** (경쟁 «없음» ⟹ 직렬성 가설 **기각**)

계기 = `gpu/l3det_run.py`(신설). `engine/full.py` 를 **그대로** 부른다(물리 식 0줄 · 바이트 불변).
같은 칸 `c100-h170-s45_M` · 같은 초기 상태(`pos0/vel0` 로 되감음) · 제약 `all` 44,920 ·
substeps 229 · **60프레임** · f32 · CUDA · **같은 프로세스 · 같은 컴파일 산출**(§0-4ㄱ).

| 채널 | 1회 | 2회 | 같은가 |
|---|---|---|---|
| **`pos` 원시 바이트 sha256** | `470e5ec3234f6691…d54c5` | `470e5ec3234f6691…d54c5` | **✅ 일치** |
| **`vel` 원시 바이트 sha256** | `f159f06d62717cfe…` | `f159f06d62717cfe…` | **✅ 일치** |
| `np.array_equal(pos)` | — | — | **✅ True** |
| **다른 정점 수** | — | — | **0 / 9,388** |
| **최대 좌표차** | — | — | **0.000000000 mm** |
| 창 순변위 궤적 6점 | — | — | **✅ 전량 동일** |
| degenerate 카운터 | 0 | 0 | ✅ |
| 벽시계 | 762.150 s (**12.703** s/프레임) | 767.674 s (**12.795** s/프레임) | ❌ **0.72% 다르다** |

**창 순변위 궤적 — 두 실행 «그리고» v4-10 직렬판(같은 값)**

| f | 10 | 20 | 30 | 40 | 50 | 60 |
|---|---|---|---|---|---|---|
| 1회 | 2.651729e-02 | 2.349800e-02 | 2.299182e-02 | 3.003855e-02 | 1.937818e-02 | 2.705761e-02 |
| 2회 | 2.651729e-02 | 2.349800e-02 | 2.299182e-02 | 3.003855e-02 | 1.937818e-02 | 2.705761e-02 |
| **v4-10 §1-④ 직렬** | 2.651729e-02 | 2.349800e-02 | 2.299182e-02 | 3.003855e-02 | 1.937818e-02 | 2.705761e-02 |

```
 ★ **갈래 (가)** — 비트 동일 ⟹ 「v4 의 직렬 경로가 실은 병렬이라 쓰기 경쟁이 있다」는 판정문 가설은
   **이 값으로 기각**된다. 경쟁이 있으면 «반드시» 값이 갈린다는 것은 §1-②ㄴ㉢ A 가 같은 기계에서
   보인다(같은 커널을 두 번 돌리면 288 → 352 로 달라진다) ⟹ **계기가 둔감해서 0 이 나온 것이 아니다.**
 ★ **시간은 갈리는데 값은 안 갈린다**(벽시계 0.72% 차 · 좌표차 0.000000000 mm) — 계기가 살아 있다는
   또 하나의 표시다.
 ★ 궤적이 v4-10 직렬판과 **인쇄 자릿수 전량 동일** ⟹ **물리 diff 0 의 바이트 증명**(§2-3).
 ★ **원인 단정 0** — 이 판은 「진동의 원인이 무엇이냐」를 ① 로는 말하지 않는다. ① 이 말하는 것은
   **「경쟁 아님」 하나**다.
```

### §1-② 병렬성 «코드 사실» — 판정 0 · 처방 0

#### ㄱ 최상위 루프가 무엇인가 — `engine/full.py` (파일:줄)

| 자리 | 원문 | 무엇인가 |
|---|---|---|
| `engine/full.py:230` | `@ti.kernel` | 커널 **하나**뿐이다(`run`) — 제약 투영 4종은 전부 `@ti.func`(`:74·116·162·206`)라 **커널이 아니다** |
| **`engine/full.py:233`** | **`ti.loop_config(serialize=True)`** | **serialize «있다»** |
| **`engine/full.py:234`** | **`for _ in range(1):`** | 이것이 커널의 **«최상위» 루프**다(1회전) |
| `engine/full.py:238-239` | `for _f in range(frames):` / `for _sub in range(substeps):` | 최상위 «안» ⟹ Taichi 규칙상 병렬화 대상이 아니다 |
| `engine/full.py:240·245·247·249·252·255·258·261·264` | 예측 · λ초기화 3 · 늘어남 · 굽힘 · 봉제 · 몸충돌 · 속도갱신 | 전부 **2중 이상 중첩 안** |
| (대조) `engine/full_color2.py:57·62·64·66·70·74·81·84` | 색분할판의 최상위 루프들 | serialize «없다» ⟹ 병렬 · 봉제만 `:77` 에 serialize |

```
 ★ **`full.py` 의 제약 투영에는 「최상위 루프」가 없다** — 최상위는 `:234` 의 1회전 루프 하나뿐이고,
   투영 루프는 전부 그 «안»에 있다. 따라서 serialize 를 지우더라도 최상위가 1회전이라
   병렬화될 몸통이 없다(아래 ㄷ 의 D 가 그것을 값으로 보인다).
```

#### ㄴ Taichi 가 실제로 병렬화했는가 — 확인 경로 3종

**㉠ 문서 근거**(설치본 인용 · 손 인용 0) — `taichi/lang/misc.py:612,624`

```
 def loop_config(*, block_dim=None, serialize=False, parallelize=None, ...)
   serialize (bool): Whether to let the for loop execute serially,
                     `serialize=True` equals to `parallelize=1`
```

**㉡ 컴파일 정보** — `gpu/ir_probe.py`(신설 · **물리 0스텝** · `frames=0` 으로 컴파일만 시킨다) ·
`ti.init(print_ir=True)` 의 CHI IR. 발췌 정본 = `docs/v4/11-ir-run-offload.txt`

| 채널 | 값 |
|---|---|
| `run` 커널의 최종 IR 안 offloaded 태스크 수 | **1** |
| 그 태스크의 표기 | **`$0 = offloaded`** — `range_for`/`struct_for` **없음** · `grid_dim`·`block_dim` **없음** |
| 같은 로그의 «병렬» 태스크 표기(대조) | `offloaded range_for(0, 9388) grid_dim=768 block_dim=128` |
| 프로파일러가 붙이는 태스크 이름(§1-④) | 직렬 = `…_serial` · 병렬 = `…_range_for` |

**㉢ 기계 검사 — 「실행 순서 각인」**(`gpu/serial_probe.py` 신설 · N=100,000 ·
`ord[i] = ti.atomic_add(cnt[None], 1)` ⟹ **진짜 직렬일 때만** `ord == [0..N-1]`)

| 모양 | serialize | 각인 == [0..N-1] | 일치 수 | 첫 8개 | 판정 |
|---|---|---|---|---|---|
| **A** 최상위 `range`-for | 없음 | **❌** | 288 / 100,000 (재실행 352) | `[2624,2625,…]` | **병렬 · 실행마다 다르다** |
| **B** 최상위 `range`-for | **있음** | ✅ | 100,000 / 100,000 | `[0,1,2,…]` | 직렬 |
| **C** `full.py:233-234` 와 **같은 중첩 모양** | **있음** | ✅ | 100,000 / 100,000 | `[0,1,2,…]` | **직렬** |
| **D** 같은 모양 · serialize **뺌** | 없음 | ✅ | 100,000 / 100,000 | `[0,1,2,…]` | **직렬**(최상위가 1회전) |

```
 ★ A 는 두 번 돌려 288 → 352 로 **값이 달라졌다** — 병렬 태스크는 실행마다 다르다는 것을
   이 계기 자신이 보인다(대조군이 살아 있다 ⟹ C·D 의 «100,000/100,000» 은 계기 둔감이 아니다).
 ★ **D 가 핵심이다** — `serialize` 를 «빼도» `full.py` 모양은 직렬이다.
   즉 `full.py:run` 은 **두 겹으로** 직렬이다(㉠ serialize 명시 · ㉡ 최상위 1회전).
 ★ **판정·처방 0**(회차 프롬프트 ②). 이 값들이 ① 과 어떻게 맞물리는지는 §4 에 적는다.
```

### §1-③ 「진짜 직렬」판 — **(다) 실행 불가** (사유 = 선행 조건 미충족 · §0-4ㅁ 대로)

회차 프롬프트가 ③ 에 건 선행 조건은 **「①이 (나) 이거나 ②에서 serialize «부재»가 확인되면」**이다.

| 선행 조건 | 실측 | 성립? |
|---|---|---|
| ① 이 (나)(비트 다름) | ① = **(가) 비트 동일**(sha 일치 · 다른 정점 0/9,388) | ❌ |
| ② 에서 `serialize` **부재** | `engine/full.py:233` 에 **`ti.loop_config(serialize=True)` 있음** · 게다가 최상위 루프가 `:234` 의 **1회전**이라 serialize 를 빼도 직렬(②ㄴ㉢ D) | ❌ |

```
 ⟹ **둘 다 불성립** ⟹ 「물리 식 0줄 · serialize=True 만 넣은 진짜 직렬판」은 **만들 대상이 없다** —
   `engine/full.py` 가 **이미 그것**이고, ② 의 세 경로(코드·IR·순서각인)가 그것을 기계로 보였다.
 ★ 이 처분은 **값을 보기 «전»에** §0-4ㅁ 에 등재됐다. **사후 갈래 신설 0.**
 ★ 이 판에서 «새로» 만든 실행판 0 · GPU 추가 실행 0(③ 몫).
```

**(참고 · 판정 채널 아님) ③ㄱㄴㄷ 이 물었을 값은 이미 등재돼 있다 — 인용만 한다(재실행 0)**

| ③ 의 물음 | 이미 있는 값 | 출처 |
|---|---|---|
| ㄱ 순변위 궤적(v3 와 나란히) | **직렬 v4** f10~f60 = 2.651729e-02 · 2.349800e-02 · 2.299182e-02 · 3.003855e-02 · 1.937818e-02 · **2.705761e-02** (감소 아님 · 진동) ↔ **v3** f10 1.600e-02 · f20 3.992e-03 · f40 7.531e-04 · f60 **2.531e-04**(18점 전부 감소 · f180 6.935e-07) | 이 판 §1-① · v4-10 §1-①ㄴ |
| ㄴ s/프레임 | **12.703 / 12.795**(이 판 두 실행) · 12.690(v4-10) · 12.758(180프레임 전량) | 이 판 §1-① · v4-10 §1-④ㄴ |
| ㄷ 리포트 5행(f=60) | 목선 1.6 · 가슴 4.9 · 허리 27.3 · 밑단 23.0 · 소매 4.7 mm(직렬판 · 대역 전부 «여유»/목선 «밀착») | v4-10 §1-④ㄹ |

```
 ★ 즉 ③ 의 사전 등재 갈래로 읽으면 이 값들은 **(나) 「여전히 진동」**에 해당한다 —
   그러나 **③ 자체는 (다) 로 적는다**(만든 판이 없으므로 새 값이 아니고, 갈래는 «새 판의 결과»에 붙는다).
   두 읽기를 **둘 다 적는다** — 어느 쪽으로 셀지는 규칙을 쓴 전략 세션 몫이다.
```

### §1-④ 비용 분해 — **(나) 계산이 지배** (오버헤드 아님)

계기 = `gpu/l3prof_run.py`(신설) · `ti.init(kernel_profiler=True)` ·
**직렬판·색분할판 «둘 다» 60프레임**(§0-4ㄹ) · JIT 1프레임은 예열로 빼고 프로파일러를 비운 뒤 잰다.

#### ㄱ 커널 실행 «횟수» — 코드에서 센 수와 프로파일러가 센 수가 **어긋난다**

| 판 | ㉠ 코드에서 센 수 | ㉡ 프로파일러가 «센» 물리 태스크 | 서브스텝당 | 어긋남 |
|---|---|---|---|---|
| **직렬** `full.py` | 6 (창당 커널 1개 × 6창) | **6** | 창당 1 | ✅ 같다 |
| **색분할2** `full_color2.py` | **398,460** (최상위 루프 29 × 서브스텝 229 × 60프레임 · v4-10 등재) | **82,440** | **6.00** | ❌ **4.83배 어긋난다** |

```
 ★ §0-4ㄷ 이 「어긋나면 어긋난 채로 적는다」고 미리 정해 뒀다. 어긋났다.
   파이썬 커널 «호출»은 13,740회(= 229 × 60)이고, 그 한 호출이 GPU 태스크 **6개**를 띄운다 —
   코드가 기대한 **29개가 아니다.**
```

#### ㄴ 커널별 누적 시간 (60프레임 · 물리 태스크만 · 나머지는 `to_numpy` 등 계기 몫)

**직렬** — 벽시계 **764.7 s** (12.744 s/프레임) · 커널 누적 **764.6 s**

| 태스크 | 횟수 | 누적 ms | 평균 ms | grid | block |
|---|---|---|---|---|---|
| **`run_c80_0_kernel_0_serial`** | 6 | **764,624.6** | 127,437.44 | **1** | **1** |
| `snode_reader_49_…_serial` | 6 | 0.5 | 0.086 | 1 | 1 |
| `matrix_to_ext_arr_…_range_for` | 13 + 6 | 0.1 | 0.005 | 74 | 128 |
| `snode_writer_49_…_serial` | 6 | 0.0 | 0.004 | 1 | 1 |

**색분할2** — 벽시계 **782.6 s** (13.044 s/프레임) · 커널 누적 **769.2 s**

| 태스크 | 횟수 | 누적 ms | 평균 ms | grid | block | 코드에서 무엇인가 |
|---|---|---|---|---|---|---|
| **`substep_c84_0_kernel_4_serial`** | 13,740 | **768,782.7** | **55.952** | **1** | **1** | **`if` 안 전부** — 늘어남 8색 + 굽힘 14색 + 봉제 + 몸충돌 |
| `substep_…_kernel_5_range_for` | 13,740 | 93.4 | 0.0068 | 74 | 128 | `:84` 속도 갱신 (9,388/128 → 74) |
| `substep_…_kernel_0_range_for` | 13,740 | 76.1 | 0.0055 | 74 | 128 | `:57` 예측 (9,388) |
| `substep_…_kernel_2_range_for` | 13,740 | 74.0 | 0.0054 | 209 | 128 | `:64` 굽힘 λ초기화 (26,648/128 → 209) |
| `substep_…_kernel_1_range_for` | 13,740 | 71.6 | 0.0052 | 141 | 128 | `:62` 늘어남 λ초기화 (18,016/128 → 141) |
| `substep_…_kernel_3_range_for` | 13,740 | 64.2 | 0.0047 | **2** | 128 | `:66` 봉제 λ초기화 (256/128 → 2) |

```
 ★ **grid 가 코드의 루프 길이와 1:1로 맞는다**(9,388 → 74 · 18,016 → 141 · 26,648 → 209 · 256 → 2) ⟹
   병렬 태스크가 된 것은 **`if` «밖»에 있는 5개 루프뿐**이다. 나머지(색 22개 + 봉제 + 충돌)는
   **한 덩어리 `…_serial`(grid 1 · block 1)** 이 됐다.
 ★ 병렬 5태스크 누적 합 = **379.3 ms** = 커널 시간의 **0.049%**. 나머지 **99.95%** 가 그 직렬 태스크다.
```

#### ㄷ 오버헤드 «추정치» vs 계산 — **계산이 지배**

| 채널 | 직렬 | 색분할2 |
|---|---|---|
| 벽시계 | 764.7 s | 782.6 s |
| **커널 누적 / 벽시계** | **764.6 s = 100.00%** | **769.2 s = 98.28%** |
| **비커널 잔여**(벽시계 − 커널) | **0.03 s = 0.00%** | **13.46 s = 1.72%** |
| launch 지연 — 1태스크 커널(`serial` / `range_for`) | 220.0 / 212.1 µs | 221.2 / 217.3 µs |
| launch 지연 — **29태스크 커널**(태스크/호출 = **29.00**, 세서 확인) | 2,577.8 µs | **2,623.2 µs** |
| 파이썬 커널 호출 수 | 6 | 13,740 |
| **추정 A** = 호출 수 × 같은 «모양»의 지연 | 0.0013 s = **0.00%** | **36.0 s = 4.61%**(상한) |

```
 ⟹ 사전 등재 갈래 **(나) = 계산이 지배**. 두 판 모두 **커널 안에서 보낸 시간이 벽시계의 98~100%** 다.
 ★ 추정 A(4.61%)는 **상한**이다 — launch 는 비동기라 GPU 실행과 겹친다. 실제로 겹치지 «못한» 몫의
   상한은 **비커널 잔여 1.72%** 다. 어느 쪽으로 읽어도 **오버헤드는 지배하지 않는다.**
 ★ 판정문의 「55ms/서브스텝은 커널 던지는 비용이 지배함을 시사」는 **이 값으로 성립하지 않는다** —
   55.95 ms 는 **한 서브스텝의 «직렬 태스크» GPU 실행 시간**이고(launch 비용은 호출당 2.6 ms),
   직렬판의 같은 자리도 **55.65 ms/서브스텝**(764,624.6 ms ÷ 13,740)이다. **두 판이 사실상 같은 값**이다.
```

#### ㄹ ★★ 「계산」의 정체 — **두 판 다 한 스레드다** (사실만 · 처방 0)

| 채널 | 직렬 `full.py` | 색분할2 `full_color2.py` |
|---|---|---|
| 지배 태스크 | `run_…_serial` | `substep_…_kernel_4_serial` |
| **grid × block** | **1 × 1** | **1 × 1** |
| **서브스텝당 시간** | **55.65 ms** | **55.95 ms** |
| 그 태스크가 커널 시간에서 차지하는 몫 | 100.00% | **99.95%** |

**왜 색 루프가 병렬 태스크가 안 됐는가 — 기계로 센다**(`gpu/if_probe.py` 신설 · 물리 0 · N=100,000)

| 모양 | 태스크 수 / 호출 | 태스크 이름 | 각인 == [0..N−1] | 일치 수 | 판정 |
|---|---|---|---|---|---|
| **A** `for` 가 커널 최상위 | **2** | `…_kernel_0_serial` · `…_kernel_1_range_for` | ❌ | 416 / 100,000 | 병렬 |
| **B** 같은 `for` 가 **`if` 안** | **1** | `…_kernel_0_serial` | ✅ | 100,000 / 100,000 | **직렬** |
| **C** `if` 안에 `for` **3개**(색 분할 모양) | **1** | `…_kernel_0_serial` | ✅ | 100,000 / 100,000 | **직렬** |

```
 ★ **`if` 안의 최상위 `for` 는 병렬 태스크가 «되지 않는다»** — 하나의 serial 태스크로 접힌다.
   `full_color2.py` 의 색 루프는 전부 `if use_ip != 0:` / `if use_bd != 0:` 안에 있다(`:68·72·76·80`)
   ⟹ ㄴ 의 표가 그대로 설명된다(태스크 6개 · 그중 1개가 serial · 코드의 29 와 어긋남).
 ★ **원인 단정·처방 0.** 이 판은 `if` 를 걷어내지 않는다(§0-2 · 「최적화 착수 0」).
```

---

## §2 자기검사

### §2-1 집행 / 미집행 총계

**집행한 것**

| # | 무엇 | 계기(신설) | GPU 실행 |
|---|---|---|---|
| ① | 결정성 검사 60프레임 **×2**(같은 프로세스) | `gpu/l3det_run.py` | 2건 · 1,529.8 s |
| ② | 순서각인 4모양 · 제품 커널 IR · 문서 인용 | `gpu/serial_probe.py` · `gpu/ir_probe.py` | 2건 · 각 1분 미만(물리 0스텝) |
| ④ | 비용 분해 직렬 60프레임 · 색분할 60프레임 | `gpu/l3prof_run.py` | 2건 · 1,547.3 s |
| ④보조 | `if` 안 최상위 루프 태스크 수 | `gpu/if_probe.py` | 1건 · 1분 미만 |
| — | 무효 실행 1건 + 중단 1건(§2-4 계기 결함) | — | 767.6 s + 약 60 s |

**실행하지 «않은» 것**(침묵으로 넘기지 않는다)

```
 ③ 「진짜 직렬판」 — **미집행**. 선행 조건 불성립(§1-③) ⟹ 갈래 **(다)** · §0-4ㅁ 대로.
 셀프 충돌 이식 **0** · 최적화·`if` 제거·색 순서 변경 **0** · `src/` 수정 **0** ·
 굽기 **0** · 캡처 **0**(§3) · 정본/blob 재생성 **0** · v3 재실행 **0**(인용만) ·
 f64 실행 **0** · 180프레임 실행 **0**(예산 60프레임 상한 · §0-5) ·
 **ecc 에이전트 0 / ecc 스킬 0** · `strategist` 서브에이전트 호출 **0**
 (사전 반증은 ② 를 ① «보다 먼저» 돌려 판정문 전제를 값으로 검사하는 것으로 대신했다 —
  회차 프롬프트가 서브에이전트를 부르지 않았다).
```

### §2-2 정밀도 · arch · 시간 (전부 2호기 `DESKTOP-OV72A46` · CUDA)

| 채널 | 값 |
|---|---|
| arch (`current_cfg().arch`) | **`Arch.cuda`** — 다섯 실행 전부 assert 통과(v4-01 함정 1) |
| 정밀도 | **f32**(`default_fp=ti.f32`) · f64 실행 0 |
| GPU | GTX 1660 Ti · Taichi 1.7.4 · llvm 15.0.1 · py 3.12.10 |
| ① 두 실행 | 762.150 s / 767.674 s (12.703 / 12.795 s/프레임) |
| ④ 두 실행 | 764.7 s / 782.6 s (12.744 / 13.044 s/프레임) · 예열 13.5 s / 68.2 s |
| 이 판의 GPU 총 실행 | 약 **3,100 s ≈ 52분**(무효 1건 포함 시 약 66분) |
| `py -m pytest gpu/tests -q` | **40 passed · 2 xfailed · 1 xpassed** (64.1 s) — v4-10 과 같은 값 |
| `npx tsc -b` | 통과(exit 0) |

### §2-3 경계 · diff

| 채널 | 값 |
|---|---|
| `src/` diff(기점 `7272eb0` ↔ 이 판) | **0** (`git diff --stat 7272eb0 HEAD -- src/` = 빈 출력) |
| `engine/` 물리 파일 blob 동일 | `full.py` `a631992832dd` · `full_color.py` `7c7a49217664` · `full_color2.py` `1ea95c3af689` · `stretch.py` `7b3d8b6bf8a5` · `bending.py` `6e1972f589d0` · `seam.py` `7abfd6074dc1` · `collide.py` `4d748bb65870` — **7/7 기점과 같은 blob** |
| 기존 실행기 blob 동일 | `l3_dom.py` `9ed45b983326` · `l3cb_run.py` `03bf87adbb21` |
| 이 판이 «만든» 파일 | `gpu/serial_probe.py` · `gpu/ir_probe.py` · `gpu/l3det_run.py` · `gpu/l3prof_run.py` · `docs/v4/11-ir-run-offload.txt` (전부 **신규**) |
| 굽기 | **0** (SDF·blob·정본 재생성 0) |
| 캡처 | **0** (§3) |
| ecc 에이전트 / 스킬 | **0 / 0** |
| `npx tsc -b` | **통과**(exit 0) |

```
 ★ **주의(사실만)** — 워킹트리의 `gpu/engine/stretch.py` 는 **CRLF**, git blob 은 LF 라
   «파일 바이트» sha256 은 다르게 나온다(8,857 ↔ 8,691 바이트 = 166줄 차). `git diff` 는 **빈 출력**이고
   blob 해시도 같다 ⟹ **내용 diff 0**. 이 판이 만든 것이 아니라 **체크아웃 시점의 줄끝 변환**이다
   (`git status` 가 이 파일을 수정으로 잡지 않는다). 다른 6개 파일은 워킹트리 바이트도 기점과 같다.
```

### §2-4 계기 결함 «등재» 1건 — 이 판이 «스스로» 낸 것 (수리 «했다» · 원문 등재)

```
 ㄱ **1차 ④ 계기가 커널 레코드를 0 개 받았다.** `taichi.profiler` 의 `_traced_records` 는
   **`_update_records()` 를 부르기 «전»에는 비어 있다**(`kernel_profiler.py:193-198` ·
   `get_total_time()` 이 그것을 부른다 `:86,94-95`). 1차 계기는 그걸 빠뜨리고 `_traced_records` 를
   바로 읽어 **레코드 0 · 커널 누적 0.0s** 를 인쇄했다.
 ㄴ **처분** — 결함을 고치고(`prof.get_total_time()` 을 먼저 부른다) **60프레임을 다시 돌렸다**.
   1차 직렬 실행(17:37:59~17:51:06 · 벽시계 767.6 s)은 **무효로 등재**하고 값은 쓰지 않는다.
   1차 색분할 실행은 17:52 에 **중단**했다(같은 결함 · GPU 시간 아낌).
 ㄷ **덧댄 채널 1개(값을 본 «뒤» 추가 · 그렇게 적는다)** — 「29태스크 커널」 마이크로벤치.
   색분할판의 한 서브스텝은 최상위 루프 29개짜리 커널 «한 번» 호출이므로, 같은 «모양»의
   빈 커널 지연을 재야 호출 오버헤드 추정이 모양과 맞는다. 태스크/호출 수는 **세서 확인**한다.
 ㄹ **두 번째(표시) 결함 — `launches` 가 0 으로 찍혔다.** 마이크로벤치의
   `clear_kernel_profiler_info()` 가 `recs` 가 «가리키던 버퍼»까지 비워서, 그 뒤에 센
   `len(recs)` 가 0 이 됐다(로그의 「launch 프로파일러 0」). **집계 자체는 그 «전»에 끝났으므로
   태스크별 횟수·시간은 영향 없다** — 이 판의 표는 `perKernel` 의 횟수 합에서 다시 세서 적었다
   (직렬 37 · 색분할 82,471 · 물리 태스크 6 / 82,440). 계기는 고쳤고(`launches = sum(cnt.values())`)
   **고친 판으로 다시 돌리지는 않았다**(값이 바뀌지 않는 표시 결함 · GPU 추가 0). 그렇게 적는다.
 ★ 이 결함은 **§1-④ 의 값에만** 걸린다 — ①②③ 은 프로파일러를 쓰지 않는다.
 ★ **v4-10 이 등재한 `minPairDist` 결함(수리 0)은 이 판에서 손대지 않았다**(`src/` diff 0).
```

### §2-5 물리 diff 0 — 바이트 증명

| 채널 | 값 |
|---|---|
| ① 1회·2회 창 순변위 6점 | v4-10 §1-④ 직렬 열과 **인쇄 자릿수 전량 동일** |
| ④ 직렬판 창 순변위 6점 | 같은 값 **또 한 번 재현**(세 번째 독립 실행) |
| ④ 색분할판 창 순변위 6점 | v4-10 §1-④ㄷ 색분할 열과 **인쇄 자릿수 전량 동일**(2.714664e-02 … 2.974919e-02) |
| `engine/` 물리 파일 | 7/7 blob 기점과 동일(§2-3) |

### §2-6 과거 노트 «정정» 2건 (이 판의 값으로)

```
 ㄱ **v4-09 §1-③ · v4-10 §1-④ㄱ 의 「최상위 루프 1 → 15 → 29」는 «코드에서 센 수»이고
   컴파일러가 «만든» 태스크 수가 아니다.** 실측(§1-④ㄱㄴ) — 색분할2 는 서브스텝당 태스크
   **6개**이고 그중 **1개가 serial(grid 1 · block 1)** 로 늘어남 8색 + 굽힘 14색 + 봉제 + 몸충돌을
   **전부** 담는다. ⟹ 색 분할판의 제약 투영은 **한 번도 병렬로 돈 적이 없다.**
 ㄴ **v4-09 §1-③ 의 「굽힘 59.3% 가 직렬로 남아 상한이 걸린다」(Amdahl)는 전제가 틀렸다** —
   굽힘만이 아니라 **늘어남·봉제·몸충돌까지 100% 가 직렬**이었다(GPU 시간 99.95%).
   v4-10 §1-④ 가 「상한을 제거했는데도 배수가 없었다」고 적은 것은 **상한이 제거되지 않았기**
   때문이다. ★ `full_color.py`(v4-09 · 늘어남만)는 **이 판에서 측정하지 않았다** — 다만 코드 모양은
   같다(`full_color.py:79·83·87·91` 전부 `if` 안). **측정 전까지 같다고 부르지 않는다.**
 ★ 정정 대상은 **해석**이고 **수치는 그대로 유효**하다(벽시계·궤적·색 수·정점공유 검증).
 ★ 함정 후보 1건 등재 — **「코드에 적은 최상위 루프 수를 컴파일러가 만든 태스크 수로 «믿었다»」**
   (세는 계기가 없었다). 정본은 `.obsidian-log/함정/함정42-루프수와태스크수.md`.
```

## §3 캡처 — **0장**(§0-5 · 회차 프롬프트가 캡처를 부르지 않았다)

## §4 판정 — **갈래 B** (③이 (다) ⟹ ①②④ 상신)

| 항 | 사전 등재 갈래 | 판정 | 한 줄 |
|---|---|---|---|
| ① | (가)/(나)/(다) | **(가) 비트 동일** | `pos`·`vel` sha256 일치 · 다른 정점 **0/9,388** · 최대 좌표차 **0.000000000 mm** ⟹ **경쟁 없음 = 직렬성 가설 기각** |
| ② | 갈래 없음(사실 등재) | — | `full.py:233` **serialize 있음** · 최상위는 `:234` 1회전 ⟹ **두 겹 직렬** · IR 은 offloaded 태스크 **1개**(grid/block 표기 없음) · 프로파일러 이름 `…_serial`(grid 1 · block 1) |
| ③ | (가)/(나)/(다) | **(다) 실행 불가** | 사유 = 선행 조건(①(나) 또는 serialize 부재) **둘 다 불성립** · §0-4ㅁ 대로 · 사후 갈래 신설 0 |
| ④ | (가)/(나)/(다) | **(나) 계산이 지배** | 커널 누적 = 벽시계의 **100.00% / 98.28%** · 비커널 잔여 **0.00% / 1.72%** · launch 추정 상한 **4.61%** |

```
 ⟹ **§4 회차 갈래 = B** (「③(다) ⟹ ①②④ 상신」). D(정지) 아님 — ①②④ 는 전부 갈래 안에서 답했다.
```

### 상신 사실 (전략 세션 몫 · CC 는 처방하지 않는다)

```
 ㄱ **직렬성 가설은 죽었다** — v4 의 직렬판은 «진짜» 직렬이고(두 겹) 두 번 돌려 **비트 동일**이다.
   ⟹ v4 의 진동은 **쓰기 경쟁이 아니다.** 같은 정의역에서 v3 는 수렴한다(v4-10 §1-①ㄴ).
 ㄴ **속도의 자리가 바뀐다** — 「병렬화해도 안 빨라진다」가 아니라 **「아직 병렬화된 적이 없다」**이다.
   색 분할판의 제약 투영은 `if` 안에 있어 **한 스레드 serial 태스크**로 접혔다(기계 검증 ㄹ).
   ⟹ v4-09/v4-10 의 Amdahl 논증은 전제가 무너졌다(§2-6).
 ㄷ **비용의 자리** — 벽시계의 98~100%가 커널 «안»이고, 그 안의 99.95%가 **grid 1 × block 1** 태스크다.
   launch 오버헤드는 상한 4.61%(실측 잔여 1.72%). ⟹ 「커널 던지는 비용」은 지배 요인이 아니다.
 ㄹ **이 판이 «말하지 않는» 것** — `if` 를 걷으면 빨라지는가 · 색 순서가 바뀌면 값이 어떻게 갈리는가 ·
   진동의 원인은 무엇인가 · 이식을 존속할 것인가. **전부 처방 0 · 착수 0.**
```
