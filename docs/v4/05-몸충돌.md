# v4-05 — 층2 시험계 교체(구속계) · 판별력 선검사 · 몸 충돌 이식

**집행 기계 = 2호기(CUDA · 정본 기계) · `src/` diff 0 · 굽기 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-05-collision` |
| 기점 | **`55b2c1f`** (= v4-04 HEAD) |
| 워크트리 | `C:\Users\banba\fit-simulator` |
| 기계 | **`DESKTOP-OV72A46`** (win32 · CUDA) |
| §0 커밋 | **`e35901e`** — 집행 «전» 별도 커밋(sha 만 그 뒤 한 줄로 채웠다) |
| 호출 ecc 에이전트/스킬 | **0** |

---

## §0 수령 (집행 «전» 별도 커밋)

### §0-1 판정문 (전략 세션 · v4-04 §4 수령 · 원문)

```
 「갈래 D 수령 · ① 층1 (가) 통과 = 늘어남·굽힘 커널 «정확 확정» ·
   ② 실패는 v4 결함 아님 — 자유 힌지는 평형이 유일하지 않아 최종 좌표가 well-posed 하지 않다
   (CC 판별력 검사가 값으로 가름 · 섭동 보존 1.00배) ·
   ★ 귀책 = 전략 세션(층2 를 세우며 시험계를 그대로 물려받음 · 3판 연속 같은 계열) ·
   처분 = 층2 시험계를 «구속계»로 교체(정답지 1칸 · 합성계는 층1 전용) ·
   문턱 이동 0 · 함정 39 · 원리 노트 수용」
```

### §0-1′ 귀책 등재 — **전략 세션 3판 연속**(v4-02 상한식 · v4-03 상한식 · v4-04 시험계)

```
 세 판 모두 **CC 는 문턱을 옮기지 않고 값을 상신**했고(`xfail(strict)` 로 고정),
 그 덕에 매번 «형식/계기» 쪽 결함이 드러났다. 결과에 맞춰 옮겼다면 셋 다 묻혔다.
```

### §0-2 사무 (이 커밋에서 집행)

| # | 항목 | 자리 |
|---|---|---|
| ㄱ | 원리 추기 — **「검증계는 well-posed 여야 한다 · 판별력은 «자가 대조»로 먼저 잰다」** | `원리/검증은-도착점과-관측량으로.md` |
| ㄴ | 함정 **40** 신설 — **「합성계는 편하지만 실제 계의 성질을 안 가질 수 있다」** | `함정/함정40-합성계가실제계의성질을안가진다.md` |
| ㄷ | Metal f64 미지원 — **v4-04 §0 에서 이미 등재 완료**(`CLAUDE.md` v4 절 · `00-현재상태`) · 이 판에서는 **재수록만** | — |

### §0-3 검증 3층 (v4-04 §0-2 **유지** · **시험계만 교체**)

| 층 | 시험계 | 상한 |
|---|---|---|
| **층1** 1스텝 | 합성계 + **정답지 1칸** — 현행 유지(v4-04 에서 통과 확정) | K × ULP_f32(그 자리 값) · M 없음 |
| **층2** 수렴 도달 | ★ **정답지 1칸**(`c100-h170-s45_M` · **몸 구속 + 중력**) · **자유 합성계 사용 0** · 궤적 비교 0 | **등재 0**(실측 분포 먼저) |
| **층3** 관측량 | 핏 리포트 5행 · 게이트 판정 | v3 정본 대조 |

### §0-4 바꾸지 않는 것

```
 `src/` · 정본 · blob · v3 동결 · **v4 커널 로직 diff 0**(늘어남 · 굽힘)
```

### §0-5 판별력 문턱 — **0.01** (집행 «전» 등재 · 출처를 명시한다)

```
 **보존비 ≤ 0.01 ⟹ 그 시험계는 층2 «적격»(수축계)** · **> 0.01 ⟹ 부적격**(그 시험 착수 0 · 사유 등재).
 ★★ **이 0.01 은 v4-04 실측(strip 0.000946 ↔ hinge 1.00)의 «사이»에서 뽑은 수가 «아니다».**
   「수축한다」의 **문면**이다 — 섭동이 **두 자릿수 이상 줄어야** 수축이라 부른다.
   두 실측 어느 쪽에서도 **유도하지 않았다**(함정 36: 판정 집합의 값으로 문턱을 만들지 않는다).
   그 결과로 두 실측이 문턱의 양쪽에 «크게» 떨어져 있는 것은 **사후 관찰**이지 근거가 아니다.
```

**섭동의 «정의»도 집행 전에 못 박는다**(v4-04 와 «같은» 계기를 쓴다 — 회차 간 대조가 끊기지 않게):

```
 섭동 = **자유 정점 «전량»의 x 좌표에 +1e-7 m**(v4-04 `scripts/v4Converge.ts:PERTURB` 와 같은 정의).
 ★ 이것은 **강체 병진**이다 — 고정점도 접촉도 없으면 «중립»일 수밖에 없는 방향이다.
   그래서 이 섭동은 **「그 계가 강체 모드를 구속하는가」를 정면으로 묻는다.**
   정답지 1칸은 고정점이 **0개**이고 구속은 **몸 충돌 + 마찰**에서만 온다 ⟹ 물음이 실질적이다.
 보존비 = (최종 좌표차 최대) / 1e-7.
```

### §0-6 사전 등재 갈래 (원문)

| 갈래 | 조건 | 처분 |
|---|---|---|
| **A** | ①~④ 전량 + ① 적격 + ③ 층1 통과 | 상신 |
| **B** | ① **정답지 1칸도 부적격**(보존비 > 0.01) | 값 상신 · **층2 설계 재검**(②③ 층2 착수 0 · **③ 층1 은 집행**) |
| **C** | ③ **층1 초과** | 값 상신 · **정지** |
| **D** | 그 외 | 정지 · 원문 상신 |

**사후 갈래 신설 0.**

### §0-7 착수 «전» 알려진 사실

```
 · 몸 충돌은 **몸 SDF 격자**가 필요하다(`bakeSdf` 산출 · `GridSdf` = ox·oy·oz·h·nx·ny·nz·band·data).
   현재 덤프에 **없다** ⟹ `scripts/v4Export.ts` 를 한 번 더 넓힌다(`src/` 는 여전히 **0줄**).
 · 정답지 1칸에는 **고정점이 0개**다(`assignMassFromMesh(..., new Set())`) ⟹
   **옷을 붙들고 있는 것은 몸 충돌과 마찰뿐**이다. 그래서 ① 의 물음이 실질적이다.
 · 정답지 1칸의 v3 제약은 `[...inplane, ...bends, ...seamCons]` 다. **봉제(`dist`)는 아직 이식 0** ⟹
   이 판의 대조는 **늘어남·굽힘·몸충돌**로 정의역을 «맞춰» 돌린다(v3 쪽도 같은 부분집합).
   **봉제를 뺀 것이 v3 정본 장면과 다르다는 사실을 여기 적어 둔다**(숨기지 않는다).
```

---

## §1 집행

### §1-① 판별력 선검사 — **세 시험계 전부 «부적격»** · 사유는 보존비가 «아니라» 산출 불가

절차는 §0-5 대로다: 시험계마다 **v3 를 v3 와** 초기값 1e-7 섭동으로 대조해 **섭동 보존비**를 먼저 잰다.
보존비를 재려면 **양쪽이 수렴해야** 한다(층2 는 «도착점» 비교다) — 그 전제가 깨졌다.

#### 먼저 잰 것 — 비용과 거동

| 시험계(제약 부분집합 + 몸 충돌 + 중력) | 제약 수 | 10프레임 창 순변위 | 프레임 비용 |
|---|---|---|---|
| **both** = 늘어남 + 굽힘 (v4 이식분 전량) | 44,664 | **1.634188e-01 m** | **9,333 ms/프레임** |
| *(참고)* **all** = 늘어남 + 굽힘 + **봉제** | 44,920 | **1.599926e-02 m** | 9,661 ms/프레임 |

#### 무슨 일이 일어나는가 — 정착 상태 대비 이동

| | 옷 y 범위 (m) | 옷 중심 (m) | 정착 대비 이동 최대 / 중앙 | 어깨선(y>1.35) 최대 |
|---|---|---|---|---|
| **v3 정본 정착** | 0.8066 ~ 1.4496 | (0.00176, **1.16436**, 0.00561) | — | — |
| **both** 10프레임 | 0.7135 ~ 1.4208 | (0.00149, **1.07980**, 0.00945) | **163.4 mm** / **96.5 mm** | **122.8 mm** |
| *(참고)* **all** 10프레임 | 0.8057 ~ 1.4512 | (0.00168, **1.16405**, 0.00556) | 16.0 mm / **0.8 mm** | 16.0 mm |

```
 ★★★ **봉제를 빼면 옷이 흘러내린다.** 10 프레임에 **중심이 84.6 mm 내려가고** 중앙 변위가 **96.5 mm** 다.
   ⟹ **「늘어남만 · 굽힘만 · 둘 다」는 «구속계»가 아니다.** 정답지 한 칸을 골랐다고 구속계가 되는 것이
     아니라, **옷을 몸에 붙들어 두는 것이 봉제 제약까지 포함한 «전체 집합»**이었다.
   ⟹ 이 세 시험계는 **수렴하지 않는다**(옷이 몸에서 벗겨져 계속 떨어진다) ⟹
     **섭동 보존비를 «산출할 수 없다»**(도착점이 없다).
 ★ 봉제를 «넣으면» 제자리에 있다 — 중심 이동 **0.31 mm** · 중앙 변위 **0.8 mm**.
   (그래도 완전한 정지는 아니다: 최대 16.0 mm. 정착 blob 은 **자기충돌까지 켜고** 만든 것이라
    자기충돌을 뺀 이 정의역에서는 평형이 조금 다르다. 그 차이가 16 mm 다.)
```

#### 판정

| 시험계 | 보존비 | 층2 적격 | 사유 |
|---|---|---|---|
| 늘어남만 | **산출 불가** | ❌ **부적격** | 수렴 없음 — 옷이 흘러내린다(늘어남만으로는 더 심하다) |
| 굽힘만 | **산출 불가** | ❌ **부적격** | 〃 |
| **둘 다** | **산출 불가** | ❌ **부적격** | 10프레임에 중앙 96.5 mm 이동 · 중심 −84.6 mm |

```
 ★ **문턱 0.01 은 쓰이지 않았다** — 재야 할 수가 «없어서»다. 이것은 「0.01 초과로 부적격」보다
   **강한** 불합격이다(측정 가능성 자체가 없다). §0-5 의 「보존비 ≤ 0.01 이면 적격」에 해당하지
   않으므로 **부적격**이고, 문턱을 옮기거나 완화한 곳은 **0** 이다.
 ★ **「늘어남만」·「굽힘만」은 «돌리지 않았다».** 「둘 다」가 이미 흘러내리는데 제약을 더 빼면
   더 심해지는 것이 구조적으로 자명하고(제약 부분집합), 한 실행이 **9.3 초/프레임 × 수백 프레임**이라
   **돌려도 같은 결론에 비용만 든다**. 미집행 사실을 여기 적는다 — 침묵으로 넘기지 않는다.
```

### §1-② 층2 첫 시행(구속계) — **착수 0**

```
 §0-6 **B = 「① 정답지 1칸도 부적격 ⟹ 값 상신 · 층2 설계 재검(②③ 층2 착수 0 · ③ 층1 은 집행)」**.
 ① 이 부적격이므로 ② 는 **한 줄도 돌리지 않았다.** 상한 등재 0 · 실측 0.
 ★ 다만 **다음 판의 설계에 쓰라고** 위 표의 「all(봉제 포함)」 실측을 남긴다 —
   **봉제(`dist`) 제약을 이식하면 이 칸이 구속계가 된다**는 첫 증거다(중앙 0.8 mm).
```

### §1-③ 몸 충돌 이식 — **층1 통과**

**v3 코드 사실 — 손으로 적지 않고 «인용»한다**

| 자리 | 원문 / 사실 |
|---|---|
| `src/v3/bodySdf.ts:246` | `sampleSdf` — **삼선형 보간** · 격자 «밖»이면 **`g.band`** 를 돌려준다(외삽 0) |
| `src/v3/bodySdf.ts:256` | `at(a,b,c) = g.data[(c*ny + b)*nx + a]` · `data` 는 **`Float32Array`** |
| `src/v3/solver.ts:126` | `sdf(kind='grid')` — 법선은 **중심차분** · 간격 **`e = g.h`**(격자 간격 그대로) |
| `src/v3/solver.ts:135-142` | `l = hypot(gx,gy,gz)` · **`l < 1e-20` 이면 법선 = (0,1,0)** |
| `src/v3/solver.ts:205` | `d = sdf(...) − cp.thickness` · **`d ≥ 0` 이면 `continue`** — 「밖 ⟹ 아무 일도 안 한다(흡착 0)」 |
| `src/v3/solver.ts:210-212` | `depth = −d` · **`pos += depth·n`** |
| `src/v3/solver.ts:218-244` | 쿨롱 마찰 — 접선 변위 `t = (pos − prev) − ((pos−prev)·n)n` · **`k = min(1, μ·depth/|t|)`** · `pos −= t·k` · **`|t| > 1e-15`** 일 때만 |
| `src/v3/consts.ts:6·10` | `THICK = 1e-3` · `MU = 0.3` |
| `src/v3/dressRun.ts:prepare` | `collision = { colliders:[{kind:'grid', g: bodyG}], thickness: THICK, mu: MU }` |
| `src/v3/solver.ts:1128` | 충돌은 **제약 투영 «뒤» · 속도 갱신 «앞»** |

**몸 SDF 격자**(`bakeSdf` 산출 · `scripts/v4Export.ts` 로 꺼냈다)

| 항목 | 값 |
|---|---|
| 격자 | **459 × 456 × 80** = **16,744,320 셀** · `float32` |
| 간격 `h` | **3.8049 mm** · band **8.609835e-03** |
| 파일 | `sdf-c100-h170-s45.bin` · **63.9 MB** ⟹ ★ **git 밖**(`.gitignore`) · 재생성 = `DUMP=… npx tsx scripts/v4Export.ts` |

**충돌 커널 K′′** — `gpu/engine/collide.py:_resolve`(+`_sample`)

| 자리 | 식 | 연산 수 | 깊이 |
|---|---|---|---|
| `_sample` | `fx,fy,fz` · `tx,ty,tz` · x·y·z 삼선형 | **33** | **10** |
| `d0` | `_sample(p)` | 33 | 10 |
| `gx·gy·gz` | `_sample(p±e)` **6회** + 차 3 (+ p±e 6) | **207** | 12 |
| `l` | `sqrt(Σg²)` | 6 | 16 |
| `n` | `g / l` | 3 | 17 |
| `d`·`depth` | `d0 − thickness` · `−d` | 2 | 12 |
| 밀어내기 | `pos += depth·n` | 2 | **19** |
| 마찰 | `t` 3 · `t·n` 5 · `t −= (t·n)n` 6 · `|t|` 6 · `k` 3 · `pos −= t·k` 6 | **29** | **34** |
| | **정점 하나 · 서브스텝 하나** | **282** | **34** |

```
 ★ **deg = 1** — 한 정점은 한 서브스텝에 충돌체 하나로 «한 번» 해소된다.
 ★ 예측 단계(중력)가 앞에 **깊이 4** 를 더하지만 **세지 않았다** — 34 로 통과하면 38 로도 통과한다
   (더 엄격한 쪽을 판정에 쓴다 · v4-04 §0-2 ㄴ 과 같은 규범).
```

**층1 — 「충돌만 1스텝」**(정의: 정착 위치 · **속도 0** · **중력 켬** · 감쇠 0 · substeps 1 ·
dt = DT/SUB · **제약 0개** · 충돌 켬)

```
 ★ **중력을 «켠» 이유**(늘어남·굽힘 1스텝과 다르다): 마찰은 «접선 변위»에 걸리는데
   중력이 없으면 예측이 위치를 안 바꿔 `pos − prev = 0` 이 되고 **마찰 경로가 한 줄도 안 돈다**.
   중력을 켜면 예측이 g·h² 만큼 내리고 그 변위에 마찰이 걸린다 ⟹ **밀어내기와 마찰을 함께** 잰다.
```

| 항목 | 값 |
|---|---|
| v3 충돌 1스텝 변위 | 최대 **1.327616e-06** m · 중앙 0.000e+00 |
| 최대 \|좌표\| | 1.449586255 m ⟹ ULP_f32 1.192093e-07 |
| **v4 f32 — 최대 좌표차** | **1.115526e-07** · 중앙 3.222e-09 |
| **층1 비**(K 34 × deg 1) | **최대 0.0275** · 중앙 0.0102 · p99 0.0271 · **초과 정점 0 / 9,388** ⟹ **통과** |
| **v4 f64 — 최대 좌표차** | **4.440892e-16** ⟹ 이식 **정확** |
| 충돌 발화 정점 | f32 **536** ↔ f64 **1057** · 최대 침투 깊이 f32 1.759734e-06 · f64 1.811913e-06 |

```
 ★★ **새 사실 — 분기 판정이 정밀도에 민감하다.** `d < 0` 경계에 걸친 정점이 많아
   **발화 수가 정밀도로 갈린다**(536 ↔ 1057 · 1.97배). 그런데도 **최종 위치 차는 1.12e-07** 에 머문다 —
   경계 정점의 `depth ≈ 0` 이라 밀어내는 양이 없기 때문이다.
   ⟹ **「발화 수」를 대조 채널로 쓰면 안 된다**(정밀도만으로 2배 갈린다). 위치가 채널이다.
   시험에 **사실로 고정**했다(`test_collide_hit_count_is_precision_sensitive` · 문턱 아님).
 ★ `Math.hypot` 부재는 여기서도 같다 — `sqrt(Σx²)` 로 옮겼고 상대차 2.584e-16(f32 ULP 보다 9자릿수 작다).
```

### §1-④ 「같은 옷」 기준 후보 — **착수 0**

```
 ②③층2 가 미집행이라 근거 분포가 없다. 층1 3종(늘어남 0.0924 · 굽힘 0.1438 · 충돌 0.0275)은
 **1스텝 산술 오차**이지 「같은 옷」의 자가 «아니다» — 그 기준은 **도착점·관측량**의 것이다
 (원리 「검증은 도착점과 관측량으로」). 층1 값으로 후보를 세우면 **자를 잘못 빌리는 것**이다.
 **등재 0 · 후보 0.**
```

### §1-⑤ 미집행 기록 — **200프레임 수렴 실행을 «중단»했다**

```
 ① 을 정량으로 더 받치려고 `CONS=both` · `CONS=all` 을 **CAP=200 으로 배경 실행**했다가
 **10.9 CPU-분 시점에 «정지»시켰다**(각 약 70 프레임 추정 · 산출물 없음).
 사유 — ① 의 결론은 **10 프레임 증거로 이미 확정**됐고(중심 −84.6 mm · 중앙 96.5 mm),
   200 프레임 자취는 **다음 판에서 정의역이 바뀐 뒤**(봉제 이식) 다시 재야 하는 값이다.
 ⟹ 이 판의 `cellconv-*.bin` 은 **10 프레임 상태**다(위 표의 출처). 침묵으로 넘기지 않는다.
```

---

## §2 자기검사

| # | 항목 | 결과 |
|---|---|---|
| ㉠ | ①③ 표 | 위 §1 (② 는 미집행) |
| ㉡ | **pytest(CUDA) 전량** | **32 passed · 2 xfailed · 1 xpassed** |
| ㉢ | `src/` diff | **0 파일**(기점 `55b2c1f` 대비) |
| ㉣ | 정본 diff | **0** — `public/v3diag` 쓰기 0 |
| ㉤ | blob sha | **36/36 일치** · 몸 27/27 |
| ㉥ | **늘어남·굽힘 커널 로직 diff** | **0** — `stretch.py` · `bending.py` 한 줄도 안 바뀌었다 |
| ㉦ | **v4-02/03/04 산출물 바이트 불변** | 변경 0 — 이 판 산출은 **새 파일** |
| ㉧ | `npx tsc -b` | **통과**(exit 0) |
| ㉨ | 굽기 | 착장 `runFrames` **0회** |
| ㉩ | 호출 ecc 에이전트 / 스킬 | **0 / 0** |
| ㉪ | 맥 Metal 재실행 | **미도착**(f64 `skip` 처리분 · v4-04 §4-1 ㉥ 이월) |

**§3 캡처 — 0장**(사전 등재 그대로).

---

## §4 판정 — **갈래 B**

```
 §0-6 대조:
   **A** = ①~④ 전량 + ① 적격 + ③ 층1 통과  → ① 이 **부적격**이므로 **불성립**
   **B** = ① 정답지 1칸도 **부적격**          → **성립**
   **C** = ③ 층1 초과                        → ③ 층1 은 **통과**(비 0.0275) ⟹ 불성립
   **D** = 그 외                              → B 가 성립하므로 해당 없음
 ⟹ **갈래 B**. 처분대로 **②③ 층2 착수 0 · ③ 층1 은 집행**했다(그리고 통과했다).
 ★ 문언의 미세한 차이를 적어 둔다 — B 의 괄호는 「보존비 > 0.01」인데 이 판의 사유는
   **「보존비 산출 불가」**다. §0-5 의 적격 정의(「보존비 ≤ 0.01 이면 적격」)에 해당하지 않으므로
   **부적격**이고, 이것은 「0.01 초과」보다 **강한** 불합격이다. **문턱을 옮기거나 완화한 곳은 0.**
 **사후 갈래 신설 0 · 상한 등재 0 · v3 diff 0 · 커널 로직 diff 0.**
```

### §4-1 상신하는 값

```
 ① `both`(늘어남+굽힘+몸충돌+중력) 10프레임 — 창 순변위 **1.634188e-01 m** ·
    옷 중심 1.16436 → **1.07980 m**(−84.6 mm) · 정착 대비 중앙 변위 **96.5 mm** · 어깨선 최대 122.8 mm
    `all`(+봉제) 10프레임 — 창 순변위 **1.599926e-02 m** · 중심 이동 **0.31 mm** · 중앙 **0.8 mm** ·
    최대 **16.0 mm**(자기충돌을 뺀 몫) · 비용 **9.3~9.7 초/프레임**
 ③ 충돌 층1 — f32 **1.115526e-07**(비 **0.0275** · 초과 정점 **0/9,388**) ↔ f64 **4.440892e-16**
    발화 정점 f32 **536** ↔ f64 **1057** · 최대 침투 1.759734e-06 / 1.811913e-06
```

### §4-2 넘기는 것

| # | 항목 | 몫 |
|---|---|---|
| **㉠** | **봉제(`dist`) 이식을 층2 의 «선행 조건»으로 세울지** — 값이 그렇게 가리킨다(중앙 **0.8 mm** ↔ **96.5 mm**). 제약 자체는 v4-02 §1-③ 에서 이미 «식으로» 읽어 두었다(`projectDistance` · α = 1/k) | 전략 세션 |
| **㉡** | **정착 blob 은 «이 정의역»에서 평형이 아니다** — 자기충돌을 빼면 최대 **16.0 mm** 움직인다. 층2 초기값을 blob 그대로 쓸지, 정의역에 맞춰 **다시 정착**시킬지 | 전략 세션 |
| ㉢ | **비용** — v3 가 이 칸에서 **9.3~9.7 초/프레임**이다(충돌 질의가 지배 · SDF 16.7M 셀). 층2 를 v3 로 도는 비용이 판을 지배한다. v4(GPU) 는 1스텝 2.9 초(컴파일 포함) | 전략 세션 |
| ㉣ | ★ **충돌 「발화 수」는 대조 채널이 될 수 없다** — 정밀도만으로 **1.97배** 갈린다(536 ↔ 1057). **위치가 채널이다** | 전략 세션 |
| ㉤ | **몸 SDF 덤프가 64 MB 라 git 밖**이다 — 맥에서 충돌 시험을 돌리려면 **재생성 또는 scp** 가 필요하다 | 사용자 · 전략 세션 |
| ㉥ | **층3(관측량)은 아직 착수 0** — 핏 리포트 5행 mm·게이트 판정 경로가 v4 에 없다 | 전략 세션 |
| ㉦ | 맥 Metal 재실행(f64 `skip` 처리분) — v4-04 §4-1 ㉥ 이월 · 아직 미도착 | 사용자(승혁) |
