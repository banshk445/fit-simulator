# v4-45 — 그리드 정본 선언(브라우저 27칸) · 기본 칸 재굽기 · 108칸 발주 재료 확정

**물리 0줄 · `src/` diff 0 · 굽기 = 탐침 1칸 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-45-canon-probe` |
| 기점 | **`f9a4060`**(= v4-44 §1~§4 + 볼트 + 보고) |
| §0 커밋 | (아래 §0-5 에 자기 기입) |
| §1~§4 커밋 | (집행 후 기입) |
| 보고 파일 | `docs/v4/보고/45.md` |

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v4-45-canon-probe f9a4060 → Switched to a new branch 'v4-45-canon-probe'
       $ git branch --show-current → v4-45-canon-probe · HEAD f9a4060
 2호기 (§0 커밋을 push 한 «뒤» 같은 자리로 전환 — §0-5 에 인쇄)
```

---

## §0 수령·사무

### §0-1 판정문 (전략 세션 · v4-44 §4 수령 · 원문)

```
 「갈래 C 수령 · ★★★ 교차차 정체 = unitScale 0.963145(rawHeight 1.765052→1.700000 · 콘솔 실측과 동일 수)
   + y 이동 · 정합 잔차 0.0002mm = 같은 몸 ·
   ★★ 정본 결정: 그리드 정본 = 브라우저 27칸(키 계약을 맞추는 좌표) · Node 기준 몸 = 참조로 강등 ·
     선택지 ㉠ 채택(㉢ 역정합 기각 — 정본을 변환 위에 안 세운다) ⟹
     기본 칸 1개 브라우저 몸 재굽기로 게이트·층3·화면 재확인
     (규칙들은 몸에서 유도되므로 스케일 자동 적응 — 그 확인이 이 판) ·
   ★ 던짐 4칸 = 정상 갈래 D(착용불가 부류) 수용 · 108칸 집계는 T포즈 규약대로」
```

### §0-2 바꾸지 않는 것

```
 물리 **0줄** · `src/` **0줄** · 워커 굽기 경로 **diff 0** · 정본·blob·문턱 **0** · 게이트 채널·문턱 **0**
```

### §0-3 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · 단위 명시)

**★ 단위** — 게이트·리포트는 **mm** · 정착은 **프레임** · `RAMP_N` 은 **프레임** · `SLV_R` 은 **mm**.

| 항 | 갈래 |
|---|---|
| **①** 기본 칸 재굽기 | **(가)** 게이트 `pass` + 층3 성립 / **(나)** fail(채널·값 · 처방 0) / **(다)** 조립 실패·발산 |
| **②** 캡처 | 앞·옆 2장 · **화면 판정 0** |
| **③** 108칸 재료 | ①(가) 전제 · **실행 0** · 목록·시간·재개 규약·던짐 예상 부류 |
| **④** 정본 등재 | 볼트에 「그리드 정본 = 브라우저 27칸(sha) · Node 몸 = 참조 · unitScale 사실」 |

**§4** — A = ①(가)+②③④ ⟹ 상신(다음 판 = 108칸 발주) / B = ①(나)(다) ⟹ 값·캡처 상신 / C = 그 외 ⟹ 정지.

### §0-4 사전 결정 4건 (값 보기 «전» · CC 집행 설계)

```
 ㄱ **축·피벗은 그 칸의 json 에서만 읽는다**(손 상수 0) —
    `ARM_AXIS_JSON=grid27/l3ap-body-c100-h170-s45-a35.json` · `ARM_ORIGIN_JSON=grid27/l3ap-origin-….json`.
    v4-37 이 쓴 Node 판 json 을 **섞지 않는다**(몸이 다르면 축도 그 몸 것이어야 한다).
 ㄴ **태그를 가른다** — 이 판의 조립·굽기 태그는 `Bwsr_M`(브라우저 몸)이다.
    v4-37 의 `Aaxi_M`(Node 몸) 산출물은 **덮지 않는다**(대조가 남게).
 ㄷ **비교는 «나란히»** — v4-37 값(노트에서 읽어 옮긴다 · 함정 44)과 이 판 값을 같은 표에 둔다.
    **스케일 차 때문에 mm 값이 달라지는 것은 사실로만** 적고, 판정은 **게이트 5채널과 대역 이름**으로 한다.
 ㄹ **③은 ①(가)일 때만 · 실행 0** — 발주 목록·시간·재개 규약까지만 적고 굽기는 다음 판이 한다.
```

### §0-5 커밋·전환 인쇄 (집행 «전»)

(§0 커밋 sha 자기 기입 · 2호기 전환 인쇄는 아래에 붙인다)

---

## §1-① 기본 칸 재굽기(브라우저 몸) — **(가) 게이트 `pass` + 층3 성립**

**입력** — 몸 `gpu/oracle/export/grid27/l3ap-body-c100-h170-s45-a35.bin` ·
축 `grid27/l3ap-body-…-a35.json` · 원점 `grid27/l3ap-origin-…-a35.json`(§0-4ㄱ 대로 **그 칸의 json** 만) ·
태그 **`Bwsr_M`**(v4-37 의 `Aaxi_M` 은 덮지 않았다 · §0-4ㄴ).

### 조립(`v4AsmExport` · NaN 가드 통과)

```
 cell c100-h170-s45_M · tag **Bwsr_M** · n **9490** · m 18216 · mb 26946 · ms 258 · substeps 231 ·
 sdf **416×479×84** · 산출 scene-Bwsr_M.bin · scene-bend-Bwsr_M.bin · scene-seam-Bwsr_M.bin ·
 sdf-Bwsr.bin · asm-Bwsr_M.bin  (조용한 NaN 0 — 가드가 던지지 않았다)
```

### 굽기(워커 · `BODY_BIN` 지정 · 조립→램프→정착)

```
 job v4-45-Bramp — fp f64 · arch Arch.cuda · cell Bwsr_M · frames 400(상한) · ramp true
 정착 **f350** · convNet **4.391290028753105e-05** ≤ TOL 1e-4 · rampN **71** · degenerate 0 ·
 2.3079086397142548 s/프레임 · JIT 51.80702219996601 s · 벽시계 807.7680238999892 s ·
 셀프 쌍/서브스텝 39539 · 해소/서브스텝 414.4805194805195
 ★ **층3이 이 판에서는 «섰다»** — `BODY_BIN` 을 환경에 준 덕에 워커가 칸을 **성공**으로 기록했다
   (v4-37 의 칸 실패는 그 지정 누락이었다 · v4-44 §1-③ㄷ 에 등재).
```

### 게이트 5채널 — **`pass: true` · `fails` 빈 집합**

| 채널 | **v4-45(브라우저 몸)** | v4-37(Node 몸) | 문턱 | 판정 |
|---|---|---|---|---|
| ③a 관통 최대 | **0.3308660086558528 mm**(관통정점 942) | 0.3641023393151302 mm(1029) | ≤ 0.5 | ✅ |
| 자기관통 교차 | **0** | 0 | 0 | ✅ |
| 목선 초과비 | **0.9889475802550393**(링 54.01675288184066 / 허용 54.62044092156057 cm) | 0.9889618866955814(56.07699976110403 / 56.70289271558697) | ≤ 1 | ✅ |
| 정착 창 순변위 | **0.04391290028753105 mm** | 0.04995022847012743 mm | ≤ 0.1 | ✅ |
| 보조 장치(invMass=0) | **0** | 0 | 0 | ✅ |
| (부수) 최소 쌍거리 | **1.9996522748392298 mm** | 1.9963123626612032 mm | — | — |
| (부수) 봉제 간극 중앙/최대 | **2.05250177532328 / 4.940200837497237 mm** | 2.052594728543278 / 4.990017302927032 | — | — |
| (부수) λ 최대 | **1.3682428330765821** | 1.3722453215150943 | — | — |
| (부수) 발산 | **false** | false | — | — |

### 겨드랑이 채널

| 채널 | **v4-45** | v4-37 |
|---|---|---|
| 팔↔몸통 최소 간격(봉제쌍 제외) | **2.2049879318722465 mm**(정점쌍 9489↔7040) | 2.424562062783695 mm(9133↔7223) |
| 팔↔몸통(봉제 포함) | **2.024061868586761 mm** | 2.0097268505141903 mm |
| 대역 자기관통 교차 | **0** | 0 |
| 대역 최소 쌍거리 | **1.9996522748392298 mm** | 1.9963123626612032 mm |
| 대역 관통 최대 | **0.1635583689118291 mm**(정점 9397) | 0.25911502011576537 mm(7896) |
| 대역 몸거리 최소 | **0.8364416310881709 mm** | 0.7408849798842346 mm |

### 층3 5행 — **산출 성립** · `chestY > waistY`

| 레벨 cm | **v4-45(브라우저 몸)** | v4-37/39(Node 몸) | 비 |
|---|---|---|---|
| `chestYCm` | **131.20635318756104** | 136.64153099060056 | 0.960223 |
| `waistYCm` | **103.22462692970475** | 107.58755126227962 | 0.959449 |
| `yLowCm` | **79.38604068756099** | 82.83684349060051 | 0.958341 |
| `yArmCm` | **131.21416568756104** | 136.64934349060056 | — |
| `cChestCm` | **94.51733260197157** | 98.15558742962283 | 0.962931 |
| `cWaistCm` | **65.53116102134912** | 68.0387578347535 | 0.963146 |

★ `chestY`(131.21) **>** `waistY`(103.22) ⟹ v4-39 의 겨드랑이 규칙이 **스케일을 자동으로 따라갔다**
(판정문이 확인하라 한 바로 그 지점). 둘레 비 **0.963146** 은 §1-① 의 배율 `0.96314478` 과 소수 다섯째 자리까지 같다.

| 5행(중앙 / p25 / p75 mm · 밀착/여유 · 표본) | **v4-45** | v4-37/39 |
|---|---|---|
| 목선 | **1.0112627327022439** / 0.9853652214163299 / 2.0176169130499706 · 40/14 · **54** | 1.0692841106160207 / 0.9999999849952131 / 2.6847060798192777 · 40/16 · 56 |
| 가슴 | **2.411176680535642** / 1.0096658234099398 / 6.20639810349252 · 109/140 · **249** | 2.0790 / 1.0016 / 5.4760 · 123/128 · 251 |
| 허리 | **49.77376443277912** / 29.04065522557891 / 66.69176880356339 · 0/225 · **225** | 48.41965459456567 / 29.736365093853223 / 61.6885133359544 · 0/228 · 228 |
| 밑단 | **27.088334975397796** / 2.2527944263316715 / 44.5169617439377 · 27/83 · **110** | 19.334009181588545 / 1.5331311849830729 / 33.207306744300446 · 31/81 · 112 |
| 소매 | **9.167982446935003** / 1.9158936572677303 / 18.73279805827941 · 260/760 · **1020** | 7.394977060365645 / 1.724933944953111 / 16.944983507437183 · 276/744 · 1020 |

**눌림 0**(다섯 행 전부 `pressN = 0`) · NaN 0.
★ **mm 값이 다른 것은 «몸이 0.963배 작아졌는데 옷 규격은 그대로»여서다**(옷 치수는 사이즈표에서 오고 몸에서 오지 않는다).
판정은 §0-4ㄷ 대로 **게이트 5채널과 대역 이름**으로 했다 — 다섯 대역 전부 같은 이름으로 섰다.

### 조립·배치 채널

| 채널 | **v4-45** | v4-37 |
|---|---|---|
| `SLV_R`(소매 반경) | **56.05492295260141 mm**(`PLACE_SIG` 0.027792/0.002000/0.215000/**0.056055**) | 56.750 mm(v4-29 등재) |
| `RAMP_N` | **71** | 73 |
| 정착 프레임 | **f350** | f290 |
| 옷 정점 `n` | **9490** | 9644 |
| `fitSleeve` 탐색 회차 | **43** · 마지막 실패항 **소매자기쌍**(min 0.0019999999999917224) | (v4-28 계기와 같은 채널) |

## §1-② 캡처 — 2장 · **화면 판정 0**

`docs/v4/캡처/45-1-front.png` · `45-2-side.png`(900×1200 · 정사영 `front`/`sideXplus` ·
몸 = **브라우저 몸** · 옷 = ①의 정착 위치 `Bwsr_M.bin` · v4-24·26·29·30·37 과 **같은 프리셋**).
대조군은 `37-1-front.png`·`37-2-side.png`(Node 몸)다. **보기 판단은 전략 세션·사용자가 한다.**

## §1-③ 108칸 발주 재료 확정(실행 0)

### 목록 규약

```
 몸 27 × 사이즈 4(S·M·L·XL) = **108칸** · 칸 이름 = `<몸id>_<사이즈>`(T포즈 108칸과 같은 규약 · `grid.ts` `cells()`)
 몸 파일 — `gpu/oracle/export/grid27/l3ap-body-<몸id>-a35.bin`(BODY_BIN)
 축 파일 — `…/grid27/l3ap-body-<몸id>-a35.json`(ARM_AXIS_JSON)
 원점    — `…/grid27/l3ap-origin-<몸id>-a35.json`(ARM_ORIGIN_JSON)
 조립    — `CELL=<칸> BODY_BIN=… ARM_AXIS_JSON=… ARM_ORIGIN_JSON=… TAG=<태그> npx tsx scripts/v4AsmExport.ts`
           태그 규약 제안 = `<몸id 축약>_<사이즈>` — `sdf-<태그의 _ 앞부분>.bin` 이 **몸마다 1개** 나오므로
           같은 몸의 네 사이즈는 **같은 접두**를 써야 SDF 를 한 번만 굽는다(`v4AsmExport.ts:23` `BODYTAG`)
 굽기    — 워커 job(형식은 v4-45 와 동일) · `cells[]` 에 칸마다 한 항 ·
           **`BODY_BIN` 은 워커를 띄우는 환경에 준다**(층3이 그 값을 물려받는다 · `worker.py:174`) ⟹
           **몸이 바뀌면 워커를 다시 띄운다**(한 job = 한 몸 · 4칸)
```

### 규모·시간(이 판 실측에서 유도 · 새 가정 0)

```
 칸당 = 2.3079086397142548 s/프레임 × 정착 프레임(이 판 **350**) ≈ **13.5 분** + JIT 51.8 s + 빌드 2.0 s
 108칸 ≈ **25.9 시간**(단순 곱 · 실패·재시도 0 가정) — 한 몸(4칸)당 ≈ 58 분
 ★ 정착 프레임은 칸마다 다르다(v4-37 290 · v4-45 350) ⟹ 위 수는 **이 판 값 기준의 추정**이다
```

### 체크포인트·재개(T포즈 108칸 규약 인용 · 새 규약 0)

```
 `gpu/bake/worker.py:11` — `results/<job>/<칸>.done` 이 있으면 그 칸을 **건너뛴다**(칸 단위 재개)
 `worker.py:10` — `gpu/bake/jobs/PAUSE` 파일이 있으면 **다음 칸을 시작하기 전** 기다린다(돌던 칸은 끝까지)
 `worker.py:225` — 자식 실패는 **다음 칸으로**(재시도 0) · 실패 칸 목록이 job 끝 로그에 남는다
 산출 — `results/<job>/<칸>.bin`(정착 위치 f64) · `<칸>.json`(궤적·시간·셀프 통계) ·
        `fit-<칸>.json` · `gate-<칸>.json`(층3 · **BODY_BIN 지정 시에만 선다**)
```

### 던짐 예상 부류(사실 · v4-44 §1-② 인용)

```
 몸 27개 중 **4개**(c122.5-h155-s45 · c122.5-h170-s50 · c122.5-h185-s45 · c122.5-h185-s50)는
 **M 사이즈**에서 옷 제도가 던진다(`garmentScene.ts:309` 소매산 / `:922` SEP 미달 · 갈래 D).
 같은 몸이라도 **사이즈가 다르면 서는 칸이 있다**(T포즈 108칸 대조표 · v4-44 §1-②) ⟹
 발주 목록은 **칸 단위**로 세우고, 던지는 칸은 T포즈 108칸과 **같은 방식으로 집계**한다(판정문 문언).
 T포즈 108칸의 던짐은 **26칸**(SEP 15 · 소매산 9 · 목 밑동 2)이었다 — A포즈에서 같은 수가 나오리라는
 **보장은 없다**(옷 제도는 몸과 무관하나 몸 스케일이 바뀌면 조립 자기간격 판정이 달라질 수 있다 · 사실만).
```

## §1-④ 정본 등재

볼트 세 곳에 아래를 등재했다(§4 뒤 커밋 sha 로 고정).

```
 ★ **A포즈 그리드 «정본» = 브라우저 27칸**(`gpu/oracle/export/grid27/` · 커밋 **80944b2** 에 들어온 82파일)
   — 키 계약을 맞추는 좌표(높이 목표차 최대 **2.0317 mm**)이고, 층3·게이트가 이 몸에서 **선다**(이 판 §1-①).
 ★ **Node 기준 몸**(`gpu/oracle/export/l3ap-body-<몸>-a35.bin`)은 **참조로 강등** — 폐기하지 않는다(이력·대조용).
 ★ **둘의 관계** = `브라우저 = 0.96314478 × Node + (0, −3.9609 mm, 0)` · 정합 잔차 **최대 0.0002 mm**(v4-44 §1-①).
   `1/0.96314478 = 1.038276` = `Mannequin.tsx` 의 **unitScale**(rawHeight 1.765052 → 목표 1.700000).
 ★ v4-37 의 정본 옷(`Aaxi_M`)은 **Node 몸 기준**이다 — 이 판의 `Bwsr_M` 이 **브라우저 몸 기준 첫 정착 산출**이다.
```

---

## §2 자기검사

| 항 | 값 |
|---|---|
| `src/` diff vs 기점 `f9a4060` | **0**(무출력) · 물리 **0줄** |
| 워커 · `gpu/engine/` | **diff 0** · 정본·blob·문턱 **0** · 게이트 채널·문턱 **0** · 격자 용량 262144 유지 |
| 이 판이 **신설**한 것 | job `v4-45-Bramp.json` · 조립 5종(`asm-Bwsr_M.bin` 등) · 굽기 산출 5종(`Bwsr_M.bin`·`.json`·`.done`·`fit-`·`gate-`) · 계기 산출 2종(`l3ap-sleeve-v4-45-B.json`·`l3ap-armpit-v4-45-B.json`) · 캡처 2장 |
| 굽기 | **1칸**(탐침 · 정착 f350 · 벽시계 807.7680238999892 s) |
| `npx tsc -b` | **통과**(종료 0) |
| pytest(2호기) | **40 passed · 2 xfailed · 1 xpassed**(58.84 s) |
| 캡처 커밋 | `6064f77`(2호기에서 git 경유 · 캡처 규약대로) |
| 브랜치 인쇄 | §0 머리에 에어 · 2호기(§0-5) · §0 은 집행 **«전»** 커밋(`abcdfd4`) |
| ecc 에이전트 / 스킬 | **0 / 0** |

## §4 회차 갈래 — **A**(①(가) + ②③④)

**넘기는 것 7건**

1. ★★★ **브라우저 몸에서 게이트가 통과했다** — `pass: true` · `fails` [] ·
   관통 최대 **0.3308660086558528 mm** · 교차 **0** · 목선 초과비 **0.9889475802550393** ·
   창 순변위 **0.04391290028753105 mm** · pinned 0. 다섯 채널이 v4-37(Node 몸)과 **같은 자리**에 섰다.
2. ★★★ **층3이 «워커 안에서» 처음 섰다** — `BODY_BIN` 을 환경에 준 덕에 칸이 **성공**으로 기록됐고
   `fit-Bwsr_M.json`·`gate-Bwsr_M.json` 이 결과 폴더에 정상 생성됐다(v4-37 의 칸 실패가 여기서 해소).
3. ★★★ **규칙들이 스케일을 자동으로 따라갔다** — `chestY` **131.20635318756104 cm** > `waistY` **103.22462692970475 cm** ·
   둘레 비 **0.963146** 이 v4-44 의 배율 **0.96314478** 과 소수 다섯째 자리까지 같다.
4. ★★ **조립·배치 채널의 차이는 사실로만** — `SLV_R` **56.05492295260141 mm**(v4-37 56.750) ·
   `RAMP_N` **71**(73) · 정착 **f350**(290) · 옷 정점 **9490**(9644).
   옷 규격은 사이즈표에서 오고 몸은 0.963배 작아졌다 ⟹ mm 값이 갈리는 것은 **그 결과**다(판정 채널 아님).
5. ★★ **캡처 2장**(`45-1-front.png`·`45-2-side.png`) — 대조군은 v4-37 의 두 장. **화면 판정 0.**
6. ★★ **108칸 발주 재료 확정** — 목록·경로 규약 · 몸당 SDF 1개(태그 접두 규약) · **한 job = 한 몸(4칸)**
   (`BODY_BIN` 이 워커 환경에서 층3으로 물려가므로) · 규모 **≈25.9시간**(칸당 13.5분 · 이 판 실측에서 유도) ·
   재개는 `.done` 파일 · `PAUSE` 파일 규약 그대로.
7. ★ **정본 등재 완료** — 그리드 정본 = **브라우저 27칸**(커밋 `80944b2`) · Node 몸 = **참조** ·
   둘의 관계는 `0.96314478` 배 + y **−3.9609 mm**(잔차 0.0002 mm).

**이 판이 하지 않은 판단** — 화면 · 108칸 발주 시점과 순서 · 던짐 칸의 최종 집계 처분.
