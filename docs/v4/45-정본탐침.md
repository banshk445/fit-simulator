# v4-45 — 그리드 정본 선언(브라우저 27칸) · 기본 칸 재굽기 · 108칸 발주 재료 확정

**물리 0줄 · `src/` diff 0 · 굽기 = 탐침 1칸 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-45-canon-probe` |
| 기점 | **`f9a4060`**(= v4-44 §1~§4 + 볼트 + 보고) |
| §0 커밋 | (아래 §0-5 에 자기 기입) |
| §1~§4 커밋 | (집행 후 기입) |
| 보고 파일 | `docs/v4/보고/45.md` |

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v4-45-canon-probe f9a4060 → Switched to a new branch 'v4-45-canon-probe'
       $ git branch --show-current → v4-45-canon-probe · HEAD f9a4060
 2호기 (§0 커밋을 push 한 «뒤» 같은 자리로 전환 — §0-5 에 인쇄)
```

---

## §0 수령·사무

### §0-1 판정문 (전략 세션 · v4-44 §4 수령 · 원문)

```
 「갈래 C 수령 · ★★★ 교차차 정체 = unitScale 0.963145(rawHeight 1.765052→1.700000 · 콘솔 실측과 동일 수)
   + y 이동 · 정합 잔차 0.0002mm = 같은 몸 ·
   ★★ 정본 결정: 그리드 정본 = 브라우저 27칸(키 계약을 맞추는 좌표) · Node 기준 몸 = 참조로 강등 ·
     선택지 ㉠ 채택(㉢ 역정합 기각 — 정본을 변환 위에 안 세운다) ⟹
     기본 칸 1개 브라우저 몸 재굽기로 게이트·층3·화면 재확인
     (규칙들은 몸에서 유도되므로 스케일 자동 적응 — 그 확인이 이 판) ·
   ★ 던짐 4칸 = 정상 갈래 D(착용불가 부류) 수용 · 108칸 집계는 T포즈 규약대로」
```

### §0-2 바꾸지 않는 것

```
 물리 **0줄** · `src/` **0줄** · 워커 굽기 경로 **diff 0** · 정본·blob·문턱 **0** · 게이트 채널·문턱 **0**
```

### §0-3 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · 단위 명시)

**★ 단위** — 게이트·리포트는 **mm** · 정착은 **프레임** · `RAMP_N` 은 **프레임** · `SLV_R` 은 **mm**.

| 항 | 갈래 |
|---|---|
| **①** 기본 칸 재굽기 | **(가)** 게이트 `pass` + 층3 성립 / **(나)** fail(채널·값 · 처방 0) / **(다)** 조립 실패·발산 |
| **②** 캡처 | 앞·옆 2장 · **화면 판정 0** |
| **③** 108칸 재료 | ①(가) 전제 · **실행 0** · 목록·시간·재개 규약·던짐 예상 부류 |
| **④** 정본 등재 | 볼트에 「그리드 정본 = 브라우저 27칸(sha) · Node 몸 = 참조 · unitScale 사실」 |

**§4** — A = ①(가)+②③④ ⟹ 상신(다음 판 = 108칸 발주) / B = ①(나)(다) ⟹ 값·캡처 상신 / C = 그 외 ⟹ 정지.

### §0-4 사전 결정 4건 (값 보기 «전» · CC 집행 설계)

```
 ㄱ **축·피벗은 그 칸의 json 에서만 읽는다**(손 상수 0) —
    `ARM_AXIS_JSON=grid27/l3ap-body-c100-h170-s45-a35.json` · `ARM_ORIGIN_JSON=grid27/l3ap-origin-….json`.
    v4-37 이 쓴 Node 판 json 을 **섞지 않는다**(몸이 다르면 축도 그 몸 것이어야 한다).
 ㄴ **태그를 가른다** — 이 판의 조립·굽기 태그는 `Bwsr_M`(브라우저 몸)이다.
    v4-37 의 `Aaxi_M`(Node 몸) 산출물은 **덮지 않는다**(대조가 남게).
 ㄷ **비교는 «나란히»** — v4-37 값(노트에서 읽어 옮긴다 · 함정 44)과 이 판 값을 같은 표에 둔다.
    **스케일 차 때문에 mm 값이 달라지는 것은 사실로만** 적고, 판정은 **게이트 5채널과 대역 이름**으로 한다.
 ㄹ **③은 ①(가)일 때만 · 실행 0** — 발주 목록·시간·재개 규약까지만 적고 굽기는 다음 판이 한다.
```

### §0-5 커밋·전환 인쇄 (집행 «전»)

(§0 커밋 sha 자기 기입 · 2호기 전환 인쇄는 아래에 붙인다)
