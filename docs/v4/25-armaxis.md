# v4-25 — armAxis 배선(예외 승인분) · A포즈 재탐침 · 화면 대조

**물리 0줄 · `src/` diff = `dressRun.ts` **배선 한정**(예외 대장 2건째) · 정본·blob·문턱 0 · 워커 3경로 diff 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-25-armaxis` |
| 기점 | **`f860af3`**(= v4-24 HEAD) |
| §0 커밋 | (집행 «전» · 이 커밋) |
| §1~§4 커밋 | (집행 후 기입) |
| 보고 파일 | `docs/v4/보고/25.md` |

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v4-25-armaxis f860af3 → Switched to a new branch 'v4-25-armaxis'
       $ git branch --show-current → v4-25-armaxis · HEAD f860af3
 2호기 (§0 커밋을 push 한 «뒤» 같은 자리로 전환 — v4-21~24 와 같은 순서 · §0-5 에 인쇄)
```

---

## §0 수령·사무

### §0-1 판정문 (전략 세션 · v4-24 §4 + 화면 수령 · 원문)

```
 「갈래 C 수령 · ★★★ A포즈 1칸 게이트 통과 = A포즈 첫 정본 성립 ·
   계기 사고 처분 수용(BODY_BIN 명시 · 「다르면」 검사 · 함정 후보 등재) · 리포트 5행 던짐 = 정직 ·
   A포즈 대역 재도출은 별도 판(그리드 몸·armAxis 뒤) · 겨드랑이 채널 성립 ·
   ★ armAxis 예외 «승인»(dressRun.ts 한정 · 물리 0줄 · T포즈 82칸 조립 비트 동일 조건) ·
   ★ 그리드 몸 = ㉡ 브라우저 경로 채택(추출 기각 · v3-70 존중) ·
   ★★ 화면 판정 = 합격(옷이다) · 관찰 3건 등재:
     ① 가슴 양쪽 뾰족 텐트(암홀 앞 · T포즈 축 잔재 유력 후보 · 단정 0)
     ② 옆 밑단 앞뒤 A라인 벌어짐
     ③ 앞 목선 V 내려앉음(초과비 통과 = 자리 잡기)
   — 셋 다 armAxis 배선 뒤 같은 칸 재굽기로 자연 실험」
```

### §0-2 바꾸지 않는 것 (회차 프롬프트 원문)

```
 물리 **0줄** · 정본·blob·문턱 **0** · 워커 **3경로 diff 0** · 계기 **v4-24 상태** ·
 `garmentScene.ts` 는 **v4-19 상태 유지** · `src/` diff 는 `dressRun.ts` **배선 한정**.
```

### §0-3 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · 단위 명시)

**★ 단위** — ①은 **칸**(T포즈 조립 82칸) · ②는 **칸 1개** · 간격·관통 **mm** · 프레임 **1/60 s**.

| 항 | 갈래 |
|---|---|
| **①** armAxis 배선 + 회귀 | **(가)** 82/82 비트 동일 · 패턴해시 동일 / **(나)** 불일치 ⟹ **즉시 복원 · diff 0 · 값 상신** |
| **②** A포즈 재탐침(armAxis = 실측 팔 축) | **(가)** 게이트 `pass` / **(나)** fail(채널·값) / **(다)** 조립 실패(사유) |
| **③** 캡처 | 같은 프리셋 앞·옆 2장 → `docs/v4/캡처/25-1-front.png` · `25-2-side.png` · **화면 판정 0** |

**§4** — A = ①(가)+②(가) / B = ②(나)(다) ⟹ 값 상신 / **C = ①(나) ⟹ 복원·상신** / D = 그 외 ⟹ 정지.

### §0-4 사전 결정 4건 (값 보기 «전» · CC 집행 설계)

```
 ㄱ **「기본값 = 몸에서 읽은 실제 축」은 «호출부가 넘기는 값»의 규약으로 읽는다.**
    `dressRun.prepare` 의 인자 «기본값»을 실측 축으로 바꾸면 T포즈 조립이 달라져
    **①의 조건(82/82 비트 동일)과 정면 충돌**한다(T포즈 실측 팔 축은 (1,0,0) 이 «아니다» — 값은 §1-①에 적는다).
    ⟹ 배선은 **넘기지 않으면 지금 그대로**(= `garmentScene.ts:635` 의 +x)이고,
       **A포즈 호출부가 «몸에서 읽은» 축을 넘긴다**(손 상수 0). 이 해석을 여기 등재한다.
 ㄴ **회귀는 «같은 기계에서 전/후»로 잰다** — v4-23 이 세운 원리 「정본은 기계 좌표를 갖는다」대로,
    배선 «전» 해시를 이 판에서 직접 뜨고(2호기) 배선 «후»와 대조한다(옛 기록과 대조하지 않는다).
 ㄷ **②의 축 출처** — `l3ap-body-c100-h170-s45-a35.json` 의 `팔축_후`(뼈에서 읽은 실측 방향).
    새 수를 만들지 않고 **그 파일에서 읽어 넘긴다**(함정 44).
 ㄹ **소비자 전부에 같은 축을 넘긴다** — 조립 export · 겨드랑이 계기 · 렌더 · 게이트는 모두
    `prepare()` 로 장면을 다시 세우므로, **축이 다르면 정점 수·패널이 갈린다**.
    ⟹ 축 인자를 **한 곳(`scripts/armAxisEnv.ts`)에서 읽어** 네 스크립트가 같이 쓴다(계기 식 0줄).
```
