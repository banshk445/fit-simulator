# v4-01 — GPU 이식 환경 확정 · 정답지 로더 · 한 정점 낙하

**새 트랙 v4(Python · Taichi) · v3 코드 diff 0 · 굽기 0**

| | |
|---|---|
| 브랜치 | `v4-01-env` |
| 기점 | **`0369f8e`** (= v3-92 HEAD) |
| §0 커밋 | **집행 «전» 별도 커밋** — sha 는 커밋 직후 한 줄로 채운다 |
| 호출 ecc 에이전트/스킬 | **0** |

---

## §0 수령 (집행 «전» 별도 커밋)

### §0-1 판정문 (전략 세션 · v3-92 §4 수령 · 원문)

```
 「갈래 A 수령 · **제품 v1 화면 출시 후보 완결** ·
   **v3 동결**(물리·조립·게이트·정본 diff 0 · **정답지 108칸**) ·
   **v4 = Python + Taichi** · **개발 = 에어 M4(Metal)** · **정본 굽기 = 2호기(CUDA)** · **float32** ·
   「같은 옷」 기준은 **v4-02 이후 실측에서 유도**(지금 등재 0) · 조명·핏 색 계단 = **렌더 백로그**」
```

### §0-2 바꾸지 않는 것

```
 `src/` **전부 diff 0** · `public/v3diag` 정본·blob **0** · 문턱 **0**
```

### §0-3 구조

```
 `gpu/`(pyproject · `gpu/engine` · `gpu/oracle` · `gpu/tests`) · `docs/v4/` ·
 볼트 `v4-노트-색인.md` **신설** · `CLAUDE.md` 에 **v4 절**(환경·정답지·규약은 v3 와 동일)
```

### §0-4 사전 등재 기대

| # | 기대 |
|---|---|
| **①** | 2호기(GPU·VRAM·드라이버·CUDA·Python·Taichi·`ti.init(arch=cuda)`+벤치) · 에어(칩·GPU 코어·Taichi·`ti.init(arch=metal)`+같은 벤치) · **두 기계 표**. 벤치 = **1e7 원소 벡터 덧셈** |
| **②** | 기본몸 M 정본(`c100-h170-s45_M` · 상태 sha `9d3b07e4` · 정점 **9388**) — 정점 9388 · **sha 재계산 일치** · 몸 높이 **1.7000 ± 0.0020** · 옷 bbox 가 v3 화면 등재값과 일치 · **27몸 sha 27/27** · 108 옷 정점 수 전량 표 |
| **③** | v3 의 중력·dt·서브스텝을 **코드 사실로 인용**(손으로 적지 않는다) · 한 정점 N 스텝 낙하 ↔ `y = y0 − ½g t²` · CUDA·Metal 각각 · **\|오차\| ≤ N × 4·ULP_f32(\|y\|)**(누적 반올림 상한 · 손 상수 0) · 두 백엔드 차이도 같은 상한 안 |
| **④** | `gpu/tests` 에 ②③ 을 **pytest** 로 · `CLAUDE.md` v4 절(사전 등재·갈래·복원·귀책 **동일** · 정본 기계 = **2호기** · 정본 파일 접두 **`v4-`** · **「같은 옷」 기준 미등재** 명시) |

**① 사전 등재 갈래** — (가) **둘 다 성공** / (나) **한쪽만**(어느 쪽·오류 원문) / (다) 둘 다 실패

### §0-5 갈래 (집행 «전» 등재)

| 갈래 | 조건 | 처분 |
|---|---|---|
| **A** | ①~④ 전량 | 상신 |
| B | ① **(나)(다)** | 환경 사실만 상신 · ②④ 집행 · ③ 은 **되는 백엔드로만** |
| C | ② 정본 로드 **불일치** | 값 상신 · **정지** |
| D | 그 외 | 정지 |

**사후 갈래 신설 0.**

### §0-6 착수 «전» 알려진 제약 — 사실

```
 이 CC 세션이 도는 기계는 **`banseunghyeog-ui-MacBookAir.local`** — **에어 M4** 다(개발 기계).
 **2호기(CUDA · `DESKTOP-OV72A46` · win32)는 이 세션에서 «닿지 않는다»** ⟹
 ① 의 2호기 항은 **이 판에서 측정할 수 없다**. 갈래 (나)가 «환경 탓»으로 미리 서 있는 셈이고,
 그 사실을 **집행 전에** 적어 둔다(사후 변명이 되지 않게).
```
