# v4-01 — GPU 이식 환경 확정 · 정답지 로더 · 한 정점 낙하

**새 트랙 v4(Python · Taichi) · v3 코드 diff 0 · 굽기 0**

| | |
|---|---|
| 브랜치 | `v4-01-env` |
| 기점 | **`0369f8e`** (= v3-92 HEAD) |
| §0 커밋 | **`80fce50`** — 집행 «전» 별도 커밋(sha 만 그 뒤 한 줄로 채웠다) |
| 호출 ecc 에이전트/스킬 | **0** |

---

## §0 수령 (집행 «전» 별도 커밋)

### §0-1 판정문 (전략 세션 · v3-92 §4 수령 · 원문)

```
 「갈래 A 수령 · **제품 v1 화면 출시 후보 완결** ·
   **v3 동결**(물리·조립·게이트·정본 diff 0 · **정답지 108칸**) ·
   **v4 = Python + Taichi** · **개발 = 에어 M4(Metal)** · **정본 굽기 = 2호기(CUDA)** · **float32** ·
   「같은 옷」 기준은 **v4-02 이후 실측에서 유도**(지금 등재 0) · 조명·핏 색 계단 = **렌더 백로그**」
```

### §0-2 바꾸지 않는 것

```
 `src/` **전부 diff 0** · `public/v3diag` 정본·blob **0** · 문턱 **0**
```

### §0-3 구조

```
 `gpu/`(pyproject · `gpu/engine` · `gpu/oracle` · `gpu/tests`) · `docs/v4/` ·
 볼트 `v4-노트-색인.md` **신설** · `CLAUDE.md` 에 **v4 절**(환경·정답지·규약은 v3 와 동일)
```

### §0-4 사전 등재 기대

| # | 기대 |
|---|---|
| **①** | 2호기(GPU·VRAM·드라이버·CUDA·Python·Taichi·`ti.init(arch=cuda)`+벤치) · 에어(칩·GPU 코어·Taichi·`ti.init(arch=metal)`+같은 벤치) · **두 기계 표**. 벤치 = **1e7 원소 벡터 덧셈** |
| **②** | 기본몸 M 정본(`c100-h170-s45_M` · 상태 sha `9d3b07e4` · 정점 **9388**) — 정점 9388 · **sha 재계산 일치** · 몸 높이 **1.7000 ± 0.0020** · 옷 bbox 가 v3 화면 등재값과 일치 · **27몸 sha 27/27** · 108 옷 정점 수 전량 표 |
| **③** | v3 의 중력·dt·서브스텝을 **코드 사실로 인용**(손으로 적지 않는다) · 한 정점 N 스텝 낙하 ↔ `y = y0 − ½g t²` · CUDA·Metal 각각 · **\|오차\| ≤ N × 4·ULP_f32(\|y\|)**(누적 반올림 상한 · 손 상수 0) · 두 백엔드 차이도 같은 상한 안 |
| **④** | `gpu/tests` 에 ②③ 을 **pytest** 로 · `CLAUDE.md` v4 절(사전 등재·갈래·복원·귀책 **동일** · 정본 기계 = **2호기** · 정본 파일 접두 **`v4-`** · **「같은 옷」 기준 미등재** 명시) |

**① 사전 등재 갈래** — (가) **둘 다 성공** / (나) **한쪽만**(어느 쪽·오류 원문) / (다) 둘 다 실패

### §0-5 갈래 (집행 «전» 등재)

| 갈래 | 조건 | 처분 |
|---|---|---|
| **A** | ①~④ 전량 | 상신 |
| B | ① **(나)(다)** | 환경 사실만 상신 · ②④ 집행 · ③ 은 **되는 백엔드로만** |
| C | ② 정본 로드 **불일치** | 값 상신 · **정지** |
| D | 그 외 | 정지 |

**사후 갈래 신설 0.**

### §0-6 착수 «전» 알려진 제약 — 사실

```
 이 CC 세션이 도는 기계는 **`banseunghyeog-ui-MacBookAir.local`** — **에어 M4** 다(개발 기계).
 **2호기(CUDA · `DESKTOP-OV72A46` · win32)는 이 세션에서 «닿지 않는다»** ⟹
 ① 의 2호기 항은 **이 판에서 측정할 수 없다**. 갈래 (나)가 «환경 탓»으로 미리 서 있는 셈이고,
 그 사실을 **집행 전에** 적어 둔다(사후 변명이 되지 않게).
```

---

## §1′ 집행

### §1-①′ 환경 — **갈래 (나) 한쪽만**(2호기 미도달)

| 항목 | 에어 M4(개발) | 2호기(정본) |
|---|---|---|
| 기계 | `banseunghyeog-ui-MacBookAir.local` | `DESKTOP-OV72A46`(win32-x64) |
| OS | macOS 26.5.2 · arm64 | — |
| 칩 / GPU | **Apple M4** · GPU **8코어** | — |
| Python | **3.13.9** | — |
| Taichi | **1.7.4**(llvm 15.0.7 · commit b4b956fd) | — |
| `ti.init(arch=metal)` | **성공** — 실제 arch `Arch.metal` | — |
| `ti.init(arch=cuda)` | **폴백** — 실제 arch **`Arch.arm64`** | — |
| 벤치 1e7 벡터 덧셈 | **metal 2.203 ms** · cpu(arm64) 1.324 ms | — |

```
 ⟹ **(나)**. 2호기 항이 «미측정»인 이유는 **환경**이다 — 이 CC 세션은 에어에서 돌고
   2호기(CUDA · win32)에 닿지 않는다. **§0-6 에 집행 «전»에 적어 뒀다**(사후 변명 아님).
 §0-5 **B** 대로 「환경 사실만 상신 · ②④ 집행 · ③ 은 되는 백엔드로만」을 따랐다.
```

```
 ★★ **Taichi 함정 1건 — 값으로 잡았다**: `ti.init(arch=ti.cuda)` 는 CUDA 가 없어도
   **예외를 던지지 않는다**. 처음 측정에서 「성공」으로 찍혔고, `ti.lang.impl.current_cfg().arch`
   를 확인하자 실제로는 **`Arch.arm64` 로 폴백**해 있었다. **#121 「조용한 항등 폴백」의 이웃**이고
   `CLAUDE.md` v4 절에 등재했다. 이 확인이 없었으면 **「2호기 없이 CUDA 성공」이라는 거짓 사실**이
   등재될 뻔했다.
 ★ 함정 2건째 — `from __future__ import annotations` 를 켜면 커널 인자 주석이 «문자열»이 돼
   `TaichiSyntaxError` 가 난다(실측). v4 모듈에서 쓰지 않는다.
```

### §1-②′ 정답지 로더 — **전량 일치**

### ② 기본몸 M 정본 대조

| 항목 | 실측 | 기대 | 판정 |
|---|---|---|---|
| 정점 수 | 9388 | 9388 | ✅ |
| 상태 sha(앞16) | 9d3b07e45b67e0c3 | 9d3b07e45b67e0c3 | ✅ |
| 인덱스 sha 일치 | True | True | ✅ |
| 몸 높이(m) | 1.7 | 1.7000 ± 0.0020 | ✅ |
| 정착 프레임 | 180 | 180 | ✅ |
| 격자 d(mm) | 9.0 | 9.0 | ✅ |

옷 bbox — x -0.3560~0.3612 · y 0.8066~1.4496 · z -0.1106~0.1555 (m)
화면 등재 대조 — 가슴 y **129.97cm** ↔ 옷 y 최대 144.96cm · 밑단 y 최소 80.66cm


```
 로더는 v3 산출물을 **읽기만** 한다(`gpu/oracle/load.py` · 쓰기 0). 포맷은 v3 코드에서 온 사실이다:
   몸 blob = float32 (x,y,z) 나열 · 옷 blob = [uint32 헤더길이][헤더 JSON][float64 페이로드] ·
   **상태 sha = 헤더를 «뺀» 페이로드의 sha256**(v3-49 규칙 · `V3Product.tsx` 대조와 같은 정의)
 상태 길이 **56328 = 9388 × 6** ⟹ 위치 3n + 속도 3n(`stateBlob` 순서 확인)
 **27몸 로드 27/27** · sha 27개 산출 · 제공 목록 **35** 확인
 108칸 중 blob 이 있는 칸 **39**(= 편입 36 + α) · 부재 **69칸** — 조립·게이트 이전에 죽어
   blob 자체가 없다(정본 분류와 정합 · 결손 아님).
```

### §1-③′ 한 정점 낙하 — **두 백엔드 차이 0**

**v3 상수 — 손으로 적지 않고 «읽었다»**(`gpu/engine/fall.py:v3_consts` 가 `src/v3/consts.ts` 를 파싱)

| 자리 | 원문 |
|---|---|
| `src/v3/consts.ts:3` | `export const G = 9.81;` |
| `src/v3/consts.ts:4` | `export const DT = 1 / 60;` |
| `src/v3/solver.ts:1050` | `const h = p.dt / p.substeps;` |
| `src/v3/solver.ts:1061` | `s.vel[o + 1] -= p.gravity * h;` |
| `src/v3/solver.ts:1069` | `s.pos[o + 1] += s.vel[o + 1] * h;` |

⟹ **semi-implicit(symplectic) Euler**. N = 600 · 서브스텝 8 ⟹ h = 0.002083333s

| 백엔드 | y(f32) | v(f32) | 이산해 대비 | 연속해 대비 | N×4·ULP 상한 | 판정 |
|---|---|---|---|---|---|---|
| **metal** | −6.676847458 | −12.262486 | **−1.152e-05** | −1.278e-02 | 1.144e-03 | **통과** |
| cpu(arm64) | −6.676847458 | −12.262486 | **−1.152e-05** | −1.278e-02 | 1.144e-03 | **통과** |

```
 정확 이산해 y = −6.676835937 · v = −12.262500  |  연속 해석해 y = −6.664062500
 ★ **연속해와의 차 −0.012773437m 은 «반올림»이 아니라 적분 방식 자체의 편차**다
   (= −g·h²·N/2 로 «정확히» 설명된다 — 값으로 확인). 그래서 ULP 상한은 **이산해 대비**로 잰다.
   발주가 「해석해와 대조」라 했으므로 **둘 다 적고 어느 쪽에 상한이 걸리는지 밝힌다**.
 ★ **두 백엔드 차이 = 0.000e+00** — 상한 1.144e-03 안(비트까지 같다).
```

### §1-④′ 규약 이관

```
 `gpu/tests/test_oracle.py` — **pytest 7/7 통과**(정답지 6항 + 낙하 2 백엔드 + 백엔드 일치)
 `CLAUDE.md` **v4 트랙** 절 — 개발=에어 M4 · **정본 기계 = 2호기** · float32 · 정답지 108칸 ·
   정본 접두 **`v4-`** · **규약은 v3 와 동일**(사전 등재·갈래·복원·귀책·문턱 인용) ·
   ★ **「같은 옷」 기준 미등재**(v4-02 이후 실측 유도) 명시 · Taichi 함정 2건 등재
 `.obsidian-log/v4-노트-색인.md` **신설**
```

---

## §2′ 자기검사

| # | 항목 | 결과 |
|---|---|---|
| ㉠ | ①②③ 표 | 위 §1′ |
| ㉡ | pytest | **7/7 통과** |
| ㉢ | `src/` diff | **0** |
| ㉣ | 정본·blob diff | **0** |
| ㉤ | `tsc -b` | 통과 |
| ㉥ | v3-92 화면 회귀 | **변경 0건**(`src/` diff 0 이 곧 증명) |

**§3 캡처 — 0장**(사전 등재 그대로).

---

## §4′ 판정 — **갈래 B**

```
 §0-5 **B = ① (나) ⟹ 환경 사실만 상신 · ②④ 집행 · ③ 은 되는 백엔드로만**.
 ① 이 (나)(2호기 미도달)이므로 **B** 다. ②③④ 는 **전량 집행**했고
 ③ 은 metal · cpu(arm64) 두 백엔드로만 돌렸다(CUDA 는 이 기계에 없다).
 사후 갈래 신설 0 · 문턱 0 · v3 diff 0.
```

### §4-1 넘기는 것

| # | 항목 | 몫 |
|---|---|---|
| **㉠** | **2호기 환경 측정** — 이 세션에서 못 닿는다. 2호기에서 `gpu/env_probe.py` 를 돌리거나, 백로그 9(원격 굽기 · Tailscale+SSH)를 먼저 세워야 한다 | 사용자 · 전략 세션 |
| ㉡ | **Taichi 함정 2건 등재**(조용한 arch 폴백 · future annotations) — 함정 대장에 번호를 줄지 | 전략 세션 |
| ㉢ | 낙하가 **두 백엔드에서 비트까지 같다** — 이식의 첫 기준선. 다음은 «여러 정점 + 제약» | 전략 세션 |
| ㉣ | blob 부재 **69칸** — 정답지로 쓸 수 있는 칸은 **39칸**뿐이다(나머지는 조립·게이트 이전에 죽었다) | 전략 세션 |
