# v4-14 — 셀프 충돌 이식(마지막 커널) · 층1 · 정의역 완성

**집행 기계 = 2호기(CUDA · 정본 기계) · `src/` diff 0 · 굽기 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-14-selfcol` |
| 기점 | **`3141055`** (= v4-13 HEAD) |
| 워크트리 | `C:\Users\banba\fit-simulator` |
| 기계 | **`DESKTOP-OV72A46`** (win32 · CUDA · GTX 1660 Ti · Taichi 1.7.4 · llvm 15.0.1 · py 3.12.10) |
| §0 커밋 | 집행 «전» 별도 커밋(이 파일의 §0 + §0-2 사무 2건만) |
| §1~§4 커밋 | (집행 후 자기 기입) |
| 호출 ecc 에이전트/스킬 | (집행 후 기입) |

---

## §0 수령 (집행 «전» 별도 커밋)

### §0-1 판정문 (전략 세션 · v4-13 §4 수령 · 원문)

```
 「갈래 A 수령 · ★ ①(가) — f64 × 병렬 CUDA 0.355 s/프레임 · 감소 유지 · v3 대비 26.4배 ·
   두 arch 4.0e-08 mm 일치 · ★ ② v4 가 정착 자리에 처음 섰다(0.0854 ≤ 0.1) ·
   v3 와 같은 f=80 에서 만난다 ·
   ★ ③ 읽기 = ㉠(나) «확정»(전략 세션 몫) — 근거: 제품 문턱은 소비자가 보는 것에서 왔고
   화면에 나오는 것은 대역 «이름»과 표시 mm 이지 정점 소속 집합이 아니며, 게이트는 pass 한
   비트로 나타난다 · 정점 소속·fails 집합은 «진단»이지 채널이 아니다 ⟹ 대역 5/5 · pass 일치 ·
   표시값 3/5 = (나) · 「같은 옷」 미선언 · 문턱 이동 0(불일치 2행은 반올림 경계 0.027mm ·
   색 순서 갈림 0.0122mm 와 같은 자릿수 = 병렬화의 이미 수용한 대가) ·
   ★ ③a 관통 v4 0.6599 > 0.5 는 셀프 충돌 «없는» 정의역의 값이라 지금 귀속 0 ·
   이식 존속 = 계속 · v4-12 grid 표기 어긋남은 재실측값 병기로 닫는다(노트 수정 0)」
```

### §0-2 사무 (이 커밋에서 «함께» 집행 · 2건)

```
 ① `docs/metrics-log.md` 의 v4-12 grid 표(「2~209」) «아래»에 v4-13 재실측값을 **병기**했다 —
    1~209 · grid>1 26종. **표 값 수정 0 · v4-12 노트 수정 0**(판정문 「재실측값 병기로 닫는다」).
 ② `.obsidian-log/전략세션-인수인계.md` 의 v4 블록 「넘기는 것 ㉣」을 **미결 등재**로 바꿨다 —
    ③a 관통 초과(v4 0.6599 > 0.5)는 **셀프 충돌 «없는» 정의역의 값이라 지금 귀속 0** ·
    이 판(이식) «뒤»에 다시 본다. 원문 값 삭제 0.
```

### §0-3 바꾸지 않는 것 (회차 프롬프트 원문)

```
 `src/` · 정본 · blob · v3 동결 · `engine/` **기존 8파일 diff 0**(셀프 충돌은 «새 파일») ·
 최적화·튜닝 **0** · 문턱 **0**.
 ⟹ 이 판이 만드는 것은 **새 파일**뿐이다(`engine/selfcol.py` · 결합 실행기 · 대조 계기 · v3 덤프 스크립트).
 ★ `full.py` · `full_color.py` · `full_color2.py` · `full_par.py` · `stretch.py` · `bending.py` ·
   `seam.py` · `collide.py` **바이트 불변**. `ulp.py` 도 **수정 0**(f64 자는 새 계기 안에서 만든다).
 ★ v4-02~13 산출물 바이트 불변(이 판 산출은 새 접두 `l3sc-` · `l3full-` 로 판다).
 ★ 108칸 착수 0 · 굽기 0 · 캡처 0.
```

### §0-4 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · **단위 명시** · 함정 43)

**★ 단위** — ①②의 단위는 **1서브스텝**(= 44,920 제약 투영 «없이» 셀프 충돌 «만» 1회 적용).
③의 단위는 **프레임**(1프레임 = 229 서브스텝). 창 순변위의 창 = **10프레임**(N_WIN).

**① 셀프 충돌 이식** — v3 의 셀프 충돌을 **코드 사실로 인용**(파일:줄 · 손 상수 0)해 Taichi 커널
(`engine/selfcol.py` 신설)로 옮긴다. 광역 탐색은 **공간 해시**로 구현하되 v3 와 **같은 쌍 집합**을 내야 한다.

| 갈래 | 조건 (단위 = **1서브스텝**) |
|---|---|
| **(가)** | 층1 통과 — 같은 초기 상태에서 셀프 충돌만 1서브스텝 적용해 **f64 차 ≤ K × ULP_f64** «그리고» **f32 차 ≤ K × ULP_f32** |
| **(나)** | 초과(값) |
| **(다)** | **쌍 집합이 다르다**(개수 · 차집합 크기) |
| **(라)** | 구현 불가(사유) |

**② 쌍 집합 동등성** — v3 광역이 내는 후보쌍 집합 ↔ v4 공간 해시가 내는 집합을 **집합으로** 대조
(개수 · 차집합 **양방향** · 순서 무관). 판정 채널은 **«쌍 집합»이지 발화 수가 아니다**
(v4-05 등재: 이산 계수는 정밀도에 민감).

| 갈래 | 조건 |
|---|---|
| **(가)** | **완전 일치** |
| **(나)** | 차집합 있음(값 · **그 쌍들의 거리 분포**) |

**③ 전 정의역 실행** — 셀프 충돌 포함 결합 솔버(`full_par` + `selfcol`)로 **f64 × 병렬 × CUDA** 를
v3 정본과 **같은 조건**으로 실행. ㄱ s/프레임 ㄴ 창 순변위 궤적 ㄷ 정착 도달 여부
ㄹ **v3 정본 blob 과 좌표차**(중앙·p99·최대) ㅁ 리포트 5행 + 제품 문턱 판정(**③ 읽기 = ㉠ 확정본**)
ㅂ 게이트 5채널 v3 정본 ↔ v4.

| 갈래 | 조건 |
|---|---|
| **(가)** | **정착 도달 + 대역 5/5 + `pass` 일치** |
| **(나)** | 일부(항 명시) |
| **(다)** | **정착 미도달** |
| **(라)** | 발산 |

**④** 최적화·튜닝 **0** · 108칸 착수 **0** · 굽기 **0**.

**§4 회차 갈래** — A = ①②③ 전량 + ①(가) + ②(가) + ③(가)(나) ⟹ 상신 /
B = ②(나) ⟹ 차집합 값 상신 · ③ 집행 / C = ①(나)(다) ⟹ 값 상신 · **③ 착수 0** /
D = ①(라) 또는 ③(다)(라) ⟹ 사유·값 상신 · **정지** / E = 그 외 ⟹ 정지. **사후 갈래 신설 0.**

### §0-5 집행 «전» 사전 결정 9건 (값을 보기 «전»에 적는다)

```
 ㄱ **인용 자리**(손 상수 0 · 이 판이 «옮기는» 것의 정본) —
    `src/v3/solver.ts:261-274` `SelfCollisionParams`(tris · thickness · iterations · every) ·
    `:277` `selfStats`(0 근접 쌍 · 1 해소 횟수 · 2 최대 침투) · `:287-290` `SC_CLAMP 2047` · `scClamp` · `scKey` ·
    `:304-341` `scEmit`(sep 안만 · `d ≤ 1e-12` 버림 · 12칸 기록) ·
    `:359-411` `closestPtTri`(Ericson §5.1.5) · `:417-458` `closestSegSeg`(Ericson §5.1.9) ·
    `:462-489` `scVT` · `:491-503` `scEE` ·
    `:509-573` 광역(**cell = mean(엣지)+2·thickness** `:547` · AABB ±thickness · 버킷 삽입) ·
    `:574-624` 협역(**중복 제거 = 최소 모서리 셀** `:580` · **인접 제외 = 정점 공유** `:595` ·
      AABB 겹침 · 근접 쌍 계수 `:610` · **15특징 = 정점–삼각형 6 + 엣지–엣지 9**) ·
    `:641-670` 해소(**Gauss–Seidel · 간극 재측정** · `depth ≤ 0` 건너뜀 · `denom < 1e-20` 건너뜀 ·
      **λ = depth / Σ w·coef²**) · `:1117-1122` **적용 순서 = 제약 투영 «뒤» · 몸 충돌 «앞»** ·
    `src/v3/dressRun.ts:120` 정본 경로의 인자(`tris` · `thickness = THICK` · `every 1`).
    ★ 이 목록 밖의 수를 커널에 적지 않는다. `iterations`·`every` 는 **정본 기본값 1**을 쓴다.

 ㄴ **① 층1 의 자(K)** — v4-03/05/06 과 «같은 절차»로 **커널 코드에서 «센다»**(실측 보고 고르지 않는다) ·
    세는 규칙도 같다(나눗셈·sqrt 각 1 · 곱·합·차·부호반전 각 1) · **깊이 K_depth 와 총량 K_ops 를 둘 다
    세고 «작은 쪽(깊이)»으로 판정한다**(느슨한 쪽을 골라 통과를 만들지 않는다 · 함정 14).
    자의 «자리 값» = 그 정점 위치의 최대 성분 크기(`ulp.py:layer1` 규약 · deg = 1).
    **ULP_f64 판은 새 계기 안에서 만든다**(`ulp.py` 수정 0 · f32 판과 «같은 식» · 지수만 −52).

 ㄷ **② 「쌍 집합」의 정의** — v3 가 `:610` 에서 세는 **협역 기하까지 간 쌍**(= 인접 제외·AABB 겹침을
    통과한 쌍)이다. 양쪽 다 **(min(i,j), max(i,j)) 로 정규화**해 **정렬된 집합**으로 비교한다
    (순서 무관 · 중복 0). 차집합은 **양방향**으로 세고, 있으면 그 쌍들의 **최근접 거리 분포**를 낸다.

 ㄹ **① 순서 규약**(Gauss–Seidel 은 **순서 의존**이다 · 이 판의 가장 큰 함정) —
    v3 의 접촉 순서 = (셀 «생성» 순서, 버킷 안 삼각형 오름차순 쌍, 특징 15개 고정 순서)다.
    v4 는 **그 순서를 재현**한다. 재현 방법도 코드 사실에서 나온다 — 셀 생성 순서는 삼각형 t 오름차순 ·
    한 t 안에서 (cx,cy,cz) 오름차순이므로 셀마다 **(최소 점유 t, 그 t 의 상자 안 순회 인덱스)** 가 순위다.
    ★ **순서 차이의 크기도 «따로» 잰다** — 같은 커널을 (ㄱ) v3 순서 (ㄴ) 병렬 자연 순서로 각각 돌려
      두 결과의 좌표차를 낸다. 이것은 «판정»이 아니라 **①의 갈래가 무엇에 걸려 있는지를 값으로 남기는 것**이다.

 ㅁ **① 층1 시험계** — 초기 상태 = 정본 blob `settled-c100-h170-s45_M.bin` 의 **f64 원본**(캐스팅 0) ·
    적용 = **셀프 충돌만 1회**(예측 0 · 다른 제약 0 · 몸 충돌 0 · 속도 갱신 0) · v3 쪽은
    `resolveSelfCollisions` **한 번**만 부르는 신설 스크립트로 낸다(물리 경로 인용 · `src/` 0줄).

 ㅂ **③ 「같은 조건」** — 프레임 수는 **정본 blob 헤더의 `frame` 필드에서 읽는다**(손 상수 0).
    문턱 도달 판정은 v4-13 과 같은 식(창 순변위 ≤ `tol`) · `tol` 도 v4-13 과 같은 자리에서 읽는다.
    **둘 중 먼저 오는 쪽에서 멈춘다**(정착 도달 또는 헤더 프레임 수) — 미도달이면 그 사실과 최종 net 등재.

 ㅅ **③ arch·예산 규약**(v4-12 §0-4ㄴ 와 같은 형태 · 값 보기 «전») — **1프레임을 먼저 재고**
    목표 프레임 수의 벽시계를 추정한다. 추정이 **2시간**을 넘으면 **프레임 수를 40으로 자르고**
    그 사실을 등재한다(자른 실행도 ㄱㄴㄷㄹ 은 낸다 · ㅁㅂ 은 그 자리 상태로 낸다).
    ★ arch 는 **CUDA 고정**(회차 프롬프트 ③ 문언) · 폴백이면 `current_cfg().arch` 를 그대로 인쇄해 등재.

 ㅇ **③ 판정 읽기** — 판정문이 확정한 **㉠** 을 쓴다: 「대역 = 대역 «이름»」 · 「게이트 판정 = `pass` 값」.
    정점 대역 소속·`fails` 집합·진단 2자리는 **진단으로 병기**하되 갈래 판정에 걸지 않는다.

 ㅈ **정지** — ①(나)(다)면 ③ 착수 0(갈래 C) · ①(라) 또는 ③(다)(라)면 정지(갈래 D) ·
    사후 갈래 신설 0 · 처방 0 · 최적화 0 · 「같은 옷」 선언은 ③(가)일 때만 하고 그 밖에는 하지 않는다.
```

### §0-6 예산 · 실행 규약

```
 ① 층1 = 1서브스텝(양쪽 다 초 단위) · ② 쌍 집합 = 같은 실행의 부산물(추가 실행 0) ·
 ③ = 헤더 프레임 수(정본 blob = 180) 또는 정착 도달 · **상한은 §0-5ㅅ 규약으로 자른다**.
 ★ **GPU 동시 실행 0** · 전부 배경 실행 + 로그 파일 · 캡처 0(§3).
 ★ 이 판은 «마지막 커널»이다 — 이식의 정의역이 여기서 v3 와 같아진다.
```
