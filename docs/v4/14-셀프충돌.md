# v4-14 — 셀프 충돌 이식(마지막 커널) · 층1 · 정의역 완성

**집행 기계 = 2호기(CUDA · 정본 기계) · `src/` diff 0 · 굽기 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-14-selfcol` |
| 기점 | **`3141055`** (= v4-13 HEAD) |
| 워크트리 | `C:\Users\banba\fit-simulator` |
| 기계 | **`DESKTOP-OV72A46`** (win32 · CUDA · GTX 1660 Ti · Taichi 1.7.4 · llvm 15.0.1 · py 3.12.10) |
| §0 커밋 | 집행 «전» 별도 커밋(이 파일의 §0 + §0-2 사무 2건만) |
| §1~§4 커밋 | (집행 후 자기 기입) |
| 호출 ecc 에이전트/스킬 | (집행 후 기입) |

---

## §0 수령 (집행 «전» 별도 커밋)

### §0-1 판정문 (전략 세션 · v4-13 §4 수령 · 원문)

```
 「갈래 A 수령 · ★ ①(가) — f64 × 병렬 CUDA 0.355 s/프레임 · 감소 유지 · v3 대비 26.4배 ·
   두 arch 4.0e-08 mm 일치 · ★ ② v4 가 정착 자리에 처음 섰다(0.0854 ≤ 0.1) ·
   v3 와 같은 f=80 에서 만난다 ·
   ★ ③ 읽기 = ㉠(나) «확정»(전략 세션 몫) — 근거: 제품 문턱은 소비자가 보는 것에서 왔고
   화면에 나오는 것은 대역 «이름»과 표시 mm 이지 정점 소속 집합이 아니며, 게이트는 pass 한
   비트로 나타난다 · 정점 소속·fails 집합은 «진단»이지 채널이 아니다 ⟹ 대역 5/5 · pass 일치 ·
   표시값 3/5 = (나) · 「같은 옷」 미선언 · 문턱 이동 0(불일치 2행은 반올림 경계 0.027mm ·
   색 순서 갈림 0.0122mm 와 같은 자릿수 = 병렬화의 이미 수용한 대가) ·
   ★ ③a 관통 v4 0.6599 > 0.5 는 셀프 충돌 «없는» 정의역의 값이라 지금 귀속 0 ·
   이식 존속 = 계속 · v4-12 grid 표기 어긋남은 재실측값 병기로 닫는다(노트 수정 0)」
```

### §0-2 사무 (이 커밋에서 «함께» 집행 · 2건)

```
 ① `docs/metrics-log.md` 의 v4-12 grid 표(「2~209」) «아래»에 v4-13 재실측값을 **병기**했다 —
    1~209 · grid>1 26종. **표 값 수정 0 · v4-12 노트 수정 0**(판정문 「재실측값 병기로 닫는다」).
 ② `.obsidian-log/전략세션-인수인계.md` 의 v4 블록 「넘기는 것 ㉣」을 **미결 등재**로 바꿨다 —
    ③a 관통 초과(v4 0.6599 > 0.5)는 **셀프 충돌 «없는» 정의역의 값이라 지금 귀속 0** ·
    이 판(이식) «뒤»에 다시 본다. 원문 값 삭제 0.
```

### §0-3 바꾸지 않는 것 (회차 프롬프트 원문)

```
 `src/` · 정본 · blob · v3 동결 · `engine/` **기존 8파일 diff 0**(셀프 충돌은 «새 파일») ·
 최적화·튜닝 **0** · 문턱 **0**.
 ⟹ 이 판이 만드는 것은 **새 파일**뿐이다(`engine/selfcol.py` · 결합 실행기 · 대조 계기 · v3 덤프 스크립트).
 ★ `full.py` · `full_color.py` · `full_color2.py` · `full_par.py` · `stretch.py` · `bending.py` ·
   `seam.py` · `collide.py` **바이트 불변**. `ulp.py` 도 **수정 0**(f64 자는 새 계기 안에서 만든다).
 ★ v4-02~13 산출물 바이트 불변(이 판 산출은 새 접두 `l3sc-` · `l3full-` 로 판다).
 ★ 108칸 착수 0 · 굽기 0 · 캡처 0.
```

### §0-4 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · **단위 명시** · 함정 43)

**★ 단위** — ①②의 단위는 **1서브스텝**(= 44,920 제약 투영 «없이» 셀프 충돌 «만» 1회 적용).
③의 단위는 **프레임**(1프레임 = 229 서브스텝). 창 순변위의 창 = **10프레임**(N_WIN).

**① 셀프 충돌 이식** — v3 의 셀프 충돌을 **코드 사실로 인용**(파일:줄 · 손 상수 0)해 Taichi 커널
(`engine/selfcol.py` 신설)로 옮긴다. 광역 탐색은 **공간 해시**로 구현하되 v3 와 **같은 쌍 집합**을 내야 한다.

| 갈래 | 조건 (단위 = **1서브스텝**) |
|---|---|
| **(가)** | 층1 통과 — 같은 초기 상태에서 셀프 충돌만 1서브스텝 적용해 **f64 차 ≤ K × ULP_f64** «그리고» **f32 차 ≤ K × ULP_f32** |
| **(나)** | 초과(값) |
| **(다)** | **쌍 집합이 다르다**(개수 · 차집합 크기) |
| **(라)** | 구현 불가(사유) |

**② 쌍 집합 동등성** — v3 광역이 내는 후보쌍 집합 ↔ v4 공간 해시가 내는 집합을 **집합으로** 대조
(개수 · 차집합 **양방향** · 순서 무관). 판정 채널은 **«쌍 집합»이지 발화 수가 아니다**
(v4-05 등재: 이산 계수는 정밀도에 민감).

| 갈래 | 조건 |
|---|---|
| **(가)** | **완전 일치** |
| **(나)** | 차집합 있음(값 · **그 쌍들의 거리 분포**) |

**③ 전 정의역 실행** — 셀프 충돌 포함 결합 솔버(`full_par` + `selfcol`)로 **f64 × 병렬 × CUDA** 를
v3 정본과 **같은 조건**으로 실행. ㄱ s/프레임 ㄴ 창 순변위 궤적 ㄷ 정착 도달 여부
ㄹ **v3 정본 blob 과 좌표차**(중앙·p99·최대) ㅁ 리포트 5행 + 제품 문턱 판정(**③ 읽기 = ㉠ 확정본**)
ㅂ 게이트 5채널 v3 정본 ↔ v4.

| 갈래 | 조건 |
|---|---|
| **(가)** | **정착 도달 + 대역 5/5 + `pass` 일치** |
| **(나)** | 일부(항 명시) |
| **(다)** | **정착 미도달** |
| **(라)** | 발산 |

**④** 최적화·튜닝 **0** · 108칸 착수 **0** · 굽기 **0**.

**§4 회차 갈래** — A = ①②③ 전량 + ①(가) + ②(가) + ③(가)(나) ⟹ 상신 /
B = ②(나) ⟹ 차집합 값 상신 · ③ 집행 / C = ①(나)(다) ⟹ 값 상신 · **③ 착수 0** /
D = ①(라) 또는 ③(다)(라) ⟹ 사유·값 상신 · **정지** / E = 그 외 ⟹ 정지. **사후 갈래 신설 0.**

### §0-5 집행 «전» 사전 결정 9건 (값을 보기 «전»에 적는다)

```
 ㄱ **인용 자리**(손 상수 0 · 이 판이 «옮기는» 것의 정본) —
    `src/v3/solver.ts:261-274` `SelfCollisionParams`(tris · thickness · iterations · every) ·
    `:277` `selfStats`(0 근접 쌍 · 1 해소 횟수 · 2 최대 침투) · `:287-290` `SC_CLAMP 2047` · `scClamp` · `scKey` ·
    `:304-341` `scEmit`(sep 안만 · `d ≤ 1e-12` 버림 · 12칸 기록) ·
    `:359-411` `closestPtTri`(Ericson §5.1.5) · `:417-458` `closestSegSeg`(Ericson §5.1.9) ·
    `:462-489` `scVT` · `:491-503` `scEE` ·
    `:509-573` 광역(**cell = mean(엣지)+2·thickness** `:547` · AABB ±thickness · 버킷 삽입) ·
    `:574-624` 협역(**중복 제거 = 최소 모서리 셀** `:580` · **인접 제외 = 정점 공유** `:595` ·
      AABB 겹침 · 근접 쌍 계수 `:610` · **15특징 = 정점–삼각형 6 + 엣지–엣지 9**) ·
    `:641-670` 해소(**Gauss–Seidel · 간극 재측정** · `depth ≤ 0` 건너뜀 · `denom < 1e-20` 건너뜀 ·
      **λ = depth / Σ w·coef²**) · `:1117-1122` **적용 순서 = 제약 투영 «뒤» · 몸 충돌 «앞»** ·
    `src/v3/dressRun.ts:120` 정본 경로의 인자(`tris` · `thickness = THICK` · `every 1`).
    ★ 이 목록 밖의 수를 커널에 적지 않는다. `iterations`·`every` 는 **정본 기본값 1**을 쓴다.

 ㄴ **① 층1 의 자(K)** — v4-03/05/06 과 «같은 절차»로 **커널 코드에서 «센다»**(실측 보고 고르지 않는다) ·
    세는 규칙도 같다(나눗셈·sqrt 각 1 · 곱·합·차·부호반전 각 1) · **깊이 K_depth 와 총량 K_ops 를 둘 다
    세고 «작은 쪽(깊이)»으로 판정한다**(느슨한 쪽을 골라 통과를 만들지 않는다 · 함정 14).
    자의 «자리 값» = 그 정점 위치의 최대 성분 크기(`ulp.py:layer1` 규약 · deg = 1).
    **ULP_f64 판은 새 계기 안에서 만든다**(`ulp.py` 수정 0 · f32 판과 «같은 식» · 지수만 −52).

 ㄷ **② 「쌍 집합」의 정의** — v3 가 `:610` 에서 세는 **협역 기하까지 간 쌍**(= 인접 제외·AABB 겹침을
    통과한 쌍)이다. 양쪽 다 **(min(i,j), max(i,j)) 로 정규화**해 **정렬된 집합**으로 비교한다
    (순서 무관 · 중복 0). 차집합은 **양방향**으로 세고, 있으면 그 쌍들의 **최근접 거리 분포**를 낸다.

 ㄹ **① 순서 규약**(Gauss–Seidel 은 **순서 의존**이다 · 이 판의 가장 큰 함정) —
    v3 의 접촉 순서 = (셀 «생성» 순서, 버킷 안 삼각형 오름차순 쌍, 특징 15개 고정 순서)다.
    v4 는 **그 순서를 재현**한다. 재현 방법도 코드 사실에서 나온다 — 셀 생성 순서는 삼각형 t 오름차순 ·
    한 t 안에서 (cx,cy,cz) 오름차순이므로 셀마다 **(최소 점유 t, 그 t 의 상자 안 순회 인덱스)** 가 순위다.
    ★ **순서 차이의 크기도 «따로» 잰다** — 같은 커널을 (ㄱ) v3 순서 (ㄴ) 병렬 자연 순서로 각각 돌려
      두 결과의 좌표차를 낸다. 이것은 «판정»이 아니라 **①의 갈래가 무엇에 걸려 있는지를 값으로 남기는 것**이다.

 ㅁ **① 층1 시험계** — 초기 상태 = 정본 blob `settled-c100-h170-s45_M.bin` 의 **f64 원본**(캐스팅 0) ·
    적용 = **셀프 충돌만 1회**(예측 0 · 다른 제약 0 · 몸 충돌 0 · 속도 갱신 0) · v3 쪽은
    `resolveSelfCollisions` **한 번**만 부르는 신설 스크립트로 낸다(물리 경로 인용 · `src/` 0줄).

 ㅂ **③ 「같은 조건」** — 프레임 수는 **정본 blob 헤더의 `frame` 필드에서 읽는다**(손 상수 0).
    문턱 도달 판정은 v4-13 과 같은 식(창 순변위 ≤ `tol`) · `tol` 도 v4-13 과 같은 자리에서 읽는다.
    **둘 중 먼저 오는 쪽에서 멈춘다**(정착 도달 또는 헤더 프레임 수) — 미도달이면 그 사실과 최종 net 등재.

 ㅅ **③ arch·예산 규약**(v4-12 §0-4ㄴ 와 같은 형태 · 값 보기 «전») — **1프레임을 먼저 재고**
    목표 프레임 수의 벽시계를 추정한다. 추정이 **2시간**을 넘으면 **프레임 수를 40으로 자르고**
    그 사실을 등재한다(자른 실행도 ㄱㄴㄷㄹ 은 낸다 · ㅁㅂ 은 그 자리 상태로 낸다).
    ★ arch 는 **CUDA 고정**(회차 프롬프트 ③ 문언) · 폴백이면 `current_cfg().arch` 를 그대로 인쇄해 등재.

 ㅇ **③ 판정 읽기** — 판정문이 확정한 **㉠** 을 쓴다: 「대역 = 대역 «이름»」 · 「게이트 판정 = `pass` 값」.
    정점 대역 소속·`fails` 집합·진단 2자리는 **진단으로 병기**하되 갈래 판정에 걸지 않는다.

 ㅈ **정지** — ①(나)(다)면 ③ 착수 0(갈래 C) · ①(라) 또는 ③(다)(라)면 정지(갈래 D) ·
    사후 갈래 신설 0 · 처방 0 · 최적화 0 · 「같은 옷」 선언은 ③(가)일 때만 하고 그 밖에는 하지 않는다.
```

### §0-6 예산 · 실행 규약

```
 ① 층1 = 1서브스텝(양쪽 다 초 단위) · ② 쌍 집합 = 같은 실행의 부산물(추가 실행 0) ·
 ③ = 헤더 프레임 수(정본 blob = 180) 또는 정착 도달 · **상한은 §0-5ㅅ 규약으로 자른다**.
 ★ **GPU 동시 실행 0** · 전부 배경 실행 + 로그 파일 · 캡처 0(§3).
 ★ 이 판은 «마지막 커널»이다 — 이식의 정의역이 여기서 v3 와 같아진다.
```

---

## §1 집행

### §1-① 셀프 충돌 이식 — **(가)** 층1 통과(f64 **비 0.0625** · f32 **비 0.0369** · 초과 정점 0/9,388)

신설 = `gpu/engine/selfcol.py`(커널) · `gpu/l3sc_run.py`(v4 쪽 1서브스텝) ·
`scripts/v4SelfStep.ts`(v3 쪽 1서브스텝) · `gpu/l3sc_cmp.py`(판정) · `gpu/l3sc_ref.py`(쌍 집합 정본).
**`engine/` 기존 8파일 + `ulp.py` blob 불변**(§2-3).

#### ㄱ 무엇을 옮겼나 (코드 사실 · §0-5ㄱ 목록 그대로)

| 구간 | v3 자리 | 옮긴 것 |
|---|---|---|
| 셀 크기 | `solver.ts:547` | `cell = mean(엣지 길이) + sep` · `sep = 2 × thickness` |
| 상자 | `:541-544` | 삼각형 AABB 를 **±thickness** 로 넓힌다 |
| 셀 범위 | `:554-557` · `:287` | `floor(box / cell)` · `SC_CLAMP 2047` 로 자른다 |
| 중복 제거 | `:580-586` | 두 셀 «범위»의 **최소 모서리 셀**에서만 한 번 |
| 인접 제외 | `:595-601` | 정점을 **하나라도** 공유하면 건너뛴다 |
| 상자 겹침 | `:602-609` | 확장 AABB 6면 비교 |
| 특징 15개 | `:612-622` | **정점–삼각형 6**(a0a1a2 vs B · b0b1b2 vs A) + **엣지–엣지 9**(3×3) |
| 최근접 | `:359-411` · `:417-458` | `closestPtTri`(Ericson §5.1.5) · `closestSegSeg`(§5.1.9) — 분기 순서 그대로 |
| 방출 | `:304-341` | `d² ≥ sep²` 버림 · `d ≤ 1e-12` 버림 · [정점4 · 계수4 · 법선3 · 깊이1] |
| 해소 | `:641-670` | **Gauss–Seidel**(간극을 «적용 시점»에 다시 잰다) · `depth ≤ 0` 건너뜀 · `denom < 1e-20` 건너뜀 · `λ = depth / Σ w·coef²` |
| 자리 | `:1117-1122` | 제약 투영 «뒤» · 몸 충돌 «앞»(§1-③ 의 `full_sc.py` 가 지킨다) |

```
 ★ **자료구조는 다르다** — v3 는 해시 맵 버킷, v4 는 **조밀 격자(고정 용량)**다. 그런데도 집합이
   같아야 하는 근거가 **코드 안에 있다**(`solver.ts:518-524` 의 논증): 확장 AABB 가 겹치면
   그 겹침 구석의 셀을 **반드시 함께 점유**하므로 후보 쌍 집합은
   **{ (i,j) : i<j · 정점 비공유 · 확장 AABB 겹침 }** 이고 **격자와 무관**하다. §1-② 가 이것을 값으로 잰다.
 ★ **순서는 격자에 의존한다**(Gauss–Seidel · §0-5ㄹ). v4 는 셀마다 **(최소 점유 삼각형 t, 그 t 의
   상자 안 순회 인덱스)** 를 원자 최소값으로 세어 v3 의 «셀 생성 순서»를 재현하고,
   (셀 순위 · i · j · 특징 번호)를 하나의 64비트 키로 묶어 접촉을 정렬한 뒤 **직렬**로 해소한다.
```

#### ㄴ 층1 의 자 K — **코드에서 «셌다»**(실측 보고 고르지 않는다 · §0-5ㄴ · v4-03/05/06 과 같은 절차)

세는 규칙(v4-03 §0-4 등재분 그대로): 나눗셈·`sqrt` 각 1 · 곱·합·차·부호반전 각 1 · min/max(클램프)는 0.

| 구간(정점–삼각형 경로) | 깊이 |
|---|---|
| `ab`·`ac`·`p−a` | 1 |
| `d1 = ab·(p−a)`(곱1+합2) · `d2`~`d6` | 4 |
| `vc = d1·d4 − d3·d2`(곱1+차1) · `vb` · `va` | 6 |
| `va+vb+vc`(합2) → `den = 1/(·)` | 9 |
| `v = vb·den` · `w = vc·den` | 10 |
| `pt = a + ab·v + ac·w`(곱1+합1+곱1+합1) | 13 |
| `dv = p − pt` | 14 |
| `d² = dv·dv`(곱1+합2) → `d = √` → `n = dv/d` | 19 |
| 해소 `c·(pos·n)`(내적3 + 곱1) | 23 |
| `gap = Σ 4항`(합3) → `depth = sep − gap` | 27 |
| `λ = depth / Σ w·c²` | 28 |
| `g = λ·w·c`(곱2) → `pos += g·n`(곱1+합1) | **32** |

```
 · **깊이 K_depth = 32**(정점–삼각형 경로 · 엣지–엣지 경로는 **31** ⟹ 큰 쪽을 쓴다)
 · **총량 K_ops = 188**(정점–삼각형 116 + 해소 72 · 엣지–엣지는 83 + 72 = 155)
 ⟹ **판정에는 «깊이 32»만 쓴다**(엄격한 쪽 · 느슨한 쪽을 골라 통과를 만들지 않는다 · 함정 14).
   자리 값 = 그 정점 위치의 최대 성분 · **deg = 1**(`ulp.py:layer1` 규약 · v4-04 §0-2ㄴ).
```

#### ㄷ 층1 실측 (1서브스텝 · 같은 초기 상태 = 정착 blob 의 f64 원본 · 2호기 CUDA)

| 채널 | **v3**(`solver.ts:step`) | **v4 f64**(v3 순서) | **v4 f32**(v3 순서) |
|---|---|---|---|
| 근접 쌍 | **41,567** | **41,567** | **41,567** |
| 방출 접촉 | (미노출) | 135 | 97 |
| 해소 횟수 | **75** | 83 | 65 |
| 최대 침투(m) | **5.1278823706259104e-05** | **5.1278823706259104e-05** | 5.123973824083805e-05 |
| 움직인 정점 | **200** | **200** | 9,388(캐스팅 때문) |
| 최대 변위(m) | 3.931502061887615e-05 | 3.9315020618859e-05 | 3.924034109882085e-05 |

| 층1 판정 | 최대 성분차(m) | 상한 = 32 × ULP(m) | **비** | 초과 정점 | 판정 |
|---|---|---|---|---|---|
| **f64 · v3 순서** | **4.440892098500626e-16** | 7.105427357601002e-15 | **0.0625** | **0 / 9,388** | **통과** |
| **f32 · v3 순서** | **1.4065420472775259e-07** | 3.814697265625e-06 | **0.0369** | **0 / 9,388** | **통과** |
| (대조) f64 · 자연 순서 | 1.5271138502637172e-06 | 7.105427357601002e-15 | **214,922,167.7** | **76** | 불통과 |
| (대조) f32 · 자연 순서 | 1.460214093862433e-06 | 3.814697265625e-06 | 0.3828 | 0 | (통과) |

```
 ⟹ **갈래 (가)** — f64 «그리고» f32 둘 다 통과. f64 의 차 4.44e-16 은 **그 자리 2 ULP_f64** 다
   (자리 값 1.449586 m · ULP 2.220446e-16). 참고: f32 캐스팅«만» 해도 5.959267e-08 m 이 생긴다.
 ★★ **순서가 갈래를 가른다**(§0-5ㄹ ★ 채널이 값이 됐다) — 같은 커널을 «자연 순서»로 돌리면
   f64 차가 **1.527e-06 m** 으로 **3.4e+09배** 커지고 층1 을 **76 정점에서 초과**한다.
   Gauss–Seidel 은 순서 의존이고, 이 이식이 통과한 것은 **v3 의 접촉 순서를 재현했기 때문**이다.
   f32 에서는 자연 순서도 통과하는데, 그것은 **f32 의 자가 그만큼 굵기 때문**이지 순서가
   무관해서가 아니다(f32 순서차 1.431e-06 은 f64 순서차와 «같은 크기»다).
 ★ **해소 횟수는 갈리는데(75 ↔ 83 ↔ 65) 위치는 2 ULP 로 만난다** — 「이산 계수는 정밀도에
   민감하다」(v4-05 등재)가 다시 값으로 나왔다. 회차 프롬프트 ②가 판정 채널을 «쌍 집합»으로
   못 박은 자리가 여기다. 갈리는 접촉은 `depth ≈ 0` 인 것들이라 위치를 사실상 안 바꾼다.
 ★ **개발 기계 대조**(에어 M4 · arm64 · f64 · v3 순서) — 최대 성분차 **2.220446049250313e-16**
   (= **1 ULP** · 비 0.03125). 2호기 CUDA 가 2 ULP 인 것은 GPU 쪽 융합 곱셈–덧셈(FMA)로 읽히지만
   **단정하지 않는다**(둘 다 층1 통과 · 처방 0).
 ★ **v3 쪽 값은 두 기계에서 같다** — 에어와 2호기가 근접 쌍 41,567 · 해소 75 ·
   최대 침투 5.1278823706259104e-05 · 최대 변위 3.931502061887615e-05 **인쇄 자릿수 전량 동일**.
```

### §1-② 쌍 집합 동등성 — **(가) 완전 일치**(차집합 **양방향 0** · f64·f32 둘 다)

계기 = `gpu/l3sc_ref.py`(신설) — **격자 0 · 해시 0**으로 삼각형 쌍 **162,279,120개 전량**을 검사해
`R = { (i,j) : i<j · 정점 비공유 · 확장 AABB 겹침 }` 를 만든다(11.3 s).

| 채널 | 값 |
|---|---|
| `R`(격자 없는 정본) | **41,567** |
| v3 가 «스스로 센» 근접 쌍(`selfStats[0]`) | **41,567** |
| ⟹ **v3 집합 = R** | v3 집합 ⊆ R 이고 **크기가 같다** ⟹ 같은 집합 |
| v4 공간 격자(f64) | **41,567** · 차집합 **GPU−R 0 · R−GPU 0** · **완전 일치** |
| v4 공간 격자(f32) | **41,567** · 차집합 **0 · 0** · **완전 일치** |

```
 ⟹ **갈래 (가)** — 「자료구조는 달라도 결과 집합은 같아야 한다」는 이식의 조건이 **값으로** 섰다.
 ★ 차집합이 0 이므로 「그 쌍들의 거리 분포」(갈래 (나)의 요구)는 **낼 것이 없다.**
 ★ **f32 에서도 집합이 같다** — 상자 판정이 경계에 걸린 쌍이 이 자리에는 없었다는 뜻이다(사실 등재).
```

### §1-③ 전 정의역 실행 — **(가)** 정착 도달 + 대역 **5/5** + `pass` **일치**

신설 = `gpu/engine/full_sc.py`(`FullPar` 상속 · 한 서브스텝을 `substep_a` / 셀프 충돌 / `substep_b` 로
«가른다» · 투영 산술 재작성 **0줄**) · `gpu/l3full_run.py`(실행기) · `scripts/v4SelfNet.ts`(v3 정본의 정착 채널).

#### ㄱ 예산 규약 적용 (§0-5ㅅ · 값 보기 «전» 등재)

| 채널 | 값 |
|---|---|
| 1프레임 probe | **2.316 s/프레임** |
| 헤더 프레임 수(정본 blob `frame`) | **180** — 손 상수 0(`settled-…bin` 헤더에서 읽었다) |
| 180프레임 추정 | 417 s ≈ **7분** < 2시간 ⟹ **자르지 않았다**(`capCut` false) |

#### ㄴ 본 실행 (f64 × 병렬 × CUDA · `arch` assert 통과)

| 채널 | 값 |
|---|---|
| **정착 도달** | **프레임 10** · 창 순변위 **9.000416e-05 ≤ tol 1e-4**(tol 은 v4-13 과 같은 자리에서 읽었다) |
| s/프레임 | **2.351** (10프레임 23.5 s · JIT 9.5 s 는 밖) |
| degenerate / NaN / 발산 | **0 / 0 / 0**(전 정점 유한) |
| 셀프 충돌(서브스텝당 평균) | 근접 쌍 **41,577.8** · 방출 접촉 **2,095.7** · 해소 **550.1** |
| 최대 침투(m) | 1.3706762668486594e-04 |
| (대조) **v3 정본 경로** 실측 | **29.276 s/프레임** · 근접 쌍/프레임 9,520,933.3 · 해소/프레임 121,316.2 ·<br>최대 침투 1.3699618018766526e-04 ⟹ **v4 가 12.45배 빠르다** |

```
 ★ **첫 창에서 이미 문턱 아래다.** 같은 blob 에서 «셀프 충돌 없이» 돌린 v4-13 은 첫 창이
   1.549149e-02(문턱의 155배)였고 f=80 까지 가서야 문턱을 넘었다. 마지막 커널이 들어오자
   **v4 가 v3 의 정착 자리에 «머문다».**
 ★ 서브스텝당 쌍 41,577.8 × 229 = 9,521,318 ↔ v3 실측 9,520,933.3(프레임당) — **0.004% 차**.
   해소 550.1 × 229 = 125,965 ↔ v3 121,316.2 — 3.8% 차(이산 계수 · §1-①ㄷ ★와 같은 성질).
```

#### ㄷ v3 «정본» blob 과의 좌표차

| 대조 | 중앙 | p99 | 최대 정점거리 | 최대 좌표차 | maxAt |
|---|---|---|---|---|---|
| **v4(셀프 충돌 · f=10) ↔ v3 정본 blob** | **0.006600 mm** | **0.051408 mm** | **0.090004 mm** | 0.077940 | 7095 |
| (v4-13 · 셀프 충돌 **없이** f=80 ↔ v3 f80) | 0.011974 mm | 0.439469 mm | **4.186409 mm** | 4.088415 | 7693 |

⟹ 최대가 **4.186 → 0.090 mm(46.5배 축소)** · p99 **0.439 → 0.051(8.5배)** · 중앙 **0.0120 → 0.0066(1.8배)**.

#### ㄹ 리포트 5행 · 제품 문턱 (읽기 = **㉠ 확정본** · §0-5ㅇ · 계기 3종 **수정 0**)

| 행 | v3 정본 중앙 | v4 중앙 | 표시 1자리 | 같은가 | 대역 이름 | 같은가 | 정점 대역 | 같은가 | 대역 n |
|---|---|---|---|---|---|---|---|---|---|
| 목선 | 1.0641 | 1.0647 | **1.1 / 1.1** | ✅ | 밀착 / 밀착 | ✅ | [0,41,13] | ✅ | 54 / 54 ✅ |
| 가슴 | 4.4660 | 4.4572 | **4.5 / 4.5** | ✅ | 여유 / 여유 | ✅ | [0,111,155] | ✅ | 266 / 266 ✅ |
| 허리 | 26.1203 | 26.1151 | **26.1 / 26.1** | ✅ | 여유 / 여유 | ✅ | [0,0,226] | ✅ | 226 / 226 ✅ |
| 밑단 | 18.3743 | 18.3650 | **18.4 / 18.4** | ✅ | 여유 / 여유 | ✅ | [0,8,102] | ✅ | 110 / 110 ✅ |
| 소매 | 4.6177 | 4.6335 | **4.6 / 4.6** | ✅ | 여유 / 여유 | ✅ | [0,319,599] | ✅ | 918 / 918 ✅ |
| **합계** | | | **5/5** | | **5/5** | | **5/5** | | **5/5** |

행별 중앙 차(mm · v4−v3): +0.000624 · −0.008790 · −0.005191 · −0.009340 · +0.015853.
진단 2자리는 **1/5**(목선만 일치) — p25·p75 의 둘째 자리에서 갈린다(진단 채널 · 판정에 걸지 않는다 · §0-5ㅇ).

#### ㅁ 게이트 (`runS4Gate` · 물리 0스텝 · 문턱은 `S4_THRESHOLD` 에서 뜬다)

| 채널 | 문턱 | **v3 정본 blob** | **v4(셀프 충돌)** |
|---|---|---|---|
| ③a 관통 최대 | ≤ 0.5 mm | **0.49717269** (961정점) ✅ | **0.49717235** (968정점) ✅ |
| **자기관통 교차** | = 0 | **0** ✅ | **0** ✅ |
| 목선 초과비 | ≤ 1 | 0.98917376 ✅ | 0.98917108 ✅ |
| 보조 장치(invMass=0) | = 0 | 0 ✅ | 0 ✅ |
| 발산 | 없음 | 없음 ✅ | 없음 ✅ |
| **정착 창 순변위** | ≤ 0.1 mm | **0.058747 mm** ✅ | **0.090004 mm** ✅ |
| 최소 쌍거리 | (참고) | 1.9988797 mm | 1.9988535 mm |
| 봉제 간극 중앙 / λ 최대 | (참고) | 2.132178 / 1.4522871 | 2.133084 / 1.4519779 |
| **`pass`** | — | **true** | **true** |
| **`fails`** | — | **[]** | **[]** |

```
 ★ **v4 가 S4 게이트를 «통과»한다 — 처음이다.** v4-13(셀프 충돌 없음)은 관통 0.6599 초과 +
   자기관통 교차 428 로 두 항이 실패했고, 그때는 **v3 도** 교차 430 으로 실패했다.
   마지막 커널이 들어오자 **양쪽 다 교차 0 · 관통 0.497 ≤ 0.5** 가 되고 `fails` 가 **둘 다 빈 집합**이다.
 ★ 정착 채널의 두 값은 «같은 뜻»으로 마주 선다 — 둘 다 **정본 blob 에서 앞으로 10프레임** 간
   창 순변위다(v3 는 `scripts/v4SelfNet.ts` 로 이 판에서 쟀다 · 전방 창이라는 사실을 그대로 등재한다).
 ⟹ **갈래 (가)** — 정착 도달 ✅ + 대역 5/5 ✅ + `pass` 일치 ✅.
 ★★ **㉡ 읽기(정점 대역 소속 · `fails` 집합)로 세도 결과가 같다** — 정점 소속 **5/5** ·
   `fails` **[] ↔ []**. v4-13 에서 두 읽기가 갈렸던 자리가 이 판에서는 **갈리지 않는다.**
```

#### ㅂ 보조 채널 — «머무는가»(180프레임 · 판정 0 · 사실만)

등재 규약(§0-5ㅂ)은 **먼저 오는 쪽**에서 멈추므로 본 실행은 f=10 에서 섰다. 「그 뒤에도 머무는가」는
판정이 아니라 사실이라 **따로** 돌렸다(`hold` 모드 · 180프레임 474.2 s · 2.634 s/프레임).

| f | 10 | 20 | 40 | 60 | 80 | 100 | 120 | 140 | 160 | **180** |
|---|---|---|---|---|---|---|---|---|---|---|
| 창 순변위(m) | 9.000416e-05 | 4.052864e-05 | 1.977460e-05 | 2.136605e-05 | 1.725874e-05 | 1.094782e-05 | 5.066130e-06 | 1.866845e-05 | 1.767578e-05 | **9.061880e-06** |

f=180 상태 ↔ v3 정본 blob — 중앙 **0.006052 mm** · p99 **0.048314 mm** · 최대 **0.090304 mm**(정점 7095).
⟹ **17배 더 돌려도 간극이 커지지 않는다**(최대 0.090004 → 0.090304 mm) · degenerate·발산 **0**.

### §1-④ 착수 0

```
 최적화·튜닝 **0**(셀 용량 · block_dim · 색 순서 · 봉제 컬러링 · `serialize` 해제 — 어떤 것도) ·
 108칸 착수 **0** · 굽기 **0** · 캡처 **0** · `src/` 수정 **0** · `engine/` 기존 8파일 수정 **0**.
 ★ §2 가 「해소 직렬 커널이 셀프 충돌 비용의 대부분」임을 값으로 보지만 **처방 0**이다.
```

---

## §2 자기검사

### §2-1 집행 / 미집행 총계

**집행한 것**

| # | 무엇 | 계기 | 실행 |
|---|---|---|---|
| ① | v3 셀프 충돌만 1서브스텝 | `scripts/v4SelfStep.ts`(**신설**) | 86 ms(광역 26.7 · 협역 54.1 · 해소 0.18) |
| ① | v4 셀프 충돌만 1서브스텝 × (f64·f32) × (v3 순서·자연 순서) | `gpu/l3sc_run.py`(**신설**) | 4회 · 각 0.11~22.6 s(첫 회 JIT 포함) |
| ① | 층1 판정 | `gpu/l3sc_cmp.py`(**신설**) | 물리 0스텝 |
| ② | 격자 없는 쌍 집합 정본 `R`(전수 162,279,120쌍) | `gpu/l3sc_ref.py`(**신설**) | 11.3 s |
| ③ | 1프레임 probe(예산 규약) | `gpu/l3full_run.py`(**신설**) | 2.316 s/프레임 |
| ③ | **본 실행**(f64 × 병렬 × CUDA · 정착 도달 f10) | 〃 + `engine/full_sc.py`(**신설**) | **23.5 s**(+ JIT 9.5 s) |
| ③ | v3 정본 경로의 정착 채널(전방 10프레임) | `scripts/v4SelfNet.ts`(**신설**) | **292.8 s**(29.276 s/프레임) |
| ③ | 좌표차 2건 · 리포트 2건 · 제품 대조 1건 · 게이트 2건 | `l3cb2_cmp.py` · `v4FitReport/ProductCmp/ProductGate.ts`(**diff 0**) | 물리 0프레임 |
| ③보조 | «머무는가» 180프레임 | `l3full_run.py hold` | **474.2 s**(2.634 s/프레임) |
| §2 | 태스크·grid/block 실측 | `gpu/sc_probe.py`(**신설**) | 1분 미만 |

**실행하지 «않은» 것**(침묵으로 넘기지 않는다)

```
 최적화·튜닝 **0** · 108칸 **0** · 굽기 **0** · 캡처 **0** · `src/` 수정 **0** ·
 `engine/` 기존 8파일 + `ulp.py` 수정 **0** · v4-02~13 산출물 수정 **0** ·
 f32 전 정의역 실행 **0**(회차 프롬프트 ③은 f64 를 지정했다) · 예산 자르기 **0**(추정 7분) ·
 셀프 충돌의 «비용 분해» **0**(§2-2 가 태스크만 센다) · 처방 **0** · 사후 갈래 신설 **0** ·
 **ecc 에이전트 0 / ecc 스킬 0**(`strategist` 서브에이전트도 0).
```

### §2-2 arch · 태스크 · grid/block · 시간

| 채널 | 값 |
|---|---|
| 기계 | **`DESKTOP-OV72A46`** · win32 · GTX 1660 Ti · Taichi **1.7.4** · llvm 15.0.1 · py **3.12.10** |
| (개발) | 에어 M4 · arm64 · Taichi 1.7.4 · llvm 15.0.7 · py 3.13.9 — **계기 개발과 예비 시행만**(§1-①ㄷ ★) |
| arch(`current_cfg().arch`) | **`Arch.cuda`** 전량 · **assert 통과**(조용한 폴백 0 · v4-01 함정 1) |
| 정밀도 | ① f64·f32 · ② 둘 다 · ③ **f64** |
| 태스크/호출 — `substep_a + substep_b` | **29.00 == 코드에서 «센» 29**(`full_par` 와 **같은 수** · 가른 것이 수를 안 바꿨다) |
| 〃 range_for / serial / grid / block | **28종 / 1종 · grid 1~209 · block 128**(v4-13 과 같은 값) |
| 태스크/호출 — **셀프 충돌** | **40.00** · 종류 38 · `range_for` **8종**(grid **141~768** · block **128**) · serial 30종 |
| 셀프 충돌 최상위 비용(프로파일러 켠 시행) | `_resolve …_serial` **61.689 ms/3호출**(grid 1 × block 1) ·<br>`_pairs` 35.907 · `_narrow` 25.457 · `_fill` 1.144 · `_rank` 1.037 · `_boxes` 0.717 |
| s/프레임 | **v4 2.351**(전 정의역 f64 CUDA) ↔ **v3 29.276**(정본 경로) ⟹ **12.45배** |
| degenerate / NaN / 발산 | **0 / 0 / 0**(본 실행 · 보조 180프레임 둘 다) |
| `py -m pytest gpu/tests -q` | **40 passed · 2 xfailed · 1 xpassed** (59.57 s) — v4-10~13 과 **같은 값** |
| `npx tsc -b` | **통과**(exit 0) |

★ 프로파일러를 켠 시행은 **다른 시행**이다 — 커널 시간 합(약 20 ms/서브스텝)이 본 실행의
벽시계(10.3 ms/서브스텝)보다 크다. 계기를 켜면 커널마다 동기화가 붙는다(v4-11 과 같은 처분 · 판정 0).

### §2-3 경계 · diff

| 채널 | 값 |
|---|---|
| `src/` diff(기점 `f677e0a` ↔ 이 판) | **0** |
| `engine/` **기존 8파일 + `ulp.py`** blob 동일 | `full.py a631992832dd` · `full_color.py 7c7a49217664` · `full_color2.py 1ea95c3af689` · `full_par.py 09a6517973bd` · `stretch.py 7b3d8b6bf8a5` · `bending.py 6e1972f589d0` · `seam.py 7abfd6074dc1` · `collide.py 4d748bb65870` · `ulp.py 40fcdfcb4414` — **9/9 기점과 같은 blob** |
| 계기 3종 diff 0 | `v4FitReport.ts` · `v4ProductCmp.ts` · `v4ProductGate.ts` — 인용만 |
| 정본 diff | **0** — 이 판 산출은 전부 **신규 파일**(§0-2 사무 2건 제외 · 그 둘은 §0 커밋) |
| 이 판이 «만든» 코드 | `gpu/engine/selfcol.py` · `gpu/engine/full_sc.py` · `gpu/l3sc_run.py` · `gpu/l3sc_ref.py` · `gpu/l3sc_cmp.py` · `gpu/l3full_run.py` · `gpu/sc_probe.py` · `scripts/v4SelfStep.ts` · `scripts/v4SelfNet.ts` (전부 신규) |
| 이 판이 «만든» 산출물 | `l3sc-v3-*` · `l3sc-v4-*` · `l3sc-ref-*` · `l3sc-cmp-*` · `l3sc-v3net-*` · `l3full-*` · `l3cb2-cmp-v4-14*` · `fit-…-v3정본` · `fit-…-v4-14-sc-f10` · `l3-gate-…` 2건 · `l3-product-cmp-v3정본-vs-v4-14sc` (전부 새 접두) |
| 굽기 / 캡처 / ecc | **0 / 0 / 0** |

### §2-4 물리 diff 0 의 «바이트» 증명

```
 ① `full_sc.FullSC` 는 `FullPar` 를 상속하고 투영 산술 4종(`_p_inplane`·`_p_bend`·`_p_seam`·
    `_p_collide`)을 **정의하지 않는다**(호출만) — `full.Full` 의 `@ti.func` 그대로다.
 ② 한 서브스텝을 두 커널로 «가른» 것이 **연산 순서를 바꾸지 않는다**는 것은 태스크 수로 선다 —
    `substep_a + substep_b` = **29.00** 으로 `full_par.substep_par` 와 **같은 수·같은 종류**다.
 ③ 셀프 충돌의 정확성은 **층1(2 ULP)** 과 **쌍 집합(차집합 0)** 두 채널이 «따로» 증명한다.
 ④ v3 쪽 값이 **두 기계에서 같다**(에어 ↔ 2호기 · §1-①ㄷ ★) ⟹ 이 판의 기준선이 흔들리지 않았다.
```

### §2-5 계기 사실 2건 (값 아님 · 처방 0)

```
 ① **`cell` 이 시행마다 마지막 자리에서 흔들린다** — 셀 크기 = Σ(엣지 길이)/(3T) + sep 인데
    그 합을 **병렬 원자 덧셈**으로 모으므로 더하는 순서가 시행마다 다르다.
    f64 에서 0.012886455006612231 ↔ ...285(상대 4e-15) · f32 에서 0.012886453 ↔ 0.012886435(상대 1.4e-06).
    **쌍 집합은 네 시행 모두 41,567 로 같았다**(§1-②) ⟹ 이 흔들림이 집합을 바꾸지 않았다.
    고치지 않았다(최적화·튜닝 0 · §0-3) · 사실만 등재한다.
 ② **`tsc -b` 가 신설 스크립트의 미사용 import 를 잡았다**(`v4SelfNet.ts` 의 `DAMP`) — 지우고 통과.
    물리·값 영향 0.
```

---

## §3 화면

**캡처 0.** 이 판은 쌍 집합·층1·궤적·리포트 수만 묻는다(회차 프롬프트 §3).

---

## §4 회차 갈래 — **A**(①②③ 전량 + ①(가) + ②(가) + ③(가)) ⟹ 상신

| 항 | 갈래 | 한 줄 |
|---|---|---|
| ① 셀프 충돌 이식 | **(가)** | 층1 통과 — f64 **4.44e-16 m = 2 ULP**(비 0.0625) · f32 **1.41e-07**(비 0.0369) · 초과 정점 **0/9,388** |
| ② 쌍 집합 동등성 | **(가)** | **41,567 = 41,567 = 41,567**(v4 = R = v3) · 차집합 **양방향 0** · f64·f32 둘 다 |
| ③ 전 정의역 | **(가)** | 정착 **f=10 도달**(9.000e-05 ≤ 1e-4) · 대역 **5/5** · `pass` **true ↔ true 일치**(fails 둘 다 빈 집합) |

**넘기는 것 7건**

```
 1. ★★★ **이식이 «닫혔다»** — 마지막 커널(셀프 충돌)이 들어오면서 v4 의 정의역이 v3 와 같아졌다.
    다섯 커널(늘어남·굽힘·봉제·몸 충돌·셀프 충돌) 전부 층1 을 통과한 상태다.
 2. ★★★ **v4 가 S4 게이트를 처음 통과한다**(`pass` true · `fails` []) — v3 정본도 통과하고
    **두 실패 목록이 둘 다 빈 집합**이다. 자기관통 교차가 428 → **0** 으로 닫혔다.
 3. ★★★ **「같은 옷」의 세 조건이 처음 «전부» 선다** — 표시값 **5/5** · 대역 이름 **5/5** ·
    게이트 `pass` **일치**. 게다가 ㉡ 읽기(정점 소속 5/5 · fails 집합 일치)로 세도 **같은 결론**이다.
    ⟹ 선언 여부는 규칙을 쓴 전략 세션 몫이고, **이 판은 조건이 섰다는 사실까지만 적는다.**
 4. ★★ **정착 자리에 «머문다»** — 첫 창부터 문턱 아래(9.00e-05)이고, 180프레임을 더 돌려도
    v3 정본과의 간극이 0.090004 → 0.090304 mm 로 **커지지 않는다**.
 5. ★★ **순서가 이식의 조건이었다**(함정 후보) — Gauss–Seidel 은 순서 의존이라 v3 의 접촉 순서를
    재현하지 않으면 층1 이 **3.4e+09배**로 어긋난다(f64 자연 순서 1.527e-06 m · 76 정점 초과).
    「자료구조는 달라도 «집합»은 같아야 한다」는 조건 옆에 **「순서도 같아야 한다」**가 선다.
 6. ★ **속도** — v4 2.351 s/프레임 ↔ v3 정본 경로 29.276 s/프레임 = **12.45배**. 셀프 충돌의
    최상위 비용은 **해소 직렬 커널**(grid 1 × block 1)이다 — **분해·처방 0**(다음 판 몫).
 7. ★ **③a 관통의 미결이 닫혔다**(v4-13 §4 ㉣ · 판정문이 「셀프 충돌 없는 정의역의 값이라 귀속 0」
    으로 미뤘던 항) — 셀프 충돌이 들어오자 v3 0.49717269 · v4 0.49717235 로 **둘 다 문턱 안**이다.
```

**이 판이 «하지 않은» 판단** — 「같은 옷」 선언 · 병렬판/f64 채택 · 108칸 확장 · 셀프 충돌 비용의
처방(해소 직렬 커널) · f32 전 정의역의 거동 · 진단 2자리 1/5 의 해석. **처방 0 · 최적화 0 · 사후 갈래 신설 0.**
