# v4-42 — 그리드 하네스 결함 2건 수정(색인 스팸 · vite 리로드) · 실패 칸 원인 계기 보강

**물리 0줄 · `src/` diff = `V4AposeGrid.tsx` + (필요시) `vite.config.ts` 감시 제외 «한정» · `Mannequin.tsx`·`bakeGrid` 0 · 굽기 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-42-grid-fix2` |
| 기점 | **`943f7ab`**(= v4-41 §1~§4 + 볼트 + 보고) |
| §0 커밋 | (아래 §0-5 에 자기 기입) |
| §1~§4 커밋 | (집행 후 기입) |
| 보고 파일 | `docs/v4/보고/42.md` |

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v4-42-grid-fix2 943f7ab → Switched to a new branch 'v4-42-grid-fix2'
       $ git branch --show-current → v4-42-grid-fix2 · HEAD 943f7ab
 2호기 (§0 커밋을 push 한 «뒤» 같은 자리로 전환 — §0-5 에 인쇄)
```

---

## §0 수령·사무

### §0-1 판정문 (전략 세션 · 승혁 27칸 실행 로그 수령 · 원문)

```
 「27칸 본실행 중단 · 수신기 로그 사실:
   ㉠ 색인이 칸마다 전송돼 카운트 소진(6/82 가 전부 색인 사본 · 하네스 결함 = CC 귀책)
   ㉡ 몸 bin 수신 0 = 칸들이 export 전 실패
   ㉢ 표 소멸 = 리로드 후보(산출이 저장소 안 폴더로 쓰여 vite 가 리로드 — dev 로그로 확인 예정) ·
   드라이런(기준 칸)은 통과했으므로 실패는 «배율 걸린 칸» 고유 — 잔차 궤적이 재료다」
```

### §0-2 바꾸지 않는 것 (회차 프롬프트 원문)

```
 물리 **0줄** · `Mannequin.tsx`·`bakeGrid` **0** · 굽기 **0** · 정본·문턱 **0**
```

### §0-3 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · 단위 명시)

**★ 단위** — 파일 수는 **개** · 잔차는 **무차원 배율** · 프레임은 **프레임**(1/60 s) · 칸은 **칸**.

| 항 | 갈래 |
|---|---|
| **①** 색인 전송 | 코드 사실(어디서 보내는가) → **마지막 1회**로 · 실패 칸이 있어도 1회 |
| **②** 리로드 차단 | vite 감시 사실 → `server.watch.ignored` 에 산출 경로 · 절차서 out 을 **저장소 밖**으로(이중 방어) |
| **③** 실패 칸 계기 | 칸 종료마다 `console.log` + `localStorage` 누적 · 잔차 궤적 4점 · **어느 조건이 안 섰는지** 값으로 |
| **④** 드라이런 확장 | `apoonly` 로 **배율 걸린 칸** 지정 가능 확인 · 절차서 3.5단계를 「기준 칸 + 배율 칸 각 1」로 |

**§4** — A = ①~④ 완료 ⟹ 승혁 재실행 안내(배율 칸 드라이런 → 27칸) / B = 막힘(사실) ⟹ 상신.

### §0-4 사전 결정 4건 (값 보기 «전» · CC 집행 설계)

```
 ㄱ **판정문의 사실 서술도 «코드»로 확인한다** — ㉠ 「칸마다 전송」이 코드에 실제로 있는지 먼저 읽고,
    없으면 **그 사실을 정정으로 적는다**(전략 세션 문언을 그대로 옮기되 코드 사실을 병기 · 다툼 0).
 ㄴ **수신기 카운트를 «먹는» 전송을 줄인다** — 실패 실행이 카운트를 소진하지 않도록,
    **반출 0건인 실행은 색인도 보내지 않는다**(부분 성공이면 종전대로 1회). 증거는 ③이 대신 남긴다.
 ㄷ **리로드는 «두 겹»으로 막는다** — 설정(감시 제외) 과 절차(저장 위치를 저장소 밖으로).
    설정 변경은 `server.watch.ignored` 한 줄이고 **빌드·제품 경로에 영향 0** 임을 diff 로 보인다.
 ㄹ **브라우저 실행은 여전히 승혁 몫**(CC 브라우저 채널 미연결 · v4-41 §1-③) —
    이 판은 코드·설정·절차까지만 하고, 값 판정은 다음 판이 한다.
```

### §0-5 커밋·전환 인쇄 (집행 «전»)

(§0 커밋 sha 자기 기입 · 2호기 전환 인쇄는 아래에 붙인다)
