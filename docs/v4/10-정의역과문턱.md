# v4-10 — 미수렴 «정의역» 가름 · 제품 문턱 «도입» · 굽힘 컬러링 가능성

**집행 기계 = 2호기(CUDA · 정본 기계) · `src/` diff 0 · 굽기 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-10-domain` |
| 기점 | **`51a136b`** (= v4-09 HEAD) |
| 워크트리 | `C:\Users\banba\fit-simulator` |
| 기계 | **`DESKTOP-OV72A46`** (win32 · CUDA · GTX 1660 Ti) |
| §0 커밋 | **`6ff2ec0`** — 집행 «전» 별도 커밋 |
| §1~§4 커밋 | **`04ab401`** (코드·산출물·본문) · 볼트 등재는 뒤이은 커밋 |
| 호출 ecc 에이전트/스킬 | **0 / 0** |

---

## §0 수령 (집행 «전» 별도 커밋)

### §0-1 판정문 (전략 세션 · v4-09 §4 수령 · 원문)

```
 「갈래 C 수령 · ★ ② 규칙 폐기 «수용» — 섭동 기반 문턱은 성립하지 않는다(경계가 연속 ·
   v3 는 결정적이라 자기 재현 폭이 없다) · 이후 섭동 문턱 재시도 0 ·
   ★★ 문턱을 «제품 쪽»에서 가져온다: 「같은 옷」 = 표시 리포트 5행이 반올림 후 일치 +
   대역 소속(v3-54) 일치 + 게이트 판정 일치 · 손 상수 0 · 표본 0 ·
   ㉡ 중심 교체 효과 수용(정의역 offset 이 지배) ·
   ★ ①ㄹ·f32 양자로 «이식 차이»와 «정밀도 바닥» 둘 다 기각 ⟹ 미수렴 후보 = 정의역
   (셀프 충돌 «없이» 셀프 충돌 «있는» 상태에서 출발) · 후보이지 단정 0 ·
   ★ ③ Amdahl — 굽힘 59.3% 가 직렬로 남으면 병렬화 무의미 · 굽힘 컬러링 가능성이 먼저 ·
   귀책 = 전략 세션(섭동 규칙 3판)」
```

### §0-2 귀책 등재 1건 (판정문이 스스로 적은 것 · CC 가 만들지 않았다)

```
 ㄱ **전략 세션 — 섭동 기반 표본 규칙 «3판».**
   v4-06/07/08/09 에 걸쳐 「v3 를 섭동해 잡음 폭을 만들고 그 폭을 문턱으로 쓴다」가 세 판
   반복됐고, v4-09 §1-② 가 값으로 부정했다(초과 채널 수가 ε 에 2→8→13→13 으로 «연속»).
   판정문이 **스스로 귀책을 적었고** 이 판은 그 문장을 **원문 그대로** 옮긴다.
 ★ **CC 귀속 0.** v4-08 §0-2ㄴ 의 CC 귀책(중심 선택)은 v4-09 §1-② 에서 값으로 해소됐다
   (허리.중앙 3σ 8.4889 → 0.0196 mm).
```

### §0-3 바꾸지 않는 것

```
 `src/` · 정본 · blob · v3 동결 ·
 **v4 커널 4종 로직 diff 0**(늘어남 · 굽힘 · 몸 충돌 · 봉제) · **`full.py` 로직 diff 0** ·
 **`full_color.py` diff 0**(v4-09 산출물 · 이 판의 굽힘 컬러링은 **새 파일**에 둔다) ·
 `scripts/v4CellConverge.ts` · `scripts/v4FitReport.ts` diff 0 ·
 v4-02~09 산출물 바이트 불변(이 판 산출은 새 접두 `l3dom-` · `l3cb-` 로 판다)
 ★ 계기·컬러링은 **별도 경로**다 — 원문과의 대조가 §2 의 채널이기 때문이다.
```

### §0-4 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0)

**①의 하위 갈래** (미수렴 정의역 가름)

| 갈래 | 조건 |
|---|---|
| **(가)** | 상위 100 이 근접쌍에 유의하게 몰리고 **그리고** ㄴ 의 v3 도 진동 ⟹ 원인 = 정의역(v4 결함 아님) |
| **(나)** | 몰리지 않거나 v3 는 안 진동 ⟹ 정의역 아님(다른 후보로) |
| **(다)** | 그 외 |

**②의 하위 갈래** (제품 문턱 재판정)

| 갈래 | 조건 |
|---|---|
| **(가)** | 5행 표시값·대역 **전부 일치** |
| **(나)** | **일부 불일치**(행·값을 적는다) |
| **(다)** | **전부 불일치** |

**③의 하위 갈래** (굽힘 컬러링 가능성)

| 갈래 | 조건 |
|---|---|
| **(가)** | 색 수가 늘어남과 «같은 자릿수»(**≤ 20**) |
| **(나)** | 색 수 **폭발**(값을 적는다) |
| **(다)** | 색칠 **불가** 구조 |

**회차 갈래**

| 갈래 | 조건 | 처분 |
|---|---|---|
| **A** | ①~④ 전량 | 상신 |
| **B** | ③ (나)(다) | **④ 착수 0** · ①②③ 상신 |
| **C** | ① (다) 또는 ② (다) | 값 상신 · **정지** |
| **D** | 그 외 | **정지** |

**사후 갈래 신설 0.**

### §0-5 이 판에서 «하지 않는» 것

```
 · 셀프 충돌 «이식» — 0(v4 커널에 넣지 않는다 · ①ㄱ 은 **읽기 전용 계기**다)
 · 미수렴의 원인 «단정» · 기전 귀속 · 처방(①은 **사실만**)
 · 섭동 기반 문턱 재시도 0(판정문 명시) · 문턱을 결과에 맞춰 옮기기 0(함정 14)
 · 전면 병렬화 «채택» 0(④는 **60프레임 시험**이다) · 봉제 컬러링 0
 · 캡처 0 · 굽기 0 · `src/` 수정 0 · 커널 4종·`full.py`·`full_color.py` 수정 0 · 사후 갈래 신설 0
```

### §0-6 집행 «전» 사전 결정 3건 (값을 보기 «전»에 적는다)

**ㄱ ①ㄴ 의 v3 실행 = «재실행 0»(이미 있는 산출물을 인용한다)**

```
 회차 프롬프트 ①ㄴ 은 「v3 를 «셀프 충돌 뺀» 정의역으로 정본 blob 에서 180프레임 돌려
 순변위 궤적 산출」을 지시한다. **그 실행은 v4-09 에서 이미 돌았고 산출물이 남아 있다**:
   `gpu/oracle/export/cellconv9-c100-h170-s45_M-all-e0-{sum,x0,x100}.json`
 코드 사실로 대조한 4항 — 넷 다 프롬프트 문면과 «같다»:
   ① 초기 상태 = `public/v3diag/v3-77/settled-c100-h170-s45_M.bin` = **정본 blob**
      (`scripts/v4CellConverge.ts:50-57` — 위치·속도 전량)
   ② 자기충돌 **없다** (`v4CellConverge.ts:11` 「자기충돌은 넣지 않는다 — v4 에 이식이 없다」 ·
      `step` 에 넘기는 `params` 에 `selfCollision` 키가 **없다**(`:78`))
   ③ 제약 = `CONS=all` = 늘어남 + 굽힘 + 봉제 **44,920** (= v4 의 정의역과 같다)
   ④ 프레임 = 180 (수렴 80 + 연장 100) · 창 순변위 궤적 **전량**(`trailAll` · 10프레임 간격 18점)
 ⟹ **같은 실행을 다시 돌리면 바이트 동일한 값이 나온다.** v4-09 §2-3 이 그것을 이미
   «바이트로» 증명했다(`cellconv9-…-e0-x100` 페이로드 ≡ `cellconv7-…-e0-x100` · sha `64b88027f9583280…`).
 ★ 그래서 **CPU 52분을 다시 태우지 않는다**(v4-09 실측 `ms` = 3,120,229). 대신 산출물을 인용하고
   그 사실을 여기 «집행 전»에 적는다. **값은 이미 공개돼 있으므로(v4-09 §1-①ㄱ 표) 결과를 보고
   고른 결정이 아니다.**
```

**ㄴ ①ㄱ 계기의 «협역 특징 집합» = 정점–삼각형 6개(엣지–엣지 9개 «제외»)**

```
 `src/` 는 diff 0 이므로 `solver.ts` 의 `scVT`/`scEE`(비공개 함수)를 **꺼내 쓸 수 없다**.
 그래서 계기는 **v3 가 이미 «판정»에 쓰는 계기와 «같은 특징 집합»**을 쓴다:
   `instruments.ts:66 minPairDist` — 비인접 삼각형 쌍의 거리를 **`ptTriSq` 6회**(정점 3 × 양방향)로
   재고, 그것으로 `viol`(< SEP − TOL_SELF) · `near`(< window) · `hits`(삼교차)를 낸다.
   `ptTriSq` 는 **`instruments.ts:25` 에서 export 돼 있다** ⟹ 복제 0 · 사본 0.
 광역은 회차 프롬프트가 지정한 **`solver.ts:531-573` 의 격자**를 옮겨 적는다
   (cell = mean(엣지)/3T + sep · AABB 를 thickness 씩 확장 · 최소 모서리 셀 중복 제거 · 인접 제외).
 ★ **엣지–엣지 9특징은 이 계기에 «없다»** — 있는 것보다 «세는 수가 적을» 수 있다(과소 계수).
   그 방향을 여기 적어 둔다. **판정 채널이 아니라 «사실 채널»이다.**
 ★ **기계 검증** — 이 계기의 최소 거리가 `minPairDist(pos, tris, window)` 의 `min` 과
   같은 수인지 대조한다(광역이 둘이라 «누락»이 있으면 갈린다). 눈으로 안 본다.
```

**ㄷ ④의 «직렬 60프레임» = ①ㄱ 을 위해 도는 180프레임 실행의 «앞 60프레임»을 쓴다**

```
 ①ㄱ 은 상위 100 정점의 «인덱스»가 필요하고, v4-09 의 `l3_diag.py` 는 그것을 **저장하지 않았다**
 (집계만 남겼다) ⟹ 180프레임을 한 번 다시 돌아야 한다. 그 실행이 **같은 초기 상태·같은 정의역**의
 직렬판이므로, ④의 직렬 기준선(궤적·속도·f=60 위치)을 **그 실행에서 뜬다**(별도 직렬 실행 0).
 ★ 두 실행(직렬 180 · 색분할 60)은 **GPU 를 다투지 않게 «줄 세운다»** — v4-09 가 오염된 수를
   판정에서 뺀 이유가 그것이다.
```

### §0-7 착수 «전» 알려진 사실 · 예산

```
 · 시험계 = `c100-h170-s45_M` · n **9,388** · substeps **229**
   제약 **44,920** = 늘어남 18,016 + **굽힘 26,648** + 봉제 256
 · v4-09 실측 — 늘어남 컬러링 **8색** [2978,2952,2910,2872,2826,2740,450,288] · 0.077 s ·
   8/8 정점 공유 0 기계 검증 · 최상위 루프 1 → 15
 · **굽힘이 제약의 59.3%** — ③이 (나)(다)면 ④는 착수 0 이고 병렬화의 상한이 그대로 남는다
 · 속도 기준선(clean) — v4-08 직렬 **12.693 s/프레임**(2호기 · f32 · Arch.cuda)
 · 비용 — 직렬 180프레임 약 **38분** · 색분할 60프레임은 **미지**(그래서 60으로 끊는다)
 · ①ㄱ·②·③ 은 **GPU 를 쓰지 않는다**(Node · numpy) ⟹ GPU 실행과 «겹쳐» 돌 수 있으나
   §0-6ㄷ 대로 **v4 실행 둘은 서로 줄 세운다**
```

---

## §1 집행

### §1-① 미수렴 «정의역» 가름 — **(나)** (사실만 · 처방 0 · 원인 단정 0)

#### ㄱ 상위 100 정점이 «셀프 충돌 근접쌍»에 속하는가 — **0/100 · 세 창 전부**

계기 = `scripts/v4SelfProx.ts`(신설 · **읽기 전용 · 물리 0스텝**). 광역은 `solver.ts:531-573` 을
옮겨 적었고, 협역은 v3 가 «판정»에 쓰는 `instruments.ts:66 minPairDist` 와 **같은 6특징**
(`ptTriSq` · export 됨 ⟹ 복제 0)이다. **엣지–엣지 9특징은 없다**(§0-6ㄴ 에 집행 전 등재 · 과소 계수 방향).

**기계 검증** — 두 계기가 «간극 ≤ 1.9mm» 쌍을 같은 수로 세는가

| 상태 | 이 계기 (`d < SEP−TOL_SELF`) | `minPairDist.viol` | 차 | 성긴 격자 셀공유 실패 | 차가 설명되는가 |
|---|---|---|---|---|---|
| v4@180 | 2,357 | 2,353 | **4** | **4** | ✅ |
| v3@180 | 2,253 | 2,234 | **19** | **19** | ✅ |

```
 ★ **계기 결함 «등재» 1건(수리 0 · src/ diff 0)** — `minPairDist` 의 광역은 삼각형 AABB 를
   **확장하지 않고** 격자에 넣는다(`instruments.ts:78-88`) ⟹ 셀 경계 양쪽에 각각 앉은 «가까운 쌍»을
   **놓친다**. 놓친 수를 기계로 셌더니 차와 **정확히 같다**(4/4 · 19/19).
 ★ **교차(hits) 채널은 영향 없다** — 겹치는 두 AABB 는 겹치는 점을 가지므로 **반드시 셀을 공유**한다.
   즉 게이트의 「자기관통 교차」는 이 결함에 걸리지 않는다. **viol · near · min 은 과소 계수될 수 있다.**
 ★ **처방 0.** 이 판은 src/ 를 안 고친다.
```

**v4@180 — sep(2mm) 안 근접쌍**

| 채널 | 값 |
|---|---|
| 광역 cell / 버킷 / 후보쌍 / 시간 | 0.012851 m / 9,242 / **42,732** / 107 ms |
| **sep(2mm) 안 쌍** | **2,589** |
| 전역 최소 거리 | 0.00098 mm |
| 삼각형 교차(`minPairDist.hits`) | **524** |

**「상위 100 이 몰리는가」 — 대조군과 나란히**

| 집합 | n | **근접쌍 소속** | 소속률 | 비인접 최소거리 중앙 | 최소 | **4mm 안** |
|---|---|---|---|---|---|---|
| 옷 전체 | 9,388 | **1,152** | **12.27%** | 6.401 mm | 0.00098 mm | 1,596 |
| **대조군 임의 100**(LCG 씨앗 12345) | 100 | **14** | **14.0%** | 6.140 mm | 0.0038 mm | 22 |
| 상위100 `f150→f160` | 100 | **0** | **0.0%** | 6.464 mm | **5.418 mm** | **0** |
| 상위100 `f160→f170` | 100 | **0** | **0.0%** | 6.252 mm | **5.931 mm** | **0** |
| 상위100 `f170→f180` | 100 | **0** | **0.0%** | 6.137 mm | **5.633 mm** | **0** |

```
 ★ **몰리지 않는다 — 반대로 «한 개도 없다».** 옷 전체가 12.27% · 임의 100 이 14.0% 인데
   상위 100 은 **세 창 모두 0/100** 이고, 그 정점들의 비인접 최소거리는 **전부 5.4mm 이상**이다
   (sep 2mm 의 2.7배 밖 · 4mm 안도 0/100).
 ★ v4-09 §1-①ㄴ 과 합치면 상위 100 은 **세 접촉 채널 «전부»에서 0** 이다 —
   **봉제 0/100 · 몸 충돌 0/100 · 셀프 근접 0/100.** 「자유롭게 늘어진 천」이라는 v4-09 의 사실이
   셀프 충돌 축에서도 같은 방향으로 나왔다. **기전 귀속 0 · 원인 단정 0.**
```

**(참고 · 판정 채널 아님) v3@180 을 같은 계기로**

| 채널 | v3@180 | v4@180 |
|---|---|---|
| 후보쌍 | 42,891 | 42,732 |
| sep 안 쌍 | **2,438** | **2,589** |
| 근접쌍 소속 정점 | **1,089 / 9,388 (11.60%)** | **1,152 / 9,388 (12.27%)** |
| 대조군 임의 100 | 15 / 100 | 14 / 100 |
| 삼각형 교차 | **428** | **524** |
| 전역 최소 거리 | 0.00016 mm | 0.00098 mm |

```
 ★ **이 정의역은 v3 에서도 «자기관통» 상태다**(교차 428 · sep 안 2,438쌍) — 셀프 충돌을 뺐으니
   놀랄 값이 아니다. **그런데 v3 는 그 상태에서 «수렴한다»**(아래 ㄴ). 사실만 적는다.
```

#### ㄴ 같은 정의역의 v3 궤적 — **진동 0** (재실행 0 · §0-6ㄱ 대로 인용)

인용 산출물 = `cellconv9-c100-h170-s45_M-all-e0-{sum,x100}.json`
(정본 blob 출발 · 자기충돌 없음 · 제약 `all` 44,920 · 180프레임 · 창 순변위 궤적 전량).

| f | 10 | 20 | 40 | 60 | **80** | 100 | 120 | 140 | 160 | **180** |
|---|---|---|---|---|---|---|---|---|---|---|
| v3 창 순변위 (m) | 1.600e-02 | 3.992e-03 | 7.531e-04 | 2.531e-04 | **8.067e-05** ← 수렴 선언 | 3.572e-05 | 1.185e-05 | 5.412e-06 | 2.150e-06 | **6.935e-07** |

```
 ★ 18점 전부 **감소**(증가 구간 0)로 4.4 자릿수 내려간다 ⟹ **v3 는 이 정의역에서 «진동하지 않는다».**
   같은 자리 v4 는 4.59e-03 ~ 3.00e-02 사이를 오르내린다(v4-09 §1-①ㄱ · 이 판이 18/18 점 재현).
```

#### 갈래 판정 — **(나)**

```
 사전 등재 (가) = 「상위 100 이 근접쌍에 유의하게 몰리고 **그리고** ㄴ 의 v3 도 진동」
 실측 — 상위 100 의 근접쌍 소속 **0/100**(대조군 14/100 · 전체 12.27%) ⟹ 「몰린다」가 **부정**
      — v3 는 같은 정의역에서 **수렴한다** ⟹ 「v3 도 진동」이 **부정**
 ⟹ **두 연언지가 «둘 다» 거짓** ⟹ 갈래 **(나)** = 「정의역 아님(다른 후보로)」
 ★ **다른 후보를 «지정하지 않는다»** — 회차 프롬프트가 ①을 「사실 · 처방 0」으로 잠갔다.
 ★ 판정문의 「미수렴 후보 = 정의역」은 **이 판의 값으로 부정됐다.** 귀책 등재는 **하지 않는다** —
   판정문 자신이 「후보이지 단정 0」이라고 적었기 때문이다(§0-1 원문).
```

### §1-② 제품 문턱 «도입» — **(나)**

#### 규칙을 코드에서 «인용»한다 (손 상수 0 · 새 수 0)

| 규칙 조각 | 코드 자리 | 인용값 |
|---|---|---|
| 표시 반올림 (제품 화면 v1) | `src/components/V3ProductV1.tsx:348` | `r.medMm.toFixed(1)` + "mm" |
| 표시 반올림 (진단 패널) | `src/components/FitReportTable.tsx:46-47` | `toFixed(2)` (중앙 · p25~p75) |
| 대역 이름 | `src/components/V3ProductV1.tsx:52-59` `phaseOf` | 눌림 `< 0` · 밀착 `0 ≤ · ≤ sepMm` · 여유 `> sepMm` |
| **대역 경계** | `src/v3/consts.ts:6,8` | `THICK = 1e-3` · `SEP = 2 × THICK` ⟹ **2.0 mm** |
| 정점 대역 소속 (v3-54) | `src/v3/fitReport.ts:98-100` | `pressN q<0` · `snugN 0≤q≤SEP` · `looseN q>SEP` |
| 대역 «정의역» | `src/v3/fitReport.ts:80-86` `bandOf` | 몸통 패널 · `abs(y − cy) ≤ d`(9 mm) |
| 게이트 문턱 | `src/v3/s4Gate.ts:47-57` `S4_THRESHOLD` | pen 5e-4 · 교차 0 · 초과비 1 · settle `TOL_SELF` 1e-4 · pinned 0 |
| 게이트 판정 | `src/v3/s4Gate.ts:142-148` | `fails` 를 쌓고 `pass = fails.length === 0` |
| 창 정의 | `src/v3/s4Gate.ts:73` | `N_WIN = round(1/(DAMP·DT))` = **10** |

계기 = `scripts/v4ProductCmp.ts` · `scripts/v4ProductGate.ts`(둘 다 신설 · **물리 0스텝**).
리포트 값은 v4-08 산출 `fit-…-v3-e0-x100.json`(= v3@180) · `fit-…-v4-f180.json`(= v4@180) **그대로**다
(새 시뮬레이션 0). 게이트는 그 두 상태를 `runS4Gate` 에 **읽기만** 시켜 낸 값이다.

#### 재판정 — v3@180 ↔ v4@180

| 행 | 표시값 1자리 (v3 / v4) | 같은가 | 대역 이름 | 같은가 | 진단 2자리 (v3 / v4) | 정점 대역 [눌림,밀착,여유] | 같은가 | 대역 n |
|---|---|---|---|---|---|---|---|---|
| 목선 | **1.1mm / 1.8mm** | ❌ | 밀착 / 밀착 | ✅ | 1.06 (1.00~1.93) / 1.85 (1.00~3.57) | [0,41,13] / [0,31,23] | ❌ | 54 / 54 ✅ |
| 가슴 | **4.0mm / 5.1mm** | ❌ | 여유 / 여유 | ✅ | 4.03 (1.00~15.24) / 5.13 (1.29~14.80) | [0,111,150] / [0,87,178] | ❌ | **261 / 265** ❌ |
| 허리 | **26.7mm / 28.3mm** | ❌ | 여유 / 여유 | ✅ | 26.73 (15.70~70.54) / 28.29 (16.85~64.47) | [0,0,228] / [0,0,225] | ❌ | **228 / 225** ❌ |
| 밑단 | **19.1mm / 22.6mm** | ❌ | 여유 / 여유 | ✅ | 19.14 (7.04~39.01) / 22.58 (6.31~36.63) | [0,8,102] / [0,10,100] | ❌ | 110 / 110 ✅ |
| 소매 | **4.5mm / 4.7mm** | ❌ | 여유 / 여유 | ✅ | 4.50 (1.02~16.32) / 4.70 (1.17~12.35) | [0,323,595] / [0,299,619] | ❌ | 918 / 918 ✅ |
| **합계** | **0/5** | | **5/5** | | **0/5** | **0/5** | | **3/5** |

#### 게이트 판정 다리 (`runS4Gate` · 물리 0스텝)

| 채널 | 문턱 | **v3@180** | **v4@180** |
|---|---|---|---|
| ③a 관통 최대 | ≤ 0.5 mm | 0.4195 (963정점) ✅ | 0.4025 (593정점) ✅ |
| **자기관통 교차** | = 0 | **428** ❌ | **524** ❌ |
| 목선 초과비 | ≤ 1 | 0.9891 ✅ | 0.9969 ✅ |
| 보조 장치(invMass=0) | = 0 | 0 ✅ | 0 ✅ |
| 발산 | 없음 | 없음 ✅ | 없음 ✅ |
| **정착 창 순변위**(인용) | ≤ 0.1 mm | **0.00069 mm** ✅ | **14.2843 mm** ❌ |
| **`pass`** | — | **false** | **false** |
| **`fails`** | — | 교차 **1건** | 교차 + 정착 **2건** |

```
 ★ `pass` 값은 **같다**(둘 다 실패) — 그러나 **실패 «사유 목록»이 다르다**(1건 ↔ 2건).
 ★ 이 정의역(셀프 충돌 없음)에서는 **v3 도 게이트를 통과하지 못한다**(교차 428).
   ⟹ 「게이트 판정 일치」를 **통과/불통과 값**으로 읽으면 «일치», **fails 집합**으로 읽으면 «불일치»다.
   **어느 쪽으로 읽을지는 규칙을 쓴 전략 세션 몫**이고 CC 가 고르지 않았다 — **둘 다 적는다.**
```

#### 갈래 판정 — **(나)**

```
 (가) = 「5행 표시값·대역 «전부» 일치」 — 표시값이 0/5 이므로 **부정**
 (다) = 「전부 불일치」 — **대역 이름이 5/5 일치**하고 대역 정의역 n 도 3/5 일치하므로 **부정**
 ⟹ 갈래 **(나) = 일부 불일치.** 불일치 목록(행·값)은 위 표 그대로:
    표시값 **5행 전부** · 진단 2자리 **5행 전부** · 정점 대역 소속 **5행 전부** ·
    대역 정의역 n **가슴(261↔265) · 허리(228↔225)** · 게이트 fails 집합(1건 ↔ 2건)
    일치: **대역 이름 5/5** · 대역 정의역 n 3/5 · 게이트 `pass` 값
 ★ **문턱을 옮기지 않았다 · 자리수를 고르지 않았다**(둘 다 코드에서 인용 · 함정 14).
```

### §1-③ 굽힘 컬러링 «가능성» — **(가)** (실행 0 · GPU 0)

계기 = `gpu/color_probe.py`(신설). 컬러링 함수는 v4-09 의 `engine/full_color.py:greedy_color` 를
**그대로 import** 한다(재작성 0 · 손 상수 0 · 색 수는 «결과»이지 입력이 아니다).

| 제약 | 개수 | 정점/제약 | **색 수** | 컬러링 시간 | 정점공유 위반 색 | 색별 제약 수 | 하한(클리크) |
|---|---|---|---|---|---|---|---|
| 늘어남 | 18,016 | 3 | **8** | 0.058 s | **0 / 8** | [2978, 2952, 2910, 2872, 2826, 2740, 450, 288] | **6** |
| **굽힘** | **26,648** | **4** | **14** | 0.087 s | **0 / 14** | [2252 × 7, 2194, 2206, 2168, 2110, 2068, 80, 58] | **12** |
| (참고) 봉제 | 256 | 2 | 3 | 0.008 s | 0 / 3 | [248, 6, 2] | 2 |

```
 ★ **늘어남 8색은 v4-09 와 «같은 수·같은 색별 크기»로 재현됐다**(계기 재현성 확인 · 새 수 0).
 ★ **기계 검증** — 색마다 정점 인덱스를 늘어놓아 중복이 0인지 셌다. **8/8 · 14/14 · 3/3 전부 0.**
   ⟹ 색 «안» 동시 갱신 = 가우스–자이델과 같은 결과(쓰기 충돌이 원리적으로 0).
 ★ **하한도 «기계로» 냈다** — 한 정점에 걸린 제약들은 서로 이웃(클리크)이므로 색 수 ≥ 정점 최대 소속 수다.
   굽힘의 정점 최대 소속 = **12** ⟹ 색 수 ≥ 12. 탐욕이 낸 **14 는 하한 + 2** 다.
   (늘어남은 최대 소속 6 ⟹ 하한 6 · 탐욕 8.)
 ⟹ **14 ≤ 20** ⟹ 사전 등재 갈래 **(가)** = 「색 수가 늘어남과 같은 자릿수」 ⟹ **④ 착수**.
```

### §1-④ 병렬 실행 시험 — 늘어남 8색 + 굽힘 14색 · 60프레임

계기 = `gpu/engine/full_color2.py` + `gpu/l3cb_run.py`(신설). `FullColor2` 가 `full.Full` 을 **상속**하고
`full_color.greedy_color` 를 import 한다 ⟹ **투영 산술 4종 한 줄도 다시 쓰지 않았다**
(`full.py` · `full_color.py` **바이트 불변**). 직렬 기준선은 §1-①ㄱ 을 위해 돈 `l3_dom.py` 180프레임
실행의 **앞 60프레임**이다(§0-6ㄷ 대로 · 별도 직렬 실행 0 · **두 실행은 GPU 를 다투지 않게 줄 세웠다**).

#### ㄱ 최상위 루프 구성 (Taichi 는 최상위 루프만 병렬화한다)

| 단계 | 직렬 `full.py` | v4-09 `full_color.py` | **이 판 `full_color2.py`** |
|---|---|---|---|
| 예측 · λ초기화(3) · 몸충돌 · 속도갱신 | 직렬 | 병렬 6 | 병렬 6 |
| 늘어남 18,016 | 직렬 1 | **병렬 8색** | **병렬 8색** |
| 굽힘 26,648 | 직렬 1 | 직렬 1 | **병렬 14색** |
| 봉제 256 | 직렬 1 | 직렬 1 | 직렬 1 |
| **최상위 루프 / 서브스텝** | **1** | **15** | **29** |

#### ㄴ 속도 — **빨라지지 않았다**

| 채널 | **직렬**(`l3dom`) | **색분할2**(`l3cb`) | 배수 |
|---|---|---|---|
| 60프레임 벽시계 | **761.415 s** | **824.604 s** | **1.083배 «느리다»** |
| s/프레임(60프레임 평균) | **12.690** | **13.744** | 1.083배 느리다 |
| 첫 창(f=10) | 126.768 s | **182.773 s** | 1.442배(**JIT 컴파일 약 56 s 포함**) |
| 정상 상태(2~6창 · 50프레임) | **12.693 s/프레임** | **12.837 s/프레임** | **1.011배 «느리다»** |
| (참고) 직렬 180프레임 전량 | 2,296.4 s · **12.758 s/프레임** | — | — |
| (참고) v4-08 등재 clean 직렬 | 12.693 s/프레임 | — | — |

```
 ★ **산수만 등재(원인 단정 0)** — 한 서브스텝의 최상위 루프가 1 → 29 로 늘었다.
   서브스텝 229 · 60프레임 ⟹ 색분할판은 GPU 작업을 **29 × 229 × 60 = 398,460회** 띄운다.
   직렬판은 `run` 커널을 창마다 한 번, 즉 60프레임에 **6회** 띄운다(그 안에서 전부 도는 한 덩어리).
   **이 수가 배수를 설명하는지는 «말하지 않는다»** — 이 판은 launch 비용을 따로 재지 않았다.
 ★★ v4-09 가 «집행 전» 걸어 둔 상한(굽힘 26,648 = 제약의 **59.3%** 가 직렬로 남는다)은
   이 판에서 **제거됐는데도** 배수가 안 났다 ⟹ 「굽힘이 직렬이라 배수가 안 난다」는
   **이 판의 값으로 성립하지 않는다.** 다음 층위가 어디인지는 **말하지 않는다.**
```

#### ㄷ 창 순변위 궤적 — 같은 크기의 진동 · 발산 0

| f | 10 | 20 | 30 | 40 | 50 | 60 |
|---|---|---|---|---|---|---|
| **직렬** | 2.651729e-02 | 2.349800e-02 | 2.299182e-02 | 3.003855e-02 | 1.937818e-02 | 2.705761e-02 |
| **색분할2** | 2.714664e-02 | 2.366242e-02 | 2.044033e-02 | 3.043853e-02 | 2.012350e-02 | 2.974919e-02 |
| 비 | 1.024 | 1.007 | 0.889 | 1.013 | 1.038 | 1.099 |

```
 NaN 0 · 발산 0 · 양쪽 다 진동(단조 0) · 비는 0.889 ~ 1.099 ⟹ **궤적의 «모양»이 같다.**
 ★ 「안정하다」고 **단정하지 않는다** — 60프레임 6점이다.
```

#### ㄹ 좌표차 · 리포트 대조(제품 문턱 적용 · f=60)

| 채널 | 값 |
|---|---|
| 좌표차 중앙 / p99 / 최대 | **1.013 / 8.515 / 19.094 mm**(최대 정점 3518) |
| 평균 변위(색분할 − 직렬) | (−0.406, −0.348, −0.166) mm |
| NaN | 0 |

| 행 | 표시값 1자리 (직렬/색분할) | 대역 이름 | 진단 2자리 | 정점 대역 소속 | 대역 n |
|---|---|---|---|---|---|
| 목선 | 1.6 / **1.4** ❌ | 밀착/밀착 ✅ | 1.59 (1.00~3.04) / 1.40 (1.01~2.64) ❌ | [0,30,24] / [0,33,21] ❌ | 54/54 ✅ |
| 가슴 | 4.9 / **4.8** ❌ | 여유/여유 ✅ | 4.89 (1.20~15.07) / 4.82 (1.23~15.02) ❌ | [0,88,175] / [0,92,175] ❌ | **263/267** ❌ |
| 허리 | 27.3 / **27.6** ❌ | 여유/여유 ✅ | 27.32 (16.19~65.50) / 27.63 (17.91~65.77) ❌ | [0,0,225] / [0,0,226] ❌ | **225/226** ❌ |
| 밑단 | 23.0 / **22.9** ❌ | 여유/여유 ✅ | 22.96 (7.49~36.22) / 22.90 (7.27~37.18) ❌ | [0,5,105] / [0,5,105] **✅** | 110/110 ✅ |
| 소매 | 4.7 / **4.5** ❌ | 여유/여유 ✅ | 4.68 (1.23~12.79) / 4.49 (1.18~12.21) ❌ | [0,293,625] / [0,300,618] ❌ | 918/918 ✅ |
| **합계** | **0/5** | **5/5** | **0/5** | **1/5** | **3/5** |

```
 ★ 제품 문턱으로 읽으면 **「같은 옷」이 아니다**(표시값 0/5). 차는 **0.1~0.3 mm** 규모다.
 ★★ **다만 이 대조의 «시점»은 f=60 이고 그 자리는 «수렴하지 않은» 상태다**(창 순변위 2.7e-02 m).
   §1-① 이 보인 대로 v4 는 180프레임까지 진동한다 ⟹ **정착 상태의 대조가 아니다.**
   「색 순서 때문에 갈렸다」와 「진동하는 계라 어차피 갈린다」를 **이 값으로는 못 가른다.**
   **귀속 0 · 단정 0.**
```

---

## §2 자기검사

### §2-1 집행 / 미집행 총계

| # | 항목 | 집행 | 결과 |
|---|---|---|---|
| ①ㄱ | 상위 100 ↔ 셀프 근접쌍 소속 · 대조군 | ✅ | **0/100 · 세 창 전부**(대조군 14/100 · 전체 12.27%) |
| ①ㄴ | 같은 정의역 v3 궤적 180프레임 | ✅ **인용**(재실행 0 · §0-6ㄱ) | 18점 전부 감소 · **진동 0** · 6.935e-07 |
| **①** | **갈래** | ✅ | **(나)** — 정의역 아님 |
| ② | 제품 문턱 인용·재판정 + 게이트 다리 | ✅ | **(나)** — 표시값 0/5 · 대역이름 5/5 · 정점대역 0/5 · n 3/5 · 게이트 `pass` 같고 `fails` 다름 |
| ③ | 굽힘 컬러링 가능성 | ✅ | **(가)** — **14색**(하한 12) · 정점공유 0 기계검증 14/14 |
| ④ | 늘어남+굽힘 둘 다 색분할 · 60프레임 | ✅ | **1.083배 «느리다»**(정상상태 1.011배 느림) · 발산 0 · 좌표차 중앙 1.013mm |

**미집행(의도)** — 셀프 충돌 «이식» 0 · 봉제 컬러링 0 · 전면 병렬화 «채택» 0 · 섭동 문턱 재시도 0 ·
원인 단정·기전 귀속·처방 0 · 후보 지정 0 · `src/` 수정 0 · 캡처 0 · 굽기 0 · 사후 갈래 신설 0.
**미집행(중단) — 없다.** 이 판은 **네 항 전부** 돌았다(v4-09 의 미완 두 건도 이 판에서 «다른 형태로» 닫혔다:
①ㄷ f64 는 이 판의 정의역 밖이라 **여전히 미집행**이고, ③ 색분할 실행은 ④가 **더 넓은 판**으로 돌았다).

**★ 여전히 남은 것 1건** — **①ㄷ f64 80프레임**(v4-09 사전 결정분). 이 판의 회차 프롬프트가
그것을 부르지 않았으므로 **착수하지 않았다**(정의역 밖). **2호기 전용**(Metal f64 미지원).

### §2-2 정밀도 · arch · 시간 (전부 2호기 `DESKTOP-OV72A46` · CUDA)

| 실행 | 정밀도 | arch | 프레임 | 시작 | 벽시계 | s/프레임 | 오염 |
|---|---|---|---|---|---|---|---|
| `l3_dom.py` 직렬 | f32 | `Arch.cuda` | **180** | 14:20:56 | **2,296.4 s** | **12.758** | **없음**(창별 126.8~128.0 s) |
| `l3cb_run.py` 색분할2 | f32 | `Arch.cuda` | **60** | 15:00:14 | **824.6 s** | **13.744** | **없음**(직렬 실행 종료 후 시작) |
| `color_probe.py` | — | CPU | — | — | 0.15 s | — | GPU 미사용 |
| `v4SelfProx.ts` ×2 | f64 | CPU(Node) | 0스텝 | — | 9.5 s / 회 | — | 직렬 실행의 **f=60 통과 뒤**에 돌렸다 |
| `v4ProductGate.ts` ×2 · `v4ProductCmp.ts` ×2 · `v4FitReport.ts` ×3 | f64 | CPU(Node) | 0스텝 | — | 각 ~10 s | — | 같음 |

```
 ★ **GPU 실행 둘은 겹치지 않았다**(직렬 14:20:56~14:59:13 · 색분할2 15:00:14~15:13:59) — §0-6ㄷ 대로.
 ★ Node 계기는 전부 **직렬 실행의 f=60 창이 끝난 뒤**(14:33:38 이후)에 돌렸다 ⟹ ④의 직렬 기준선
   60프레임 구간(126.8·126.6·127.1·127.0·127.0·126.9 s)에는 **다른 프로세스가 없었다**.
```

### §2-3 경계 · diff

| 채널 | 값 |
|---|---|
| `src/` diff (기점 `51a136b`) | **0 파일** — `git diff --stat 51a136b -- src/` 가 비었다 |
| **커널 4종 + `full.py` diff** | **0** — `full_color2.py` 는 **상속**이고 원문을 안 고쳤다 |
| **`full_color.py`(v4-09 산출) diff** | **0** — 이 판은 `greedy_color` 를 import 만 한다 |
| `scripts/v4FitReport.ts` · `v4CellConverge.ts` diff | **0 / 0** |
| **v4 물리 diff 0 증명 ①** | `l3_dom.py` 창 순변위가 v4-09 `l3_diag.py` 와 **18/18 지점 인쇄 자릿수 전량 동일** |
| **v4 물리 diff 0 증명 ②** | `l3_dom` f=180 상태의 **핏 리포트가 v4-08 산출 `fit-…-v4-f180.json` 과 «완전 동일»**(키·값 전량) |
| **v3 재실행 0 근거** | `cellconv9-…-e0` 인용(§0-6ㄱ · 코드 사실 4항 대조) |
| **①ㄱ 계기 기계검증** | 이 계기 ↔ `minPairDist` 의 1.9mm 쌍 수 차가 **성긴 격자 셀공유 실패 수와 정확히 일치**(4/4 · 19/19) |
| **③ 기계검증** | 색 안 정점 공유 **0** — 늘어남 8/8 · 굽힘 14/14 · 봉제 3/3 |
| 정본 blob | `settled-*.bin` **39개** · 몸 **27개** · 시험칸 상태 sha `9d3b07e4…` — **pytest `test_oracle.py` 통과** |
| **pytest** | **40 passed · 2 xfailed · 1 xpassed**(60.8 s · GPU 비운 뒤 실행) |
| `npx tsc -b` | **통과**(exit 0) |
| v4-02~09 산출물 | **바이트 불변**(이 판 산출 접두 = `l3dom-` · `l3cb-` · `l3prox-` · `l3-color-*` · `l3-product-cmp-` · `l3-gate-` · `fit-…-v4dom-` · `fit-…-v4cb-`) |
| 호출 ecc 에이전트 / 스킬 | **0 / 0** |
| 캡처 | **0장** |
| 굽기 | **0**(SDF 재굽기 0 · 몸 blob 0) |

**신설** — `gpu/l3_dom.py` · `gpu/color_probe.py` · `gpu/engine/full_color2.py` · `gpu/l3cb_run.py` ·
`scripts/v4SelfProx.ts` · `scripts/v4ProductCmp.ts` · `scripts/v4ProductGate.ts` ·
`l3dom-…-f{60,180}.{bin,json,npz}` · `l3cb-…-f60.{bin,json}` · `l3prox-…-{v3-x180,v4-f180}.json` ·
`l3-color-probe.json` · `l3-color-diff-f60.json` · `l3-gate-…-{v3-x180,v4-f180}.json` ·
`l3-product-cmp-{v3x180-vs-v4f180,serial60-vs-color60}.json` · `fit-…-{v4dom-f180,v4dom-f60,v4cb-f60}.json`
**수정** — **없다**(`src/` 0줄 · 기존 스크립트 0줄 · `.gitignore` 0줄)
**정정** — 없음. **원문 무삭제 · 새 함정 0 · 새 규범 0 · 문턱 이동 0 · 사후 갈래 신설 0.**

```
 ★ `l3dom-…-f180.npz`(2.2 MB)는 **커밋한다** — 창 18개의 위치 전량이 들어 있어, 다음 판이
   같은 38분 실행을 다시 돌지 않고 상위 100·창 대조를 할 수 있다(이 판이 그것이 없어서 다시 돌았다).
```

### §2-4 계기 결함 «등재» 1건 (수리 0 · `src/` diff 0)

```
 `src/v3/instruments.ts:66 minPairDist` 의 **광역이 불완전하다** — 삼각형 AABB 를 **확장하지 않고**
 균일 격자에 넣으므로(`:78-88`), 셀 경계 양쪽에 각각 앉은 «가까운 쌍»을 **놓친다.**
   실측 — v3@180 에서 **19쌍** · v4@180 에서 **4쌍**(간극 ≤ 1.9mm 인 쌍 기준 · 기계로 셈)
 영향 범위 — `viol` · `near` · `min` 은 **과소 계수될 수 있다.**
 영향 «없는» 곳 — `hits`(삼각형 교차). 겹치는 두 AABB 는 겹치는 점을 가지므로 **반드시 셀을 공유**한다
   ⟹ 게이트의 「자기관통 교차 = 0」 채널은 이 결함에 **걸리지 않는다.**
 대조 — `solver.ts:531-573`(실제 자기충돌 해소가 쓰는 광역)은 AABB 를 thickness 씩 **확장**하므로
   sep 안 쌍을 **원리적으로 놓치지 않는다**(그 파일의 주석이 그렇게 도출한다).
 ★ **수리하지 않았다**(이 판은 `src/` diff 0). **처방 0 · 다른 회차 판정 회수 0** —
   과거 회차가 `minPairDist` 의 `viol`·`near`·`min` 을 판정에 쓴 자리가 있는지는 **이 판이 안 훑었다.**
```

---

## §3 캡처 — **0장**(§0-5)

---

## §4 판정 — **갈래 A**

```
 ① = **(나)**  ② = **(나)**  ③ = **(가)**  ④ = 착수·완주(60프레임)
 ⟹ 회차 갈래 표에서 **A**(①~④ 전량) — B(③ (나)(다)) · C(① 또는 ② 가 (다)) · D 는 전부 불성립.
 ★ 사후 갈래 신설 0 · 갈래에 없는 결과를 만들어 넣지 않았다.
```

### 상신 사실 (전략 세션 몫 · CC 는 처방하지 않는다)

```
 ㉠ ★★ **「미수렴 후보 = 정의역」이 부정됐다.** 진동하는 상위 100 정점은 셀프 충돌 근접쌍에
   **한 개도 속하지 않는다**(0/100 · 세 창 전부 · 대조군 14/100 · 옷 전체 12.27%). 그 정점들의
   비인접 최소거리는 **전부 5.4mm 이상**(sep 2mm 의 2.7배 밖)이다. 그리고 **같은 정의역의 v3 는
   진동하지 않는다**(18점 전부 감소 · 6.935e-07 까지). ⟹ 사전 등재 **(나)**.
   v4-09 와 합치면 상위 100 은 **세 접촉 채널 전부 0** 이다 — 봉제 0 · 몸 충돌 0 · 셀프 근접 0.
   **다음 후보는 CC 가 지정하지 않는다.**
 ㉡ ★ **이 정의역은 v3 에서도 자기관통 상태다**(교차 428 · v4 524) — 그런데 v3 는 «수렴한다».
   ⟹ 「자기관통 상태 자체」가 미수렴을 만드는 것은 아니다(사실 · 기전 0).
 ㉢ ★★ **제품 문턱은 «세울 수 있다» — 그러나 이 정의역에서는 v3 도 게이트를 못 넘는다.**
   규칙 세 다리 전부 코드에서 «인용»으로 세워졌고(손 상수 0), v3@180 ↔ v4@180 은 **(나) 일부 불일치**다
   (표시값 0/5 · 대역이름 5/5 · 정점대역 0/5 · 대역 정의역 n 3/5). 게이트는 **둘 다 실패**인데
   **fails 집합이 다르다**(v3 = 교차 1건 · v4 = 교차 + 정착 2건).
   ⟹ **넘기는 판단 1건**: 「게이트 판정 일치」를 **`pass` 값**으로 읽을지 **`fails` 집합**으로 읽을지.
   그리고 **셀프 충돌이 빠진 정의역에서는 「같은 옷」의 셋째 다리가 «양쪽 다 실패»로 포화**한다 —
   판별력이 있는 다리인지 전략 세션이 정해야 한다. **CC 가 고르지 않았다.**
 ㉣ ★ **굽힘도 색이 선다** — **14색**(하한 12 · 탐욕이 하한+2) · 정점 공유 0 **14/14 기계 검증** ·
   0.087 s. 늘어남 8색은 v4-09 와 **같은 수·같은 크기로 재현**됐다.
 ㉤ ★★ **그런데 배수가 «안 난다».** 늘어남+굽힘을 «둘 다» 병렬로 돌린 60프레임이
   **824.6 s vs 직렬 761.4 s = 1.083배 «느리다»**(JIT 56 s 를 빼도 정상 상태 **1.011배 느림**).
   ⟹ v4-09 가 집행 전 걸어 둔 상한(**굽힘 59.3% 직렬**)은 **제거됐는데도** 배수가 없었다 ⟹
   **「굽힘이 직렬이라 느리다」는 성립하지 않는다.** 산수 하나만 등재한다 —
   최상위 루프가 **1 → 29** 로 늘어 GPU 작업 launch 가 60프레임에 **6회 → 398,460회**가 된다.
   **이 수가 배수를 설명하는지는 CC 가 말하지 않는다**(launch 비용 미측정).
 ㉥ ★ **계기 결함 1건 등재(수리 0)** — `minPairDist` 의 광역이 AABB 확장 없이 격자에 넣어
   가까운 쌍을 **놓친다**(v3@180 19쌍 · v4@180 4쌍 · 기계로 셈). `hits`(교차)는 영향 없다.
   과거 회차가 `viol`·`near`·`min` 을 판정에 쓴 자리가 있는지는 **이 판이 안 훑었다** — 회수 대상 판단은 전략 세션 몫.
 ㉦ 남은 실행 1건 — **①ㄷ f64 80프레임**(v4-09 사전 결정 · 약 85분 · **2호기 전용**).
   이 판의 정의역 밖이라 착수하지 않았다.
```
