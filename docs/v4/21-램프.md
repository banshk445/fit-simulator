# v4-21 — 봉제 램프 이식 · T포즈 재생 검증 · A포즈 굽기 탐침 · 캡처

**집행 기계 = 2호기 · 물리 식 0줄 · `garmentScene` 0 · 정본·blob·문턱 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-21-ramp` |
| 기점 | **`f10b399`**(회차 프롬프트 지정 = v4-20 §1~§4) + **`184e87d`** cherry-pick(= v4-20 sha 자기 기입 · 문서 4줄) |
| §0 커밋 | **`cf0b69b`** |
| §1~§4 커밋 | **`29df357`** — 워커 램프 단계 · 계기 2 · 산출물 · 본문 · 보고 파일 · 볼트 3종 · metrics **한 커밋** |
| 보고 파일 | `docs/v4/보고/21.md` |

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v4-21-ramp f10b399 → Switched to a new branch 'v4-21-ramp'
       $ git branch --show-current → v4-21-ramp · HEAD f10b399
       $ git cherry-pick 184e87d → 2 files changed(문서 4줄) · HEAD 12821d3
 2호기 (§0 커밋을 push 한 «뒤» 같은 자리로 전환 — v4-19·20 의 어긋남 재발 금지)
```

★ **기점 사실 1건** — 회차 프롬프트가 준 `f10b399` 는 v4-20 의 §1~§4 커밋이고, v4-20 의 실제 HEAD 는
그 자식 **`184e87d`**(노트 머리에 sha 를 적은 문서 커밋)다. **프롬프트가 준 자리에서 갈라내고**
그 문서 커밋을 **cherry-pick 해 잃지 않았다**(값 변경 0).

---

## §0 수령

### §0-1 판정문 (전략 세션 · v4-20 §4 수령 · 원문)

```
 「갈래 C 수령 · ★ 모드는 섰다(조건 2/2 · 기존 모드 비트 동일) — 실패한 것은 전략 세션이 정의한
   파이프라인 · ★★ 봉제 램프 읽기 «확정»(중앙 차 전부 + · 목선 1.0158 · 교차 8 이 한 자리를 가리킴) —
   굽기 = 「조립 → 램프 → 정착」이었고 가운데를 전략 세션이 빠뜨렸다 · 귀책 = 전략 세션(v4-19 ③ 문언) ·
   ★ 처방 = 램프를 v3 규칙 그대로 이식(dressRun.ts:112-117 · RAMP_N · 손 상수 0) ·
   ★ 던지는 커널 설계가 값을 드러냄(조용한 누락 0) 등재 · 용량 호출부 한정 수용 ·
   CC §0 사후 커밋 자기신고 수용(판정 영향 0)」
```

**귀책 등재 1건** — 「굽기 파이프라인의 가운데(램프)를 빠뜨린 문언」(v4-19 ③) = **전략 세션**(판정문 자인).

### §0-2 바꾸지 않는 것 (회차 프롬프트 원문)

```
 `solver`·`engine` **물리 식 0줄** · `garmentScene` **0** · 정본·blob·문턱 **0** ·
 워커 **기존 모드·조립 모드(v4-20 상태) diff 0** — 램프는 «추가 단계»다.
 ★ 램프는 **rest 값 «스케줄»**이다 — 커널은 `sm_rest` 를 «읽기만» 하고, 그 배열을 **호출부**가
   프레임마다 갱신한다(커널 코드 0줄 · 필드 정의 0줄).
```

### §0-3 인용 자리 (손 상수 0 · 이 판이 옮기는 것의 정본)

```
 `src/v3/dressRun.ts:111`  rest0[k] = |pos_i − pos_j|  ← **조립 «직후» 위치**에서 잰 봉제 쌍 거리
 `src/v3/dressRun.ts:112`  RAMP_N = ceil((max(rest0) − SEP) / (G · DT²))
 `src/v3/dressRun.ts:113-116` setRest(f): t = min(1, f / RAMP_N) ·
                              rest[k] = rest0[k] + (SEP − rest0[k]) · t      ← **선형**
 `src/v3/dressRun.ts:177-178` 호출 순서 = **`setRest(startFrame + f + 1)` «먼저», 그다음 `step`**
                              ⟹ 프레임 f(0부터)의 t 는 **(f+1)/RAMP_N**
 적용 대상 = **`sc.seamCons`(dist 제약)뿐** — 늘어남·굽힘·충돌은 손대지 않는다.
 상수는 전부 «있는 것»에서 뜬다 — G·DT 는 장면 헤더, SEP = 2×THICK.
```

### §0-4 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · 단위 명시)

**★ 단위** — ①②는 **칸 1개**(정착까지) · 프레임 = 1/60 s.

| 항 | 갈래 |
|---|---|
| **①** 램프 이식 + T포즈 재생 | **(가)** 「같은 옷」 3조건 성립 **+ 게이트 `pass`·`fails` 일치** / **(나)** 일부 불일치(행·채널·값) / **(다)** 미정착·발산(값) |
| **②** A포즈 굽기 탐침 | **(가)** 게이트 pass / **(나)** fail(채널·값 · 처방 0) / **(다)** 발산·미정착 |
| **③** 캡처 | 앞·옆 2장 · **불가면 캡처 0 · 사유** |

**§4** — A = ①(가)+②(가) / B = ②(나)(다) / C = ①(나)(다) ⟹ **② 착수 0** / D = 그 외 ⟹ 정지.

### §0-5 사전 결정 5건 (값 보기 «전»)

```
 ㄱ **램프는 «추가 단계»다** — 칸 항목에 `"ramp": true` 가 있을 때만 돈다. 없으면 v4-20 의
    조립 모드가 **한 줄도 안 바뀐 채로** 돈다(§0-2 · §2 가 회귀로 확인한다).
 ㄴ **rest0 는 «조립 상태»에서 잰다** — 워커가 초기 위치 `pos0` 와 봉제 인덱스 `sm_idx` 로 계산한다
    (v3 와 «같은 식» · `dressRun.ts:111`). 장면 덤프의 `spar`(= SEP)를 쓰지 «않는다».
 ㄷ **프레임 단위로 돈다** — 램프가 프레임마다 rest 를 바꾸므로 창(N_WIN 10) 묶음 대신
    **1프레임씩** 밟고 창 경계에서만 순변위를 잰다(순변위 식·창 정의 불변).
 ㄹ **기록** — 결과 JSON 에 `ramp: {rampN, rest0Max, rest0Min, mode}` 를 남긴다(판정문 조건).
 ㅁ **정지** — ①(나)(다)면 ② 착수 0(갈래 C) · 사후 갈래 신설 0 · 처방 0 · 문턱 이동 0.
```

### §0-6 §0 커밋 sha

이 절은 §0 커밋 **직후** 그 sha 를 적는 **별도 커밋**으로 채운다(회차 프롬프트 「커밋 sha 를 §0 에」).

| | |
|---|---|
| §0 커밋 | **`cf0b69b`** — 집행 «전» 별도 커밋(이 파일의 §0 만) |

---

## §1 집행

### §1-① 램프 이식 + T포즈 재생 — **(나)** 게이트는 «일치», 중앙 차 두 행이 문턱 초과

#### ㄱ 옮긴 것 (커널 식 0줄 · rest 배열을 «호출부»가 프레임마다 갱신)

| 인용(`src/v3/dressRun.ts`) | 이 판의 구현(`gpu/bake/worker.py`) |
|---|---|
| `:111` `rest0[k] = |pos_i − pos_j|`(조립 직후 위치) | `pos0` 와 `sm_idx` 로 계산(장면 덤프의 `spar`= SEP 를 **쓰지 않는다**) |
| `:112` `RAMP_N = ceil((max rest0 − SEP)/(G·DT²))` | 같은 식 · G·DT 는 장면 헤더 · `SEP = 2 × THICK`(SDF 헤더) |
| `:113-116` `t = min(1, f/RAMP_N)` · `rest = rest0 + (SEP−rest0)·t` | 같은 식 · **선형** |
| `:177-178` `setRest(f+1)` **먼저**, 그다음 `step` | 프레임 f(0부터) «전»에 `t = (f+1)/RAMP_N` 로 `sm_rest` 를 올린다 |
| 적용 대상 = `seamCons`(dist)뿐 | `sm_rest` 하나만 갱신 · 늘어남·굽힘·충돌 **손대지 않음** |

**실측 램프 파라미터** — `RAMP_N` **79** · `rest0` **2.984649e-03 ~ 2.170112e-01 m** · `SEP` **2.000000e-03**
(결과 JSON `ramp` 필드에 기록 · 판정문 조건).

#### ㄴ T포즈 기본몸 M — 「조립 → 램프 → 정착」

| 채널 | 값 |
|---|---|
| `inputMode` / `ramp` | **`assembled`** / **기록됨**(`rampN` 79 외) |
| **정착 도달** | **프레임 180** · 창 순변위 **9.291560e-05** ≤ 1e-4 |
| s/프레임 | **2.275** · 발산·NaN 0 |
| 궤적 | f10 **1.234e-01** → f20 1.105e-01 → f30 4.173e-02 → f40 3.684e-02 → f170 1.141e-04 → **f180 9.292e-05** |

#### ㄷ 층3 대조 — **게이트는 일치, 표시 중앙이 두 행에서 문턱을 넘는다**

| 행 | 표시(v3 / 램프판) | 대역 이름 | 대역 n | **중앙 차(mm)** | ③ 적용 | v4-20(램프 없음) |
|---|---|---|---|---|---|---|
| 목선 | 1.1 / 1.1 ✅ | 밀착 ✅ | **54 / 54** | **−0.002525** ✅ | 적용 | +0.019108 |
| 가슴 | 4.5 / 4.2 | 여유 ✅ | 266 / 264 ❌ | −0.273787 | **밖**(밴드 다름) | +1.289313 |
| 허리 | 26.1 / 26.4 | 여유 ✅ | 226 / 224 ❌ | +0.288516 | **밖** | +8.120595 |
| 밑단 | 18.4 / 19.0 | 여유 ✅ | **110 / 110** | **+0.632179** ❌ | 적용 | +10.339331 |
| 소매 | 4.6 / 4.5 | 여유 ✅ | **918 / 918** | **−0.139005** ❌ | 적용 | +1.454911 |
| **합** | 1/5 | **5/5** | 3/5 | | **2행 초과** | |

| 게이트 | **v3 정본** | **램프판** | v4-20(램프 없음) |
|---|---|---|---|
| `pass` / `fails` | **true / []** | **true / []** ✅ | false / 교차 8 · 목선 1.0158 |
| 자기관통 교차 | **0** | **0** ✅ | 8 |
| 목선 초과비 | 0.989174 | **0.989106** ✅ | 1.015835 |
| ③a 관통 최대 | 0.497173 mm | 0.454548 mm | 0.430275 mm |
| 최소 쌍거리 | 1.998880 mm | 1.993713 mm | 1.980782 mm |
| 정착 창 순변위 | 0.058747 mm | 0.092916 mm | 0.057623 mm |

```
 ★★★ **램프가 판정문이 가리킨 자리를 «값으로» 고쳤다** —
   자기관통 교차 **8 → 0** · 목선 초과비 **1.015835 → 0.989106**(둘 다 v3 정본과 같은 쪽) ·
   게이트 **`pass`·`fails` 완전 일치** · 중앙 차 밑단 **+10.339 → +0.632**(16.4배) ·
   허리 +8.121 → +0.289 · 가슴 +1.289 → −0.274 · 소매 +1.455 → −0.139.
 ⟹ 그러나 **갈래 (가)는 「3조건 성립 «그리고» 게이트 일치」**이고, 새 정의 ③
   (**밴드 집합이 같은 행**의 중앙 차 ≤ **0.05 mm**)이 **밑단 +0.632179 · 소매 −0.139005** 두 행에서 깨진다
   (목선 −0.002525 는 통과 · 가슴·허리는 밴드 n 이 달라 ③ 적용 밖).
 ⟹ **갈래 (나) 일부 불일치.** **문턱을 옮기지 않았다**(함정 14) · 처방 0.
 ★ 정착 자체는 선다(f=180 · 9.29e-05) ⟹ (다) 아님.
```

### §1-② A포즈 굽기 탐침 — **착수 0**(갈래 C · §0-4)

①이 **(나)** 이므로 등재 갈래 C 대로 **②를 착수하지 않았다**(사후 갈래 신설 0).

### §1-③ 캡처 — **0장**(사유)

②가 착수 0 이라 **찍을 대상이 없다**. 오프스크린 렌더도 만들지 않았다.

---

## §2 자기검사

| 채널 | 값 |
|---|---|
| **워커 두 기존 모드 회귀** | 기존 정착-blob 모드 `c100-h185-s40_L` **비트 동일**(`3d8fac29f90fbfe4`) ·<br>조립 모드(램프 없음) `Tasm_M` **비트 동일**(`0d833601326922e2`) ⟹ **2/2** |
| 물리 diff | **0줄**(`solver.ts` · `engine/` 전량 — 램프는 `sm_rest` 배열 갱신뿐) |
| `src/` diff | **0** |
| 정본 · blob · 문턱 | **diff 0** |
| `py -m pytest gpu/tests -q` | **40 passed · 2 xfailed · 1 xpassed**(62.20 s) |
| `npx tsc -b` | **통과** |
| 브랜치 | 두 기계 모두 `v4-21-ramp` · **§0 커밋이 집행 «전»에 섰다**(v4-20 위반 재발 0) |

---

## §4 회차 갈래 — **C**(①(나) ⟹ 값 상신 · ② 착수 0)

| 항 | 갈래 | 한 줄 |
|---|---|---|
| ① 램프 + T포즈 재생 | **(나)** | **게이트 `pass`·`fails` 완전 일치**(교차 8→0 · 목선 1.0158→0.9891) · 대역 이름 5/5 ·<br>중앙 차 **밑단 +0.632179 · 소매 −0.139005** 두 행이 문턱 0.05 초과 |
| ② A포즈 굽기 | **착수 0** | 갈래 C |
| ③ 캡처 | **0장** | 대상 부재 |

**넘기는 것 5건**

```
 1. ★★★ **램프 읽기가 값으로 맞았다** — 판정문이 가리킨 자리 하나를 넣자 게이트가 **완전히 일치**하고
    중앙 차가 최대 **10.34 → 0.63 mm** 로 줄었다. 「조립 → 램프 → 정착」이 v3 파이프라인이다.
 2. ★★★ **그래도 «같은 옷»은 아직 아니다** — 밴드 집합이 같은 두 행(밑단·소매)에서 중앙이
    **0.632 / 0.139 mm** 벌어진다(문턱 0.05). 남은 차이가 무엇인지는 **이 판이 판정하지 않는다**.
 3. ★★ **가슴·허리는 밴드 집합이 갈린다**(266/264 · 226/224) ⟹ 새 정의 ③의 «적용 밖»이다.
    v4-16 이 등재한 「밴드 경계 정점」 문제가 여기서도 두 행에 걸린다.
 4. ★★ **워커 두 기존 모드가 비트로 보존됐다**(2/2) — 램프는 «추가 단계»로만 들어갔다.
 5. ★ **§0 을 집행 «전»에 세웠다**(sha `cf0b69b` · 자기 기입 `f37b55e`) — v4-20 위반 재발 0.
```

**이 판이 하지 않은 판단** — 남은 0.63/0.14 mm 의 원인 · 밴드 경계 처분 · A포즈 굽기 · 캡처 경로.
**처방 0 · 문턱 이동 0 · 사후 갈래 신설 0.**
