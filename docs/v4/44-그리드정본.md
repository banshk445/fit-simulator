# v4-44 — 교차차 69 mm 의 부위 사실 · 「확인 필요」 4칸의 던짐 성격 · 그리드 정본 선언 재료

**물리 0줄 · `src/` diff 0(계기만) · 굽기 0 · 캡처 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-44-grid-canon` |
| 기점 | **`80944b2`**(= 승혁 push 「v4-43 그리드 몸 27칸 산출」) |
| §0 커밋 | (아래 §0-5 에 자기 기입) |
| §1~§4 커밋 | (집행 후 기입) |
| 보고 파일 | `docs/v4/보고/44.md` |

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git fetch --all → 승혁 커밋 **80944b2** 수령
       $ git checkout -b v4-44-grid-canon 80944b2 → v4-44-grid-canon · HEAD 80944b2
 2호기 (§0 커밋을 push 한 «뒤» 같은 자리로 전환 — §0-5 에 인쇄)
```

---

## §0 수령·사무

### §0-1 판정문 (전략 세션 · 검증기 출력 수령 · 원문)

```
 「27칸 생성 성립(n 15,882 균일 · 높이 ±2mm · 비유한 0) · 하네스 트랙 종결 ·
   ★ 확인 필요 4칸 = 옷 스모크 던짐(c122.5 부류 · T포즈 108칸의 착용불가 26칸과 같은 부류 추정 · 확인만) ·
   ★★ 교차차 69.16mm — 브라우저 기준 칸 ↔ Node 기준 몸 불일치 ·
     정황 = Node 몸의 전역 y 오프셋(v4-39 실측 ~46mm) + 잔여 후보 팔 배치 · 부위별로 가른다 ·
     이 답이 108칸 정본 몸 경로 결정을 정한다(v4-37 정본 옷은 Node 몸 기준)」
```

### §0-2 바꾸지 않는 것

```
 물리 **0줄** · `src/` **0줄**(계기만 신설) · 굽기 **0** · 정본·blob·문턱 **0** · 몸 재생성 **0**
```

### §0-3 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · 단위 명시)

**★ 단위** — 좌표차·잔차는 **mm** · 정점 수는 **개** · 칸은 **칸**.

| 항 | 갈래 |
|---|---|
| **①** 교차차 부위 | **(가)** 오프셋 제거 후 **옷 영역** 잔차가 자연 변동 자릿수(≤ 수 mm) / **(나)** 옷 영역에 구조적 차이(값·부위) / **(다)** 그 외 |
| **②** 4칸 던짐 성격 | 던진 규칙의 파일:줄 · T포즈 108칸의 던짐 26칸과 같은 규칙인지 · 제품에서 「착용불가」 부류인지(**사실만**) |
| **③** 정본 재료 | ①의 갈래별 선택지 **사실** + 108칸 굽기 전 나머지(BODY_BIN 규약 · 워커 jobs 초안) · **판단 0** |

**§4** — A = ①(가)+②③ ⟹ 상신(108칸 발주 재료 완비) / B = ①(나) ⟹ 값 상신 / C = 그 외 ⟹ 정지.

### §0-4 사전 결정 4건 (값 보기 «전» · CC 집행 설계)

```
 ㄱ **부위 분류는 «몸에서» 만든다**(손 상수 0) — 팔/손/몸통/머리를 나누는 경계는
    그 몸의 **어깨 높이·몸통 반폭**에서 뜬다(예: 어깨 y 는 v4-38·39 의 `chestY`/`Y_ARM` 규칙 산출값 ·
    몸통 반폭은 그 높이 단면의 |x| 분포). 새 상수를 손으로 정하지 않는다.
 ㄴ **「옷 영역」의 정의를 지금 고정한다** — v4-37 정본 옷이 닿는 곳 = **몸통 + 위팔**.
    코드에서 뜨는 값으로 자른다: `y ≥ Y_LOW`(밑단 아래는 제외) **그리고** 어깨 피벗에서 소매 길이 안.
    자를 수 없으면 자르지 않고 **전체 분포와 상위 부위 목록**을 그대로 적는다.
 ㄷ **오프셋은 «중앙값»으로 뺀다** — 평균이 아니라 중앙값(꼬리에 안 끌린다). 뺀 값과 뺀 뒤 분포를 함께 적는다.
 ㄹ **판단 0** — 어느 몸을 정본으로 삼을지는 **전략 세션이 정한다**. 이 판은 선택지와 그 값만 적는다.
```

### §0-5 커밋·전환 인쇄 (집행 «전»)

(§0 커밋 sha 자기 기입 · 2호기 전환 인쇄는 아래에 붙인다)

---

## §1-① 교차차 69 mm 의 부위 사실 — **(다) 그 외**: 차이는 «균일 배율 + y 이동» 하나뿐이다

계기 신설 `scripts/v4BodyDiff.ts`(측정만 · `src/` 0줄). 대상 = 브라우저 산출
`grid27/l3ap-body-c100-h170-s45-a35.bin` ↔ Node 기준 `gpu/oracle/export/l3ap-body-c100-h170-s45-a35.bin`
(정점 **15,882** 동일).

### 부위 경계(그 몸에서 유도 · §0-4ㄱ)

`shoulderY(=chestY)` **1.3664 m**(v4-39 규칙) · `waistY` **1.0759 m** · `Y_LOW` **0.8284 m** ·
`Y_TOP` **1.4570 m** · 몸통 반폭(허리 단면 |x| 최대) **120.8438 mm**.

### 1차 — 좌표차와 「전역 y 오프셋」 제거

| 채널 | 값 mm |
|---|---|
| 좌표차(원본) | 최소 **6.5493** · 중앙 **49.1788** · p95 61.1388 · p99 67.3091 · **최대 69.1669** |
| y 차 분포 | 최소 **−69.1642** · 중앙 **−42.7756** · p95 −4.3570 · 최대 **−4.1123** |
| 전역 y 중앙 오프셋 | **−42.7756** |
| 오프셋 «후» 잔차 | 최소 1.6758 · 중앙 **22.6048** · p95 39.1167 · **최대 39.7624** |
| 오프셋 후 부위별(정점 수) | 몸통(2,107) 중앙 **6.0909** 최대 14.4137 · 팔(4,071) 중앙 **21.7514** 최대 26.9819 · 머리(1,717) 중앙 18.2467 · 다리(5,754) 중앙 30.7450 최대 39.7624 · 손(2,233) 중앙 26.8750 |
| 오프셋 후 **옷 영역**(몸통+팔 · 6,178) | 중앙 **14.9537** · p95 26.1815 · **최대 26.9819** |

★ **y 차가 «상수»가 아니다**(−69.16 ~ −4.11) ⟹ 전역 이동만으로는 설명되지 않는다. 그래서 배율을 함께 쟀다.

### 2차 — 상사(相似) 정합 `A ≈ s·B + t`(축별 최소제곱 · 회전 0 · 손 상수 0)

| 축 | 배율 `s` | 이동 `t` mm |
|---|---|---|
| x | **0.96314478** | 0.0000 |
| y | **0.96314455** | **−3.9609** |
| z | **0.96314467** | 0.0000 |

| 정합 «후» 잔차 mm | 값 |
|---|---|
| 전체(15,882) | 최소 0 · 중앙 **0.0001** · p95 0.0002 · **최대 0.0002** |
| 몸통 / 팔 / 머리 / 다리 / 손 | 최대 **0.0001 / 0.0002 / 0.0002 / 0.0002 / 0.0002** |
| **옷 영역**(몸통+팔 · 6,178) | 중앙 **0.0000** · p95 0.0001 · **최대 0.0002** |

⟹ **두 몸은 «같은 몸»이다** — 세 축의 배율이 소수 여섯째 자리까지 같고(등방), 정합 후 잔차가
**f32 표현 한계 자리(2e-4 mm)** 로 소멸한다. **구조적 차이 0 · 팔 배치 차이 0.**

★ **배율의 정체(사실 병기)** — `1 / 0.96314478 = 1.038276`. 이 수는 볼트에 이미 등재돼 있다:
`00-현재상태` 「**재생성 ÷ 저장 = 1.03827 (26칸 «유일값»)**」(v3-84·85 unitScale 계열).
브라우저 몸은 `Mannequin.tsx` 의 **씬 배율(unitScale)** 이 실린 몸이고, Node 경로(`bakeBodyVerts` 직접 호출)는
그 배율이 **안 실린** 몸이다. 높이로도 같은 말이 나온다 — 브라우저 27칸의 높이 목표차는 **최대 2.0317 mm**
(중앙 1.1417)인데, Node 기준 몸은 **+65.0517 mm**였다(v4-40 §1-②ㄹ).

★ **갈래 판정** — 사전 등재 (가)는 「**오프셋 제거 후** 옷 영역 잔차 ≤ 수 mm」다. 오프셋만 빼면
옷 영역 잔차가 중앙 **14.9537** · 최대 **26.9819 mm** 라 **(가)가 아니다**. (나)의 「구조적 차이」도 아니다
(정합 후 0.0002 mm). ⟹ **사전 등재에 없는 형태이므로 (다)로 적는다**(사후 갈래 신설 0 · 함정 14).

## §1-② 「확인 필요」 4칸의 던짐 성격 — **T포즈 108칸의 던짐과 «같은 규칙»**

### 던진 자리(파일:줄)

| 문언 | 자리 |
|---|---|
| 「소매산 실수해 없음 — 암홀 반쪽 … < 소매산 반폭 … — 갈래 D」 | `src/v3/garmentScene.ts:309` |
| 「옷 자기 간격 SEP 미달 — 최소쌍 …↔… — 갈래 D」 | `src/v3/garmentScene.ts:922` |

둘 다 **옷 제도·조립 단계**의 사전 등록 갈래(D)이고 **몸 blob 을 읽기 전에** 발화한다 —
`deriveLevels`(층3)나 A포즈와 무관하다.

### T포즈 108칸(`l3asm-v4-37-before.json` · v4-37 회귀 산출)과의 대조

```
 T포즈 108칸 던짐 **26칸** — 「옷 자기 간격 SEP 미달」 **15** · 「소매산 실수해 없음」 **9** ·
 「목 밑동(벽/지붕 전이)을 못 찾는다」 **2** · 그중 c122.5 계열이 **18칸**
```

| 검증기가 「확인 필요」로 적은 칸(그 몸의 **M** 사이즈로 옷을 세운다) | 검증기 문언 | 같은 몸의 T포즈 108칸 결과(S/M/L/XL) |
|---|---|---|
| `c122.5-h155-s45` | 옷 자기 간격 SEP 미달 · 최소쌍 back↔back **1.678 mm** | S **SEP 미달** · **M SEP 미달** · L ok · XL ok |
| `c122.5-h170-s50` | 옷 자기 간격 SEP 미달 · **1.951 mm** | S 소매산 · **M SEP 미달** · L ok · XL ok |
| `c122.5-h185-s45` | 소매산 실수해 없음 · 암홀 반쪽 22.20 cm < 소매산 반폭 **24.44 cm** | S 소매산 · **M 소매산** · L 소매산 · XL SEP 미달 |
| `c122.5-h185-s50` | 소매산 실수해 없음 · 암홀 반쪽 22.20 cm < 소매산 반폭 **28.81 cm** | S 소매산 · **M 소매산** · L 소매산 · XL 소매산 |

⟹ **네 칸 모두 M 사이즈에서 T포즈와 «같은 규칙»으로 던진다**(판정문의 「같은 부류 추정」이 값으로 확인됐다).
**몸의 결함이 아니라 «그 치수 조합의 옷이 성립하지 않는 것»** 이고, A포즈·브라우저 경로와 무관하다.
★ 사실 병기 — 검증기의 ㉤는 **몸마다 M 한 칸**만 세운다. 같은 몸의 **L·XL 은 T포즈에서 서는 칸이 있다**
(위 표) ⟹ 「그 몸은 못 쓴다」가 아니라 「그 몸 × 그 사이즈」의 문제다. 제품 분류(착용불가 등재)의
처분은 이 판이 하지 않는다.

## §1-③ 정본 선언 재료(판단 0)

### 27칸 산출의 상태(검증기 값 · 재측정 0)

정점 **15,882 균일**(27/27) · 비유한 **0** · 높이 목표차 **최대 2.0317 mm**(중앙 1.1417) ·
층3 레벨 성립 **23칸**(나머지 4칸은 §1-② 의 옷 던짐) · 성립 칸 전부 **`chestY > waistY`** ·
`sin(35°)` 기대차 최대 **0.142767**(팔 축 방향 채널 · 판정 문턱 없음 · 사실만).

### 선택지(값만 · 고르지 않는다)

| # | 선택지 | 값·귀결 |
|---|---|---|
| ㉠ | **그리드 정본 = 브라우저 27칸**(키 정합 몸) | 27칸이 곧 정본. v4-37 정본 옷은 **Node 몸**에서 구웠으므로 기본 칸 하나를 **다시 구워야** 같은 좌표계가 된다(굽기 1칸) |
| ㉡ | **그리드 정본 = Node 몸** | Node 경로는 슬라이더 치수를 못 실어 **기본 몸 1칸밖에 못 만든다**(v4-40 §1-②ㄱ) ⟹ 27칸을 못 만든다 |
| ㉢ | **브라우저 27칸을 Node 좌표계로 역정합** | `B = (A − t)/s` · `s = 0.96314478` · `t_y = −3.9609 mm` · 정합 잔차 **0.0002 mm** 라 **가역**. v4-37 정본 옷과 같은 좌표계를 유지하되, 몸 높이가 목표 키와 **+65 mm** 어긋난 상태를 그대로 안고 간다 |

### 108칸 굽기 전 남은 것(사실)

```
 ㄱ **BODY_BIN 규약** — 층3·조립·게이트 계기는 전부 `BODY_BIN` 으로 몸을 갈아 끼운다
    (`v4AsmExport.ts:27` · `v4FitReport.ts:35` · `v4ProductGate` · `v4Armpit`) ·
    A포즈 축·원점은 `ARM_AXIS_JSON`(`l3ap-body-<칸>-a35.json`)·`ARM_ORIGIN_JSON`(`l3ap-origin-<칸>-a35.json`)
    ⟹ 27칸 산출에 **셋 다 들어 있다**(칸당 3파일).
 ㄴ **칸당 조립 export 1회** — `CELL=<칸>_<사이즈> BODY_BIN=… TAG=<태그> npx tsx scripts/v4AsmExport.ts`
    산출 `scene-<TAG>.bin` · `scene-bend-<TAG>.bin` · `scene-seam-<TAG>.bin` ·
    **`sdf-<몸태그>.bin`**(몸마다 1개) · `asm-<TAG>.bin`.
 ㄷ **워커 job 초안**(형식은 v4-37 과 같다 · 값은 발주 시 채운다)
    { "name": "v4-XX-A108", "fp": "f64", "arch": "cuda", "frames": 400,
      "cells": [ { "cell": "<TAG>", "asm": "asm-<TAG>.bin", "reportCell": "<칸>_<사이즈>",
                   "frames": 400, "cellCap": { "cell_cap": 192, "max_cells": 262144 }, "ramp": true }, … ] }
    ★ 워커 층3은 `BODY_BIN` 을 **환경에서 물려받는다**(`worker.py:174` `dict(os.environ, …)`) ⟹
      A포즈 칸을 구울 때는 **BODY_BIN 을 반드시 지정**해야 층3이 선다(v4-37 §1-④ 의 실패가 그 사례).
 ㄹ **던짐 4칸** — §1-② 대로 «옷» 쪽 갈래 D 다. 발주 목록에서 **뺄지/사이즈를 바꿀지**는 전략 세션 몫.
```

---

## §2 자기검사

| 항 | 값 |
|---|---|
| `src/` diff vs 기점 `80944b2` | **0**(무출력) · 물리 **0줄** · 정본·blob·문턱 **0** |
| 이 판이 **신설**한 것 | `scripts/v4BodyDiff.ts`(측정만 계기) · 산출 `l3ap-bodydiff-c100-h170-s45-a35.json` |
| 굽기 · 몸 생성 · 캡처 | **0 · 0 · 0**(승혁 산출을 읽기만 했다) |
| 회수 | 2호기에서 `l3asm-v4-37-before.json`(T포즈 108칸 회귀 산출)을 가져와 던짐 26칸을 대조했다(새 측정 0) |
| `npx tsc -b` | **통과**(종료 0) |
| pytest(2호기) | **40 passed · 2 xfailed · 1 xpassed**(62.86 s) |
| 브랜치 인쇄 | §0 머리에 에어 · 2호기(§0-5) · §0 은 집행 **«전»** 커밋(`0d2d016`) |
| ecc 에이전트 / 스킬 | **0 / 0** |

## §4 회차 갈래 — **C**(①(다) ⟹ 값 상신 · 정지)

사전 등재 §0-3 은 A = ①(가)+②③ · B = ①(나) · **C = 그 외**다. ①이 **(다)** 이므로 **C** 로 적는다.
**②③은 집행했고 값은 위에 전부 있다**(갈래 문언이 ①에 걸려 있어 §4 이름만 C 다 · 사후 갈래 신설 0).

**넘기는 것 7건**

1. ★★★ **두 몸은 «같은 몸»이다** — 상사 정합 `A ≈ s·B + t` 로 **s = 0.96314478 / 0.96314455 / 0.96314467**(x/y/z ·
   등방) · `t_y = −3.9609 mm` · **정합 후 잔차 최대 0.0002 mm**(f32 자리). 구조·팔 배치 차이 **0**.
2. ★★★ **69.16 mm 는 «배율» 차다** — 전역 y 오프셋만으로는 안 지워진다(y 차가 −69.16 ~ −4.11 로 «상수가 아니다»).
   오프셋만 빼면 옷 영역 잔차가 중앙 14.9537 · 최대 26.9819 mm 로 남는다.
3. ★★★ **배율의 정체** — `1/0.96314478 = 1.038276` 은 볼트의 「재생성 ÷ 저장 = **1.03827**(26칸 유일값)」과 같은 수다
   (v3-84·85 unitScale 계열). 브라우저 몸은 **키가 맞고**(목표차 최대 2.0317 mm),
   Node 몸은 **+65.0517 mm 어긋난다**(v4-40 실측).
4. ★★★ **던짐 4칸은 «옷» 문제다** — `garmentScene.ts:309`(소매산) · `:922`(SEP 미달)에서 **몸을 읽기 전에** 던지고,
   같은 몸의 **M 사이즈가 T포즈 108칸에서도 같은 규칙으로 던진다**(대조표). 몸·A포즈와 무관.
5. ★★ **T포즈 던짐 26칸의 구성** — SEP 미달 **15** · 소매산 **9** · 목 밑동 **2** · 그중 c122.5 계열 **18칸**.
   같은 몸이라도 **L·XL 은 서는 칸이 있다** ⟹ 「몸 폐기」가 아니라 「몸 × 사이즈」 문제다.
6. ★★ **정본 선택지 셋** — ㉠ 브라우저 27칸 정본(기본 칸 옷 1회 재굽기) · ㉡ Node 몸(27칸 생성 불가) ·
   ㉢ 역정합(`B = (A − t)/s` · 가역 · 잔차 0.0002 mm). **고르지 않았다.**
7. ★ **108칸 발주 재료** — `BODY_BIN`·`ARM_AXIS_JSON`·`ARM_ORIGIN_JSON` 셋이 27칸 산출에 다 있고,
   칸당 `v4AsmExport` 1회 → 워커 job(형식 위) · **워커 층3은 `BODY_BIN` 을 환경에서 물려받는다**(지정 필수).

**이 판이 하지 않은 판단** — 어느 몸을 정본으로 삼을지 · 던짐 4칸의 발주 처분 · 화면.
