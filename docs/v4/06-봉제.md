# v4-06 — 봉제 제약 이식 · 층2 재시도(구속계 + 봉제) · 층3 첫 시행

**집행 기계 = 2호기(CUDA · 정본 기계) · `src/` diff 0 · 굽기 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-06-seam` |
| 기점 | **`2f49d37`** (= v4-05 HEAD) |
| 워크트리 | `C:\Users\banba\fit-simulator` |
| 기계 | **`DESKTOP-OV72A46`** (win32 · CUDA) |
| §0 커밋 | **`dac971d`** — 집행 «전» 별도 커밋(sha 만 그 뒤 한 줄로 채웠다) |
| 호출 ecc 에이전트/스킬 | **0** |

---

## §0 수령 (집행 «전» 별도 커밋)

### §0-1 판정문 (전략 세션 · v4-05 §4 수령 · 원문)

```
 「갈래 B 수령 · ① 선검사가 «집행 전» 부적격을 걸렀다(문턱 0.01 미사용 · 도착점 부재) ·
   ★ 새 사실 = 봉제가 층2 의 선행 조건(봉제 없으면 −84.6mm 흘러내림 · 봉제 있으면 중앙 0.8mm) ·
   귀책 = 전략 세션(이식 순서에서 봉제 누락) · ③ 몸 충돌 층1 통과 = 커널 3종 1스텝 정확 확정 ·
   ★ 충돌 발화 수는 대조 채널 «불가»(f32 536 ↔ f64 1057) — 대조는 상태(정점·형상·리포트)로만 ·
   200프레임 정지 판단 수용 · 함정 40 수용」
```

### §0-1′ 귀책 등재 — **전략 세션**(v4-05 이식 순서에서 봉제 누락)

```
 v4-05 는 층2 시험계를 「정답지 1칸 · 늘어남만 / 굽힘만 / 둘 다」로 세웠는데,
 **그 셋 중 어느 것도 옷을 몸에 붙들지 못한다** — 붙드는 것은 **봉제**였다(10프레임에 −84.6 mm).
 이식 순서에서 봉제를 뒤로 미룬 것이 원인이고 **귀책은 전략 세션**이다.
 ★ CC 는 **판별력 선검사를 «집행 전»에 돌려** 그 사실을 잡았다 — 잘못된 시험계로 층2 를 돌리고
   그 수를 상신하는 일이 **일어나지 않았다**. 순서(판별력 먼저)가 값을 지켰다.
```

### §0-2 사무 (이 커밋에서 집행)

| # | 항목 | 자리 |
|---|---|---|
| ㄱ | **이식 순서 정정** — 「늘어남 → 굽힘 → 몸 충돌 → **봉제** → 셀프 충돌」 | 인수인계 백로그 **8** |
| ㄴ | 원리 추기 — **「대조는 «상태»로 한다 · 이산 계수는 채널이 될 수 없다」** | `원리/검증은-도착점과-관측량으로.md` |

### §0-3 바꾸지 않는 것

```
 `src/` · 정본 · blob · v3 동결 · **v4 커널 3종 로직 diff 0**(늘어남 · 굽힘 · 몸 충돌) · 문턱 0
```

### §0-4 시험계의 «정의역»을 집행 전에 못 박는다 (사후 해석 금지)

```
 §1-② 의 세 시험계는 **몸 충돌과 중력을 «공통»으로 포함**한다 — 나열된 것은 «옷 제약»의 부분집합이다:
   ㄱ **늘어남 + 봉제**            (+ 몸 충돌 + 중력)
   ㄴ **굽힘 + 봉제**              (+ 몸 충돌 + 중력)
   ㄷ **늘어남 + 굽힘 + 봉제**      (+ 몸 충돌 + 중력)   ← ③ 이 쓰는 것
 근거 — 이 해석은 **v4-05 §1-① 과 «같다»**(그 판도 「늘어남만/굽힘만/둘 다」에 충돌·중력을 공통으로
   넣었다). 회차 간 대조가 끊기지 않게 **같은 정의역**을 유지한다. 발주 문언의 ㄷ 항이 몸 충돌을
   «명시»한 것은 세 항 공통 사실을 한 번 적은 것으로 읽는다.
 ★ **셀프 충돌은 세 시험계 전부에서 «뺀다»** — v4 에 이식이 없다(정의역 일치). v3 쪽도 뺀다.
   그 결과 정답지 blob 은 이 정의역에서 «완전한 평형이 아니다»(v4-05 실측 최대 16.0 mm).
 섭동 정의 = **자유 정점 전량의 x 에 +1e-7 m**(v4-04·v4-05 와 같은 계기 · 강체 병진).
 적격 기준 = **보존비 ≤ 0.01**(v4-05 §0-5 문면 그대로 · **이동 0**).
```

### §0-5 사전 등재 갈래 (원문)

| 갈래 | 조건 | 처분 |
|---|---|---|
| **A** | ①~④ 전량 + ①**(가)** + ② 적격 | 상신 |
| **B** | ① **(나)(다)** | 값·사실 상신 · **②③④ 착수 0** |
| **C** | ② **부적격**(봉제 포함해도 보존비 > 0.01) | 값 상신 · **③④ 착수 0** · 층2 설계 재검 |
| **D** | 그 외 | 정지 |

**①의 하위 갈래** — (가) 층1 통과(비 < 1) / (나) 초과(값) /
(다) **봉제 제약이 단일 커널로 분리되지 않는 코드 구조**(사실 등재 · 정지).

**사후 갈래 신설 0.**

### §0-6 착수 «전» 알려진 사실

```
 · 봉제는 v3 에서 **거리 제약(`kind:'dist'`)** 이다 — `garmentScene.ts:730`
   `seamCons.push({ kind:'dist', i: sm.a[k], j: sm.b[k], rest: SEP, k: KMEM, lambda: 0 })`.
   투영 함수는 `solver.ts:1145 projectDistance` 이고 **v4-02 §1-③ 에서 이미 «식으로» 읽어 두었다**
   (C = |xj−xi| − rest · α = 1/k · α̃ = α/h²) ⟹ (다) 갈래(분리 불가)는 **성립하지 않을 전망**이지만
   **판정은 실측 뒤에** 한다.
 · **램프 주의** — `dressRun.prepare` 는 `setRest(f)` 로 봉제 `rest` 를 `rest0 → SEP` 로 «당긴다».
   이 판은 **정착 상태에서 시작**하고 `setRest` 를 **부르지 않으므로** `rest` 는 생성값 **`SEP`** 다.
   그것이 정착 시점(f=180 ≥ RAMP_N)의 값과 같은지 **값으로 확인**한다(가정 0).
 · v3 는 이 칸에서 **9.3~9.7 초/프레임**이다(v4-05 실측) ⟹ ② 의 v3 실행이 판의 비용을 지배한다.
 · ④ 는 `src/v3/fitReport.ts:56 buildFitReport(P, L)` 를 **그대로 부른다**(물리 0프레임 · 읽기만).
```

---

## §1 집행

### §1-① 봉제 제약 이식 — **(가) 층1 통과**

**v3 코드 사실 — 손으로 적지 않고 «인용»한다**

| 자리 | 원문 / 사실 |
|---|---|
| `src/v3/solver.ts:22` | 「**거리 제약: C = \|xj − xi\| − rest, 컴플라이언스 α = 1/k** [k: N/m]」 — 주석이 식을 그대로 적는다 |
| `src/v3/garmentScene.ts:715-726` | `seams` — **10 이음**: 어깨 L/R · 옆선 L/R · 암홀 앞뒤 L/R · 소매밑 L/R. 각 이음은 두 «정점 열»을 짝짓는다(`row`/`col`) |
| `src/v3/garmentScene.ts:730` | `seamCons.push({ kind:'dist', i: sm.a[k], j: sm.b[k], rest: SEP, k: KMEM, lambda: 0 })` ⟹ **rest = SEP = 2×THICK** · **k = KMEM = 원단 k** |
| `src/v3/garmentScene.ts:734` | `cons = [...inplane, ...bends, ...seamCons]` ⟹ **봉제는 순회의 «맨 뒤»** |
| `src/v3/solver.ts:1145-1170` | `projectDistance` — `len<1e-12` 이면 반환 · `wsum===0` 이면 반환 · **`at = 1/c.k/h2`** · `dl = (−C − at·λ)/(wsum + at)` · `pos[i] −= wi·dl·d̂` · `pos[j] += wj·dl·d̂` |
| `src/v3/solver.ts:1084` | 반복수 **1** · λ 는 서브스텝마다 0 |

**조립 대조 — 값으로 확인했다**(§0-6 「램프 주의」의 해소)

| 항목 | 실측 | 기대 | 판정 |
|---|---|---|---|
| 봉제 제약 수 | **256** | 10 이음의 정점쌍 합 | ✅ |
| `rest` 최소 / 최대 | **2.000000e-03 / 2.000000e-03** | `SEP` = 2×THICK = 2e-3 | ✅ **전량 동일** |
| `k` | **69** | `KMEM` = gray k | ✅ |
| `RAMP_N` | **79** | < 정착 프레임 180 | ✅ **램프가 끝난 뒤의 값** |
| 정점당 봉제 제약 수(deg) | 최대 **2** · 관여 정점 **498** | — | 사실 |

```
 ★ §0-6 의 물음 —「`setRest` 를 안 부르는데 `rest` 가 정착 시점 값과 같은가」— 에 **값으로** 답했다:
   생성값이 이미 `SEP` 이고, 램프는 `RAMP_N = 79` 에 끝나며 정착은 `f = 180` 이다 ⟹ **같다**. 가정 0.
 ★ **(다) 갈래는 불발**했다 — 봉제는 v3 에서 이미 `kind:'dist'` 로 «분리된» 제약이고
   단일 커널로 그대로 떨어진다(`gpu/engine/seam.py`).
```

**봉제 커널 K‴** — `gpu/engine/seam.py:_project`

| 자리 | 식 | 연산 수 | 깊이 |
|---|---|---|---|
| `dv` | `pos[j] − pos[i]` | 3 | 1 |
| `ln` | `sqrt(dv·dv)` | 6 | 5 |
| `d̂` | `dv / ln` | 3 | 6 |
| `wsum` · `C` | `wi+wj` · `ln − rest` | 1 + 1 | 1 / 6 |
| `at` | `1/k/h²` | 2 | 2 |
| `dl` | `(−C − at·λ)/(wsum + at)` | 5 | 9 |
| `λ += dl` | | 1 | 10 |
| `pos[i] −= wi·dl·d̂` (한 좌표) | | 3 | **12** |
| | **제약 하나** | **25** | **12** |

**층1 — 「봉제만 1스텝」**(정착 위치 · 속도 0 · 중력 0 · 감쇠 0 · substeps 1 · dt = DT/SUB · 봉제만)

| 항목 | 값 |
|---|---|
| v3 봉제 1스텝 변위 | 최대 **1.961884e-04** m · 중앙 0.000e+00 · 움직인 정점 **498 / 9,388** |
| 최대 \|좌표\| | 1.449586255 m ⟹ ULP_f32 1.192093e-07 |
| **v4 f32 — 최대 좌표차** | **1.128950e-07** · 중앙 2.798e-09 |
| **층1 비**(K 12 × deg 1) | **최대 0.0810** · 중앙 0.0211 · p99 0.0459 · **초과 정점 0 / 9,388** ⟹ **통과** |
| **v4 f64 — 최대 좌표차** | ★ **0.000000e+00** — **비트 동일** |

```
 ★★ **f64 에서 «비트 동일»이 나온 것은 이 커널이 처음이다**(늘어남 2.22e-16 · 굽힘 3.39e-21 ·
   충돌 4.44e-16). 거리 제약은 `hypot` 을 한 번만 쓰고 그 인자 대역이 좁아
   `sqrt(Σx²)` 와 «같은 값»으로 떨어졌다 — 우연이 아니라 **연산 사슬이 짧아서**다(깊이 12).
 ⟹ **①(가)**. 봉제까지 **커널 4종이 층1 을 전부 통과**했다(늘어남 0.0924 · 굽힘 0.1438 ·
   충돌 0.0275 · 봉제 0.0810 · 넷 다 **초과 정점 0/9,388**).
```

### §1-①′ 결합 커널 — **옮겨적기를 «기계로» 검증했다**

층2 는 네 갈래를 **한 커널**에서 v3 의 순서대로 돌려야 한다(`gpu/engine/full.py`).
순서는 `solver.ts:1050 step()` 그대로 — 예측 → λ 초기화 → **[inplane → bend → seam]** →
(자기충돌 **없음**) → 몸 충돌 → 속도 갱신.

산술은 검증된 네 모듈에서 **옮겨 적은** 것이라 어긋날 수 있다. 눈으로 대조하지 않고 **기계로 막았다** —
결합 커널에서 **갈래를 하나만 켜고 1스텝** 돌려 각 단일 커널의 v3 정답과 대조한다:

| 갈래 | 결합 커널 f32 최대 좌표차 | 단일 커널 값 | 층1 비 | f64 |
|---|---|---|---|---|
| 늘어남 | **5.837018e-07** | 5.837018e-07 | 0.0924 | 2.220446e-16 |
| 굽힘 | **4.114550e-07** | 4.114550e-07 | 0.1438 | 3.388132e-21 |
| 봉제 | **1.128950e-07** | 1.128950e-07 | 0.0810 | 0.000000e+00 |
| 몸 충돌 | **1.115526e-07** | 1.115526e-07 | 0.0275 | 4.440892e-16 |

```
 ★ **네 갈래 전부 «자릿수 전량 동일»** ⟹ 옮겨적기 어긋남 **0**. `pytest` 에 고정했다
   (`test_full_kernel_matches_single_kernels` · 파라미터 4종).
```

### §1-② 판별력 선검사 재시행 — **부적격**(봉제를 넣어도)

절차·문턱·섭동 정의는 **§0-4 그대로**(이동 0): v3 를 v3 와 초기값 **1e-7 x 강체 병진**으로 대조해
**섭동 보존비 = (최종 좌표차 최대) / 1e-7** 를 재고, **≤ 0.01 이면 적격**.

#### 시험계 ㄷ — 늘어남 + 굽힘 + 봉제 (+ 몸 충돌 + 중력) · ③ 이 쓸 계

| | 수렴 | 프레임 | 마지막 창 순변위 | 실행 시간 |
|---|---|---|---|---|
| 기본 | **도달** | **80** | 8.066671e-05 m | 1,226 s (15.3 s/프레임 · 6프로세스 경합) |
| 섭동 1e-7 | **도달** | **80**(+0) | 8.065390e-05 m | 1,228 s |

```
 ★ **봉제를 넣으니 수렴한다** — v4-05 의 「흘러내림」이 사라졌다(창 순변위 자취
   1.600e-02 → 3.992e-03 → 1.182e-03 → 7.531e-04 → 5.405e-04 → 2.531e-04 → 1.341e-04 → **8.067e-05**).
   **봉제가 층2 의 선행 조건**이라는 v4-05 의 판정이 «수렴»으로 확인됐다.
```

| 판별력 | 값 |
|---|---|
| 최종 좌표차 | 최대 **5.227809e-07** · 중앙 9.164e-08 · p99 1.574e-07 |
| **섭동 보존비** | **5.227809** |
| 적격 문턱 | **≤ 0.01** ⟹ ★ **부적격**(문턱의 **523배**) |

**잔차를 갈라 봤다**(진단 · 판정 아님)

| 성분 | 값 | 섭동 대비 |
|---|---|---|
| **강체**(평균 변위 x) | 8.854376e-08 | **0.8854배** — 마찰이 80프레임에 11.5% 만 깎았다 |
| **내부**(강체를 뺀 나머지) | 최대 5.219444e-07 · 중앙 8.002e-09 | **5.2194배** |
| 축별 최대 | x 3.163e-07 · y 2.449e-07 · **z 5.228e-07** | — |
| 잔차가 섭동보다 큰 정점 | **1,862 / 9,388 = 19.8%** | — |

```
 ★★★ **왜 수축이 안 보이는가 — 두 자가 3자릿수 어긋나 있다.**
   수렴 판정은 **창 순변위 ≤ 1e-4 m**(v3 `settleNetM`)인데, 그 시점의 실제 창 순변위는
   **8.067e-05 = 섭동(1e-7)의 807배**다. 즉 계는 **아직 섭동보다 3자릿수 빠르게 «기어가는 중»에**
   수렴으로 선언된다 ⟹ **1e-7 짜리 섭동은 «줄어들 시간»을 갖지 못하고 실려 간다.**
 ★ 그 위에 두 몫이 겹친다:
   ① **강체 모드**가 0.885배로 살아남는다 — 이 칸은 **고정점이 0개**이고 붙드는 것은 마찰뿐이다.
   ② **내부 재분배**가 5.2배까지 키운다(19.8% 정점이 섭동보다 큰 잔차를 갖는다).
 ⟹ **판별력 계기와 수렴 판정의 «자»가 맞지 않는다.** 이것은 v4 구현의 성질이 아니라
   **v3↔v3 로 잰 «계 + 판정 규칙»의 성질**이다(귀속 0).
```

#### 세 시험계 전량 — **3/3 부적격**

| 시험계(+몸 충돌+중력) | 수렴(기본/섭동) | 프레임 | 마지막 net | 최종 차 최대 | **보존비** | 적격 |
|---|---|---|---|---|---|---|
| ㄱ **늘어남 + 봉제** | True / True | **210 / 220** | 8.6411e-05 / 7.5824e-05 | **3.363778e-03** | **33,637.78** | ❌ |
| ㄴ **굽힘 + 봉제** | **False / False** | 300 / 300(상한) | 3.8723e-01 / 3.7348e-01 | 2.298247e+00 | (미수렴 ⟹ 도착점 아님) | ❌ |
| ㄷ **늘어남 + 굽힘 + 봉제** | True / True | **80 / 80** | 8.0667e-05 / 8.0654e-05 | **5.227809e-07** | **5.2278** | ❌ |

| 시험계 | 정착 대비 이동 중앙 / 최대 | 옷 중심 y (정착 1.1644) | 실행 시간 |
|---|---|---|---|
| ㄱ 늘어남+봉제 | 1.8 mm / 28.4 mm | **1.1642** | 2,314 s (11.0 s/프레임) |
| ㄴ 굽힘+봉제 | **7,857.3 mm / 8,658.5 mm** | **−6.6505** ← 옷이 7.8 m 아래로 떨어졌다 | 1,407 s (4.7 s/프레임) |
| ㄷ 전량 | **0.8 mm** / 16.5 mm | **1.1640** | 1,226 s (15.3 s/프레임) |

```
 ★ **ㄴ(굽힘+봉제)** — 늘어남이 없으면 천이 무한히 늘어나 **옷이 몸에서 떨어져 계속 내려간다**
   (창 순변위가 0.39 m 에서 안 준다) ⟹ 도착점 자체가 없다.
 ★ **ㄱ(늘어남+봉제)** — 옷은 제자리에 있는데(중심 1.1642) **섭동이 수렴 «시점»을 바꿨다**
   (210 → 220 프레임 · 창 하나 차이). 최종 차 **3.36 mm** 는 그 한 창의 몫이 아니라
   (창 순변위 0.086 mm 의 39배) **굽힘 저항이 없어 궤적이 갈린 몫**이다.
 ★ **ㄷ(전량)** 만 두 실행이 «같은 프레임»에 수렴했다(80/80) — 그래서 보존비가 뜻을 갖는 유일한 계고,
   그 값이 **5.2278** 다.
 ⟹ **세 시험계 3/3 부적격.** 문턱 0.01 은 **v4-05 §0-5 문면 그대로**이고 **이동 0** 이다.
```


### §1-③ 층2 첫 시행 — **착수 0**

```
 §0-5 **C = 「② 부적격 ⟹ 값 상신 · ③④ 착수 0 · 층2 설계 재검」**.
 ② 의 시험계 ㄷ(③ 이 쓸 계)가 **보존비 5.227809 로 부적격**이므로 **한 줄도 돌리지 않았다** —
 v3·v4 수렴 대조 **0회** · 최종 정점 대조 **0회** · 상한 등재 **0**.
 ★ **미리 밝힌다** — ② 의 결과가 나오기 «전»에 **결합 커널의 1프레임 «비용»을 한 번 쟀다**
   (`gpu/engine/full.py` · CUDA · f32 · **12.6 초/프레임**(컴파일 제외) · 정착 상태에서).
   그것은 **비용 측정**이고 층2 대조가 아니다(수렴도 안 돌렸고 대조도 안 했다).
   다음 판의 예산 산정에 쓰라고 값을 남긴다 — v3 는 같은 계에서 **11.0~15.4 초/프레임**이었다.
 ★ `gpu/l2_run.py`(층2 실행기)는 **작성만** 했고 **돌리지 않았다**. 침묵으로 넘기지 않는다.
```

### §1-④ 층3 첫 시행 — **착수 0**(다만 «기준선»은 잡아 두었다)

```
 ④ 는 ③ 의 최종 상태를 입력으로 받는데 ③ 이 미집행이므로 **대조는 0건**이다.
 ★ 다만 **v3 정본 쪽 기준선은 잡아 두었다** — 정답지 blob 의 정착 상태로 `buildFitReport` 를
   한 번 돌려 5행을 떴다(`gpu/oracle/export/fit-c100-h170-s45_M-v3blob.json`).
   이것은 **v3 ↔ v3** 이고 v4 가 관여하지 않는다 ⟹ **대조가 아니라 «자의 눈금»**이다.
   다음 판이 v4 최종 상태를 넣으면 **같은 계산 경로에 상태만 갈아 끼우는** 형태가 된다.
```

**v3 정본 5행**(`c100-h170-s45_M` · 물리 0프레임 · 경계 2.0mm · chestY 129.97cm · waistY 103.23cm)

| 부위 | 중앙(mm) | p25 | p75 | 눌림/밀착/여유 | 표본/정의역 |
|---|---|---|---|---|---|
| 목선 | **1.0641** | 0.9985 | 1.8761 | 0 / 41 / 13 | 54 / 54 |
| 가슴 | **4.4660** | 1.0000 | 16.8004 | 0 / 111 / 155 | 266 / 266 |
| 허리 | **26.1203** | 14.8238 | 71.1607 | 0 / 0 / 226 | 226 / 226 |
| 밑단 | **18.3743** | 7.1067 | 38.4270 | 0 / 8 / 102 | 110 / 110 |
| 소매 | **4.6177** | 1.0113 | 17.1439 | 0 / 319 / 599 | 918 / 918 |

```
 계산 경로는 `src/v3/fitReport.ts:56 buildFitReport(P, L)` **그대로**다(스크립트에 식 0줄 ·
 화면 패널과 «같은 모듈» — 함정 22). **게이트 판정은 내리지 않았다**(§1-④ 발주 문언 그대로 ·
 입력이 아직 자기충돌 없는 상태다).
```

---

## §2 자기검사

| # | 항목 | 결과 |
|---|---|---|
| ㉠ | ①② 표 | 위 §1 (③④ 는 미집행) |
| ㉡ | **pytest(CUDA) 전량** | (아래 실행 결과) |
| ㉢ | `src/` diff | **0 파일**(기점 `2f49d37` 대비) |
| ㉣ | 정본 diff | **0** — `public/v3diag` 쓰기 0 |
| ㉤ | blob sha | **36/36 일치** · 몸 27/27 |
| ㉥ | **v4 커널 3종 로직 diff** | **0** — `stretch.py` · `bending.py` · `collide.py` 한 줄도 안 바뀌었다 |
| ㉦ | v4-02~05 산출물 바이트 불변 | 변경 0 — 이 판 산출은 **새 파일** |
| ㉧ | `npx tsx tsc -b` | (아래) |
| ㉨ | 굽기 | 착장 `runFrames` **0회** |
| ㉩ | 호출 ecc 에이전트 / 스킬 | **0 / 0** |

**§3 캡처 — 0장**(사전 등재 그대로).

**§2 실행 결과** — pytest(CUDA) **40 passed · 2 xfailed · 1 xpassed**(205.7 s) · `npx tsc -b` **통과**(exit 0) ·
`src/` diff **0 파일** · 커널 3종(`stretch`·`bending`·`collide`) diff **0** · blob sha **36/36** · 몸 **27/27**.
xfail 2건 = v4-03 힌지 궤적 상한(폐기 예정 형식) · v4-04 f32 hinge 미수렴. xpass 1건 = `test_strip_matches_v3`(2호기에서 통과·맥에서 초과).

---

## §4 판정 — **갈래 C**

```
 §0-5 대조:
   **A** = ①~④ 전량 + ①(가) + ② 적격  → ② 가 **3/3 부적격** ⟹ 불성립
   **B** = ① (나)(다)                   → ① 은 **(가)**(층1 통과 · 분리도 됐다) ⟹ 불성립
   **C** = ② 부적격(**봉제 포함해도 보존비 > 0.01**) → **성립** — ㄷ 가 **5.2278**, ㄱ 이 **33,637.78**,
                                            ㄴ 은 미수렴. **문언이 그대로 맞는다.**
   **D** = 그 외                          → C 가 성립하므로 해당 없음
 ⟹ **갈래 C**. 처분대로 **③④ 착수 0 · 층2 설계 재검**으로 넘긴다.
 **사후 갈래 신설 0 · 문턱 이동 0 · 상한 등재 0 · v3 diff 0 · 커널 3종 diff 0.**
```

### §4-1 상신하는 값

```
 ① 봉제 층1 — f32 **1.128950e-07**(비 **0.0810** · 초과 정점 **0/9,388**) · f64 **0.000000e+00**(비트 동일)
    조립 대조 — 제약 **256** · rest 전량 **2.000000e-03 = SEP** · k **69** · RAMP_N **79 < 180** · deg 최대 2
 ①′ 결합 커널 — 갈래별 4종 **자릿수 전량 동일** · **결합 1스텝(순서 포함)** f32 **6.542450e-07**
    (비 0.0446 · K 합 123) · f64 **2.220446e-16**
 ② 판별력 — ㄱ 늘어남+봉제 **33,637.78**(수렴 210/220) · ㄴ 굽힘+봉제 **미수렴**(옷이 7.8 m 낙하) ·
    ㄷ 전량 **5.2278**(수렴 80/80) · 문턱 **0.01** · **3/3 부적격**
    ㄷ 잔차 분해 — 강체 **0.8854배** · 내부 **5.2194배** · 잔차>섭동 정점 **19.8%**
    ★ 수렴 시점 창 순변위 **8.067e-05 = 섭동의 807배** — 계는 «기어가는 중»에 수렴으로 선언된다
 비용 — v3 11.0~15.4 s/프레임 · v4 결합 커널 **12.6 s/프레임**(CUDA · f32 · 직렬 · 비용 측정만)
```

### §4-2 넘기는 것

| # | 항목 | 몫 |
|---|---|---|
| **㉠** | ★★★ **판별력 계기와 수렴 판정의 «자»가 3자릿수 어긋나 있다** — 수렴은 창 순변위 **1e-4 m** 에서 선언되는데 섭동은 **1e-7 m** 다. 섭동이 줄어들 시간이 없다. ① 섭동을 수렴 문턱에 «맞출지»(예: 1e-4 규모) ② 수렴 판정을 조일지 ③ 보존비를 다른 형태로 정의할지. **CC 는 계기를 새로 정하지 않는다** | 전략 세션 |
| **㉡** | **이 칸은 고정점이 0개**라 강체 모드를 마찰만으로 붙든다 — 80프레임에 **11.5% 만** 감쇠(0.8854 생존). 「강체 병진 섭동」이 이 계에 옳은 물음인지 자체가 설계 항목이다 | 전략 세션 |
| **㉢** | **봉제는 층2 의 선행 조건이 맞았다** — 넣으니 **80프레임에 수렴**하고 정착 대비 중앙 이동이 **0.8 mm** 다(v4-05 의 96.5 mm ↔). v4-05 §4-2 ㉠ 이 값으로 확인됐다 | 전략 세션 |
| ㉣ | **커널 4종 + 결합 커널이 «준비 완료»** — 층1 전량 통과 · 결합은 순서까지 검증. **층2 만 열리면 바로 돈다**(12.6 s/프레임 · 80프레임 ⟹ 약 17분) | 전략 세션 |
| ㉤ | **자기충돌을 뺀 정의역에서 정착 blob 은 평형이 아니다** — 전량 시험계도 최대 **16.5 mm** 움직인 뒤 선다. 층2 초기값을 blob 그대로 쓸지, 이 정의역에서 «다시 정착»시킬지 | 전략 세션 |
| ㉥ | **층3 자는 걸어 두었다** — v3 정본 5행(목선 1.0641 · 가슴 4.4660 · 허리 26.1203 · 밑단 18.3743 · 소매 4.6177 mm). 상태만 갈아 끼우면 대조가 된다 | 전략 세션 |
| ㉦ | 맥 Metal 재실행(f64 `skip` 처리분) — v4-04 §4-1 ㉥ 이월 · 아직 미도착 | 사용자(승혁) |

---

## 정정 (2026-08-31 · v4-07 §1-① 실측 수령 · **원문 무삭제**)

**§1-② 의 ★★★ 블록에 적은 «읽기»가 실측에서 부정됐다.** 값은 전부 맞고, 그 값에 붙인 **해석**이 틀렸다.

```
 이 노트가 적은 읽기 —
   「수렴 판정은 창 순변위 ≤ 1e-4 m 인데 그 시점 실제 순변위가 8.067e-05 = 섭동(1e-7)의 807배다 ⟹
    계는 아직 섭동보다 3자릿수 빠르게 «기어가는 중»에 수렴으로 선언된다 ⟹
    1e-7 짜리 섭동은 «줄어들 시간»을 갖지 못하고 실려 간다.」
 ⟹ **「시간이 없어서」 부분이 무효.** v4-07 이 두 축으로 반증했다:
   ① **ε 축** — 섭동을 1e-7 → 1e-2 로 다섯 자릿수 키워도 보존비(중앙)가 **0.87~0.94 로 그대로**다
      (척도 불변). 「섭동이 작아서 묻힌다」가 아니다.
   ② **시간 축** — 수렴 «후» 400프레임 더 돌리면 각 실행의 창 순변위는 8.07e-05 → **1.749e-11**,
      섭동 쪽은 5.82e-05 → **2.363e-11** 로 «진짜로» 선다. **그런데 두 실행 사이의 차이는
      2.691139e-03 → 2.691147e-03 에서 얼어붙는다**(+100프레임부터 6자리 불변).
      「기어가는 중이라서」가 아니다 — **다 서고 나서도 안 줄어든다**.
 ⟹ 옳은 읽기는 **「이 계는 강체 방향으로 평형이 유일하지 않다」**이다(중립 모드).
   고정점이 0개이고 옷을 붙드는 것이 정지 마찰뿐이라, 옷은 «놓인 자리»에 그대로 선다.
```

| 이 노트의 항목 | 상태 |
|---|---|
| ② 판별력 수치(5.2278 · 33,637.78 · 미수렴) | ★ **유효** — v4-07 이 기본·1e-7 을 **인쇄 자릿수 전량 동일**하게 재현했다 |
| ㄷ 잔차 분해(강체 0.8854배 · 내부 5.2194배 · 잔차>섭동 19.8%) | ★ **유효** — v4-07 의 ε 훑기에서 1e-7 행으로 그대로 재현 |
| 「수렴 시점 순변위 = 섭동의 807배」라는 **사실** | ★ **유효**(값이다) |
| 그 사실에 붙인 **「그래서 섭동이 줄어들 시간이 없다」는 해석** | ❌ **무효**(위) |
| §4-2 ㉠ 「섭동을 수렴 문턱에 맞출지」라는 **처분 후보** | ❌ **닫혔다** — ε 로도 P 로도 계기가 교정되지 않는다(v4-07 갈래 B) |
| §4-2 ㉡ 「강체 병진 섭동이 이 계에 옳은 물음인가」 | ★ **답이 나왔다: 아니다**(v4-07 방향 실측 — x 로 민 것에 y 로 더 크게 답한다) |
| ①·①′ 봉제 층1 · 결합 커널 검증 | ★ **전량 유효**(이 판이 건드리지 않았다 · 커널 diff 0) |

**귀책** — 해석은 이 노트(CC)가 적고 판정문(전략 세션)이 받아 처방으로 굳혔다.
**v4-07 은 그 처방을 «집행해서» 부정했다** — 처방대로 ε 를 훑고 P 를 훑은 결과가 (라)였다.
문턱 이동 0 · 사후 갈래 신설 0.

**관련** — `docs/v4/07-계기교정.md` §1-① · [[../../.obsidian-log/함정/함정40-합성계가실제계의성질을안가진다]] 정정 사례 2
