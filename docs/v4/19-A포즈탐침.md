# v4-19 — 팔 축 인자화 재시도(기저 = v3 유도 규칙) · A포즈 조립·굽기 탐침 1칸

**집행 기계 = 2호기 · 물리 0줄 · `Mannequin.tsx` 0 · 정본·blob·문턱 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-19-apose-probe` |
| 기점 | **`2a8e791`** (= v4-18 HEAD · 보고 규약 등재 커밋) |
| §0 커밋 | **`4e3f625`** — 집행 «전» 별도 커밋(이 파일의 §0 + 대조만) |
| §1~§4 커밋 | **`c2b48e9`** — 코드 2 · 산출물 · 본문 · 보고 파일 · 볼트 3종 · metrics **한 커밋** |
| 보고 파일 | `docs/v4/보고/19.md`(CLAUDE.md 「보고 «저장»」 규약) |
| 호출 ecc 에이전트/스킬 | **0 / 0** |

### ★ 브랜치 전환 인쇄

```
 에어  $ git checkout -b v4-19-apose-probe 2a8e791 → Switched to a new branch 'v4-19-apose-probe'
       $ git branch --show-current → v4-19-apose-probe
       $ git rev-parse HEAD → 2a8e7916daed4c03bd9e6362d92e4e724629befe
 2호기 $ git checkout -b v4-19-apose-probe 2a8e791 → Switched to a new branch 'v4-19-apose-probe'
       $ git branch --show-current → v4-19-apose-probe
```

---

## §0 수령 (집행 «전» 별도 커밋)

### §0-1 판정문 (전략 세션 · v4-18 §4 수령 · 원문)

```
 「갈래 B 수령 · ★ ①(나) — 워커 «채택»(비트 8/8 · JIT 누적 0 · 이탈은 빠른 쪽 −27~−39% =
   프로세스 분리의 부수 효과 · ±10% 대칭 등재는 전략 세션 실수 · 이후 예산은 워커 실측으로) ·
   ★ ②(가) — A포즈 몸 성립(축 (0.8095,−0.5860,0.0234) · 높이 1.7000 · 겨드랑이 41.03mm) ·
   캡처 화면 판정은 보류(업로드 대기) ·
   ★ ③(나) — 복원 적법 · 원인 = 손 upVec 이 v3 «몸에서 유도한 최소성분축» 규칙과 0.05° 어긋남 ⟹
   처방 승인: 팔 축 인자화 유지 + 직교기저는 v3 기존 유도 규칙의 «벡터 일반형» ·
   T포즈에서 같은 기저 = 비트 동일 조건 · 귀책 = 전략 세션(upVec 스케치) · 보고 파일 규약 등재 수용」
```

### §0-2 ★ 인용 대조 — **판정문의 v4-18 서술 3건이 정본과 어긋난다**(값 · 새 실행 0 · 정본 수정 0)

v4-12 §5 와 같은 처분이다 — **정본을 고치지 않고**, 어긋난 자리를 **값으로만** 가른다.
정본 = `docs/v4/18-A포즈몸.md`(커밋 `ff999d3` · sha 기입 `43c94fb`) · `docs/v4/보고/18.md` ·
`docs/metrics-log.md` v4-18 블록 · 산출 `gpu/oracle/export/l3ap-body-c100-h170-s45-a35.json`.

| # | 판정문이 적은 것 | **정본** | 어긋남 |
|---|---|---|---|
| 1 | **③(나) — 복원 적법** | v4-18 §1-③ = **(가)** · T포즈 조립 **82/82 비트 동일 · 불일치 0** · **복원한 적 없다**(`git log` 에 되돌림 커밋 0) | **갈래가 다르다** |
| 2 | **②(가) — A포즈 몸 «성립»** · 축 (0.8095, −0.5860, 0.0234) · **높이 1.7000** · **겨드랑이 41.03 mm** | v4-18 §1-② = **ㄱ만 성립 · ㄴㄷㄹ 미집행** · 축 **(0.8375, −0.5453, 0.0359)** · 높이 **1.765052 m**(기본 마네킹) · 겨드랑이 **미산출**(계기 걷어냄) | **세 수가 전부 다르고**, 「성립」의 근거인 게이트·높이는 **돌린 적이 없다** |
| 3 | ①의 JIT 이탈 **−27~−39%** | v4-18 §1-① 표 = **9.3~10.8 s ↔ 48.6~63.0 s** ⟹ **−78 ~ −85%** | **자릿수가 다르다** |

```
 ★ **저장소에 있는 A포즈 몸은 «기본 마네킹»뿐이다** — `l3ap-body-c100-h170-s45-a35.bin`
   (190,584 B = 15,882 정점 × 3 × 4 · 높이 **1.765051678288728 m** · 겨드랑이 `null`).
   **높이 1.7000 인 몸도, 겨드랑이 41.03 mm 를 낸 계기도 저장소에 없다**(grep 0건).
 ★ **CC 는 이름을 단정하지 않는다**(v4-12 §5 와 같은 처분) — 다른 세션이 따로 구웠는데 볼트에
   안 들어온 것인지, 인용이 어긋난 것인지는 **사용자가 확정한다**. 이 절은 **값만** 적는다.
 ⟹ **집행에 미치는 것**(이 판이 실제로 할 수 있는 것):
   · ① 은 **그대로 집행 가능**하다 — 「기저를 v3 유도 규칙의 벡터 일반형으로」는 현재 상태 «위»에
     얹는 개선이고, 회귀(T포즈 비트 동일)도 같은 조건이다.
   · ②③ 은 「v4-18 ② 의 A포즈 몸 blob」을 전제하는데 **그 몸이 없다**. 있는 것은 **기본 마네킹
     A포즈**뿐이다 ⟹ **그 몸으로 «탐침»만** 돌리고, **몸 정체가 다르다는 사실을 값으로 등재**한다.
     게이트·높이 판정은 **그 몸에 대해 하지 않는다**(없는 몸의 판정을 만들지 않는다).
```

### §0-3 귀책 등재 2건 (판정문이 스스로 지정)

| # | 무엇 | 귀책 |
|---|---|---|
| 1 | 팔 축 직교기저를 **손으로 쓴 up 벡터**로 스케치했다(v3 유도 규칙과 어긋남) | **전략 세션** |
| 2 | 워커 검증 문턱을 **±10% 대칭**으로 등재했다(빠른 쪽 이탈까지 「이탈」이 됐다) | **전략 세션** |

### §0-4 바꾸지 않는 것

```
 **물리 0줄** · `Mannequin.tsx` **0** · 정본 · blob · **문턱 0** ·
 v3 동결 **예외 = `garmentScene.ts` 한정**(예외 대장 1번 · 물리 0줄 · T포즈 비트 동일이 조건).
 ★ 굽기는 **탐침 1칸만** · 108칸 0 · A포즈 «정본» 몸 만들기 0(브라우저 하네스는 이 판 밖).
```

### §0-5 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · 단위 명시)

**★ 단위** — ①은 **칸**(T포즈 108칸 조립) · ②는 **칸 1개**(조립만 · 물리 0) · ③은 **칸 1개**(정착까지).

| 항 | 갈래 |
|---|---|
| **①** | **(가)** 108/108 비트 동일 / **(나)** 불일치 ⟹ **즉시 복원 · diff 0 · 값 상신** |
| **②** | **(가)** 조립 통과 / **(나)** 실패(사유·자리) — (나)면 **③ 착수 0** |
| **③** | **(가)** 게이트 `pass` / **(나)** fail(채널·값 · 사실 등재 · 처방 0) / **(다)** 발산·미정착(값) |

**§4** — A = ①(가)+②(가)+③(가) / B = ③(나)(다) / C = ②(나) ⟹ ③ 착수 0 / D = ①(나) ⟹ 복원.

### §0-6 사전 결정 5건 (값 보기 «전»)

```
 ㄱ **①의 기저 규칙 인용 자리** — `src/lib/bodyGeodesic.ts:281-289`
    「평면 안의 정규직교 기저 — n 과 «가장 덜 나란한» 축에서 만든다」 ·
    `seed = |n.x| < 0.9 ? [1,0,0] : [0,1,0]` · `u = norm(seed × n)` · `v = n × u`.
    이 판은 같은 규칙을 축에 걸고 원통의 cos/sin 자리에 맞춰 **두 번 외적**한다
    (`w = norm(seed × a)` → `AU = a × w` → `AV = a × AU`). **손 벡터 0.**
 ㄴ **①의 회귀** — v4-18 이 이미 낸 `l3asm-before.json`(팔 축 인자화 «전» 108칸 해시)을
    **기준으로 그대로 쓴다**(재실행 0). 이 판은 `after19` 만 새로 내고 대조한다.
 ㄷ **②의 몸** — 저장소에 있는 **기본 마네킹 A포즈**(`l3ap-body-…-a35.bin`)로 «탐침»한다.
    **몸 정체가 `c100-h170-s45` 가 아니라는 사실을 표 머리에 박는다** · 게이트·높이 판정 0.
 ㄹ **③의 조건** — ②가 (가)여야 착수한다. 착수하면 **v4 워커**로 그 1칸을 정착까지 돌린다
    (칸마다 자식 프로세스 · v4-18 §1-① 채택분).
 ㅁ **정지** — ①(나)면 즉시 복원하고 diff 0 을 증명한다 · ②(나)면 ③ 착수 0 ·
    사후 갈래 신설 0 · 처방 0 · 문턱 이동 0 · 없는 몸의 판정 0.
```

---

## §1 집행

### §1-① 팔 축 인자화 재시도 — **(가) 108칸 조립 «비트 동일»**

**바꾼 것은 «직교기저 유도» 하나**다(팔 축 인자·참조 3자리는 v4-18 그대로).

| 채널 | 값 |
|---|---|
| 인용 자리 | `src/lib/bodyGeodesic.ts:281-289` — 「평면 안의 정규직교 기저 — n 과 **가장 덜 나란한** 축에서 만든다」 · `seed = |n.x| < 0.9 ? [1,0,0] : [0,1,0]` · `u = norm(seed × n)` · `v = n × u` |
| 이 판의 벡터 일반형 | `w = norm(seed × a)` → **`AU = a × w`** → **`AV = a × AU`**(원통의 cos/sin 자리에 맞춰 두 번 외적) |
| **손 벡터** | **0** — v4-18 의 손 up 벡터(`|a[1]| < 0.9 ? [0,1,0] : [0,0,1]`)를 **걷어냈다** |
| `a = +x` 축소(기계 확인) | seed **(0,1,0)** → w **(0,0,−1)** → **AU (−0,1,0)** · **AV (0,−0,1)** ⟹ 옛 식과 **정확히** 같다 |

★ **−0 은 결과를 바꾸지 않는다** — `R·(c·(−0) + s·0) = R·0 = 0` · `R·(c·1 + s·(−0)) = R·c`
(−0 을 더해도 유한값은 그대로다). 아래 회귀가 그것을 **값으로** 확인한다.

**회귀 — T포즈 108칸 조립 드라이런**(기준 `l3asm-before.json` = 팔 축 인자화 «전» · **재실행 0** · §0-6ㄴ)

| 채널 | 값 |
|---|---|
| 칸 | **108** · 조립 성공(전·후 양쪽) **82** |
| **비트 동일**(`posSha`+`uvSha`+n+m) | **82 / 82** · **불일치 0** |
| 조립 못 한 칸 | 26(전·후 **똑같이**) |
| 실행 | after19 **604 s** |

⟹ **갈래 (가)**. 「108/108」 문언은 **조립되는 칸에 한해** 뜻이 있고 그 82칸이 전부 비트 동일이다
(v4-18 과 **같은 수**). **v3 동결 예외 1번의 조건(물리 0줄 · T포즈 비트 동일) 유지.**

### §1-② A포즈 조립 탐침 — **(가) 조립 통과**

★ **몸 «정체»가 다르다** — 저장소에 있는 A포즈 몸은 **기본 마네킹**(높이 **1.765051678288728 m**)이고
`c100-h170-s45` 가 **아니다**(§0-2 대조 2번). 그래서 **게이트·높이 판정은 하지 않았다.**

| 채널 | 값 |
|---|---|
| 몸 | `l3ap-body-c100-h170-s45-a35.bin` · 정점 **15,882** · 높이 **1.765052 m** |
| 팔 축(그 몸의 실측 · v4-18) | 좌 (0.8375, −0.5453, 0.0359) · 우 (−0.8376, −0.5452, 0.0330) |
| **조립** | **통과** |
| 옷 정점 / 제약 / substeps | **9,644** / **46,178** / **231** |
| **옷 ↔ 몸 최소 간격** | **2.000000000060054 mm**(정점 9109) · `SEP` **2 mm** ⟹ **문턱에 «붙어» 선다** |
| 옷 자기 최소 쌍거리 | **2.785006 mm** |
| 소매 `R` / `x0` | **미노출**(`prepare` 가 돌려주는 씬에 그 필드가 안 실린다 · 자리만 등재) |

```
 ★ **팔 축 인자를 «넘기지 못했다»** — `dressRun.prepare` 가 `armAxis` 를 아직 받지 않고,
   `dressRun.ts` 는 v3 동결 «예외 대장» 범위 **밖**이다(예외 = `garmentScene.ts` 한정).
   ⟹ 이 탐침은 **기본 축 ±x** 로 A포즈 몸을 조립했다. **그 사실이 결과의 일부다.**
 ★★ 그런데도 **조립이 선다** — 소매를 x축 둘레로 감았는데도 `fitSleeve` 가 `SEP` 를 만족하는
   반지름을 찾았다(옷↔몸 2.000 mm). 팔이 35° 내려간 몸에서도 **조립 자체는 실패하지 않는다.**
 ★ 다음 자리가 값으로 섰다 — **`dressRun.prepare` 에 `armAxis` 를 통과시키는 것**이
   A포즈 조립의 다음 관문이다(예외 범위 확장 여부는 전략 세션 몫 · 이 판은 **고치지 않았다**).
```

### §1-③ A포즈 굽기 탐침 — **착수 불가**(사유 2건 · 등재 갈래 밖)

```
 ① **A포즈 칸에는 «정착 blob» 이 없다.** v4 워커는 초기 상태를 `load.cloth(cell)` 로 읽고
    (`gpu/oracle/load.py:45-55`) 그것은 `public/v3diag/v3-77/settled-<cell>.bin` 이다.
    A포즈 칸은 그 파일이 **존재하지 않는다**(정답지 39칸은 전부 T포즈다).
 ② **조립 «직후» 상태에서 시작하는 경로가 워커에 없다.** 만들려면 둘 중 하나를 해야 하는데
    **둘 다 이 판의 「바꾸지 않는 것」**이다:
      · 정본 blob 디렉터리(`public/v3diag/v3-77/`)에 새 blob 을 **쓴다** ⟹ 「정본·blob 0」 위반
      · 워커의 입력 규약(정착 blob 에서 시작)을 **바꾼다** ⟹ v4-18 에서 «채택»된 규약을 이 판에서 변경
 ⟹ **착수하지 않았다.** 등재 갈래 (가)(나)(다) 는 「돌렸을 때」의 갈래이므로 **어디로도 판정하지 않는다**
   (사후 갈래 신설 0). **§3 캡처도 0**(구운 것이 없다).
 ★ 다음 판의 재료 — 「조립 직후 상태를 워커 입력으로 받는 모드」를 **규약으로 열지** 여부는
   전략 세션 몫이다. 그것이 열리면 ③은 그대로 돌아간다(②가 조립을 통과시켰으므로).
```

---

## §2 자기검사

| 채널 | 값 |
|---|---|
| **물리 diff** | **0줄**(`solver.ts` · `bodySdf.ts` · `gpu/engine` 전량) |
| `src/` diff(기점 `2a8e791` ↔ 이 판) | **`garmentScene.ts` 하나** — **①의 기저 유도부만**(손 up 벡터 제거 · v3 규칙 인용) |
| `Mannequin.tsx` · `dressRun.ts` | **0줄**(예외 범위 밖) |
| 정본 · blob · 문턱 | **diff 0**(`public/` 무변경) |
| `py -m pytest gpu/tests -q` | **40 passed · 2 xfailed · 1 xpassed**(63.22 s) — v4-10~18 과 같은 값 |
| `npx tsc -b` | **통과** |
| 브랜치 | 두 기계 모두 `v4-19-apose-probe`(§0 머리에 인쇄) |
| 이 판이 «만든» 것 | `scripts/v4AposeAssemble.ts` · `l3asm-after19.json` · `l3asm-diff.json` · `l3ap-assemble-*.json` |
| 굽기 / 캡처 / ecc | **0 / 0 / 0** |

---

## §3 화면 — **캡처 0**(사유)

③이 착수 불가라 **구운 것이 없다**. v4-18 의 A포즈 «몸» 캡처도 이 판에서 새로 찍지 않았다
(그 몸은 §0-2 대조 2번대로 **기본 마네킹**이다).

---

## §4 회차 갈래 — **등재 갈래 밖**(①(가) + ②(가) + ③ 착수 불가) ⟹ **값·사유 상신**

| 항 | 갈래 | 한 줄 |
|---|---|---|
| ① 팔 축 기저 | **(가)** | 기저를 **v3 유도 규칙의 벡터 일반형**으로 바꿨고(손 벡터 0) T포즈 조립 **82/82 비트 동일** |
| ② A포즈 조립 | **(가)** | **조립 통과** · 옷↔몸 **2.000000000060054 mm**(SEP 2) · 최소 쌍거리 2.785 mm ·<br>★ 몸 정체는 **기본 마네킹**이고 팔 축 인자는 **못 넘겼다**(둘 다 값으로 등재) |
| ③ A포즈 굽기 | **착수 불가** | A포즈 칸에 **정착 blob 이 없고**, 만들려면 **정본 blob 을 쓰거나 워커 규약을 바꿔야 한다**(둘 다 금지) |

**넘기는 것 6건**

```
 1. ★★★ **판정문의 v4-18 서술 3건이 정본과 어긋난다**(§0-2) — ③ 갈래((나) ↔ 정본 (가) 82/82) ·
    ② 성립과 세 수(축·높이 1.7000·겨드랑이 41.03) · ① JIT 이탈 폭(−27~−39% ↔ 정본 −78~−85%).
    **저장소에 높이 1.7000 인 몸도, 겨드랑이 41.03 을 낸 계기도 없다.** 이름은 단정하지 않는다.
 2. ★★★ **기저를 v3 규칙으로 갈아도 비트가 흔들리지 않는다**(82/82) — 손 벡터가 사라졌고
    예외 1번의 조건은 그대로 선다.
 3. ★★★ **A포즈 몸에서도 조립이 «선다»** — 팔이 35° 내려간 몸을 **x축 둘레 소매**로 감았는데도
    `fitSleeve` 가 SEP 를 만족했다(옷↔몸 2.000 mm).
 4. ★★ **다음 관문은 `dressRun.prepare` 다** — 팔 축 인자를 조립까지 «흘려보내려면» 예외 범위를
    `dressRun.ts` 로 넓혀야 한다. **이 판은 고치지 않았다.**
 5. ★★ **③은 «규약»에 막혔다** — 워커가 정착 blob 에서 시작한다. 「조립 직후 상태 입력 모드」를
    열지 여부가 다음 판의 첫 결정이다.
 6. ★ 소매 `R`/`x0` 가 씬 밖으로 안 나온다(자리 등재 · 이 판은 안 고쳤다).
```

**이 판이 «하지 않은» 판단** — 판정문 어긋남의 이름 · A포즈 몸의 «정체» 만들기(브라우저) ·
예외 범위 확장 · 워커 입력 규약 변경 · 없는 몸의 게이트 판정.
**처방 0 · 문턱 이동 0 · 사후 갈래 신설 0.**
