# v4-04 — 검증 기준 3층 재정의 · 늘어남+굽힘 수렴 대조 · 몸 충돌 이식

**집행 기계 = 2호기(CUDA · 정본 기계) · `src/` diff 0 · 굽기 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-04-collision` |
| 기점 | **`0c74d5e`** (= v4-03 HEAD) |
| 워크트리 | `C:\Users\banba\fit-simulator` |
| 기계 | **`DESKTOP-OV72A46`** (win32 · CUDA) |
| §0 커밋 | **`143b30b`** — 집행 «전» 별도 커밋(sha 만 그 뒤 한 줄로 채웠다) |
| 호출 ecc 에이전트/스킬 | **0** |

---

## §0 수령 (집행 «전» 별도 커밋)

### §0-1 판정문 (전략 세션 · v4-03 §4 수령 · 원문)

```
 「갈래 C 수령 · 이식 정확(f64 1.11e-16) · 증폭 확인(M 600배 ↔ 오차 7.6e5배) ·
   ★ 귀책 = 전략 세션 2연속(궤적 일치를 검증 기준으로 잡은 것 자체가 오류 —
   증폭계에서 두 옳은 시뮬레이터는 궤적이 갈린다) ·
   처분 = 상한 재조정 «아님» · 검증 기준 3층 교체 · 문턱 이동 0 ·
   함정 등재(증폭계 궤적 상한 불성립)」
```

### §0-1′ 귀책 등재 — **전략 세션 2연속**(v4-02 · v4-03 상한 «형식»)

```
 v4-02 `M × 4·ULP` → 1.224배 초과 · v4-03 `K × deg × ULP × M` → 30.126배 초과.
 **두 번 다 이식은 정확했다**(f64 재현 1.110223e-16 / 3.388132e-21).
 결함은 계수가 아니라 **«재는 양»**이었다 — **궤적 일치**를 기준으로 잡은 것.
 ⟹ 함정 **39** 「증폭계에서는 궤적 일치를 상한으로 잡을 수 없다」 신설 ·
    원리 노트 「검증은 궤적이 아니라 도착점과 관측량으로」 신설.
 ★ CC 가 두 판 모두 **문턱을 옮기지 않고** 값을 그대로 상신했기 때문에(`xfail(strict)` 고정)
   두 번째 판에서 **형식 자체의 결함**이 드러났다. 결과에 맞춰 옮겼다면 발견되지 않았다.
```

### §0-2 검증 기준 **3층** (집행 «전» 등재 · 이후 변경 0)

| 층 | 재는 것 | 상한 |
|---|---|---|
| **층1** 1스텝 정확도 | 각 제약 커널을 **1스텝**만 적용해 **f64 참조와 대조** | **K × ULP_f32(그 자리 값)** · **M 없음** · K 는 커널 코드에서 «센» 값 |
| **층2** 수렴 도달 | 같은 초기 상태에서 **수렴 판정까지** 돌려 **최종 정점** 대조. **궤적 비교 0** | **이 판에서 등재하지 않는다** — 실측 분포를 먼저 본다(사후 문턱 금지 · 다음 판에서 유도) |
| **층3** 관측량 | 핏 리포트 5행 mm · 게이트 판정 | v3 정본과 대조 |

**층1 의 두 말을 «집행 전에» 못 박는다**(사후 해석 금지):

```
 ㄱ **「그 자리 값」** = **그 정점 «위치»의 최대 성분 크기**(max(|x|,|y|,|z|) of that vertex).
    좌표 하나는 0 을 지날 수 있고 **0 에는 자가 없다** — 좌표값 단독을 자로 쓰지 않는다.
    갱신식이 세 좌표를 섞으므로(외적·내적) 정점 단위가 물리적으로 옳은 자다.
    **참고로 «전역 최대|좌표|» 판과 «좌표별» 판도 함께 적는다**(판정은 아래 ㄴ 로 한다).
 ㄴ **K** = **제약 «하나»의 깊이**(늘어남 53 · 굽힘 24 · `gpu/engine/ulp.py`).
    한 정점은 한 스텝에 **deg 번** 갱신되므로 실제 사슬은 K×deg 이지만, **더 엄격한 쪽인
    K 단독으로 판정한다.** 초과하면 K×deg 판을 **값으로 함께 상신하되 판정은 바꾸지 않는다.**
```

### §0-3 바꾸지 않는 것

```
 `src/` · 정본 · blob · v3 동결 · **v4-02 / v4-03 커널 로직 diff 0**
```

### §0-4 사무 (이 커밋에서 집행)

| # | 항목 | 자리 |
|---|---|---|
| ㄱ | 함정 **39** 「증폭계 궤적 상한 불성립」 신설 | `.obsidian-log/함정/함정39-증폭계궤적상한.md` |
| ㄴ | 원리 「검증은 궤적이 아니라 도착점과 관측량으로」 신설 | `.obsidian-log/원리/검증은-도착점과-관측량으로.md` |
| ㄷ | **「f64 참조 시험의 정본 기계 = 2호기(CUDA)뿐 · Metal 은 f32 전용」** 등재 + 3층 기준 | `CLAUDE.md` v4 절 · `00-현재상태.md` |
| ㄹ | f64 참조 시험 **`skip`**(백엔드 미지원 시 · 사유에 백엔드명·오류 원문) | `gpu/tests/_backend.py` 신설 |
| ㅁ | `test_strip_matches_v3` → **`xfail(strict=False)`**(궤적 형식 폐기 예정 · 값은 표에) | `gpu/tests/test_stretch.py` |

### §0-5 갈래 (집행 «전» 등재 · 원문)

| 갈래 | 조건 | 처분 |
|---|---|---|
| **A** | ①~④ 전량 + ①**(가)** | 상신 |
| **B** | ① **(나)(다)** | 값 상신 · **②③ 집행** |
| **C** | ② 에서 v3·v4 가 **다른 평형**으로 수렴(육안으로도 다른 형상) | 값 상신 · **③ 착수 0** |
| **D** | 그 외 | 정지 |

**①의 하위 갈래** — (가) 늘어남·굽힘 **둘 다** 상한 안 ⟹ **커널 2종 정확 확정** /
(나) 한쪽 초과(값) / (다) 둘 다 초과.

**사후 갈래 신설 0.**

### §0-6 착수 «전» 알려진 사실

```
 · **맥 Metal 재실행 도착**(v4-02 §4-1 ㉤ 이월 **종결**) — 16 통과 · 6 실패 · 1 xfail.
   실패 5 = `Type f64 not supported`(Metal 백엔드 제약) · 실패 1 = `test_strip_matches_v3`
   **7.382e-05 > 7.153e-05**(비 1.032) — **백엔드 간 f32 차이의 첫 실측**(귀속 0 · 처방 0).
 · ③ 몸 충돌은 **몸 SDF 격자**가 필요하다. `bakeSdf` 산출물은 v4-02/03 덤프에 **없다** ⟹
   덤프를 또 한 번 넓혀야 한다(v3 를 읽는 창의 확장 · `src/` 는 여전히 **0줄**).
 · 층2 는 **수렴 판정**이 필요하다. 새 수를 만들지 않고 **v3 코드에서 인용**한다.
```

---

## §1 집행

### §1-① 층1 재판정 — **(가) 통과** · 커널 2종 정확 확정

상한 = **K × ULP_f32(그 자리 값)** · **M 없음**. 「그 자리 값」과 K 의 뜻은 §0-2 ㄱ·ㄴ 에
**집행 전에** 못 박아 두었다(정점 자 · K 단독).

| 커널 | K | deg | 최대 좌표차 | **비 최대(판정)** | 비 중앙 | 비 p99 | **초과 정점** | 판정 |
|---|---|---|---|---|---|---|---|---|
| **늘어남**(inplane) | 53 | 6 | 5.837018e-07 | **0.0924** | 0.0130 | 0.0509 | **0 / 9,388** | **통과** |
| **굽힘**(bend) | 24 | 12 | 4.114550e-07 | **0.1438** | 0.0124 | 0.0669 | **0 / 9,388** | **통과** |

**자를 바꿔 본 세 판**(판정은 위 표의 «정점 자 · K 단독» 하나로 한다 — §0-2 등재분)

| 자 | 늘어남 비 | 굽힘 비 | 비고 |
|---|---|---|---|
| **정점 자 · K 단독**(판정) | **0.0924** | **0.1438** | 가장 엄격한 조합 |
| 정점 자 · K×deg(참고) | 0.0154 | 0.0120 | 실제 사슬 길이. 통과 폭이 더 넓다 |
| 전역 최대\|좌표\| 자 · K 단독(참고) | 0.0924 | 0.1438 | 정점 자와 **같은 값** — 최대 오차 정점이 전역 최대 좌표 근처다 |
| 좌표별 자(문자 그대로 · **참고**) | **35.96** | — | ★ **0 근처 좌표에서 발산** |

```
 ★★ **§0-2 ㄱ 의 사전 등재가 실측으로 «값에 의해» 정당화됐다.**
   「그 자리 값」을 **좌표 하나**로 읽으면 비가 **35.96** 까지 튄다 — 0 을 지나는 좌표에는 «자»가 없기
   때문이다(ULP(0)=0). 정점 단위로 읽으면 **0.0924**. 두 값의 차이는 380배다.
   이 해석을 **실측을 보기 «전»에** 고정해 두었기 때문에 사후 선택이 아니다.
 ⟹ **①(가)** — 늘어남·굽힘 **두 커널 모두 상한 안**. **커널 2종 정확 «확정»**.
   (f64 참조와의 대조는 v4-02·v4-03 에서 이미 1.11e-16 / 3.39e-21 로 잡혀 있다.)
```

### §1-② 층2 첫 시행 — **상한 등재 0 · 실측만**

수렴 판정은 **v3 코드에서 인용**했다(새 수 **0**):

| 자리 | 원문 / 값 |
|---|---|
| `src/v3/dressRun.ts:N_WIN` | `Math.round(1 / (V3CONST.DAMP * V3CONST.DT))` ⟹ **10 프레임 창** |
| `src/v3/s4Gate.ts:S4_THRESHOLD.settleNetM` | `= TOL_SELF` ⟹ **1e-4 m**(0.1mm) · 「v3-22 형상 불변 채널」 |
| `src/v3/consts.ts:TOL_SELF` | `1e-4` |
| `src/v3/dressRun.ts:runFrames` | 창 순변위 = `max_v |pos_v − ref_v|`(창 시작 `ref` 대비 · 정점 최대) |

| 계 | 정밀도 | 수렴 | **프레임** | 마지막 창 순변위 | **최종 좌표차 최대** | 중앙 | p99 |
|---|---|---|---|---|---|---|---|
| **strip**(늘어남) | v3 (TS · f64) | 도달 | **180** | 6.124782e-05 | — | — | — |
| 〃 | v4 **f64** | 도달 | **180**(+0) | 6.124782e-05 | **4.940492e-15** | 5.222e-16 | 4.504e-15 |
| 〃 | v4 **f32** | 도달 | **150**(−30) | 9.666438e-05 | **3.065567e-04** | 4.371e-05 | 2.925e-04 |
| **hinge**(굽힘) | v3 (TS · f64) | 도달 | **110** | 1.741759e-05 | — | — | — |
| 〃 | v4 **f64** | 도달 | **110**(+0) | 1.741759e-05 | **1.110223e-16** | 0.000e+00 | 1.019e-16 |
| 〃 | v4 **f32** | ★ **미도달** | **20,000**(상한) | **2.479553e-04 «고정»** | ★ **4.962418e-01** | 0.000e+00 | 4.417e-01 |

최대\|좌표\| — strip 0.280079624 m · hinge 0.339307095 m. **상한은 등재하지 않았다**(§0-2 층2) ⟹
위 표는 **실측이고 판정이 아니다**.

```
 ★ **f64 는 두 계 모두 v3 와 «같은 프레임»에 수렴하고 «같은 자리»에 선다**
   (4.94e-15 / 1.11e-16 — v3 자신의 수렴 문턱 1e-4 의 **5e-11 · 1e-12 배**) ⟹ **이식의 도착점 증거**.
 ★ f32 strip 은 v3 보다 **30 프레임 «먼저»** 수렴한다 — ULP 아래 움직임이 반올림으로 사라져
   창 순변위가 먼저 문턱을 통과한다. 최종 좌표차 3.07e-04 는 **수렴 문턱(1e-4)의 3.07배**다.
```

#### ★★ hinge f32 — **수렴하지 않는다**. 정체를 값으로 갈랐다

| 항목 | v3 (f64) | v4 f32 |
|---|---|---|
| 자유 정점 3 위치 | (0, **−2.50857069e-04**, **−3.39307095e-01**) | (0, −6.05e-28, **−8.35548937e-01**) |
| 이면각 θ | **−7.393215e-04** rad | **−7.238670e-28** rad |
| 수렴 | 110 프레임 도달 | **미도달**(20,000) |
| 고정 정점 0·1·2 차 | — | 3.874e-10 ← **f32 캐스팅 몫**(0.009 → 0.008999999612569809), 움직인 것이 아니다 |

창 순변위 자취 — `0.2102 → 0.0852 → 0.0341 → 0.0128 → 0.0043 → 0.00205 → … →`
**19,000 · 19,500 · 20,000 프레임에서 전부 `2.479553e-04`**(비트까지 같다).

```
 ⟹ **감쇠가 멈추고 «등속 표류»가 된다.** 창마다 정확히 같은 거리를 움직인다 —
   f32 에서 위치 갱신량이 ULP 대역에 걸리면 속도 갱신 `(pos−prev)/h` 가 속도를 **재생성**해
   `decay` 가 상쇄하지 못한다. 날개는 −z 로 계속 미끄러져 v3 보다 **0.496 m** 멀리 간다.
 ★ 이면각은 **0 으로 눌려 있다**(−7.2e-28) — **제약이 «구속하는» 양은 만족돼 있고**,
   자유롭게 움직인 것은 **제약이 구속하지 않는 방향**이다.
```

#### ★★★ 계기 «판별력» 검사 — v3 ↔ v3(초기값만 1e-7 흔듦)

이 계기가 **구현 차이를 잴 수 있는지**부터 물었다. 같은 v3 코드를 초기값만 `1e-7` 흔들어 돌렸다.

| 계 | 수렴 프레임 | 최종 좌표차 최대 | **섭동 대비 이득** | 뜻 |
|---|---|---|---|---|
| **strip** | 180 → 180 | 9.458008e-11 | **0.000946배** | **수축계** — 섭동이 줄어든다 ⟹ **판별력 있음** |
| **hinge** | 110 → 110 | **1.000000e-07** | **1.00배** | ★ **중립 방향** — 섭동이 «그대로 남는다» |
| 〃 이면각 | | v3 −7.393215e-04 ↔ 섭동판 −7.393215e-04 | **동일** | 구속량은 유일하게 정해진다 |

```
 ★★★ **hinge 계는 평형이 «유일하지 않다».** 굽힘 제약은 스칼라 하나인데 자유 정점은 3자유도라
   **2차원 중립 방향**(이면각을 바꾸지 않는 이동)이 남는다. 그 방향으로는 **v3 자신도** 섭동을
   1:1 로 보존한다 ⟹ **「최종 정점 좌표」는 이 계에서 well-posed 하지 않다.**
 ⟹ f32 의 0.496 m 는 **「v4 의 굽힘이 v3 와 다르다」는 증거가 «아니다»** — 층1 에서 굽힘 커널은
   상한의 14% 이고, f64 로는 v3 와 같은 프레임·같은 자리에 선다. **귀속 0.**
 ⟹ **갈린 것은 «구현»이 아니라 «계기»다**(함정 13 계열 — 계기 정의역).
```

### §1-③ 몸 충돌 이식 — **착수 0**

```
 §0-5 갈래 판정(아래 §4)이 **정지**이므로 **한 줄도 쓰지 않았다.**
 착수 전에 확인해 둔 사실만 남긴다: 몸 SDF 격자(`bakeSdf` 산출)는 v4-02/03 덤프에 **없고**
 덤프 형식을 한 번 더 넓혀야 한다(§0-6). **그 확장도 하지 않았다.**
```

### §1-④ 「같은 옷」 기준 후보 — **착수 0**

```
 ②③ 실측 분포로 후보를 적는 항인데 ③ 이 미집행이고 ② 는 hinge 에서 판별력이 없다 ⟹
 **근거가 되는 분포가 갖춰지지 않았다.** 등재 0 · 후보 0.
```

---

## §2 자기검사

| # | 항목 | 결과 |
|---|---|---|
| ㉠ | ①② 표 | 위 §1 (③ 은 미집행) |
| ㉡ | **pytest(CUDA) 전량** | **28 passed · 2 xfailed · 1 xpassed** |
| ㉢ | `src/` diff | **0 파일**(기점 `0c74d5e` 대비) |
| ㉣ | 정본 diff | **0** — `public/v3diag` 쓰기 0 |
| ㉤ | blob sha | **36/36 일치** · 몸 27/27 |
| ㉥ | **v4-02 / v4-03 커널 로직 diff** | **0** — `stretch.py` · `bending.py` 한 줄도 안 바뀌었다 |
| ㉦ | `ulp.py` | **+30 / −0**(순수 추가 · 기존 K 상수·`bound` 불변) |
| ㉧ | **v4-02/03 산출물 바이트 불변** | 변경 0 — 층2 산출은 **새 파일**(`converge-*.bin`) |
| ㉨ | `npx tsc -b` | **통과**(exit 0) |
| ㉩ | 굽기 | 착장 `runFrames` **0회** · 조립 덤프 **0회**(이 판은 합성계만) |
| ㉪ | 호출 ecc 에이전트 / 스킬 | **0 / 0** |

**xfail 2건** — ㄱ v4-03 힌지 궤적 상한(폐기 예정 형식) · ㄴ v4-04 f32 hinge 미수렴(위 §1-②).
**xpass 1건** — `test_strip_matches_v3`(§0-4 ㅁ 로 `xfail(strict=False)` 처리 · 2호기에서는 통과하고
맥 Metal 에서 초과한다). 셋 다 **값을 표에 남기고 문턱은 옮기지 않았다.**

**§3 캡처 — 0장**(사전 등재 그대로).

---

## §4 판정 — **갈래 D**(정지)

```
 §0-5 대조:
   **A** = ①~④ 전량 + ①(가)  → ③④ 가 미집행이므로 **불성립**
   **B** = ① (나)(다)          → ① 은 **(가)** 였다 ⟹ **불성립**
   **C** = ② 에서 v3·v4 가 «다른 평형»으로 **수렴**  → f32 hinge 는 **수렴하지 않았다**
                                  (20,000 프레임 상한 도달) ⟹ **문언 미충족 · 불성립**
   **D** = 그 외 ⟹ **정지**    → **성립**
 ⟹ **갈래 D**. C 가 가장 가까웠고 그 처분(③ 착수 0)은 D 에 포함되므로 **행동은 같다**.
 **사후 갈래 신설 0 · 문턱 이동 0 · 상한 등재 0 · v3 diff 0 · v4-02/03 커널 diff 0.**
```

### §4-1 상신하는 값

```
 ① **(가) 통과** — 늘어남 비 0.0924 · 굽힘 비 0.1438 · **초과 정점 0/9,388**(둘 다) ⟹ 커널 2종 정확 확정
 ② strip — v3 180 ↔ f64 180(차 4.94e-15) ↔ f32 150(차 3.07e-04) · **셋 다 수렴**
    hinge — v3 110 ↔ f64 110(차 1.11e-16) ↔ **f32 미수렴**(20,000 · net 2.479553e-04 «고정» · 차 4.96e-01)
    **판별력** — 섭동 1e-7 대비 이득: strip **0.000946배**(수축) · hinge **1.00배**(중립)
```

### §4-2 넘기는 것

| # | 항목 | 몫 |
|---|---|---|
| **㉠** | **hinge 합성계를 층2 에서 «뺄지»** — 이 계는 **중립 방향** 때문에 최종 정점 좌표가 well-posed 하지 않다(v3 자신이 섭동을 1:1 보존). 층2 계기는 **구속된 계**만 써야 하는가, 아니면 **구속량(이면각)** 을 관측량으로 쓰는가. **CC 는 계기를 새로 정하지 않는다** | 전략 세션 |
| **㉡** | ★ **f32 정지 마찰 — 새 사실** — 위치 갱신량이 ULP 대역에 걸리면 `(pos−prev)/h` 가 속도를 **재생성**해 감쇠가 멈추고 **등속 표류**가 된다(창 순변위가 2.479553e-04 에 비트까지 고정). **실옷에서도 일어나는지**는 미확인 — 다음 판의 표적 후보 | 전략 세션 |
| ㉢ | **층1 은 작동한다** — 두 커널이 초과 정점 0 으로 통과했고, 「그 자리 값」의 **사전 정의가 380배 차이를 갈랐다**(정점 자 0.0924 ↔ 좌표별 자 35.96). 층1 을 **다음 커널(몸 충돌)의 1차 관문**으로 그대로 쓸 수 있다 | 전략 세션 |
| ㉣ | **몸 충돌은 착수 0** — 재개 시 필요한 것은 **몸 SDF 격자 덤프**(`bakeSdf` 산출 · 현재 덤프에 없다) | 전략 세션 |
| ㉤ | **층3(관측량)은 아직 한 번도 안 돌았다** — 핏 리포트 5행 mm·게이트 판정을 v3 정본과 대조하는 경로가 v4 에 없다 | 전략 세션 |
| ㉥ | 맥 Metal — **f64 전량 skip** 처리로 재실행 시 「실패 5」가 사라진다. `test_strip_matches_v3` 는 **xfail(strict=False)** 라 2호기에서 xpass · 맥에서 xfail 로 **양쪽 다 초록**이 된다(값은 표에 남는다) | 사용자(승혁) |
