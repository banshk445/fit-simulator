# v4-15 — 넓이 검증(정답지 표본 8칸) · f32 전 정의역 · 해소 커널 비용 분해

**집행 기계 = 2호기(CUDA · 정본 기계) · `src/` diff 0 · 굽기 0(정본 재생성 0) · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-15-breadth` |
| 기점 | **`18b41ac`** (= v4-14 HEAD) |
| 워크트리 | `C:\Users\banba\fit-simulator` |
| 기계 | **`DESKTOP-OV72A46`** (win32 · CUDA · GTX 1660 Ti · Taichi 1.7.4 · llvm 15.0.1 · py 3.12.10) |
| §0 커밋 | **`44baf2b`** — 집행 «전» 별도 커밋(이 파일의 §0 + §0-2 사무 3건만) |
| §1~§4 커밋 | **`7a0b05b`** — 계기 4 · 산출물 · 본문 · 볼트 3종 · `docs/metrics-log.md` **한 커밋** |
| 호출 ecc 에이전트/스킬 | **0 / 0** (`strategist` 서브에이전트도 0) |

---

## §0 수령 (집행 «전» 별도 커밋)

### §0-1 판정문 (전략 세션 · v4-14 §4 수령 · 원문)

```
 「갈래 A 수령 · ★★★ 이식 «닫힘» — 커널 5종 전부 층1 통과 · 쌍 집합 41,567 완전 일치 ·
   ★★★ 「같은 옷」 «선언»(전략 세션 확정) — 표시값 5/5 · 대역 이름 5/5 · 정점 소속 5/5 ·
   대역 n 5/5 · pass 일치 · fails 양쪽 빈 집합 ⟹ ㉠㉡ 두 읽기 모두 성립 ·
   ★★ v4 가 S4 게이트 첫 통과 · v4-13 ③a 관통 미결은 정의역 완성으로 닫힘(내 「귀속 0」 판단 유효) ·
   ★★ 정착 자리에 머문다(180프레임에 0.090004 → 0.090304 mm) ·
   ★ 새 원리 채택: 「순서도 이식의 조건이다」 — Gauss-Seidel 은 접촉 순서가 곧 결과
     (재현 안 하면 3.4e+09배 · 76 정점 초과) · 함정 후보 「자연 순서를 정답 순서로 안다」 ·
   ★ 속도 12.45배 · 최상위 비용 = 해소 직렬 커널(grid 1×block 1) ·
   ★ 다음 순서 = 넓이 검증 → 최적화 → 108칸(넓이가 최적화보다 «먼저»)」
```

### §0-2 사무 (이 커밋에서 «함께» 집행 · 3건)

```
 ① **원리 노트 신설** — `.obsidian-log/원리/순서도-이식의-조건이다.md`
    (판정문이 「새 원리 채택」으로 지정 · v4-14 §1-①ㄷ 의 값이 근거)
 ② **함정 후보 등재** — `.obsidian-log/함정/함정후보-자연순서를-정답순서로-안다.md`
    ★ **번호는 붙이지 않았다** — 판정문이 「번호는 대장 몫」이라고 못 박았다.
 ③ **인수인계 v4 절 교체** — 「이식 «닫힘» · 「같은 옷」 «선언»」로. 선언의 주체가
    전략 세션임을 문면에 남긴다(v4-14 는 「조건이 섰다」까지만 적었다).
```

### §0-3 바꾸지 않는 것 (회차 프롬프트 원문)

```
 `src/` · 정본 · blob · v3 동결 · `engine/` **9파일 diff 0** · 최적화·튜닝 **0** · 문턱 **0**.
 ⟹ 이 판이 만드는 것은 **새 실행기·계기와 산출물**뿐이다(커널 신설 0 · 물리 식 0줄).
 ★ `full.py` · `full_color.py` · `full_color2.py` · `full_par.py` · `full_sc.py` · `selfcol.py` ·
   `stretch.py` · `bending.py` · `seam.py` · `collide.py` · `ulp.py` **바이트 불변**.
 ★ **굽기 0** = 정답지 blob 재생성 0. **몸 SDF·장면 «내보내기»는 굽기가 아니다**(v4-02 §1-② 등재 ·
   `scripts/v4Export.ts` 는 `prepare` 를 부르고 `runFrames` 를 **0회** 부른다) — 8칸을 돌리려면
   그 칸의 장면·SDF 가 있어야 하므로 이 판에서 **내보낸다**(산출은 `gpu/oracle/export` · 정본 밖).
 ★ v4-02~14 산출물 바이트 불변(이 판 산출은 새 접두 `l3br-` 로 판다) · 108칸 착수 0 · 캡처 0.
```

### §0-4 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · **단위 명시** · 함정 43)

**★ 단위** — ①의 단위는 **칸**(정답지 39칸 중 표본 8칸). 각 칸 안의 단위는 **프레임**
(1프레임 = 229 서브스텝 · 창 = 10프레임). ③의 단위는 **서브스텝**.

**① 넓이 검증**(8칸 · v4 = f64 × 병렬 × CUDA + 셀프 충돌 · 정본 blob 헤더의 프레임 수까지)

| 갈래 | 조건 (단위 = **칸**) |
|---|---|
| **(가)** | **8/8** 이 「같은 옷」 3조건(표시 **5/5** · 대역 이름 **5/5** · `pass` 일치) 성립 |
| **(나)** | 일부 칸에서 불일치(**칸·행·값** 명시) |
| **(다)** | 일부 칸이 **정착 미도달·발산**(칸·값) |

**② f32 전 정의역**(2칸 · f32 × 병렬 × CUDA + 셀프 충돌 · 같은 조건)

| 갈래 | 조건 |
|---|---|
| **(가)** | 정착 도달 + 「같은 옷」 3조건 성립 ⟹ **f32 채택 후보**(속도 이득 등재) |
| **(나)** | 정착하나 조건 일부 불일치(값) |
| **(다)** | **진동·발산**(값) |

**③ 해소 커널 비용 분해** — 사실만 · **처방 0 · 최적화 0 · 방안 제시 0**.

**§4 회차 갈래** — A = ①(가) + ②③ 집행 ⟹ 상신 / B = ①(나) ⟹ 칸·값 상신 · ②③ 집행 /
C = ①(다) ⟹ 값 상신 · **② 착수 0** · ③ 집행 / D = 그 외 ⟹ 정지. **사후 갈래 신설 0.**

### §0-5 집행 «전» 사전 결정 8건 (값을 보기 «전»에 적는다)

```
 ㄱ **8칸을 고르는 규칙**(손 고르기 0 · 기계가 고른다 · `gpu/l3br_pick.py` 신설이 인쇄한다) —
    후보 = 정답지 39칸(`public/v3diag/v3-77/settled-*.bin` 디렉터리에서 «뜬다» · 손 목록 0) 을
    **칸 id 사전순**으로 정렬한 것. 슬롯 8개를 **이 순서로** 채운다:
      ① 가슴 최소 ② 가슴 최대 ③ 키 최소 ④ 키 최대 ⑤ 어깨 최소 ⑥ 어깨 최대 ⑦ 사이즈 S ⑧ 사이즈 XL
    각 슬롯은 그 조건을 만족하는 칸 중 **아직 뽑히지 않은 첫 칸**(사전순)을 고른다
    (= 회차 프롬프트의 「겹치면 다음 후보로 밀되」). 축의 최소·최대는 **39칸에 실재하는 값**에서 뜬다.

 ㄴ **②의 「기본몸 M 포함」과 「같은 8칸 중」이 어긋난다 — 어긋남을 등재하고 이렇게 집행한다.**
    규칙 ㄱ 은 축의 «극단»과 사이즈 S·XL 만 뽑으므로 기본몸 M(`c100-h170-s45_M` · 축 기준점 ·
    사이즈 M)은 **뽑히지 않는다.** 두 문언을 다 지킬 수 없으므로:
      · ① 은 **규칙 ㄱ 의 8칸 그대로** 집행한다(회차 프롬프트 ①의 문언이 규칙을 정했다).
      · ② 는 **기본몸 M + 8칸 중 사전순 첫 칸**, 합 **2칸**으로 집행한다
        (「2칸」과 「기본몸 M 포함」을 지키고, 「같은 8칸 중」은 한 칸에서 어긋난다).
    ★ 기본몸 M 을 넣는 실익도 값에 있다 — v4-14 가 그 칸을 **f64 로 이미 돌았다**(2.351 s/프레임 ·
      「같은 옷」 3조건 성립) ⟹ f32 대조의 **기준선이 이미 있는 유일한 칸**이다.

 ㄷ **「같은 조건」의 정의**(v4-14 §0-5ㅂ 를 그대로) — 프레임 수는 **그 칸 정본 blob 헤더의
    `frame` 필드에서 읽는다**(손 상수 0 · 칸마다 다르다) · 정착 문턱 `tol` 은 v4-13/14 와 같은 자리
    (`cellconv7-c100-h170-s45_M-all-e0-x0.json` 의 `tol`)에서 읽는다 · **정착 도달과 헤더 프레임 수 중
    먼저 오는 쪽**에서 멈춘다 · N_WIN 10 · substeps 는 그 칸 장면의 값 · 초기 상태 = 그 칸 blob 의 f64 원본.

 ㄹ **게이트의 정착 채널**(v4-14 §1-③ㅁ 과 같은 처분) — v4 쪽 `NET` = 그 실행의 **마지막 창 순변위** ·
    v3 정본 쪽 `NET` = `scripts/v4SelfNet.ts` 로 **그 칸 blob 에서 앞으로 10프레임** 간 창 순변위.
    **전방 창이라는 사실을 그대로 등재한다.**

 ㅁ **「같은 옷」 3조건의 정의**(v4-14 판정문이 확정한 ㉠ 읽기) — 표시값 1자리 5/5 ·
    **대역 «이름»** 5/5 · 게이트 **`pass` 값** 일치. **정점 대역 소속·대역 정의역 n·`fails` 집합·
    진단 2자리는 «병기»**하되 갈래 판정에 걸지 않는다(v4-14 에서는 둘 다 성립했다 · 이 판도 둘 다 적는다).

 ㅂ **실패 칸의 처분**(회차 프롬프트 ★) — **실패로 등재하고 문턱·순서·구현을 고치지 않는다**(함정 14).
    재시도 0 · 파라미터 조정 0 · 「그 칸만 예외」 0.

 ㅅ **예산**(v4-12/13/14 와 같은 형태 · 값 보기 «전») — 칸마다 **1프레임 probe 없이** 바로 돌리되,
    한 칸이 **30분**을 넘으면 그 칸을 **정지**시키고 그 사실과 그때까지의 궤적을 등재한다(갈래 (다) 후보).
    8칸 합이 **4시간**을 넘으면 남은 칸을 **미집행으로 등재**한다(손대지 않고 사실만).
    ★ 장면·SDF 내보내기는 예산 밖(§0-3 ★) · **GPU 동시 실행 0**.

 ㅇ **③의 채널**(사실만) — 프로파일러로 ㉠ 커널별 누적 시간 **상위 5** ㉡ 해소 직렬 커널의
    **호출 수·평균·grid/block** ㉢ 셀프 충돌 전체가 한 서브스텝에서 차지하는 **비율** ㉣ `_pairs`(광역)와
    `_resolve`(해소)의 **분담**. **「무엇을 병렬화하면 얼마가 준다」의 재료만** 낸다 — **방안 제시 0.**
    ★ 계기를 켠 시행은 **다른 시행**이다(v4-14 §2-2 ★ 와 같은 처분 · 벽시계와 섞지 않는다).
```

### §0-6 예산 · 실행 규약

```
 ① 8칸 × (정본 blob 헤더 프레임 수 상한) · ② 2칸 · ③ 1칸(프로파일러) ·
 전부 배경 실행 + 로그 파일 · 캡처 0(§3) · 굽기 0(정본 재생성 0) · 108칸 착수 0.
 ★ 이 판은 **넓이**를 묻는다 — 한 칸에서 선 것이 여덟 칸에서도 서는가.
```

---

## §1 집행

### §1-① 넓이 검증 8칸 — **(나)** 「같은 옷」 3조건 **3/8** (불일치는 전부 **표시 반올림 경계**)

#### ㄱ 표본 — **기계가 골랐다**(`gpu/l3br_pick.py` 신설 · 손 고르기 0)

규칙 ㄱ 을 **39칸 위에** 그대로 적용한 8칸(A)과 **«대조 가능» 37칸 위에** 적용한 8칸(B)을 둘 다 냈다.
**두 집합은 한 칸만 다르다** — A 에만 `c87.5-h155-s40_L` · B 에만 `c87.5-h155-s45_XL`.

```
 ★ **보류 2칸**(값으로 뜬 사실 · v4-02 §1 이 이미 「정점 대조 가능 칸은 37」로 등재) —
   `c87.5-h155-s40_L`(blob n **9888** ↔ 지금 조립 **9948**) · `c87.5-h155-s40_XL`(**11118** ↔ **10846**).
   정답지 blob 을 현재 장면에 **실을 수조차 없다**(정점 수가 다르다) ⟹ **실행 «불가»이지 실패가 아니다.**
 ⟹ **집행은 B 로 했다.** 이 결정은 **값을 보기 «전»**, 그리고 «실행 결과»가 아니라 **자료 가용성**을
   근거로 내렸다(문턱·순서·구현은 한 줄도 고치지 않았다 · 함정 14). A 와 B 의 차 한 칸을 여기 적는다.
```

| 슬롯 | 칸 | 바디 | n | 정본 blob 프레임 | substeps |
|---|---|---|---|---|---|
| 가슴 최소 | **`c87.5-h155-s45_XL`** | c87.5-h155-s45 | 10,846 | 210 | 254 |
| 가슴 최대 | **`c122.5-h155-s40_L`** | c122.5-h155-s40 | 10,434 | 200 | 260 |
| 키 최소 | **`c100-h155-s45_L`** | c100-h155-s45 | 9,948 | 200 | 253 |
| 키 최대 | **`c100-h185-s40_L`** | c100-h185-s40 | 10,518 | 200 | 229 |
| 어깨 최소 | **`c100-h170-s40_L`** | c100-h170-s40 | 10,152 | 200 | 231 |
| 어깨 최대 | **`c100-h155-s50_M`** | c100-h155-s50 | 9,184 | 200 | 237 |
| 사이즈 S | **`c100-h155-s50_S`** | c100-h155-s50 | 8,354 | 190 | 234 |
| 사이즈 XL | **`c100-h155-s45_XL`** | c100-h155-s45 | 10,846 | 220 | 257 |

바디 **6종** · 기본몸 M 은 규칙으로 **뽑히지 않는다**(§0-5ㄴ 가 미리 등재한 어긋남 · ②가 따로 돈다).
장면·SDF 는 이 판에서 **내보냈다**(`scripts/v4Export.ts` · 굽기 아님 · §0-3 ★).

#### ㄴ 실행 — **8/8 정착 도달 · 발산 0 · 예산 자르기 0**

| 칸 | 정착 | 프레임 | 도달 시 창 순변위 | s/프레임 | **중앙** | **p99** | **최대**(mm) |
|---|---|---|---|---|---|---|---|
| `c87.5-h155-s45_XL` | ✅ | **20**/210 | 3.340268e-05 | 2.804 | 0.005506 | 0.047102 | **0.117057** |
| `c122.5-h155-s40_L` | ✅ | **20**/200 | 4.643096e-05 | 3.107 | 0.006101 | 0.073677 | **0.113552** |
| `c100-h155-s45_L` | ✅ | **20**/200 | 4.019445e-05 | 3.037 | 0.006766 | 0.064965 | **0.123158** |
| `c100-h185-s40_L` | ✅ | **10**/200 | 9.012449e-05 | 2.612 | 0.005771 | 0.047644 | **0.090124** |
| `c100-h170-s40_L` | ✅ | **10**/200 | 9.773627e-05 | 2.674 | 0.006396 | 0.051889 | **0.097736** |
| `c100-h155-s50_M` | ✅ | **20**/200 | 4.351445e-05 | 2.803 | 0.008376 | 0.059210 | **0.130446** |
| `c100-h155-s50_S` | ✅ | **30**/190 | 7.368777e-05 | 2.724 | 0.006534 | 0.072763 | **0.166926** |
| `c100-h155-s45_XL` | ✅ | **30**/220 | 7.166808e-05 | 3.152 | 0.006461 | 0.061382 | **0.228007** |
| (기준선) `c100-h170-s45_M` | ✅ | **10**/180 | 9.000416e-05 | 2.345 | 0.006600 | — | **0.090004** |

```
 ★ **8/8 이 정착에 도달한다**(문턱 1e-4 · 칸마다 f=10~30) · **발산·NaN·degenerate 0** ·
   예산 30분 초과 **0칸** · 8칸 합 벽시계 **약 8분**(예산 4시간의 3%).
 ★ **회차 간 비트 재현** — 기준선 칸(`c100-h170-s45_M`)을 **이 판의 실행기로 다시** 돌린 값이
   v4-14 §1-③ㄴ 과 **인쇄 자릿수 전량 동일**(f=10 · 9.000416e-05 · 중앙 0.006600 · 최대 0.090004).
   ⟹ 칸을 인자로 받게 일반화한 실행기가 v4-14 와 «같은 것»을 잰다.
 ★ 간극의 크기가 **칸마다 고르다** — 중앙 0.0055~0.0084 mm · 최대 0.090~0.228 mm.
   v4-14 의 한 칸(중앙 0.006600 · 최대 0.090004)이 **8칸에서 같은 자릿수로 재현된다.**
```

#### ㄷ 리포트 5행 · 제품 문턱 (읽기 = ㉠ 확정본 · 계기 3종 **수정 0** · 물리 0프레임)

| 칸 | 표시값 | 대역 이름 | 정점 소속 | 대역 n | 진단 2자리 | **3조건** |
|---|---|---|---|---|---|---|
| `c87.5-h155-s45_XL` | **5/5** | 5/5 | 3/5 | 4/5 | 1/5 | **성립** |
| `c122.5-h155-s40_L` | 4/5 | 5/5 | 5/5 | 5/5 | 0/5 | 불성립 |
| `c100-h155-s45_L` | **5/5** | 5/5 | 5/5 | 5/5 | 4/5 | **성립** |
| `c100-h185-s40_L` | 4/5 | 5/5 | 3/5 | 4/5 | 1/5 | 불성립 |
| `c100-h170-s40_L` | 4/5 | 5/5 | 5/5 | 5/5 | 1/5 | 불성립 |
| `c100-h155-s50_M` | 3/5 | 5/5 | 5/5 | 5/5 | 0/5 | 불성립 |
| `c100-h155-s50_S` | 4/5 | 5/5 | 4/5 | 5/5 | 0/5 | 불성립 |
| `c100-h155-s45_XL` | **5/5** | 5/5 | 4/5 | 5/5 | 0/5 | **성립** |
| **행 단위 합계(40행)** | **34/40** | **40/40** | 34/40 | 38/40 | 7/40 | **3/8 칸** |

**표시값이 갈린 행 6개(5칸) — 전부 «반올림 경계»다**

| 칸 | 행 | v3 정본 → v4 | 중앙 차(mm) |
|---|---|---|---|
| `c100-h155-s50_M` | 허리 | 30.5 → **30.4** | **−0.0083** |
| `c100-h155-s50_M` | 소매 | 5.4 → **5.3** | **−0.0444** |
| `c100-h155-s50_S` | 소매 | 5.1 → **5.2** | **+0.0181** |
| `c100-h170-s40_L` | 밑단 | 26.7 → **26.8** | **+0.0097** |
| `c122.5-h155-s40_L` | 허리 | 42.1 → **42.2** | **+0.0279** |
| `c100-h185-s40_L` | 가슴 | 4.0 → **4.2** | **+0.1782** |

```
 ★ 여섯 행 중 **다섯이 0.05 mm 미만**에서 1자리 표시를 넘긴다(0.0083 · 0.0097 · 0.0181 · 0.0279 · 0.0444).
   나머지 하나(`c100-h185-s40_L` 가슴 **+0.1782 mm**)만 «자릿수»가 다르다.
 ★ **대역 이름은 40/40 전량 일치**(눌림/밀착/여유의 이름이 갈린 행 0). 「어느 대역인가」는 전 칸에서 같고,
   갈린 것은 **그 안의 mm 를 1자리로 접는 자리**다.
 ★ **문턱을 옮기지 않았다 · 자리수를 고르지 않았다**(둘 다 코드에서 인용 · 함정 14).
```

#### ㄹ 게이트 — **8/8 `pass` 일치 · `fails` 양쪽 «빈 집합»**

| 채널 | 문턱 | **v3 정본**(8칸 범위) | **v4**(8칸 범위) |
|---|---|---|---|
| ③a 관통 최대 | ≤ 0.5 mm | **0.3013 ~ 0.4532** ✅ | **0.3005 ~ 0.4532** ✅ |
| 자기관통 교차 | = 0 | **0**(전 칸) ✅ | **0**(전 칸) ✅ |
| 보조 장치 · 발산 | 0 · 없음 | 0 · 없음 ✅ | 0 · 없음 ✅ |
| 정착 창 순변위 | ≤ 0.1 mm | **0.0227 ~ 0.0849** ✅ | **0.0334 ~ 0.0977** ✅ |
| **`pass`** | — | **true**(8/8) | **true**(8/8) |
| **`fails`** | — | **[]**(8/8) | **[]**(8/8) |

★ **v4 가 여덟 칸 전부에서 S4 게이트를 통과한다.** v4-14 의 한 칸에서 선 것이 **넓이에서도 선다.**
★ `fails` 집합까지 **8/8 일치**(㉡ 읽기로도 게이트는 갈리지 않는다).

#### ㅁ 갈래 판정

```
 (가) = 「8/8 이 3조건 성립」 — **3/8** 이므로 **부정**
 (다) = 「일부 칸이 정착 미도달·발산」 — **8/8 정착 도달 · 발산 0** 이므로 **부정**
 ⟹ **갈래 (나)** = 일부 칸에서 불일치. 칸·행·값은 위 표 그대로.
 ★ **실패 칸을 고치지 않았다**(§0-5ㅂ) — 재시도 0 · 파라미터 조정 0 · 「그 칸만 예외」 0 · 문턱 이동 0.
 ★ 갈리는 채널은 **표시값 하나**다 — 대역 이름 40/40 · 게이트 8/8 은 **전 칸 일치**.
```

### §1-② f32 전 정의역 — **(다) 정착 미도달**(셀프 충돌이 들어와도 f32 는 서지 못한다)

2칸 = **기본몸 M**(`c100-h170-s45_M` · §0-5ㄴ) + 8칸 중 사전순 첫 칸(`c100-h155-s45_L`).

| 칸 | 정밀도 | 프레임 | 정착 | 마지막 창 순변위 | **중앙** | p99 | **최대**(mm) | s/프레임 |
|---|---|---|---|---|---|---|---|---|
| `c100-h170-s45_M` | **f64** | 10/180 | **도달** | 9.000416e-05 | **0.006600** | — | **0.090004** | 2.345 |
| `c100-h170-s45_M` | **f32** | **180/180** | **미도달** | **9.610826e-03** | **15.5228** | 24.6081 | **28.4843** | 2.260 |
| `c100-h155-s45_L` | **f32** | **200/200** | **미도달** | **8.105995e-03** | **4.4637** | 17.7917 | **25.3655** | 2.230 |

```
 ⟹ **갈래 (다)** — 두 칸 다 헤더 프레임 수를 «완주»하고도 문턱(1e-4)에 못 든다.
   마지막 창 순변위가 문턱의 **81배 · 96배**다. **f32 채택 후보 아님.**
 ★ **같은 칸·같은 코드에서 정밀도 하나만 바꾼 값이다** — f64 는 f=10 에 정착하고 중앙 0.0066 mm,
   f32 는 180프레임을 돌고도 중앙 **15.5228 mm**(**2,352배**).
 ★ **속도 이득도 없다** — f32 2.260 ↔ f64 2.345 s/프레임(**3.6%**). 셀프 충돌이 들어온 뒤로는
   비용이 정밀도가 아니라 **접촉 수와 직렬 해소**에 걸려 있다(§1-③이 그 자리를 값으로 보인다).
 ★ v4-12 가 「미수렴 원인 = f32」로 확정한 것이 **정의역이 완성된 뒤에도 그대로 선다** —
   셀프 충돌은 f32 의 미수렴을 **고치지 않는다**(원인 단정은 v4-12 등재분 그대로 · 새 단정 0).
```

### §1-③ 해소 커널 비용 분해 — **사실만**(처방 0 · 최적화 0 · 방안 제시 0)

계기 = `gpu/l3br_prof.py`(신설) · 칸 = 기본몸 M · f64 · 서브스텝 **10회** · **프로파일러 켠 시행**.
그 시행의 셀프 충돌 = 서브스텝당 쌍 **41,580.0** · 접촉 **2,082.7** · 해소 **546.7**.

#### ㉠ 커널별 누적 시간 상위 5 (총 **21.9388 ms/서브스텝**)

| # | 커널 | ms/서브스텝 | 호출/서브스텝 | grid × block | **비율** |
|---|---|---|---|---|---|
| 1 | **`_resolve …_serial`**(해소) | **8.0404** | 1 | **1 × 1** | **36.65%** |
| 2 | `_pairs …_range_for`(광역) | 4.4768 | 1 | 768 × 128 | 20.41% |
| 3 | `_narrow …_range_for`(협역) | 3.0678 | 1 | 768 × 128 | 13.98% |
| 4 | `substep_a …_serial`(봉제) | 2.0774 | 1 | **1 × 1** | 9.47% |
| 5 | `substep_b …_range_for`(몸 충돌) | 0.6404 | 1 | 74 × 128 | 2.92% |

#### ㉡ 해소 직렬 커널

**`_resolve_c102_0_kernel_0_serial`** · 호출 **1/서브스텝** · **8.0404 ms/서브스텝**(누적 80.404 ms/10서브스텝) ·
**grid 1 × block 1** · 전체의 **36.65%**. 그 서브스텝의 접촉은 **2,082.7개**이고 그중 **546.7개**가 실제로 밀린다
⟹ 접촉 하나당 **3.86 µs**(= 8.0404 ms ÷ 2,082.7).

#### ㉢ 한 서브스텝의 분담

| 구간 | 비율 |
|---|---|
| `substep_a + substep_b`(예측 · λ · 늘어남 · 굽힘 · 봉제 · 몸 충돌 · 속도) | **21.05%** |
| **셀프 충돌 전체** | **78.95%** |

#### ㉣ 셀프 충돌 «안»의 분담

| 구간 | 비율 |
|---|---|
| **`_resolve`(해소 · 직렬)** | **36.65%** |
| **`_pairs`(광역 · 셀 안 쌍 열거)** | **20.71%** |
| `_narrow`(협역 · 15특징) | 14.37% |
| 그 밖(스칼라 읽기 등 · 호스트 동기화) | 3.80% |
| `_fill`(격자 적재) | 1.41% |
| `_rank`(순서 재현) | 1.09% |
| `_boxes`(상자) | 0.69% |
| `_cells`(셀 범위) | 0.23% |

```
 ★ **재료만 적는다**(§0-5ㅇ) — 「무엇을 병렬화하면 얼마가 주는가」의 상계는 이 표에서 곧바로 읽힌다:
   해소를 **완전히 없애도** 서브스텝의 **36.65%** 가 최대 이득이고, `_pairs` 까지 더해도 **57.36%** 다.
   **방안 제시 0 · 처방 0 · 어떤 튜닝도 하지 않았다.**
 ★ **순서를 지키는 값이 이 자리에 있다** — `_rank`(순서 재현)는 **1.09%** 로 싸고,
   비싼 것은 «순서대로 푸는» `_resolve`(36.65% · grid 1 × block 1)다.
   (원리 [[../../.obsidian-log/원리/순서도-이식의-조건이다]] 가 말한 「순서를 바꾸면 더 이상 같지 않다」의 대가.)
 ★ 계기를 켠 시행이라 벽시계(2.345 s/프레임 = 10.24 ms/서브스텝)와 **섞지 않는다**(v4-14 §2-2 ★).
```

---

## §2 자기검사

### §2-1 집행 / 미집행 총계

| # | 무엇 | 계기 | 실행 |
|---|---|---|---|
| ① | 표본 8칸 고르기(A·B 둘 다) | `gpu/l3br_pick.py`(**신설**) | 1초 미만 |
| ① | 장면·SDF 내보내기 **8칸 / 바디 6종** | `scripts/v4Export.ts`(**diff 0** · 인용) | 착장 굽기 **0회**(§0-3 ★) |
| ① | **8칸 f64 실행** | `gpu/l3br_run.py`(**신설**) | 합 **약 8분**(칸당 26~95 s) |
| ① | 층3 다리(리포트 2 · 제품 대조 1 · 게이트 2) × 8칸 | `gpu/l3br_layer3.py`(**신설**) + 계기 3종(**diff 0**) | 물리 0프레임 |
| ① | v3 정본의 정착 채널(전방 10프레임) × 8칸 | `scripts/v4SelfNet.ts`(**diff 0** · v4-14 신설분) | 칸당 약 5~6분 |
| ② | f32 2칸 + 기준선 칸 f64 재실행 | `gpu/l3br_run.py` | 406.8 + 446.0 + 23.5 s |
| ③ | 해소 커널 비용 분해(서브스텝 10회) | `gpu/l3br_prof.py`(**신설**) | 1분 미만 |

**실행하지 «않은» 것**

```
 최적화·튜닝 **0** · 문턱 이동 **0** · 실패 칸 재시도 **0** · 파라미터 조정 **0** ·
 108칸 착수 **0** · 굽기(정본 재생성) **0** · 캡처 **0** · `src/`·`public/` 수정 **0** ·
 `engine/` 11파일 수정 **0** · 커널 신설 **0** · 보류 2칸 «고치기» **0**(실행 불가로 등재만) ·
 f32 를 8칸 전부 돌리기 **0**(회차 프롬프트가 2칸을 지정) · ③의 방안 제시 **0** ·
 **ecc 에이전트 0 / ecc 스킬 0**(`strategist` 서브에이전트도 0).
```

### §2-2 arch · 태스크 · grid/block · 시간

| 채널 | 값 |
|---|---|
| 기계 | **`DESKTOP-OV72A46`** · win32 · GTX 1660 Ti · Taichi **1.7.4** · llvm 15.0.1 · py **3.12.10** |
| arch(`current_cfg().arch`) | **`Arch.cuda`** 전량 · **assert 통과**(조용한 폴백 0 · v4-01 함정 1) |
| 정밀도 | ① **f64** 8칸 · ② **f32** 2칸 + f64 1칸 · ③ f64 |
| 태스크/호출 | `substep_a + substep_b` **29.00 == 코드 29**(v4-14 와 같은 수 · 칸이 바뀌어도 같다) |
| grid × block | `substep` = block 128 · grid 1~209 · 봉제 serial 1×1 ·<br>셀프 충돌 = `range_for` block 128 · **grid 768**(`_pairs`·`_narrow`) · **해소 serial 1×1** |
| s/프레임(f64) | **2.612 ~ 3.152**(8칸) · 기준선 칸 2.345 |
| degenerate / NaN / 발산 | **0 / 0 / 0**(11개 실행 전부) |
| 예산 | 칸당 30분 상한 **초과 0칸** · 8칸 합 약 8분(상한 4시간) |
| `py -m pytest gpu/tests -q` | **40 passed · 2 xfailed · 1 xpassed** (63.45 s) — v4-10~14 와 **같은 값** |
| `npx tsc -b` | **통과**(exit 0) |

### §2-3 경계 · diff

| 채널 | 값 |
|---|---|
| `src/` · `public/` diff(기점 `44baf2b` ↔ 이 판) | **0**(정답지 blob 포함 · 읽기만 했다) |
| `engine/` **11파일** blob 동일 | `full.py a631992832dd` · `full_color.py 7c7a49217664` · `full_color2.py 1ea95c3af689` · `full_par.py 09a6517973bd` · **`full_sc.py 69ef78951b27`** · **`selfcol.py 0ebe38912cdb`** · `stretch.py 7b3d8b6bf8a5` · `bending.py 6e1972f589d0` · `seam.py 7abfd6074dc1` · `collide.py 4d748bb65870` · `ulp.py 40fcdfcb4414` — **11/11 기점과 같은 blob** |
| 표본 8칸 정답지 blob sha256(앞 16) | `baf1e6c8c913b1b3` · `243d1990dff9a80e` · `aa46cc16f88a701b` · `32a12a430342c0da` · `bde3501ae5ce34eb` · `1816fed02a8b79b6` · `3a34104e7a9e898a` · `8d7493f1fad65e29` — **읽기 전용**(git 수정 0) |
| 계기 diff 0 | `v4Export.ts` · `v4FitReport.ts` · `v4ProductCmp.ts` · `v4ProductGate.ts` · `v4SelfNet.ts` |
| 이 판이 «만든» 코드 | `gpu/l3br_pick.py` · `gpu/l3br_run.py` · `gpu/l3br_layer3.py` · `gpu/l3br_prof.py` (전부 신규) |
| 이 판이 «만든» 산출물 | `l3br-pick` · `l3br-<8칸>-f64-cuda` · `l3br-<2칸>-f32-cuda` · `l3br-layer3-f64` · `l3br-prof-…` ·<br>`l3sc-v3net-<8칸>` · `fit-…-v3정본-<칸>` · `fit-…-v4-15-<칸>-f64` · `l3-gate-…` 16건 ·<br>`l3-product-cmp-<칸>-f64` 8건 · `scene-*`·`scene-bend-*`·`scene-seam-*`·`sdf-*`(8칸 / 바디 6종) |
| 굽기 / 캡처 / ecc | **0 / 0 / 0** |

### §2-4 «칸이 바뀌어도 같은 것»의 증명

```
 ① **기준선 칸의 비트 재현** — 이 판의 일반화 실행기(`l3br_run.py`)가 기본몸 M 에서 낸 값이
    v4-14 §1-③ㄴ(전용 실행기 `l3full_run.py`)과 **인쇄 자릿수 전량 동일**
    (f=10 · net 9.000416e-05 · 중앙 0.006600 · 최대 0.090004 mm).
 ② **삼각형 배열의 출처** — 칸마다 `tris` 를 v3 에서 따로 뜨지 않고 **그 칸 장면의 `ip_idx`** 를 썼다.
    기본몸 M 에서 v3 가 낸 `tris` 와 `ip_idx` 가 **바이트 동일**임을 확인했고
    (`makeInplane`(solver.ts:806-814)이 `tris` 를 «순서대로» 돌므로 구조적으로 같다), ①이 그 결과를 재현했다.
 ③ **태스크 수 29.00** 이 칸이 바뀌어도 같다(색 수 8/14 는 칸마다 재계산되지만 «종류»는 같다).
```

---

## §3 화면

**캡처 0.** 이 판은 칸별 궤적·리포트 수·게이트 값·커널 시간만 묻는다(회차 프롬프트 §3).

---

## §4 회차 갈래 — **B**(①(나) ⟹ 칸·값 상신 · ②③ 집행) ⟹ 상신

| 항 | 갈래 | 한 줄 |
|---|---|---|
| ① 넓이 8칸 | **(나)** | **8/8 정착 도달 · 게이트 8/8 `pass` 일치(`fails` 양쪽 빈 집합) · 대역 이름 40/40** ·<br>「같은 옷」 3조건은 **3/8**(표시값 34/40 · 갈린 6행은 전부 **반올림 경계**) |
| ② f32 전 정의역 | **(다)** | 2칸 다 **정착 미도달**(문턱의 81배 · 96배) · 중앙 4.46 / 15.52 mm ⟹ **f32 채택 후보 아님** |
| ③ 비용 분해 | 사실 | 셀프 충돌이 서브스텝의 **78.95%** · **해소 직렬 36.65%**(grid 1×1) · `_pairs` 20.71% · `_narrow` 14.37% |

**넘기는 것 7건**

```
 1. ★★★ **넓이에서 «게이트»는 완전히 선다** — 8칸 전부 `pass` true ↔ true 이고 `fails` 가
    **양쪽 다 빈 집합**이다(자기관통 교차 0 · 관통 0.30~0.45 ≤ 0.5 · 정착 0.023~0.098 ≤ 0.1).
    v4-14 의 한 칸이 우연이 아니었다.
 2. ★★★ **「같은 옷」은 8칸 중 3칸에서만 선다 — 갈린 채널은 «표시값 하나»다.**
    대역 이름 **40/40** · 게이트 **8/8** 인데 표시값이 **34/40**. 갈린 6행 중 다섯이
    **0.05 mm 미만**(0.0083 · 0.0097 · 0.0181 · 0.0279 · 0.0444)에서 1자리 표시를 넘긴다.
    ⟹ **「같은 옷」의 정의가 «반올림 경계»에 걸려 있다**는 것이 넓이에서 드러났다.
    **문턱을 옮기지 않았다** — 어떻게 읽을지는 규칙을 쓴 전략 세션 몫이다.
 3. ★★ **자릿수가 다른 한 칸** — `c100-h185-s40_L` 가슴 **+0.1782 mm**(4.0 → 4.2). 나머지 다섯과
    한 자릿수 이상 차이난다. 원인 귀속 0 · 다음 판 몫.
 4. ★★ **f32 는 정의역이 완성돼도 서지 못한다**(②) — 셀프 충돌은 f32 의 미수렴을 고치지 않는다.
    **속도 이득도 3.6% 뿐**이다(2.260 ↔ 2.345 s/프레임) ⟹ f32 축은 값으로 닫힌 셈이다.
 5. ★★ **최적화의 상계가 값으로 섰다**(③) — 해소를 없애도 **36.65%**, `_pairs` 까지 더해야 **57.36%**.
    셀프 충돌 전체가 **78.95%** 다. **처방·방안 0**(회차 프롬프트 ③).
 6. ★ **순서를 «재현»하는 비용은 싸고, 순서대로 «푸는» 비용이 비싸다** — `_rank` 1.09% ↔ `_resolve` 36.65%.
 7. ★ **보류 2칸**(`c87.5-h155-s40_L` · `_XL`)은 정답지 blob 이 옛 조립이라 **실을 수 없다**.
    이 판은 규칙 표본을 «대조 가능 37칸» 위에서 골랐고 그 사실·차이 한 칸을 등재했다. 고치지 않았다.
```

**이 판이 «하지 않은» 판단** — 「같은 옷」 정의의 재조정(반올림 자리·문턱) · 표시값 불일치의 원인 귀속 ·
f32 축의 폐기 선언 · 최적화 방안 · 108칸 확장 시점 · 보류 2칸의 처분.
**처방 0 · 최적화 0 · 사후 갈래 신설 0.**
