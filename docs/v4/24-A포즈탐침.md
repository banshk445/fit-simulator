# v4-24 — A포즈 준비 4건 해소 · A포즈 굽기 탐침 · 캡처

**물리 0줄 · `src/` diff 0 · 정본·blob·문턱 0 · 워커 3경로 diff 0 · 굽기 = 탐침 1칸 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-24-apose` |
| 기점 | **`8c7792d`**(= v4-23 HEAD) |
| §0 커밋 | (집행 «전» · 이 커밋) |
| §1~§4 커밋 | (집행 후 기입) |
| 보고 파일 | `docs/v4/보고/24.md` |

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v4-24-apose 8c7792d → Switched to a new branch 'v4-24-apose'
       $ git branch --show-current → v4-24-apose · HEAD 8c7792d
 2호기 (§0 커밋을 push 한 «뒤» 같은 자리로 전환 — v4-21~23 과 같은 순서 · §0-5 에 인쇄)
```

---

## §0 수령·사무

### §0-1 판정문 (전략 세션 · v4-23 §4 수령 · 원문)

```
 「①(가/나 기계별) 수용 · ★ 새 원리 「정본은 기계 좌표를 갖는다」(v3 정본 = 맥 · v4 정본 = 2호기 ·
   맥 재굽기 바이트 동일 = 정본 맥산 확인) · ★★ 사전 규칙 (b) 적용 —
   실비교(v4@2호기 ↔ 정본@맥)의 자연 변동 = v3 기계 간 갈림(중앙 0.777) ·
   v4 잔여 0.632/0.139 는 그 안 ⟹ 「같은 옷」 «성립 선언» ·
   0.05mm 채널은 «같은 기계» 전용으로 존치 · T포즈 재생 검증 트랙 종료 ·
   ② 남은 3축 단독 귀속 불가 수용 · 추가 조사 0 ·
   ★ A포즈 = 비교 대상 부재 — 판정은 게이트 + 겨드랑이 채널 + 화면(전략 세션) ·
   v4 가 A포즈 첫 정본 생산자 ·
   ★ 사무 종결: v4-18 캡처 = 원본 없음(헤드리스 · §3 캡처 0 이 정본 · 복사 지시는 전략 세션 착오) —
   대기 항목에서 제거」
```

**★ 트랙 종결 등재** — 「T포즈 재생 검증」 트랙은 판정문이 **종결**했다(「같은 옷」 성립 선언 · 0.05 mm 채널은
같은 기계 전용 존치). 본 회차는 그 선언을 **옮겨 적을 뿐** 재판정하지 않는다(CC 판단 0).

### §0-2 사무 3건 (이 §0 커밋에 «함께» 든다)

| # | 사무 | 이행 |
|---|---|---|
| 1 | 원리 노트 「정본은 기계 좌표를 갖는다」 신설 | `.obsidian-log/원리/정본은-기계-좌표를-갖는다.md` — 근거 표(같은 기계 = 바이트 동일 / 기계 사이 = 중앙 0.777 mm)와 따르는 것 4항 |
| 2 | CLAUDE.md **캡처 규약 전문 등재** | `CLAUDE.md` v4 트랙 절에 「캡처 규약 — git 경유가 유일한 경로」 신설 · **판정문 문언 그대로** |
| 3 | 인수인계에서 **캡처 대기 항목 제거** | ★ **인수인계에 v4-18 캡처 대기 항목이 «없다»** — 그 대기는 대화 안에만 있었고 볼트에 등재된 적이 없다(grep `v4-18` → 예외 대장 1줄뿐). ⟹ **제거할 항목 0** · 이 줄이 그 사실의 기록이다. 인수인계에 남아 있는 캡처 대기는 **v3 트랙**(v3-85·91·92 업로드)뿐이라 **손대지 않았다** |

### §0-3 바꾸지 않는 것 (회차 프롬프트 원문)

```
 물리 **0줄** · `src/` diff **0** · 정본·blob·문턱 **0** ·
 워커 **3경로 diff 0**(A포즈는 기존 조립+램프 경로를 «사용»한다).
```

### §0-4 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · 단위 명시)

**★ 단위** — ①은 **항목**(4건) · ②는 **칸 1개** · 간격·관통 **mm** · 프레임 **1/60 s**.

| 항 | 갈래 |
|---|---|
| **①** 준비 4건 해소 | 항목별 「원문 인용 → 해소 방법(있는 것에서 유도 · 손 상수 0) → 해소 증거」 · 넷 다 닫히지 않으면 **그 사실과 사유** |
| **②** A포즈 굽기 탐침 | **(가)** 게이트 `pass` / **(나)** fail(채널·값 · 처방 0) / **(다)** 발산·미정착(값) |
| **③** 캡처 | 앞·옆 2장 → `docs/v4/캡처/24-1-front.png` · `24-2-side.png` · 불가면 **캡처 0 · 사유** · **화면 판정 0**(전략 세션 몫) |

**§4** — A = ①4/4 + ②(가) / B = ②(나)(다) ⟹ 값 상신 / **C = ① 미해소 항목 존재 ⟹ 사유 상신 · ②는 해소분으로 가능하면 집행** / D = 그 외 ⟹ 정지.

### §0-5 사전 결정 4건 (값 보기 «전» · CC 집행 설계)

```
 ㄱ **①의 「해소」 기준을 지금 고정한다** — 「그 항목이 막던 «다음 한 걸음»이 실제로 서면 해소」다.
    문서로 「없음을 설명한 것」은 해소가 아니다. 각 항목에 **파일·값·명령** 중 하나를 증거로 붙인다.
 ㄴ **`src/` 를 고쳐야만 닫히는 항목은 «미해소»로 적는다** — §0-3 이 `src/` diff 0 이고,
    v3 동결 «예외» 대장에는 `garmentScene.ts` 조립 코드 한정 1건뿐이다(`dressRun.ts` 는 없다).
    그 경우 **필요한 승인 문언과 정확한 diff 를 «제안»으로만** 적는다(적용 0).
 ㄷ **②의 몸은 회차 프롬프트가 지정한 것** — v4-19 A포즈 조립 상태(**기본 마네킹** A포즈 · 그리드 몸 아님).
    ①의 그리드 몸 항목이 닫히든 말든 **②의 입력은 바뀌지 않는다**(같은 판에서 두 몸을 섞지 않는다).
 ㄹ **리포트 5행은 «값 등재»로만 적는다** — A포즈에는 비교 대상이 없고,
    대역 정의역은 **T포즈 그리드 몸**에서 뜬다(`v4FitReport.ts` 가 `body-<bodyId>.bin` 을 읽는다) ⟹
    **A포즈 상태에 T포즈 대역을 얹은 수**임을 값 옆에 명시한다. 판정에 쓰지 않는다.
```
