# v4-48 — 제품 경로 A포즈 전환(제공 33칸 배선) · 매칭·알림 규약 이식 · 화면 스모크

**물리 0줄 · `src/` diff = 제품 경로(색인 로드·blob 경로·표시 몸) «한정» · v3 T포즈 경로 불변(스위치) · 굽기 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-48-product-apose` |
| 기점 | **`47a652e`**(= v4-47 보고·부록) |
| §0 커밋 | (아래 §0-5 에 자기 기입) |
| §1~§4 커밋 | (집행 후 기입) |
| 보고 파일 | `docs/v4/보고/48.md` |

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v4-48-product-apose 47a652e → v4-48-product-apose · HEAD 47a652e
 2호기 (§0 커밋을 push 한 «뒤» 같은 자리로 — §0-5 에 인쇄)
```

---

## §0 수령·사무

### §0-1 판정문 (전략 세션 · v4-47 §4 수령 · 원문)

```
 「갈래 A 수령 · ★ 전략 세션 전제 기각·귀책 12건째: ③a = THICK×0.5(옷 상수 · 포즈 무관 원리 문턱) —
   재도출 대상이 아니었다 · CC 의 문턱 불이동 집행이 옳다 · 상시 접촉 가설 값으로 기각(대역 3.44%) ·
   ★★ fail 63 = 보류(T포즈 43 과 같은 성격) · 경계 칸 화면 관찰은 품질 백로그 재료로만 ·
   ★★ 편입 33 · 착용불가 12 확정 — 제공 목록 성립(T 35 와 같은 자릿수) · 다음 = 제품 배선」
```

**★ 귀책 등재(전략 세션 · 열둘째)** — 「③a 는 옷 상수에서 온 원리 문턱이라 **재도출 대상이 아니었다**」를
판정문이 자기 귀책으로 등재했다. 본 회차는 **그대로 옮겨 적는다**(CC 판단 0).

### §0-2 바꾸지 않는 것

```
 물리 **0줄** · 굽기 **0** · v3 T포즈 정본(제공 35 · 분류 108 · settled blob · body bin) **불변** ·
 게이트 채널·문턱 **0** · 매칭 규칙 **0**(T포즈 규약을 «그대로» 쓴다)
```

### §0-3 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · 단위 명시)

**★ 단위** — 칸은 **칸** · 좌표는 **m**(blob f64) · 몸 정점은 **Float32 3n** · 사이즈는 **S·M·L·XL**.

| 항 | 갈래 |
|---|---|
| **①** 색인·blob 배선 | 제품이 A포즈 제공·분류 정본을 읽는다 · 매칭·회색·사유는 **T포즈 규약 동형**(코드 재사용) |
| **②** 표시 몸 | 마네킹 표시가 **A포즈 그리드 몸** · 옷 blob = A포즈 산출 · `?tpose=1` 폴백 유지 |
| **③** 검증 정보 | 리포트 5행 표시 확인 + **가슴 = 겨드랑이 정의** 각주 1줄(소비자 언어) |
| **④** 스모크 | 승혁 실행 절차 + 캡처 3장 권고 목록(§5) |

**§4** — A = ①~③ 완료 ⟹ 승혁 스모크 안내 / B = 막힘(사실) ⟹ 상신.

### §0-4 사전 결정 5건 (값 보기 «전» · CC 집행 설계)

```
 ㄱ **분류 로직은 한 줄도 새로 쓰지 않는다** — `src/v3/provide.ts`(v3-79·80 정본)를 **그대로** 쓴다.
    바뀌는 것은 «어느 파일을 읽는가»뿐이다.
 ㄴ **스위치는 «기본 A포즈 · `?tpose=1` 이 구판»** — 판정문·회차 프롬프트 문언 그대로.
    구판 경로의 **파일 이름·형식·코드 흐름은 건드리지 않는다**(회귀는 그것으로 증명한다).
 ㄷ **자산은 «복사»로 만든다** — v4-46 산출(`gpu/bake/results/v4-46-A108/<칸>.bin`)은 헤더 없는 raw f64 이고
    제품 주입 규약은 `[u32 헤더길이][JSON][f64…]`(`dressRun.ts:128-142`)다 ⟹ **포장만** 한다(값 변형 0).
    몸은 그리드 정본(`grid27/l3ap-body-<몸>-a35.bin`)을 **그대로** 복사한다.
 ㄹ **화면 판정 0** — CC 는 브라우저 채널이 없다(v4-41 §1-③). 스모크는 **승혁 실행**이고 이 판은 절차만 낸다.
 ㅁ **회귀 증명** — `?tpose=1` 경로가 읽는 파일·코드가 **한 글자도 안 바뀌었음**을 diff 로 보인다
    (구판 값 재현은 승혁 스모크에서 화면으로 확인).
```

### §0-5 커밋·전환 인쇄 (집행 «전»)

(§0 커밋 sha 자기 기입 · 2호기 전환 인쇄는 아래에 붙인다)
