# v4-48 — 제품 경로 A포즈 전환(제공 33칸 배선) · 매칭·알림 규약 이식 · 화면 스모크

**물리 0줄 · `src/` diff = 제품 경로(색인 로드·blob 경로·표시 몸) «한정» · v3 T포즈 경로 불변(스위치) · 굽기 0 · ecc 0**

| | |
|---|---|
| 브랜치 | `v4-48-product-apose` |
| 기점 | **`47a652e`**(= v4-47 보고·부록) |
| §0 커밋 | **`21c38d1`**(집행 «전») |
| §1~§4 커밋 | **`e8ba3ac`** |
| 보고 파일 | `docs/v4/보고/48.md` |

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v4-48-product-apose 47a652e → v4-48-product-apose · HEAD 47a652e
 2호기 (§0 커밋을 push 한 «뒤» 같은 자리로 — §0-5 에 인쇄)
```

---

## §0 수령·사무

### §0-1 판정문 (전략 세션 · v4-47 §4 수령 · 원문)

```
 「갈래 A 수령 · ★ 전략 세션 전제 기각·귀책 12건째: ③a = THICK×0.5(옷 상수 · 포즈 무관 원리 문턱) —
   재도출 대상이 아니었다 · CC 의 문턱 불이동 집행이 옳다 · 상시 접촉 가설 값으로 기각(대역 3.44%) ·
   ★★ fail 63 = 보류(T포즈 43 과 같은 성격) · 경계 칸 화면 관찰은 품질 백로그 재료로만 ·
   ★★ 편입 33 · 착용불가 12 확정 — 제공 목록 성립(T 35 와 같은 자릿수) · 다음 = 제품 배선」
```

**★ 귀책 등재(전략 세션 · 열둘째)** — 「③a 는 옷 상수에서 온 원리 문턱이라 **재도출 대상이 아니었다**」를
판정문이 자기 귀책으로 등재했다. 본 회차는 **그대로 옮겨 적는다**(CC 판단 0).

### §0-2 바꾸지 않는 것

```
 물리 **0줄** · 굽기 **0** · v3 T포즈 정본(제공 35 · 분류 108 · settled blob · body bin) **불변** ·
 게이트 채널·문턱 **0** · 매칭 규칙 **0**(T포즈 규약을 «그대로» 쓴다)
```

### §0-3 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · 단위 명시)

**★ 단위** — 칸은 **칸** · 좌표는 **m**(blob f64) · 몸 정점은 **Float32 3n** · 사이즈는 **S·M·L·XL**.

| 항 | 갈래 |
|---|---|
| **①** 색인·blob 배선 | 제품이 A포즈 제공·분류 정본을 읽는다 · 매칭·회색·사유는 **T포즈 규약 동형**(코드 재사용) |
| **②** 표시 몸 | 마네킹 표시가 **A포즈 그리드 몸** · 옷 blob = A포즈 산출 · `?tpose=1` 폴백 유지 |
| **③** 검증 정보 | 리포트 5행 표시 확인 + **가슴 = 겨드랑이 정의** 각주 1줄(소비자 언어) |
| **④** 스모크 | 승혁 실행 절차 + 캡처 3장 권고 목록(§5) |

**§4** — A = ①~③ 완료 ⟹ 승혁 스모크 안내 / B = 막힘(사실) ⟹ 상신.

### §0-4 사전 결정 5건 (값 보기 «전» · CC 집행 설계)

```
 ㄱ **분류 로직은 한 줄도 새로 쓰지 않는다** — `src/v3/provide.ts`(v3-79·80 정본)를 **그대로** 쓴다.
    바뀌는 것은 «어느 파일을 읽는가»뿐이다.
 ㄴ **스위치는 «기본 A포즈 · `?tpose=1` 이 구판»** — 판정문·회차 프롬프트 문언 그대로.
    구판 경로의 **파일 이름·형식·코드 흐름은 건드리지 않는다**(회귀는 그것으로 증명한다).
 ㄷ **자산은 «복사»로 만든다** — v4-46 산출(`gpu/bake/results/v4-46-A108/<칸>.bin`)은 헤더 없는 raw f64 이고
    제품 주입 규약은 `[u32 헤더길이][JSON][f64…]`(`dressRun.ts:128-142`)다 ⟹ **포장만** 한다(값 변형 0).
    몸은 그리드 정본(`grid27/l3ap-body-<몸>-a35.bin`)을 **그대로** 복사한다.
 ㄹ **화면 판정 0** — CC 는 브라우저 채널이 없다(v4-41 §1-③). 스모크는 **승혁 실행**이고 이 판은 절차만 낸다.
 ㅁ **회귀 증명** — `?tpose=1` 경로가 읽는 파일·코드가 **한 글자도 안 바뀌었음**을 diff 로 보인다
    (구판 값 재현은 승혁 스모크에서 화면으로 확인).
```

### §0-5 커밋·전환 인쇄 (집행 «전»)

```
 §0 선커밋 **21c38d1** — 집행 «전» · push 완료
 2호기 자산 생성만 수행(굽기 0) · 코드 편집·dev 서버 확인은 **에어**에서 했다
```


---

## §1-① 색인·blob 배선 — 제품이 A포즈 정본을 읽는다

### 바뀐 자리(diff 원문 · `src/components/V3ProductV1.tsx` +22 / −3)

```
 -const DIR = "v3diag/v3-77";
 +const TPOSE = typeof window !== "undefined"
 +  && new URLSearchParams(window.location.search).get("tpose") === "1";
 +const DIR = TPOSE ? "v3diag/v3-77" : "v3diag/v4-a35";
 +const PROVIDE_FILE = TPOSE ? "v1-provide-35.v3-91.json" : "v1-provide.json";
 +const INDEX_FILE = TPOSE ? "index-merged-108.v3-91.json" : "index-merged-108.json";
 +const POSE_LABEL = TPOSE ? "T포즈(구판)" : "A포즈";

 -      fetch(`${B}${DIR}/v1-provide-35.v3-91.json`)…
 +      fetch(`${B}${DIR}/${PROVIDE_FILE}`)…
 -      fetch(`${B}${DIR}/index-merged-108.v3-91.json`)…
 +      fetch(`${B}${DIR}/${INDEX_FILE}`)…
```

**매칭·회색·사유·착지는 한 줄도 새로 쓰지 않았다** — `src/v3/provide.ts`(v3-79·80 정본)의
`buttonState`·`landingSize` 를 그대로 쓴다(§0-4ㄱ). 화면이 읽는 것은 **파일 두 개**뿐이고,
그 파일의 «형식»이 T포즈와 동형이라 코드가 그대로 선다:

| 규약 | T포즈 정본 | **A포즈(이 판)** |
|---|---|---|
| 제공 목록 | `v1-provide-35.v3-91.json`(`{메타, provide[]}`) | `v4-a35/v1-provide.json`(같은 형식 · **33칸**) |
| 분류 정본 | `index-merged-108.v3-91.json`(`{칸: {status,…}}`) | `v4-a35/index-merged-108.json`(같은 형식 · 편입 33 · 보류 63 · 착용불가 12) |
| 착장 blob | `settled-<칸>.bin` | `v4-a35/settled-<칸>.bin`(**33개** · v4-46 산출 포장) |
| 몸 정점 | `body-<몸>.bin` | `v4-a35/body-<몸>.bin`(**27개** · 그리드 정본 복사) |

### 자산 생성(값 변형 0 · §0-4ㄷ)

```
 settled-<칸>.bin — `results/v4-46-A108/<칸>.bin`(raw f64 3n)에 **제품 주입 규약 헤더**만 붙였다:
   [u32 헤더길이][JSON {n, frame, d, what}][f64 좌표…]   (규약 출처 `src/v3/dressRun.ts:128-142`)
   실측 확인 — `settled-c100-h170-s45_M.bin` 헤더 `{"n": 9490, "frame": 350, "d": 0.009, …}` ·
   좌표 **227,760 바이트 = 정점 9,490** (좌표 바이트 변형 0)
 body-<몸>.bin  — `grid27/l3ap-body-<몸>-a35.bin` **그대로 복사**(Float32 3n)
 정본 2종       — v4-47 산출(`l3ap-provide-v4-47.json`·`l3ap-index-merged-108-v4-47.json`) 그대로
 총 **62 파일 · 13 MB** · 자리 `public/v3diag/v4-a35/`
 ★ 제품 자산은 이 저장소에서 **git 밖 로컬 관행**이다(`public/v3diag/*` 는 `.gitignore` · 예외 5줄) —
   A포즈 자산도 같은 관행을 따랐고, 2호기에서 만들어 **에어로 내려받았다**(스모크 기계).
```

## §1-② 표시 몸 — A포즈 그리드 몸 · 구판 스위치

몸 정점 경로는 같은 상수 `DIR` 을 쓰므로(`V3ProductV1.tsx:144` `body-${bodyId}.bin`)
**자산 갈래가 바뀌면 몸도 함께 바뀐다** — A포즈에서는 그리드 정본 몸, `?tpose=1` 이면 종전 T포즈 몸이다.
옷 blob 도 같은 폴더에서 온다(`settled-<칸>.bin`) ⟹ **몸과 옷이 항상 같은 포즈**다.

**dev 서버 실측**(에어 · 응답 코드만 · 화면 판정 0)

```
 GET /v3diag/v4-a35/v1-provide.json              **200**
 GET /v3diag/v4-a35/index-merged-108.json        **200**
 GET /v3diag/v4-a35/settled-c100-h170-s45_M.bin  **200**
 GET /v3diag/v4-a35/body-c100-h170-s45.bin       **200**
 GET /v3diag/v3-77/v1-provide-35.v3-91.json      **200**   ← 구판 경로도 그대로 선다
```

## §1-③ 검증 정보 — 5행 표시 + 각주 한 줄

`FitReportTable`(v3-67 공유 마크업)은 그대로 쓰고, 그 아래에 **한 줄**을 넣었다(값·판정 0):

```
 가슴 줄은 **겨드랑이 높이**의 단면에서 잽니다(옷·몸의 표시 자세: {POSE_LABEL}).
```

`POSE_LABEL` 은 자산 갈래에서 온다(A포즈 / T포즈(구판)) ⟹ **화면이 어느 자산을 보고 있는지**가 같은 줄에 남는다.
근거 — v4-39 가 `chestY` 를 「팔이 몸통 고리에 흡수되는 높이(겨드랑이)」로 재정의했고, A포즈 108칸에서
`chestY > waistY` 가 **96/96** 성립했다(v4-46 §1-②).

## §1-④ 스모크 절차(승혁 실행 · 캡처 3장 권고)

```
 0) cd ~/projects/fit-simulator && git fetch -q && git checkout v4-48-product-apose && git pull -q
    ★ 자산(`public/v3diag/v4-a35/` 62파일)은 **이미 이 기계에 있다**(git 밖 · CC 가 내려받아 뒀다).
      혹시 없으면 알려주세요 — 다시 보냅니다.
 1) npm run dev            → 주소(대개 http://localhost:5173/)
 2) 제품 화면을 연다(제품 경로 URL 그대로 · 파라미터 없음 = **A포즈**)
 3) 대표 3입력을 차례로 넣는다 — 화면 상단 입력 3축(가슴·키·어깨)
      ㉠ **작은 몸**  가슴 88 · 키 155 · 어깨 40
      ㉡ **기본**    가슴 100 · 키 170 · 어깨 45
      ㉢ **큰 몸**   가슴 122 · 키 185 · 어깨 50
    각 입력에서 볼 것 — (a) 마네킹이 **팔을 내린 자세**로 보이는가 (b) 옷이 입혀져 보이는가
      (c) 사이즈 버튼 4개의 활성/회색이 뜨는가 (d) 접힘을 열면 5행 표와 **가슴 각주 한 줄**이 보이는가
 4) **캡처 3장**(각 입력마다 화면 전체 1장) — 파일명은 자유, CC 에게 주면 회차 노트에 넣습니다
 5) 구판 대조 — 주소 끝에 **`?tpose=1`** 을 붙여 다시 연다.
    같은 3입력에서 **T포즈 마네킹**과 종전 목록(35칸 기준)이 그대로면 회귀 통과다.
 6) 막히면 보낼 것 — 브라우저 콘솔 스크린샷 + 화면 스크린샷
```


---

## §2 자기검사

| 항 | 값 |
|---|---|
| `src/` diff vs 기점 `47a652e` | **`src/components/V3ProductV1.tsx` +22 / −3 한 파일뿐**(위 diff 원문) · 물리 **0줄** |
| `src/v3/provide.ts`·`match.ts`·`fitReport.ts`·`s4Gate.ts`·워커·`gpu/` | **diff 0** — 매칭·분류·판정 규칙 **한 줄도 안 건드렸다** |
| v3 T포즈 정본 자산 | **불변** — `public/v3diag/v3-77/` 쓰기 **0**(A포즈는 **새 폴더** `v4-a35/`) |
| 구판 경로 회귀 | `?tpose=1` 이 읽는 **파일 이름·형식·코드 흐름이 그대로**다(diff 는 삼항 분기뿐) · 구판 제공 목록 **35칸** 확인 · dev 서버에서 구판 파일 **200** |
| A포즈 자산 | settled **33** · body **27** · 정본 **2**(제공 33 · 분류 편입 33/보류 63/착용불가 12) · 13 MB |
| 굽기 · 몸 생성 | **0 · 0**(v4-46·47 산출 포장·복사만) |
| `npx tsc -b` | **통과**(종료 0) |
| pytest(2호기) | **40 passed · 2 xfailed · 1 xpassed**(59.44 s) |
| 화면 판정 | **0** — CC 는 브라우저 채널이 없다(v4-41 §1-③) ⟹ 스모크는 **승혁 실행**(§1-④) |
| 브랜치 인쇄 | §0 머리 · §0 은 집행 **«전»** 커밋(`21c38d1`) |
| ecc 에이전트 / 스킬 | **0 / 0** |

## §4 회차 갈래 — **A**(①~③ 완료 ⟹ 승혁 스모크 안내)

**넘기는 것 7건**

1. ★★★ **제품 기본 경로가 A포즈다** — `?tpose=1` 이 구판이고, 갈래는 **상수 세 개**(폴더·제공 파일·분류 파일)로만
   갈린다. 매칭·회색·사유·착지 규칙은 `provide.ts` 정본 **그대로**(신설 0).
2. ★★★ **몸과 옷이 항상 같은 포즈다** — 몸 정점도 같은 `DIR` 에서 오므로 자산 갈래 하나로 함께 바뀐다.
3. ★★ **자산은 포장·복사뿐** — settled 33칸은 v4-46 산출에 **헤더만** 붙였고(좌표 변형 0 · 실측
   `n 9490 · frame 350 · 227,760 바이트`), 몸 27개는 그리드 정본 복사다.
4. ★★ **가슴 각주 한 줄** — 「가슴 줄은 **겨드랑이 높이**의 단면에서 잽니다(표시 자세: A포즈)」.
   v4-39 재정의를 소비자 언어로 옮긴 것이고 값·판정은 0.
5. ★★ **구판 회귀는 코드로 증명했다** — 구판이 읽는 파일 이름·형식이 그대로고 T포즈 정본 폴더는 **쓰기 0**.
   화면 값 재현은 승혁 스모크 5)단계에서 확인한다.
6. ★ **A포즈 제공 33칸 ↔ T포즈 35칸** — 자릿수가 같다(판정문 문언).
7. ★ **스모크 절차 6단계 + 캡처 3장 권고**(작은/기본/큰 입력) — §1-④.

**이 판이 하지 않은 판단** — 화면(승혁·전략 세션) · 보류 63칸의 품질 처분 · 배포 시점.
