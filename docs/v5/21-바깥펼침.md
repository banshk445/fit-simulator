# v5-21 — 조립 2세대 마지막 후보 «바깥 펼침»(S3 = 봉제선에서 몸 바깥으로 평면 걸기 · 결합은 물리에) · f0 사실 · 처방 0 · 굽기 0

> 기점 `0e6d0d7`(v5-20 HEAD) · 브랜치 `v5-21-outward-hang`
> 물리 0줄 · **`src/` diff = `redrapeAsm2` 하위 플래그 «한 후보» 한정(off 비트 불변)** · 굽기 0 · ecc 0 · **처방 0 · 판단 0**

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v5-21-outward-hang 0e6d0d7 → Switched to a new branch
       $ git branch --show-current → v5-21-outward-hang · HEAD 0e6d0d7
 2호기 (이 판은 조립만 — 굽기 0 · CUDA 불요)
```

---

## §0 선커밋 — 집행 «전» 등재

### §0-1 판정문(전략 세션 · v5-20 §4 수령 · 사용자 결정 · 원문)

```
 「갈래 A 수용 · ★ 관통과 교차는 다른 두 문제(교집합 1/7/2) · 점은 전부 존재 ·
   막는 것은 «이웃과의 결합»(독립 사영이 메시를 찢음) ⟹ 결합을 풀 주체는 기하 배치가 아니라 제약 솔버 ·
   레귤러핏도 같은 자리 ⟹ 재배치 절차 고유 ·
   사용자 결정: 2세대 트랙은 «마지막 후보 하나»를 f0 로만 시험하고 결과에 따라 보관/종결 ·
   후보(반증 대상) = 바깥 펼침: 봉제선(능선 위 · 호길이 사상 · 간극 SEP)에서 아래 행들을 몸 «바깥쪽»
   (그 자리 몸 법선 n 의 바깥 + −y 합성 · 각도는 손 상수 0 — 예: 능선 접평면과 수직인 평면에 패널을
   «평평하게» 걸기)으로 평면으로 걸어 둔다 · 실루엣 튜브 행(j ≤ N_side)은 현행 ·
   두 구간 사이는 잇지 않는다(물리가 잇는다 · v4-05 가 봉제 없는 상태의 낙하를 보였으니 봉제는 켜 둔 채) ·
   관통 0(구성상) · 교차 0(평면) · 결합 보존(패널 그대로)이 예측 · S4 없음 ·
   소매 축 이동은 재배치된 암홀 중심으로 s 를 뜨는 순서 정정 포함」
```

### §0-2 §1 집행 4항 (회차 프롬프트 원문)

```
 ① 형식화(값 보기 전 · CC) — 펼침 방향의 정의(손 상수 0 · 몸 법선·능선 접선에서 유도) ·
    펼침 구간 행 범위(N_side 위) · 튜브 구간과의 «빈 틈» 허용(이음 엣지 신장은 채널로만) ·
    소매 축 이동 순서 정정 diff.
 ② f0 채널(세 칸: supima · 기본 M · mst-over) — 교차 수 전수(분포 · 몸판끼리/소매) ·
    몸 관통 정점 수·깊이(밴드 8.2291 대비 · 밴드 밖 0 이 예측) · 이음 엣지 최대 신장(2D 패턴 대비 배율) ·
    봉제쌍 거리(어깨 ≈ SEP · 암홀↔소매캡 거리 분포 — 램프가 닫을 거리) · 링/rest · 행/열 간격 ·
    조립 자기검사 통과 여부.
 ③ 갈래(사전 등재): (가) 세 칸 교차 0 ∧ 밴드 밖 관통 0 ∧ 자기검사 통과 → 2세대 «보관(살아 있음)» —
    다음 굽기 판 재료(암홀 봉제 거리 분포 · 예상 RAMP) / (나) 교차 > 0 또는 밴드 밖 > 0(값·자리) →
    2세대 «종결» 재료 / (다) 이음 신장이 램프 정의역 밖(등재 자: 기존 신장 문턱) → (나)와 같이 종결 재료.
 ④ 결론 문안 초안(판단 0) — (가)면 「2세대 보관 · 1막 종결 뒤 재개」 /
    (나)(다)면 「2세대 종결 · 정의역 등재(2번 항목)」 각각 볼트 문안.
 §4 — A = ③ 어느 갈래든 값 완비 ⟹ 상신 / B = 구현 불가(사유) ⟹ 상신. 사후 갈래 신설 0.
```

**★ 단위 명시**(함정 43) — 교차 = **삼각형 «쌍» 개수** · 관통 = **정점 개수** · 깊이·거리·밴드 = **mm**
(밴드 = `sdfSpec.band` **8.2291 mm**) · 신장 = **배율**(무차원 · 3D ÷ 2D) · 행/열 간격 = **mm** ·
칸 = **셀**(세 칸 = `supima-L` · `c100-h170-s45_M` · `mst-over-L`) · 굽기 = **0칸**.

### §0-3 §1-① 형식화 — **값 보기 «전» 등재**(사후 신설 0)

```
 ㄱ **펼침 방향** — 열 `i` 의 «꼭대기 정점»(행 `nvB` · S1/S2 가 능선·목선 링에 놓은 자리)에서
    몸 SDF 기울기 단위 `n̂` 을 읽고(이 파일의 `nHat` · S4 가 쓰는 그 중앙차분 · 새 식 0)
    **`d̂ = normalize(n̂ + (−ŷ))`** — 단위 벡터 «둘의 합» = 이등분 방향이므로 **손 상수 0**이다.
    (판정문 「몸 법선 n 의 바깥 + −y 합성」의 직역이다. 능선 꼭대기점은 링의 `|x|` 최대점이므로
     `n̂ ≈ ±x̂` 이고 목선 토막은 `n̂ ≈ ±ẑ` 라 합이 0 이 되지 않는다.)
    `n̂` 이 없거나 `|n̂ + (−ŷ)| ≤ 1e-9` 이면 **`d̂ = (0, −1, 0)`** 로 떨어뜨리고 **그 열 수를 값으로 인쇄**한다.
 ㄴ **펼침 구간 행 범위** — **`N_side < j < nvB`**. 행 `nvB` 는 S1/S2 배치 그대로(봉제선) ·
    **`j ≤ N_side` 는 튜브 스냅숏 그대로**(판정문 「실루엣 튜브 행은 현행」).
 ㄷ **열 위치** — 꼭대기에서 `d̂` «직선»으로 **2D 패턴 열 엣지 길이를 누적**해 내려 건다
    (기존 S3 가 쓰는 그 `len` 과 같은 자 · 면내 변형 0 · 열마다 직선 ⟹ 두 열 사이는 직선면).
 ㄹ **S4 없음 · 밀어냄 없음** — `ITERCAP = 0`(`NOS4` 와 «같은 자리») · `pushOut` **적용 0**.
    「관통 0」은 **구성상 예측**이고 검사는 ②의 채널이다 — **밀어내기로 만들어 내지 않는다**.
 ㅁ **빈 틈** — `j = N_side ↔ N_side+1` 엣지는 **잇지 않는다**(물리가 잇는다).
    그 엣지의 **3D 길이 ÷ 2D 패턴 길이 = 이음 엣지 신장 «배율»**을 **채널로만** 낸다(문턱 0 · 판정 0).
 ㅂ **소매 축 이동 «순서» — 코드 사실(값 보기 전 인용)**: `assemble()` 에서
    `garmentScene.ts:1379` `if (ASM2) redrapeAsm2(B, s.pos);` 가 **먼저** 돌고,
    v4-37 소매 축 이동은 `:1396-1424`(`if (AO) { … }`)로 **그 뒤**에 돈다. 그 블록의 `s` 는
    `seams` 의 암홀 쌍을 **`s.pos`(= 재배치 «후» 위치)** 에서 읽는다(`:1411-1413`).
    ⟹ **「재배치된 암홀 중심으로 `s` 를 뜬다」는 이미 성립한다 · 순서 정정 diff = 0줄**(아래 §1-① 에 재확인).
 ㅅ **새 상수 0** — 쓰는 양은 `N_side`·`nvB`·`nuB`·패널 `uv`·`sdfSpec.h`·`SEP`·`TOL_SELF` 뿐이고 전부 등재분이다.
 ㅇ **`src/` diff 한정** — ① `asm2Fix` 유니온에 `'OUT'` ② `S3OUT` 도입 ③ `S4NONE` 에 `OUT` 포함
    ④ `tube` 스냅숏 조건에 `OUT` 포함 ⑤ `if (S3OUT && tube)` 블록 하나. **`asm2` 가 꺼지면 `FIX = undefined`**
    ⟹ 다섯 자리 모두 거짓 ⟹ **off 경로 비트 불변**(§2 에서 T108 해시 대조로 확인한다).
```

### §0-4 산출 형식 — 값 보기 «전» 고정

```
 계기 = 기존 `scripts/v5NoS4.ts`(v5-19 · `ASM2FIX` 인자를 받는다)를 **그대로** 쓰고,
 ㅁ(이음 엣지 신장)·ㄱ(떨어뜨린 열 수)만 신설 계기 `scripts/v5OutHang.ts` 로 더한다(`src/` 0줄 · 훅만 읽는다).
 산출 파일 접두 = `v5-21-` · 진입 인자 = v5-7b §0-5ㄴ 확정분(grid27 a35 · `D_MM` 9).
 기준선(대조용 · v5-19·20 등재분) — `NOS4` 에서 교차 **296 / 288 / 136**(M / supima / mst) ·
 밴드 밖 관통 **157 / 114 / 134**.
```

### §0-5 §0 사무

```
★ 이 판에서 호출한 ecc 에이전트 = 0 · 스킬 = 0 (집행 전 등재 · 회차 종료 시 재확인)
★ 굽기 0칸 · 캡처 0장(정의역 밖) · 화면 판정 0.
★ off 비트 불변 — 기점 `0e6d0d7` 의 T108 조립 해시를 **집행 «전»에 떠 두고**(배경 실행) 구현 뒤 대조한다.
★ §0 선커밋 sha 는 아래에 자기 기입한다.
```

```
 §0 선커밋 **5121f41** — 집행 «전»(자기 기입)
```

---

## §1 집행

집행 기계 = **에어 M4**(조립만 · 물리 0프레임 · CUDA 불요) · 진입 인자 = **v5-7b §0-5ㄴ 확정분**
(`BODY_BIN`·`ARM_AXIS_JSON`·`ARM_ORIGIN_JSON` = grid27 a35 · `D_MM` 9) · `ASM2FIX=OUT`.
계기 = `scripts/v5NoS4.ts`(v5-19 · 그대로) + 신설 `scripts/v5OutHang.ts`(이음 엣지 · 꼭대기 행 기울기).

### §1-① 형식화 — **등재 그대로 집행했다**(§0-3 · 사후 변경 0)

**구현된 것**(`src/v3/garmentScene.ts` · `asm2Fix = 'OUT'` 일 때만 돈다)

```
 ㄱ 방향  d̂ = normalize(n̂ + (−ŷ)) — 꼭대기 정점(행 nvB)의 몸 SDF 기울기 `n̂`(그 파일의 `nHat`)
 ㄴ 범위  N_side < j < nvB 를 펼치고 · j ≤ N_side 는 튜브 스냅숏 그대로 · 행 nvB 는 S1/S2 그대로
 ㄷ 위치  꼭대기 + d̂ × (2D 패턴 열 엣지 길이 «누적»)
 ㄹ S4    `S4NONE` 에 `OUT` 을 넣어 `ITERCAP = 0` · `pushOut` 호출 0
 ㅁ 이음  j = N_side ↔ N_side+1 엣지는 «잇지 않는다» · 3D ÷ 2D 배율을 인쇄 채널로만
```

**★ 소매 축 이동 «순서 정정» diff = 0줄 — 이미 그 순서다**(§0-3ㅂ 의 코드 사실을 회차 끝에 재확인).
`assemble()` 는 `garmentScene.ts:1379` 에서 `redrapeAsm2(B, s.pos)` 를 먼저 돌리고,
v4-37 소매 축 이동(`:1396-1424`)이 **그 뒤**에 `s.pos`(= 재배치 «후» 위치)에서 암홀 봉제쌍 중심을 읽는다.
⟹ 판정문의 「재배치된 암홀 중심으로 `s` 를 뜨는 순서 정정」은 **이미 성립해 있었다 · 고칠 줄이 없다**.

### §1-② f0 채널 — ★★★ **세 칸 모두 «자기검사 던짐»**(교차 수천 개)

**ㄱ. 교차·관통 — 기준선(`NOS4`)과 나란히**

| 칸 | 교차 «총» 쌍 | 그중 **몸판끼리**(S3) | 소매 관련 | 밴드 밖 관통 정점 | 관통 정점(`signed < 0`) | 자기검사 |
|---|---|---|---|---|---|---|
| `c100-h170-s45_M` | **9241** | 7703 | 1538 | **762** | 1080 | **던짐** |
| `supima-L` | **5495** | 4890 | 605 | **932** | 1269 | **던짐** |
| `mst-over-L` | **2224** | 2033 | 191 | **1066** | 1456 | **던짐** |
| *(기준선 `NOS4` · v5-19·20 등재)* | *296 / 288 / 136* | *296 / 288 / 136* | *0 / 26 / 40* | *157 / 114 / 134* | — | *던짐* |

```
 ★★★ 교차가 **21.4배(M 296→9241) · 19.1배(supima 288→5495) · 16.4배(mst 136→2224)** 로 «늘었다».
 ★★★ 밴드 밖 관통도 **4.85배(157→762) · 8.18배(114→932) · 7.96배(134→1066)** 로 «늘었다».
 ⟹ 판정문의 예측(「관통 0(구성상) · 교차 0(평면) · 결합 보존」)은 **세 채널 전부에서 반증됐다.**
 던짐 문구 — M 「옷 자기 «교차» — 교차 9241개 · 최소쌍 front↔back 0.001523mm — 갈래 D」 ·
   supima 「교차 5495개 · 최소쌍 front↔front 0.009964mm」 · mst 「교차 2224개 · 최소쌍 front↔front 0.003229mm」.
```

**패널 조합**(상위) — M `back↔front` **5166** · `back↔back` 1362 · `front↔front` 1175 · 소매 넷 433/418/351/336 /
supima `back↔front` **2439** · `back↔back` 1325 · `front↔front` 1126 / mst `back↔front` **1034** · `front↔front` 563 · `back↔back` 436.
**암홀 열 안 교차 비율** = **0.999243 / 0.996360 / 1**(교차는 여전히 암홀 열에서 난다).

**ㄴ. 이음 엣지 신장 — 채널로만**(문턱 0 · 판정 0)

| 칸 | 최소 배율 | **중앙 배율** | **최대 배율** | 최대 자리 | 3D mm 중앙 / 최대 |
|---|---|---|---|---|---|
| `c100-h170-s45_M` | 3.8254 | **15.1765** | **40.8496** | `front(i52)` | 139.106 / 385.889 |
| `supima-L` | 3.9912 | **11.8957** | **41.3813** | `front(i13)` | 113.872 / 394.944 |
| `mst-over-L` | 4.5317 | **11.7941** | **43.0397** | `front(i47)` | 106.656 / 418.153 |

```
 ★ 나란히 놓을 등재 자 — 이 저장소에 «정점 쌍 신장 문턱»은 **없다**(v5-20 §2ㄴ 가 등재한 사실 ·
   `s4Gate.ts` 다섯 채널에 그 항이 없다). 면내 제약의 rest 는 **2D 패턴 거리**이고,
   `CLAUDE.md` 가 남긴 v1 계열 자는 「`maxStrain` limiter 상한 **1.2**」 뿐이다.
   그 자로 보면 중앙 배율 11.79~15.18 은 **9.8~12.6배 밖**, 최대 43.04 는 **35.9배 밖**이다.
   ⟹ (다)의 「램프 정의역 밖」은 **값으로는 그렇게 보이지만**, 등재된 문턱이 없으므로 **판정하지 않는다**.
     (나)가 이미 성립하므로 갈래는 어차피 (나)다.
```

**ㄷ. 봉제쌍 거리 — 어깨는 «닫혀 있다» · 암홀은 램프가 닫을 거리**

| 칸 | 어깨 n · 최소 / 중앙 / 최대 mm | 암홀↔소매캡 중앙 mm(앞R/뒤R/앞L/뒤L) | 암홀 최소 mm | 암홀 최대 mm |
|---|---|---|---|---|
| `M` | 34 · 1.9999999999999982 / 2.0000000000000018 / 112.14316948323102 | 104.34 / 106.96 / 106.20 / 108.02 | 26.66~33.17 | 175.4~182.7 |
| `supima-L` | 36 · 2.0 / 2.0 / 132.6627 | 61.05 / 69.74 / 61.01 / 69.77 | 14.40~15.46 | 119.5~134.4 |
| `mst-over-L` | 44 · 2.0 / 2.0 / 141.8661 | 69.98 / 60.16 / 69.96 / 60.16 | 13.73~13.87 | 130.0~144.4 |

★ 어깨 봉제쌍 **중앙이 정확히 `SEP` = 2 mm**(판정문 「간극 SEP」 그대로) — 다만 **최대가 112~142 mm** 다
(펼침이 좌우 끝 열에서 앞뒤를 서로 다른 방향으로 보내기 때문이다 · 값만 · 해석 0).

**ㄹ. 링/rest · 행/열 간격**

| 칸 | 목선 링 3D mm | rest(패턴) mm | 비 | 행 간격 최소/중앙/최대 mm | 설계 행 간격 mm | 열 간격 최소/중앙/최대 mm | 최근접 정점쌍 mm |
|---|---|---|---|---|---|---|---|
| `M` | 495.3954 | 469.5765 | 1.055 | 8.3095 / 9.5459 / **385.8889** | 9.855072463768117 | 5.2523 / 8.9925 / **366.4749** | 0.569049 |
| `supima-L` | 521.3663 | 469.6881 | 1.110 | 8.1729 / 9.4420 / **394.9444** | 9.726027397260273 | 3.8765 / 9.1061 / **378.0640** | 0.793012 |
| `mst-over-L` | 532.8758 | 469.6107 | 1.1347 | 8.3697 / 9.4652 / **418.1529** | 9.736842105263158 | 5.2637 / 8.8473 / **390.4067** | 1.039074 |

★ 행 간격 «최대»가 이음 엣지(3D 385.9 / 394.9 / 418.2 mm)와 **같은 수**다 — 그 한 엣지가 분포의 꼬리를 만든다.

### §1-③ ★★★ **왜 «평면»이 안 됐나 — 꼭대기 행의 몸 법선이 «없다»**(신설 계기 · 측정)

```
 꼭대기 행(j = nvB)의 정점마다 몸 SDF 값과 기울기 크기를 «훅이 준 자»(`__v3gapProbe.sdf`)로 쟀다.
```

| 칸 | 꼭대기 행 정점 | **기울기 = 0 인 정점** | 앞/뒤 | 꼭대기 `sdf` 최소 mm | 꼭대기 `sdf` 중앙 mm | 코드가 «떨어뜨린» 열 |
|---|---|---|---|---|---|---|
| `M` | 118 | **58**(49.2%) | 28 / 30 | 7.0501 | **8.229149505496025** | **68** |
| `supima-L` | 122 | **100**(82.0%) | 50 / 50 | 7.8009 | **8.229149505496025** | **104** |
| `mst-over-L` | 138 | **120**(87.0%) | 60 / 60 | 8.1328 | **8.229149505496025** | **128** |

```
 ★★★ **기울기가 0 인 정점의 `sdf` 는 «전부» 8.229149505496025 mm = 밴드 값**(`sdfSpec.band` 8.229149175625224)이다.
   ⟹ 그 자리는 **격자 SDF 의 밴드 «밖»** 이라 값이 상수로 잘려 있고 **중앙차분이 0** 이다.
 ★★★ 구조적 사실 — 배치 간격 `D_MM` = **9 mm** > 밴드 **8.2291 mm** ⟹ **꼭대기 행은 «설계상» 밴드 밖**이다.
   판정문이 요구한 「그 자리 몸 법선 `n`」이 **절반~87% 의 열에서 존재하지 않는다.**
 ⟹ 그 열들은 §0-3ㄱ 의 등재대로 **전역 `−y`** 로 떨어졌다 ⟹ 그 열의 펼침은 «바깥»이 아니라 **수직 낙하**이고,
   앞뒤 패널이 같은 자리로 내려와 `back↔front` 교차(5166 / 2439 / 1034)를 만든다.
 ★ 「코드가 떨어뜨린 열」(68 / 104 / 128)이 「기울기 0 정점」(58 / 100 / 120)보다 **10 / 4 / 8 개 많다** —
   차이는 `|n̂ + (−ŷ)| ≤ 1e-9`(법선이 `+ŷ` 에 거의 나란한 열 · §0-3ㄱ 의 둘째 조건)와
   두 계기가 보는 δ 회차가 다른 것(코드 = 마지막 장면 · 계기 = `gapProbe` 회차 7)에서 온다. **값으로만 적는다.**
```

### §1-④ 결론 문안 초안 — **판단 0**(CC 가 고르지 않는다)

**(나)(다) 쪽 문안**(이 판의 값이 가리키는 쪽 · 사용자·전략 세션이 고른다)

```
 「2세대 종결 — 조립 «재배치» 축은 후보를 다 썼다. 마지막 후보 «바깥 펼침»은 세 칸 모두
   자기검사에서 던졌고(교차 9241 / 5495 / 2224 · 밴드 밖 관통 762 / 932 / 1066),
   기준선(`NOS4` 296 / 288 / 136 · 157 / 114 / 134)보다 **나빠졌다**.
   기전은 이 판이 값으로 특정했다 — **꼭대기 행이 밴드 밖이라 «몸 법선»이 없다**(기울기 0 정점
   58 / 100 / 120 · `sdf` 가 전부 밴드 값 8.2291 mm) ⟹ 「몸 바깥으로 평면으로 건다」가
   **기하적으로 정의되지 않는 자리**가 절반 이상이다.
   ⟹ 정의역 등재(2번 항목): 「어깨 끝이 몸 어깨보다 «밖»인 옷(드롭숄더)의 2세대 재배치는 정의역 밖」 ·
     1세대 조립(현행 제품 경로)은 **그대로 유효**하고 회귀·재굽기 비용은 **0**(`asm2` 기본 off · off 비트 불변).」
```

**(가) 쪽 문안**(성립하지 않았으므로 **쓰지 않는다** · 자리만 남긴다)

```
 「2세대 보관 · 1막 종결 뒤 재개」 — ③(가)의 조건(세 칸 교차 0 ∧ 밴드 밖 0 ∧ 자기검사 통과)이
 **하나도** 성립하지 않았다 ⟹ 이 문안은 이 판에서 **쓰지 않는다**.
```

---

## §2 자기검사

```
ㄱ **`src/` diff = 이 후보 «한정»** — 바꾼 자리 다섯 + 인쇄 채널 하나다:
   ① `asm2Fix` 유니온에 `'OUT'`(`garmentScene.ts:84` · `dressRun.ts:56`)
   ② `S3OUT` 도입 ③ `S4NONE` 에 `OUT` 포함 ④ `tube` 스냅숏 조건에 `OUT` 포함
   ⑤ `if (S3OUT && tube) { … }` 블록 하나(펼침)
   ⑥ ★ **인쇄 채널 2건**(`__asm2Probe` 안 · **거동 0줄**) — 「이음 엣지 신장」·「펼침 떨어뜨린 열」.
     §1-② ㄴ 이 프롬프트가 요구한 채널이라 넣었고, **`asm2` 가 켜진 장면에서만 계산**한다.
     ⟹ 「플래그 한정」 문언보다 **한 건 넓다**(자기신고 · v5-19 가 같은 훅에 「목선 링」을 넣은 선례와 같은 형태).
ㄴ **물리 0줄 · 처방 0 · 굽기 0칸 · 캡처 0장 · 화면 판정 0 · 판단 0.**
ㄷ **`pushOut` 을 부르지 않았다**(§0-3ㄹ 등재대로) — 관통 수는 «구성의 결과»이지 밀어냄의 결과가 아니다.
ㄹ ★ **한정: 이 판이 시험한 것은 «형식화된 그대로의» 바깥 펼침이다.** 꼭대기 행의 절반~87% 에서
   몸 법선이 없어 그 열들은 **전역 `−y`** 로 떨어졌다(§1-③) ⟹ 「모든 열이 진짜로 바깥으로 펼쳐진 판」은
   **이 판이 시험하지 않았다**. 그런 판을 만들려면 **밴드 밖에서도 법선을 주는 자**(예: 배치 간격 `D_MM` 을
   밴드 안으로 줄이거나 · 몸 표면에서 직접 법선을 뜨는 계기)가 필요하고, 그것은 **새 규칙**이다(처방 0).
ㅁ ★ **침투 깊이는 여전히 포화한다**(v5-19·20 과 같은 한정) — 중앙·p99·최대가 전부 **8.2291 mm**(= 밴드)다.
   「밴드 밖」은 «그보다 깊다»는 뜻이고 **얼마나 깊은지는 이 자로 못 잰다**.
ㅂ **새 상수 0 · 문턱 신설·이동 0** — 쓴 수는 `N_side`·`nvB`·`nuB`·패널 `uv`·`sdfSpec.h`·`SEP`·`TOL_SELF`·
   `sdfSpec.band` 뿐이고 전부 등재분이다.
ㅅ **이 판에서 호출한 ecc 에이전트 = 0 · 스킬 = 0.**
```

### §2-① off 비트 불변 — T108 조립 해시 대조 · **108/108 동일**

```
 계기 `scripts/v3Assemble108.ts`(v3-88 · `asm2` 를 «켜지 않는다» ⟹ off 경로) 를 두 번 돌려 대조했다.
   «전» = 기점 `0e6d0d7` 의 작업트리에서 구현 «전»에 띄운 배경 실행(모듈은 시작 시 한 번 읽힌다) ·
   «후» = 구현(`30b2a0d`) 뒤 같은 인자로 재실행.
 결과 — **칸 108 / 108 «완전 동일»**(칸마다 결과·사유·패턴해시·정점·최소쌍·회차·R비·호 전부) ·
   산출 파일 전체 sha256 앞 32 = **87bc2018faf4309d4d770cac3f5afd4f**(전·후 «같다») ·
   조립 성립 82칸(v4-37 이래의 그 수).
 ⟹ **off 경로 비트 불변**(갈래 D 사유 없음). `asm2` 가 꺼지면 `FIX = undefined` 라 이 판이 만든 여섯 자리가
   **한 번도 참이 되지 않는다**는 §0-3ㅇ 의 주장이 값으로 확인됐다.
 ★ A29(A포즈 29칸)는 **이 판에서 돌리지 않았다** — off 경로가 T108 에서 비트 동일이면 같은 코드 경로이므로
   추가 증거가 아니지만, 「A29·T108」을 둘 다 요구한 판(v5-18)과 달리 **이 판은 T108 만 집행했다**(자기신고).
```

---

## §4 판정 — **갈래 A**(③ 의 갈래 = **(나)**)

```
 ★ A = ③ 어느 갈래든 «값 완비» ⟹ 상신   … **✔ 이것이다**(세 칸 전 채널 측정 완료)
   B = 구현 불가(사유) ⟹ 상신             … ✗ 구현했고 돌았다
 ③ 의 갈래 = **(나) 교차 > 0 또는 밴드 밖 > 0 ⟹ 2세대 «종결» 재료**
   — 교차 **9241 / 5495 / 2224** · 밴드 밖 관통 **762 / 932 / 1066** · 자기검사 **세 칸 모두 던짐**.
   (가)는 세 조건 중 **하나도** 성립하지 않았다. (다)는 값으로는 그렇게 보이나(중앙 배율 11.79~15.18 ·
   최대 43.04) **등재된 신장 문턱이 없어 판정하지 않는다** — (나)가 이미 성립한다.
 사후 갈래 신설 0.

 ① = 형식화 등재대로 집행 · **소매 축 이동 순서 정정 diff = 0줄**(이미 재배치 «뒤»에 `s` 를 뜬다).
 ② = 전 채널 측정 완료(교차·관통·이음 신장·봉제쌍·링/rest·행/열 간격·자기검사).
 ③ = **(나)**.
 ④ = (나)(다) 문안 초안 등재 · (가) 문안은 **쓰지 않았다**.

 ★★★ 이 판이 «새로» 준 사실 — **꼭대기 행(봉제선)이 밴드 «밖»이라 몸 법선이 없다**:
   기울기 0 정점 **58 / 100 / 120**(전체 118 / 122 / 138) · 그 자리 `sdf` 가 **전부 밴드 값 8.2291 mm** ·
   배치 간격 `D_MM` **9 mm** > 밴드 **8.2291 mm** ⟹ **구조적**이다.
   ⟹ 「몸 바깥으로 건다」는 처방은 **그 자리에서 정의되지 않는다**(v5-19 의 「밴드 밖은 기울기가 소실돼
     S4 가 못 민다」와 **같은 원인**이 이번엔 «배치»를 막았다).
```
