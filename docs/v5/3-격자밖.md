# v5-3 — 격자 밖 실측표(오버핏) · 조립·굽기 · 판정 · 캡처 — «새 옷 경로» 첫 시험

**물리 0줄 · `src/` diff = v5 신설 모듈 «안»의 데이터 등재(+ 형식 수용 최소) · v5-2 진입 경로 재사용 · 굽기 1칸 · ecc 0**

| | |
|---|---|
| 브랜치 | `v5-3-offgrid-spec` |
| 기점 | **`03a2cf6`**(= v5-2 보고 부록) |
| §0 커밋 | (아래 §0-5 에 자기 기입) |
| §1~§4 커밋 | (집행 후 기입) |
| 보고 파일 | `docs/v5/보고/3.md` |

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v5-3-offgrid-spec 03a2cf6 → v5-3-offgrid-spec · HEAD 03a2cf6
 2호기 (§0 커밋 push 뒤 같은 자리로 — §0-5 에 인쇄)
```

---

## §0 수령·사무

### §0-1 판정문 (전략 세션 · v5-2 §4 수령 · 원문)

```
 「v5-2 갈래 A 수용 · 단서: 첫 실측이 격자 위 점(자사 L 과 차 0)이라 «새 옷» 경로 미시험 —
   파이프라인 결정론 검증으로만 등재 ·
   v5-3 = 격자 밖 + 핏 성격이 다른 실측: 무신사 스탠다드 오버사이즈 T(L 급 공개 실측)
   총장 74 · 어깨 52 · 가슴단면 60 · 소매길이 24 (cm · SLEN 직접 제공 — hwa 아님 ·
   파서의 두 형식 수용 확인 겸함) ·
   성격 = 자사 격자와 축 비율이 다른 점(어깨는 XL 초과 · 총장은 XL 미만) · 이름 = mst-over-L ·
   몸 = 기본 칸(Bwsr_M) ·
   기대(값 아님 · 방향만 등재): 자사 L 대비 어깨·가슴이 크게 남고 총장 유사 — 화면에서 오버핏 드레이프」
```

### §0-2 바꾸지 않는 것

```
 물리 **0줄** · `garmentScene`·`dressRun`·워커·게이트·문턱 **0** · 기존 4사이즈·supima-L 경로 **불변** ·
 v4 자산·제공 목록 **0**
 ★ **사실 병기** — 회차 프롬프트는 「`src/` diff 0(데이터 등재만)」을 걸었다. 그런데 이번 실측은
   **소매길이(SLEN)를 «직접» 준다**(화장 `hwa` 가 아니다). v5-2 의 등재 자료형은 `ChartRow`(화장 형식) 하나뿐이라
   **그대로는 담을 수 없다**. 손으로 `hwa = SLEN + SW/2` 를 계산해 넣는 것은 **손 환산**이라 금지다(v5-2 §0-4ㄱ)
   ⟹ **v5 신설 모듈 안에서** 두 형식을 받는 최소 확장을 한다(기존 파일·기존 경로 **0줄**).
   판정문도 「파서의 두 형식 수용 확인 겸함」이라 적었다.
```

### §0-3 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · 단위 명시)

**★ 단위** — 실측 cm · 패턴 m · 정착 프레임 · 게이트 mm · 어깨 채널 정점 개수.

| 항 | 갈래 |
|---|---|
| **①** 파싱·패턴 | SLEN **직접 입력** 경로 확인 · 자사 4행과 나란히(격자 밖을 값으로) |
| **②** 조립·굽기 | 기본 칸 A포즈 규약 · **던짐 시 규칙·사유를 값으로**(템플릿 정의역 문제인지) |
| **③** 판정 | 게이트 5채널 + 어깨 걸림(≥172) + 겨드랑이 + 층3 5행(자사 M · supima-L 과 **3열 비교**) |
| **④** 캡처 | 2장 · **화면 판정 0** |

**§4** — A = 게이트 pass / B = fail(값·캡처) / C = 조립 던짐(정의역 확장 재료) / D = 그 외 ⟹ 정지.

### §0-4 사전 결정 4건 (값 보기 «전» · CC 집행 설계)

```
 ㄱ **두 형식을 «자료형»으로 가른다** — 화장 형식(`row`)과 실측표 형식(`spec`)을 **둘 다** 받는다.
    변환식은 v5-1 것을 그대로 쓴다(`SLEN = hwa − SW/2`). **손 환산 0.**
 ㄴ **던지면 그것도 결과다** — 조립이 갈래 D 로 던지면 **규칙 문언과 값**을 적고 §4 C 로 간다.
    문턱·템플릿 상수를 **그 자리에서 바꾸지 않는다**(정의역 확장은 전략 세션 판정).
 ㄷ **비교는 3열로** — 자사 M · supima-L · mst-over-L 을 같은 표에 둔다(같은 몸 · 다른 옷).
 ㄹ **기대는 «방향»만 등재하고 판정에 쓰지 않는다** — 판정은 게이트·채널이 한다(화면 판정 0).
```

### §0-5 커밋·전환 인쇄 (집행 «전»)

(§0 커밋 sha 자기 기입 · 2호기 전환 인쇄는 아래에 붙인다)
