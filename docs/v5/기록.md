# v5-기록 — 프로젝트 연대기(v1~v4) 집행 노트

## §0 수령·사무 (집행 «전» 선커밋)

### git 스탬프
- 집행 기계 = 에어(M4 · Metal)
- 브랜치 = `v5-chronicle`
- 기점 커밋 = `74a13a1` (= v4-52 HEAD · "v4-52 보고 — 부록 링크")
- 워크트리 = `/Users/banshk/projects/fit-simulator`
- 기점 검증 — `git merge-base --is-ancestor 74a13a1 v5-7a-prep` = YES(29 커밋 앞) ·
  `git rev-list --count main..74a13a1` = **984**(main 이 v2 T2/T3 에 멈춰 있다)

### 이 판에서 하지 않는 것
- 물리 0줄 · `src/` diff 0 · `gpu/` diff 0 · 굽기 0 · 측정 0
- ecc 에이전트 0 · ecc 스킬 0
- 창작 0 — 모든 서술은 저장소 안 재료(볼트 · `docs/v1~v4` · `docs/metrics-log.md` · 캡처)에서 «읽어» 옮긴다

### 판정문 (§0-1 · 원문)
> 용도 = 개인 기록(외부 독자 아님) ⟹ 잘 쓴 글보다 «정확한 기억» — 몇 달 뒤의 승혁이
> 그때의 판단·막힘·뚫은 방법을 되살릴 수 있는 문서 · 재료는 저장소 안 전부 · 창작 0 ·
> 모든 서술에 근거(회차 번호) · 길이 제한 없음 · 한국어

### 사무 — 기점 제약 1건 (등재)
`docs/v5/` 는 기점 `74a13a1` 에 **없다**(`git ls-tree 74a13a1 docs/v5/` = 빈 출력).
`docs/v5/v5-색인.md` 는 v5 본선(`v5-7a-prep`, HEAD `8e58c66`)에만 있다.
⟹ **「v5 색인 등재」는 이 브랜치에서 못 한다.** 아래 순서로 집행한다:
1. `v5-chronicle`(이 브랜치) — 연대기 · 보고 작성 · push
2. `main` ← `v5-chronicle` merge (= v4-52 전량 + 연대기) · 태그 `v1.0-apose` at `74a13a1`
3. `v5-7a-prep` ← `v5-chronicle` merge → 그 위에서 `docs/v5/v5-색인.md` 등재 · push

### 산출 예정
- `docs/연대기-v1-v4.md` — 본편(§1 ①~⑥)
- `docs/v5/보고/기록.md` — 회차 보고(본문 전량 · v4 보고 형식)
- `docs/v5/기록.md` — 이 노트(집행 정본)

---

## §1 집행

### §1-① 재료 훑기 (판정 전 · 읽기만)

**읽은 재료 전량**(파일 · 줄):

```
 .obsidian-log/00-현재상태.md               5,958줄  전량(v3 캠페인 + v4 + 제품 축 P10~P34 + v3 설계)
 .obsidian-log/00-현재상태-v2캠페인.md      6,966줄  구조 블록 선택(§위치 · 기준선 · 게이트 상태 ·
                                                    막고 있는 것 · 28~49회차 요약 · 물리 도달 이력 ·
                                                    사용자 미학 기준 · 몸 좌표 민감도)
 .obsidian-log/전략세션-인수인계.md         6,618줄  헤딩 구조 + 귀책 grep
 .obsidian-log/함정/ 44 + 후보 6 + 지표함정  머리 8~14줄씩 전량
 .obsidian-log/원리/ 8편                    머리 8줄씩 전량
 .obsidian-log/규칙/화면판정.md             앞 30줄 · .obsidian-log/감사/계기의존판정감사.md 앞 40줄
 docs/metrics-log.md                       30,562줄  머리 52줄 + 헤딩 200 + v3-78 · v4-46 블록
 docs/v5/보고/1~6.md                        1,306줄  전량(v5-7a-prep 브랜치에서 git show)
 docs/v5/v5-색인.md · docs/v5/7a-준비.md     전량 · 앞 80줄
 docs/product-axis/P1-진단.md               앞 28줄(제품 축 성격 확인)
 git                                        커밋 경계 5지점 · 날짜별 커밋 수 · 캡처 디렉터리 날짜
```

★ **읽지 않은 것**(사실 등재) — `docs/v3/*.md` 92편 · `docs/v4/*.md` 52편 ·
`.obsidian-log/회차/*.md` 108편 **개별 전문**. 근거는 `00-현재상태.md` 가 그 회차들의
**요지·수치를 회차별로 담고 있고**(회차마다 「정본 = …」 포인터를 함께 적는다)
v3·v4 노트가 그 요지의 출처이기 때문이다. **이 연대기의 인용은 전부 볼트 요약본에서 왔고**
그 사실을 보고에 적는다.

### §1-② 산출 — `docs/연대기-v1-v4.md`

| 항 | 값 |
|---|---|
| 총 줄 | **1,029** |
| 절 | ① 한 장 요약 · ②-1~②-4 버전 4장 · ③ 사고와 귀책 · ④ 도구와 체제 · ⑤ 숫자 · ⑥ v5 앞에서 |
| 표 | **89행**(11개 표) · 코드 펜스 **11블록** |
| 캡처 임베드 | **9장**(v1 1 · v2 2 · v3 3 · v4 3) — 전부 상대 경로 · 파일 존재 검사 통과 |
| 볼트 링크 | **9종**(원리 6 · 규칙 1 · 감사 1 · 중복 제외) — 마크다운 상대 링크로 · 파일 존재 검사 통과 |
| 회차 인용 | v3 **50종** · v4 **43종** · v5 **8종** · 2b **18종** · 제품 축 **13종** · 함정 **18종** |
| 핵심 사건 수 | v1 **8** · v2 **10** · v3 **17** · v4 **17** |

★ **위키링크를 마크다운 상대 링크로 바꿨다** — 이 파일은 `docs/` 에 있어 옵시디언 볼트
(`.obsidian-log/`) **밖**이고 `[[ ]]` 가 풀리지 않는다. GitHub 에서도 열리는 형태로 통일했다.

### §1-③ 겸사

| 항 | 상태 |
|---|---|
| `main` ← `v5-chronicle` merge | §3 에 기입 |
| 태그 `v1.0-apose` | §3 에 기입(대상 = `74a13a1` = v4-52 HEAD) |
| `docs/v5/v5-색인.md` 등재 | **이 브랜치에서 불가**(§0 사무) ⟹ v5 본선에서 별도 집행 · §3 에 기입 |

## §2 자기검사

| 항 | 값 |
|---|---|
| `src/` diff vs 기점 `74a13a1` | **0**(무출력) · 물리 **0줄** |
| `gpu/` · 워커 · 게이트 · 문턱 · `public/` diff | **0** |
| 굽기 · 측정 · 계기 실행 | **0** |
| 신설 | `docs/연대기-v1-v4.md` · `docs/v5/기록.md` · `docs/v5/보고/기록.md` |
| 수정 | **0**(기존 파일을 한 줄도 고치지 않았다 — 캡처는 «참조»만) |
| 링크·이미지 존재 검사 | **전량 통과**(`docs/` 기준 상대 경로 18종) |
| ecc 에이전트 / 스킬 | **0 / 0** |
| 화면 판정 | **0** — 캡처는 회차 노트가 적은 «무엇을 찍은 것인가»로만 라벨했다 |
| 창작 | **0** — 모든 서술에 회차 번호를 붙였고, 이 문서가 «계산»한 수는 그 자리에 「이 문서 계산」으로 표기 |

### 이 문서가 «계산»한 수 (측정 아님 · 함정 44 대응)

```
 커밋 수 5종     git rev-list --count 경계 5지점의 «차»  — 201 / 376 / 407 / 216 / 29
 v1 회차 수 63   awk 로 metrics 블록 헤딩을 센 값(파일 시작 ~ Stage 1a 블록 직전)
 문서 통계       총 줄 · 표 행 · 캡처 · 링크 · 인용 종수(grep/wc)
 ★ 그 밖의 모든 수는 볼트·metrics 에 «문자열로» 있는 것을 읽어 옮겼다
```

## §3 회차 갈래 — **A(완성)**

여섯 장 전부 섰다. **부분 결손 1건 · 한정 3건**을 아래 자기신고에 적는다.

### 자기신고

```
 ① 결손 1건 — 전략 세션 귀책 «11건째»의 문언을 볼트에서 찾지 못했다
    (git grep "11건째" = docs/v4·docs/v5 에서 0건 · v2 캠페인 파일의 「정정 14건째」는 다른 계열).
    ⟹ 표에 **「등재 확인 실패」로 적고 추정하지 않았다.**
 ② 한정 1 — 사전 등재는 「핵심 사건 5~10」이었는데 v3·v4 를 **17개씩** 썼다.
    사유 = 두 캠페인이 각각 90·52판이고 10 으로 접으면 「막힘과 뚫은 방법」이 빠진다
    (판정문이 그것을 명시적 목표로 걸었다). 「길이 제한 없음」 조항으로 초과를 택했다.
 ③ 한정 2 — §⑥ 「v5 앞에서」가 «반 페이지»를 넘는다(약 70줄).
    사유 = v5-1~7a 일곱 판의 값을 요지로 담았다. 접으면 다음 판이 이 문서로 못 이어받는다.
 ④ 한정 3 — **v5 캡처는 임베드하지 않았다.** 이 브랜치 기점(`74a13a1`)에 `docs/v5/캡처/` 가
    없다(§0 사무와 같은 사유). §⑥는 경로만 적었다.
 ⑤ 한정 4 — v3·v4·2b 회차 노트 **개별 전문은 읽지 않았다**(§1-①). 인용은 전부 볼트 요약본에서 왔다.
```

### git 스탬프

```
 브랜치      v5-chronicle
 기점        74a13a1(= v4-52 HEAD)
 §0 선커밋   a9eef35
 §1~§3 커밋  004a081
```
