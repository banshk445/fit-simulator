# v5-16 — (A) 확정 · 자기검사 «교차 0» 진단 경로 · R3 굽기 3칸 = **2세대 첫 굽기**

```
집행 기계  2호기(굽기) + 에어(조립·sha 대조 · 물리 0프레임)
브랜치     v5-16-first-bake   기점 **98803dc**(v5-15 HEAD · 확인 = git log -1 이 98803dc)
약정       물리 0줄 · `src/` diff = `redrapeAsm2` 하위 플래그 정리 + **조립 자기검사의 «진단 완화
           경로»**(`asm2` on 에서만 · off 비트 불변) · 굽기 **3칸** · ecc 0 · §0 선커밋 · 브랜치 인쇄 ·
           보고 = docs/v5/보고/16.md · 캡처 3칸 앞·옆 · 콘솔 4줄
```

## §0-1 판정문 (전략 세션 · v5-15 §4 수령 · **원문**)

```
갈래 B 수용 · (A) 걸음 상한 THICK 채택(행 건너뜀 소멸 · 유도 새 수 0) · (B) 보류(곡률 R<행 길이
구간에서 열 모임 · 사전 점검 적중 · 열 간격 항 없이는 불성립) ·
★★ 남은 f0 결함의 성격 전환: 관통·교차가 아니라 천끼리 SEP 미만 근접(0.5~1.6mm) = 자기충돌 제약의
정의역 안(sep 안 쌍 전부 처리 · v3-12) ⟹ v4-37 규칙의 자기충돌판: 「f0 자기 근접이 교차 0 이고
제약 작동 범위 안이면 최종 판정은 굽기(게이트)가 한다」 ·
조립 자기검사는 asm2 경로에서 «거리 ≥ SEP» 대신 «면 교차 0 ∧ 최소쌍 > 0» 으로 자의 대상 정정
(문턱 이동 아님 · 근거 = 제약 정의역 · off 경로 불변)
```

## §0-2 수령 — 집행 «전»에 읽은 코드 사실 3건

```
 ㄱ ★ **필요한 계기가 이미 있다** — `instruments.ts:66 minPairDist(pos, tris, window)` 가
    **`min`(최소쌍 거리) · `worst`(삼각형 쌍) · `hits`(삼각형–삼각형 «교차» 수)** 를 함께 준다.
    그 `hits` 가 **게이트의 「자기관통 교차」 채널 그 자체**다(`s4Gate.ts:143` `mp.hits !== 0`) ·
    교차 판정기는 `triTriHit`(`:135`) · `segTriHit`(`:118`) ⟹ **새 계기 0**.
 ㄴ **자기검사의 판정식은 한 줄이다** — `garmentScene.ts:1243` `if (m0 >= need) { ok = true; break; }` ·
    `need = SEP`(`:1225`) · `m0 = minPairDistLite(...)`(`:1230` · **거리만** 준다) ·
    실패 시 `:1251` 에서 `minPairDist(..., SEP*2)` 로 **다시** 재어 문언을 만든다.
    ⟹ 판정문의 「자의 대상 정정」은 **`m0` 을 «거리»에서 «교차 + 최소쌍 > 0»으로 바꾸는 것**이고,
      `asm2` 가 꺼져 있으면 **그 줄을 그대로 지나간다**(off 경로 바이트 불변).
 ㄷ **`v4AsmExport.ts` 는 아직 `asm2` 를 넘기지 않는다** — ③의 굽기용 조립을 내려면 그 자리에
    `ASM2`/`ASM2FIX` env 를 열어야 한다(계기 쪽 · `src/` 아님).
```

## §0-3 사전 등재 갈래 (회차 프롬프트 그대로 · 사후 신설 0)

| 항 | 집행 | 갈래 |
|---|---|---|
| **①** (A) 경로 f0 채널 | 세 칸: **면 교차 수**(`minPairDist.hits` · **0 이 조건**) · 최근접 정점쌍·면쌍 거리 · 관통 정점 · 행 간격 최소/중앙 · 봉제쌍 ≈ `SEP` · 링/rest · 어깨끝 `s` · S4 반복 | **(가)** 교차 0 ∧ 최소 > 0 **세 칸** / **(나)** 교차 > 0(칸·자리) ⟹ ③ 착수 0 |
| **②** 자기검사 진단 경로 | `asm2` on 에서만 «교차 0 ∧ 최소쌍 > 0» 판정(diff 원문) · **off 비트 불변 A29·T108 재확인** | 통과/깨짐 |
| **③** R3 굽기 3칸(on · (A)) | 게이트 5채널 · 어깨 걸림(**정도**) · 겨드랑이 · **링 궤적**(f0→정착) · 층3 5행(현행 나란히) · `RAMP_N` 실측(예측 ≈52/42) · **f0 근접쌍이 몇 프레임에 `SEP` 로 벌어지는지**(위반 수 궤적 · v4-37 형식) · 캡처 | — |
| **④** 「같은 옷」 | 레귤러핏 2칸을 **v4-17 ①②**로 현행과 대조 · mst-over 회복 **정도** | 판단 0 |
| **⑤** 종합 | R4~R8 착수 재료(제공 29 재굽기 여부는 **화면 판정 뒤**) | 판단 0 |

**§4** — **A** = ①(가)+②+③ **게이트 pass ≥ 2**(레귤러 2칸 pass **필수**) ⟹ 상신(화면 판정) /
**B** = ③ 레귤러 fail(값) ⟹ 상신 · 판정은 전략 세션 / **C** = ①(나) ⟹ 사실 상신 /
**D** = ② 비트 불변 깨짐 ⟹ **즉시 정지** / **E** = 그 외 ⟹ 정지.
**사후 갈래 신설 0 · 걸림은 «정도»로.**

## §0-4 사전 결정 5건 (값 보기 «전»)

```
 ㄱ **②의 diff 는 «한 줄 분기»다** — `asm2` on 이면 판정을 `mp.hits === 0 && mp.min > 0` 로,
    off 면 `m0 >= need` 그대로. 재는 함수는 **이미 있는 `minPairDist`**(새 계기 0) ·
    `window = SEP * 2`(실패 문언이 이미 쓰는 그 창 · 새 수 0).
 ㄴ **「최소쌍 > 0」의 뜻** — 부동소수에서 정확히 0 은 «같은 점»이다 ⟹ `min > 0` 은
    **정점/면이 겹치지 않는다**는 뜻이고 문턱이 아니다(판정문 「문턱 이동 아님」).
 ㄷ **(A) 를 «정본 하위 플래그 기본값»으로 올린다** — `asm2` 가 켜지면 `asm2Fix` 기본이 **`'A'`** 다
    (판정문 「(A) 채택」). `'B'` 는 **보류**로 남긴다(코드 삭제 0).
 ㄹ **③의 「몇 프레임에 벌어지는지」는 굽기 trail 이 아니라 «덤프»가 필요하다** — 이 판의 잡은
    덤프를 켜지 않으므로 **정착 상태에서만** 재고, 「f0 위반 수 → 정착 위반 수」 두 점으로 적는다
    (중간 궤적은 **미측정**으로 적는다 · 추정 0).
 ㅁ **`git add` 명시 경로만 · 구현 중간 산출 즉시 커밋.**
```
