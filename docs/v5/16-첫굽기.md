# v5-16 — (A) 확정 · 자기검사 «교차 0» 진단 경로 · R3 굽기 3칸 = **2세대 첫 굽기**

```
집행 기계  2호기(굽기) + 에어(조립·sha 대조 · 물리 0프레임)
브랜치     v5-16-first-bake   기점 **98803dc**(v5-15 HEAD · 확인 = git log -1 이 98803dc)
약정       물리 0줄 · `src/` diff = `redrapeAsm2` 하위 플래그 정리 + **조립 자기검사의 «진단 완화
           경로»**(`asm2` on 에서만 · off 비트 불변) · 굽기 **3칸** · ecc 0 · §0 선커밋 · 브랜치 인쇄 ·
           보고 = docs/v5/보고/16.md · 캡처 3칸 앞·옆 · 콘솔 4줄
```

## §0-1 판정문 (전략 세션 · v5-15 §4 수령 · **원문**)

```
갈래 B 수용 · (A) 걸음 상한 THICK 채택(행 건너뜀 소멸 · 유도 새 수 0) · (B) 보류(곡률 R<행 길이
구간에서 열 모임 · 사전 점검 적중 · 열 간격 항 없이는 불성립) ·
★★ 남은 f0 결함의 성격 전환: 관통·교차가 아니라 천끼리 SEP 미만 근접(0.5~1.6mm) = 자기충돌 제약의
정의역 안(sep 안 쌍 전부 처리 · v3-12) ⟹ v4-37 규칙의 자기충돌판: 「f0 자기 근접이 교차 0 이고
제약 작동 범위 안이면 최종 판정은 굽기(게이트)가 한다」 ·
조립 자기검사는 asm2 경로에서 «거리 ≥ SEP» 대신 «면 교차 0 ∧ 최소쌍 > 0» 으로 자의 대상 정정
(문턱 이동 아님 · 근거 = 제약 정의역 · off 경로 불변)
```

## §0-2 수령 — 집행 «전»에 읽은 코드 사실 3건

```
 ㄱ ★ **필요한 계기가 이미 있다** — `instruments.ts:66 minPairDist(pos, tris, window)` 가
    **`min`(최소쌍 거리) · `worst`(삼각형 쌍) · `hits`(삼각형–삼각형 «교차» 수)** 를 함께 준다.
    그 `hits` 가 **게이트의 「자기관통 교차」 채널 그 자체**다(`s4Gate.ts:143` `mp.hits !== 0`) ·
    교차 판정기는 `triTriHit`(`:135`) · `segTriHit`(`:118`) ⟹ **새 계기 0**.
 ㄴ **자기검사의 판정식은 한 줄이다** — `garmentScene.ts:1243` `if (m0 >= need) { ok = true; break; }` ·
    `need = SEP`(`:1225`) · `m0 = minPairDistLite(...)`(`:1230` · **거리만** 준다) ·
    실패 시 `:1251` 에서 `minPairDist(..., SEP*2)` 로 **다시** 재어 문언을 만든다.
    ⟹ 판정문의 「자의 대상 정정」은 **`m0` 을 «거리»에서 «교차 + 최소쌍 > 0»으로 바꾸는 것**이고,
      `asm2` 가 꺼져 있으면 **그 줄을 그대로 지나간다**(off 경로 바이트 불변).
 ㄷ **`v4AsmExport.ts` 는 아직 `asm2` 를 넘기지 않는다** — ③의 굽기용 조립을 내려면 그 자리에
    `ASM2`/`ASM2FIX` env 를 열어야 한다(계기 쪽 · `src/` 아님).
```

## §0-3 사전 등재 갈래 (회차 프롬프트 그대로 · 사후 신설 0)

| 항 | 집행 | 갈래 |
|---|---|---|
| **①** (A) 경로 f0 채널 | 세 칸: **면 교차 수**(`minPairDist.hits` · **0 이 조건**) · 최근접 정점쌍·면쌍 거리 · 관통 정점 · 행 간격 최소/중앙 · 봉제쌍 ≈ `SEP` · 링/rest · 어깨끝 `s` · S4 반복 | **(가)** 교차 0 ∧ 최소 > 0 **세 칸** / **(나)** 교차 > 0(칸·자리) ⟹ ③ 착수 0 |
| **②** 자기검사 진단 경로 | `asm2` on 에서만 «교차 0 ∧ 최소쌍 > 0» 판정(diff 원문) · **off 비트 불변 A29·T108 재확인** | 통과/깨짐 |
| **③** R3 굽기 3칸(on · (A)) | 게이트 5채널 · 어깨 걸림(**정도**) · 겨드랑이 · **링 궤적**(f0→정착) · 층3 5행(현행 나란히) · `RAMP_N` 실측(예측 ≈52/42) · **f0 근접쌍이 몇 프레임에 `SEP` 로 벌어지는지**(위반 수 궤적 · v4-37 형식) · 캡처 | — |
| **④** 「같은 옷」 | 레귤러핏 2칸을 **v4-17 ①②**로 현행과 대조 · mst-over 회복 **정도** | 판단 0 |
| **⑤** 종합 | R4~R8 착수 재료(제공 29 재굽기 여부는 **화면 판정 뒤**) | 판단 0 |

**§4** — **A** = ①(가)+②+③ **게이트 pass ≥ 2**(레귤러 2칸 pass **필수**) ⟹ 상신(화면 판정) /
**B** = ③ 레귤러 fail(값) ⟹ 상신 · 판정은 전략 세션 / **C** = ①(나) ⟹ 사실 상신 /
**D** = ② 비트 불변 깨짐 ⟹ **즉시 정지** / **E** = 그 외 ⟹ 정지.
**사후 갈래 신설 0 · 걸림은 «정도»로.**

## §0-4 사전 결정 5건 (값 보기 «전»)

```
 ㄱ **②의 diff 는 «한 줄 분기»다** — `asm2` on 이면 판정을 `mp.hits === 0 && mp.min > 0` 로,
    off 면 `m0 >= need` 그대로. 재는 함수는 **이미 있는 `minPairDist`**(새 계기 0) ·
    `window = SEP * 2`(실패 문언이 이미 쓰는 그 창 · 새 수 0).
 ㄴ **「최소쌍 > 0」의 뜻** — 부동소수에서 정확히 0 은 «같은 점»이다 ⟹ `min > 0` 은
    **정점/면이 겹치지 않는다**는 뜻이고 문턱이 아니다(판정문 「문턱 이동 아님」).
 ㄷ **(A) 를 «정본 하위 플래그 기본값»으로 올린다** — `asm2` 가 켜지면 `asm2Fix` 기본이 **`'A'`** 다
    (판정문 「(A) 채택」). `'B'` 는 **보류**로 남긴다(코드 삭제 0).
 ㄹ **③의 「몇 프레임에 벌어지는지」는 굽기 trail 이 아니라 «덤프»가 필요하다** — 이 판의 잡은
    덤프를 켜지 않으므로 **정착 상태에서만** 재고, 「f0 위반 수 → 정착 위반 수」 두 점으로 적는다
    (중간 궤적은 **미측정**으로 적는다 · 추정 0).
 ㅁ **`git add` 명시 경로만 · 구현 중간 산출 즉시 커밋.**
```

---

## §1-① (A) 경로 f0 채널 — **갈래 (나) 교차 > 0** ⟹ ③ 착수 0

계기 = **기존 `minPairDist(pos, tris, SEP*2)`**(새 계기 0) — `hits` 가 게이트의 「자기관통 교차」 그 채널이다.

| 칸 | **면 교차 수**(`hits`) | 최소쌍 면 거리 mm | 최소쌍 패널 | **최근접 정점쌍 mm** | 그 자리 | S4 반복/상한 | `FIX` |
|---|---|---|---|---|---|---|---|
| `c100-h170-s45_M` | **529** | 0.0004334 | front↔back | 1.58249 | front(i11,j66)↔back(i11,j67) | 21 / 61 | **A** |
| `mst-over-L` | **446** | 0.0001976 | front↔back | 1.32808 | front(i18,j73)↔back(i17,j73) | 20 / 61 | **A** |
| `supima-L` | **512** | 0.00008126 | front↔back | 0.93036 | front(i46,j70)↔back(i47,j71) | 21 / 61 | **A** |

### ★★★ ①의 답 — **판정문의 전제가 «값으로» 기각된다**

```
 ㉠ **교차가 0 이 아니다 — 529 / 446 / 512 개다.**
    판정문은 「남은 f0 결함의 성격 전환: 관통·교차가 **아니라** 천끼리 `SEP` 미만 근접(0.5~1.6mm)」이라
    적었고, 그 위에 완화 근거(「제약 정의역 안이면 굽기가 판정한다」)를 세웠다. 실측이 그 전제를 뒤집는다.
 ㉡ ★ **v5-12~15 의 「삼각형 쌍 거리 ~0」은 «근접»이 아니라 «교차의 증상»이었다** —
    교차하는 두 삼각형의 거리는 **0 으로 보고된다**(`minPairDist` 의 정의) ⟹ 그 판들이 본
    0.000003382 ~ 0.001260 mm 는 **거리가 아니라 교차**였다. **이 판이 처음 `hits` 를 셌다.**
    (v5-12~15 는 `min` 만 인쇄했고 `hits` 를 인쇄하지 않았다 — **CC 자기신고**: 같은 함수가 그 값을
     이미 주고 있었는데 네 판 동안 읽지 않았다.)
 ㉢ **완화된 자로도 통과하지 못한다** — 「교차 0 ∧ 최소쌍 > 0」에서 **교차 조건이 깨진다**
    ⟹ 사전 등재 「(나) 교차 > 0(칸·자리) ⟹ ③ 착수 0」.
 ㉣ **최근접 «정점»쌍은 여전히 0.93 ~ 1.58 mm**(v5-15 (A) 와 같다 — (A) 가 기본값이 됐을 뿐 동작 동일) ·
    S4 는 **20~21회 / 상한 61** 로 수렴한다 ⟹ **(A) 자체는 v5-15 대로 작동한다.**
 ★ **교차의 «자리»는 이 판이 세지 않았다** — `minPairDist` 는 **최악 한 쌍**만 돌려준다(패널 이름은
   세 칸 모두 `front↔back`). 교차 529개의 분포(어느 행·어느 열·어느 패널 쌍)는 **미측정**이고
   계기를 한 줄 더 넣으면 난다(다음 판 항목 · 추정 0).
```

## §1-② 자기검사 진단 경로 — **구현 · off 비트 불변 통과**

```
 diff(원문 요지) — `assemble` 뒤 δ 보정 루프의 판정 한 줄을 갈랐다:
   `if (ASM2) { const mpA = minPairDist(sc0.s.pos, sc0.tris, SEP * 2); … if (mpA.hits === 0 && mpA.min > 0)
    { ok = true; break; } } else if (m0 >= need) { ok = true; break; }`
   · 재는 함수는 **이미 있는 `minPairDist`**(새 계기 0) · 창 `SEP * 2` 는 실패 문언이 이미 쓰는 그 창 ·
   · `min > 0` 은 **문턱이 아니라 «같은 점이 아니다»**(부동소수에서 0 = 겹침) ·
   · 실패 문언도 `asm2` 경로에서 **「옷 자기 «교차» — 교차 N개 · 최소쌍 …」** 으로 갈라 **그 수를 드러낸다**
     (이 판의 ① 값이 그 문언에서 나왔다) · **`asm2` off 면 분기 밖**이다.
 ★ **off 비트 불변 — A포즈 29 / 29 동일 · T포즈 108 / 108 동일**(기준선 = v5-12 에서 «커밋된 v5-11
   코드»로 뜬 표 · 같은 계기 `scripts/v5AsmSha.ts` · 던지는 **26칸의 문언까지 동일**) ⟹ **갈래 D 아님**.
   ★ 이것으로 비트 불변은 **v5-12 · v5-14 · v5-16 세 판 연속 통과**다.
 ★ **(A) 를 기본값으로 올렸다**(판정문 「(A) 채택」) — `asm2` 가 켜지면 `asm2Fix` 기본이 **`'A'`** 다 ·
   `'B'`(표면 추종)는 **보류**로 코드에 남긴다(삭제 0).
```

## §1-③④⑤ — **착수 0**(사전 등재 (나))

```
 R3 굽기 3칸 · 「같은 옷」 대조 · 종합 = **전부 착수 0** — **굽기 0칸 · 2호기 접촉 0 · 캡처 0장**.
 게이트 5채널 · 어깨 걸림 · 겨드랑이 · 링 궤적 · 층3 5행 · `RAMP_N` 실측 · 위반 수 궤적은 **미집행**이고
 사유는 ①(나) 한 줄이다. ⟹ **2세대 첫 굽기는 다음 판으로 넘어간다.**
```

## §2 자기검사

| 항 | 약정 | 실측 |
|---|---|---|
| 기점 | `98803dc`(v5-15 HEAD) | ✅ 등재 |
| 물리 0줄 | | ✅ `gpu/` diff **0** · 굽기 **0칸** |
| `src/` diff | 하위 플래그 정리 + 진단 완화 경로 · off 비트 불변 | ✅ **A 29/29 · T 108/108 동일**(3판 연속) |
| 굽기 / 캡처 | 3칸 / 3칸 | **0 / 0**(사전 등재 (나)) |
| ecc | | **0 · 0** |
| §0 선커밋 | | ✅ `bd40efe` |
| 갈래 | 사후 신설 0 | ✅ ① **(나)** · ② 통과 · ③④⑤ 착수 0 |
| 문턱 | 신설 0 · 이동 0 | ✅ 완화는 **«자의 대상» 정정**(판정문)이고 `min > 0` 은 문턱이 아니다 |

**자기신고 2건**
```
 1 ★ **CC 귀책 — `hits` 를 네 판 동안 읽지 않았다.** `minPairDist` 는 v5-12 부터 쓰던 함수이고
   `hits`(교차 수)를 **이미 돌려주고 있었다**. v5-12~15 는 `min` 만 인쇄해 「거리 ~0」으로 적었고,
   그래서 「근접인가 교차인가」가 **네 판 동안 미확인**으로 남았다(판정문의 전제 오류에도 기여했다).
   ⟹ 교훈 = **계기가 이미 주는 값을 전부 인쇄한다**(함정 13·18 계열 · 「대역 요약에 중앙값 단독 금지」와 같은 방향).
 2 **교차의 «자리·분포»는 미측정**이다(최악 한 쌍만 안다) — 다음 판 항목으로 등재.
```

## §4 갈래 판정 — **C**

```
 §0-3 등재 — 「**C** = ①(나) ⟹ 사실 상신」
 ① = **(나) 교차 > 0** — **529 / 446 / 512 개**(세 칸 · 최소쌍 패널 전부 `front↔back`)
 ② = **통과**(진단 경로 구현 · off 비트 불변 **A 29/29 · T 108/108** = 3판 연속)
 ③④⑤ = **착수 0**
 ⟹ **C**. ★ **D 가 아니다**(비트 불변 유지) · **A·B 가 아니다**(굽기를 착수하지 않았다).
 ★★★ 이 판의 산출 = **판정문 전제의 기각**(「근접」이 아니라 **교차 수백 개**)과 **그 수를 처음 센 것**.
   ⟹ 2세대 배치는 **아직 «면이 서로 통과하지 않는» 상태가 아니다** — 그 사실이 굽기 «전»에 잡혔다.
```
