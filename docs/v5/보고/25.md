# v5-25 보고 — 목선 채널 게이트에서 내림 · S 던짐 원인 · 회귀 굽기 2칸 · **갈래 B**

git 스탬프 — 브랜치 `v5-25-neck-retire` · 기점 `ce22e0c`(v5-24 HEAD) ·
워크트리 `/Users/banshk/projects/fit-simulator` · 커밋 §0 `c1c067b` · 구현 `c3c7ea9` · 계기 `e8b1c4d` ·
§1-①② `3b878a7` · §1-④ `b50cd9f` · §1-③·§2·§4+캡처 `39f59df` · metrics+볼트 `0d9ac03`.
`src/` diff = **세 파일 24+/11−** · 물리 0줄 · **처방 0** · 굽기 **2칸**(2호기 도달 회복) · 캡처 **4장**.
이 판에서 호출한 ecc 에이전트 = **0** · 스킬 = **0**.

## 1. 갈래와 사유

**갈래 B**(③ fail ⟹ 값 상신). 사유 한 줄 — ①은 편입 변동 **0** 으로 통과했고 ③에서 `supima-L` 은 **pass** 했으나
`기본 M` 이 **`자기관통 교차 1 ≠ 0`** 하나로 떨어졌다.

A 아님(③에서 M fail) · **C 아님**(① 변동 0) · **D 아님**(조립 sha T 108/108 · A 29/29 동일) · E 아님.
사후 갈래 신설 0.

## 2. §1 항목별 판정과 그 판정을 만든 수 전량

### ① 목선 채널 내림 — 통과

`s4Gate.ts` 의 판정 줄 **하나를 지웠다**(값·문턱은 남긴다):

```ts
-  if (!(ringRestRatio <= S4_THRESHOLD.ringRestMax))
-    fails.push(`목선 휴지비 … > 1.1 (링 …cm / 휴지 …cm · 옛 초과비 …)`);
+  // if (!(ringRestRatio <= S4_THRESHOLD.ringRestMax)) fails.push(`목선 휴지비 …`);   ← v5-25 판정 제외
```

⟹ **게이트 판정 채널은 넷**이다: ③a 관통 ≤ 0.5 mm · 자기관통 교차 0 · 정착 창 순변위 ≤ 0.1 mm · pinned 0.
두 비율(`ringExcess`·`ringRestRatio`)과 딸린 값(`ringM`·`ringBodyM`·`ringAllowM`)은 **계속 산출·인쇄**하고,
문턱 `ringExcess: 1`·`ringRestMax: 1.1` 도 **지우지 않았다** ⟹ 되돌림은 그 한 줄을 되쓰는 것이다.
사유 셋을 코드 주석에 값으로 남겼다 — ±5 mm 뒤집힘(1.002145→0.999151 · 1.021083→0.991968) ·
1.10 은 v3-40 이 「어림수」로 기각한 수 · 옛 자는 정본 37칸에서 **0건**(최대 0.998283).

**정본 재판정 — 편입 변동 0**(계기 `scripts/v5Rejudge.ts` · 굽기 0)

| 항목 | 값 |
|---|---|
| 재판정 칸 | **37** |
| 건너뜀 | **71**(정본 정착 blob 없음 **69** = 판정 대조 69칸 · 원리적 불가 + `blob n ≠ 장면 n` **2**) |
| 옛 pass | **37 / 37** |
| **새 pass** | **37 / 37** |
| **변동 칸** | **0** |
| 휴지비 분포(보고 값) | 최소 1.059141 · p25 1.074616 · 중앙 1.088685 · p75 1.105991 · 최대 1.173295 |

★ §0-5ㄱ 에 값 보기 **전** 적은 구조적 예측이 그대로 맞았다 — 채널을 내리는 것은 `fails` 에서 한 항목을 빼는
일이라 `pass → fail` 은 **원리적으로 불가능**하고, `fail → pass` 는 「목선 단독」 실패 칸에서만 나는데
v5-24 가 그 칸을 **0개**로 쟀다 ⟹ **변동 0 이 예측**이었고 실측이 같다.
★ **한정** — A포즈 29칸은 정본 정착 blob 이 T포즈 계열뿐이라 **재판정 불가**다. 판정문의 검증 조건
「옛 29칸 편입 변동 0」은 **값으로 확인하지 못했다**(구조적 논거는 칸에 무관하다).

### ② S 던짐의 자리와 원인 — ★★★ **앵커다**

플래그를 두 줄로 갈라(`asm1x: boolean | 'anchor' | 'drop'` · `true` 면 둘 다 = v5-22~24 와 항등) 네 판본을 f0 로 쟀다.

| 판본 | `n` | 제도 분할 | 던짐 | 최소쌍 mm | 교차 | 최악 쌍 |
|---|---|---|---|---|---|---|
| **off** | 8354 | `N_sh` 15 · `N_nk` 22 · `N_side` 48 · `nuB` 52 · `nvB` 73 | 없음 | **2.2616** | 0 | `front↔back`(정상) |
| **앵커만** | 8354 | (off 와 같다 — 앵커는 제도를 안 바꾼다) | **던짐** | **0.08915** | **6** | **`back↔sleeveL`** |
| **`SH_DROP`만** | 8148 | `N_sh` **17** · `N_side` **41** · `nuB` **56** · `nvB` **66** | **없음** | **2.25036** | 0 | `front↔back`(정상) |
| 둘 다 | 8148 | (drop 과 같다) | 던짐 | **1.39419** | 0 | `back↔back` |

**자리**(격자 해독 — 형제 판본이 보고한 제도 분할로 폭을 확정했다 · 추측 0)

```
 앵커만 (nuB 52 · nvB 73)
   A 14976 : back(**i0**, j71) y1298.5 · back(i1, j71) y1298.5 · back(**i0**, j72 = nvB−1) y1307.2
   B 15929 : sleeveL(off 176) y1275.8 · sleeveL(off 227) y1303.0 · sleeveL(off 226) y1297.7
   붙는 높이 **1275.792 ~ 1307.159 mm** = `Y_NECK`(1315.769) 아래 **8.6 ~ 40.0 mm**
   ★ `i0` = 패널 가장자리 열 = **암홀 봉제선**(`dEdge` 0) ⟹ **몸판 암홀 가장자리가 소매와 겹친다.**

 둘 다 (nuB 56 · nvB 66)
   A 14716 : back(i22, j65 = nvB−1) · back(i23, j65) · back(i22, j66 = nvB)
   B 14719 : back(i24, j65) · back(i24, j66) · back(i23, j66)
   붙는 높이 **1264.860 ~ 1288.118 mm** · `dEdge` = min(22, 34) = **22 > N_sh 17** ⟹ **목선 구간**
```

★★★ `SH_DROP` **만** 켜면 **통과한다**(2.25036 · 최악 쌍이 off 와 같은 `front↔back`) ·
앵커 **만** 켜면 **가장 심하게 던진다**(0.08915 mm · **교차 6개**) ⟹ **던짐의 원인은 앵커 정정**이다.
★★★ 그리고 **`SH_DROP` 이 앵커의 피해를 덜어준다** — 둘 다면 0.08915 → **1.39419**(15.6배 완화) ·
**교차 6 → 0** · 자리가 **암홀 가장자리 → 목선 구간**으로 옮긴다.
★ 「off 에서는 왜 안 겹치는가」 — off 는 옷을 `Y_TOP`(1238.046)에 매달아 맨 윗줄이 몸의 목 대역에 들지 않고,
최악 쌍도 허리 아래(1116.720 ~ 1125.475 mm)의 정상 `front↔back` 이다.
★ **처방 0**(프롬프트 「사실만」) · `anchor`/`drop` 은 **진단용**이고 정본 후보는 `asm1x: true` 다.

### ③ 회귀 굽기 — `supima-L` **pass** · `기본 M` **fail**

굽기 = 2호기(DESKTOP-OV72A46 · GTX 1660 Ti · `arch=cuda` · f64) · `frames` 400 · `ramp` ·
총 벽시계 **1023.4 s** · 실패 **0칸**.
★ 조립이 v5-22 와 **비트 동일**이라 자산을 재사용하고 **게이트 한 파일만** 보냈다 —
물리 재현이 값으로 보인다(`vs 정본 중앙` **68.930630 / 70.765870 mm** = v5-22 와 같은 수 · 정착 f190 둘 다).

| 채널 | 문턱 | `supima-L` | `기본 M` |
|---|---|---|---|
| ③a 관통 최대 mm | ≤ 0.5 | **0.33132481902223526** ✅ | **0.3752726257434258** ✅ |
| 관통 정점 수 | 참고 | 881 | 927 |
| **자기관통 교차** | = 0 | **0** ✅ | **1** ❌ |
| 최소 쌍거리 mm | 참고 | 1.970646652526943 | 1.9798624570389312 |
| 정착 창 순변위 mm | ≤ 0.1 | **0.07229682189324102** ✅ | **0.03930519393313997** ✅ |
| pinned | = 0 | **0** ✅ | **0** ✅ |
| 봉제 간극 중앙 / 최대 mm | 참고 | 2.0903705401250483 / 3.8480564011988636 | 2.107906669570362 / 4.008345064334165 |
| λ 최대 | 참고 | 1.4609440147599382 | 1.4559966679005811 |
| **`pass`** | — | **true** | **false** |
| `fails` | — | **`[]`** | **`['자기관통 교차 1 ≠ 0']`** |

**목선 두 비율 — 보고 값(판정 제외 확인)**

| 칸 | 옛 초과비 | 링 cm | 허용 cm | 새 휴지비 |
|---|---|---|---|---|
| `supima-L` | **1.0021454460806467** | 49.488944768371766 | 49.38299621269665 | 1.0427966483091002 |
| `기본 M` | **1.0210830840101046** | 49.42449231916582 | 48.40398699492775 | 1.037657 |

★★★ **v5-22 에서 «목선 하나»로 떨어졌던 `supima-L` 이 통과했다.** 같은 옷·같은 물리에 **판정만** 바뀌었고,
두 비율은 여전히 1 을 넘지만(1.0021 · 1.0211) 판정에 들어가지 않는다 ⟹ 채널 내림이 뜻대로 작동한다.
★ `기본 M` 은 **교차 1 하나**가 남는다 — v5-22 에도 있던 그 한 개이고 **겨드랑이 대역 밖**이다
(v5-22 §1-②ㅁ: 대역 교차 0 ↔ 전체 1). **자리는 아직 미특정**이다.

**층3 5행(중앙 mm) · `pressN` 전부 0**

| 칸 | 목선 | 가슴 | 허리 | 밑단 | 소매 |
|---|---|---|---|---|---|
| `supima-L` | 3.6944 | 4.0668 | 69.2904 | 45.1043 | 6.0533 |
| `기본 M` | 2.2718 | 3.7102 | 61.7286 | 25.7186 | 6.4275 |

★ 판정문이 목선 결함 포착을 분담시킨 **층3 눌림 채널은 이 두 칸에서 0** 이다(`pressN` 0 / 0).

**어깨 걸림(정착) · 옷 y 극값**

| 칸 | `Y_TOP` mm | **어깨선 위 옷 정점** | 옷 최고점 mm | 옷 밑단 mm |
|---|---|---|---|---|
| `supima-L` | 1393.4802 | **1002** | 1468.378 | 783.411 |
| `기본 M` | 1403.9385 | **832** | 1471.023 | 820.494 |

편입 규약 **172** 를 두 칸 다 크게 넘는다(v5-22 와 같은 값 — 물리 재현).

**캡처 4장** — `docs/v5/캡처/25-{L,M}-{1-front,2-side}.png` · 화면 판정 0. ★ **S 는 보류 · 캡처 0.**

### ④ 승격 재료 — 판단 0

★ **108 순서에서 한 겹이 빠진다** — ①이 변동 0 을 보였으므로 재판정 단계가 **«확인»으로 줄어든다**:
**① 채널 내림 확정 → ② 37칸 변동 0 확인(완료) → ③ A포즈 29칸 재굽기 → ④ 제공 재발행**.
69칸(판정 대조)은 blob 이 없어 값은 못 보지만 **판정이 나빠질 수 없다**(채널이 사라졌을 뿐).
★ **v5-24 의 「편입 11칸 감소」는 취소됐다** — 재발행은 편입 집합을 바꾸지 않고, 바뀌는 것은 산출의 «채널 목록»이다
(`fails` 에서 목선 줄이 사라지고 두 비율이 `채널` 에 값으로 남는다) ⟹ 판본 접미를 새로 끊고 옛 판본 보존.
★ **v3 동결 예외 5건째를 «교체»에서 «내림»으로 정정** — ① `s4Gate.ts` 판정 **한 줄 제거**(판정 채널 5 → 4) ·
② 게이트는 모든 굽기의 판정이라 플래그 뒤에 둘 수 없다 · ③ 되돌림 = 그 한 줄 되쓰기(문턱 보존 ⟹ 복원 0).
6건째 후보 = `asm1x` 분리 플래그(진단용 · `true` 면 항등)이고 등재 여부는 전략 세션 판단.
★ **S 보류 규약(초안)** — 「조립 자기검사가 던지는 칸은 **그 칸만 «보류»**로 등재하고 나머지 승격을 막지 않는다 ·
보류 칸은 제공 목록에 «미제공»으로 표기하고 사유(던짐 문언 원문)를 함께 적는다.」
★ 원인이 앵커로 특정됐으므로 보류는 «영구»가 아니라 **다음 판의 표적**이다. **108 중 던지는 칸의 전수는 세지 않았다**(미집행).

### 갈래 D 의 자 — 조립 sha 불변 **통과**

기점 `ce22e0c` 워크트리 대조 · 같은 계기 `scripts/v5AsmSha.ts` ·
**T 포즈 108 / 108 동일**(던지는 **26칸의 문언까지**) · **A 포즈 29 / 29 동일**(던짐 0).
★ `asm1x` 타입을 넓혔는데도 불변이다 — `true` 면 두 자리가 함께 켜져 항등이고, 키가 없으면 분기가 죽는다.

## 3. 사전 등재 기대 ↔ 실측 대조

| 판정문의 지시/검증 조건 | 실측 | 판정 |
|---|---|---|
| 목선 채널을 게이트 판정에서 내린다 | 판정 채널 5 → **4** · 두 비율 보고 값 병기 | **성립** |
| 옛 편입 변동 **0** 이 검증 조건 | 37칸 **37 pass → 37 pass · 변동 0** | **성립**(A29 는 미확인) |
| 조립 sha 불변 | T 108/108 · A 29/29 동일 | **성립** |
| 결함 포착은 ③a·층3 눌림·어깨 걸림이 분담 | ③a 0.331 / 0.375 통과 · `pressN` **0 / 0** · 어깨 1002 / 832 | **③a·어깨는 값 있음 · 눌림은 0** |
| S 던짐은 «자리부터» | `back(i0)↔sleeveL`(앵커만) · 목선 구간(둘 다) · **원인 = 앵커** | **성립 + 원인 가름** |
| ③ 3칸 굽기 | **2칸**(L pass · M fail) · S 보류 | **부분** |

## 4. 기각·정정된 것과 근거 수

```
 ㉠ ★★★ **「`SH_DROP` 이산화」 진단의 잔여가 정리됐다** — v5-24 가 `N_sh` 세 방향을 반증했고, 이 판이
   **원인은 `SH_DROP` 이 아니라 앵커**임을 갈랐다(`SH_DROP` 만 = 통과 2.25036 · 앵커만 = 0.08915 · 교차 6).
   오히려 `SH_DROP` 은 **완화 요인**이다(15.6배 · 교차 6 → 0).
 ㉡ ★★ **v5-24 의 「편입 11칸 감소」 취소** — 채널을 내렸으므로 감소가 없다(변동 0).
 ㉢ ★ **동결 예외 5건째의 성격 정정** — 「교체」가 아니라 «내림»이다(교체판은 한 판만 살았다).
 ㉣ ★ **목선 두 비율이 1 을 넘어도 통과한다** — `supima-L` 초과비 1.0021 · 휴지비 1.0428 로 `pass true`.
```

## 5. 자기신고 — 절차 이탈 · 한정 · 미집행 · 귀책

```
 ★ **CC 귀책 0**(이 판). 절차 이탈 0.
 ★ **미집행 1 — A포즈 29칸 재판정**(정본 정착 blob 이 T포즈 계열뿐) ⟹ 판정문의 검증 조건을 **37칸으로** 확인했다.
 ★ **미집행 2 — ③의 세 번째 칸(S)**. `asm1x: true` 에서 조립이 던지므로 굽지 않았다 ·
   진단용 `anchor`/`drop` 판본으로는 **굽지 않았다**(정본 후보가 아니다 · §0-5ㄷ 사전 등재).
 ★ **미집행 3 — `기본 M` 의 교차 1개 «자리»**. 겨드랑이 대역 밖이라는 것만 안다(v5-22 실측).
 ★ **미집행 4 — 108 중 던지는 칸 전수**(S 보류 규약이 몇 칸에 걸리는지 모른다).
 ★ **한정 — 굽기가 자산 재사용이다.** 조립을 다시 반출해 sha 를 비교한 것이 아니라, `asm1x: true` 경로가
   플래그 분리에서 **항등**이라는 코드 사실 + `vs 정본`·`RAMP_N`·정착 프레임이 v5-22 와 **같은 수**라는
   실측으로 뒷받침했다.
 ★ **한정 — 캡처가 v5-22 것과 바이트 크기까지 같다**(114162 / 82986 / 112523 / 81387). 같은 정착 상태를
   같은 렌더러로 그렸으니 그렇게 나오는 것이 맞고, 새로 그려 자리를 `25-*` 로 갈랐다.
 ★ **문턱 신설·이동 0** — 이 판은 문턱을 **하나도 쓰지 않았다**(내렸다). 남은 넷은 v3 누적 등재값 그대로다.
 ★ **집행 환경** — `git` 이 Xcode 라이선스 미동의로 멈춰 `DEVELOPER_DIR=/Library/Developer/CommandLineTools`
   로 우회했다(v5-23 부터 세 판 연속 · 기계 설정·저장소 파일 변경 0).
 ★ **물리 0줄 · 처방 0 · ecc 0 · 스킬 0.**
```

### §0 사무 — 귀책 21건째 · 백로그

```
 ★ **전략 세션 귀책 21건째** 등재 — 판정문 원문 「휴지 1.10 = v3-40 기각 어림수를 되살림」.
   v5-24 발주문이 그 수를 「새 수 0」의 근거로 삼았으나 `s4Gate.ts:51-52` 는 그것을 **v3-40 이 기각한 어림수**로
   적고 있었다. 그 자로 재판정하니 37칸 중 11칸이 흔들렸고, 이 판이 **두 형태 모두 판정에서 내렸다**.
 ★ **백로그 등재** — 「**목선 과신장 채널은 «천 신장 한계 상수»가 생길 때 재정의한다**」(볼트 백로그 절 첫 행).
```

## 6. 다음 판 설계에 필요한 새 사실

```
 ㉠ ★★★ **108 승격의 남은 벽은 «교차 1개»다** — `기본 M` 의 `fails` 가 그 하나뿐이다(`자기관통 교차 1 ≠ 0`).
   자리는 겨드랑이 대역 밖이고 **미특정**이다 ⟹ 그 하나를 집는 것이 다음 표적이다.
 ㉡ ★★★ **S 던짐의 원인은 앵커이고, 그 기전은 «암홀 가장자리 ↔ 소매»다** —
   앵커가 맨 윗줄을 `Y_NECK` 대역으로 올리면 `i0`(암홀 봉제선) 열이 소매와 만난다(교차 6 · 0.08915 mm).
   `SH_DROP` 이 그것을 덜어주지만 목선 구간에 1.39419 mm 를 남긴다.
 ㉢ ★★ **목선 두 비율이 «보고 값»으로 남았다** — 1 을 넘어도 통과한다. 그 값을 언제 다시 판정으로 올릴지는
   백로그의 「천 신장 한계 상수」에 달렸다.
 ㉣ ★ **층3 눌림은 이 두 칸에서 0** 이다(`pressN` 0 / 0) — 판정문이 목선 포착을 분담시킨 셋 중 이 채널은
   지금 아무것도 잡지 않는다. 분담이 실제로 성립하는지는 더 큰 표본이 필요하다.
 ㉤ ★ **A포즈 29칸 재판정은 굽기 한 판이 필요하다**(정본 정착 blob 이 T포즈 계열뿐).
 ㉥ ★ **108 중 던지는 칸 전수**가 S 보류 규약의 규모를 정한다(미집계).
```

## 부록 — 캡처와 raw 링크

캡처 **4장**(2칸 × 앞·옆 · `v4AposeRender.ts` 정사영 오프스크린 · 화면 판정 0) —
`docs/v5/캡처/25-L-1-front.png` · `25-L-2-side.png` · `25-M-1-front.png` · `25-M-2-side.png`.
★ `작은몸 S` 는 조립이 던져 **캡처 0**.

- 회차 노트 `docs/v5/25-목선내림.md`
- 보고 `docs/v5/보고/25.md`
- metrics `docs/metrics-log.md`(2026-09-18 v5-25 블록)
- 볼트 `.obsidian-log/전략세션-인수인계.md`(v5 트랙 교체 + 전략 세션 귀책 **21행** + 백로그 첫 행)
- 색인 `docs/v5/v5-색인.md`
- 코드 `src/v3/s4Gate.ts`·`src/v3/garmentScene.ts`·`src/v3/dressRun.ts` ·
  계기 `scripts/v5Rejudge.ts`·`scripts/v5SThrow.ts` · 잡 `gpu/bake/jobs/v5-25-on.json`

━━━ 👉 다음: 캡처 4장을 업로드하세요 (전략 세션은 raw URL 로 이미지를 못 읽습니다) ━━━
