# v5-14 보고 — 어깨선 국소 좌표계(`t × n` 벌림 · S3 국소 낙하) · 사실 검증

git 스탬프 — 브랜치 **`v5-14-local-frame`** · 기점 **`4934876`**(v5-13 HEAD) · §0 선커밋 **`501495c`** ·
사무 **`010ee53`** · 노트 **`f3a017b`** · metrics **`47aee30`** · 볼트 **`774b524`** ·
워크트리 `/Users/banshk/projects/fit-simulator`. 집행 기계 — **에어 M4(맥)**: 물리 0프레임 ·
**2호기 접촉 0 · 굽기 0칸 · 캡처 0장**. **ecc 에이전트 0 · 스킬 0.**

## 1. 갈래 — **C**

**C = ①(나) ⟹ 사실 상신 · ② 착수 0.** ①이 §0-4ㄴ 의 자(값 보기 «전» 등재)를 **한 칸도** 만족하지
못했다. 그래서 `b = t × n` 벌림·국소 낙하를 **구현하지 않았다**(형식화는 초안으로 남김 · 사후 확대 0).
★ **D 가 아니다**(비트 불변 유지) · **B 가 아니다**(②를 착수하지 않았다).
★★★ 이 판이 낸 것은 두 가지다 — **판정문의 원인·처방이 값으로 기각**되고, **진짜 원인이 특정**됐다.

## 2. §1 항목별 — 값 전량

### ⓞ 사무 2건 — 집행 완료

**ㄱ 설계서에 「닫힌 = 간극 0」 반영**(집행 전 `grep` 으로 **0건** 확인) — `docs/v5/설계-조립2세대.md`
의 **S2 절**을 교체했다: 「이미 닫힌」은 **거리 0 이 아니라 «거리 = rest = `SEP`»(간극 0)** 이고,
근거(`garmentScene.ts` 「봉제」 절 원문 「rest를 0으로 두면 자기충돌(분리 2×두께)과 «서로 싸운다» …
rest = 2×두께가 옳고」)와 실측(`front↔back 0.000mm` 던짐)을 달았다. **채널 기대값도 정정**했다
(「봉제쌍 거리 0」 → **「거리 ≈ `SEP`(2 mm) · 간극 ≈ 0」**). v5-10 의 「0.0000 mm」가 **자기검사 밖
가상값**이었음을 병기했다.
**ㄴ `CLAUDE.md` 에 「구현 중간 산출 즉시 커밋」 등재**(집행 전 **0건** 확인) — v5-12 §1-① 사고 3
(`src/` 를 기점 판본으로 되돌려 정정 2건이 덮였다)을 근거로 달고 「작업트리를 건드리는 명령 앞에서
`git status --porcelain` 을 먼저 보고, 미커밋분은 커밋하거나 `git stash` 로 격리한다」를 적었다.

### ① ㉡ 사실 — **(나) 불성립** · 판정문의 원인·처방 **기각**

계기 = `redrapeAsm2` 의 **인쇄 훅**에 «행별 추적»을 더했다(ASM2 전용 · 동작 0) —
같은 `i` 의 앞뒤 간격을 `j = nvB` 에서 아래로 훑어 **`SEP` 밑으로 가는 첫 `j`**, 그 `i` 의
**능선 기울기 `|t·ŷ|`**, **`n`↔`ẑ` 각**, 실패 자리의 **같은행/대각 거리와 행 간격**을 낸다.

**㉠ 상단 행은 완벽하다** — 세 칸 · **모든 `i`** 에서 **상단 간격 = 2.0000 mm**(= `SEP` 정확히)
⟹ `±SEP/2` **전역 `z` 벌림은 상단 행에서 의도대로 작동한다**. 벌림 «크기»는 문제가 아니다.

**㉡ ★★★ `n` 이 `ẑ` 와 수직이다 ⟹ `t × n ≈ ±ẑ` ⟹ 처방이 무효다**

| 칸 | `n`↔`ẑ` 각(좌 · 어깨 토막 전체) |
|---|---|
| `c100-h170-s45_M` | 89.57 · 92.79 · 91.47 · 92.88 · 90.39 · 90.09 · 89.03 · **90.00 × 9** |
| `mst-over-L` | 89.73 · **90.00 × 20** |
| `supima-L` | 91.04 · **90.00** · 93.29 · **90.00 × 14** |

산수 — `n ⊥ ẑ` 이면 `n = (n_x, n_y, 0)` 이고 능선 접선 `t` 도 z 성분이 거의 0 이므로
`t × n = (t_y·0 − 0·n_y, 0·n_x − t_x·0, t_x n_y − t_y n_x) = (0, 0, ·)` ⟹ **`b ∥ ẑ`**.
⟹ 판정문의 처방(「`b = t × n` 으로 ±`SEP`/2 벌림」)은 이 기하에서 **전역 `z` 벌림과 «같은 축»**이다
⟹ **구현했어도 아무것도 바뀌지 않는다**(무효 처방). 그리고 판정문의 원인 문언(「전역 z 벌림이
기울어진 능선에서 면에 수직이 아님」)도 **같은 이유로 기각**된다 — `n ⊥ ẑ` 이므로 `z` 벌림은
**이미 앞뒤를 가르는 방향**이다.

**㉢ 「기울기 클수록 먼저 교차」 — 상관 없음**(§0-4ㄴ 기준 미달 ⟹ (나))

| 칸 | `\|t·ŷ\|` 범위 | `SEP` 밑 첫 `j` 의 `(nvB−j)` | 단조 |
|---|---|---|---|
| `c100-h170-s45_M` | 0.1857 ~ 1.0000 | 0 · 14 · 12 · 12 · 1 · 13 · −1 · −1 · −1 · 2 · 3 · 2 · −1 · 2 · 14 · −1 | **없음** |
| `mst-over-L` | 0.1436 ~ 1.0000 | 14 · −1 · −1 · 13 · −1 · 13 · 2 · −1 · 4 · 3 · 4 · −1 · 3 · −1 · −1 · −1 · −1 · 13 · −1 · −1 · −1 | **없음** |
| `supima-L` | 0.2004 ~ 0.3773 | 13 · 14 · 12 · 3 · −1 · −1 · −1 · −1 · 4 · −1 · −1 · 3 · 3 · 13 · −1 · 12 · −1 | **없음** |

(`−1` = 그 `i` 는 끝까지 `SEP` 밑으로 안 간다.) §0-4ㄴ 의 자 = 「기울기가 큰 `i` 에서 **더 높은 `j`**
에서 먼저 내려간다」가 **세 칸 중 둘 이상**에서 단조 — **한 칸도 성립하지 않는다**. 특히 기울기가
**최대(1.0000)** 인 `i = 0` 이 **세 칸 다 `−1`**(아예 안 내려간다)이다. ⟹ **①(나).**

**㉣ ★★★ 진짜 원인 — S4 한 걸음이 «한 행»을 건너뛴다**

| 칸 | `i` | 첫 `j` | **같은행 mm** | **대각 mm** | **행 간격 mm** |
|---|---|---|---|---|---|
| `c100-h170-s45_M` | 15 | 69(=nvB) | 2.0000 | 6.2129 | — |
| | 14 | 55 | 2.1500 | **1.5387** | **1.0325** |
| | 13 | 57 | 1.9787 | 1.8927 | 25.3965 |
| | 12 | 57 | 2.0048 | 1.9551 | 21.8753 |
| | 10 | 56 | 2.0149 | **1.4688** | **1.7870** |
| `mst-over-L` | 20 | 62 | 2.0446 | **1.0486** | **1.0945** |
| | 17 | 63 | 2.0636 | **1.7301** | **1.5381** |
| | 15 | 63 | 2.0211 | **1.6132** | **2.7080** |
| `supima-L` | 15 | 59 | 1.9786 | **1.1957** | **0.8706** |
| | 13 | 70 | 4.0554 | **0.4353** | **2.2199** |
| | 16 | 60 | 1.8725 | 3.0021 | 23.5523 |

㉠ **같은 행 간격은 실패 자리에서도 유지된다** — **1.8169 ~ 4.0554 mm**(≈ `SEP`) ⟹ **벌림은 산다**.
㉡ **실패는 «대각»에서 난다** — **0.4353 ~ 1.9551 mm** ⟹ 앞판의 한 정점이 뒤판의 **이웃 행**과 붙는다.
㉢ ★ **그 자리의 «행 간격»이 붕괴해 있다** — **0.8706 · 1.0325 · 1.0945 · 1.5381 · 1.7870 · 2.2199 ·
2.7080 mm**(정상 = `L / nvB` ≈ **9.9 mm**) ⟹ 한 열의 두 이웃 행이 **1 mm 안으로** 모였다.
㉣ ★★★ **S4 의 한 걸음이 그만큼이다** — S4 최대이동 **첫 회 10.2291 mm**(세 칸 동일)이고 행 간격이
**≈9.9 mm** 다 ⟹ **밀어냄 한 번이 «한 행»을 통째로 건너뛸 수 있다.** 그래서 이웃 행이 서로 지나쳐
앞뒤가 «행 사이»로 엇갈린다(v5-13 §1-③㉡ 이 특정한 그 자리).
★ 보조 — 「`SEP` 밑」으로 잡힌 자리 일부는 같은행이 **1.95~2.00 mm** 인 «경계 걸림»이다(`SEP` = 2.0 mm
바로 밑 · 수치 경계) ⟹ **실질 실패는 행 간격이 붕괴한 자리**다(위 굵은 값).

### ② 국소 좌표계 구현 — **착수 0**

§0-3 이 「C = ①(나) ⟹ ② 착수 0」을 걸었고 ①이 (나)로 나왔다 ⟹ **구현하지 않았다.** §0-4ㄱ 의
형식화(`b = normalize(t × n)` · S3 국소 낙하 `d = normalize(−ŷ − (−ŷ·n)n)` · 평행 시 전역 `−y` 가드)는
**초안으로 남긴다**. ★ 그리고 ㉡ 가 그 처방이 **이 기하에서 무효**임을 값으로 보였다.

### ③ off 비트 불변 — **통과**(v5-13 한정 **해소**)

**A포즈 29 / 29 동일** · **T포즈 108 / 108 동일**(기준선 = v5-12 에서 «커밋된 v5-11 코드»로 뜬 표 ·
같은 계기 `scripts/v5AsmSha.ts` · 줄 단위 대조 · 던지는 **26칸의 문언까지 동일**)
⟹ v5-13 §1-④ 의 한정(「T포즈 108칸 미재확인」)이 **이 판에서 해소**됐다. 이 판의 `src/` diff 는
**전부 `redrapeAsm2` 안의 인쇄 훅**(플래그 off 면 호출되지 않는 함수)이다.

### ④⑤ R3 진단 굽기 · 종합 — **착수 0**

②가 착수 0 이므로 **굽기 0칸 · 2호기 접촉 0 · 캡처 0장**. 게이트 · 어깨 걸림(정도) · 링 · 층3 5행 ·
`RAMP_N` 실측(예측 ≈52/42 대조) · v4-17 「같은 옷」 ①② 대조는 **전부 미집행**이고 사유는 이 한 줄이다.

## 3. 사전 등재 기대 ↔ 실측 대조

| 등재 | 기대 | 실측 | 판정 |
|---|---|---|---|
| ① 「기울기 클수록 먼저 교차」 | 세 칸 중 둘 이상 단조 | **한 칸도 없음**(기울기 최대 `i=0` 이 세 칸 다 −1) | **(나)** |
| 판정문 원인 「z 벌림이 면에 수직이 아님」 | 그렇다 | **`n`↔`ẑ` ≈ 90.00°** ⟹ z 벌림이 이미 앞뒤를 가른다 | **기각** |
| 판정문 처방 `b = t × n` | 다른 축이 나온다 | **`b ∥ ẑ`**(산수 · `n`, `t` 의 z 성분 ≈ 0) | **무효 처방** |
| 상단 벌림이 작동하는가 | — | **2.0000 mm** 정확히(세 칸 · 전 `i`) | **작동** |
| ③ off 비트 불변 | 동일 | **A 29/29 · T 108/108** | **통과** |

## 4. 기각·정정된 것

- ★★★ **판정문의 원인 기각** — 「전역 z 벌림이 기울어진 능선에서 면에 수직이 아님」. 근거 = `n`↔`ẑ`
  **89.03~93.29°**(대부분 90.00°) · 상단 간격 **2.0000 mm**.
- ★★★ **판정문의 처방 무효** — `b = t × n` 이 이 기하에서 **`ẑ` 와 같은 축**이다(산수 + 각 실측).
- **①의 상관 가설 기각** — 기울기와 교차 시점 사이에 순위 관계가 **없다**.
- ★ **원인 교체(값 근거)** — 「벌림 방향」이 아니라 **「S4 밀어냄의 한 걸음 크기」**다
  (10.2291 mm ≈ 행 간격 9.9 mm · 실패 자리 행 간격 0.87~2.71 mm · 같은행 간격은 유지).

## 5. 자기신고 1건

①이 (나)로 갈리면서 **②의 구현을 하지 않았다**(사전 등재대로) ⟹ **판정문의 처방이 집행되지 않았다.**
그 사유는 ㉡ 의 값이고, **처방을 임의로 바꾸거나 다른 처방을 지어 넣지 않았다**(사후 확대 0).
미집행 = ② 구현 · ④ 굽기·캡처 · ⑤ 종합(전부 사전 등재 C 의 귀결 · 사유 명시) · 그 외 0.
문턱 **신설 0 · 이동 0**(①의 자를 값 보기 «전»에 §0-4ㄴ 에 적고 그대로 적용했다) · 사후 갈래 신설 0 ·
화면 판정 0 · ecc 0·0 · 물리 0줄 · `git add` 명시 경로만 · **구현 중간 산출 즉시 커밋**(이 판 신설 규약 준수).
이 판 diff — `src/v3/garmentScene.ts`(인쇄 훅 2회) · `CLAUDE.md` +6 · 설계서 S2 절 교체 · **계기 신설 0**.

## 6. 다음 판에 필요한 새 사실

㉠ ★ **S4 의 «한 걸음 크기»를 행 간격에 묶어야 한다** — 이 판의 값(첫 회 **10.2291 mm** ↔ 행 간격
**≈9.9 mm**)이 그 설계의 근거다. 후보(이 판은 **고르지 않는다**) = 한 걸음에 상한을 두기 ·
밀어냄을 «행 순서»로 훑기 · S3 낙하를 S4 «뒤»로 돌리기.
㉡ **능선 규칙과 호길이 사상은 그대로 둔다** — v5-13 ①(부풂 아님)·②(대칭 · 팔 윗면)가 성립했다.
㉢ ★ **`n ⊥ ẑ` 은 어깨 위 기하의 «사실»이다** ⟹ 「면 법선에서 축을 만드는」 계열 처방은 **전부 전역 `z`
와 같아진다**. 다른 층위(걸음 크기 · 순서)의 처방이 필요하다.
㉣ **비트 불변이 두 판 연속 통과**했다(v5-12 · v5-14) ⟹ R4·R5 계획은 그대로 유효하다.
㉤ 「`SEP` 밑」 판정에는 **수치 경계(1.95~2.00 mm)** 가 섞인다 — 다음 판에서 그 부류를 분리해 셀 것.

## 부록 — raw 링크 (SHA 고정)

신규
- `docs/v5/14-국소좌표계.md` — 회차 노트 정본(§0~§4 · 209줄) · 커밋 `f3a017b`
  https://raw.githubusercontent.com/banshk445/fit-simulator/f3a017b/docs/v5/14-%EA%B5%AD%EC%86%8C%EC%A2%8C%ED%91%9C%EA%B3%84.md
- `docs/v5/보고/14.md` — 이 보고

수정
- `docs/v5/설계-조립2세대.md` — **S2 절에 「닫힌 = 간극 0(거리 `SEP`)」 반영** · 커밋 `010ee53`
  https://raw.githubusercontent.com/banshk445/fit-simulator/010ee53/docs/v5/%EC%84%A4%EA%B3%84-%EC%A1%B0%EB%A6%BD2%EC%84%B8%EB%8C%80.md
- `CLAUDE.md` — 「구현 중간 산출 즉시 커밋」 등재 · 커밋 `010ee53`
- `src/v3/garmentScene.ts` — 인쇄 훅 2회(행별 추적 · 실패 자리 행 간격) · `redrapeAsm2` 안
- `.obsidian-log/전략세션-인수인계.md` · `.obsidian-log/00-현재상태.md` · `docs/v5/v5-색인.md` · 커밋 `774b524`
  https://raw.githubusercontent.com/banshk445/fit-simulator/774b524/.obsidian-log/%EC%A0%84%EB%9E%B5%EC%84%B8%EC%85%98-%EC%9D%B8%EC%88%98%EC%9D%B8%EA%B3%84.md
- `docs/metrics-log.md` — v5-14 블록(수치 정본) · 커밋 `47aee30`
  https://raw.githubusercontent.com/banshk445/fit-simulator/47aee30/docs/metrics-log.md

━━━ 👉 다음: 전략 세션에 이 보고를 전달하세요 (CC 대기) ━━━
