# v5-16 보고 — (A) 확정 · 자기검사 «교차 0» 진단 경로 · R3 굽기

git 스탬프 — 브랜치 **`v5-16-first-bake`** · 기점 **`98803dc`**(v5-15 HEAD) · §0 선커밋 **`bd40efe`** ·
구현 **`6535109`** · 노트 **`029a204`** · metrics·볼트 **`4545674`**. 집행 기계 — **에어 M4(맥)**:
물리 0프레임 · **2호기 접촉 0 · 굽기 0칸 · 캡처 0장**. **ecc 에이전트 0 · 스킬 0 · 계기 신설 0.**

## 1. 갈래 — **C**

**C = ①(나) ⟹ 사실 상신.** ①에서 **면 교차가 0 이 아니다**(529 / 446 / 512 개) ⟹ 사전 등재대로
**③④⑤ 착수 0**(2세대 첫 굽기는 다음 판으로). ②(진단 경로 + 비트 불변)는 **통과**했다.
★ **D 가 아니다**(비트 불변 유지 · 3판 연속) · **A·B 가 아니다**(굽기를 착수하지 않았다).
★★★ **이 판의 산출은 판정문 전제의 기각과 «그 수를 처음 센 것»이다.**

## 2. §1 항목별 — 값 전량

### ① (A) 경로 f0 채널 — **(나) 교차 > 0**

계기 = **기존 `minPairDist(pos, tris, SEP*2)`**(`src/v3/instruments.ts:66` · **새 계기 0**) —
그 반환의 `hits` 가 **게이트의 「자기관통 교차」 채널 그 자체**다(`s4Gate.ts:143` `mp.hits !== 0`).

| 칸 | **면 교차 수 `hits`** | 최소쌍 면 거리 mm | 최소쌍 패널 | 최근접 정점쌍 mm | 그 자리 | S4 반복/상한 | `FIX` |
|---|---|---|---|---|---|---|---|
| `c100-h170-s45_M` | **529** | 0.0004334 | `front↔back` | 1.58249 | front(i11,j66)↔back(i11,j67) | 21 / 61 | **A** |
| `mst-over-L` | **446** | 0.0001976 | `front↔back` | 1.32808 | front(i18,j73)↔back(i17,j73) | 20 / 61 | **A** |
| `supima-L` | **512** | 0.00008126 | `front↔back` | 0.93036 | front(i46,j70)↔back(i47,j71) | 21 / 61 | **A** |

㉠ ★★★ **교차가 0 이 아니다 — 529 / 446 / 512 개다.** 판정문은 「남은 f0 결함의 성격 전환:
관통·교차가 **아니라** 천끼리 `SEP` 미만 근접(0.5~1.6mm)」이라 적고 그 위에 완화 근거(「제약 정의역
안이면 굽기가 판정한다」)를 세웠다. **실측이 그 전제를 뒤집는다.**
㉡ ★ **v5-12~15 의 「삼각형 쌍 거리 ~0」은 «근접»이 아니라 «교차의 증상»이었다** — 교차하는 두
삼각형의 거리는 **0 으로 보고된다**(`minPairDist` 의 정의) ⟹ 그 판들이 본 **0.000003382 ~ 0.001260 mm**
는 거리가 아니라 **교차**였다. **이 판이 처음 `hits` 를 셌다.**
㉢ **완화된 자로도 통과하지 못한다** — 「교차 0 ∧ 최소쌍 > 0」에서 **교차 조건이 깨진다** ⟹
사전 등재 「(나) 교차 > 0 ⟹ ③ 착수 0」.
㉣ **(A) 자체는 v5-15 대로 작동한다** — 최근접 «정점»쌍 **0.93036 / 1.32808 / 1.58249 mm**(v5-15 와
동일) · S4 **20~21회 / 상한 61** 수렴 · 최소쌍 패널은 세 칸 모두 `front↔back`.
★ **교차의 «자리·분포»는 이 판이 세지 않았다** — `minPairDist` 는 **최악 한 쌍**만 돌려준다.
529개가 어느 행·어느 열·어느 패널 쌍에 있는지는 **미측정**이고 계기를 한 줄 더 넣으면 난다
(다음 판 항목 · **추정 0**).

### ② 자기검사 진단 경로 — **구현 · off 비트 불변 통과**

diff 요지 — δ 보정 루프의 판정 한 줄을 갈랐다:
`if (ASM2) { const mpA = minPairDist(sc0.s.pos, sc0.tris, SEP * 2); …probe…; if (mpA.hits === 0 && mpA.min > 0) { ok = true; break; } } else if (m0 >= need) { ok = true; break; }`
· 재는 함수는 **이미 있는 `minPairDist`**(새 계기 0) · 창 `SEP * 2` 는 **실패 문언이 이미 쓰던 그 창**
(새 수 0) · **`min > 0` 은 문턱이 아니라 «같은 점이 아니다»**(부동소수에서 0 = 겹침 · 판정문
「문턱 이동 아님」) · 실패 문언도 `asm2` 경로에서 **「옷 자기 «교차» — 교차 N개 · 최소쌍 …」** 으로
갈라 **그 수를 드러낸다**(①의 값이 그 문언에서 나왔다) · **`asm2` 가 꺼져 있으면 분기 밖**이다.

★ **off 비트 불변 — A포즈 29 / 29 동일 · T포즈 108 / 108 동일**(기준선 = v5-12 에서 «커밋된 v5-11
코드»로 뜬 표 · 같은 계기 `scripts/v5AsmSha.ts` · 줄 단위 대조 · 던지는 **26칸의 문언까지 동일**)
⟹ **갈래 D 아님**. 이것으로 비트 불변은 **v5-12 · v5-14 · v5-16 세 판 연속 통과**다.
★ **(A) 를 기본값으로 승격**했다(판정문 「(A) 채택」) — `asm2` 가 켜지면 `asm2Fix` 기본이 **`'A'`** 다 ·
`'B'`(표면 추종)는 **보류**로 코드에 남긴다(삭제 0 · 열 간격 항이 생기면 다시 시험한다).

### ③④⑤ — **착수 0**

R3 굽기 3칸 · 「같은 옷」 대조 · 종합 = **전부 착수 0**(사유 = ①(나)) ⟹ **굽기 0칸 · 2호기 접촉 0 ·
캡처 0장**. 게이트 5채널 · 어깨 걸림(정도) · 겨드랑이 · 링 궤적(f0→정착) · 층3 5행 · `RAMP_N` 실측 ·
**f0 근접쌍이 몇 프레임에 `SEP` 로 벌어지는지**(위반 수 궤적)는 **전부 미집행**이다.
⟹ **2세대 첫 굽기는 다음 판으로 넘어간다.**

## 3. 사전 등재 기대 ↔ 실측 대조

| 등재 | 기대 | 실측 | 판정 |
|---|---|---|---|
| ① 교차 0 ∧ 최소쌍 > 0(세 칸) | 교차 0 | **529 / 446 / 512** | **(나)** |
| 판정문 「관통·교차가 아니라 근접」 | 근접뿐 | **교차 수백 개** | **기각** |
| ② 진단 경로 diff | 한 줄 분기 | ✅(재는 함수·창 모두 기존) | **성립** |
| ② off 비트 불변 | 동일 | **A 29/29 · T 108/108** | **통과(3판 연속)** |
| ③ 게이트 pass ≥ 2 | — | **미집행**(착수 0) | — |

## 4. 기각·정정된 것

- ★★★ **판정문의 성격 전환 기각** — 「관통·교차가 아니라 `SEP` 미만 근접」이 아니다(교차 **529/446/512**).
  ⟹ 그 위에 세운 완화 근거(「제약 정의역 안이면 굽기가 판정한다」)는 **교차가 0 이 된 뒤에야** 쓸 수 있다
  (**폐기가 아니라 «순서»**다).
- ★ **v5-12~15 의 값 판독 정정** — 「삼각형 쌍 거리 0.000003382~0.001260 mm」는 **거리가 아니라 교차**였다.
- **자기검사 문언 정정**(`asm2` 경로) — 「옷 자기 간격 `SEP` 미달」 → **「옷 자기 «교차» — 교차 N개」**.

## 5. 자기신고 2건

1 ★ **CC 귀책(대장 `C2` 등재) — `hits` 를 네 판 동안 읽지 않았다.** `minPairDist` 는 v5-12 부터 쓰던
함수이고 `hits`(교차 수)를 **이미 돌려주고 있었다**. v5-12~15 는 `min` 만 인쇄해 「거리 ~0」으로
적었고, 그래서 「근접인가 교차인가」가 **네 판 동안 미확인**으로 남았다 — 전략 세션이 그 전제 위에
완화 처방을 세우는 데도 **기여했다**. 교훈 = **계기가 이미 주는 값을 «전부» 인쇄한다**
(함정 13·18 계열 · 「대역 요약에 중앙값 단독 금지」와 같은 방향).
2 **교차의 «자리·분포»는 미측정**이다(최악 한 쌍만 안다) — 다음 판 항목으로 등재.

미집행 = ③④⑤(사전 등재 (나) 의 귀결 · 사유 명시) · 그 외 0. 문턱 **신설 0 · 이동 0**(완화는
판정문의 «자의 대상 정정»이고 `min > 0` 은 문턱이 아니다) · 사후 갈래 신설 0 · 화면 판정 0 ·
물리 0줄 · **계기 신설 0** · `git add` 명시 경로만 · 구현 중간 산출 즉시 커밋.
이 판 diff — `src/v3/garmentScene.ts`(자기검사 `asm2` 분기 · `FIX` 기본 `'A'`) ·
`scripts/v4AsmExport.ts`(`ASM2`/`ASM2FIX` env).

## 6. 다음 판에 필요한 새 사실

㉠ ★★★ **교차 수백 개를 «줄이는» 처방이 필요하다** — 완화로는 안 된다. v5-15 가 남긴
**(A)+(B) 결합**과 **열 간격 항**이 그 자리이고, 두 후보 코드는 **하위 플래그로 이미 있다**
(결합은 «한 줄 조건»이면 된다).
㉡ ★ **교차의 자리·분포를 세는 계기 한 줄** — `minPairDist` 를 «전수 열거»로 바꿔 어느 패널 쌍 ·
어느 행 · 어느 열에 몇 개가 있는지 낸다. 이 판의 세 칸 값(529/446/512)이 그 계기의 기준선이다.
㉢ **「근접 = 제약 정의역 안」 논리는 «교차 0 이 된 뒤에» 다시 쓸 수 있다**(폐기 아님 · 순서).
㉣ **능선 규칙 · 호길이 사상 · `SH_DROP` · 앵커 · (A)는 그대로 둔다**(v5-12 R0 · v5-13 ①② · v5-15 (A)).
㉤ **비트 불변이 세 판 연속 통과**했다(v5-12 · v5-14 · v5-16) ⟹ R4·R5 계획은 유효하다.

## 부록 — raw 링크 (SHA 고정)

신규
- `docs/v5/16-첫굽기.md` — 회차 노트 정본(§0~§4 · 159줄) · 커밋 `029a204`
  https://raw.githubusercontent.com/banshk445/fit-simulator/029a204/docs/v5/16-%EC%B2%AB%EA%B5%BD%EA%B8%B0.md
- `docs/v5/보고/16.md` — 이 보고

수정
- `src/v3/garmentScene.ts` · `scripts/v4AsmExport.ts` — 진단 경로 · (A) 기본값 · `ASM2` env · 커밋 `6535109`
- `.obsidian-log/전략세션-인수인계.md` — v5 트랙 교체 + **CC 귀책 대장 `C2` 등재**
  https://raw.githubusercontent.com/banshk445/fit-simulator/4545674/.obsidian-log/%EC%A0%84%EB%9E%B5%EC%84%B8%EC%85%98-%EC%9D%B8%EC%88%98%EC%9D%B8%EA%B3%84.md
- `.obsidian-log/00-현재상태.md` · `docs/v5/v5-색인.md` · `docs/metrics-log.md` — 커밋 `4545674`
  https://raw.githubusercontent.com/banshk445/fit-simulator/4545674/docs/metrics-log.md

━━━ 👉 다음: 전략 세션에 이 보고를 전달하세요 (CC 대기) ━━━
