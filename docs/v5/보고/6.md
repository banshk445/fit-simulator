# v5-6 — 경계 양쪽(W 54.5 ↔ 56)의 차이 측정: 후보 4종 계기 · 처방 0

**물리 0줄 · `src/` diff 0(계기 신설만) · 굽기 ≤ 2칸 · ecc 0 · 측정 판**

| | |
|---|---|
| 브랜치 | `v5-6-boundary-probe` |
| 기점 | **`e385f64`**(= v5-5 보고 부록) |
| §0 커밋 | **`7a135a5`**(집행 «전») |
| §1~§4 커밋 | (집행 후 기입) |
| 보고 파일 | `docs/v5/보고/6.md` |

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v5-6-boundary-probe e385f64 → v5-6-boundary-probe · HEAD e385f64
 2호기 (§0 커밋 push 뒤 같은 자리로 — §0-5 에 인쇄)
```

---

## §0 수령·사무

### §0-1 판정문 (전략 세션 · v5-5 §4 수령 · 원문)

```
 「갈래 A 수령 · 선행 채널 = 어깨 봉제 y(mst-over 는 반전 없음 — 끌어올림 미점화) · 교차 후행 ·
   ★ 경계 = 절벽(1.5cm 에 858→3 · 링 이봉 54↔92) ⟹ 상전이 — 어느 접촉·구속 조건의 성립/불성립 ·
   지배 변수 = W 단독(어깨 52 고정에도 54.5 는 걸림) · 전략 세션 후보 지정 0(오독 2연속 교훈) ·
   v5-5 ③ 의 CC 측정 설계 4종(램프 수축 궤적 · 봉제 λ · 마찰 상수의 코드 사실 · 어깨 접촉 정점 수 궤적)을
   경계 양쪽 두 칸에서 나란히 — 걸림/흘러내림 상태의 «차이 나는 채널»을 지목하는 판 · 처방 0」
```

### §0-2 바꾸지 않는 것

```
 물리 **0줄** · `src/` **0줄**(계기는 `scripts/` 신설) · 게이트·문턱·상수 **0** · **처방 0**
```

### §0-3 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · 단위 명시)

**★ 단위** — 길이 **mm/m** · 프레임 **1/60 s** · 정점 **개** · 수축률 **무차원**.

| 항 | 갈래 |
|---|---|
| **①** 마찰 코드 사실 | 항의 유무·식·상수값(파일:줄) · 없으면 「접선 구속 0」 등재 |
| **②** 궤적 4종 | **(가)** 갈림 구간에서 **먼저 갈라지는 채널** 지목 / **(나)** 불가 |
| **③** 종합 | 사실 목록 · 처방 후보는 **측정 가능 형태로만**(처방 0) |

**§4** — A = ②(가) / B = ②(나) ⟹ ①③만 / C = 그 외 ⟹ 정지.

### §0-4 사전 결정 5건 (값 보기 «전» · CC 집행 설계)

```
 ㄱ **두 칸은 «같은 옷의 W 만 다른» 점이어야 한다** — supima-L 은 총장·어깨가 달라 대조군이 못 된다.
    ⟹ `mst-over-L` 에서 **가슴·밑단만 54.5** 로 둔 가상 점(`mst-over-L-w545`)을 새로 등재하고,
    v5-5 의 `w56` 과 나란히 놓는다(둘 다 가상 실험 · 실물 아님).
 ㄴ **덤프가 없으면 굽는다** — v5-5 의 `w56` 은 궤적 덤프 없이 구웠다 ⟹ **두 칸 모두 slip_probe 로 재굽기**
    (예산 2칸 · 채널 정의 수정 0).
 ㄷ **λ 는 덤프에 없다** — 덤프는 위치뿐이다. ⟹ **봉제 간극**(쌍 거리 − 그 프레임의 rest)으로 대체하고
    「λ 자체가 아니다」를 명시한다(계기가 못 재는 것을 «잰 척» 하지 않는다).
 ㄹ **「어깨 대역」은 몸에서 정의한다**(손 상수 0) — 어깨선 `Y_TOP` 아래 **암홀 깊이 `ARM_D`** 까지의 띠 ·
    그 안에서 **몸 거리 ≤ SEP** 인 옷 정점을 «접촉»으로 센다(SEP 은 정본 상수).
 ㅁ **처방 0** — 상수·코드·문턱을 한 줄도 바꾸지 않는다.
```

### §0-5 커밋·전환 인쇄 (집행 «전»)

```
 §0 선커밋 **7a135a5** — 집행 «전» · push 완료
 2호기 **응답 없음**(`Operation timed out` · 46회) ⟹ 이 판은 **에어에서만** 집행(굽기 0)
```


---

## §1-① 마찰의 코드 사실 — **접선 구속은 «있다»**(쿨롱 마찰) · 다만 **관통 깊이에 비례**

| 자리 | 원문 |
|---|---|
| `src/v3/consts.ts:9-10` | `/** 마찰계수 — v3-16(출처 미확보 #23 이월) */` · `export const MU = 0.3;` |
| `gpu/engine/collide.py:1` | 「v3 의 격자 SDF 질의 + 밀어내기 + **쿨롱 마찰**을 Taichi 로 옮긴다」 |
| `gpu/engine/collide.py:12-13` | 「마찰: 접선 변위 `t = (pos − prev)` 에서 법선 성분을 뺀 것 · **`k = min(1, μ·depth/|t|)`** · **`pos −= t·k`** · `|t| > 1e-15` 일 때만(정지/운동 마찰을 따로 두지 않는다)」 |
| `gpu/engine/collide.py:14-15` | `THICK = 1e-3` · `MU = 0.3` · `dressRun.ts:prepare` 가 `collision = { …, thickness: THICK, mu: MU }` 로 넘긴다 |
| `gpu/engine/collide.py:97` | 「`solver.ts:199 resolveCollisions` 의 정점 하나 몫 — 밀어내기 + 쿨롱 마찰」 |

⟹ 「접선 방향 구속 0」은 **사실이 아니다**. 다만 그 크기가 **`μ·depth`** 로 제한되므로
**접촉이 없거나(depth ≤ 0) 얕으면 마찰이 사실상 0** 이다 — 「어깨에 얹혀야 마찰이 붙는다」는 구조다.
★ `MU = 0.3` 은 **출처 미확보**(주석이 `#23 이월` 이라고 적고 있다 · 사실 등재 · 처방 0).

## §1-② 궤적 4종 — **(나) 지목 불가**(경계 «아래» 점이 조립에서 던졌고, 굽기 기계가 내려갔다)

### 사고 1 — 경계 아래 점 `W 54.5`(어깨 52 고정)은 **조립이 던진다**

```
 SPEC=mst-over-L-w545(총장 74 · 가슴/밑단 54.5 · 어깨 52 · 소매 24)
 → `src/v3/garmentScene.ts:922`
   **「옷 자기 간격 SEP 미달 — 최소쌍 back↔sleeveL 0.8876mm — 갈래 D」**
```

⟹ §0-4ㄱ 이 설계한 「**같은 옷의 W 만 다른** 두 점(54.5 ↔ 56)」은 **만들 수 없다**.
어깨 52 를 유지한 채 몸판만 54.5 로 좁히면 **소매와 뒤판이 SEP 안으로 겹쳐** 제도가 성립하지 않는다.
★ **판정문의 「지배 변수 = W 단독」에 대한 반례 재료** — W 를 내리려면 SW 도 함께 내려야 한다
(supima-L 이 걸리는 것은 W 54.5 «와» SW 44.5 의 조합이다). **판정은 이 판의 몫이 아니다.**

### 사고 2 — **2호기가 응답하지 않는다**(굽기·덤프 불가)

```
 `ssh: connect to host desktop-ov72a46 port 22: Operation timed out` — **연속 46회 실패**
 (직접 6회 + 감시 루프 40회 · 12:10 ~ 13:37) ⟹ slip_probe 재굽기 **0칸**(예산 ≤ 2 중 0 사용) ·
 v5-5 의 덤프도 그 기계에 있어 **읽을 수 없다**.
```

### 확보한 것 — **조립 직후(f0) 채널 대조**(에어 · 굽기 0)

신설 계기 `scripts/v5BoundaryProbe.ts` 로 네 채널 중 **셋**(봉제 수축률 · 봉제 간극 · 어깨 대역 접촉 ·
어깨선 위 정점)을 **f0** 에서 쟀다(궤적은 덤프가 있어야 한다 ⟹ 2호기 복구 뒤).
어깨 대역 정의는 §0-4ㄹ 대로 **`Y_TOP` 아래 `ARM_D` 까지의 띠 · 몸 거리 ≤ SEP**(몸에서 · 손 상수 0).

| 옷(같은 몸 · A포즈 그리드 정본) | 가슴단면 cm | `ARM_D` m | `RAMP_N` | **어깨 대역 접촉** | 어깨선 위 정점 | 봉제 간극 중앙 mm | 간극 최대 mm |
|---|---|---|---|---|---|---|---|
| **supima-L**(걸림 · 게이트 pass) | 54.5 | 0.2149 | **77** | **174** | 44 | **0.571** | 2.72 |
| `mst-over-L-w56`(가상) | 56 | 0.2208 | 81 | **144** | 84 | 0.755 | 2.70 |
| `mst-over-L-w58`(가상) | 58 | 0.2194 | 86 | **137** | 78 | 0.715 | 2.70 |
| **mst-over-L**(흘러내림) | 60 | 0.2175 | **89** | **138** | 74 | **0.656** | 2.72 |
| `mst-over-L-w545`(가상) | 54.5 | — | — | **조립 던짐**(갈래 D · back↔sleeveL 0.8876 mm) | — | — | — |

★ f0 만으로는 **갈리지 않는다** — 어깨 대역 접촉은 174 / 144 / 137 / 138 로 **단조적이지 않고**,
봉제 간극·수축률(f0 은 정의상 1)도 자릿수가 같다. **갈림은 램프 «중»에 생긴다**(v5-5 가 f20~f30 으로 특정했다)
⟹ **궤적 없이는 지목할 수 없다** ⟹ 사전 등재 갈래 **(나)**.

## §1-③ 종합(판단 0)

### 사실 목록

```
 ㉠ 마찰은 **있다**(쿨롱 · `k = min(1, μ·depth/|t|)` · `MU = 0.3`) — 다만 **`μ·depth` 로 상한**이 걸린다.
    ⟹ 「어깨에 얹히지 않으면 마찰이 붙지 않는다」는 구조가 **코드에 그대로 있다**.
 ㉡ `MU = 0.3` 은 **출처 미확보**(주석 · `#23 이월`).
 ㉢ 경계 아래 점을 «W 만 내려서» 만들 수 없다 — W 54.5 × SW 52 는 **제도가 던진다**(SEP 미달).
    ⟹ 걸림/흘러내림을 가르는 변수는 **W 단독이 아닐 수 있다**(W × SW 조합 · 사실 등재 · 판단 0).
 ㉣ f0(조립 직후) 채널은 네 옷에서 **갈리지 않는다**(어깨 대역 접촉 174/144/137/138 · 간극 중앙 0.571~0.755 mm).
 ㉤ `RAMP_N` 은 옷이 클수록 길다(77 / 81 / 86 / 89) — 램프가 **천천히** 당긴다는 뜻이다(v5-5 ㉦ 과 같은 사실).
 ㉥ 2호기 부재로 **궤적 채널은 이 판에서 못 쟀다**(굽기 0칸).
```

### 다음 판이 잴 수 있는 형태(처방 0)

```
 ① **경계 두 점을 «성립하는» 자리에서 잡는다** — 예: (W 56, SW 52) ↔ (W 56, SW 46.5) 처럼
    **SW 를 함께 움직여** 걸림/흘러내림이 갈리는 짝을 찾는다(제도 던짐을 피한다).
 ② **궤적 4종**(수축률 · 간극 · 어깨 접촉 · 어깨 봉제 y)을 그 두 점에서 slip_probe 로 — **2호기 복구가 전제**.
 ③ **마찰의 기여** — `MU` 만 다른 가상 굽기 2칸에서 f20~f30 의 어깨 봉제 y 반전 유무를 본다
    (상수는 «가상 실험»에서만 바꾼다 · 정본 0).
 ④ **접촉 상한** — `μ·depth` 가 상한이므로 「깊이 0 근방에서 마찰이 소멸하는가」를 f10~f30 에서 값으로.
```


---

## §2 자기검사

| 항 | 값 |
|---|---|
| `src/v3`·`src/components`·워커·`gpu/engine`·게이트·문턱·`public/` | **diff 0** · 물리 **0줄** · **처방 0** |
| 신설 | `scripts/v5BoundaryProbe.ts`(계기 · 측정만) |
| 신설 모듈 안 변경 | `src/v5/specToPattern.ts` +5(가상 점 `mst-over-L-w545` 등재 · 실물 아님) |
| 굽기 | **0칸**(예산 ≤ 2 · 2호기 부재로 착수 불가) |
| `npx tsc -b` | **통과**(종료 0) |
| pytest | ★ **미집행** — 2호기 응답 없음(같은 사유) · `gpu/` 변경 **0** 이라 회귀 대상은 아니지만 **사실로 적는다** |
| 화면 판정 | **0** |
| 브랜치 인쇄 | §0 머리 · §0 은 집행 **«전»** 커밋(`7a135a5`) |
| ecc 에이전트 / 스킬 | **0 / 0** |

## §4 회차 갈래 — **B**(②(나) ⟹ ①③ 상신)

**넘기는 것 7건**

1. ★★★ **마찰은 있다** — 쿨롱 마찰 `k = min(1, μ·depth/|t|)` · `pos −= t·k`(`gpu/engine/collide.py:12-13`) ·
   `MU = 0.3`(`src/v3/consts.ts:10`). **「접선 구속 0」은 사실이 아니다.**
   다만 **`μ·depth` 상한** ⟹ 얹히지 않으면 마찰이 사실상 0 이라는 구조가 코드에 있다.
2. ★★★ **`MU = 0.3` 은 출처 미확보**(주석이 `v3-16 · #23 이월` 이라 적는다) — 사실 등재.
3. ★★★ **경계 아래 점을 W 만 내려 만들 수 없다** — `W 54.5 × SW 52` 는 제도가 **던진다**
   (`garmentScene.ts:922` 「SEP 미달 — back↔sleeveL **0.8876 mm**」) ⟹
   **「지배 변수 = W 단독」의 반례 재료**(걸리는 supima 는 W 54.5 **와** SW 44.5 의 조합이다 · 판단 0).
4. ★★ **f0 로는 갈리지 않는다** — 어깨 대역 접촉 **174 / 144 / 137 / 138**(W 54.5·56·58·60) ·
   봉제 간극 중앙 0.571~0.755 mm · 수축률 정의상 1. **갈림은 램프 중**(v5-5 의 f20~f30)이다.
5. ★★ **`RAMP_N` 은 옷이 클수록 길다**(77 / 81 / 86 / 89).
6. ★★ **2호기가 46회 연속 응답 없음**(12:10~13:37) ⟹ 궤적 굽기 **0칸** · v5-5 덤프도 못 읽는다 ⟹ **(나)**.
7. ★ **다음 판이 잴 형태 4종**을 남겼다(성립하는 경계 짝 · 궤적 4종 · `MU` 가상 실험 · 깊이-마찰 소멸).

**이 판이 하지 않은 판단** — 지배 변수(W 단독인지 W×SW 인지) · 마찰 상수의 처분 · 시뮬레이터 오답 여부.
