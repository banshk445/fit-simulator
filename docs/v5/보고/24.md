# v5-24 보고 — 목선 채널 자 교체 · 옛 산출 재판정 · `N_sh` 후보 · **갈래 C**

git 스탬프 — 브랜치 `v5-24-neck-channel` · 기점 `de771be`(v5-23 HEAD) ·
워크트리 `/Users/banshk/projects/fit-simulator` · 커밋 §0 `e848bae` · 자 교체 `f6b7bf5` ·
계기 `703fa06` · §1 `666e146` · §2·§4 `bc5f35a` · metrics+볼트 `c185ab0`.
★ `src/` diff = **`s4Gate.ts` 하나뿐**(26+/8−) · `garmentScene.ts` 는 **기점과 바이트 동일** ·
물리 0줄 · **굽기 0 · 캡처 0 · 처방 0**.
이 판에서 호출한 ecc 에이전트 = **0** · 스킬 = **0**.

## 1. 갈래와 사유

**갈래 C**(② 에서 옛 편입이 크게 흔들림 ⟹ 값 상신 · 처분 전략 세션). 사유 한 줄 —
자를 바꾸자 정본 37칸 중 **11칸이 pass → fail** 로 뒤집혔고(29.7%), ④ 굽기는 2호기 부재로 집행할 수 없었다.

A 아님(④ 미집행) · B 아님(같은 이유로 ④의 값이 없다) · **D 아님**(조립 sha T 108/108 · A 29/29 동일) ·
E 아님(C 가 먼저 선다 — ②가 값으로 흔들림을 보였다). 사후 갈래 신설 0.

## 2. §1 항목별 판정과 그 판정을 만든 수 전량

### ① 자 교체 — 완료

`s4Gate.ts` 한 파일. 목선 판정을 **`ringExcess ≤ 1` → `ringRestRatio ≤ 1.10`** 으로 바꿨다.

```ts
   ringExcess: 1,                    // ★ v5-24 — 판정에서 «내림»(보고 값으로 병기 · 삭제 0)
+  ringRestMax: 1.1,                 // ★ v5-24 — 새 판정 채널(휴지 대비 신장률)
-  if (!(ringExcess <= S4_THRESHOLD.ringExcess)) fails.push(`목선 초과비 … > 1 (링 … > 허용 …)`);
+  if (!(ringRestRatio <= S4_THRESHOLD.ringRestMax))
+    fails.push(`목선 휴지비 … > 1.1 (링 …cm / 휴지 …cm · 옛 초과비 …)`);
```

★ **새 수 0 · 새 식 0** — 자 `ringRestRatio = (앞+뒤 목선 폴리라인) / ringRest` 는 이 파일이 **이미 매 판 계산**해
`S4Result` 로 내놓던 값이고, 문턱 **1.10** 은 `ringExcess` 주석이 인용한 **v3-24 어림수** 그대로다.
`ringRest = 2·LEN_NECK`. 옛 채널은 **지우지 않고** 값으로 계속 인쇄한다.
★ **기준선 재현** — `on supima-L` 정착에서 옛 자 **1.0021454460806467**(v5-22 게이트와 비트 일치) ·
새 자 **1.0427966483091002**(v5-23 값) · 새 문턱으로 **통과**.

### ② 옛 산출 재판정 — ★★★ 편입이 크게 흔들린다

계기 `scripts/v5Rejudge.ts`(신설) — 정본 blob 을 얹고 **`runS4Gate` 를 그대로** 부른다(게이트 식 재작성 0).

| 항목 | 값 |
|---|---|
| 재판정 칸 | **37** |
| 건너뜀 | **71** = 정본 정착 blob 없음 **69**(판정 대조 69칸 · **원리적** 불가) + `blob n ≠ 장면 n` **2** |
| **옛 pass** | **37 / 37** |
| **새 pass** | **26 / 37** |
| **변동** | **11칸 · 전부 `pass → fail`**(`fail → pass` **0**) |
| 옛 초과비 분포 | 최소 **0.987804** · 중앙 **0.989700** · **최대 0.998283** |
| 새 휴지비 분포 | 최소 **1.059141** · p25 **1.074616** · 중앙 **1.088685** · p75 **1.105991** · 최대 **1.173295** |

**pass → fail 11칸**

| 칸 | 옛 초과비 | **새 휴지비** | 링 cm | 휴지 cm |
|---|---|---|---|---|
| `c100-h170-s45_XL` | 0.996790 | **1.100619** | 53.16 | 47.53 |
| `c100-h185-s45_S` | 0.989338 | **1.119709** | 58.89 | 51.63 |
| `c100-h185-s45_XL` | 0.989032 | **1.106258** | 58.11 | 51.64 |
| `c100-h185-s50_XL` | 0.989823 | **1.124213** | 59.04 | 51.56 |
| `c122.5-h155-s40_S` | 0.989789 | **1.115648** | 50.89 | 44.80 |
| `c122.5-h155-s40_L` | 0.989420 | **1.105991** | 50.39 | 44.79 |
| `c122.5-h155-s45_L` | 0.988046 | **1.155725** | 47.40 | 40.14 |
| `c122.5-h155-s50_XL` | 0.988854 | **1.172694** | 48.16 | 40.14 |
| `c122.5-h170-s40_XL` | 0.989700 | **1.173295** | 52.66 | 43.91 |
| `c122.5-h170-s50_L` | 0.991017 | **1.168417** | 58.52 | 49.01 |
| `c122.5-h170-s50_XL` | 0.990376 | **1.161716** | 58.13 | 49.01 |

★★★ **옛 자는 이 37칸에서 판별력이 «0» 이다** — 초과비 최대가 **0.998283** 으로 문턱 1 에 한 칸도 닿지 않는다.
v5-23 의 43칸 재판정(「목선 «단독» 실패 칸 0」)과 **같은 방향이 더 큰 표본에서** 확인됐다.
★★★ **새 자는 11칸(29.7%)을 거른다** — 휴지비 중앙이 **1.088685** 라 문턱 **1.10** 이 분포의 **p75 근처**를 자른다.
★ **흔들리는 칸의 성격** — `c122.5`(가슴 122.5) **6칸** · `c100-h185`(키 185) **3칸** · `XL` **6칸** ⟹
**큰 몸 · 큰 사이즈**에 몰린다(휴지가 작은데 링이 큰 조합).
★ **건너뛴 2칸** — `blob n 9888 ≠ 장면 n 9948` · `blob n 11118 ≠ 장면 n 10846` ⟹
정본 blob 이 **옛 판본 제도**로 만들어져 현재 제도와 정점 수부터 다르다.

### ③ `N_sh` 수정 — ★ 후보 전부 반증 · **채택 0**

세 후보 모두 `SH_DROP = 0` 이면 현행 식과 **항등**이다(off 불변은 구조적 보장).

| 후보 | `supima-L` n / 최소쌍 mm | `기본 M` n / 최소쌍 | `작은몸 S` n / 최소쌍 |
|---|---|---|---|
| **현행**(대조) | 10150 / **2.0 통과** | 9280 / **2.0 통과** | 8148 / **1.39419 던짐** |
| ㉢ 높이 걸음 ≤ `d` | 10150 / 2.0 통과 | 9280 / 2.0 통과 | 8148 / 1.39419 던짐(**현행과 항등**) |
| ㉠ 높이 걸음 ≤ `SEP` | 14590 / **1.95733 던짐** | 13760 / **1.89265 던짐** | 12168 / **1.21605 던짐** |
| ㉡ 높이 걸음 ≤ `THICK` | 미실행 | 22720 / **1.15383 던짐** | 20744 / **0.81733 던짐** |

★★★ **열을 촘촘히 할수록 `back↔back` 최소쌍이 «단조» 감소한다** —
`S` 1.39419 → 1.21605 → **0.81733** · `M` 2.0(통과) → 1.89265 → **1.15383**.
⟹ **기전** — `N_sh` 를 키우면 어깨 토막의 **열 간격이 좁아지고**, 자기검사가 그 좁아진 이웃 열을 «붙음»으로 읽는다.
⟹ **판정문의 처방 방향(「`SH_DROP` 이산화 → `N_sh` 산식 한 줄」)이 값으로 뒤집혔다.**
세 칸을 다 통과시키는 후보가 없어 **채택 0** 이고 `garmentScene.ts` 는 기점과 **바이트 동일**로 되돌렸다.
★ 다른 방향(`N_sh` 를 «줄이는» 쪽)은 **등재한 후보가 아니라 시험하지 않았다**(사후 신설 0).

### ④ 회귀 3칸 굽기 · 캡처 — **미집행**

2호기가 판 내내 도달 불가다 — `ssh: connect to host desktop-ov72a46 port 22: Operation timed out` ·
`ping desktop-ov72a46.tail2666a5.ts.net` → **2 패킷 전송 · 0 수신 · 100% 손실**.
게이트(새 자)·어깨 걸림·층3·링 휴지비·**캡처 3칸** 전부 못 냈다. §0-5ㄹ 에 값 보기 **전** 적어 둔 상황 그대로다.

### ⑤ 승격 재료 — 판단 0

★ **108 재굽기의 «순서»가 바뀐다** — 자 교체는 **정본 경로**라 **재굽기 없이도 판정이 바뀐다**(②가 값으로 보였다) ⟹
**① 자 확정 → ② 37칸 재판정 → ③ 나머지 69칸은 «재굽기로만»**(blob 이 없어 재판정 자체가 불가).
제공 목록 재발행은 **편입 11칸 감소**를 반영해야 한다(실제 제공 35칸과의 관계는 이 판이 확인하지 않았다).
**v3 동결 예외 «5건째»** 문언 초안 — ① 바꾸는 것 = `s4Gate.ts` 판정 한 줄 + 문턱 한 줄(값은 둘 다 계속 인쇄) ·
② 왜 v3 인가 = 게이트는 **모든 굽기의 판정**이라 플래그 뒤에 둘 수 없다 · ③ 되돌림 = 판정 줄을 옛 자로 되쓰는 **한 줄**.

### 갈래 D 의 자 — 조립 sha 불변 **통과**

기점 `de771be` 워크트리 대조 · 같은 계기 `scripts/v5AsmSha.ts` ·
**T 포즈 108 / 108 동일**(던지는 **26칸의 문언까지**) · **A 포즈 29 / 29 동일**(던짐 0)
⟹ 「자만 바뀌었으니 조립·blob sha 불변」이 값으로 성립한다.

## 3. 사전 등재 기대 ↔ 실측 대조

| 판정문의 지시/기대 | 실측 | 판정 |
|---|---|---|
| 자 교체 = 휴지 기준 · **새 수 0 · 새 식 0** | `ringRestRatio` + v3-24 어림수 1.10 · 둘 다 파일 안에 있었다 | **성립** |
| 「조립 sha 는 불변」 | T 108/108 · A 29/29 동일 | **성립** |
| off 게이트 «결과»가 바뀔 수 있음 | **11칸 pass → fail** | **성립**(그 규모가 이 판의 상신 사유다) |
| 검증 = 옛 산출 T108·A29 재판정 | **37칸만 가능**(69칸 blob 부재 · A29 는 T포즈 blob 뿐) | **부분**(원리적 한계) |
| S 던짐 = `SH_DROP` 이산화 → `N_sh` 한 줄 | 후보 셋 전부 **악화 또는 항등** | **반증** |
| ④ 3칸 굽기 | 2호기 부재 | **미집행** |

## 4. 기각·정정된 것과 근거 수

```
 ㉠ ★★★ **「`N_sh` 산식 한 줄」 처방 반증** — 근거 = `back↔back` 최소쌍의 **단조 감소**
   (S 1.39419 → 1.21605 → 0.81733 · M 2.0 → 1.89265 → 1.15383) 와 **통과하던 두 칸의 붕괴**
   (supima-L 2.0 → 1.95733 · M 2.0 → 1.89265).
 ㉡ ★★ **옛 목선 자의 판별력이 «0» 이었다는 사실** — 정본 37칸 초과비 **최대 0.998283**.
   그 채널은 정본 체제에서 아무도 거르지 않고 있었다.
 ㉢ ★ **「T108·A29 전량 재판정」은 원리적으로 불가** — 108칸 중 **69칸은 정본 정착 blob 이 없다**
   (CLAUDE.md 등재 「판정 대조 69칸(blob 부재)」). 게이트는 위치가 있어야 도는 자다.
```

## 5. 자기신고 — 절차 이탈 · 한정 · 미집행 · 귀책

```
 ★ **CC 귀책 0**(이 판). 절차 이탈 0.
 ★ **미집행 1 — ④ 굽기 3칸과 캡처 3칸**(2호기 도달 불가 · v5-23 부터 **두 판 연속**).
 ★ **미집행 2 — A포즈 29칸 재판정**(정본 정착 blob 이 T포즈 계열뿐 · §0-5ㄴ 사전 등재).
 ★ **미집행 3 — 건너뛴 2칸**(`blob n ≠ 장면 n`). 맞추려 하지 않았다 — 자산 재생성이고 이 판의 정의역 밖이다.
 ★ **③ 후보 한 칸 미실행** — ㉡ 의 `supima-L`. ㉠ 에서 이미 던졌고 ㉡ 은 더 촘촘하며 최소쌍이 **단조 감소**하는 것이
   두 칸에서 확인됐다. **방향 판정에는 영향이 없다**.
 ★ **다른 방향의 `N_sh` 후보는 시험하지 않았다**(등재 셋 밖 · 사후 신설 0).
 ★ **자 교체가 정본 경로를 바꿨고 off 게이트 판정이 실제로 바뀌었다**(11칸). 프롬프트가 그렇게 등재했으므로
   약정 위반은 아니나 **승격 전에 전략 세션의 처분이 필요한 규모**다 — 그래서 **C 로 상신**한다.
 ★ **문턱 신설·이동 0** — 1.10 은 `s4Gate.ts` 주석이 인용한 **v3-24 어림수**이고 이 파일이 새로 정한 수가 아니다.
   자 `ringRestRatio` 도 이미 계산되던 값이다. **손 상수 0.**
 ★ **집행 환경** — `git` 이 Xcode 라이선스 미동의로 멈춰 `DEVELOPER_DIR=/Library/Developer/CommandLineTools`
   로 우회했다(v5-23 과 같은 사고 · 기계 설정·저장소 파일 변경 0).
 ★ **물리 0줄 · 굽기 0 · 처방 0(③ 채택 0) · ecc 0 · 스킬 0.**
```

## 6. 다음 판 설계에 필요한 새 사실

```
 ㉠ ★★★ **문턱 1.10 이 휴지비 분포의 p75 근처에 있다**(중앙 1.088685 · p75 1.105991) ⟹
   「11칸을 거르는 것」이 자의 «판별력»인지 «문턱 위치»인지는 이 값이 가른다. 전략 세션의 처분 대상이다.
 ㉡ ★★★ **옛 자는 판별력 0 이었다** — 그 채널이 지금까지 무엇을 보증했는지 다시 봐야 한다.
 ㉢ ★★ **흔들리는 칸이 «큰 몸·큰 사이즈»에 몰린다** — 휴지(패턴 목선)가 작은데 링이 큰 조합이다.
   제도(목선 길이)와 자의 관계가 다음 질문이다.
 ㉣ ★★ **S 던짐은 여전히 열려 있다** — `N_sh` 방향이 반증됐으므로 **다른 층위**가 필요하다.
   이 판의 값이 가리키는 것: 열 간격은 «넓어야» 하고, 붙는 자리는 **목선 구간**이다(v5-23 §1-③).
 ㉤ ★ **69칸은 재굽기로만 판정할 수 있다** — 승격 계획의 순서를 바꾼다.
 ㉥ ★ **2호기 도달 회복이 두 판 연속 막고 있다**(v5-23 · v5-24).
```

## 부록 — raw 링크

- 회차 노트 `docs/v5/24-목선채널.md`
- 보고 `docs/v5/보고/24.md`
- metrics `docs/metrics-log.md`(2026-09-17 v5-24 블록)
- 볼트 `.obsidian-log/전략세션-인수인계.md`(v5 트랙 교체)
- 색인 `docs/v5/v5-색인.md`
- 코드 `src/v3/s4Gate.ts` · 계기 `scripts/v5Rejudge.ts` · 산출 `gpu/oracle/export/v5-24-rejudge.json`

━━━ 👉 다음: 전략 세션에 이 보고를 전달하세요 (CC 대기) ━━━
