# v5-19 보고 — S4 «없음»: S3(BLEND) 직후 관통·교차 사실 · **갈래 B**

git 스탬프 — 브랜치 `v5-19-no-s4` · 기점 `d223804`(v5-18 HEAD) ·
워크트리 `/Users/banshk/projects/fit-simulator` ·
커밋 §0 `70553a6` · 구현 `710c628` · 계기 `3568e6d`·`e85c2c9`·`99f1a79` ·
노트 `2df73f3`·`cf11481` · metrics+볼트 `4f01888`.
집행 기계 = 에어 M4(f0 조립 = 물리 0프레임 · CUDA 불요) · **2호기 접촉 0**.
이 판에서 호출한 ecc 에이전트 = **0** · 스킬 = **0**.

## 1. 갈래와 사유

**갈래 B**(①(나)(다) ⟹ 값·분포 상신). 사유 한 줄 —
S4 를 빼도 **교차가 296 / 136 / 288** 로 사전 등재한 「소수」 자(세 칸 각각 ≤ 3)를 크게 넘고,
**밴드 밖 관통 정점이 157 / 134 / 114** 개 있어 ③ 착수 조건 두 가지가 **모두** 불성립이다.

A 아님(③ 조건 불성립) · C 아님(굽기 0칸) · **D 아님**(off 비트 불변 T108/A29 동일) · E 아님.
사후 갈래 신설 0.

## 2. §1 항목별 판정과 그 판정을 만든 수 전량

### §0-7 처치 이행 — 기준선 한 칸 재현 **통과**

새 계기 `scripts/v5NoS4.ts` 로 `ASM2FIX=BLEND` · `c100-h170-s45_M` 을 먼저 돌려
**교차 466 · n 9,280 · 삼각형 17,808** 을 얻었다 — v5-18 값과 정확히 일치.
진입 env(v5-7b §0-5ㄴ 확정분) 증명 완료 ⟹ CC 귀책 `C3` 의 처치가 작동했다.

### ① 밴드 자 — **이 장면의 수는 8.5054 가 아니다**(규칙 불변 · 수만 정정)

| 값 | 이 장면(세 칸 공통) | 판정문이 인용한 v4-34 장면 |
|---|---|---|
| `h` | **3.6145745878126117 mm** | 3.7527 mm |
| **`band = THICK + 2h`** | **8.229149175625224 mm** | 8.5054 mm |
| `THICK` / `SEP` | 1 / 2 mm | 1 / 2 mm |

계기가 `sdfSpec.band` 를 **읽어** 인쇄한다(손 상수 0) — 그래서 이 차이가 드러났다.
`h` 는 `deriveSpacing` 이 «몸 bbox + SDF 예산»에서 정하므로 장면마다 다르다.
판정문의 **규칙**(「밴드 안이면 최종 판정은 굽기가 한다」)은 그대로 서고 **수만** 바뀐다 —
어느 자로 재도 밴드 밖 정점이 있으므로 **갈래 판정에는 영향이 없다**.

### ① 교차 — **(나)**

| 칸 | 교차 | 몸판끼리 | 소매 관련 | 패널 조합(상위) |
|---|---|---|---|---|
| `c100-h170-s45_M` | **296** | 296 | **0** | `back↔back` 180 · `back↔front` 93 · `front↔front` 23 |
| `mst-over-L` | **136** | 96 | **40** | `back↔back` 61 · `front↔front` 35 · `back↔sleeveR` 12 · `front↔sleeveR` 10 · `front↔sleeveL` 9 · `back↔sleeveL` 9 |
| `supima-L` | **288** | 262 | **26** | `front↔front` 143 · `back↔back` 99 · `back↔front` 20 · `back↔sleeveR` 8 · `front↔sleeveR` 7 · `front↔sleeveL` 6 · `back↔sleeveL` 5 |

행 분포(`nvB − j` · 상위 6) — M `nvB−1` 61 · `−3` 43 · `−2` 35 · `−14` 25 · `−13` 25 · `−6` 18 ·
mst `nvB−1` 40 · `−2` 14 · `−5` 13 · `−6` 12 · `−3` 11 · `−16` 10 ·
supima `nvB−1` 56 · `−4` 50 · `−5` 35 · `−3` 22 · `−8` 19 · `−2` 14.
열 분포(상위 6) — M i50 46 · i51 43 · i4 41 · i52 40 · i5 24 · i6 21 ·
mst i58 14 · i25 13 · i49 12 · i50 11 · i9 11 · i17 7 ·
supima i55 49 · i54 30 · i51 27 · i58 23 · i56 15 · i53 14.
**암홀 열 안 교차 비율 = 1.0**(세 칸).

사전 등재한 「소수」 자 = §0-6 에 **세 칸 각각 교차 ≤ 3**(판정문의 «mst 1 급» 을 자로 삼아 값 보기 전 고정).
⟹ **세 칸 모두 초과 = (나)**.

★ **판정문의 전제에 대한 사실** — 「S3 1 → S4 18」은 v5-18 의 **㉯ `RC`** 판본 수다.
이 판이 돌린 **BLEND** 의 S3 는 애초에 **296 / 96 / 262** 였다(v5-18 §1-③ ㉢ 표에 그대로 있다).
회차 제목이 「S3(**BLEND**) 직후」이므로 그대로 집행했고,
**`'RC'` 는 프롬프트가 「코드 유지·미사용」이라 하여 돌리지 않았다.**

### ① 관통 — **(다)**

| 칸 | 관통 정점(`signed < 0`) | 최대 | 중앙 | p99 | **밴드 밖 정점** | 게이트 식 `pen` 최대 |
|---|---|---|---|---|---|---|
| `c100-h170-s45_M` | 223 | 8.229149505496025 mm | 8.229149505496025 | 8.229149505496025 | **157** | 9.229149505496025 mm |
| `mst-over-L` | 173 | 8.229149505496025 mm | 8.229149505496025 | 8.229149505496025 | **134** | 9.229149505496025 mm |
| `supima-L` | 173 | 8.229149505496025 mm | 8.229149505496025 | 8.229149505496025 | **114** | 9.229149505496025 mm |

★★★ **최대 = 중앙 = p99 로 전부 같다 ⟹ 자가 포화했다.** 이 값은 `band`(8.229149175625224)와
소수 일곱째 자리에서만 다르다 — `bakeSdf` 가 「띠 밖은 `band` 로 자른다」(`bodySdf.ts:144`)고 적어 둔
클램프 값이다. ⟹ **이 정점들의 진짜 깊이는 알 수 없고, 아는 것은 「`band` 이상」뿐이다.**
v4-34 가 「10.5054 는 깊이가 아니라 «자의 끝»이었다」로 등재한 것과 **같은 현상**이다.

### ★★★ ㉠ S4 는 밴드 밖 정점을 «한 톨도» 못 민다

같은 칸(`c100-h170-s45_M`) · 같은 계기 · 같은 env 로 `BLEND`(S4 있음) ↔ `NOS4`(S4 없음):

| 채널 | `BLEND` | `NOS4` |
|---|---|---|
| 교차(최종) | 466 | **296** |
| 단계 S3 → S4 | 296 → **466** | 296 → 296(루프가 돌지 않는다) |
| 관통 정점(`signed < 0`) | **157** | **223** |
| **밴드 밖 정점** | **157** | **157** |
| 밴드 밖 패널 분포 | front 104 · back 53 | **front 104 · back 53** |
| 밴드 밖 행 분포 | `nvB−11` 27 · `−10` 25 · `−9` 24 · `−8` 23 · `−12` 22 · `−7` 18 · `−13` 10 · `−6` 6 · `−5` 2 | **동일** |
| 밴드 밖 y mm | 1298.8662760500263 ~ 1404.2773123501017 | **동일** |
| 밴드 밖 팔축 `s` mm | −196.39668372167122 ~ 248.55655552380446 | **동일** |

밴드 밖 집합이 **개수·패널·행·y·팔축 좌표까지 전부 같다** ⟹
**S4 의 `NY`(= 61) 회 반복이 이 157 개를 한 번도 움직이지 못했다.**
기전은 코드에 있다 — `nHat` 은 중앙차분이고 밴드 밖은 모든 표본이 클램프값이라 **기울기가 0**,
`gn > 1e-12` 이 거짓이라 `continue` 한다.
⟹ **v4-34 가 «미확인»으로 남기고 판정문이 인용한 명제(「밴드 밖은 기울기가 소실돼 솔버도 못 민다」)가
값으로 확인됐다.**

★ 반대로 **S4 가 실제로 한 일** = 밴드 «안» 관통 정점 **66 개**(223 − 157)를 밀어낸 것이고,
그 대가로 **교차를 170 개 만들었다**(296 → 466).

### ★★★ ㉡ `redrapeAsm2` «뒤»의 이동자를 특정했다 (v5-18 미규명 항목 닫힘)

조립 장면 9개의 S3·S4 스냅숏을 **전수** 훑어 `gapProbe` 장면과 비트 일치하는 것을 찾았다 ⟹ **없다**(세 칸).
장면별 S3 교차 — M `[323,318,317,317,317,312,308,296,296]` · mst `[96]×9` ·
supima `[260,260,260,260,257,262,259,262,262]`.

★ 그런데 **「S3 스냅숏 교차」가 「최종 장면의 «몸판끼리» 교차」와 정확히 같다**:
**M 296 = 296 · mst 96 = 96 · supima 262 = 262**. 그리고 **남은 전부가 소매 관련**(0 · 40 · 26)이다.

⟹ 자리 = **`src/v3/garmentScene.ts:1404-1424` — v4-37 §1-③ 소매 축 방향 이동**
(v3 동결 예외 3건째 «최종 형태 v2» · 전략 세션 v4-36 §4 승인). 승인 문언 원문:

```
「전달 분기에서 소매 패널 배치 «후» 축 방향 평행이동: p ← p + s·AX ·
 s = (어깨 이음 봉제쌍의 암홀 쪽 중심 − 소매 쪽 중심)·AX · 좌우 각각 · 조립 자신에서 계산 ·
 수직 성분 적용 0(램프의 몫) · axDot·axPoint·fitSleeve·sgn 불변 · 미전달 분기 불변」
```

코드는 `if (AO) { … s.pos[v*3] += sg*sAx*AX[0]; … }` 이고 **`AO` 가 null 인 미전달(T포즈) 분기는
통째로 지나간다** ⟹ off 비트 불변과 무관하다.
★ 이 블록은 **2세대를 모른다** — `redrapeAsm2` 가 몸판을 능선에 새로 얹은 «뒤» 소매만 축으로 민다.
M 칸은 소매 교차 0 인데 mst·supima 는 40·26 이다.
★ **이 판은 처방 0**(프롬프트 ② 「처방 0 · 사실만」).

### ② 밴드 밖 정점의 «부위»

| 칸 | 패널 | **암홀 열 비율** | 행(`nvB − j` · 상위) | y mm(`Y_ANCHOR`) | 팔축 `s` mm(`SLV_X0`) |
|---|---|---|---|---|---|
| `c100-h170-s45_M` | front 104 · back 53 | **1.0** | `nvB−11` 27 · `−10` 25 · `−9` 24 · `−8` 23 · `−12` 22 · `−7` 18 | 1298.8663 ~ 1404.2773 (1442.0892) | −196.3967 ~ **+248.5566** (215) |
| `mst-over-L` | front 19 · **back 115** | **1.0** | `nvB−11` 21 · `−13` 19 · `−12` 19 · `−15` 14 · `−14` 14 · `−10` 13 | 1263.5286 ~ 1403.9336 (1442.0861) | −217.7001 ~ **+319.4223** (260) |
| `supima-L` | front 64 · back 50 | **1.0** | `nvB−10` 22 · `−11` 18 · `−9` 17 · `−8` 14 · `−13` 14 · `−7` 10 | 1292.6484 ~ 1398.1772 (1442.0791) | −199.9650 ~ **+261.1312** (222.5) |

⟹ 밴드 밖 정점은 **전부 암홀 열**(어깨 토막)이고, 행은 **봉제선 아래 5~15행**,
y 는 `Y_ANCHOR` 아래 **38 ~ 179 mm**(어깨마루 ~ 겨드랑이),
**팔축 `s` 의 최대가 `SLV_X0` 를 넘는다** ⟹ 판정문이 물은 **«팔 윗면» 구간이 맞다**.

### 나머지 채널 (M / mst / supima 순)

```
 봉제쌍 ≈ SEP  어깨 봉제 중앙 2.0000 / 2.0000 / 2.0000 mm(= SEP ⟹ 간극 0 · 닫힘) ·
              n 34 / 44 / 36 · 최대 112.1432 / 141.8661 / 132.6627 mm
 소매 캡↔암홀  앞R·뒤R·앞L·뒤L 중앙 — 44.6016·82.5070·43.5286·81.8811 /
              37.6949·40.8314·37.6929·40.6718 / 51.1975·91.7880·41.5228·92.0134 mm
 링 / rest    3D 495.3954 / 532.8758 / 521.3663 mm ↔ rest(패턴) 469.5765 / 469.6107 / 469.6881 mm ⟹
              비 1.054984 / 1.134718 / 1.110027
 최근접 정점쌍 1.32775 / 2.00000 / 0.92208 mm — M front(i8,j69)↔back(i9,j68) ·
              mst front(i0,j76)↔back(i0,j76)(정확히 SEP) · supima front(i52,j70)↔front(i53,j71)
 행 간격      중앙 9.9999 / 9.8057 / 9.8354 · 최대 26.4570 / 33.2942 / 37.2193 mm
              (설계 9.8551 / 9.7368 / 9.7260)
 열 간격      중앙 8.9846 / 8.9398 / 9.1600 · 최대 116.1359 / 132.3126 / 122.1656 mm
 S4           반복 0 · 상한 0 · 수렴 false · 최대이동 궤적 [] ⟹ 플래그가 뜻대로 작동했다
 던짐         M 「교차 296개 · 최소쌍 back↔back 0.02601mm」 · mst 「교차 136개 · back↔sleeveR 0.004904mm」 ·
              supima 「교차 288개 · front↔front 0.0007917mm」
```

### ③ ④ — **착수 0**

③ 착수 조건(§0-6 · 값 보기 «전» 등재) = 「(가) 교차 0 ∧ 밴드 안」 또는 「교차 ≤ 3 ∧ 밴드 안」.
실측 — 교차 **296 / 136 / 288** · 밴드 밖 정점 **157 / 134 / 114** ⟹ **두 조건 모두 불성립**.
**굽기 0칸 · 캡처 0장 · 2호기 접촉 0.** ④ 도 착수 0. 규칙을 값 본 뒤에 움직이지 않았다(함정 14).

## 3. 사전 등재 기대 ↔ 실측 대조

| 등재한 기대 | 실측 | 비 |
|---|---|---|
| S4 를 빼면 교차가 «소수»(≤ 3)가 된다 | 296 / 136 / 288 | **99 / 45 / 96 배** |
| 관통이 전부 밴드 안이다 | 밴드 밖 157 / 134 / 114 개 | **불성립** |
| 밴드 자 = 8.5054 mm(v4-34 인용) | **8.229149175625224 mm**(이 장면) | 0.968 |
| off 비트 불변 유지 | T 108/108 · A 29/29 동일 | **1.000** |
| S4 가 교차를 만든다(v5-18 ㉢) | M 296 → 466 (+170) | **1.574** |

## 4. 기각·정정된 것과 근거 수

```
 ㉠ ★ **밴드 자의 «수» 정정** — 판정문이 인용한 8.5054 mm 는 **v4-34 장면**의 값이다.
   이 장면은 `h` = 3.6145745878126117 · `band` = **8.229149175625224 mm** 다.
   **규칙은 기각되지 않았다** — 수만 장면의 것으로 바뀐다(계기가 `sdfSpec` 에서 읽은 덕에 드러났다).
 ㉡ ★★★ **v4-34 가 «미확인»으로 남긴 명제가 «확인»됐다** — 「밴드 밖은 기울기가 없어 솔버도 못 민다」.
   근거 수 = `BLEND` ↔ `NOS4` 의 밴드 밖 집합이 **157 개로 같고 패널·행·y·팔축까지 전부 같다**.
 ㉢ ★★★ **v5-18 §1-③ ㉢ 의 «미특정» 항목이 닫혔다** — `redrapeAsm2` 뒤의 이동자 = v4-37 소매 축 이동.
   근거 수 = S3 스냅숏 교차 = 최종 «몸판끼리» 교차(296=296 · 96=96 · 262=262) · 나머지 전부 소매 관련(0·40·26) ·
   조립 장면 9개 전수 탐색에서 비트 일치 스냅숏 **0**.
 ㉣ ★ **「진짜 깊이」는 이 자로 측정 불가**(포화) — 최대 = 중앙 = p99.
```

## 5. 자기신고 — 절차 이탈 · 한정 · 미집행 · 귀책

```
 ★ **절차 이탈 0 · CC 귀책 0**(이 판). v5-18 의 `C3` 처치(기준선 한 칸 재현)를 **이행**했고 통과했다.
 ★ **한정 1 — 관통 자**: 이 판의 자는 `sampleSdf` 다(§0-4ㄷ 에 미리 등재). 게이트는 밴드 «안»에서
   `exactBodyDist` 로 다시 재므로 ①의 수와 ③ 게이트의 수는 같지 않을 수 있다. ③ 미착수라 **미확인**.
 ★ **한정 2 — 진짜 깊이 미측정**: 밴드 밖 정점의 침투는 「`band` 이상」만 안다. 더 깊이 재려면
   `deriveSpacing` 의 예산·`h` 를 바꿔야 하고 그것은 **이 판의 정의역 밖**이다(변경 0).
 ★ **미집행 — `'RC'`·`'ARM'` 을 돌리지 않았다.** 프롬프트가 「RC·팔관 후보는 코드 유지·**미사용**」이라 했다.
   그러므로 판정문의 전제(「S3 1 → S4 18」)가 `RC` 판본 수라는 점은 **사실로 적기만** 했다.
 ★ **미집행 — 굽기 0칸 · 캡처 0장**(③ 조건 불성립 · 사전 규칙 그대로).
 ★ **문턱 신설·이동 0** — 쓴 수는 `sdfSpec.band`·`sdfSpec.h`·`THICK`·`SEP`·`N_sh`·`nuB`·`nvB`·
   `N_side`·`SLV_X0`·`Y_ANCHOR`·`NY`·`TOL_SELF` 뿐이고 전부 이미 등재된 양이다. 손 상수 0.
 ★ **물리 0줄** · `src/` diff = `redrapeAsm2` 본문(플래그 분기 1 + 인쇄 훅) + `asm2Fix` 선언 2줄 ·
   **소매 축 이동 블록은 «읽기만» 했다**(한 줄도 고치지 않았다).
 ★ **ecc 에이전트 0 · 스킬 0.**
```

### off 비트 불변 — **통과**

```
 기준선 = 기점 `d223804` 를 갈라 낸 워크트리(`../fit-simulator-v519base` · 본체 `src/` 접촉 0) ·
 같은 계기 `scripts/v5AsmSha.ts` · 같은 `D_MM=9`.
 ★ **T 포즈 108 / 108 동일** — 던지는 **26칸의 문언까지 동일** · ★ **A 포즈 29 / 29 동일**(던짐 0).
 ⟹ 갈래 D 아님. **v5-12 · v5-14 · v5-16 · v5-18 · v5-19 다섯 판 연속**.
```

## 6. 다음 판 설계에 필요한 새 사실

```
 ㉠ ★★★ **밴드 밖 정점은 «밀어냄»으로 못 고친다**(기울기 0). 길은 둘뿐이다 —
   ⓐ 배치(S3)가 애초에 그 대역에 들어가지 않게 한다 · ⓑ SDF 예산·`h` 를 키워 밴드를 넓힌다
   (ⓑ 는 굽기 비용에 직결 · 이 판의 정의역 밖).
 ㉡ ★★★ **소매 축 이동(v4-37)이 2세대와 어긋난다** — 몸판↔소매 교차의 «전부»가 여기서 난다
   (M 0 · mst 40 · supima 26). 그 블록은 `redrapeAsm2` 를 모른 채 소매만 축으로 민다.
 ㉢ ★ **몸판끼리 교차는 S4 와 무관하게 남는다** — 296 / 96 / 262. v5-18 ㉯ `RC` 가 이 대역을
   −79.4% 줄였으나 어깨 봉제를 열었다(채택 0). 이 층위가 여전히 열려 있다.
 ㉣ ★ **밴드 밖 정점의 자리가 좁다** — 암홀 열 100% · 봉제선 아래 5~15행 · 팔 윗면.
   교차의 자리(`nvB−1~8`)와 **겹치되 같지 않다**(관통은 더 아래 행).
 ㉤ ★ **밴드 자의 수가 장면마다 다르다**(8.2291 ↔ 8.5054) — 규칙 문언에 「`sdfSpec.band` 에서 읽는다」를
   박아 두면 다음 판에서 다시 어긋나지 않는다.
 ㉥ **어깨 봉제는 S4 없이도 닫혀 있다**(중앙 2.0000 mm = `SEP` · 세 칸) — S4 는 어깨 폐합에 기여하지 않는다.
```

## 부록 — raw 링크

- 회차 노트 `docs/v5/19-S4없음.md`
- 보고 `docs/v5/보고/19.md`
- metrics `docs/metrics-log.md`(2026-09-12 v5-19 블록)
- 볼트 `.obsidian-log/전략세션-인수인계.md`(v5 트랙 교체)
- 색인 `docs/v5/v5-색인.md`
- 계기 `scripts/v5NoS4.ts` · 코드 `src/v3/garmentScene.ts`·`src/v3/dressRun.ts`

━━━ 👉 다음: 전략 세션에 이 보고를 전달하세요 (CC 대기) ━━━
