# v5-27 — 자기관통 채널 허용(SEP−TOL_SELF) · 37칸 재판정 · 회귀 2칸 → 108칸 본굽기(asm1x)

> 기점 `df424dc`(v5-26 HEAD) · 브랜치 `v5-27-bake108-asm1x`
> 물리 0줄 · `src/` diff = **`s4Gate` 자기관통 채널 거리 한정**(정본 · 조립 sha 불변) · ecc 0

```
 ★ 집행 «전» 사실
   ㉠ `git` 은 Xcode 라이선스 미동의로 멈춰 `DEVELOPER_DIR=/Library/Developer/CommandLineTools` 로 우회한다
      (v5-23 부터 **다섯 판 연속** · 기계 설정 변경 0).
   ㉡ 이 판의 굽기는 **회귀 2칸 + 108칸(≈17h)** 이다 — 2호기 재접속 루프 · `.done` 재개로 돌린다.
   ㉢ 회귀 2칸이 (나) 로 떨어지면 **③ 착수 0**(갈래 B) — 17시간을 걸기 «전»에 갈린다.
```

---

## §0 선커밋 — 집행 «전» 등재

### §0-0 회차 프롬프트 원문

```
v5-27 — 자기관통 채널 허용(SEP−TOL_SELF) · 37칸 재판정 · 회귀 2칸 → 통과 시 같은 판에서 108칸 본굽기(asm1x) 착수

집행 기계 = 2호기 · 브랜치 v5-27-bake108-asm1x · 기점 = v5-26 HEAD df424dc · 물리 0줄 ·
src/ diff = s4Gate 자기관통 채널 거리(정본 · 조립 sha 불변) · 굽기 = 회귀 2 + 108(≈17h · 재접속 루프 · .done 재개) ·
ecc 0 · §0 선커밋 · 브랜치 인쇄 · 보고 = docs/v5/보고/27.md · 캡처 대표 4칸 · 콘솔 4줄
```

### §0-1 판정문(전략 세션 · v5-26 §4 수령 · 원문)

```
「갈래 C 수용 · ① M 교차 1 = 경계 사건(1.980 vs SEP 2.000 · TOL_SELF 0.1 안 · 진짜 교차 아님) ⟹
 채널 거리 SEP → SEP−TOL_SELF(솔버 허용 오차 · 새 수 0) · 검증 = 37칸 재판정 변동 사실 ·
 ② S = 암홀 이동 (Y_NECK−Y_TOP)−SH_DROP 이 작은 몸에서 양수 → 소매 관 관통 ·
 조립 규약(수직은 램프 몫)의 경계 · 처방 = 지금 0 · «던짐 = 보류» 규약으로 108 진행 ·
 던짐 증가·분포를 값으로 본 뒤 소매 트랙 여부 결정」
```

### §0-2 §1 집행 4항 (원문)

```
① 채널 허용 구현(diff 원문) · T 37칸 재판정 변동 표(예상 0 · 다르면 사실) · 조립 sha 불변.
② 회귀 2칸(on) — 기본 M(교차 0 기대) · supima-L(pass 유지) · 게이트 4채널 · 어깨 걸림 · 층3 · 캡처.
   갈래: (가) 2/2 pass / (나) fail(값) ⟹ ③ 착수 0.
③ 108칸 본굽기(asm1x · 27몸 × 4사이즈 · v4-46 규약 그대로 · 순서 M→S→L→XL · 중간 집계 metrics append) —
   집계: pass/fail(채널·값)/던짐(자리·부류) · 층3 96/96 급 · 어깨 걸림 분포 · 목선 두 비율 보고값 ·
   v4-46 대비 표(던짐 12 → ? · pass 33 → ? · 강등/승격 칸 목록) · 던짐이 작은 몸·S 에 몰리는지.
④ 대표 캡처 4칸(작은몸 S 가 던지면 작은몸 M 으로 대체) · 제공 목록 재발행 초안(어깨 채널 ≥172 규약 그대로) ·
   v3 동결 예외 등재 초안.
```

### §0-3 §4 사전 등재 갈래 (원문 · 사후 갈래 신설 0)

```
A = ②(가)+③ 완주 ⟹ 집계·캡처 상신(화면 판정 → 제공 재발행)
B = ②(나) ⟹ 값 상신 · 108 착수 0
C = ③ 중단(사유·재개 절차) ⟹ 상신
D = 조립 sha 변동 ⟹ 정지
E = 그 외 ⟹ 정지
```

---

### §0-4 코드 사실 — 값 보기 «전» 인용

**ㄱ. 지금의 자기관통 채널은 «거리»가 아니라 «위상»이다**

```ts
// src/v3/s4Gate.ts:104
const mp = minPairDist(pos, sc.tris, SEP * 3);
// src/v3/s4Gate.ts:159
if (mp.hits !== S4_THRESHOLD.crossings) fails.push(`자기관통 교차 ${mp.hits} ≠ 0`);
```

```ts
// src/v3/instruments.ts:100-112 — 후보 쌍마다
let d = Infinity;                       // 여섯 «정점↔삼각형» 거리의 최소
if (d < window) near++;
if (d < SEP - TOL_SELF) viol++;         // ← **이미 있는 «거리» 채널**(게이트는 안 쓴다)
if (triTriHit(pos, A, Bt)) hits++;      // ← 게이트가 쓰는 **위상** 채널(거리 조건 «없음»)
```

```
 ★ `hits` 는 **거리와 무관**하게 삼각형 교차를 센다. 그래서 v5-26 의 `기본 M` 처럼
   **최소 쌍거리 1.9798624570389312 mm 인 채로 교차 1** 이 성립할 수 있다
   (`d` 는 «정점↔삼각형» 여섯 거리의 최소일 뿐 «엣지↔엣지» 를 재지 않는다 ⟹ 교차해도 `d > 0` 일 수 있다).
 ★ **`viol`(= `d < SEP − TOL_SELF`)은 이미 계산돼 반환된다** — 판정문이 말하는 「채널 거리 SEP−TOL_SELF」는
   **이미 있는 양**이고, 이 판은 **새 수도 새 식도 만들지 않는다**.
 ★ 상수 — `THICK = 1e-3` · `SEP = 2 · THICK = 2 mm` · `TOL_SELF = 1e-4 = 0.1 mm`
   (`src/v3/consts.ts:6·8·14`) ⟹ `SEP − TOL_SELF = 1.9 mm`.
 ★ `S4_THRESHOLD.crossings = 0` 은 **지우지 않는다**(v5-24·v5-25 의 처분 방식과 같다 · 되돌림 한 줄).
```

**ㄴ. 솔버가 실제로 요구하는 거리**

```
 `TOL_SELF` 는 이 파일이 이미 **정착 창 순변위 문턱**으로 쓰는 그 수다(`settleNetM: TOL_SELF` · v3-22 채널).
 자기충돌 해소가 «다 풀었다»고 보는 자리는 **`SEP` 가 아니라 `SEP − TOL_SELF`** 이고,
 v5-26 의 `기본 M` 은 **1.97986 ≥ 1.9** 로 그 안에 있다.
```

---

### §0-5 형식화 — 값 보기 «전» 고정(사후 신설 0)

**ㄱ. ① 구현의 «형태»를 미리 못 박는다**(값을 보고 고르지 않는다)

```
 판정 줄을 아래 한 형태로만 바꾼다 — **삭제 0 · 새 수 0 · 새 식 0**:

   if (mp.hits !== S4_THRESHOLD.crossings) …                     ← 옛 줄(주석으로 남긴다)
   if (mp.hits !== S4_THRESHOLD.crossings && mp.min < SEP - TOL_SELF) …   ← 새 줄

 ★ 뜻 — **교차가 있어도 «모든» 비인접 쌍이 솔버 허용 거리(`SEP − TOL_SELF`) 밖이면 관통으로 세지 않는다.**
   `mp.min` 은 그 blob 의 **전역 최소**이므로 `mp.min ≥ SEP − TOL_SELF` 이면 교차 쌍을 포함해
   **어떤 쌍도** 허용 안으로 들어오지 않았다는 뜻이다(쌍별 거리를 새로 꺼내지 않는다 · 함정 12).
 ★ 실패 문언에 **두 수를 병기**한다(교차 수 · 최소 쌍거리 mm) — 값이 보고에 그대로 남게.
 ★ 보고 값으로 **`viol`**(= `d < SEP − TOL_SELF` 인 쌍 수)을 `S4Result` 에 **더해서 인쇄**한다
   (판정에는 쓰지 않는다 · v5-24·25 의 「내림·병기」와 같은 처분의 반대 방향 = 「올림 전 병기」).
 ★ **`instruments.ts` 는 한 줄도 고치지 않는다** — 고치면 조립 자기검사(S3b)가 같이 움직인다.
```

**ㄴ. ① 재판정의 판정 규칙**(37칸 · 굽기 0)

```
 계기는 v5-24 의 `scripts/v5Rejudge.ts` 와 **같은 골격**을 쓰되 이 판의 채널을 인쇄한다
 (새 파일 `scripts/v5Rejudge27.ts` · `src/` 0줄). 한 번 돌려 **옛 판정과 새 판정을 함께** 낸다 —
 옛 판정 = 새 `fails` 에서 「자기관통」 줄을 빼고 **옛 규칙(`crossings !== 0`)** 을 다시 끼운다.
 ★ **기대(값 보기 전 등재) = 변동 0**. 근거 — 정본 37칸은 **옛 자로 이미 전부 판정된 산출**이고
   새 자는 옛 자보다 **느슨하다**(교차 조건에 거리 조건이 «그리고»로 붙는다) ⟹ `fail → pass` 만 가능하고
   `pass → fail` 은 **원리적으로 불가능**하다. 변동이 나오면 그것은 **「fail → pass」 방향뿐**이고 사실로 적는다.
 ★ 기준선 재현(CC 귀책 `C3` 처치) — 이 판의 기준선 = `기본 M` 정착의 **교차 1 · 최소 쌍거리
   1.9798624570389312 mm**(v5-25·v5-26 이 같은 수로 재현한 값).
 ★ **조립 sha 불변** — `scripts/v5AsmSha.ts` 로 T 108 · A 29 를 기점과 대조한다(갈래 D 의 자).
```

**ㄷ. ② 회귀 2칸의 자**

```
 칸 = `기본 M`(= `c100-h170-s45_M`) · `supima-L` · 판본 `asm1x: true` · v5-22~26 과 같은 진입 인자
 (그 칸의 A포즈 몸 grid27 a35 · `D_MM` 9).
 (가) = 2/2 `pass` ⟹ ③ 착수. (나) = 하나라도 fail ⟹ **③ 착수 0 · 갈래 B**.
 ★ 기대 — `기본 M` 은 **교차 1 이 판정에서 빠져 pass** · `supima-L` 은 **pass 유지**(v5-25 실측).
 ★ 함께 적는 값 — 게이트 4채널 · 어깨 걸림(규약 172) · 층3 5행 · 목선 두 비율(보고 값).
```

**ㄹ. ③ 108칸 본굽기의 절차·중단 규약**(값 보기 전)

```
 ㉠ 규약은 **v4-46 그대로**다 — 27몸 × 4사이즈 = 108칸 · 칸마다 **프로세스 분리**(Taichi 함정 ③) ·
    `.done` 파일로 재개 · 순서 **M → S → L → XL**.
 ㉡ **`asm1x: true`** 를 헤더에 싣는다(v5-22 가 고친 `worker.py` 화이트리스트 경로 · `ASM1X=1`).
 ㉢ **던짐 = 보류**(판정문 원문) — 조립이 던지는 칸은 **fail 이 아니라 «보류»**로 집계하고
    자리·부류(패널·쌍·mm)를 적는다. **처방 0.**
 ㉣ **중단 조건** — ABORT 2회 · 2호기 도달 불가가 이어질 때 · 산출이 헤더 검사에 걸릴 때 ⟹ **갈래 C**
    (사유와 **재개 절차**를 적는다 · `.done` 이 남으므로 다음 판이 이어 굽는다).
 ㉤ 중간 집계는 **metrics 에 append** 한다(다 끝나고 한 번에 적지 않는다).
 ★ **17시간 동안 `src/` 를 고치지 않는다** — 굽기 중 저장소 상태가 바뀌면 칸마다 다른 옷이 된다.
```

### §0-6 §0 사무

```
★ 계기 산출 파일명 접두 = `v5-27-` · 굽기 결과 폴더 = `gpu/bake/results/v5-27-108/`.
★ `git add` 는 **명시 경로만**(`gpu/bake/results/` 는 추적 밖이다).
★ 회차 끝에 `git diff df424dc -- src/` 를 인쇄한다(자기관통 줄 + `viol` 병기 외에 0 이어야 한다).
★ 캡처 = 대표 4칸(작은몸 S 가 던지면 작은몸 M 으로 대체 · `docs/v5/캡처/27-*.png`).
★ 이 판에서 호출한 ecc 에이전트 = 0 · 스킬 = 0 (집행 전 등재 · 종료 시 재확인)
```

---

## §1 집행

계기 = `scripts/v5Rejudge27.ts` **신설**(`src/` 0줄) · 드라이버 `scripts/bake108.py` **env 3개 확장**(기본값 불변) ·
`src/` diff = **`s4Gate.ts` 한 파일**(판정 줄 + 보고 값 `violPairs`) · 진입 인자 = v5-22~26 과 같은 것.

### §1-① 채널 허용 — **구현 · 재판정 변동 0 · 조립 sha 불변**

**ㄱ. diff 원문**(`src/v3/s4Gate.ts` · 판정 줄 · 주석 전량은 파일에 있다)

```ts
-  if (mp.hits !== S4_THRESHOLD.crossings) fails.push(`자기관통 교차 ${mp.hits} ≠ 0`);
+  if (mp.hits !== S4_THRESHOLD.crossings && mp.min < SEP - TOL_SELF)
+    fails.push(`자기관통 교차 ${mp.hits} ≠ 0 (최소 쌍거리 ${(mp.min * 1000).toFixed(4)}mm < ${((SEP - TOL_SELF) * 1000).toFixed(1)}mm)`);
```

```ts
   crossings: number; minPairM: number;
+  /** ★ v5-27 — **보고 값**(판정 미사용) · `d < SEP − TOL_SELF` 인 비인접 쌍 수 */
+  violPairs: number;
…
-    crossings: mp.hits, minPairM: mp.min,
+    crossings: mp.hits, minPairM: mp.min, violPairs: mp.viol,
```

```
 ★ **새 수 0** — `SEP`(2 mm) · `TOL_SELF`(0.1 mm) 둘 다 `consts.ts` 의 등재 상수이고 이 파일이 이미 쓰던 것이다
   (`settleNetM: TOL_SELF`). **새 식 0** — `mp.viol` 은 `instruments.ts:109` 이 **이미 계산해 반환하던** 양이다.
 ★ **`instruments.ts` 0줄**(조립 자기검사 S3b 가 같이 움직이지 않게) · `S4_THRESHOLD.crossings = 0` **삭제 0**.
 ★ 계기 `scripts/v4ProductGate.ts` 의 채널 표에 **「허용 미달 쌍」**(= `violPairs`)을 병기했다 ⟹
   이 판의 108칸 게이트 json 에 그 수가 **칸마다 남는다**.
```

**ㄴ. 정본 37칸 재판정 — ★ 변동 0**(굽기 0 · `scripts/v5Rejudge27.ts` · 산출 `gpu/oracle/export/v5-27-rejudge.json`)

| 항목 | 값 |
|---|---|
| 재판정 칸 | **37** · 건너뜀 **71**(정본 정착 blob 없음 = 판정 대조 69칸 + `blob n ≠ 장면 n` 2) |
| 옛 pass(교차 ≠ 0) | **37** |
| 새 pass(교차 ≠ 0 **그리고** 최소쌍 < 1.9 mm) | **37** |
| **변동 칸 수** | **0** |
| 교차 ≠ 0 인 칸 | **0 건** |
| 최소 쌍거리 분포 mm | 최소 **1.849210475153262** · p25 1.9595116663258845 · 중앙 **1.9828475925296913** · p75 1.9930608334973945 · 최대 1.9988797385993082 |

```
 ★ §0-5ㄴ 의 **기대(변동 0)가 그대로 발화**했다 — 정본 37칸은 **교차가 애초에 0** 이라 새 조건이 붙을 자리가 없다.
 ★★ **조건이 «비지 않았다»는 사실** — 최소 쌍거리의 **최소가 1.8492 mm 로 허용 1.9 mm 아래**다 ⟹
   그 칸에 교차가 하나라도 있었다면 **새 자로도 fail** 이다. 이 채널은 「교차를 전부 눈감는 자」가 아니다.
 ★ 방향 — 새 자는 옛 자보다 **느슨하다**(조건이 «그리고»로 붙는다) ⟹ `pass → fail` 은 원리적으로 불가능하고
   실제 변동도 0 이었다.
```

**ㄷ. `asm1x` 정착 산출 재판정**(v5-25 굽기 결과 · 굽기 0 · 같은 게이트 다리 `v4ProductGate.ts`)

| 칸 | 옛 판정 | **새 판정** | 교차 | 최소 쌍거리 mm | **허용 미달 쌍** | ③a 관통 mm(정점수) | 목선 초과비 | λ 최대 |
|---|---|---|---|---|---|---|---|---|
| `기본 M` | **fail** `자기관통 교차 1 ≠ 0` | ★ **pass** | 1 | **1.9798624570389312** | **0** | 0.3752726257434258 (927) | 1.0210830840101046 | 1.4559966679005811 |
| `supima-L` | pass | **pass** | 0 | 1.970646652526943 | **0** | 0.33132481902223526 (881) | 1.0021454460806467 | 1.4609440147599382 |

```
 ★★★ **`기본 M` 의 마지막 fail 이 사라졌다** — v5-22 부터 다섯 판을 막던 `자기관통 교차 1 ≠ 0` 이다.
 ★★★ **`허용 미달 쌍` 이 두 칸 모두 0** 이다 — 즉 **어떤 비인접 쌍도 1.9 mm 안으로 들어오지 않았다**.
   v5-26 이 「교차가 f0<t≤20 에 생겨 170 프레임 풀리지 않는다」고 쟀던 그 쌍은 **거리로는 처음부터 밖**이었고,
   이 판의 자는 그것을 **경계 사건**으로 읽는다(판정문의 「진짜 교차 아님」).
 ★ 기준선 재현(CC 귀책 `C3` 처치) — `기본 M` 의 교차 **1** · 최소 쌍거리 **1.9798624570389312 mm** 로
   v5-25·v5-26 과 **자릿수 전부** 같다.
```

**ㄹ. 조립 sha — ★ 불변**(갈래 D 의 자 · 기점 `df424dc` 워크트리 `../fit-simulator-v527base` 와 대조)

| 표 | 칸 | 결과 |
|---|---|---|
| `MODE=tpose` | 108 | **108/108 동일**(던지는 **26칸** 문언까지) |
| `MODE=apose` | 29 | **29/29 동일**(던짐 0) |

```
 ★ 당연한 결과의 «값» — `prepare()` 경로는 `s4Gate.ts` 를 **import 하지 않는다**(참조는 주석 한 줄뿐) ⟹
   판정 채널을 고쳐도 조립은 한 바이트도 움직이지 않는다. **갈래 D 아님.**
 ★ 산출 = `gpu/oracle/export/v5-27-asmsha-{tpose,apose}.tsv`(이 판의 표 · 추적 밖).
```

### §1-② 회귀 2칸 — ★ **(가) 2/2 `pass`**

2호기에서 **다시 구웠다**(job `gpu/bake/jobs/v5-27-reg2.json` · `asm1x` 조립 산출은 v5-25 와 **같은 파일** ·
총 벽시계 **1162.1 s** · 실패 0칸).

| 칸 | 정착 | s/프레임 | `vs 정본 중앙` mm | **게이트** | 교차 | 최소 쌍거리 mm | 허용 미달 쌍 | ③a 관통 mm(정점) | 목선 초과비 | 어깨 걸림(≥172) |
|---|---|---|---|---|---|---|---|---|---|---|
| `기본 M` | **f190** | 2.938 | **70.765870** | ★ **pass** | 1 | 1.9798624570389312 | **0** | 0.3752726257434258 (927) | 1.0210830840101046 | **832** ✔ |
| `supima-L` | **f190** | 2.600 | **68.930630** | **pass** | 0 | 1.970646652526943 | **0** | 0.33132481902223526 (881) | 1.0021454460806467 | **1002** ✔ |

```
 ★★★ **물리가 비트로 재현됐다** — `기본 M` 의 `vs 정본 중앙` **70.765870 mm** 는 v5-25·v5-26 과 **같은 수**이고,
   게이트 채널 값도 §1-①ㄷ 의 «굽기 0» 재판정과 **자릿수 전부** 일치한다 ⟹
   이 판의 `src/` 변경이 **물리를 건드리지 않았다**는 실측(판정만 움직였다).
 ★ 어깨 걸림은 v5-22 가 f0 에서 쟀던 수(1002 / 832)를 **정착에서도** 그대로 낸다(계기 `scripts/v5Shoulder27.ts`).
 ★ 목선 두 비율은 **보고 값**이다(v5-25 처분 · 판정 밖) — 둘 다 1 을 넘지만 통과에 영향이 없다.
```

**층3 5행**(`bandHalfMm` 9 · `sepMm` 2 · 단위 mm · p25 / 중앙 / p75 · `pressN` 은 눌림 정점 수)

| 행 | `기본 M` n · p25/중앙/p75 · press | `supima-L` n · p25/중앙/p75 · press |
|---|---|---|
| 목선 | 54 · 1.0000000447715156 / **2.271847244252619** / 6.635882407113522 · **0** | 54 · 1.0450314319401437 / **3.694429205563228** / 8.214405422538288 · **0** |
| 가슴 | 238 · 1.0051671214209994 / **3.7101871936601487** / 14.011332891464695 · **0** | 248 · 1.049281314648991 / **4.066782162031521** / 14.107264129053807 · **0** |
| 허리 | 223 · 18.15622324665093 / **61.728586993099256** / 74.0101538790064 · **0** | 233 · 23.079898004875165 / **69.29042564858443** / 90.53906802576577 · **0** |
| 밑단 | 118 · 13.33530228039645 / **25.71858463332665** / 41.1484253431398 · **0** | 122 · 31.192559600538335 / **45.1043114609863** / 52.74861837172856 · **0** |
| 소매 | 1020 · 1.0549867088849947 / **6.427472139916171** / 25.82228128709412 · **0** | 1122 · 1.0213119753646207 / **6.053287708967557** / 23.665072434234602 · **0** |

```
 ★ **5/5 성립 · 눌림 0** — 두 칸 모두 `pressN` 이 전 행에서 0 이다.
 ⟹ §0-5ㄷ 의 자로 **(가)** ⟹ **③ 108칸 본굽기 착수**.
```

### §1-③ 108칸 본굽기 — **완주**(처리 108 · 굽기 76 · 던짐 32) · ★ **pass 33 → 50**

```
 드라이버 = `scripts/bake108-v527.bat`(§1-③ 처분 · ssh 와 «분리») · 순서 M→S→L→XL ·
 착수 [1/108] ~ 끝 **[108/108] 2026-09-22 20:10:24**(로그 278줄 · 마지막 칸 1196.6s · rc 0 · f400) ·
 총 벽시계 **40500.7 s**(11.25 h) · `s/프레임` 중앙 **2.704755412857048** · 최대 **3.250785076190507**.
 집계 계기 = `scripts/v5Agg27.py`(측정만) · 산출 `gpu/oracle/export/v5-27-agg.json` ·
 문턱은 전부 인용 — ③a **0.5 mm** · 자기관통 허용 **1.9 mm** · 정착 **0.1 mm**.
```

**ㄱ. 총괄**

| 항 | 값 |
|---|---|
| 처리 | **108** |
| 굽기(조립 성립) | **76** |
| 던짐(조립 전 정지) | **32** |
| **`pass`** | **50** |
| `fail` | **26** |
| `fail` 부류(겹침 있음) | **③a 25** · 자기관통 **8** · 정착 **1** |
| 층3 5행 성립 | **76 / 76** · `press` 합 0 인 칸 **72** |
| 정착 | 수렴 **75** · 미수렴 **1** · `f` 최소 **160** · 중앙 **190** · 최대 **400** |
| `vs 정본 중앙` mm | 최소 **56.2394** · 중앙 **72.3869** · 최대 **93.9005** |
| ③a 관통 최대 mm | 중앙 **0.4382** · 최대 **12.6663** · **≤ 0.5 인 칸 51 / 76** |
| 목선 초과비(**보고 값** · v5-25 처분으로 판정 밖) | 최소 **0.934328** · 중앙 **0.999937** · 최대 **1.06152** · **> 1 인 칸 38** |

**ㄴ. 사이즈별 · 몸통별**

| 사이즈 | 굽기 | `pass` | `fail` | 던짐 |
|---|---|---|---|---|
| S | 19 | **16** | 3 | 8 |
| M | 17 | **12** | 5 | 10 |
| L | 20 | **14** | 6 | 7 |
| XL | 20 | **8** | 12 | 7 |

| 몸통 | `pass` | `fail` | **던짐** |
|---|---|---|---|
| `c87.5` | 11 | 6 | **19** |
| `c100` | 24 | 12 | **0** |
| `c122.5` | 15 | 8 | **13** |

```
 ★★★ **던짐은 「작은 몸·S」에 몰리는 것이 아니라 «몸통 극단»에 몰린다** — `c87.5` 19 + `c122.5` 13 이고
   **`c100` 은 0** 이다. 사이즈로는 오히려 고르다(M 10 · S 8 · L 7 · XL 7).
   ⟹ §0-2③ 의 질문(「던짐이 작은 몸·S 에 몰리는지」)에 대한 답은 **「작은 몸 «과» 큰 몸」**이다.
 ★ `pass` 는 사이즈가 커질수록 떨어진다 — S 16 / M 12 / L 14 / **XL 8**.
```

**ㄷ. 던짐 32칸의 부류**

| 부류 | 칸 | 요지 |
|---|---|---|
| **ㄱ 조립 자기검사** | **28** | 「옷 자기 간격 `SEP` 미달 — 최소쌍 `back↔back` …」 · 최소쌍 **0.001664 ~ 1.999 mm** |
| **ㄴ 제도 불능** | **4** | 「소매산 실수해 없음 — 암홀 반쪽 **22.20 cm** < 소매산 반폭 24.44~29.94 cm」 — `c122.5-h185-s45_M` · `-s45_S` · `-s50_M` · `-s50_S` |

**ㄹ. v4-46 대비 — ★ 「채널이 옮긴 몫」과 「조립이 옮긴 몫」**

| 판 | `pass` | `fail` | 던짐 |
|---|---|---|---|
| v4-46(그때 판정) | **33** | 63 | 12 |
| v4-46 산출을 **«지금 자»로 재판정** | **35** | 61 | (12은 산출 없음) |
| **v5-27(`asm1x`)** | **50** | 26 | **32** |

```
 ★★★ 순증 +17 의 분해 — **채널이 옮긴 몫 = 2칸**(33 → 35) · **조립(`asm1x`)이 옮긴 몫 = 15칸**(35 → 50).
   ⟹ 이 판의 이득은 「문턱을 풀어서」가 아니라 **앵커 정정 + `SH_DROP` 자체**에서 왔다.
 ★ 그 대가 — **던짐 12 → 32**(+20). 늘어난 20칸은 전부 몸통 극단(`c87.5`·`c122.5`)이다.
```

**칸 단위 이동**(108칸 전수)

| v4-46 → v5-27 | 칸 |
|---|---|
| `fail` → **`pass`** | **30** |
| `pass` → `pass` | 17 |
| `fail` → `fail` | 17 |
| `fail` → 던짐 | 16 |
| `pass` → `fail` | 9 |
| 던짐 → 던짐 | 9 |
| `pass` → 던짐 | 7 |
| 던짐 → **`pass`** | **3** |

**승격 33칸**(v4-46 에서 `pass` 가 아니었는데 이 판에서 `pass`)

```
 c100-h155-s40_M · c100-h155-s40_S · c100-h155-s45_M · c100-h155-s50_L · c100-h155-s50_M · c100-h155-s50_XL ·
 c100-h170-s40_L · c100-h170-s40_M · c100-h170-s50_S · c100-h185-s40_L · c100-h185-s40_S · c100-h185-s40_XL ·
 c100-h185-s45_L · c100-h185-s45_M · c100-h185-s45_XL · c100-h185-s50_L · c122.5-h155-s40_L · c122.5-h155-s40_XL ·
 c122.5-h155-s45_M · c122.5-h155-s45_XL · c122.5-h170-s40_L · c122.5-h170-s40_M · c122.5-h170-s45_S ·
 c87.5-h155-s45_S · c87.5-h155-s50_L · c87.5-h155-s50_M · c87.5-h155-s50_S · c87.5-h170-s45_S · c87.5-h170-s50_L ·
 c87.5-h170-s50_XL · c87.5-h185-s45_L · c87.5-h185-s45_S · c87.5-h185-s50_L
```

**강등 16칸**(v4-46 `pass` → 이 판에서 `fail`/던짐)

```
 c100-h170-s40_S(fail) · c100-h170-s50_M(fail) · c100-h185-s45_S(fail) · c122.5-h170-s45_L(던짐) ·
 c122.5-h170-s45_XL(fail) · c122.5-h170-s50_XL(던짐) · c122.5-h185-s40_L(fail) · c122.5-h185-s40_M(fail) ·
 c122.5-h185-s45_XL(fail) · c87.5-h155-s40_L(던짐) · c87.5-h155-s40_XL(던짐) · c87.5-h155-s45_M(던짐) ·
 c87.5-h155-s45_XL(fail) · c87.5-h170-s50_M(fail) · c87.5-h170-s50_S(던짐) · c87.5-h185-s50_M(던짐)
```

**ㅁ. 어깨 걸림(편입 규약 v4-52 · 문턱 172) — `pass` 50칸 전량**

```
 계기 = `scripts/v5Shoulder27.ts`(v5-27 §1-② 신설 · 값·문턱은 `v4Provide.ts:25-52` 인용) ·
 기계 = **에어**(굽기 산출 17.5 MB 를 회수해 돌렸다 · 2호기 접촉 0 · §2 사고 ㄱ).
```

| 항 | 값 |
|---|---|
| 측정 칸 | **50**(`pass` 전량) |
| **문턱 172 이상** | **50 / 50** |
| 문턱 미달 | **0** |
| 어깨선 위 옷 정점 | 최소 **584** · 중앙 **1004** · 최대 **1506** |

★ 문턱 **172** 에 가장 가까운 칸도 **584**(3.4배)다 ⟹ **어깨 걸림은 이 판의 병목이 아니다.**
⟹ 편입 집합 = **`pass` 집합 그대로**(50칸).

### §1-④ 제공 목록 재발행 — ★ **29칸 → 50칸**(초안 · 발행은 사용자·전략 세션 몫)

| 항 | 현행(v4-52) | **이 판(`asm1x`)** |
|---|---|---|
| 편입(제공) 칸 | **29** | **50** |
| 새로 드는 칸 | — | **33** |
| 빠지는 칸 | — | **12** |

**빠지는 12칸**(현행 제공 → 이 판에서 `fail`/던짐 · **제품 후퇴분**)

```
 c100-h170-s40_S · c100-h170-s50_M · c100-h185-s45_S · c122.5-h170-s45_L · c122.5-h170-s45_XL ·
 c122.5-h170-s50_XL · c122.5-h185-s40_L · c122.5-h185-s40_M · c122.5-h185-s45_XL ·
 c87.5-h170-s50_M · c87.5-h170-s50_S · c87.5-h185-s50_M
```

**새로 드는 33칸** — §1-③ㄹ 의 「승격 33칸」과 **같은 집합**이다(어깨 문턱이 50칸 전부 통과하므로 `pass` = 편입).

```
 ★ v5-11 §④ 가 등재한 위험 1건(「제공 29칸이 줄면 제품 후퇴」)의 답 — **총량은 늘어난다(29 → 50)**.
   다만 **12칸은 실제로 빠진다** ⟹ 「순증 +21 · 후퇴 12칸」이 재발행의 정확한 성격이다.
 ★ 재발행 «절차»(초안 · 이 판에서 실행 0) — ㄱ `scripts/v4Provide.ts` 를 `ASM1X=1` 로 108칸에 돌려
   `v1-provide.json`·`index-merged-108.json` 을 다시 쓴다 ㄴ `settled-<칸>.bin` 33칸을 새로 반출한다
   (v3-86 수신기 규약) ㄷ 제품 경로(`V3ProductV1.tsx`)는 **파일만 갈린다**(코드 0줄) ㄹ 빠지는 12칸은
   제품에서 「보류」로 내려간다 — **그 처분은 사용자·전략 세션이 고른다.**
```

**대표 캡처 4칸**(앞·옆 8장 · `scripts/v4AposeRender.ts` · 정사영 오프스크린 · **화면 판정 0**)

| 칸 | 자리 |
|---|---|
| 기본 M `c100-h170-s45_M` | `docs/v5/캡처/27-기본M-{1-front,2-side}.png` |
| 작은몸 S `c87.5-h155-s45_S`(승격) | `docs/v5/캡처/27-작은몸S-{1-front,2-side}.png` |
| 큰몸 XL `c122.5-h155-s45_XL`(승격) | `docs/v5/캡처/27-큰몸XL-{1-front,2-side}.png` |
| 큰키 L `c100-h185-s45_L`(승격) | `docs/v5/캡처/27-큰키L-{1-front,2-side}.png` |

★ 작은몸 **S 가 던지지 않았다** ⟹ §0-2④ 의 대체 조항(「작은몸 S 가 던지면 M 으로」)은 **쓰지 않았다**.

**v3 동결 예외 등재 초안**(문언만 · 등재는 전략 세션)

```
 「예외 N건째 — `asm1x`(1세대 배치 + 앵커 정정 `Y_ANCHOR = Y_NECK` + 어깨 경사 `SH_DROP = 0.0635 m`) ·
   자리 = `garmentScene.ts:131·290`(두 줄) + `dressRun.ts` 인자 1개 ·
   근거 = 실측표의 「총장」이 «뒷목 중심» 기준이라는 출처(`specToPattern.ts:6`)와 제도 관행 상수(1차 출처 2건) ·
   대가 = 던짐 12 → 32(몸통 극단) · 제공 29 → 50(후퇴 12칸) ·
   기본값 **off** 이고 off 경로는 T108/A29 조립 sha 불변이 확인돼 있다(§1-①).」
```

---

## §2 자기검사

```
ㄱ **`src/` diff = `s4Gate.ts` 한 파일 · +19/−2** (`git diff df424dc HEAD -- src` · §1-① 의 자기관통 채널
  거리 한정뿐이다) · **물리 0줄** · 조립 sha **T108/A29 불변**(§1-①에서 값으로 확인) · 문턱 신설 0.
ㄴ **굽기 108칸 완주**(76 굽기 + 32 던짐 · 총 40500.7 s) · **캡처 8장**(4칸 앞·옆) · **화면 판정 0**.
ㄷ **계기 신설 3건** — `scripts/v5Agg27.py`(집계) · `scripts/v5Shoulder27.ts`(어깨 걸림) ·
  `scripts/bake108-v527.bat`·`scripts/shoulder27-pass50.bat`(실행 파일) — 전부 측정·실행 전용(거동 0줄).
ㄹ ★ **사고 1 — 2호기에서 «분리 실행»이 뜨지 않는다.** `Start-Process -WindowStyle Hidden` 으로 세 번
  띄웠으나(`-FilePath <bat>` 2회 · `cmd.exe /c <bat>` 1회) **로그 파일조차 생기지 않았다**(`Test-Path` False ·
  json 0 · stderr 리다이렉트도 빈 파일). ssh 세션 종료와 함께 자식이 죽는 것으로 보인다.
  ⟹ 처분: **CPU 계기는 에어에서 돌린다** — 굽기 산출(`*.bin`·`*.json` 336파일 **17.5 MB**)을 `scp` 로
  회수해 어깨 채널 50칸·캡처 4칸을 에어에서 냈다. **굽기만 2호기**라는 규약과 같은 방향이다.
  ★ 다만 **108칸 본굽기 자체는 그 방식으로 완주했다**(`bake108-v527.bat` · 09-22 20:10 끝) — 즉
  「분리 실행이 되는 경우와 안 되는 경우」가 갈린다. **원인 미특정**(사실만 등재 · 다음 판 재료).
ㅁ ★ **사고 2 — 첫 어깨 실행이 `ENOENT` 로 죽었다**: 산출 폴더 `gpu/oracle/export/v5-27-shoulder` 가
  없어 `writeFileSync` 가 던졌다(`v5Shoulder27.ts:58`). **계기는 정상**이고 폴더만 없었다 — 만들고 재실행.
ㅂ ★ **회선 사고(등재분 유지)** — 09-22 14:01 이후 2호기가 ssh 로 닿지 않았고(09-23~26 · 핑은 응답 ·
  22번 포트 시간 초과), 원인 두 겹이 값으로 갈렸다: ① `~/.ssh/config` 의 `HostName desktop-ov72a46` 이
  «접미사 없이» 안 풀린다(`.local` 필요 · 172.30.1.100) ② 2호기 `sshd` 정지. **굽기는 그 사이에도 돌아
  09-22 20:10 에 끝나 있었다** — 즉 «닿지 않음»은 굽기 중단이 아니었다(3.5일 대기 손실).
ㅅ **pytest 미집행** — 이 판의 diff 는 `src/v3/s4Gate.ts` 뿐이고 `gpu/` 변경은 0 이다(회귀 대상 아님).
  **사실로 적는다**(v5-6 이래의 관행).
ㅇ **이 판에서 호출한 ecc 에이전트 = 0 · 스킬 = 0.**
```

---

## §4 판정 — **갈래 A**(②(가) + ③ 완주 ⟹ 집계·캡처 상신)

```
 ★ A = ②(가)+③ 완주 ⟹ 집계·캡처 상신(화면 판정 → 제공 재발행)   … **✔ 이것이다**
   B = ②(나) ⟹ 값 상신 · 108 착수 0        … ✗ ②는 2/2 pass 였다
   C = ③ 중단(사유·재개 절차) ⟹ 상신        … ✗ **완주**했다([108/108] 09-22 20:10:24 · rc 0)
   D = 조립 sha 변동 ⟹ 정지                 … ✗ T108/A29 불변
   E = 그 외 ⟹ 정지                          … ✗
 사후 갈래 신설 0.

 ① = 채널 허용 구현 · 37칸 재판정 **변동 0** · 조립 sha 불변(§1-①).
 ② = 회귀 2칸 **2/2 pass**(§1-②).
 ③ = **108칸 완주** — 처리 108 · 굽기 76 · 던짐 32 · **`pass` 50**(v4-46 의 33 에서 +17) ·
      분해: **채널 몫 2칸 · 조립 몫 15칸** · 승격 33 / 강등 16 ·
      던짐 12 → 32 이고 **전부 몸통 극단**(`c87.5` 19 · `c122.5` 13 · `c100` **0**) ·
      층3 5행 76/76 · `press` 0 인 칸 72 · 정착 수렴 75/76.
 ④ = 어깨 걸림 **50/50**(최소 584 ≫ 172) · **제공 29 → 50칸**(신규 33 · 후퇴 12) ·
      대표 캡처 4칸 8장 · 재발행 절차·동결 예외 **초안**(발행·등재는 사용자·전략 세션 몫 · 판단 0).

 ★★★ 이 판이 세운 것 — **앵커 정정 + `SH_DROP` 은 제품 커버리지를 «늘린다»**(제공 29 → 50) ·
   그 이득의 대부분은 **조립 자체**에서 왔다(채널 완화 몫은 2칸뿐) ·
   대가는 **몸통 극단 20칸의 조립 던짐**이고, 그 20칸은 지금 「보류」로 내려간다.
 ★ 다음 판의 재료(판단 0) — ㄱ 던짐 28칸의 `SEP` 미달(최소쌍 0.001664~1.999 mm)이 **옆선 `GAP_SIDE`
   도출로 풀리는 종류인지** ㄴ 제도 불능 4칸(`c122.5-h185` · 암홀 반쪽 22.20 cm < 소매산 반폭)은
   **패턴 정의역 문제**이므로 실측표 축(v5) 으로 가는 것이 맞는지 ㄷ 후퇴 12칸의 처분.
```

---

## 부록 — raw 링크 (커밋 `160782e` 고정 · 브랜치 경로 0)

- [`docs/v5/27-108칸.md`](https://raw.githubusercontent.com/banshk445/fit-simulator/160782e/docs/v5/27-108%EC%B9%B8.md) — v5-27 정본 노트(§0 사전 등재 · §1-①~④ 값 전량 · §2 사고 3건 · §4 갈래 A)
- [`docs/v5/보고/27.md`](https://raw.githubusercontent.com/banshk445/fit-simulator/160782e/docs/v5/%EB%B3%B4%EA%B3%A0/27.md) — 이 보고의 파일 정본(본문 전량 사본 · 부록은 이 커밋 «뒤»)
- [`docs/v5/v5-색인.md`](https://raw.githubusercontent.com/banshk445/fit-simulator/160782e/docs/v5/v5-%EC%83%89%EC%9D%B8.md) — 수정 — v5-27 줄 추가
- [`docs/metrics-log.md`](https://raw.githubusercontent.com/banshk445/fit-simulator/160782e/docs/metrics-log.md) — 수정 — v5-27 최종 집계 블록 append
- [`.obsidian-log/전략세션-인수인계.md`](https://raw.githubusercontent.com/banshk445/fit-simulator/160782e/.obsidian-log/%EC%A0%84%EB%9E%B5%EC%84%B8%EC%85%98-%EC%9D%B8%EC%88%98%EC%9D%B8%EA%B3%84.md) — 수정 — v5 트랙 블록 교체(v5-26 → v5-27)
- [`.obsidian-log/00-현재상태.md`](https://raw.githubusercontent.com/banshk445/fit-simulator/160782e/.obsidian-log/00-%ED%98%84%EC%9E%AC%EC%83%81%ED%83%9C.md) — 수정 — v5-27 블록 추가 · 갱신일

**캡처 8장**(전략 세션은 raw URL 로 이미지를 못 읽는다 ⟹ **업로드 필요**) — `docs/v5/캡처/27-{기본M,작은몸S,큰몸XL,큰키L}-{1-front,2-side}.png`

**계기·산출** — `scripts/v5Agg27.py` · `scripts/v5Shoulder27.ts` · `scripts/{bake108-v527,shoulder27-pass50}.bat` · 집계 json `gpu/oracle/export/v5-27-agg.json`(추적 밖).

━━━ 👉 다음: 캡처 8장을 업로드하세요 (화면 판정 → 제공 재발행 29 → 50칸 · 후퇴 12칸 처분) ━━━
