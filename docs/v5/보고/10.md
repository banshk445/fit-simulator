# v5-10 보고 — 어깨선 «능선 배치»(f0 봉제쌍 일치) 가상 실험 · 회귀 «영향 범위» 사실

git 스탬프 — 워크트리 `/Users/banshk/projects/fit-simulator` · 브랜치 **`v5-10-ridge-placement`** ·
기점 **`1602914`**(v5-9 HEAD) · §0 선커밋 **`1512687`** · 사무 **`ab8496c`** ·
계기·①②③④ **`9816c11`** · ⑤§2§4 **`dfdaf1b`** · metrics **`3620a9a`** · 볼트 **`55042e0`**.
집행 기계 — ①②는 **에어 M4(맥)**: 물리 **0프레임** · GPU 0(오프스크린 raster 뿐) · **2호기 접촉 0 · 굽기 0칸**.
**이 판에서 호출한 ecc 에이전트 0 · 스킬 0.**

## 1. 갈래 — **C**

**C = ②(나) 또는 ③(다) ⟹ 사실 상신.** ②가 **(나) 초과**로 나와 사전 등재대로 **③ 착수 0** 이다.
「능선 배치」는 어깨 폐합과 링 축소를 **f0 에 공짜로** 준다 — 그러나 **«상단 행만» 옮기는 형태로는
성립하지 않는다**(바로 아래 한 행이 10배 늘어난다).

## 2. §1 항목별 — 갈래 이름과 그 판정을 만든 수 전량

### ⓞ §0 사무 1건

`CLAUDE.md` 에 「**`git add -A` · `git add .` 를 쓰지 않는다 — 명시 경로만 스테이징한다**」를 **+7줄**
등재했다(판정문 지시 · v5-9 사고 1·2 의 처분). 본문에 그 두 사고의 값을 근거로 달았고(추적 밖 14파일
커밋 · 정정 글롭이 추적 json **480개**를 인덱스에서 뺐다) 되돌릴 때 글롭이 아니라 **그 회차가 담은 파일
목록**으로 좁히라는 절차(`git show --name-only` · 한글 경로는 `-c core.quotepath=false`)를 함께 적었다.
이 판의 **세 커밋 모두 명시 경로**이고 커밋 전에 `git diff --cached --name-only` 로 목록을 확인했다.

### ① 능선 정의 — **사실**(판정 0)

계기 = 신설 `scripts/v5RidgeProbe.ts`(측정만 · **`src/` 0줄** · `createScene` 이 이미 내보내는
함수·값만 호출: `Y_TOP` · `Y_NECK` · `NECK_A` · `SH_LEN` · `LEN_NECK` · `SLAB` · `DELTA` · `AXIS_Z` ·
`supportAt` · `boundaryOf` · `arcOn` · `bodyPoint` · `at`).

**형식화 보충 2건**(값을 만들기 «전»에 코드에서 드러났다 · 새 상수 0) —
㉠ 정의역에 **`y ≤ Y_NECK` 상한**이 필요하다. §0-4ㄱ 의 「띠 안 최상단」을 그대로 쓰면 목 쪽
(`x = NECK_A` ≈ 8.5 cm)에서 **머리 꼭대기**를 집는다. `Y_NECK` 은 `neckBaseY()` 도출값이고
`x = SW/2` 에서는 `Y_TOP < Y_NECK` 이라 검산이 유지된다. ㉡ `supportAt` 은 `|x| > SW/2` 를 버린다
(`garmentScene.ts:485`) ⟹ 목 밑동 링은 **어깨폭 안**만 본다(배치가 쓰는 그 링이다 · 새 규칙 0).

검산 —

| 칸 | `Y_TOP` m | `ridge(SW/2)` 몸 y m | **차 mm** | `Y_NECK` m | `NECK_A` cm | `h` mm | `DELTA` mm | `SH_LEN` cm | `ringRest` cm |
|---|---|---|---|---|---|---|---|---|---|
| mst-over-L(SW 52) | 1.3598263263702393 | 1.3598263263702393 | **0** | 1.4420861371069447 | 8.5031 | 3.6146 | 27.2392 | 17.4969 | 47.0421 |
| supima-L(SW 44.5) | 1.3934800624847412 | 1.3934800624847412 | **0** | 1.4420791 | 8.5057 | 3.6146 | 29.1777 | 13.7443 | 47.0498 |

★★★ **능선 규칙은 `y` 는 정하지만 `z` 를 «정하지 못한다»** — 어깨 위가 z 방향으로 «평지»라
`argmax y` 의 z 가 임의로 튄다:

| 칸 | 규칙 | 폴리라인 길이 cm | 능선/봉제선 | z 폭 mm | 인접 z차 중앙 mm | 인접 z차 최대 mm |
|---|---|---|---|---|---|---|
| mst-over-L | V1 argmax z | 126.3291 | **7.2201** | 100.093 | 17.111 | 74.625 |
| mst-over-L | V2 고원 z중앙 | 124.6811 | **7.1259** | 100.110 | 19.486 | 74.625 |
| supima-L | V1 | 97.1434 | **7.0679** | 100.093 | 17.176 | 67.369 |
| supima-L | V2 | 96.0978 | **6.9918** | 100.110 | 19.486 | 67.421 |

(V2 = 고원 `{ v ∈ B(x) : y_v ≥ y(x) − SEP }` 의 `(min z + max z)/2` · `SEP` 는 v3 가 등재한 **유일한
간극 척도**라 새 상수가 아니다.) 두 규칙 다 **7배 안팎**으로 부풀므로 **호길이는 이 규칙으로 «산출
불가»**다 — 추정치로 채우지 않았다(CLAUDE.md 계기 규약).

★★★ **「늘림 필요 여부」는 잘 정의된 하한으로 답한다** — 두 끝점의 **직선 거리**는 V1·V2 가 소수
넷째까지 일치한다:
mst-over 목쪽 **(8.503 cm, 1.43981, −0.02453)** ↔ 어깨끝 **(26.000 cm, 1.35983, −0.02668)** ·
직선 **19.2396 cm** ↔ `SH_LEN` **17.4969 cm** ⟹ **하한 비 1.09960** /
supima 목쪽 **(8.506 cm, 1.43981, −0.02453)** ↔ 어깨끝 **(22.250 cm, 1.39348, −0.02368)** ·
직선 **14.5044 cm** ↔ `SH_LEN` **13.7443 cm** ⟹ **하한 비 1.05531**.

★★★ **초과분의 정체가 «수직 상승»이다.** `Δx` 가 `SH_LEN` 과 **정확히 같다**(정의상
`SH_LEN = SW/2 − NECK_A` · `garmentScene.ts:319`) ⟹ 직선 거리가 `SH_LEN` 을 넘는 양은 **전부**
`Y_NECK − Y_TOP` = **82.2598 mm**(mst) / **48.5991 mm**(supima) 에서 온다. 즉 **평면 패턴의 어깨
이음선은 «수평 직선»인데 몸의 능선은 목 쪽으로 그만큼 올라간다.** ⟹ 「늘림 필요 여부」 = **필요하다 ·
최소 9.96% / 5.53%**(하한 · 실제 호길이는 더 길다)이고 **SW 의 함수**다(mst 82.26 ↔ supima 48.60 mm).

팔축 되재기(§0-5ㄴ) — 능선점의 `s` 가 목쪽 **−114.49 mm** 에서 어깨끝 **+74.98 mm** 까지 단조로
올라간다(mst · `r` 8.57 → 32.87 mm) ⟹ 능선의 바깥 3분의 1이 **피벗보다 팔 쪽**이다(v5-9 ①과 정합).

### ② 가상 실험(f0 · 코드 0줄) — **갈래 (나) 초과** ⟹ **③ 착수 0**

사전 등재 정의 그대로 — **상단 행만** 옮긴다(어깨 토막 = 능선 · 목선 토막 = 목 밑동 링 ·
앞뒤 어깨는 **같은 점** ⟹ 봉제쌍 거리 0) · **하위 행 접촉 0**.

| 채널 | 현행 배치(f0) | 능선 V1 · 상단 행 전부 | 능선 V2 · 상단 행 전부 | [보조] V2 · 어깨 토막만 |
|---|---|---|---|---|
| **mst-over-L** | | | | |
| 목선 링 cm | 97.2594 | **59.2017** | **59.2017** | 104.9603 |
| 링/`ringRest` | 2.0675 | **1.2585** | **1.2585** | 2.2312 |
| 상단 행 정점 | 130 | 130 | 130 | 130 |
| 여유 음수 개수 | **0** | **48** | **48** | 48 |
| 여유 최소 mm | +22.2874 | −11.0409 | **−11.3424** | −11.3424 |
| 관통 깊이 최대 mm | **0** | 11.0409 | **11.3424** | 11.3424 |
| 어깨 봉제쌍 거리 최대 / 중앙 mm | 243.9596 / 194.3379 | **0 / 0** | **0 / 0** | 0 / 0 |
| 하위행 엣지 신장 중앙 | **1.0587** | 14.5015 | **14.2375** | 9.8715 |
| 하위행 엣지 신장 최대 | 1.3156 | **24.1373** | **24.1373** | 22.5132 |
| 신장 > 2 인 정점 | **0** | **130 / 130** | **130 / 130** | 80 / 130 |
| 최대 자리 | 어깨R i=64 | 목선 i=32 | 목선 i=32 | 어깨L i=17 |
| **supima-L** | | | | |
| 목선 링 cm | 91.7762 | **60.0925** | **60.0925** | 93.5453 |
| 링/`ringRest` | 1.9506 | **1.2772** | **1.2772** | 1.9882 |
| 상단 행 정점 | 114 | 114 | 114 | 114 |
| 여유 음수 개수 | **0** | **40** | **36** | 36 |
| 여유 최소 mm | +21.9849 | −6.5356 | **−7.4239** | −7.4239 |
| 관통 깊이 최대 mm | **0** | 6.5356 | **7.4239** | 7.4239 |
| 어깨 봉제쌍 거리 최대 / 중앙 mm | 211.1720 / 183.0758 | **0 / 0** | **0 / 0** | 0 / 0 |
| 하위행 엣지 신장 중앙 | **1.0663** | 11.8215 | **11.8878** | 7.9605 |
| 하위행 엣지 신장 최대 | 1.3561 | **19.8120** | **19.8120** | 19.2582 |
| 신장 > 2 인 정점 | **0** | **114 / 114** | **114 / 114** | 64 / 114 |
| 최대 자리 | 목선 i=16 | 목선 i=28 | 목선 i=28 | 어깨R i=43 |

판정 — §0-4ㄴ 이 값 보기 «전»에 적은 (가)의 두 조건이 **둘 다 밖**이다:
㉠ 「관통 band 안」 = 몸 여유가 **−SEP(−2 mm)보다 크다** ↔ 실측 관통 깊이 최대 **11.0409 / 11.3424 mm**
(mst) · **6.5356 / 7.4239 mm**(supima) = **SEP 의 3.3~5.7배** · 음수 정점 **48/130 · 36~40/114** ⟹ **초과**.
㉡ 「신장 허용 자릿수」 = 현행 배치와 같은 자릿수 ↔ 현행 중앙 **1.0587 / 1.0663**(최대 1.3156 / 1.3561)
대 능선 중앙 **14.2375~14.5015 / 11.8215~11.8878**(최대 **24.1373 / 19.8120**) = **13.4~13.7배** ·
신장 > 2 인 정점이 **전부** ⟹ **초과**. ⟹ **갈래 (나)** · **③ 착수 0**(굽기 0칸).

★ **예측 대조** — §0-1 판정문의 넷 중 ① 「f0 목선 링 ≈ rest 자릿수」 = **부분 성립**(링/rest
**2.0675 → 1.2585** · **1.9506 → 1.2772** — 1.0 에는 못 닿는다) · ② 「끌어올림 불필요」 = **성립**
(어깨 봉제쌍 거리 최대 **0.0000 mm** — 어깨가 이미 닫혀서 시작한다) · ③④(SW 52 회복 · supima pass
유지) = **미측정**(③ 착수 0).

★★ **원인이 값으로 보인다** — 상단 행이 `Y_TOP` 에서 `Y_NECK` 으로 **82.26 / 48.60 mm** 올라가는데
하위 행은 **그 자리에 있다**. 행 간격은 `L / nvB` = 74 cm / 83 ≈ **8.9 mm** 이고 면내 rest 는
**2D 패턴 거리**(`assemble` 의 `makeInplane(tris, uv, …)`)라 변하지 않는다 ⟹ 그 한 엣지가
8.9 mm → 80~90 mm, 즉 **10배 안팎**으로 늘어난다. 최대 자리가 **목선 중앙**(i=32 / i=28)인 것이 그
증거다. ⟹ 판정문의 **「하위 행 배치 불변(국소)」 전제가 이 실험에서 값으로 깨진다** — 상단 행만
옮기는 처방은 **국소일 수 없다**.

★ [보조 · **갈래 판정에 쓰지 않았다**] 어깨 토막만 옮기면(목선 불변) 신장 중앙이 **14.24 → 9.87**
(mst) · **11.89 → 7.96**(supima)로 내려가지만 여전히 자릿수가 다르고(>2 인 정점 80/130 · 64/114)
링은 2배로 돌아간다(**2.2312 / 1.9882**) ⟹ 둘 다 산다. 사전 등재 ②는 「상단 행만」 = 어깨+목선
전부이므로 판정은 위 열로 했고, 이 열은 다음 판 설계용 사실이다.

### ③ 진단 굽기 — **착수 0**(사전 등재대로)

§0-3 이 ②(나)에 「③ 착수 0」을 걸었고 ②가 (나)로 나왔다 ⟹ **굽기 0칸 · 플래그 미구현 · 2호기 접촉 0 ·
예산 3칸 전부 미사용**. §0-4ㄷ 의 diff 초안(`place()` 분기 · `prepare` 선택 인자 · `RIDGETOP` env)은
**초안 그대로 남겼다**(코드에 넣지 않았다 · 사후 확대 0).

### ④ 회귀 «영향 범위» 사실(판단 0)

㉠ **T포즈 108 조립 sha 영향 = 측정 불가(미집행)** · 사유 = ③ 착수 0 ⟹ 플래그를 코드에 넣지 않았다.
**추정치로 채우지 않는다.** 초안의 형태상 «기대»는 0 이다(분기 조건이 `cfg.ridgeTop` 이고 기본값이
`undefined` 이며 분기 자리가 `j === nvB` 하나다) — 그러나 **그 기대를 값으로 확인하지 않았다.**
확인 방법을 등재해 둔다: ⓐ 플래그 off 재-export asm **본문 바이트** 대조 · ⓑ `scene`·`scene-bend`·
`scene-seam` **payload sha** 대조(v5-9 §1-⓪ 에서 쓴 그 계기).

㉡ **A포즈 제공 29칸 재굽기 = 필요하다.** 조립 위치가 바뀌면 정착 상태가 바뀌므로 제공 목록의 근거
(게이트 pass **그리고** 어깨위 옷 정점 ≥ 172 · v4-52 §1-② 편입 규약)가 **전부 다시 계산돼야 한다**.
**칸당 실측 기준**(이 캠페인에서 «측정된» 벽시계 · 기억·역산 0) — v5-8 `mstoverNR_XL` **989.1 s** ·
`supimaNR_L` **550.1 s** · v5-9 `mstoverRO_XL` **799.1 s** · `supimaRO_L` **697.2 s** ⟹ 4칸 평균
**758.875 s** ⟹ 29칸 직렬 **29 × 758.875 = 22,007.4 s ≈ 6.11 h**(칸마다 자식 프로세스 · v4-17 §1-① 규약).
★ **한정** — 이 네 칸은 **v5 실측표 옷**(정점 12,144 / 10,356)이고 제공 29칸은 **차트 사이즈 옷**이다
⟹ 위 값은 **자릿수 기준**이지 29칸의 실측이 아니다. 또 재굽기 전에 **조립 export 29회**와 **층3 계기
29회**가 붙는데 그 시간은 이 판이 재지 않았다.

㉢ **「정본 승격」 결정에 필요한 항목 목록**(사실 · 우선순위 0) — 1 플래그 off 비트 불변 ⓐⓑ 실측 ·
2 ②의 두 초과를 없애는 배치 설계 = **「상단 행만」이 아니라 하위 행까지 다시 놓는 판**(판정문의
「앞뒤 패널은 거기서 아래로」가 그 뜻이면 **②의 정의역이 좁았다**) · 3 평면 패턴의 어깨 이음선에
**수직 상승 9.96% / 5.53%(하한)** 을 넣을지 — 제도 변경이면 `SH_LEN`(`:319`)·`topB`(`:416-419`)가
바뀌고 **패턴 자체가 바뀐다** · 4 **v3 동결 예외** 등재(배치는 v3 정본 코드다) · 5 108칸 계정 재굽기
예산·순서 + v3 정답지 대조 기준 재수립 · 6 제공 29칸 **목록이 바뀌는지**(편입 규약은 그대로 두고 값만
다시 계산) · 7 어깨 봉제 `rest0` 이 0 이 되면 **`RAMP_N` 을 다른 봉제가 정한다**(§0-2ㅁ · 89 → ≈52 ·
예산·정착 프레임이 함께 움직인다).

### ⑤ 캡처 — **4장**(화면 판정 0)

③ 이 착수 0 이라 정착 캡처가 없다. 대신 ②의 **가상 배치 자체**를 렌더했다 —
`docs/v5/캡처/10-1-mst-now-{1-front,2-side}.png`(현행 f0) ·
`10-2-mst-ridge-{1-front,2-side}.png`(능선 V2 · 상단 행 전부 · f0 가상). 900×1200 ·
계기 `scripts/v4AposeRender.ts` · ②가 만든 위치를 `POSOUT` 으로 덤프해 `POS` 규약으로 넘겼다
(측정 0 · 새 렌더 규칙 0).

## 3. 사전 등재 기대 ↔ 실측 대조

| 등재 기대 | 기대 | 실측 | 판정 |
|---|---|---|---|
| 능선 바깥 끝 = `Y_TOP` | 같다 | 차 **0 mm**(두 옷) | **성립** |
| ① 봉제선 ↔ 능선 길이 비교(늘림 필요 여부) | 값이 나온다 | 호길이 **산출 불가**(z well-posed 아님) · **하한 1.09960 / 1.05531** | **하한으로만 성립** |
| ② f0 목선 링 ≈ rest 자릿수 | ≈ 1.0 | **1.2585 / 1.2772**(현행 2.0675 / 1.9506) | **부분 성립** |
| ② 끌어올림 불필요(봉제쌍 거리 0) | 0 | **0.0000 mm** | **성립** |
| ② 관통 band 안(> −SEP = −2 mm) | band 안 | 깊이 **11.3424 / 7.4239 mm** | **초과** |
| ② 신장 허용 자릿수(현행과 같은 자릿수) | ≈ 1.06 | 중앙 **14.2375 / 11.8878** | **초과** |
| 「하위 행 배치 불변(국소)」 | 국소다 | 바로 아래 한 행이 **10배** | **깨진다** |
| ③ SW 52 회복 정도 > 0 · supima pass 유지 | — | **미측정**(③ 착수 0) | — |

## 4. 기각·정정된 것

- **「하위 행 배치 불변(국소)」 전제 기각** — 근거 수 = 하위행 엣지 신장 중앙 **14.2375 / 11.8878**
  (현행 1.0587 / 1.0663) · 원인 = 상단 행 상승 **82.26 / 48.60 mm** ↔ 행 간격 **8.9 mm**.
- **능선 «호길이» 산출 불가 판정** — z 폭 **100.093 mm** · 인접 z차 중앙 **17.111~19.486 mm** ·
  폴리라인/`SH_LEN` **6.99~7.22배**(두 규칙 다). 추정치로 채우지 않고 **하한**으로만 답했다.
- **형식화 보충 2건**(§0-4ㄱ 규칙의 정의역) — `y ≤ Y_NECK` 상한 없으면 **머리 꼭대기**를 집는다 ·
  `supportAt` 은 `|x| > SW/2` 를 버린다.

## 5. 자기신고 — 정정·한정 4건

1 **능선 정의에 형식화 보충 2건이 필요했다**(위 §4). 둘 다 도출값·기존 코드이고 **새 상수 0**,
**값 보기 «전»**에 적었다.
2 ★ **능선 호길이는 산출 불가다** — 추정치로 채우지 않고 직선 거리 하한으로만 답했다.
3 **②는 「상단 행만」이라는 사전 등재 정의 그대로 집행했다** — 판정문의 「앞뒤 패널은 거기서 아래로」가
«하위 행도 다시 놓는다»는 뜻이면 **②의 정의역이 좁았다**. 그 판은 이 회차가 재지 않았다(사실로 적는다).
[보조] 열(어깨 토막만)은 **갈래 판정에 쓰지 않았다**.
4 **④㉠(T포즈 sha 영향)은 미집행**이다 — ③ 착수 0 ⟹ 플래그 미구현. 확인 방법만 등재했다.

미집행 = ③(사전 등재대로) · ④㉠(사유 명시) · 그 외 0. 문턱 이동 0 · 사후 갈래 신설 0 ·
화면 판정 0 · ecc 0·0. 물리 0줄 확인 = `git diff --stat 1602914 HEAD -- src/ gpu/` **빈 출력**.
이 판 diff(실측) — `CLAUDE.md` **+7/−0** · `scripts/v5RidgeProbe.ts` **신설 +230** · 노트 **+175**
(§0 뒤) · 캡처 4. 굽기 **0칸**(예산 ≤ 3칸 전부 미사용).
**`git add -A` 금지 규약**(이 판 신설)을 세 커밋 모두에서 지켰다.

## 6. 다음 판 설계에 필요한 새 사실

㉠ **어깨 폐합과 링 축소는 f0 배치로 «공짜»다** — 봉제쌍 거리 0.0000 mm · 링/rest 2.0675 → 1.2585.
즉 v5-9 가 특정한 「팔을 관통하는 폐합 경로」는 **배치를 바꾸면 사라진다**.
㉡ **대가는 «상단 행만» 옮긴 데서 온다** — 관통 11.34 mm · 하위 엣지 10배. 다음 판의 정의역은
**하위 행까지 다시 놓는 배치**다.
㉢ **평면 패턴의 어깨선에 «수직 상승»이 없다** — 필요한 늘림의 하한이 **9.96% / 5.53%** 이고 SW 의
함수다. 이것은 배치가 아니라 **제도(`SH_LEN`·`topB`)** 의 문제로도 풀 수 있다.
㉣ **능선의 z 를 정하는 규칙이 없다** — 이 판의 두 규칙 다 산출 불가. 새 규칙이 필요하다.
㉤ **정본 승격의 값비싼 항목이 값으로 섰다** — 29칸 재굽기 **≈ 6.11 h**(자릿수) + v3 동결 예외 등재.
㉥ **어깨 `rest0` 이 0 이 되면 `RAMP_N` 이 89 → ≈52 로 바뀐다** — 예산·정착 프레임이 함께 움직인다.

## 부록 — raw 링크 (SHA 고정)

신규
- `docs/v5/10-능선배치.md` — 회차 노트 정본(§0~§4 · 373줄) · 커밋 `dfdaf1b`
  https://raw.githubusercontent.com/banshk445/fit-simulator/dfdaf1b/docs/v5/10-%EB%8A%A5%EC%84%A0%EB%B0%B0%EC%B9%98.md
- `docs/v5/보고/10.md` — 이 보고
- `scripts/v5RidgeProbe.ts` — ①② 계기(능선 정의 · f0 가상 배치 · 측정만 · `src/` 0줄) · 커밋 `9816c11`
  https://raw.githubusercontent.com/banshk445/fit-simulator/9816c11/scripts/v5RidgeProbe.ts
- `docs/v5/캡처/10-1-mst-now-{1-front,2-side}.png` · `10-2-mst-ridge-{1-front,2-side}.png` — 캡처 4장

수정
- `CLAUDE.md` — 「`git add -A` 금지 · 명시 경로만」 등재 · 커밋 `ab8496c`
  https://raw.githubusercontent.com/banshk445/fit-simulator/ab8496c/CLAUDE.md
- `.obsidian-log/전략세션-인수인계.md` — v5 트랙 **교체**(갈래 C) · 커밋 `55042e0`
  https://raw.githubusercontent.com/banshk445/fit-simulator/55042e0/.obsidian-log/%EC%A0%84%EB%9E%B5%EC%84%B8%EC%85%98-%EC%9D%B8%EC%88%98%EC%9D%B8%EA%B3%84.md
- `.obsidian-log/00-현재상태.md` — v5-10 블록 삽입 · 커밋 `55042e0`
  https://raw.githubusercontent.com/banshk445/fit-simulator/55042e0/.obsidian-log/00-%ED%98%84%EC%9E%AC%EC%83%81%ED%83%9C.md
- `docs/v5/v5-색인.md` — v5-10 등재 · 커밋 `55042e0`
  https://raw.githubusercontent.com/banshk445/fit-simulator/55042e0/docs/v5/v5-%EC%83%89%EC%9D%B8.md
- `docs/metrics-log.md` — v5-10 블록(수치 정본) · 커밋 `3620a9a`
  https://raw.githubusercontent.com/banshk445/fit-simulator/3620a9a/docs/metrics-log.md

━━━ 👉 다음: 전략 세션에 이 보고를 전달하세요 (CC 대기) ━━━
