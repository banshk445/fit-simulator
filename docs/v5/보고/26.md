# v5-26 보고 — 기본 M 교차 1 의 자리·기전 · S 암홀↔소매 닫힌 식 · 처방 0 · **갈래 C**

git 스탬프 — 브랜치 `v5-26-last-two` · 기점 `27ad7fa`(v5-25 HEAD) ·
워크트리 `/Users/banshk/projects/fit-simulator` · 커밋 §0 `b94be83` · 계기 `bde1838`(①) · 하네스 `145556f` ·
계기 `872b082`(②) · §1-①②③ `7f47a1a` · §1-①㉢·④·§2·§4 `8daa722` · metrics+볼트 `6bb3467`.
★ **`src/` diff = 0**(`git diff 27ad7fa -- src/` 빈 출력) · 물리 0줄 · **처방 0** ·
굽기 **1칸**(궤적용) · 캡처 **0장**(③ 미착수) · 문턱 신설·이동 0.
이 판에서 호출한 ecc 에이전트 = **0** · 스킬 = **0**.

## 1. 갈래와 사유

**갈래 C**(자리 = 물리 ⟹ 사실 상신 · 처분 전략 세션). 사유 한 줄 —
`기본 M` 의 정착 교차 1 은 **f0 에 없고**(네 판본 전부 교차 0 · 최소쌍 2.00000) `0 < t ≤ 20` 프레임에서 생기므로
§0-5ㄷ 에 값 보기 **전** 고정한 자에 따라 **물리 자리**이고, 그 자에 따라 **③ 처방은 0**이다.

A 아님(③ 미착수) · B 아님(같은 이유 · fail 값 없음) · **D 아님**(`src/` diff **0** · 변경은 하네스뿐) · E 아님.
사후 갈래 신설 0.

## 2. §1 항목별 판정과 그 판정을 만든 수 전량

### §0-6 처치 이행 — 기준선 재현 «비트 일치»

CC 귀책 `C3`(v5-18)의 처치 = 「새 계기는 기준선 한 칸을 먼저 재현한다」. 이행 값 —
정착 `기본 M` 에서 `minPairDist` 의 `min` = **1.9798624570389312 mm** · `hits` = **1**.
v5-25 게이트 값과 **자릿수 전부** 같고, 새로 만든 전수 열거도 **교차 1개**로 일치한다.

### ① `기본 M` 교차 1 — **자리 = 물리다**

**ㄱ. 정착 상태의 교차 쌍(전수 열거 · 교차 1개)**

| | 삼각형 | 정점(패널 · i · j) | y mm | 몸거리 mm |
|---|---|---|---|---|
| A | 15808 | `back`(i16, j67) · `back`(i17, j67) · `back`(i16, j68) | 1452.48 / 1455.04 / 1461.40 | 10.682 / 9.285 / 18.600 |
| B | 15925 | `back`(i17, j68) · `back`(i17, j69) · `back`(i16, j69) | 1464.17 / 1457.23 / 1455.13 | 17.174 / 12.563 / 13.062 |

제도 분할 — `nuB` 58 · `nvB` 69 · `N_sh` **16** · `N_nk` 26 · `N_side` 44 · `N_arm` 25 · `n` 9,280 · 삼각형 17,808.

자리 — `back` 패널 · 열 **i 16~17** · 행 **j 67~69 = nvB−2 ~ nvB** · y **1452 ~ 1464 mm** ·
몸거리 **9.3 ~ 18.6 mm**(몸에서 떠 있다 ⟹ v5-22 §1-②ㅁ 의 「겨드랑이 대역 밖」과 일치).
`N_sh` = 16 이므로 **`i16` = 어깨 토막의 «안쪽 끝»**(목선과 만나는 자리) · **`i17` = 목선 구간 첫 열** ⟹
**어깨 토막과 목선이 만나는 «모서리»**이고, 행은 **봉제선과 그 바로 아래 두 행**이다.
v5-11 §①-5 가 등재한 한계 **#5(앞뒤 공통 `topB`)**·**#7(이름 어긋남)** 이 걸린 바로 그 이음매이고,
v5-25 ②에서 S 의 「둘 다」 판본이 붙은 자리(i 22~24 · 목선 구간)와 **같은 계열**이다.

**ㄴ. f0 에도 있는가 — 없다**(네 판본 · 같은 계기)

| 판본 | `n` | `nuB` / `nvB` / `N_sh` | 던짐 | 최소쌍 mm | **전수 교차** |
|---|---|---|---|---|---|
| off | 9490 | 54 / 76 / 14 | 없음 | **2.00000** | **0** |
| **앵커만** | — | — | **던짐** `옷 자기 간격 SEP 미달 — 최소쌍 back↔sleeveL 1.628mm` | 1.628 | — |
| `SH_DROP`만 | 9280 | 58 / 69 / 16 | 없음 | **2.00000** | **0** |
| **둘 다**(`asm1x: true`) | 9280 | 58 / 69 / 16 | 없음 | **2.00000** | **0** |

⟹ `asm1x: true` 의 f0 에서 `기본 M` 은 **교차 0 · 최소쌍 2.0**(통과)이고 교차 1 은 **정착에서만** 생긴다 ⟹
**자리 = 물리 · ③ 처방 0**(갈래 C).
부수 사실 — **`기본 M` 도 «앵커만» 켜면 던진다**(`back↔sleeveL` **1.628 mm**) ⟹ **S 와 같은 부류**이고
`SH_DROP` 이 더해지면 통과한다(2.0). 「앵커가 암홀을 소매로 밀어붙이고 `SH_DROP` 이 그것을 덜어낸다」는
v5-25 의 사실이 **S 만의 일이 아니라 두 칸에서 같은 방식으로** 나타난다.

**ㄷ. 궤적(①㉢) — `f0 < t ≤ 20` 에 생겨 «풀리지 않는다»**

하네스에 중간 프레임 위치 덤프를 더해(`DUMP_EVERY` 가 있을 때만 · 없으면 기존 경로 바이트 불변)
`기본 M` 한 칸을 다시 구웠다(2호기 · `RAMP_N` 64 · 정착 f190 · **벽시계 490.1 s** ·
`vs 정본 중앙` **70.765870 mm** = v5-25 와 같은 수 ⟹ 물리 재현).

| 프레임 | **전수 교차** | `minPairDist.min` mm | `hits` | 자리 |
|---|---|---|---|---|
| **f0**(조립) | **0** | 2.00000 | 0 | — |
| f20 | **1** | 2.00000 | 1 | `back(i16,j67) ↔ back(i17,j68)` |
| f40 | 1 | 2.00000 | 1 | 〃 |
| f60 | 1 | 1.99466 | 1 | 〃 |
| f80 | 1 | 1.98006 | 1 | 〃 |
| f100 | 1 | 1.97987 | 1 | 〃 |
| f120 | 1 | 1.97986 | 1 | 〃 |
| f140 | 1 | 1.97986 | 1 | 〃 |
| f160 | 1 | 1.97986 | 1 | 〃 |
| f180 | 1 | 1.97986 | 1 | 〃 |
| **f190**(정착) | **1** | **1.9798624570389312** | **1** | 〃 |

생기는 때 — f0 에 0, f20 에 1 ⟹ **`0 < t ≤ 20` 프레임**. `RAMP_N` = 64 이므로 **램프의 31% 이전**이다.
그 뒤 **170 프레임 동안 자리도 수도 바뀌지 않는다** — 같은 삼각형 쌍이 끝까지 남고 `min` 은
2.00000 → 1.97986 으로 **0.014 mm** 만 줄고 굳는다 ⟹ **램프도 자기충돌 해소도 이것을 풀지 못하고**,
점점 나빠지는 것이 아니라 **초반에 한 번 생기고 고정**된다.
한정 — `ramp` 모드는 창 경계(`N_WIN` 10)에서만 상태를 읽으므로 덤프가 **10의 배수 프레임에만** 떨어진다.
`f0~f10` 으로 반 줄이려면 굽기 한 칸이 더 필요하다 ⟹ **미집행**.

### ② S 암홀 ↔ 소매 — **닫힌 식이 나왔다**

세 칸 × (off · `asm1x: true`) · 봉제 대장의 암홀 쌍을 **직접** 써서 쟀다(별도 배열 0 · 함정 12).

**ㄱ. 암홀 중심 y 의 이동 = `(Y_NECK − Y_TOP) − SH_DROP`**

| 칸 | `Y_TOP` mm | `Y_NECK` mm | `Y_NECK − Y_TOP` | **식값**(− 63.5) | 암홀 중심 y off → on | **실측 Δy** |
|---|---|---|---|---|---|---|
| `기본 M` | 1403.939 | 1442.089 | **38.151** | **−25.349** | 1296.443 → 1271.093 | **−25.350** |
| `supima-L` | 1393.480 | 1442.079 | **48.599** | **−14.901** | 1287.317 → 1272.416 | **−14.901** |
| **`작은몸 S`** | 1238.046 | 1315.769 | **77.723** | **+14.223** | 1129.432 → (on 던짐) | 측정 불가 |

식과 실측이 **소수 셋째 자리까지 같다** — M **−25.349 ↔ −25.350** · L **−14.901 ↔ −14.901** ⟹
암홀은 `(Y_NECK − Y_TOP) − SH_DROP` 만큼 움직인다(앵커가 올리고 `SH_DROP` 이 내린다 · 두 효과의 합).
**부호가 칸을 가른다** — `Y_NECK − Y_TOP` 가 `SH_DROP`(63.5 mm)보다 크면 암홀이 **올라간다**.
`작은몸 S` 가 **유일하게 양수**다(**+14.223 mm**) ⟹ **S 에서만 암홀이 소매 관 쪽으로 올라간다.**
v5-10 등재값으로 넓히면 `Y_NECK − Y_TOP` = 48.5991(SW 44.5) / **82.2598**(SW 52) ⟹
**SW 52 계열(mst-over)도 +18.76 mm 로 올라간다**(이 판은 그 칸을 재지 않았다 · 사실만).

**ㄴ. 소매는 «따라오지 않는다» — Δs 가 Δy 의 1/10 이다**

| 칸 | 소매 `s` 중앙 off → on | **Δs** | 암홀 Δy | 비 |
|---|---|---|---|---|
| `기본 M` | −349.25 → −351.77 | **−2.52 mm** | −25.350 | **0.099** |
| `supima-L` | −354.47 → −356.45 | **−1.98 mm** | −14.901 | **0.133** |

판정문의 후보 「소매 축 이동은 따라오나 반경/높이 관계가 어긋남」이 **성립하되 더 세다** —
축 이동조차 **2 mm 뿐**이고 몸판 암홀은 **15~25 mm** 움직인다. 기전은 코드에 있다 —
v4-37 승인 문언이 「`p ← p + s·AX` · **수직 성분 적용 0(램프의 몫)**」이라 소매는 **축 방향으로만** 보정되고
**높이는 따라가지 않는다**. §0-5ㄴ 에 값 보기 **전** 적은 판정 규칙(「Δy 가 크고 Δs 가 작으면 성립」)이
**그대로 발화**했다.

**ㄷ. 암홀 봉제쌍 거리 — 앵커가 켜지면 «더 벌어진다»**(mm · 최소 / 중앙 / 최대)

| 칸 | 판본 | 암홀앞R | 암홀뒤R |
|---|---|---|---|
| `기본 M` | off | 53.7949 / **69.9064** / 112.1424 | 42.2189 / **61.6104** / 111.3590 |
| `기본 M` | on | 74.0210 / **99.6527** / 132.9521 | 63.7548 / **112.7421** / 132.1941 |
| `supima-L` | off | 56.2822 / **74.8261** / 113.8308 | 46.0017 / **69.8709** / 114.7398 |
| `supima-L` | on | 69.8764 / **94.0556** / 127.3973 | 57.6960 / **106.6872** / 126.6010 |
| **`작은몸 S`** | off | 78.5177 / **135.2996** / 146.6004 | 76.4810 / **151.8101** / 177.4645 |

앵커가 켜지면 봉제쌍이 **30 ~ 51 mm 더 벌어진다**(M 중앙 69.9 → 99.7 · 61.6 → 112.7) ⟹
램프가 닫아야 할 양이 늘어난다.
**`작은몸 S` 는 off 에서 이미 중앙 135 ~ 152 mm** 로 M·L(62 ~ 75)의 **약 두 배**다 ⟹
S 는 «출발선»부터 암홀 쌍이 멀고, 거기에 암홀이 **위로** 14.2 mm 더 가면 소매 관과 만난다.
소매 관 `ρ` 중앙은 세 칸 모두 **332 ~ 348 mm** 로 비슷하고 `SLV_R` 계열의 변화는 작다 ⟹
**갈린 것은 «반경»이 아니라 «높이»다**(판정문의 「반경/높이 관계」 중 높이 쪽).

### ③ 처방 — **0**

㉠ `기본 M` 의 교차 1 은 **물리 자리**다(①ㄴ·ㄷ) ⟹ §0-5ㄷ 의 자 그대로 처방 0.
㉡ `작은몸 S` 는 **조립 자리**가 확정이지만(f0 에서 던진다), ②가 낸 닫힌 식이 가리키는 최소 처방은
「암홀이 올라가지 않게 `SH_DROP` 을 `Y_NECK − Y_TOP` 이상으로」이고 그것은
**v5-12 검수 결정 ①이 «기각»한 「몸 도출」 계열**이다 — 설계서 (b) 원문
「**옷이 몸에 종속된다** — 같은 옷을 다른 몸에 입히면 패턴이 달라진다. 이것은 v5 의 축
(「옷을 데이터로 받는다」)과 **정면으로 어긋난다**」 · v5-12 §0-1 결정 ① 「**몸 도출 기각**」.
⟹ **CC 는 그 처방을 고르지 않는다**(폐쇄 계열 · 처분은 전략 세션).
⟹ **③ 착수 0** — 회귀 굽기 0칸 · 캡처 0장(굽기는 ①㉢ 궤적용 한 칸뿐).

### ④ 승격 재료 갱신 — 판단 0

ㄱ **108 계획** — v5-25 의 순서(자 확정 → 37칸 확인 → A포즈 29칸 재굽기 → 제공 재발행)는 그대로다.
이 판이 더하는 것 — `기본 M` 은 **물리 자리의 교차 1** 로 막히므로 **조립을 고쳐도 풀리지 않는다** ⟹
승격 전에 **그 한 개를 어느 층위에서 다룰지**(램프 · 자기충돌 해소 · 게이트 정의)가 먼저 정해져야 한다.
ㄴ **«보류 칸» 규약** — 「조립 자기검사가 던지는 칸」에 더해 **「정착 게이트가 교차로 막히는 칸」**도 같은 방식으로
보류한다. 이 판까지의 실측으로 두 부류가 각각 하나씩 있다 — `작은몸 S`(조립 던짐) · `기본 M`(정착 교차 1).
몇 칸이 보류인지는 **여전히 모른다**(108 전수 미집계 · v5-25 와 같은 한정).
ㄷ **v3 동결 예외** — `src/` 를 한 줄도 고치지 않았다(처방 0) ⟹ 등재 항목 추가 **0**.
하네스(`worker.py` 덤프)는 `src/` 밖이고 `DUMP_EVERY` 가 없으면 기존 경로 **바이트 불변**이다.
ㄹ **다음 판이 쓸 수 있는 자** — ②의 닫힌 식 `(Y_NECK − Y_TOP) − SH_DROP` 은 칸마다 **부호를 미리 준다** ⟹
108 중 「암홀이 올라가는 칸」을 **굽기 0 으로** 셀 수 있다(이 판은 세지 않았다).

## 3. 사전 등재 기대 ↔ 실측 대조

| 사전 등재(값 보기 전) | 실측 | 비/판정 |
|---|---|---|
| §0-5ㄷ 「f0 에 있으면 조립 자리 · 없으면 물리 자리」 | f0 교차 **0** · 정착 **1** | **물리 자리**(갈래 C) |
| §0-5ㄴ 「Δy 크고 Δs 작으면 «축 추종·높이 미추종» 성립」 | Δy 25.350 / 14.901 ↔ Δs 2.52 / 1.98 | 비 **0.099 / 0.133** ⟹ **성립** |
| §0-4ㄷ(b) 「앵커가 옷을 `Y_NECK − Y_TOP` = 77.72 mm 올린다(S)」 | 식이 세 칸에서 닫혔다(M·L 실측 일치) | **일반화**(− `SH_DROP` 항 추가) |
| 판정문 「(b) S 는 암홀 첫 열 i0 이 소매 관과 만남」 | S off 봉제쌍 중앙 135~152 mm(M·L 의 두 배) + 식값 **+14.223** | **기전 확인**(높이 쪽) |
| 기준선 재현(CC 귀책 C3 처치) | `min` 1.9798624570389312 · `hits` 1 | **비트 일치** |
| 굽기 예산 ≤ 3칸 | **1칸**(궤적) · 벽시계 490.1 s | 예산 안 |
| `vs 정본 중앙`(같은 칸 재현) | **70.765870 mm** | v5-25 와 **같은 수** |

## 4. 기각·정정된 것과 그 근거 수

- **정정(v5-25 캡처)** — `25-L`·`25-M` 은 `22-L`·`22-M` 과 **md5 가 같다**
  (`b3804b148bcd0cdc04530c1493224963` · `4bf8ef38b34087821a5b1ff650fff76b`). 정착 상태가 비트 동일이라
  같은 그림이 나오는 것이 **맞다**. v5-25 보고의 「바이트 크기가 같다」를
  **「md5 가 같다 = 픽셀까지 같은 파일이다」**로 정정한다(「새로 그린 캡처」라는 인상 방지).
- **좁혀진 것(기각 아님)** — 판정문의 후보 「소매 축 이동은 따라오나 반경/높이 관계가 어긋남」은
  **반경이 아니라 «높이»**로 좁혀졌다(소매 관 `ρ` 중앙 332~348 mm 로 세 칸 비슷).
- **기각된 처방 계열** — `SH_DROP ≥ Y_NECK − Y_TOP` 은 **v5-12 검수 결정 ①(몸 도출 기각)** 으로 닫혀 있다.
- **v5-22 의 「겨드랑이 대역 밖」이 자리로 특정됐다** — 어깨↔목선 이음 모서리(`back` i16~17 · j67~69).

## 5. 자기신고 — 절차 이탈 · 한정 · 미집행 · 귀책

- **절차 이탈 0** · **귀책 0**(전략 세션 · CC 모두). §0 은 집행 **전** 선커밋(`b94be83`).
- **`src/` diff 0** — 회차 끝에 `git diff 27ad7fa -- src/` 가 빈 출력임을 확인했다. 고친 것은 하네스 한 자리
  (`gpu/bake/worker.py` 의 `DUMP_EVERY` 덤프)뿐이고, 그 변수가 없으면 기존 경로가 바이트 불변이다 ⟹
  조립 sha 대조를 **돌리지 않았다**(변경이 `src/` 밖 · 물리 0줄) ⟹ 갈래 D 는 정의역 밖이지만
  「돌려서 확인하지는 않았다」를 신고한다.
- **①㉢ 해상도 한정 · 미집행** — 덤프가 **10의 배수 프레임**에만 떨어져(`ramp` 는 창 경계 `N_WIN` 10 에서만
  읽는다) 발생 시점을 `f0 < t ≤ 20` 까지만 좁혔다. `f0~f10` 으로 반 줄이기는 **미집행**(굽기 한 칸 필요).
- **②의 S on 값은 못 쟀다** — 조립이 던지므로 `asm1x: true` 의 암홀 중심 y 가 없다. 식값 **+14.223 mm** 는
  **예측**이고 실측이 아니다(근거 = M·L 에서 식이 소수 셋째 자리까지 맞았다).
- **`mst-over-L`(SW 52) 미측정** — v5-10 등재값으로 식에 넣으면 **+18.76 mm** 이지만 이 판의 측정이 아니다.
- **문턱 신설·이동 0** — 쓴 수는 `SEP`·`THICK`·`ASM2_SH_DROP`·`Y_NECK`·`Y_TOP`·`N_sh`·`nuB`·`nvB`·`N_side`
  뿐이고 전부 등재된 양이다. **손 상수 0.**
- **물리 0줄 · 굽기 1칸 · 캡처 0장 · ecc 에이전트 0 · 스킬 0.**
- **집행 환경** — `git` 이 Xcode 라이선스 미동의로 멈춰 `DEVELOPER_DIR=/Library/Developer/CommandLineTools`
  로 우회했다(v5-23 부터 **네 판 연속** · 기계 설정·저장소 파일 변경 0).

## 6. 다음 판 설계에 필요한 새 사실

㉠ **`기본 M` 의 마지막 교차는 조립이 아니라 물리가 만든다** — f0 **0** → f20 **1** → 끝까지 **1** ·
같은 쌍 · `min` 2.00000 → 1.97986 으로 **굳는다**. 자리는 **어깨 토막 안쪽 끝 ↔ 목선 첫 열의 모서리**이고
몸에서 **9.3~18.6 mm 떠 있다** ⟹ 몸 충돌이 아니라 **한 패널 안의 자기 접힘**이다(v5-17 이 남긴 층위).
㉡ **암홀의 오르내림이 한 식으로 닫힌다** — `(Y_NECK − Y_TOP) − SH_DROP` · **부호가 칸을 가른다** ⟹
108 중 「올라가는 칸」을 **굽기 0 으로** 셀 수 있다(미집계 · 다음 판의 값싼 항목).
㉢ **소매는 높이를 따라오지 않는다** — Δs **2 mm** ↔ Δy **15~25 mm**. 기전은 v4-37 예외의
「수직 성분 적용 0(램프의 몫)」이고, 고치려면 **그 승인 문언을 건드리는 일**이 된다.
㉣ **앵커는 두 칸에서 같은 방식으로 던진다** — `기본 M` 도 「앵커만」이면 `back↔sleeveL` **1.628 mm** ⟹
S 는 특수 칸이 아니라 **부호가 뒤집힌 같은 계**다.
㉤ **보류 규약이 두 부류로 넓어진다** — 조립 던짐(`작은몸 S`) · 정착 교차(`기본 M`).

## 부록 — 캡처와 raw 링크

캡처 **0장** — §1-③ 이 **미착수(처방 0)** 라 회귀 굽기가 없고, ①㉢ 의 궤적 굽기는 숫자 산출뿐이다
(v5-25 의 `25-{L,M}-{1-front,2-side}.png` 가 이 판의 `asm1x` 화면 정본으로 **그대로 유효**하다 ·
위 4절의 md5 정정 참고).

- 회차 노트 `docs/v5/26-마지막둘.md`
- 보고 `docs/v5/보고/26.md`
- metrics `docs/metrics-log.md`(2026-09-18 v5-26 블록)
- 볼트 `.obsidian-log/전략세션-인수인계.md`(v5 트랙 교체 + 갱신일 줄 교체)
- 색인 `docs/v5/v5-색인.md`
- 계기 `scripts/v5CrossAt.ts`(교차 전수 열거 · 자리) · `scripts/v5ArmSleeve.ts`(암홀 봉제쌍 · `(s,ρ,φ)` · Δy/Δs)
- 하네스 `gpu/bake/worker.py`(`DUMP_EVERY` 중간 프레임 덤프) · 잡 `gpu/bake/jobs/v5-26-trail.json`
- 코드 `src/` **변경 0**

━━━ 👉 다음: 전략 세션에 이 보고를 전달하세요 (CC 대기) ━━━
