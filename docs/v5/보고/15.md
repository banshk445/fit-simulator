# v5-15 보고 — S4 붕괴 처방 2후보 가상 실험(f0 · 조립만)

git 스탬프 — 브랜치 **`v5-15-s4-fix`** · 기점 **`c0c0ea6`**(v5-14 HEAD) · §0 선커밋 **`aef7d74`** ·
사무 **`ac672fd`** · 구현 **`9528053`** · 노트 **`0d24157`** · metrics·볼트 **`ea21b49`**.
집행 기계 — **에어 M4(맥)**: 물리 0프레임 · **2호기 접촉 0 · 굽기 0칸 · 캡처 0장**.
**ecc 에이전트 0 · 스킬 0.**

## 1. 갈래 — **B**

**B = ②(다) ⟹ 값 상신.** 여섯 조합(세 칸 × 두 후보)이 **전부** 조립 자기검사에서 던졌다 ⟹ 사전
등재대로 **승자를 고르지 않고** ③④⑤ 를 **착수 0** 했다. ★ 그러나 **두 후보가 각자 «다른 것»을
고쳤다** — (A)는 «걸음», (B)는 «관통» — 그리고 (B)의 **새 실패 부류**와 ①의 **곡률 예보 적중**이
다음 판의 재료다.

## 2. §1 항목별 — 값 전량

### ⓞ 사무 2건

**ㄱ 귀책 17건째 등재** — 「국소 좌표계 처방(`b = t × n`)을 발주했다 — `n ⊥ ẑ` 를 «산수»로 걸렀어야
한다」(실측 89.03~93.29° · 대부분 90.00° · 상단 간격은 세 칸·전 `i` 에서 **정확히 2.0000 mm** 로
작동하고 있었다). **ㄴ 「폐쇄된 후보 계열 1건」 등재** — **«면 법선에서 축을 만드는» 처방 계열**은
어깨 위 기하에서 **전부 전역 `z` 와 같아진다** ⟹ 다시 발주하지 않는다.

### ① 산수 사전 점검

**(A) 걸음 상한의 유도**(등재 자 인용 · 손 상수 0) — 행 간격(설계) `L / nvB` = **9.8551**(기본 M ·
nvB 69) · **9.7368**(mst · 76) · **9.7260 mm**(supima · 73). 등재 자 = `THICK` **1 mm** ·
`SEP` **2 mm** · `TOL_SELF` **0.1 mm**. 비 **행 간격/`THICK` = 9.855 / 9.737 / 9.726** ⟹ `THICK` 이
**정확히 «한 자릿수 아래»**다(`SEP` 는 1/4.9 = 반 자릿수 · `TOL_SELF` 는 1/97 = 두 자릿수 아래로
반복이 100회 급이 된다). ⟹ **걸음 상한 = `THICK`** · 반복 상한 = **`NY`(61)**(이 함수가 이미 능선
표본 상한으로 쓰는 등재 값) · 수렴 = `TOL_SELF`. **새 수 0.** 예상 반복 ≈ 10 mm ÷ 1 mm = **10회 급**
⟹ 실측 **20~21회**(자릿수 정합).

**(B) 곡률 사전 점검** — `R ≈ Δ / ∠(n̂(c), n̂(c + d·Δ))` · `Δ = SEP`(등재 자) · `d` = 접평면 낙하 방향.

| 칸 | **R 최소 mm** | R 중앙 mm | 유한 표본 | **R < 행 길이 표본** | 행 길이 mm |
|---|---|---|---|---|---|
| `c100-h170-s45_M` | **3.974** | 15.327 | 22 | **8 / 22** | 9.8551 |
| `mst-over-L` | **7.316** | 27.676 | 6 | **2 / 6** | 9.7368 |
| `supima-L` | **23.421** | 56.252 | 8 | **0 / 8** | 9.7260 |

§0-4ㄷ 이 **값 보기 «전»**에 적은 기준 — 「R < 행 길이」 구간이 하나라도 있으면 (B)는 그 구간에서
**접평면 한 걸음이 표면을 벗어난다** ⟹ 그 사실을 값으로 적고 ②를 함께 읽는다(**후보를 미리 떨구지
않는다**). ⟹ **기본 M(8/22) · mst(2/6) 에서 걸리고 supima(0/8) 에서는 안 걸린다.**
★ 이 예보가 ②에서 **그대로 맞았다**(아래 ㉢).

### ② 가상 실험 6조합 — **(다) 둘 다 실패**

| 칸 | 후보 | 자기검사(삼각형 쌍) | **최근접 정점쌍 mm** | 그 자리 | S4 반복/상한 | 걸음 상한 | S4 최대이동 첫 / 끝 mm |
|---|---|---|---|---|---|---|---|
| `c100-h170-s45_M` | **A** | `front↔back` 0.0004334 | **1.58249** | front(i11,j66)↔back(i11,j67) | **21 / 61** | **1 mm** | **1** / 0.0628 |
| `mst-over-L` | **A** | `front↔back` 0.0001976 | **1.32808** | front(i18,j73)↔back(i17,j73) | **20 / 61** | **1 mm** | **1** / 0.0944 |
| `supima-L` | **A** | `front↔back` 0.00008126 | **0.93036** | front(i46,j70)↔back(i47,j71) | **21 / 61** | **1 mm** | **1** / 0.0790 |
| `c100-h170-s45_M` | **B** | **`back↔back`** 0.0007549 | **0.52353** | front(i43,j69)↔back(i42,j68) | **1 / 8** | — | **0** / **0** |
| `mst-over-L` | **B** | `front↔back` 0.0005646 | **0.93855** | front(i48,j75)↔back(i49,j76) | **1 / 8** | — | **0** / **0** |
| `supima-L` | **B** | **`front↔front`** 0.0009134 | **0.67310** | front(i15,j71)↔front(i13,j73) | **1 / 8** | — | 0.0047 / 0.0047 |

대조(v5-14 무처방) — 최근접 정점쌍 **1.594234**(기본 M) · **1.046869**(mst) · **0.435297 mm**(supima) ·
S4 첫 회 최대이동 **10.2291 mm** · 반복 6~7/8.

㉠ ★ **(A) 는 «걸음»을 고쳤다** — S4 첫 회 최대이동이 **10.2291 → 1 mm**(걸음 상한 그대로)로 내려가고
**20~21회**(상한 61)에 **0.0628~0.0944 mm** 로 수렴한다 ⟹ ①의 예상 자릿수와 정합하고 **행 건너뛰기가
사라졌다**. 최근접 정점쌍이 supima **0.435297 → 0.93036 mm**(**2.14배**) · mst **1.046869 → 1.32808**
(1.27배) · 기본 M **1.594234 → 1.58249**(≈동일) 로 **개선 방향**이다. ⟹ 그런데 **여전히 `SEP` 밑**이고
삼각형 면 거리는 ~0 ⟹ **던진다**.
㉡ ★ **(B) 는 «관통»을 원천 차단했다** — S4 반복이 **1회**이고 최대이동이 **0 / 0 / 0.0047 mm** 다 ⟹
**S3 가 끝난 시점에 이미 몸 밖 `SEP` 가 확보된다**(S4 가 할 일이 없다) ⟹ 판정문의 「(B) 관통 원천
차단」은 **성립**한다.
㉢ ★★★ **그런데 (B) 는 «같은 패널 안»에서 깨진다** — 기본 M 이 **`back↔back`** · supima 가
**`front↔front`** 로 던지고(v5-12 이래 처음 보는 부류) 최근접 정점쌍이 **0.52353 / 0.67310 mm** 다.
자리도 **같은 패널의 다른 열·다른 행**(front(i43,j69)↔back(i42,j68) · front(i15,j71)↔front(i13,j73))
⟹ **접평면 낙하가 이웃 «열»을 서로 모은다**(곡률이 작은 데서 두 열의 낙하 방향이 수렴한다).
①의 곡률 경고(R 최소 **3.974 / 7.316 mm** < 행 길이 **9.86 / 9.74**)가 **그 구간을 미리 지목했다**.
㉣ **둘 다 세 칸 통과 실패** ⟹ 사전 등재 「(다) ⟹ ③~⑤ 착수 0」 · **승자를 고르지 않았다**
(둘 중 하나를 억지로 택하지 않는다 · 사후 갈래 신설 0).
★ **수치 경계 분리**(판정문 지시) — 여섯 조합의 최근접 정점쌍이 **0.52353 ~ 1.58249 mm** 로 **전부
«실질 미달»(< 1.95 mm)** 이다 ⟹ 「1.95~2.00 경계 걸림」은 **0건**.

### ③④⑤ — **착수 0**

off 비트 불변 · R3 진단 굽기 3칸 · 종합 = **전부 착수 0**(사유 = ②(다)) ⟹ **굽기 0칸 · 2호기 접촉 0 ·
캡처 0장**. ★ 이 판의 `src/` diff 는 **전부 `redrapeAsm2` 안**이고 두 후보는 **하위 플래그**
(`asm2Fix` · 기본 `undefined`)라 **정본 경로를 건드리지 않았다** — 비트 불변은 v5-12·v5-14 에서
**두 판 연속 통과**한 상태다.

## 3. 사전 등재 기대 ↔ 실측 대조

| 등재 | 기대 | 실측 | 판정 |
|---|---|---|---|
| (A) 걸음 상한을 등재 자에서 유도 | 손 상수 0 | **`THICK`** (비 9.726~9.855 = 한 자릿수) | **성립** |
| (A) 예상 반복 | 10회 급 | **20~21회** | **자릿수 정합** |
| (A) 행 건너뛰기 소멸 | 걸음 ≪ 행 간격 | 첫 회 **1 mm** ↔ 행 간격 9.73~9.86 | **성립** |
| (B) 관통 원천 차단 | S4 가 할 일 없음 | S4 **1회** · 이동 **0 mm** | **성립** |
| (B) 곡률 「R < 행 길이」 구간 | 있으면 벗어난다 | **8/22 · 2/6 · 0/8** ⟹ 기본 M·mst | **경고 발생 · 적중** |
| ② 한 후보라도 세 칸 통과 | (가)/(나) | **여섯 조합 전부 던짐** | **(다)** |

## 4. 기각·정정된 것

- **두 후보 모두 «단독으로는» 불충분**하다(여섯 조합 전부 던짐) — 다만 **각자 다른 채널을 고쳤다**.
- ★ **(B) 의 실패 부류가 새롭다** — «같은 패널» 쌍(`back↔back` · `front↔front`)은 v5-12 이래 처음이고
  원인이 **열 수렴**이다(앞뒤 교차가 아니다).
- **①의 곡률 점검이 «사전 경고»로 작동했다** — 값 보기 전에 적은 기준이 실패 칸을 맞혔다.

## 5. 자기신고 1건

②가 (다)로 갈려 **승자를 고르지 않았고 ③④⑤ 를 착수하지 않았다**(사전 등재대로). 두 후보 코드는
**하위 플래그로 남긴다**(다음 판이 그대로 쓸 수 있다 · 삭제 0). **(A)+(B) 결합은 시험하지 않았다** —
사전 등재에 없었다(사후 확대 0).
미집행 = ③④⑤(사유 명시) · 그 외 0. 문턱 **신설 0 · 이동 0**((A) 걸음 상한·(B) 곡률 기준을 값 보기
«전» §0-4 에 적고 그대로 적용) · 사후 갈래 신설 0 · 화면 판정 0 · ecc 0·0 · 물리 0줄 ·
`git add` 명시 경로만 · 구현 중간 산출 즉시 커밋(v5-14 규약 준수).
이 판 diff — `src/v3/garmentScene.ts`(곡률 훅 · 후보 A · 후보 B) · `src/v3/dressRun.ts` **+3** ·
`scripts/v5Asm2Probe.ts`(플래그 env) · **계기 신설 0**.

## 6. 다음 판에 필요한 새 사실

㉠ ★★★ **두 후보가 «다른 것»을 고쳤다 ⟹ (A)+(B) 결합이 자연스러운 다음 후보**다 — (A)의 걸음 상한이
행 건너뛰기를 없애고 (B)의 표면 추종이 관통을 없앤다. **이 판은 결합을 시험하지 않았다**(사전 등재에
없었음 · 코드는 하위 플래그로 남아 있어 결합은 **한 줄 조건**이면 된다).
㉡ ★ **(B) 의 잔여 = «열 수렴»** — 접평면 낙하는 **열마다 독립**이라 곡률 작은 데서 이웃 열이 모인다
⟹ **열 사이 간격을 지키는 항**(또는 이웃 열을 «쌍으로» 내리기)이 필요하다. 재는 방법 = 같은 행에서
`i`↔`i+1` 거리를 행별로 찍어 2D 패턴의 그 거리와 비교한다.
㉢ **남은 값의 자릿수는 맞다** — 최근접 정점쌍 **0.52~1.58 mm** = `SEP`(2 mm)의 **26~79%** ⟹
「두 자릿수 부족」이 아니라 **한 자릿수 안의 문제**다.
㉣ **능선 규칙 · 호길이 사상 · `SH_DROP` · 앵커는 그대로 둔다**(v5-13 ①② · v5-12 R0 가 성립했다).
㉤ **곡률 R 최소가 기본 M 에서 3.974 mm** 다 — 행 길이를 줄이면(= `d` 를 줄여 행을 늘리면) (B)의
조건이 완화된다. 그 대가(정점 수 증가)는 이 판이 재지 않았다.

## 부록 — raw 링크 (SHA 고정)

신규
- `docs/v5/15-S4처방.md` — 회차 노트 정본(§0~§4 · 197줄) · 커밋 `0d24157`
  https://raw.githubusercontent.com/banshk445/fit-simulator/0d24157/docs/v5/15-S4%EC%B2%98%EB%B0%A9.md
- `docs/v5/보고/15.md` — 이 보고

수정
- `src/v3/garmentScene.ts` · `src/v3/dressRun.ts` · `scripts/v5Asm2Probe.ts` — 후보 A·B + 곡률 훅 ·
  커밋 `9528053`
- `.obsidian-log/전략세션-인수인계.md` — **귀책 17건째 + 계열 폐쇄**(`ac672fd`) · v5 트랙 교체(`ea21b49`)
  https://raw.githubusercontent.com/banshk445/fit-simulator/ea21b49/.obsidian-log/%EC%A0%84%EB%9E%B5%EC%84%B8%EC%85%98-%EC%9D%B8%EC%88%98%EC%9D%B8%EA%B3%84.md
- `docs/v5/v5-색인.md` · `docs/metrics-log.md` — 커밋 `ea21b49`
  https://raw.githubusercontent.com/banshk445/fit-simulator/ea21b49/docs/metrics-log.md

━━━ 👉 다음: 전략 세션에 이 보고를 전달하세요 (CC 대기) ━━━
