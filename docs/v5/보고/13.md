# v5-13 보고 — 어깨선 배치 재설계: 호길이 사상 · 간극 `SEP` 오프셋 · 능선 길이 검증

git 스탬프 — 브랜치 **`v5-13-arclength`** · 기점 **`ad091d8`**(v5-12 HEAD) · §0 선커밋 **`4061d6d`** ·
구현 **`5b5427d`** · 노트 **`e811888`** · metrics·볼트 **`c2cfd23`** · 워크트리
`/Users/banshk/projects/fit-simulator`. 집행 기계 — **에어 M4(맥)**: 물리 0프레임 ·
**2호기 접촉 0 · 굽기 0칸 · 캡처 0장**. **ecc 에이전트 0 · 스킬 0.**

## 1. 갈래 — **B**

**B = ③(나) ⟹ 값 상신.** ①이 성립했고(C 아님) ④ 비트 불변이 유지됐다(D 아님). ③에서 세 칸 모두
조립이 던졌으므로 ⑤ 굽기·캡처는 **착수 0**. **다만 이 판은 v5-12 가 남긴 질문에 답을 냈다** —
맞닿는 **«자리»가 특정**되고, 그것이 「길이 불일치」가 아니라 **«면 교차»**라는 **다른 층위**의
문제임이 값으로 갈렸다.

## 2. §1 항목별 — 값 전량

### ① 능선 길이 검증 — **부풂 아님 · 호길이 자로 쓸 수 있다**

| 칸 | 폴리라인 mm(좌) | **현 mm**(좌) | **폴리라인/현**(좌 / 우) | 표본 |
|---|---|---|---|---|
| `supima-L`(SW 44.5) | 190.3853 | **128.4236** | **1.482479 / 1.483321** | 12 |
| `c100-h170-s45_M`(SW 43) | 215.6264 | **122.7120** | **1.757175 / 1.756883** | 13 |
| `mst-over-L`(SW 52) | 189.4439 | **156.3056** | **1.212010 / 1.212315** | 12 |

§0-4ㄱ 이 **값 보기 «전»**에 못 박은 자 — 「자릿수로 어긋난다」 = **비 ≥ 2**(근거 = v5-10 이 z 튐을
잡았을 때 그 비가 **6.99~7.22** 였다). 실측은 **1.212 ~ 1.757** 로 **전부 2 미만** ⟹ **부풂 아님** ·
**능선 규칙 교체 0**(기준 ㉠~㉣ 의 다음 후보 = 평면 절단으로 **가지 않았다**) ⟹ **능선을 «호길이 자»로
쓸 수 있다**. 1.2~1.76 은 잡음이 아니라 능선이 63.5 mm 내려가며 `|x|` 가 커지는 **실제 곡률**이다.
★ v5-12 의 「능선/패턴 **1.4277**」과 v5-10 의 「현/`SH_LEN` **1.0996**」은 **분모가 다른 양**이었고
(§0-2ㄹ) 이 표가 **같은 능선의 폴리라인/현**을 처음 나란히 놓았다.

### ② 호길이 사상 — 구현과 **끝점의 자리**

사상 = `at3(t)`(정규화 · 끝점 맞춤) → **`atS(sArc)`** · `sArc_i` = **목점으로부터의 패턴 거리** ·
능선을 `Y_NECK` 에서 `max(h, SLAB)` 간격으로 **`PATLEN = hypot(SH_LEN, SH_DROP)` 을 담을 때까지**
내린다(상한 = `NY` 표본 · `Y_HEM` — 둘 다 도출값 · **새 상수 0**) ⟹ 판정문의 「팔 윗면으로 넘어간다」가
구현됐다. 오프셋 = 앞판 `+z` · 뒤판 `−z` · **`SEP/2`**(`AXIS_Z` 방향).
**능선이 `PATLEN` 을 담는다 — 세 칸 · 좌우 모두 «참»**(능선 189.44~215.63 mm ↔ `PATLEN` 144.66~186.14 mm).

| 칸 | 끝점 x cm | 끝점 y m | **\|x\| − SW/2** mm | **y − `Y_TOP`** mm | **팔축 `s`** mm | 팔축 `r` mm |
|---|---|---|---|---|---|---|
| `supima-L` | ±24.5793 / 24.5732 | 1.397436 / 1.397456 | **+23.293 / +23.232** | **+3.956 / +3.976** | **+43.574 / +43.597** | 55.855 / 56.207 |
| `c100-h170-s45_M` | ±23.2123 / 23.2097 | 1.404203 / 1.404163 | **+17.123 / +17.097** | **+0.264 / +0.225** | **+28.115 / +28.176** | 52.735 / 52.824 |
| `mst-over-L` | ±28.3363 / 28.3322 | 1.371395 / 1.371400 | **+23.363 / +23.322** | **+11.568 / +11.574** | **+88.806 / +88.813** | 53.114 / 53.077 |

★★★ **끝점은 «몸 어깨점 앞에서 멈추지» 않는다 — `x` 로는 넘어가고 `y` 로는 위에 남는다.**
`|x|` 가 `SW/2` 보다 **+17.1 ~ +23.4 mm** 밖이고 `y` 는 `Y_TOP` 보다 **+0.23 ~ +11.57 mm** 위다
(능선이 내려가며 `|x|` 를 **패턴의 수평 폭보다 빨리** 키운다 — ①의 곡률).
★ **팔축 투영 `s` = +28.115 / +43.574 / +88.806 mm** — 전부 **양수**(어깨 피벗보다 팔 쪽)이고
v5-11 §1-③ 의 몸통/팔 경계 **x\* ≈ 20.4~20.8 cm** 보다 밖이므로 **세 칸 다 팔 윗면**이다
(SW 43 인 기본 M 도 그렇다 — v5-11 ㉡ 「레귤러핏조차 팔 위」와 정합).
★ 좌우 대칭 — 같은 채널의 좌/우 차가 **0.03~0.06 mm** 다(사상이 좌우 대칭임을 값으로 확인).

### ③ f0 채널 — **(나) 던짐** · 그러나 «자리»가 특정됐다

| 채널 | `supima-L` | `c100-h170-s45_M` | `mst-over-L` |
|---|---|---|---|
| **조립** | **던짐** | **던짐** | **던짐** |
| 자기검사(삼각형 쌍) | `front↔back` **0.000003382 mm** | `front↔back` **0.00006507 mm** | `front↔front` **0.00008030 mm** |
| **★ 최근접 «정점»쌍** | **0.435297 mm** | **1.594234 mm** | **1.046869 mm** |
| **★ 그 «자리»** | `front(i13,j70)` ↔ `back(i13,j71)` | `front(i57,j66)` ↔ `back(i58,j67)` | `front(i18,j73)` ↔ `back(i17,j73)` |
| S4 반복 / 상한 | **6 / 8** | **7 / 8** | **6 / 8** |
| S4 수렴 | **예** | **예** | **예** |
| S4 최대이동 궤적 mm | 10.2291 · 9.4843 · 2.3835 · 0.9517 · 0.2929 · **0.0820** | 10.2291 · 9.3541 · 1.8689 · 0.7237 · 0.2639 · 0.1244 · **0.0623** | 10.2291 · 7.8734 · 1.4609 · 0.7623 · 0.2556 · **0.0651** |
| 링/rest · 몸 관통 · 하위행 신장 · 최소 쌍거리 | **산출 불가** | **산출 불가** | **산출 불가** |

㉠ ★ **정점은 겹치지 않는다** — 최근접 «정점»쌍이 **0.435297 / 1.594234 / 1.046869 mm** 다(0 이 아니다).
자기검사가 보는 ~0 은 **삼각형 «면» 거리**다 ⟹ **면 교차**이고 v5-12 의 «정점 붕괴»와 **다른 부류**다.
㉡ ★ **자리는 앞뒤가 «행(`j`) 하나» 또는 «열(`i`) 하나» 차이로 엇갈리는 곳**이다 —
`front(i13,j70)↔back(i13,j71)` · `front(i57,j66)↔back(i58,j67)` · `front(i18,j73)↔back(i17,j73)`
⟹ **`±SEP/2` 로 `z` 를 벌려도 두 패널이 서로의 «행 사이»로 들어간다.**
㉢ **호길이 사상은 v5-12 의 «누름»을 없앴다** — v5-12 는 최근접 정점쌍을 재지 못했고(정점이 겹쳐
자기검사가 `0.000mm` 로 던졌다) 이 판은 **0.4~1.6 mm 로 유한하다** ⟹ 남은 문제는 「길이 불일치」가
아니라 **«면 교차»**다(**다른 층위**).
★ **후보 원인(사실 · 단정 0)** — `±SEP/2` 를 **전역 `z`** 로 주는데 능선이 기울어 있어(끝점 `r`
52.7~56.2 mm · `s` +28~+89 mm) 그 자리에서 **`z` 벌림이 «면에 수직이 아니다»**.

### ④ R1·R2 off 비트 불변 — **통과**(요약 검사)

**A포즈 29 / 29 동일**(기준선 = v5-12 에서 «커밋된 v5-11 코드»로 뜬 표 · 같은 계기 `v5AsmSha.ts`).
★ **한정(자기신고)** — 이 판은 **T포즈 108칸을 다시 돌리지 않았다**. 근거 = 이 판의 `src/` diff 가
**전부 `redrapeAsm2` 함수 «안»**(플래그 off 면 호출되지 않는 함수)이고 v5-12 에서 **같은 성질의 diff 로
108/108 · 29/29 · 최종 재확인 29/29** 를 확인했다. 그래도 **⑤ 착수 전 108칸 재확인은 필수**로 남긴다.

### ⑤ R3 진단 굽기 — **착수 0**

③이 (나)이므로 굽기·캡처를 착수하지 않았다 — **굽기 0칸 · 2호기 접촉 0 · 캡처 0장**(정착 상태가 없다).
게이트·어깨 걸림·층3 5행·`RAMP_N` 실측은 **전부 미집행**이고 사유는 이 한 줄이다.

## 3. 사전 등재 기대 ↔ 실측 대조

| 등재 | 기대 | 실측 | 판정 |
|---|---|---|---|
| ① 폴리라인/현 비가 자릿수로 어긋나는가 | 비 ≥ 2 면 부풂 | **1.212 ~ 1.757** | **부풂 아님 · 교체 0** |
| ② 능선이 `PATLEN` 을 담는가 | 담아야 한다 | **세 칸 좌우 모두 참** | **성립** |
| ② 끝이 몸 어깨점 앞에서 멈추거나 팔 윗면으로 넘어간다 | 둘 중 하나 | **`x` 로 +17~+23 mm 넘고 `y` 로 +0.2~+11.6 mm 위** · 팔축 `s` 전부 양수 | **「넘어간다」** |
| ③ 어깨 봉제쌍 거리 = `SEP` 자릿수 | 2 mm | **확인 불가**(조립 던짐) | — |
| ③ 앞뒤 면 최소 거리 ≥ `SEP` | ≥ 2 mm | **0.000003382 ~ 0.00008030 mm** | **초과** |
| ③ S4 수렴 | 수렴 | **6/8 · 7/8 · 6/8 · 전부 수렴** | **성립** |
| ④ off 비트 불변 | 동일 | **A포즈 29/29 동일**(T포즈 미재확인) | **통과(한정)** |

## 4. 기각·정정된 것

- **「능선 폴리라인 부풂」 의심 기각** — 폴리라인/현 **1.212~1.757 < 2**(v5-10 의 부풂은 6.99~7.22).
  ⟹ v5-12 의 「1.4277」은 **부풂이 아니라 분모가 다른 비**였다.
- **「끝점이 몸 어깨점 앞에서 멈춘다」 기각** — `x` 로 **+17~+23 mm 넘는다**(팔축 `s` 전부 양수).
- ★ **문제의 층위 정정** — v5-12 는 「길이 불일치」를 원인으로 지목했는데, 호길이 사상으로 그것을
  없앤 뒤에도 던진다 ⟹ **남은 것은 «면 교차»**이고 정점은 **0.4~1.6 mm 로 겹치지 않는다**.

## 5. 자기신고 2건

㉠ **T포즈 108칸 재확인을 하지 않았다**(§1-④ · 사유·근거를 적고 다음 판 필수 항목으로 남겼다).
㉡ ③의 f0 채널 일부(링/rest · 몸 여유 · 하위행 신장 · 최소 쌍거리 · 어깨 봉제쌍 거리)가
**산출 불가**다 — 조립이 반환되지 않는다(**추정 0**).
미집행 = ⑤ 굽기·캡처(사전 등재대로) · 그 외 0. 문턱 **신설 0 · 이동 0**(①의 자를 값 보기 전에 등재) ·
사후 갈래 신설 0 · 화면 판정 0 · ecc 0·0 · 물리 0줄 · `git add` 명시 경로만.
이 판 diff — `src/v3/garmentScene.ts` **한 파일**(전부 `redrapeAsm2` 안) · **계기 신설 0**(기존 2종 재사용).

## 6. 다음 판에 필요한 새 사실

㉠ **오프셋 방향을 «면 법선»으로 바꿀지** — 지금은 전역 `z` 인데 능선이 기울어 그 자리에서 면에 수직이
아니다(끝점 `r` 52.7~56.2 mm · `s` +28~+89 mm). 봉제선 접선에 수직인 방향으로 벌리는 것이 후보다
(**새 규칙 1개** · 이 판은 고르지 않는다).
㉡ **S3 드레이프가 «수직 낙하»인 것**이 엇갈림의 다른 후보다 — 앞뒤 열이 같은 `(x, z)` 를 공유하며
내려가므로 **행이 하나 밀리면 교차한다**(최근접 쌍이 전부 「행/열 하나 차이」인 것이 그 방향을 가리킨다).
재는 방법 = 같은 `i` 의 앞뒤 열을 «행별로» 나란히 찍어 `z` 차가 어디서 `SEP` 밑으로 내려가는지 본다.
㉢ 「닫힌 = **간극 0**」 규약을 설계서에 반영해야 한다(v5-12 사고 2 · 아직 설계서 본문 미반영).
㉣ **T포즈 108칸 재확인**(⑤ 착수 전 필수).
㉤ **능선 규칙은 더 흔들지 않는다** — ①이 값으로 성립했다.

## 부록 — raw 링크 (SHA 고정)

신규
- `docs/v5/13-호길이사상.md` — 회차 노트 정본(§0~§4 · 197줄) · 커밋 `e811888`
  https://raw.githubusercontent.com/banshk445/fit-simulator/e811888/docs/v5/13-%ED%98%B8%EA%B8%B8%EC%9D%B4%EC%82%AC%EC%83%81.md
- `docs/v5/보고/13.md` — 이 보고

수정
- `src/v3/garmentScene.ts` — 호길이 절대 사상 · 현 길이/비 훅 · 최근접 «자리» 훅 · 커밋 `5b5427d`
- `.obsidian-log/전략세션-인수인계.md` · `.obsidian-log/00-현재상태.md` · `docs/v5/v5-색인.md` ·
  `docs/metrics-log.md` — 커밋 `c2cfd23`
  https://raw.githubusercontent.com/banshk445/fit-simulator/c2cfd23/.obsidian-log/%EC%A0%84%EB%9E%B5%EC%84%B8%EC%85%98-%EC%9D%B8%EC%88%98%EC%9D%B8%EA%B3%84.md
  https://raw.githubusercontent.com/banshk445/fit-simulator/c2cfd23/docs/metrics-log.md

━━━ 👉 다음: 전략 세션에 이 보고를 전달하세요 (CC 대기) ━━━
