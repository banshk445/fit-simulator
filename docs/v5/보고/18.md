# v5-18 보고 — 잔여 접힘(암홀 열) 처방 2후보 f0 시험 · **갈래 B**

git 스탬프 — 브랜치 `v5-18-armhole-blend` · 기점 `6a704fe`(v5-17 HEAD) ·
워크트리 `/Users/banshk/projects/fit-simulator` ·
커밋 §0 `32fa928` · 구현 `4a7440d` · 계기 `d0d5038`·`6fb7bd0` · 노트 `ddcca66` · metrics+볼트 `4157492`.
집행 기계 = 에어 M4(f0 조립 = 물리 0프레임 · CUDA 불요) · **2호기 접촉 0**.
이 판에서 호출한 ecc 에이전트 = **0** · 스킬 = **0**.

## 1. 갈래와 사유

**갈래 B**(감소·잔여 ⟹ 상신). 사유 한 줄 — 후보 ㉯ 가 교차를 **1008 → 208(−79.4%)** 로 줄였으나
**0 이 아니므로** 사전 규칙대로 §1-④(굽기)를 착수하지 않았다.

A 아님(교차 0 미달) · C 아님(㉯ 가 −79.4%) · **D 아님**(off 비트 불변 T108/A29 동일) · E 아님.
사후 갈래 신설 0.

## 2. §1 항목별 판정과 그 판정을 만든 수 전량

### ① 암홀 열 정의 — **(가) 일치**

패턴 분할 수(계기 인쇄 · 새 상수 0):

| 칸 | N_sh | N_nk | N_side | N_arm | N_und | nuB | nvB | nuS | n |
|---|---|---|---|---|---|---|---|---|---|
| `c100-h170-s45_M` | 16 | 26 | 44 | 25 | 9 | 58 | 69 | 50 | 9,280 |
| `supima-L` | 17 | 26 | 48 | 25 | 10 | 60 | 73 | 50 | 10,150 |
| `mst-over-L` | 21 | 26 | 51 | 25 | 11 | 68 | 76 | 50 | 11,850 |

`nuB = 2·N_sh + N_nk` 이므로 **암홀 열 `I` ⟺ `dEdge(i) = min(i, nuB − i) ≤ N_sh`** 이고,
이 집합은 `garmentScene.ts` 봉제 대장의 **「어깨L·어깨R」 열 범위와 같은 집합**이다:

| 칸 | 왼쪽 어깨 토막 `i` | 오른쪽 어깨 토막 `i` | 암홀 행 `j` | 암홀 행 수 |
|---|---|---|---|---|
| `c100-h170-s45_M` | 0 ~ 16 | 42 ~ 58 | 45 ~ 69 | 25 |
| `supima-L` | 0 ~ 17 | 43 ~ 60 | 49 ~ 73 | 25 |
| `mst-over-L` | 0 ~ 21 | 47 ~ 68 | 52 ~ 76 | 25 |

★★★ **열두 판본(네 후보 × 세 칸) 전부에서 「암홀 열 안 교차 비율 = 1.0」** 이다.
교차가 **하나도** 그 집합 밖에 없다 ⟹ v5-17 이 「어깨 토막 열에 «몰린다»」로 적은 것은
이 판의 정의로 **집합 전체**였다.

**팔 관 — `v4-37` 조립이 소매에 쓰는 값 그대로**(계기가 코드에서 읽어 인쇄 · 손 상수 0):

| 값 | 세 칸 |
|---|---|
| 축 `AX` | `[0.8376379945395596, −0.5452287955034811, 0.032987128665102686]` (공통) |
| 피벗 `AP` | `[0.17996513035645062, 1.3753699307990903, −0.02702724279729303]` (공통) |
| 원점 `AO` | `[0, 1.3753699307990903, −0.02702724279729303]` (공통) |
| `ARM.yc` / `ARM.zc` | M 0.8075708150863647 / 0.009699724614620209 · supima 0.7908961474895477 / 0.007105298340320587 · mst 0.7263843417167664 / 0.00426851212978363 |
| **`SLV_R`** | **56.054922952609026 / 55.09695190630549 / 51.11396176830287 mm** (M / supima / mst) |
| `SLV_X0` | 215 / 222.5 / 260 mm · `SEP` 2 mm |

`SLV_R` 은 `fitSleeve()` 의 이분 탐색이 `probe(x0, R) ≥ SEP` 로 고른 값이다 ⟹
**반지름 `SLV_R` 의 관 = 몸에서 `SEP` 떨어진 등위면**(판정문의 「SEP 등위면」이 이 값이다).
㉮ 는 이 값을 **읽기만** 했다.

### ② 후보 구현 — **완료**

```
㉮ 'ARM'   — armProj(q): 축 AX · 피벗 AP(없으면 중심 (0, ARM.yc, ARM.zc)) · 반지름 SLV_R 의 관으로
             «반경만» 옮긴다(축 좌표·위상 보존). BLEND 의 w = 0 재료를
             T = (1 − u)·tube + u·armProj(tube) 로 바꿨다.
             경계 열 형식화(값 보기 «전» 등재) — u(i) = max(0, 1 − dEdge(i)/N_sh).
             ★ u ≡ 0 이면 T = tube ⟹ BLEND 와 «같은 계산»(진확장 · 목선 구간 회귀 표면 0).
㉯ 'RC'    — 같은 행 i↔i+1 «과» 같은 열 j↔j+1 을 2D 패턴 거리로 (중점 대칭 이동) ·
             수렴 TOL_SELF · 상한 NY · 끝나고 pushOut. 새 상수 0.
  'ARMRC'  — 둘 다 ·  'BLEND' — **기본값 승격** ·  'AB' — **폐기 표시**(주석 · 삭제 0).
src/ diff  = redrapeAsm2 본문 + asm2Fix 플래그 선언 + 인쇄 훅.  **물리 0줄**(clothPhysics 접촉 0).
계기       = scripts/v5ArmholeDist.ts 신설(src/ **거동 0줄**).
```

### ③ f0 시험 — **선택 = ㉯ `RC`**(규칙대로) · **0 이 아니다**

진입 인자 = **v5-7b §0-5ㄴ 확정분 그대로**(`BODY_BIN`·`ARM_AXIS_JSON`·`ARM_ORIGIN_JSON` = grid27 a35 · `D_MM` 9).

| 후보 | `c100-h170-s45_M` | `mst-over-L` | `supima-L` | **합** | BLEND 대비 |
|---|---|---|---|---|---|
| **BLEND**(v5-17 채택 · 기준선) | 466 | 176 | 366 | **1008** | — |
| **㉮ `ARM`** | **804** | **387** | **484** | **1675** | **+66.2%** |
| **★ ㉯ `RC`** | **46** | **62** | **100** | **★ 208** | **−79.4%** |
| ㉮+㉯ `ARMRC` | 51 | 150 | 155 | **356** | −64.7% |

선택 규칙(§0-7 · 값 보기 «전») = 「세 칸 교차 합 최소 · 동률이면 BLEND · 0 이 아니면 굽기 0」
⟹ **㉯ `RC`**(합 **208** = BLEND 의 **20.6%** · v5-17 기준선 A 1487 의 **14.0%**) · **④ 착수 0**.

**㉠ ㉮ 는 반증됐다 — 그런데 «등재한 기대»는 충족했다.**
사전 등재한 기대 채널 = 소매 캡↔암홀 봉제쌍 거리(네 암홀 봉제선 중앙값의 평균):

| 칸 | BLEND | ㉮ `ARM` | 비 |
|---|---|---|---|
| `c100-h170-s45_M` | 63.1300 mm | **40.2254 mm** | **0.637** |
| `supima-L` | 69.3880 mm | **44.6398 mm** | **0.643** |
| `mst-over-L` | 39.2215 mm | 42.5615 mm | 1.085 |

⟹ **처방은 의도한 일(암홀 열을 팔로 당김)을 했는데 교차는 늘었다.**
㉮ 의 `back↔back` 이 M 에서 **224 → 491** 로 뛴다 — 팔로 감기면서 **같은 패널 이웃 열과의 간격이 더 무너진다**.
㉮ 의 행 간격 최대가 **30.8180 → 131.3432 mm**(M)로 폭증한 것이 그 증상이다.

**㉡ ㉯ 가 이긴 이유는 «간격»이다.**

| 후보 | 열 간격 최대 mm (M / mst / supima) | 행 간격 최대 mm |
|---|---|---|
| BLEND | 116.1443 / 131.8816 / 122.1500 | 30.8180 / 33.2942 / 38.0610 |
| ㉮ `ARM` | 117.6841 / 131.8816 / 122.1500 | 131.3432 / 123.0068 / 125.4312 |
| **㉯ `RC`** | **25.9780 / 23.4921 / 23.1185** | **26.5908 / 27.9242 / 26.2184** |
| ㉮+㉯ | 26.1787 / 24.0793 / 24.3311 | 26.4047 / 26.3710 / 26.7582 |

설계 행 간격 = 9.8551 / 9.7368 / 9.7260 mm ⟹ BLEND 는 최대가 설계의 **12~13배**,
㉯ 는 **2.4~2.7배**. v5-17 (ㄴ)`ABI` 가 «열만» 잡아 실패한 것이 여기서 갈렸다.

**㉢ ★★★ 새 사실 — `S4`(밀어냄)가 교차를 «만든다».** (v5-17 미측정 항목 「S3 직후 vs S4 직후」 해소)

| 후보 | M: S3 → S4 | mst: S3 → S4 | supima: S3 → S4 |
|---|---|---|---|
| BLEND | 296 → **466** | 96 → **136** | 262 → **340** |
| ㉮ `ARM` | 479 → **804** | 208 → **341** | 270 → **459** |
| **㉯ `RC`** | 24 → **46** | **1 → 18** | 37 → **91** |
| ㉮+㉯ | 29 → **51** | 87 → **96** | 108 → **141** |

**열두 판본 전부에서 S4 ≥ S3** 다. 밀어냄이 교차를 줄인 적은 한 번도 없다.
★ **㉯ · `mst-over-L` 은 S3 직후 교차가 «1개»이고 S4 가 18개로 만든다** ⟹
**다음 판의 표적은 S3 가 아니라 S4 다.**

**㉣ ㉯ 의 «대가» — 어깨 봉제가 열린다**(등재 밖 채널 · 값으로 적는다):

| 후보 | 어깨 봉제쌍 거리 중앙 mm (M / mst / supima) |
|---|---|
| BLEND · ㉮ | **2.0000 / 2.0000 / 2.0000** (= `SEP` ⟹ 간극 0 · **닫힘**) |
| **㉯ `RC`** | **12.7204 / 10.9494 / 15.6013** (`SEP` 의 5.5~7.8배 · **열림**) |
| ㉮+㉯ | 8.1390 / 6.8518 / 11.2436 |

사전 등록 선택 규칙은 «교차 합»뿐이므로 **선택은 ㉯ 그대로**다(값을 본 뒤 규칙을 바꾸지 않는다 · 함정 14).
이 사실은 **다음 판의 재료**로 상신한다.

**㉤ 잔여의 성격**(㉯):

```
 M       back↔back 29 · front↔front 17          — 전부 «한 패널 안» · 행 nvB−4~7 · 열 i52·i56·i53·i10
 mst     back↔back 18 · 몸판↔소매 44            — ★ 소매가 섞인다(44/62 = 71.0%) · 행은 nvB−1 에 41개
 supima  front↔front 88 · 몸판↔소매 9           — 행 nvB−6·5·9·7 · 열 i57·i56·i2·i58
 ⟹ `back↔front`(v5-17 의 지배 부류)가 **세 칸 다 0** 이다. 남은 것은 ㉠ 한 패널 자기접힘,
   ㉡ **몸판↔소매**(이 판이 처음 본다).
```

**㉥ 최근접 정점쌍 mm** — BLEND 1.32775 / 2.0 / 0.92080 · ㉮ 1.56089 / 1.72193 / 1.51605 ·
**㉯ 1.68652 / 1.94692 / 1.86303** · ㉮+㉯ 1.65634 / 1.52533 / 0.19148 (M / mst / supima).
**㉦ S4 반복**(상한 `NY` 61 · 전부 수렴) — BLEND 10·13·11 · ㉮ 11·13·11 · **㉯ 12·11·9** · ㉮+㉯ 10·11·10.

### ④ — **착수 0**(교차 합 208 ≠ 0) · 굽기 **0칸** · 캡처 **0장**

## 3. 사전 등재 기대 ↔ 실측 대조

| 등재한 기대 | 실측 | 비 |
|---|---|---|
| ㉮ 에서 **소매 캡↔암홀 봉제쌍 거리가 줄어든다** | M 63.1300 → 40.2254 | **0.637** |
| 〃 | supima 69.3880 → 44.6398 | **0.643** |
| 〃 | mst 39.2215 → 42.5615 | 1.085(**반대**) |
| ㉮ 가 교차를 줄인다(처방의 목적) | 1008 → **1675** | **1.662**(**반증**) |
| ㉯ 가 교차를 줄인다 | 1008 → **208** | **0.206** |
| off 비트 불변 유지 | T 108/108 · A 29/29 동일 | **1.000** |

## 4. 기각·정정된 것과 근거 수

```
 ㉠ ★ **㉮(팔 관 표면 사영) 기각** — 전략 세션 v5-17 §4 가 「반증 대상」으로 발주한 그 후보다.
   근거 수 = 교차 합 **1675 ↔ 기준선 1008**(+66.2%) · 세 칸 전부 악화(466→804 · 176→387 · 366→484) ·
   `back↔back` M **224 → 491** · 행 간격 최대 **30.8180 → 131.3432 mm**.
   ★ **한정** — 이 반증은 **경계 열 형식화 `u(i) = max(0, 1 − dEdge/N_sh)` 한정**이다.
     다른 감쇠(예: `N_sh/2` 폭)는 시험하지 않았다.
 ㉡ ★ **v5-17 의 「어깨 토막 열에 몰린다」를 «집합 전체»로 정정** — 교차의 100% 가 암홀 열 안이다.
   (v5-17 노트의 열 번호 47~60 은 세 칸을 합친 상위 12 목록이고 칸마다 `nuB` 가 58/60/68 로 다르다 ⟹
    번호 대 번호 대조는 뜻이 약하다. 「비율 = 1.0」이 번호에 기대지 않는 답이다.)
 ㉢ ★ **v5-17 이 미측정으로 남긴 「S3 직후 vs S4 직후」를 측정했다** — S4 가 교차의 생산자다.
```

## 5. 자기신고 — 절차 이탈 · 한정 · 미집행 · 귀책

```
 ★ **CC 귀책 `C3`** — §1-③ 을 **v5-7b §0-5ㄴ 확정 진입 인자 없이** 한 바퀴 돌렸다.
   `BODY_BIN`·`ARM_AXIS_JSON`·`ARM_ORIGIN_JSON` 을 넘기지 않아 **T-포즈 기본 축**으로 조립됐고
   정점 수가 **9,178(잘못) ↔ 9,280(옳음)** 이었다. 그 판 값(BLEND 1076 · ARM 1463 · RC 269 · ARMRC 610)은
   **전량 폐기**했다.
   ★ 자체 검출 — BLEND 합이 v5-17 의 1008 과 달라 「후보를 재기 전에 «기준선 한 칸»을 재현한다」로
     `A`·`c100-h170-s45_M` 을 돌렸더니 **343 ≠ 529**. env 를 바로잡자 **529 / n 9280 / 삼각형 17808** 이
     v5-17 산출 파일과 정확히 일치했고, BLEND 도 **466 · 176 · 366 = 1008** 로 v5-17 을 재현했다.
   ★ 처치 — 계기가 `_args` 에 진입 env 를 **적는다**(커밋 `6fb7bd0`).
   ★ **규범 제안**(전략 세션 판정 대상) — 「**계기를 새로 만들면 «기준선 한 칸»을 먼저 재현해 env 를 증명한다**」.
     v5-9 의 `layer3` env 누락과 **같은 부류**이고 이번엔 CC 가 냈다.
 ★ **v5-17 산출 파일 3개를 덮어썼다**(`v5-17-cross-{A,BLEND}-*.json` · 같은 파일명으로 쓰는 계기를 재실행).
   `A` 계열은 **백업에서 원복**했고 `BLEND` 계열은 **같은 값으로 재생성**됐음을 확인했다(466 · 176 · 366).
   산출 파일은 git 밖이라 이력이 없다 ⟹ 신설 계기부터 파일명에 회차 접두(`v5-18-`)를 붙였다.
 ★ **미집행 1 — S4 뒤에도 `pos` 가 움직인다**(S4 스냅숏이 `gapProbe` 장면과 비트 불일치 · 열두 판본 전부 ·
   조립 장면 9개 ↔ `gapProbe` 회차 7). **그 자리를 특정하지 않았다.**
 ★ **미집행 2 — 관통을 직접 재지 않았다.** 계기가 `prepare()` 의 던짐 앞에서 값을 받아 몸 SDF 관통을
   따로 셀 계기가 없다. 대리 채널 = S4 수렴(네 판본 전부 `true`).
 ★ **한정 — ㉮ 의 경계 열 형식화는 한 식만 시험했다**(위 4-㉠).
 ★ **굽기 0칸 · 캡처 0장 · 2호기 접촉 0**(사전 규칙 「0 아니면 굽기 0」 그대로).
 ★ **문턱 신설·이동 0** — 쓴 수는 `N_sh`·`N_nk`·`nuB`·`nvB`·`N_side`·`N_arm`·`SLV_R`·`SEP`·`TOL_SELF`·`NY`
   뿐이고 전부 이미 등재된 양이다. 손 상수 0.
 ★ **ecc 에이전트 0 · 스킬 0.**
```

### §0 사무 — off 비트 불변 **집행 · 통과**

```
 기준선 = 기점 `6a704fe` 를 **갈라 낸 워크트리**(`../fit-simulator-v518base` · 본체 `src/` 접촉 0) ·
 같은 계기 `scripts/v5AsmSha.ts` · 같은 `D_MM=9`.
 ★ **T 포즈 108 / 108 동일** — 줄 단위 `diff` 빈 차이 · 던지는 **26칸의 문언까지 동일**.
 ★ **A 포즈  29 /  29 동일** — 던지는 칸 0.
 ⟹ 갈래 D 아님. 비트 불변 **v5-12 · v5-14 · v5-16 · v5-18 네 판 연속**.
 ★ 이 판은 `asm2Fix` 기본값을 `'A'` → `'BLEND'` 로 **바꿨는데도** off 가 불변이다 —
   `asm2` 키가 없으면 `FIX = undefined` 라 그 분기 전체가 죽는다는 것이 값으로 확인됐다.
```

## 6. 다음 판 설계에 필요한 새 사실

```
 ㉠ ★★★ **S4 가 교차의 생산자다** — 열두 판본 전부 S4 ≥ S3 · ㉯·mst 는 **S3 1개 → S4 18개**.
   ⟹ 처방의 표적을 **배치(S3)에서 밀어냄(S4)으로** 옮길 수 있다.
 ㉡ ★★ **㉯ 는 교차를 −79.4% 줄이는 대신 어깨 봉제를 연다**(2.0000 → 10.9~15.6 mm).
   ⟹ 「어깨 봉제선을 «고정»한 채 간격을 잡는 항」이 다음 후보의 형태다.
 ㉢ ★★ **앞뒤(`back↔front`) 교차가 세 칸 다 0 이다** — v5-17 의 지배 부류가 닫혔다.
   남은 부류 둘 = ㉠ 한 패널 자기접힘 · ㉡ **몸판↔소매**(mst 71.0% · 새 부류).
 ㉣ ★ **암홀 열 정의가 코드에서 선다**(`dEdge ≤ N_sh`) — 국소 처방의 정의역을 손 상수 없이 적을 수 있다.
 ㉤ ★ **S4 뒤에 `pos` 를 움직이는 자리가 있다**(미특정) — 다음 판에서 한 줄로 특정할 수 있다.
 ㉥ **팔 관 값이 등재됐다** — `SLV_R` 56.0549 / 55.0970 / 51.1140 mm · 축·피벗 세 칸 공통.
```

## 부록 — raw 링크

(커밋 SHA 고정 · `docs/` 는 볼트 밖이라 저장소 경로 그대로)

- 회차 노트 `docs/v5/18-암홀블렌드.md`
- 보고 `docs/v5/보고/18.md`
- metrics `docs/metrics-log.md`(2026-09-12 v5-18 블록)
- 볼트 `.obsidian-log/전략세션-인수인계.md`(v5 트랙 교체 · CC 귀책 대장 `C3` 신설)
- 색인 `docs/v5/v5-색인.md`
- 계기 `scripts/v5ArmholeDist.ts` · 코드 `src/v3/garmentScene.ts`·`src/v3/dressRun.ts`

━━━ 👉 다음: 전략 세션에 이 보고를 전달하세요 (CC 대기) ━━━
