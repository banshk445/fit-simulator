# v5-7a 보고 — 맥 집행분: MU 오버라이드 경로 사실 · 진단 계기 준비 · 경계 재정의 조립(f0) 검사 · 굽기 0

**물리 0줄 · `src/v3`·`gpu/engine`·워커 diff 0 · 굽기 0칸 · ecc 0 · 처방 0**

| | |
|---|---|
| 브랜치 | `v5-7a-prep` |
| 기점 | **`8e58c66`**(= v5-6 metrics) |
| §0 커밋 | (집행 «전» · 아래 §0-6 에 기입) |
| §1~§4 커밋 | **`e5edd30`** · 절차 정정 **`9ff9ca7`** |
| 보고 파일 | `docs/v5/보고/7a.md` |
| 집행 기계 | **에어**(2호기 부재) · **별도 워크트리** `~/projects/fit-simulator-7a` |

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git worktree add ../fit-simulator-7a v5-7a-prep
       → Preparing worktree (checking out 'v5-7a-prep') · HEAD is now at 8e58c66 metrics: v5-6 · 볼트 갱신
       $ cd ../fit-simulator-7a && git branch --show-current → v5-7a-prep · HEAD 8e58c66
       $ npm ci → rc 0
 2호기 (부재 · 이 판은 굽기 0)
```

---

## §0 수령·사무

### §0-1 판정문 (전략 세션 · v5-7 발주문 원문)

```
 「갈래 B 수령 · ★★★ 마찰 구조 = 유력 기전: 한계 μ·depth 라 얹힌 천은 접선 저항 ≈ 0 —
   좁은 옷은 기하로 걸리고 넓은 옷은 기하가 못 막는 순간 잡을 마찰이 없어 통째로 미끄러짐
   (절벽과 정합 · 단정 0 · 진단으로 가른다) · MU 0.3 출처 미확보 등재 ·
   정정·귀책 14건째(전략 세션): v5-5 54.5 행 = supima(SW 44.5) — 「W 단독」 판독 철회 ·
   경계는 SW 고정으로 재정의 · 2호기 절전 재발 — 사용자 설정 조치」
 + 「2호기 부재로 굽기 항목은 v5-7b 로 분리 · 이 판은 굽기 없이 되는 전부」(v5-7a 발주문)
```

**★ 귀책 등재(전략 세션 · 열넷째)** — v5-5 의 「W 54.5 행」은 **supima(SW 44.5)** 였는데
같은 어깨(SW 52)의 W 훑기처럼 읽어 **「지배 변수 = W 단독」**을 세웠다. 판정문이 이를 자기 귀책으로
등재하고 그 판독을 **철회**했다. 본 회차는 **그대로 옮겨 적는다**(CC 판단 0).
★ 사실 병기 — v5-6 §1-② 가 낸 **`W 54.5 × SW 52` 조립 던짐**(`garmentScene.ts:922` · back↔sleeveL **0.8876 mm**)이
그 철회의 실측 근거다.

### §0-2 사용자 판정 (이 판에만 · 원문 요지)

```
 ㄱ 「v5-기록」 세션은 현재 워크트리에서 그대로 진행 · v5-7a 는 **별도 워크트리**로 분리 집행
    (`git worktree add ../fit-simulator-7a v5-7a-prep` · 같은 원격 · 상대 세션 파일 접촉 **0** · 그 폴더에서 `npm ci`)
 ㄴ §1-① 확보 사실 **수용** — MU 오버라이드 경로 = 진단 `TAG` 로 조립 export 후
    `sdf-<BODYTAG>.bin` **헤더 JSON 의 `MU` 만 고쳐 재포장**(코드 0 · 정본 상수 불변 ·
    `slip_probe` 도 같은 헤더를 읽으므로 굽기·궤적 양쪽 적용) — 그 방식으로 잡 준비
 ㄷ §1-② **`SPEC_LIBRARY` 데이터 2줄 추가 승인**(`src/v5` 데이터 항목 · 물리 0 · `src/v3` diff 0 · 자기신고 등재)
 ㄹ §1-③ 덤프 형식 사실 수용 — 신규 계기는 **봉제 수축률 3종만**
```

### §0-3 바꾸지 않는 것 (회차 프롬프트 원문)

```
 물리 **0줄** · `src/`·`engine` diff 0(**계기·잡 정의만** · §0-2ㄷ 로 `src/v5` 데이터 2줄은 예외) ·
 굽기 **0칸** · ecc **0** · 정본 상수(`MU = 0.3`) **불변** · 정본 자산 **불변** · 화면 판정 **0**.
```

### §0-4 사전 등재 갈래 (회차 프롬프트 원문 · 사후 신설 0 · **단위 명시**)

**★ 단위** — `MU` **무차원** · 잡 **개** · 스펙 **개** · W·SW **cm** · 간극·거리 **mm** ·
`ARM_D` **m** · `RAMP_N` **프레임** · 어깨 대역 접촉·어깨선 위 **정점 개수** · 굽기 **칸**.

| 항 | 갈래 |
|---|---|
| **①** 오버라이드 경로 | **(가)** 경로 **있음** ⟹ 진단 잡 **2개**(MU 0.6 · 1.0 · 이름에 `diag`) 작성 / **(나)** **부재** ⟹ 최소 침습 diff **초안만**(적용 0) |
| **②** 경계 재정의 조립 | **(가)** 두 스펙 **조립 성립**(던짐 0) ⟹ f0 채널 표 + 굽기 잡 2개 / **(나)** 한쪽 이상 **던짐**(부위·값 명시) / **(다)** 그 외 오류 |
| **③** 궤적 계기 | **(가)** 작성 완료 + `tsc` 통과(맥 실행 **0** — 덤프는 2호기) / **(나)** 형식 불명으로 작성 불가 |

**§4** — **A** = ①~③ 준비 완료 ⟹ v5-7b 실행 목록(잡 **6개** · 순서 · 예상 시간) 상신 /
**B** = ①(나) ⟹ diff 초안과 함께 상신(적용 판정은 전략 세션).

### §0-5 사전 결정 5건 (값 보기 «전» · CC 집행 설계)

```
 ㄱ **정본 자산 보호** — 이 판은 export **0**·굽기 **0** 이다. v5-7b 의 진단 export 는 `TAG` 에
    **`diag` 를 반드시 넣어** 정본 `scene-*`·`sdf-*` 를 덮지 않는다(v4-46 사고 계열의 재발 방지).
 ㄴ **워크트리 격리** — 상대 세션 폴더의 파일은 **쓰지 않는다**. 자산은 **읽기 전용 심링크**만 걸고,
    산출은 이 워크트리 안에 쓴다.
 ㄷ **몸은 v5-6 과 «같은 것»을 쓴다** — 어느 몸이었는지는 기록에 없으므로 **재현 대조로 확정**한다:
    `supima-L` 을 다시 재서 v5-6 표의 **`ARM_D` 0.2149 · `RAMP_N` 77 · 어깨 대역 접촉 174** 가
    재현되는 쪽을 쓴다(맞지 않으면 그 사실을 적고 표를 새 몸으로 «전량» 다시 만든다).
 ㄹ **새 스펙 이름** = `supima-L-w56` · `supima-L-w58`(총장·어깨·소매는 supima-L 그대로 · **가상 · 실물 아님**).
 ㅁ **MU 0.6 · 1.0 은 «진단값»이지 처방이 아니다** — 정본 `MU = 0.3` 은 한 줄도 건드리지 않는다.
```

### §0-6 §0 선커밋

```
 §0 선커밋 **7a06eb4** — 집행 «전»(자기 기입) · push 는 §1 뒤 일괄
```

---

## §1-① MU 오버라이드 경로 — **(가) 있다**(코드 0줄 · 정본 상수 불변)

### 전달 사슬 (파일:줄 · 읽어 옮김)

| 자리 | 사실 |
|---|---|
| `src/v3/consts.ts:9-10` | `/** 마찰계수 — v3-16(출처 미확보 #23 이월) */` · `export const MU = 0.3;` |
| `scripts/v4AsmExport.ts:15` | `import { …, MU, … } from '../src/v3/consts.ts';` |
| `scripts/v4AsmExport.ts:101-102` | `writeFileSync('sdf-${BODYTAG}.bin', pack({ …, THICK, MU, G, DT, … }, 격자))` ⟹ **`MU` 는 자산 헤더의 «데이터»다** |
| `gpu/bake/worker.py:89` | `sh, sdata = CO.load_sdf(load.EXPORT / f"sdf-{body}.bin")` · `body = cell.rsplit("_", 1)[0]` |
| `gpu/bake/worker.py:105` | `… sh, sdata, sh["THICK"], sh["MU"], fp=fp, …` ⟹ 엔진에 **헤더 값이 그대로** 들어간다 |
| `gpu/bake/slip_probe.py:81` | `… sh, sdata, sh["THICK"], sh["MU"], fp=fp, …` ⟹ **궤적 계기도 같은 헤더**를 읽는다 |
| `gpu/engine/collide.py:47,55` | `def __init__(…, thickness, mu, …)` · `self.thickness, self.mu = float(thickness), float(mu)` |
| `gpu/engine/collide.py:114,119` | `if self.mu > 0.0:` · `k = ti.min(1.0, (self.mu * depth) / tl)` |

**잡 json 에는 `MU` 키가 없다** — `worker.py:37-47` 이 읽는 키는 `name·cells·fp·arch·frames`(셀별 `cell·asm·reportCell·frames·cellCap·ramp`)뿐이다.
**환경변수로 `MU` 를 읽는 자리도 없다** — `worker.py:175` 의 `os.environ` 은 자식에게 `PYTHONIOENCODING`·`CELL` 만 넘긴다.
⟹ 오버라이드는 **잡·환경변수가 아니라 «자산 헤더»** 에서 한다(사용자 판정 §0-2ㄴ 로 확정).

### 신설 계기 `scripts/v5DiagSdfMu.ts`

`sdf-<BODYTAG>.bin` 의 `[u32 헤더길이][JSON 헤더][격자 f64]` 에서 **헤더 JSON 의 `MU` 만** 바꿔 다시 포장한다.
**본문(격자)은 바이트 그대로** 옮기고, 다시 읽어 `MU`·본문 길이·본문 동일성을 **검산**한다.
안전장치 둘 — `OUT` 이름에 **`diag`** 가 없으면 던진다(정본 자산 보호 · §0-5ㄱ) · `OUT` 이 이미 있으면 던진다.

**스모크 3건**(맥에는 실제 `sdf-*.bin` 이 **없다** — `.gitignore:27` · 자산은 2호기에 있다 ⟹ 같은 포장의 «가짜 파일»로 쟀다)

```
 ㉠ 재포장     {"MU 전":0.3,"MU 후":0.6,"본문 바이트":64,"헤더 바이트 전":101,"헤더 바이트 후":188,"본문 동일":true}
 ㉡ 이름 가드  Error: OUT 이름에 «diag» 가 없다 — 정본 자산 보호 · …/sdf-정본.bin
 ㉢ 덮어쓰기   Error: OUT 이 이미 있다 — 덮어쓰지 않는다 · …/sdf-fakediag06.bin
```

### 진단 잡 2개 (MU 0.6 · 1.0 · 이름에 `diag`)

| 잡 파일 | 칸 | 몸 태그(= sdf 파일) | 보고 칸 | MU |
|---|---|---|---|---|
| `gpu/bake/jobs/v5-7b-diagMU06.json` | `mstoverDiagMU06_XL` | `mstoverDiagMU06` | `c100-h170-s45_XL` | **0.6** |
| `gpu/bake/jobs/v5-7b-diagMU10.json` | `mstoverDiagMU10_XL` | `mstoverDiagMU10` | `c100-h170-s45_XL` | **1.0** |

**대조군(MU 0.3)은 새로 굽지 않는다** — v5-3 의 `mstover_XL` 산출이 그 자리다(굽기 예산 절약 · 같은 조립).
★ 조립 자체는 `mstover_XL` 과 **같아야 한다**(스펙 동일 · TAG 만 다름) ⟹ v5-7b 에서 `asm-*.bin` 본문 sha 대조로 확인한다.

## §1-② 경계 재정의(어깨 44.5 고정) — **(가) 두 스펙 모두 조립 성립**(던짐 0)

### 등재 2건 (`src/v5/specToPattern.ts` +7줄 · 데이터 항목 · 물리 0줄 · `src/v3` diff 0)

```
 'supima-L-w56' — row { totalLength 71 · shoulder 44.5 · chestFlat 56 · hwa 44 }
 'supima-L-w58' — row { totalLength 71 · shoulder 44.5 · chestFlat 58 · hwa 44 }
 ★ 둘 다 **가상**(실물 아님) · 제공·제품 사용 0 · 소매길이는 종속 규칙대로 hwa − shoulder/2 = 44 − 22.25 = 21.75
```

### ★ 재현 확정 (§0-5ㄷ) — v5-6 표의 진입 인자가 **기록에 없었다**

v5-6 노트·metrics 어디에도 계기 진입 인자가 없어 **조합 8회**를 재봤고(몸 3종 × 축·원점 4종),
**대화 기록에서 회수**해 맞췄다. 맞는 인자는

```
 BODY_BIN         gpu/oracle/export/grid27/l3ap-body-c100-h170-s45-a35.bin
 ARM_AXIS_JSON    gpu/oracle/export/grid27/l3ap-body-c100-h170-s45-a35.json
 ARM_ORIGIN_JSON  gpu/oracle/export/grid27/l3ap-origin-c100-h170-s45-a35.json   ← **이것 하나가 어긋나 있었다**
 CELL             supima 계열 c100-h170-s45_L · mst 계열 c100-h170-s45_XL · D_MM 9(기본)
```

원점 파일을 **최상위** `l3ap-origin-c100-h170-s45-a35.json` 으로 주면 같은 몸·같은 `Y_TOP`·같은 `RAMP_N` 인데도
**어깨 대역 접촉 7 · 어깨선 위 262 · 간극 중앙 1.3752 mm** 가 나온다(v5-6 값 174 / 44 / 0.5715 와 전혀 다르다).
**두 번 돌려 비트 동일**(재현성은 있다) — 갈린 것은 **입력**이다.

**재현 확인 2칸 · 4채널 전량 일치**

| 칸 | 채널 | v5-6 기록 | 이 판 재현 |
|---|---|---|---|
| `supima-L` | `RAMP_N` · 접촉 · 어깨위 · 간극중앙 mm | 77 · 174 · 44 · 0.571 | **77 · 174 · 44 · 0.5715** ✅ |
| `mst-over-L` | 같음 | 89 · 138 · 74 · 0.656 | **89 · 138 · 74 · 0.6557** ✅ |

★ **함정 후보 등재** — 「**계기 산출을 남기면서 «진입 인자»를 적지 않으면 재현이 안 된다**」.
산출 json 은 `spec`·`cell` 만 적고 **몸·축·원점 파일 경로를 적지 않는다** ⟹ 다음 판이 같은 표를 못 만든다.

### f0 채널 표 (같은 몸 `c100-h170-s45` A포즈 그리드 정본 · 같은 인자 · 굽기 0)

| 옷 | 어깨 SW cm | 가슴단면 W cm | `ARM_D` m | `RAMP_N` 프레임 | **어깨 대역 접촉** 정점 | 어깨선 위 정점 | 간극 중앙 mm | 간극 최대 mm |
|---|---|---|---|---|---|---|---|---|
| **supima-L**(걸림 · 실물) | **44.5** | 54.5 | 0.21494579885967308 | **77** | **174** | 44 | 0.5714512535549208 | 2.7165200914445187 |
| **`supima-L-w56`**(가상 · 신규) | **44.5** | 56 | 0.2126619854786823 | **84** | **177** | 36 | 0.4836 | 2.707 |
| **`supima-L-w58`**(가상 · 신규) | **44.5** | 58 | 0.20909569668035827 | **87** | **177** | 34 | 0.4186 | 2.6974 |
| `mst-over-L-w56`(가상 · v5-5) | 52 | 56 | 0.220837 | 81 | 144 | 84 | 0.755 | 2.70 |
| `mst-over-L-w58`(가상 · v5-5) | 52 | 58 | 0.219442 | 86 | 137 | 78 | 0.715 | 2.70 |
| **mst-over-L**(흘러내림 · 실물) | 52 | 60 | 0.217481 | **89** | **138** | 74 | 0.6557 | 2.7186469061140084 |
| `mst-over-L-w545`(가상 · v5-6) | 52 | 54.5 | — | — | **조립 던짐**(back↔sleeveL 0.8876 mm) | — | — | — |

★ **사실**(판단 0) — **어깨(SW)를 고정하면 f0 채널이 W 를 따라 거의 움직이지 않는다**:
SW 44.5 줄에서 접촉 **174 → 177 → 177**, 어깨선 위 **44 → 36 → 34**(W 54.5 → 58) ·
SW 52 줄은 접촉 **144 / 137 / 138**, 어깨선 위 **84 / 78 / 74**(W 56 / 58 / 60).
**두 줄 사이의 차(≈ 40 정점)가 줄 안의 차(≈ 3~7 정점)보다 크다.** 굽기 전 값이므로 **걸림/흘러내림 판정은 아니다.**
★ 던짐 **0** — 어깨를 44.5 로 유지한 채 W 를 넓히는 방향은 v5-6 의 `w545`(W 를 좁히는 방향)와 달리 **제도가 성립한다**.

### 굽기 잡 2개

| 잡 파일 | 칸 | 스펙 | 보고 칸 |
|---|---|---|---|
| `gpu/bake/jobs/v5-7b-supimaW56.json` | `supimaW56_L` | `supima-L-w56` | `c100-h170-s45_L` |
| `gpu/bake/jobs/v5-7b-supimaW58.json` | `supimaW58_L` | `supima-L-w58` | `c100-h170-s45_L` |

## §1-③ 궤적 계기 — **(가) 작성 완료**(맥 실행 0 · `tsc` 통과)

신설 `scripts/v5TrajProbe.ts` — 덤프에서 **셋을 재고**, 넷째는 **읽어 옮긴다**:

```
 ㉠ 봉제 수축률(중앙·최소) · ㉡ 봉제 간극(중앙·최대 mm · λ 대체) · ㉢ 어깨 대역 접촉 정점 수
 ㉣ **어깨 봉제 y중앙** — `slip_probe.py:56` 이 이미 `<칸>-trail.json` 의 행마다 적는다 ⟹
    **다시 세지 않고 그 파일에서 읽어 붙인다**(두 계기가 같은 양을 따로 세지 않는다 · 함정 44 방향)
 `STEP`(기본 10) · `FMAX`(기본 400) 로 **f10~f30 을 볼 수 있다** — v5-5 는 `dumpEvery` 20 이라 그 구간이 f20 하나뿐이었다.
```

**덤프 형식 사실** — `slip_probe.py:112-113` `p.tofile(<칸>-f<프레임>.bin)` ⟹ **헤더 없는 n×3 f64 원시 배열**
(정착 blob 의 `[u32][JSON]` 포장이 **아니다**). 길이가 `n×24` 가 아니면 **던진다**.

**스모크 3건**(맥에 이 스펙의 덤프가 없다 ⟹ f0 · 남의 덤프 · 길이 가드로 쟀다)

```
 ㉠ f0 단독      RAMP_N 77 · 접촉 174 · 어깨위 44 · 간극중앙 0.5714512535549208 mm(= §1-② supima 행과 같다)
 ㉡ trail 병합   v4-30-slip 의 `Apiv_M-trail.json` 에서 어깨봉제y중앙 **1.4570403099060059** 를 읽어 붙였다
 ㉢ 길이 가드    Error: 덤프 길이가 다르다 — …/7a-smoke-f020.bin · 231456 ≠ 248544
                (v5-3 을 한 판 잡아먹은 「위치 파일 길이가 다르다」와 **같은 종류**를 계기가 먼저 잡는다)
```

---

## §2 자기검사

| 항 | 값 |
|---|---|
| `src/v3` · `src/components` · `gpu/engine` · `gpu/bake/worker.py` · `slip_probe.py` · `public/` | **diff 0** · 물리 **0줄** · 문턱 **0** · 처방 **0** |
| `src/v5/specToPattern.ts` | **+7줄**(가상 스펙 2건 등재 · 데이터 항목 · 사용자 §0-2ㄷ 승인 · **자기신고**) |
| 신설 | `scripts/v5DiagSdfMu.ts` · `scripts/v5TrajProbe.ts` · 잡 6개(`gpu/bake/jobs/v5-7b-*.json`) |
| 굽기 · 캡처 | **0칸 · 0장**(2호기 부재 · 이 판의 정의역 밖) |
| `npx tsc -b` | **통과**(종료 0) |
| pytest | ★ **미집행** — 2호기 응답 없음(v5-6 미집행분도 그대로 이월) · `gpu/` 변경 **0** |
| 워크트리 | **별도** `~/projects/fit-simulator-7a`(`git worktree`) · 상대 세션(`v5-chronicle`) 파일 접촉 **0** · `npm ci` rc 0 |
| 자산 | 본체 워크트리의 **읽기 전용 심링크 6건**만(몸·축·원점) · 정본 자산 쓰기 **0** |
| 화면 판정 | **0** |
| 브랜치 인쇄 | §0 머리 · §0 은 집행 **«전»** 커밋(`7a06eb4`) |
| ecc 에이전트 / 스킬 | **0 / 0** |
| ★ **절차 자기신고** | §1 커밋(`e5edd30`)에서 `git add -A` 가 **추적 자산 7건**(`gpu/oracle/export/l3ap-*` · `grid27/l3ap-*`)을 **심링크로 형식 변경**해 담았다 — **다음 커밋에서 되돌렸다**(`git restore --source=23c3263` · 추적본과 심링크 대상은 **sha 7/7 동일**이라 측정값 영향 0 · 이력은 지우지 않았다). 측정 산출 json 17건도 인덱스에서 뺐다(v5-6 관례대로 **추적 밖** · 디스크 보존). ★ 애초에 심링크가 필요 없었다 — 그 7건은 **이미 저장소에 추적되고 있었다**. |

## §4 회차 갈래 — **A**(①(가) · ②(가) · ③(가) ⟹ v5-7b 실행 목록 상신)

### v5-7b 실행 목록 (잡 6개 · 순서 · 2호기)

| # | 무엇 | 명령(요지) | 예상 |
|---|---|---|---|
| 0 | 자산 준비 | 4칸 조립 export — `TAG=supimaW56_L`·`supimaW58_L`·`mstoverDiagMU06_XL`·`mstoverDiagMU10_XL` (`SPEC` 지정 · `BODY_BIN`·축·원점은 §1-② 인자 그대로) | 칸당 ~1분 |
| 1 | **MU 재포장** | `SRC=…/sdf-mstoverDiagMU06.bin MU=0.6 OUT=…/sdf-mstoverDiagMU06.bin` 형태로 **헤더만** 고쳐 재포장(0.6 · 1.0) — ★ export 가 이미 그 이름으로 썼으므로 **덮어쓰기 가드에 걸린다** ⟹ 임시 이름으로 내보내고 `OUT` 을 최종 이름으로 준다 | 즉시 |
| 2 | ② 굽기 2칸 | `py -m gpu.bake.worker gpu/bake/jobs/v5-7b-supimaW56.json` · `…W58.json` | 칸당 **539~600 s**(v5-2 600.3097899999702 s · v5-3 539.0481538000749 s 인용) |
| 3 | ① 진단 굽기 2칸 | `…/v5-7b-diagMU06.json` · `…/v5-7b-diagMU10.json` · 대조군은 v5-3 산출 | 같음 |
| 4 | ③ 궤적 색인 | `scripts/v4SlipIdx.ts` 를 `SPEC` 로 2회 — `l3slip-idx-v5-7-supimaL_L.json` · `…-mstover_XL.json` | 즉시 |
| 5 | ③ 궤적 2칸 | `py -m gpu.bake.slip_probe …/v5-7b-traj-supima.json` · `…-mstover.json`(**`dumpEvery` 10** · frames 200) | 칸당 ~400 s |
| 6 | 판독 | `scripts/v5TrajProbe.ts` 를 네 산출에 (`STEP=10 FMAX=200`) — f10~f30 에서 **어깨 봉제 y 반전**·접촉 소멸을 본다 | 즉시 |

**합계 ≈ 6칸 · 55~65분**(JIT·export 포함 · 2호기 기준).

**넘기는 것 6건**

1. ★★★ **MU 오버라이드 경로는 있다** — 코드 0줄. `MU` 는 **`sdf-<몸>.bin` 헤더 JSON 의 데이터**이고
   워커(`worker.py:105`)와 궤적 계기(`slip_probe.py:81`)가 **같은 헤더**를 읽는다.
2. ★★★ **어깨(SW) 고정 경계는 «제도가 성립한다»** — `supima-L-w56`·`-w58` 던짐 0.
   v5-6 이 못 만든 짝(W 만 내리기)의 **반대 방향**으로 짝이 생겼다.
3. ★★★ **f0 는 W 가 아니라 SW 로 갈린다**(사실 · 판단 0) — 어깨 대역 접촉 SW 44.5 줄 **174/177/177** ↔
   SW 52 줄 **144/137/138** · 어깨선 위 **44/36/34** ↔ **84/78/74**.
4. ★★ **v5-6 표는 재현됐다**(supima·mst 4채널 전량) — 다만 **진입 인자가 기록에 없어** 조합 8회가 들었다.
   **함정 후보 등재** — 「계기 산출에 «진입 인자»를 적지 않으면 재현이 안 된다」.
5. ★★ **궤적 계기는 f10~f30 을 볼 수 있다**(`STEP` · `dumpEvery` 10) — v5-5 의 20 간격이 그 구간을 f20 하나로 만들었다.
6. ★ **pytest 는 v5-6 분까지 미집행**(2호기 부재) — `gpu/` 변경은 두 판 모두 0.

**이 판이 하지 않은 판단** — 마찰이 기전인지 · 경계가 어느 (W, SW) 에 있는지 · MU 진단값의 처분 · 화면.

---

## 부록 — raw 링크 (커밋 `4d5afea` 고정 · 브랜치 경로 0)
★ 이 부록을 붙인 커밋 자신은 `4d5afea` **뒤**에 있다 — 위 링크의 `보고/7a.md` 는 부록 «전» 판본이다.
- [`docs/v5/7a-준비.md`](https://raw.githubusercontent.com/banshk445/fit-simulator/4d5afea/docs/v5/7a-%EC%A4%80%EB%B9%84.md) — 신규 — v5-7a 정본 노트(§0 사전 등재 · §1-①②③ 값 전량 · §2 자기검사 · §4 갈래 A · v5-7b 실행 목록)
- [`docs/v5/보고/7a.md`](https://raw.githubusercontent.com/banshk445/fit-simulator/4d5afea/docs/v5/%EB%B3%B4%EA%B3%A0/7a.md) — 신규 — 이 보고의 파일 정본(본문 전량 사본)
- [`docs/v5/v5-색인.md`](https://raw.githubusercontent.com/banshk445/fit-simulator/4d5afea/docs/v5/v5-%EC%83%89%EC%9D%B8.md) — 수정 — v5-7a 줄 추가
- [`docs/metrics-log.md`](https://raw.githubusercontent.com/banshk445/fit-simulator/4d5afea/docs/metrics-log.md) — 수정 — v5-7a 블록 append(수치 정본)
- [`.obsidian-log/전략세션-인수인계.md`](https://raw.githubusercontent.com/banshk445/fit-simulator/4d5afea/.obsidian-log/%EC%A0%84%EB%9E%B5%EC%84%B8%EC%85%98-%EC%9D%B8%EC%88%98%EC%9D%B8%EA%B3%84.md) — 수정 — v5 트랙 블록 교체(v5-6 → v5-7a) · 갱신일
- [`.obsidian-log/00-현재상태.md`](https://raw.githubusercontent.com/banshk445/fit-simulator/4d5afea/.obsidian-log/00-%ED%98%84%EC%9E%AC%EC%83%81%ED%83%9C.md) — 수정 — v5-7a 블록 추가 · 갱신일

**잡·계기(코드)** — `scripts/v5DiagSdfMu.ts` · `scripts/v5TrajProbe.ts` · `gpu/bake/jobs/v5-7b-{diagMU06,diagMU10,supimaW56,supimaW58,traj-supima,traj-mstover}.json` · `src/v5/specToPattern.ts`(+7줄) · 전부 커밋 `e5edd30`.

━━━ 👉 다음: 사용자 조치가 필요합니다 — 2호기(DESKTOP-OV72A46) 복구 후 v5-7b 집행 지시 ━━━
