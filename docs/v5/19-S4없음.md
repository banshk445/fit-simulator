# v5-19 — S4 «없음»: S3(BLEND) 직후 관통·교차 사실 · 밴드 안이면 v4-37 규칙으로 2세대 첫 굽기

> 기점 `d223804`(v5-18 HEAD) · 브랜치 `v5-19-no-s4` · 집행 기계 = 2호기(DESKTOP-OV72A46 · CUDA · f64)
> 물리 0줄 · `src/` diff = `redrapeAsm2` 한정 · ecc 에이전트/스킬 호출 0 · 굽기 ≤ 3칸 · 캡처 3칸

---

## §0 선커밋 — 집행 «전» 등재

### §0-0 회차 프롬프트 원문

```
v5-19 — S4 «없음»: S3(BLEND) 직후 관통·교차 사실 · 밴드 안이면 v4-37 규칙으로 2세대 첫 굽기

집행 기계 = 2호기 · 브랜치 v5-19-no-s4 · 기점 = v5-18 HEAD d223804 · 물리 0줄 ·
src/ diff = redrapeAsm2 «한정»(S4 하위 플래그 off 경로 · RC·팔관 후보는 코드 유지·미사용 · off 비트 불변) ·
굽기 ≤ 3칸 · ecc 0 · §0 선커밋 · 브랜치 인쇄 · 보고 = docs/v5/보고/19.md · 캡처 3칸 · 콘솔 4줄
```

### §0-1 판정문(전략 세션 · v5-18 §4 수령 · 원문)

```
「갈래 B 수용 · ㉮ 사전 등재 반증 사망(귀책 아님) · ㉯ = 증상 이동(어깨 봉제 개방) 채택 0 ·
 ★★★ 열쇠 = S4 가 교차를 만든다(S3 1 → S4 18 · 열두 판본 전부 S4 ≥ S3) ⟹
 질문 = S4 없이: S3 직후 몸 관통이 밴드(THICK+2h = 8.5054mm · v4-34) 안이면
 v4-37 규칙(밴드 안 침투 = 몸 충돌 제약의 정의역 = 굽기가 판정)으로 굽는다 ·
 조립 자기검사 asm2 경로 = 교차 0 ∧ 최소쌍 > 0(v5-16) 유지 · 관통은 밴드 자로 별도 판정 · C3 수용」
```

### §0-2 §1 집행 4항 (원문)

```
① S3-only f0(세 칸) — 교차 수 전수(분포) · 몸 관통 정점 수·깊이 분포(최대 vs 밴드 8.5054 ·
   밴드 밖 정점 수) · 최근접쌍 · 행/열 간격 · 봉제쌍 ≈ SEP · 링/rest · 소매 관련 교차 별도.
   갈래: (가) 교차 0 ∧ 관통 전부 밴드 안 / (나) 교차 > 0(자리 · S3 자체의 접힘) /
   (다) 밴드 밖 관통(수·깊이·부위).
② (나)일 때 — S3 교차가 소수(mst 1 급)면 그 자리 특정(어느 행·열 · 팔 윗면인지) · 처방 0 · 사실만.
   (다)일 때 — 밴드 밖 정점의 부위(팔 윗면 곡률 구간인지) 사실만.
③ (가) 또는 「교차 ≤ 소수 ∧ 밴드 안」이면(후자는 사전 등재: 교차 «소수»는 v4-37 처럼
   «램프가 푸는지»를 위반 궤적으로 본다 — 굽기 판정) — off 비트 불변 A29·T108 → 굽기 3칸:
   게이트 5채널 · 어깨 걸림(정도) · 겨드랑이 · 관통·교차 «궤적»(f0 → 정착 · 20 간격 · v4-37 형식) ·
   링 궤적 · 층3 5행(현행 나란히) · RAMP_N 실측 · 캡처 앞·옆.
④ 「같은 옷」 — 레귤러 2칸 v4-17 ①② 대조 · mst-over 회복 정도.
```

### §0-3 §4 사전 등재 갈래 (원문 · 사후 갈래 신설 0)

```
A = ③ 굽기 3칸 ⟹ 상신(화면 판정)
B = ①(나)(다) ⟹ 값·분포 상신
C = ③ 굽기 발산·미정착 ⟹ 값 상신
D = 비트 불변 깨짐 ⟹ 즉시 정지
E = 그 외 ⟹ 정지
사후 갈래 신설 0 · 걸림은 «정도»로.
```

---

### §0-4 코드 사실 — 값 보기 «전» 인용

**ㄱ. 밴드 자 — 「손 상수 0」의 출처**(`src/v3/bodySdf.ts:118-128` · `deriveSpacing`)

```ts
const cost = (h: number) => {
  const band = thickness + 2 * h;
  const margin = band + 2 * h;
  …
};
```

v4-34 §1 이 이 장면에서 읽은 값 원문 — 「`h` = **3.7527 mm** · `band` = **8.5054 mm**
(= 1.0 + 2×3.7527) · `THICK` 1.0 · `SEP` 2.0」 · 「SDF 로 잰 깊이의 상한은 **`SEP + band` =
2.0 + 8.5054 = **10.5054 mm**」 · 「**10.5054 는 깊이가 아니라 «자의 끝»이었다**」.
★ 이 판은 `band` 를 **계기가 `sdfSpec` 에서 읽어 인쇄**한다 — 8.5054 를 손으로 적지 않는다.

**ㄴ. v4-37 규칙 원문**(`docs/v4/37-굽기최종심.md` §0-1 판정문 · 그대로)

```
 ★★ 이번 215 위반의 침투(깊이 − SEP)는 최대 6.7032mm = band(8.5054) «안» —
   기울기 존재 = 제약 작동 범위(v4-33 의 41mm 밴드 밖과 질이 다름) ·
 ★★ 새 규칙(문턱은 코드에서): f0 침투가 전부 band 안이면 최종 판정은 굽기(게이트 5채널)가 한다 ·
   가상 실험은 유지하되 정적 도구의 한계 등재
```

**ㄷ. 관통의 «식» — 게이트 ③a 가 쓰는 그것**(`src/v3/instruments.ts:244-258` `bodyClearance`)

```ts
const g = sampleSdf(bodyG, x, y, z);
if (g > CELL) { if (g < minD) minD = g; continue; }
const e = exactBodyDist(x, y, z);
const signed = g < 0 ? -e : e;
const pen = THICK - signed;
if (pen > 1e-9) { penCnt++; if (pen > maxPen) { maxPen = pen; worstPen = v; } }
```

`src/v3/s4Gate.ts:48,142` — `penMaxM: 5e-4`(③a 관통 ≤ 0.5 mm) · `crossings: 0`.

```
 ★ 이 판이 ①에서 쓰는 자(등재 · 값 보기 «전»):
   `signed` … 그 정점의 몸 부호 있는 거리(음수 = 몸 «안»)
   **침투 `P := max(0, −signed)`** … 몸 표면 아래로 들어간 깊이. v4-37 의 「침투(깊이 − SEP)」와 같은 양이다
     (v4-34 가 「깊이 = SEP − signed」로 잰다 ⟹ 깊이 − SEP = −signed).
   **밴드 안 ⟺ `P ≤ band`** … 판정문의 「기울기 존재 = 제약 작동 범위」가 이 부등식이다.
   게이트 식 `pen = THICK − signed` 도 **함께** 인쇄한다(③a 채널과 같은 수로 읽히게).
 ★ **한정(미리 등재)** — ①의 자는 `sampleSdf`(= `S4`·`pushOut` 이 쓰는 그 자)다. 게이트는 밴드 안에서
   `exactBodyDist` 로 다시 재므로 **①의 수와 ③ 게이트의 수는 같지 않을 수 있다**. 그 차이를 ③에서 본다.
```

**ㄹ. S3 이 이미 밀어낸다 — `pushOut`**(`redrapeAsm2` · v5-15 (B) 도입분)

```ts
const pushOut = (q) => {
  const dd = sampleSdf(bodyG, q[0], q[1], q[2]);
  if (!(dd < SEP)) return q;
  const n = nHat(q[0], q[1], q[2]);   if (!n) return q;
  const st = SEP - dd;
  return [q[0] + n[0] * st, q[1] + n[1] * st, q[2] + n[2] * st];
};
```

★ BLEND 블록은 섞은 점마다 `pushOut` 을 **한 번** 건다 ⟹ **S4 를 빼도 「밀어냄이 전혀 없는」 것이 아니다.**
곡률이 큰 자리에서 한 걸음으로 `SEP` 에 못 닿을 수 있고, ①이 그 잔여를 잰다.

**ㅁ. 자기검사 — `asm2` 경로는 v5-16 그대로 유지한다**(판정문 지시)

```ts
if (ASM2) {
  const mpA = minPairDist(sc0.s.pos, sc0.tris, SEP * 2);
  … if (mpA.hits === 0 && mpA.min > 0) { ok = true; break; }
}
```

⟹ **S3-only 에서 교차가 하나라도 있으면 조립이 던진다.** ①은 v5-18 과 같이 그 던짐을 잡고
**마지막 회차의 씬**으로 전수 열거한다(계기 `src/` 거동 0줄).

**ㅂ. v5-18 이 남긴 한정 — 「S4 뒤에도 `pos` 가 움직인다」**
S4 스냅숏이 `gapProbe` 장면과 비트 불일치였다(조립 장면 9 ↔ `gapProbe` 회차 7).
★ 이 판은 **S4 자체가 없으므로** S3 스냅숏과 최종 장면의 일치 여부가 **그 미규명 자리를 직접 드러낸다** —
①에서 그 비트 일치를 **채널로 인쇄**한다(추가 질문 0 · 사실만).

---

### §0-5 ② 구현 형식화 — 값 보기 «전»

```
하위 플래그 `asm2Fix` 에 **`'NOS4'`** 를 더한다:
  'NOS4'  = BLEND 의 S3(튜브↔드레이프 행 비율 보간 + `pushOut`) 그대로 · **S4 밀어냄 루프를 돌지 않는다**.
            `STEPCAP`·`ITERCAP`·`list` 는 그대로 두고 **반복 상한만 0** 이 되게 한다 ⟹ `iter = 0` ·
            `gradPush = []` · `maxMove = Infinity`(초기값) — 인쇄 채널이 그대로 「S4 없음」을 말한다.
  'BLEND' = 기본값 그대로(v5-18 승격분) · 'ARM' · 'RC' · 'ARMRC' = **코드 유지 · 이 판 미사용** ·
  'AB'    = 폐기 표시 그대로 · 'A' · 'B' · 'ABI' = 이력 그대로.
★ `asm2` off 면 `FIX = undefined` ⟹ 이 분기 전체가 죽는다(off 비트 불변).
계기 `scripts/v5NoS4.ts` 신설(`src/` **거동 0줄**) — v5-18 `v5ArmholeDist.ts` 의 전수 열거·분포·봉제쌍·
  간격을 그대로 쓰고 **관통 채널**(위 ㄷ 의 자)과 **S3 스냅숏 ↔ 최종 장면 비트 일치**를 더한다.
  관통은 `redrapeAsm2` 의 **인쇄 전용 훅**이 `sampleSdf` 로 재어 넘긴다(거동 0줄).
```

### §0-6 ① 사전 등재 갈래와 ③ 착수 조건

```
(가) 교차 0 ∧ 관통 전부 밴드 안            ⟹ ③ 착수(굽기 3칸)
(나) 교차 > 0                               ⟹ 자리 특정(행·열·팔 윗면 여부) · 처방 0 · 사실만
(다) 밴드 밖 관통                           ⟹ 부위 사실만
★ ③ 착수의 «또 하나»(판정문 원문 등재) — 「교차 ≤ 소수 ∧ 밴드 안」이면 굽는다.
  ★ **「소수」의 자는 값을 보기 «전»에 고정한다** — 판정문이 예로 든 «mst 1 급» 을 그대로 자로 삼아
    **세 칸 각각 교차 ≤ 3**(= v5-18 ㉯·mst 의 S3 교차 1 을 포함하는 한 자릿수 대역의 상한)으로 등재한다.
    이 수는 **사전 등재이며 값을 본 뒤에 움직이지 않는다**(함정 14). 초과하면 (나) 로 간다.
```

### §0-7 §0 사무

```
★ off 비트 불변 A29·T108 — ③ 착수 조건이자 갈래 D 의 자. 기점 `d223804` 워크트리 대조로 집행한다.
★ 계기 산출 파일명은 회차 접두 `v5-19-` 로 쓴다(v5-18 §2 ㄴ 처치 계속).
★ 계기를 새로 만들면 **「기준선 한 칸」을 먼저 재현해 진입 env 를 증명한다**(CC 귀책 `C3` 의 처치).
  이 판의 기준선 = `ASM2FIX=BLEND` · `c100-h170-s45_M` = **교차 466**(v5-18 값).
★ 진입 인자 = v5-7b §0-5ㄴ 확정분 — `BODY_BIN`·`ARM_AXIS_JSON`·`ARM_ORIGIN_JSON` = grid27 a35 · `D_MM` 9.
★ 이 판에서 호출한 ecc 에이전트 = 0 · 스킬 = 0 (집행 전 등재 · 회차 종료 시 재확인)
```

---

## §1 집행

집행 기계 = **에어 M4**(f0 조립 = 물리 0프레임 · CUDA 불요) · 진입 인자 = **v5-7b §0-5ㄴ 확정분 그대로**
(`BODY_BIN`·`ARM_AXIS_JSON`·`ARM_ORIGIN_JSON` = `gpu/oracle/export/grid27/l3ap-{body,body,origin}-c100-h170-s45-a35.{bin,json,json}` · `D_MM` 9).

### §0-7 처치 이행 — **기준선 한 칸 재현 · 통과**

```
 새 계기 `scripts/v5NoS4.ts` 로 `ASM2FIX=BLEND` · `c100-h170-s45_M` 을 먼저 돌렸다 ⟹
 **교차 466 · n 9,280 · 삼각형 17,808** — v5-18 값과 «정확히» 일치. env 증명 완료(CC 귀책 `C3` 처치 작동).
```

### §1-① S3-only f0 — **(나) 그리고 (다)**

**ㄱ. 밴드 자 — 이 장면의 값은 8.5054 가 아니다**(정정 · 손 상수 0 의 효과)

| 값 | 이 장면(세 칸 공통) | 판정문이 인용한 v4-34 장면 |
|---|---|---|
| `h` | **3.6145745878126117 mm** | 3.7527 mm |
| **`band = THICK + 2h`** | **8.229149175625224 mm** | 8.5054 mm |
| `THICK` / `SEP` | 1 / 2 mm | 1 / 2 mm |

```
 ★ 계기가 `sdfSpec.band` 를 **읽어** 인쇄한다 — 8.5054 를 손으로 적지 않았고, 그래서 이 차이가 드러났다.
   판정문의 **규칙**(「밴드 안이면 굽기가 판정」)은 그대로 서고, **수**만 이 장면의 것으로 바뀐다.
   `h` 는 `deriveSpacing` 이 «몸 bbox + 예산»에서 정하므로 장면마다 다르다(v4-34 장면 ≠ 이 장면).
```

**ㄴ. 교차 — (나)**(사전 등재한 「소수」 자 = 세 칸 각각 ≤ 3 · **세 칸 모두 초과**)

| 칸 | 교차 | 몸판끼리 | 소매 관련 | 패널 조합(상위) |
|---|---|---|---|---|
| `c100-h170-s45_M` | **296** | 296 | **0** | `back↔back` 180 · `back↔front` 93 · `front↔front` 23 |
| `mst-over-L` | **136** | 96 | **40** | `back↔back` 61 · `front↔front` 35 · `back↔sleeveR` 12 · `front↔sleeveR` 10 · `front↔sleeveL` 9 · `back↔sleeveL` 9 |
| `supima-L` | **288** | 262 | **26** | `front↔front` 143 · `back↔back` 99 · `back↔front` 20 · `back↔sleeveR` 8 · `front↔sleeveR` 7 · `front↔sleeveL` 6 · `back↔sleeveL` 5 |

행 분포(`nvB − j` · 상위 6) — M `nvB−1` 61 · `−3` 43 · `−2` 35 · `−14` 25 · `−13` 25 · `−6` 18 ·
mst `nvB−1` 40 · `−2` 14 · `−5` 13 · `−6` 12 · `−3` 11 · `−16` 10 ·
supima `nvB−1` 56 · `−4` 50 · `−5` 35 · `−3` 22 · `−8` 19 · `−2` 14.
열 분포(상위 6) — M i50 46 · i51 43 · i4 41 · i52 40 · i5 24 · i6 21 ·
mst i58 14 · i25 13 · i49 12 · i50 11 · i9 11 · i17 7 · supima i55 49 · i54 30 · i51 27 · i58 23 · i56 15 · i53 14.
**암홀 열 안 교차 비율 = 1.0**(세 칸 · v5-18 과 같다).

```
 ⟹ **S4 를 빼도 교차는 «수백»이다.** 사전 등재한 ③ 착수 조건(「교차 ≤ 3 ∧ 밴드 안」)을 **크게 벗어난다**.
 ★ 판정문의 전제(「S3 1 → S4 18」)는 **v5-18 의 ㉯ `RC` 판본**의 수였고, 이 판이 돌린 **BLEND** 의 S3 는
   애초에 296 / 96 / 262 였다(v5-18 §1-③ ㉢ 표에 그대로 있다). 회차 제목이 「S3(**BLEND**) 직후」이므로
   그대로 집행했고, **`'RC'` 는 프롬프트가 「코드 유지·미사용」이라 하여 돌리지 않았다.**
```

**ㄷ. 관통 — (다)**(밴드 밖 정점이 있다)

| 칸 | 관통 정점(`signed < 0`) | 최대 침투 | 중앙 | p99 | **밴드 밖 정점** | 게이트 식 `pen` 최대 |
|---|---|---|---|---|---|---|
| `c100-h170-s45_M` | 223 | 8.229149505496025 mm | 8.2291495 | 8.2291495 | **157** | 9.229149505496025 mm |
| `mst-over-L` | 173 | 8.229149505496025 mm | 8.2291495 | 8.2291495 | **134** | 9.229149505496025 mm |
| `supima-L` | 173 | 8.229149505496025 mm | 8.2291495 | 8.2291495 | **114** | 9.229149505496025 mm |

```
 ★★★ **최대 = 중앙 = p99 = 8.2291495 mm 로 «전부 같다» ⟹ 자가 포화했다.**
   이 값은 `band`(8.2291492)와 소수 일곱째 자리에서만 다르다 — `bakeSdf` 가 「띠 밖은 `band` 로 자른다」
   (`bodySdf.ts:144`)고 적어 둔 그 클램프 값이다.
   ⟹ **이 정점들의 «진짜 깊이»는 알 수 없다.** 아는 것은 「`band` 이상」뿐이다.
   이것은 v4-34 가 「10.5054 는 깊이가 아니라 «자의 끝»이었다」로 등재한 것과 **같은 현상**이다.
 ⟹ 사전 등재 갈래 **(다) 성립** — 밴드 밖 관통이 있다.
```

**ㄹ. ★★★ S4 는 밴드 밖 정점을 «한 톨도» 못 민다**(판정문이 v4-34 에서 미확인으로 남긴 명제)

같은 칸(`c100-h170-s45_M`)을 같은 계기·같은 env 로 `BLEND`(S4 있음) ↔ `NOS4`(S4 없음) 대조:

| 채널 | `BLEND`(S4 있음) | `NOS4`(S4 없음) |
|---|---|---|
| 교차(최종) | 466 | **296** |
| 단계 S3 → S4 | 296 → **466** | 296 → 296(루프가 돌지 않는다) |
| 관통 정점(`signed < 0`) | **157** | **223** |
| **밴드 밖 정점** | **157** | **157** |
| 밴드 밖 패널 분포 | front 104 · back 53 | **front 104 · back 53** |
| 밴드 밖 행 분포 | `nvB−11` 27 · `−10` 25 · `−9` 24 · `−8` 23 · `−12` 22 · `−7` 18 · `−13` 10 · `−6` 6 · `−5` 2 | **동일** |
| 밴드 밖 y mm | 1298.8662760500263 ~ 1404.2773123501017 | **동일** |
| 밴드 밖 팔축 `s` mm | −196.39668372167122 ~ 248.55655552380446 | **동일** |

```
 ★★★ **밴드 밖 집합이 «비트 단위로 같다»** — 개수 · 패널 · 행 · y · 팔축 좌표까지 전부.
   ⟹ **S4 의 `NY`(=61) 회 반복이 이 157 개 정점을 한 번도 움직이지 못했다.**
   기전은 코드에 있다 — `nHat` 은 중앙차분이고 밴드 밖은 모든 표본이 `−band` 로 클램프돼 **기울기가 0** 이라
   `gn > 1e-12` 이 거짓 ⟹ `continue`. 「밴드 밖은 기울기가 소실돼 솔버도 못 민다」(v4-34 가 **미확인**으로
   남기고 판정문이 인용한 그 명제)가 **값으로 확인**됐다.
 ★ 반대로 **S4 가 실제로 한 일** = 밴드 «안» 관통 정점 **66 개**(223 − 157)를 밀어낸 것이고,
   그 대가로 **교차를 170 개 만들었다**(296 → 466).
```

**ㅁ. 밴드 밖 정점의 «부위» — ②(다)의 답**

| 칸 | 패널 | **암홀 열 비율** | 행(`nvB − j` · 상위) | y mm(`Y_ANCHOR`) | 팔축 `s` mm(`SLV_X0`) |
|---|---|---|---|---|---|
| `c100-h170-s45_M` | front 104 · back 53 | **1.0** | `nvB−11` 27 · `−10` 25 · `−9` 24 · `−8` 23 · `−12` 22 · `−7` 18 | 1298.8663 ~ 1404.2773 (1442.0892) | −196.3967 ~ **+248.5566** (215) |
| `mst-over-L` | front 19 · **back 115** | **1.0** | `nvB−11` 21 · `−13` 19 · `−12` 19 · `−15` 14 · `−14` 14 · `−10` 13 | 1263.5286 ~ 1403.9336 (1442.0861) | −217.7001 ~ **+319.4223** (260) |
| `supima-L` | front 64 · back 50 | **1.0** | `nvB−10` 22 · `−11` 18 · `−9` 17 · `−8` 14 · `−13` 14 · `−7` 10 | 1292.6484 ~ 1398.1772 (1442.0791) | −199.9650 ~ **+261.1312** (222.5) |

```
 ⟹ **밴드 밖 정점은 «전부» 암홀 열(어깨 토막)이다**(비율 1.0 · 세 칸).
 ⟹ 행은 **봉제선 아래 5~15행** 에 몰린다(교차의 행 `nvB−1~8` 보다 **아래**다 — 겹치되 같지 않다).
 ⟹ y 는 **1263 ~ 1404 mm** = `Y_ANCHOR`(1442) 바로 아래 **38 ~ 179 mm** 대역 = 어깨마루 ~ 겨드랑이.
 ⟹ ★ 팔축 `s` 의 **최대가 `SLV_X0` 를 넘는다**(248.6 > 215 · 319.4 > 260 · 261.1 > 222.5) ⟹
   **소매가 시작하는 지점보다 «바깥»까지 몸판이 걸쳐 있다** = 판정문이 물은 «팔 윗면» 구간이 맞다.
```

**ㅂ. ★★★ 새 사실 — `redrapeAsm2` «뒤»에서 `pos` 가 움직인다 · 그 자리를 특정했다**
(v5-18 §1-③ ㉢ 이 「미특정」으로 신고한 항목 · 이 판이 닫는다)

```
 계기가 **모든 조립 장면의 S3·S4 스냅숏**을 훑어 `gapProbe` 장면과 비트 일치하는 것을 찾았다 ⟹ **없다**(세 칸).
 장면별 S3 교차 — M `[323,318,317,317,317,312,308,296,296]` · mst `[96,…,96]`(9개 전부) ·
 supima `[260,260,260,260,257,262,259,262,262]`.
 ★★★ **그런데 「S3 스냅숏 교차」가 「최종 장면의 «몸판끼리» 교차」와 «정확히» 같다**:
     M 296 = 296 · mst 96 = 96 · supima 262 = 262.
     그리고 **남은 전부가 소매 관련**이다 — M 0 · mst 40 · supima 26.
 ⟹ **`redrapeAsm2` 뒤에 소매를 움직이는 코드가 있고, 그것이 몸판↔소매 교차를 «전부» 만든다.**
 ★ 자리 = `src/v3/garmentScene.ts:1404-1424` — **v4-37 §1-③ 소매 축 방향 이동**(v3 동결 예외 3건째
   «최종 형태 v2» · 전략 세션 v4-36 §4 승인). 원문:
     「전달 분기에서 소매 패널 배치 «후» 축 방향 평행이동: `p ← p + s·AX` ·
      `s = (어깨 이음 봉제쌍의 암홀 쪽 중심 − 소매 쪽 중심)·AX` · 좌우 각각 · 조립 자신에서 계산 ·
      수직 성분 적용 0(램프의 몫) · axDot·axPoint·fitSleeve·sgn 불변 · 미전달 분기 불변」
   코드는 `if (AO) { … s.pos[v*3] += sg*sAx*AX[0]; … }` 이고 **`AO` 가 null 인 미전달(T포즈) 분기는
   통째로 지나간다** ⟹ off 비트 불변과 무관하다.
 ★ **이 블록은 2세대를 «모른다»** — `redrapeAsm2` 가 몸판을 능선에 새로 얹은 뒤에 소매만 축으로 민다.
   `M` 칸은 소매 교차 0 인데 `mst`·`supima` 는 40·26 이다 ⟹ **밀린 거리가 클수록 소매가 몸판을 뚫는다.**
 ★ **이 판은 처방 0** — 사실만 적는다(프롬프트 ② 「처방 0 · 사실만」).
```

**ㅅ. 나머지 채널**(세 칸 · M / mst / supima 순)

```
 봉제쌍 ≈ SEP  어깨 봉제 중앙 **2.0000 / 2.0000 / 2.0000 mm**(= `SEP` ⟹ 간극 0 · **닫힘**) ·
              n 34 / 44 / 36 · 최대 112.1432 / 141.8661 / 132.6627 mm(목선 쪽 끝)
 소매 캡↔암홀  앞R·뒤R·앞L·뒤L 중앙 —
              M 44.6016 · 82.5070 · 43.5286 · 81.8811 / mst 37.6949 · 40.8314 · 37.6929 · 40.6718 /
              supima 51.1975 · 91.7880 · 41.5228 · 92.0134 mm
 링 / rest    3D **495.3954 / 532.8758 / 521.3663 mm** ↔ rest(패턴) **469.5765 / 469.6107 / 469.6881 mm** ⟹
              비 **1.054984 / 1.134718 / 1.110027**(목선 링이 rest 보다 5.5 ~ 13.5% 크다)
 최근접 정점쌍 **1.32775 / 2.00000 / 0.92208 mm** — 자리 M `front(i8,j69)`↔`back(i9,j68)` ·
              mst `front(i0,j76)`↔`back(i0,j76)`(정확히 `SEP`) · supima `front(i52,j70)`↔`front(i53,j71)`
 행 간격      중앙 **9.9999 / 9.8057 / 9.8354** · 최대 **26.4570 / 33.2942 / 37.2193** mm
              (설계 9.8551 / 9.7368 / 9.7260)
 열 간격      중앙 **8.9846 / 8.9398 / 9.1600** · 최대 **116.1359 / 132.3126 / 122.1656** mm
 S4           반복 **0** · 상한 **0** · 수렴 `false` · 최대이동 궤적 `[]` ⟹ 플래그가 뜻대로 작동했다
 던짐         M 「교차 296개 · 최소쌍 `back↔back` 0.02601mm」 · mst 「교차 136개 · `back↔sleeveR` 0.004904mm」 ·
              supima 「교차 288개 · `front↔front` 0.0007917mm」 — 자기검사(v5-16 경로)는 그대로 작동했다
```

### §1-② (나)·(다)의 자리 — **위 ㅁ·ㅂ 에 적었다** · **처방 0**

### §1-③ — **착수 0** · §1-④ — **착수 0**

```
 ③ 착수 조건(§0-6 · 값 보기 «전» 등재) = 「(가) 교차 0 ∧ 밴드 안」 또는 「교차 ≤ 3 ∧ 밴드 안」.
 실측 — 교차 **296 / 136 / 288**(≫ 3) · 밴드 밖 정점 **157 / 134 / 114**(> 0) ⟹ **두 조건 모두 불성립**.
 ⟹ **굽기 0칸 · 캡처 0장 · 2호기 접촉 0.** ④ 도 착수 0.
 ★ 규칙을 값 본 뒤에 움직이지 않았다(함정 14).
```

---

## §2 자기검사

```
ㄱ **「소수」의 자를 값 보기 «전»에 고정했다** — §0-6 에 「세 칸 각각 교차 ≤ 3」으로 등재했고
  실측(296/136/288)이 그 자를 크게 벗어났다. 자를 옮기지 않았다.
ㄴ **밴드 수를 손으로 적지 않았다** — 판정문이 인용한 8.5054 대신 이 장면의 `sdfSpec.band` =
  **8.229149175625224 mm** 를 계기가 읽어 썼고, 그래서 두 장면의 `h` 가 다르다는 사실이 드러났다.
  ★ 판정문의 **규칙은 그대로 서고 수만 바뀐다** — 갈래 판정에는 영향이 없다(어느 자로 재도 밴드 밖 정점이 있다).
ㄸ **`'RC'`·`'ARM'` 을 돌리지 않았다** — 프롬프트가 「RC·팔관 후보는 코드 유지·**미사용**」이라 했다.
  ⟹ 판정문의 전제(「S3 1 → S4 18」)가 `RC` 판본의 수라는 점은 **§1-①ㄴ 에 사실로 적고 측정은 하지 않았다.**
ㄹ **관통 자의 한정(§0-4ㄷ 에 미리 등재)** — 이 판의 자는 `sampleSdf` 다. 게이트는 밴드 «안»에서
  `exactBodyDist` 로 다시 재므로 ①의 수와 ③ 게이트의 수는 같지 않을 수 있다. ③을 착수하지 않았으므로
  **그 차이는 이 판에서 확인되지 않았다**(미집행).
ㅁ **「진짜 깊이」는 측정 불가**(§1-①ㄷ) — 밴드 밖 정점의 침투는 `band` 이상이라는 것만 안다.
  더 깊이 재려면 `deriveSpacing` 의 예산·`h` 를 바꿔야 하고, 그것은 **이 판의 정의역 밖**이다(변경 0).
ㅂ **문턱 신설·이동 0** — 쓴 수는 `sdfSpec.band`·`sdfSpec.h`·`THICK`·`SEP`·`N_sh`·`nuB`·`nvB`·`N_side`·
  `SLV_X0`·`Y_ANCHOR`·`NY`·`TOL_SELF` 뿐이고 **전부 이미 등재된 양**이다. 손 상수 0.
ㅅ **물리 0줄** · `src/` diff = `redrapeAsm2` 본문(플래그 분기 1 + 인쇄 훅) + `asm2Fix` 선언 2줄.
  `clothPhysics` 접촉 0 · **소매 축 이동 블록은 «읽기만» 했다**(한 줄도 고치지 않았다).
ㅇ **이 판에서 호출한 ecc 에이전트 = 0 · 스킬 = 0.**
```

### §2-① off 비트 불변 — **통과**

```
 기준선 = 기점 `d223804` 를 **갈라 낸 워크트리**(`../fit-simulator-v519base` · 본체 `src/` 접촉 0) ·
 같은 계기 `scripts/v5AsmSha.ts` · 같은 `D_MM=9`.
 ★ **T 포즈 108 / 108 동일** — 줄 단위 `diff` 빈 차이 · 던지는 **26칸의 문언까지 동일**.
 ★ **A 포즈  29 /  29 동일** — 던지는 칸 **0**.
 ⟹ **갈래 D 가 아니다.** 비트 불변은 **v5-12 · v5-14 · v5-16 · v5-18 · v5-19 다섯 판 연속** 통과다.
 ★ 이 판이 더한 것은 `asm2Fix` 값 하나(`'NOS4'`)와 인쇄 훅뿐이고, `asm2` 키가 없으면 `FIX = undefined` 라
   그 분기 전체가 죽는다 — 그 사실이 다시 값으로 확인됐다.
```

---

## §4 판정 — **갈래 B**

```
 A = ③ 굽기 3칸 ⟹ 상신(화면 판정)     … ✗ ①이 (나)·(다) 라 ③ 착수 조건 불성립
 ★ B = ①(나)(다) ⟹ 값·분포 상신        … **✔ 이것이다**(둘 다 성립)
 C = ③ 굽기 발산·미정착 ⟹ 값 상신       … ✗ 굽기 0칸
 D = 비트 불변 깨짐 ⟹ 즉시 정지         … ✗ T108 / A29 동일(5판 연속)
 E = 그 외 ⟹ 정지                       … ✗
 사후 갈래 신설 0 · 걸림은 «정도»로(③ 미착수라 이 판에는 걸림 값이 없다).

 ① = **(나) 그리고 (다)** — 교차 296 / 136 / 288(사전 등재 자 「≤ 3」 초과) ·
      밴드 밖 관통 정점 157 / 134 / 114(> 0).
 ② = **자리 특정 완료** — 밴드 밖 정점은 **전부 암홀 열**(비율 1.0) · 봉제선 아래 5~15행 ·
      y 1263~1404 mm · **팔축 `s` 최대가 `SLV_X0` 를 넘는다**(팔 윗면 구간) · **처방 0**.
 ③ = **착수 0**(조건 불성립) · ④ = **착수 0**.

 ★★★ 이 판이 닫은 두 가지(둘 다 판정문·v5-18 이 «미확인/미특정»으로 남긴 것):
   ㉠ **밴드 밖은 기울기가 소실돼 S4 가 못 민다** — `BLEND` ↔ `NOS4` 의 밴드 밖 집합이
     개수·패널·행·y·팔축까지 **완전히 동일**(157). v4-34 가 미확인으로 남긴 명제가 확인됐다.
   ㉡ **`redrapeAsm2` 뒤의 이동자 = `garmentScene.ts:1404-1424` v4-37 소매 축 이동** —
     S3 스냅숏 교차가 최종 «몸판끼리» 교차와 정확히 같고(296=296 · 96=96 · 262=262)
     나머지가 **전부 소매 관련**(0 · 40 · 26)이다.
```
