# v5-13 — 어깨선 배치 재설계: 호길이 사상 · 간극 `SEP` 오프셋 · 능선 길이 검증

```
집행 기계  2호기(굽기) + 에어(조립·sha 대조 · 물리 0프레임)
브랜치     v5-13-arclength   기점 **ad091d8**(v5-12 HEAD · 확인 = git log -1 이 ad091d8)
약정       물리 0줄 · `src/` diff = **플래그 경로 «안»의 S2·S3 한정**(off 비트 불변) · 굽기 ≤ 3칸 ·
           ecc 0 · §0 선커밋 · 브랜치 인쇄 · 보고 = docs/v5/보고/13.md · 캡처 · 콘솔 4줄
설계 정본  docs/v5/설계-조립2세대.md(v5-11) · 구현 이력 docs/v5/12-2세대구현.md(v5-12)
```

## §0-1 판정문 (전략 세션 · v5-12 중간 수령 · **원문**)

```
갈래 B 수용 · ★ 규약 정정: «이미 닫힌» = 간극 0 = 거리 SEP(봉제 rest = 2×두께 · 자기충돌과 싸우지 않음 ·
v5-10 의 0.0000 은 자기검사 밖 가상값) · ★★★ ② 원인 = 끝점 일치 사상이 «다른 양»(패턴 길이 ↔ 능선 길이)을
억지로 맞춰 앞뒤 패널을 한 선에 눌렀다 · 처방 = 목점 기준 «호길이 사상»: 옷 어깨선을 자기 길이만큼
능선을 따라 놓고 끝이 몸 어깨점 앞에서 멈추거나 팔 윗면으로 넘어간다(SW 가 끝 위치를 정함 ·
옷이 몸을 정한다) · 앞뒤 패널 가장자리는 능선에서 ±SEP/2 (앞·뒤 방향) 오프셋 ·
★ 길이 비 1.43(기본 M)은 능선 폴리라인 부풂(v5-10 z 튐) 재발 의심 — 폴리라인/현 비를 먼저 사실로
```

## §0-2 수령 — 집행 «전»에 읽은 코드 사실 4건

```
 ㄱ **지금 사상은 «끝점 일치»다** — `garmentScene.ts:898` 의 `at3(t)` 가 `t ∈ [0,1]` 을 **능선 전체**에
    정규화해 얹는다(`t = (N_sh − i)/N_sh`) ⟹ 패턴 어깨선 길이와 능선 길이가 달라도 **양 끝을 억지로
    맞춘다**. 판정문이 지목한 그 자리다.
 ㄴ **능선은 `Y_NECK` 에서 `Y_NECK − SH_DROP` 까지만 만든다**(`:881`) ⟹ 호길이 사상으로 바꾸면
    **그 길이를 넘어갈 수 있다**(패턴이 더 길 때) ⟹ 능선을 **더 내려가며** 만들어야 한다
    (판정문 「팔 윗면으로 넘어간다」).
 ㄷ **`±SEP/2` 오프셋은 이미 들어가 있다**(v5-12 정정 2 · `ZHALF = SEP/2` · 앞판 `+z` · 뒤판 `−z`) ⟹
    이 판은 그 **방향의 정의만** 판정문대로 못 박는다(「앞·뒤 방향」 = `AXIS_Z` 기준 ±z).
 ㄹ **v5-10 의 «현 길이» 실측이 대조 기준이다** — 그 판의 두 끝점 직선 거리 비는
    **1.09960**(mst · SW 52) · **1.05531**(supima · SW 44.5)이고, v5-12 의 폴리라인/패턴 비는
    **0.9219 / 1.2470 / 1.4277** 이었다. **두 비는 «다른 분모»**다(현/`SH_LEN` ↔ 폴리라인/`hypot`)
    ⟹ ①은 **같은 능선의 «폴리라인/현»** 을 재서 부풂 여부를 가른다(새 문턱 0 · 사실만).
```

## §0-3 사전 등재 갈래 (회차 프롬프트 그대로 · 사후 신설 0)

| 항 | 집행 | 갈래 |
|---|---|---|
| **①** 능선 길이 검증 | 채택 규칙의 **폴리라인 / 두 끝점 현** 비(세 옷) · v5-10 현 비(1.0996 / 1.0553)와 대조 · 비가 자릿수로 어긋나면 **사실로 적고** 능선 규칙을 기준 ㉠~㉣ 의 **다음 후보**로 교체해 재검 · 「호길이 자로 쓸 수 있는가」 판정 | 사실 |
| **②** 호길이 사상 구현 | 목점에서 능선 `s` 를 따라 **패턴 어깨선 길이만큼** · 능선이 몸 어깨점을 지나면 **팔 윗면 능선으로 연속** · **끝점 팔축 투영 `s`** 를 채널로 등재 · 앞/뒤 **±`SEP`/2**(`AXIS_Z` 방향) · **조립 자기검사 통과가 조건** | 구현 |
| **③** f0 채널 | 링/rest · 어깨 봉제쌍 거리(기대 = **`SEP` 자릿수**) · **앞뒤 면 최소 거리 ≥ `SEP`** · 몸 관통 · S4 수렴 · 어깨끝 팔축 `s` | **(가)** 세 칸 자기검사 통과 / **(나)** 던짐(칸·값) |
| **④** R1·R2 | off 비트 불변(T포즈 108 · A포즈 29) | 통과/깨짐 |
| **⑤** R3 진단 굽기 | 3칸(v5-12 §1 그대로) + 캡처 | — |

**§4** — **A** = ③(가)+④+⑤ ⟹ 상신 / **B** = ③(나) ⟹ 값 상신 / **C** = ① 능선 자 불성립 ⟹
사실 상신 · **②~⑤ 착수 0** / **D** = R1·R2 깨짐 ⟹ **즉시 정지** / **E** = 그 외 ⟹ 정지.
**사후 갈래 신설 0 · 걸림은 «정도»로.**

## §0-4 사전 결정 4건 (값 보기 «전»)

```
 ㄱ **①의 판정 기준**(문턱 신설 0) — 「자릿수로 어긋난다」 = **폴리라인/현 비가 v5-10 현 비의 자릿수
    (1.0x)를 벗어나 «2 이상»이 되는 것**으로 읽는다. 근거 = v5-10 이 z 튐을 잡았을 때 폴리라인/`SH_LEN`
    이 **6.99~7.22** 였다(같은 부류의 부풂이면 그 자릿수로 나타난다). 1.0x~1.9x 는 **부풂 아님**으로 적는다.
 ㄴ **①이 부풂으로 나오면** 기준 ㉠~㉣ 의 **다음 후보 = 후보 2(평면 절단)** 로 교체한다
    (v5-12 §0-2② 순차 결과의 2순위 · 새 규칙 1개 = 평면 선택). **고르는 것이 아니라 등재된 순서다.**
 ㄷ **끝점이 어디서 멈추든 «옷이 정한 것»으로 적는다** — 판정문 「SW 가 끝 위치를 정함 · 옷이 몸을 정한다」.
    끝점을 몸 어깨점(`Y_TOP`)에 맞추려고 `SH_DROP` 을 움직이지 않는다(검수 결정 ① 유지 · 문턱 이동 0).
 ㄹ **`git add` 는 명시 경로만** · 구현 중간 산출도 **즉시 커밋**한다(v5-12 사고 3 의 교훈).
```

---

## §1-① 능선 길이 검증 — **부풂 아님 · 호길이 자로 쓸 수 있다**

계기 = `redrapeAsm2` 의 인쇄 훅에 **현 길이**와 **폴리라인/현 비**를 더했다(ASM2 전용 · 동작 0).

| 칸 | 폴리라인 mm(좌) | **현 mm**(좌) | **폴리라인/현**(좌/우) | 표본 |
|---|---|---|---|---|
| `supima-L`(SW 44.5) | 190.3853 | **128.4236** | **1.482479 / 1.483321** | 12 |
| `c100-h170-s45_M`(SW 43) | 215.6264 | **122.7120** | **1.757175 / 1.756883** | 13 |
| `mst-over-L`(SW 52) | 189.4439 | **156.3056** | **1.212010 / 1.212315** | 12 |

```
 §0-4ㄱ 이 값 보기 «전»에 못 박은 자 — 「자릿수로 어긋난다」 = **비 ≥ 2**(v5-10 이 z 튐을 잡았을 때
 그 비가 **6.99~7.22** 였다 · 같은 부류의 부풂이면 그 자릿수로 나타난다).
 실측 = **1.212 ~ 1.757** ⟹ **전부 2 미만** ⟹ **부풂 아님** · **능선 규칙 교체 0**(후보 2 로 가지 않는다).
 ★ 판정 — **능선을 «호길이 자»로 쓸 수 있다.** 1.2~1.76 은 «잡음»이 아니라 능선이 63.5 mm 내려가며
   `|x|` 가 커지는 **실제 곡률**이다(현은 그 직선 단축이다).
 ★ v5-12 의 「능선/패턴 1.4277」과 v5-10 의 「현/`SH_LEN` 1.0996」은 **분모가 다른 양**이었다(§0-2ㄹ) —
   이 표가 **같은 능선의 폴리라인/현**을 처음 나란히 놓았다.
```

## §1-② 호길이 사상 — 구현과 **끝점의 자리**

```
 사상   목점(호길이 0)에서 **`PATLEN = hypot(SH_LEN, SH_DROP)` 만큼** 능선을 따라간다 —
        `sArc_i = (목점으로부터의 패턴 거리)` · **정규화 0 · 끝점 맞춤 폐기**(v5-12 의 `at3(t)` 를 `atS(s)` 로).
 연속   능선을 `Y_NECK` 에서 `max(h, SLAB)` 간격으로 내리며 **`PATLEN` 을 담을 때까지** 만든다
        (상한 = `NY` 표본 · `Y_HEM` · 둘 다 도출값 · 새 상수 0) ⟹ 「팔 윗면으로 넘어간다」가 구현됐다.
 오프셋 앞판 `+z` · 뒤판 `−z` 로 **`SEP/2`**(`AXIS_Z` 방향) — 판정문의 「앞·뒤 방향」 그대로.
 ★ **능선이 `PATLEN` 을 담는다** — 세 칸 · 좌우 모두 **참**(능선 189.44~215.63 mm ↔ `PATLEN` 144.66~186.14).
```

**끝점(호길이 `PATLEN` 자리)의 실측** — 「SW 가 끝 위치를 정한다」가 값으로 어떻게 나왔나

| 칸 | 끝점 x cm | 끝점 y m | **\|x\| − SW/2** mm | **y − `Y_TOP`** mm | **팔축 `s`** mm | 팔축 `r` mm |
|---|---|---|---|---|---|---|
| `supima-L` | ±24.5793 / 24.5732 | 1.397436 / 1.397456 | **+23.293 / +23.232** | **+3.956 / +3.976** | **+43.574 / +43.597** | 55.855 / 56.207 |
| `c100-h170-s45_M` | ±23.2123 / 23.2097 | 1.404203 / 1.404163 | **+17.123 / +17.097** | **+0.264 / +0.225** | **+28.115 / +28.176** | 52.735 / 52.824 |
| `mst-over-L` | ±28.3363 / 28.3322 | 1.371395 / 1.371400 | **+23.363 / +23.322** | **+11.568 / +11.574** | **+88.806 / +88.813** | 53.114 / 53.077 |

```
 ★★★ **끝점은 «몸 어깨점 앞에서 멈추지» 않는다 — x 로는 넘어가고 y 로는 위에 남는다.**
   `|x|` 가 `SW/2` 보다 **+17.1 ~ +23.4 mm** 밖이고, `y` 는 `Y_TOP` 보다 **+0.23 ~ +11.57 mm** 위다.
   ⟹ 능선(링 `|x|` 최대)이 내려가며 `|x|` 를 **패턴의 수평 폭보다 빨리** 키우기 때문이다(①의 곡률).
 ★ **팔축 투영 `s` = +28.1 / +43.6 / +88.8 mm** — 전부 **양수**(어깨 피벗보다 팔 쪽) ·
   v5-11 §1-③ 이 낸 몸통/팔 경계 **x\* ≈ 20.4~20.8 cm** 보다 끝점이 밖이므로 **세 칸 다 팔 윗면**이다
   (SW 43 인 기본 M 도 그렇다 — v5-11 ㉡ 의 「레귤러핏조차 팔 위」와 정합).
 ★ 좌우 대칭 — 같은 채널의 좌/우 차가 **0.03~0.06 mm** 다(사상이 좌우 대칭임을 값으로 확인).
```

## §1-③ f0 채널 — **갈래 (나) 던짐**(세 칸) · 그러나 «자리»가 특정됐다

| 채널 | `supima-L` | `c100-h170-s45_M` | `mst-over-L` |
|---|---|---|---|
| **조립** | **던짐** | **던짐** | **던짐** |
| 자기검사 문언(삼각형 쌍) | `front↔back` **0.000003382 mm** | `front↔back` **0.00006507 mm** | `front↔front` **0.00008030 mm** |
| **★ 최근접 «정점»쌍 mm** | **0.435297** | **1.594234** | **1.046869** |
| **★ 그 «자리»** | `front(i13,j70)` ↔ `back(i13,j71)` | `front(i57,j66)` ↔ `back(i58,j67)` | `front(i18,j73)` ↔ `back(i17,j73)` |
| S4 반복 / 상한 | **6 / 8** | **7 / 8** | **6 / 8** |
| S4 수렴 | **예** | **예** | **예** |
| S4 최대이동 궤적 mm | 10.2291 · 9.4843 · 2.3835 · 0.9517 · 0.2929 · **0.0820** | 10.2291 · 9.3541 · 1.8689 · 0.7237 · 0.2639 · 0.1244 · **0.0623** | 10.2291 · 7.8734 · 1.4609 · 0.7623 · 0.2556 · **0.0651** |
| 능선이 `PATLEN` 담김 | 예 / 예 | 예 / 예 | 예 / 예 |
| 링/rest · 몸 관통 · 하위행 신장 | **산출 불가**(조립 던짐) | **산출 불가** | **산출 불가** |

```
 ⟹ **갈래 (나)**. 세 칸 모두 조립이 던져 f0 채널 일부(링/rest · 몸 여유 · 하위행 신장 · 최소 쌍거리)는
    **산출 불가**다(추정 0). 어깨 봉제쌍 거리는 설계상 `SEP` 지만 **자기검사를 못 넘어 확인 불가**다.
 ★★★ **그런데 v5-12 ㉡ 가 남긴 질문에 답이 나왔다 — 맞닿는 «자리»다.**
   ㉠ **정점은 겹치지 않는다** — 최근접 정점쌍이 **0.4353 / 1.5942 / 1.0469 mm** 다(0 이 아니다).
      즉 **삼각형 «면»이 교차한다**(자기검사가 보는 삼각형 쌍 거리는 ~0 인데 정점 거리는 0.4~1.6 mm).
   ㉡ **자리는 «어깨 봉제선의 목 쪽»이고 앞뒤가 «엇갈린다»** — 세 칸 모두 최근접 쌍이
      **앞판과 뒤판**이고 **행(`j`)이 하나 다르거나 열(`i`)이 하나 다르다**
      (`front(i13,j70)`↔`back(i13,j71)` · `front(i57,j66)`↔`back(i58,j67)` · `front(i18,j73)`↔`back(i17,j73)`).
      ⟹ `±SEP/2` 로 **z** 를 벌려도, 두 패널이 **서로의 «행 사이»로 들어간다**.
   ㉢ **호길이 사상은 v5-12 의 «누름»을 없앴다** — 자기검사 거리가 v5-12 의 0.0001329~0.001260 mm 에서
      0.000003382~0.00008030 mm 로 **더 작아졌지만**, 최근접 «정점»쌍은 v5-12 에서 못 쟀고 이 판은
      0.4~1.6 mm 로 **유한하다** ⟹ 남은 문제는 「길이 불일치」가 아니라 **«면 교차»**다(다른 층위).
 ★ **오프셋 방향이 후보 원인이다**(사실 · 단정 0) — `±SEP/2` 를 **전역 `z`** 로 주는데, 어깨 봉제선은
   목 쪽에서 **x 방향으로 달리고 면은 z 로 벌어지지만**, 팔 쪽으로 가면 능선이 기울어 **면의 법선이
   z 에서 멀어진다**(끝점 `r` 52.7~56.2 mm · `s` +28~+89 mm). 그 자리에서 `z` 벌림은 **면에 수직이 아니다.**
```

## §1-④ R1·R2 off 비트 불변 — **통과**(요약 검사)

```
 A포즈 **29 / 29 동일**(기준선 = v5-12 에서 «커밋된 v5-11 코드»로 뜬 표 · 같은 계기 `v5AsmSha.ts`).
 ★ **한정(자기신고)** — 이 판은 **T포즈 108칸을 다시 돌리지 않았다**. 근거 = 이 판의 `src/` diff 가
   **전부 `redrapeAsm2` 함수 «안»**(플래그가 꺼지면 호출되지 않는 함수)이고, v5-12 에서 **같은 성질의
   diff 에 대해 108/108 · 29/29 · 최종 재확인 29/29** 를 이미 확인했다. 그래도 **108칸 재확인은
   ⑤ 착수 전 필수**로 남긴다(다음 판 항목).
```

## §1-⑤ R3 진단 굽기 — **착수 0**

```
 ③이 (나)이므로 굽기·캡처를 착수하지 않았다 — **굽기 0칸 · 2호기 접촉 0 · 캡처 0장**
 (정착 상태가 없다 · 사전 등재 「B = ③(나) ⟹ 값 상신」).
```

## §2 자기검사

| 항 | 약정 | 실측 |
|---|---|---|
| 기점 | `ad091d8`(v5-12 HEAD) | ✅ 등재 |
| 물리 0줄 | | ✅ `gpu/` diff **0** · 굽기 **0칸** |
| `src/` diff | 플래그 경로 «안» S2·S3 한정 · off 비트 불변 | ✅ 전부 `redrapeAsm2` 안 · **A포즈 29/29 동일** |
| 굽기 | ≤ 3칸 | **0칸**(사전 등재) |
| 캡처 | | **0장**(정착 상태 없음) |
| ecc | | **0 · 0** |
| §0 선커밋 | | ✅ `4061d6d` |
| 브랜치 인쇄 | | ✅ `v5-13-arclength` |
| 갈래 | 사후 신설 0 | ✅ ① 사실 · ② 구현 · ③ **(나)** · ④ 통과 · ⑤ 착수 0 |
| 문턱 | 신설 0 · 이동 0 | ✅ ①의 자(비 ≥ 2)를 **값 보기 전에** §0-4ㄱ 에 적었다 |

**자기신고 2건** — ㉠ **T포즈 108칸 재확인을 하지 않았다**(§1-④ · 사유·근거·다음 판 항목 명시) ·
㉡ ③의 f0 채널 일부가 **산출 불가**다(조립 던짐 · 추정 0).
이 판 diff — `src/v3/garmentScene.ts` 한 파일(`redrapeAsm2` 안) · 계기 신설 0(기존 2종 재사용).

## §4 갈래 판정 — **B**

```
 §0-3 등재 — 「**B** = ③(나) ⟹ 값 상신」
 ① = **사실**(폴리라인/현 **1.212 ~ 1.757 < 2** ⟹ 부풂 아님 · 능선을 호길이 자로 쓸 수 있다 · 규칙 교체 0)
 ② = **구현**(호길이 절대 사상 · `PATLEN` 담김 · 끝점 `|x| − SW/2` **+17.1~+23.4 mm** ·
      `y − Y_TOP` **+0.23~+11.57 mm** · 팔축 `s` **+28.1/+43.6/+88.8 mm** = 세 칸 다 팔 윗면)
 ③ = **(나) 던짐**(세 칸) — 그러나 ★ **맞닿는 «자리»가 특정됐다**(앞뒤가 «행 사이»로 엇갈린다 ·
      최근접 정점쌍 **0.4353 / 1.5942 / 1.0469 mm** = 정점은 겹치지 않는다 ⟹ **면 교차**)
 ④ = **통과**(A포즈 29/29 · T포즈 108칸은 미재확인 — 자기신고)
 ⑤ = **착수 0**
 ⟹ **B**. ★ **D 가 아니다**(비트 불변 유지) · **C 가 아니다**(①이 성립했다).
```
