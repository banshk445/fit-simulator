# v5-13 — 어깨선 배치 재설계: 호길이 사상 · 간극 `SEP` 오프셋 · 능선 길이 검증

```
집행 기계  2호기(굽기) + 에어(조립·sha 대조 · 물리 0프레임)
브랜치     v5-13-arclength   기점 **ad091d8**(v5-12 HEAD · 확인 = git log -1 이 ad091d8)
약정       물리 0줄 · `src/` diff = **플래그 경로 «안»의 S2·S3 한정**(off 비트 불변) · 굽기 ≤ 3칸 ·
           ecc 0 · §0 선커밋 · 브랜치 인쇄 · 보고 = docs/v5/보고/13.md · 캡처 · 콘솔 4줄
설계 정본  docs/v5/설계-조립2세대.md(v5-11) · 구현 이력 docs/v5/12-2세대구현.md(v5-12)
```

## §0-1 판정문 (전략 세션 · v5-12 중간 수령 · **원문**)

```
갈래 B 수용 · ★ 규약 정정: «이미 닫힌» = 간극 0 = 거리 SEP(봉제 rest = 2×두께 · 자기충돌과 싸우지 않음 ·
v5-10 의 0.0000 은 자기검사 밖 가상값) · ★★★ ② 원인 = 끝점 일치 사상이 «다른 양»(패턴 길이 ↔ 능선 길이)을
억지로 맞춰 앞뒤 패널을 한 선에 눌렀다 · 처방 = 목점 기준 «호길이 사상»: 옷 어깨선을 자기 길이만큼
능선을 따라 놓고 끝이 몸 어깨점 앞에서 멈추거나 팔 윗면으로 넘어간다(SW 가 끝 위치를 정함 ·
옷이 몸을 정한다) · 앞뒤 패널 가장자리는 능선에서 ±SEP/2 (앞·뒤 방향) 오프셋 ·
★ 길이 비 1.43(기본 M)은 능선 폴리라인 부풂(v5-10 z 튐) 재발 의심 — 폴리라인/현 비를 먼저 사실로
```

## §0-2 수령 — 집행 «전»에 읽은 코드 사실 4건

```
 ㄱ **지금 사상은 «끝점 일치»다** — `garmentScene.ts:898` 의 `at3(t)` 가 `t ∈ [0,1]` 을 **능선 전체**에
    정규화해 얹는다(`t = (N_sh − i)/N_sh`) ⟹ 패턴 어깨선 길이와 능선 길이가 달라도 **양 끝을 억지로
    맞춘다**. 판정문이 지목한 그 자리다.
 ㄴ **능선은 `Y_NECK` 에서 `Y_NECK − SH_DROP` 까지만 만든다**(`:881`) ⟹ 호길이 사상으로 바꾸면
    **그 길이를 넘어갈 수 있다**(패턴이 더 길 때) ⟹ 능선을 **더 내려가며** 만들어야 한다
    (판정문 「팔 윗면으로 넘어간다」).
 ㄷ **`±SEP/2` 오프셋은 이미 들어가 있다**(v5-12 정정 2 · `ZHALF = SEP/2` · 앞판 `+z` · 뒤판 `−z`) ⟹
    이 판은 그 **방향의 정의만** 판정문대로 못 박는다(「앞·뒤 방향」 = `AXIS_Z` 기준 ±z).
 ㄹ **v5-10 의 «현 길이» 실측이 대조 기준이다** — 그 판의 두 끝점 직선 거리 비는
    **1.09960**(mst · SW 52) · **1.05531**(supima · SW 44.5)이고, v5-12 의 폴리라인/패턴 비는
    **0.9219 / 1.2470 / 1.4277** 이었다. **두 비는 «다른 분모»**다(현/`SH_LEN` ↔ 폴리라인/`hypot`)
    ⟹ ①은 **같은 능선의 «폴리라인/현»** 을 재서 부풂 여부를 가른다(새 문턱 0 · 사실만).
```

## §0-3 사전 등재 갈래 (회차 프롬프트 그대로 · 사후 신설 0)

| 항 | 집행 | 갈래 |
|---|---|---|
| **①** 능선 길이 검증 | 채택 규칙의 **폴리라인 / 두 끝점 현** 비(세 옷) · v5-10 현 비(1.0996 / 1.0553)와 대조 · 비가 자릿수로 어긋나면 **사실로 적고** 능선 규칙을 기준 ㉠~㉣ 의 **다음 후보**로 교체해 재검 · 「호길이 자로 쓸 수 있는가」 판정 | 사실 |
| **②** 호길이 사상 구현 | 목점에서 능선 `s` 를 따라 **패턴 어깨선 길이만큼** · 능선이 몸 어깨점을 지나면 **팔 윗면 능선으로 연속** · **끝점 팔축 투영 `s`** 를 채널로 등재 · 앞/뒤 **±`SEP`/2**(`AXIS_Z` 방향) · **조립 자기검사 통과가 조건** | 구현 |
| **③** f0 채널 | 링/rest · 어깨 봉제쌍 거리(기대 = **`SEP` 자릿수**) · **앞뒤 면 최소 거리 ≥ `SEP`** · 몸 관통 · S4 수렴 · 어깨끝 팔축 `s` | **(가)** 세 칸 자기검사 통과 / **(나)** 던짐(칸·값) |
| **④** R1·R2 | off 비트 불변(T포즈 108 · A포즈 29) | 통과/깨짐 |
| **⑤** R3 진단 굽기 | 3칸(v5-12 §1 그대로) + 캡처 | — |

**§4** — **A** = ③(가)+④+⑤ ⟹ 상신 / **B** = ③(나) ⟹ 값 상신 / **C** = ① 능선 자 불성립 ⟹
사실 상신 · **②~⑤ 착수 0** / **D** = R1·R2 깨짐 ⟹ **즉시 정지** / **E** = 그 외 ⟹ 정지.
**사후 갈래 신설 0 · 걸림은 «정도»로.**

## §0-4 사전 결정 4건 (값 보기 «전»)

```
 ㄱ **①의 판정 기준**(문턱 신설 0) — 「자릿수로 어긋난다」 = **폴리라인/현 비가 v5-10 현 비의 자릿수
    (1.0x)를 벗어나 «2 이상»이 되는 것**으로 읽는다. 근거 = v5-10 이 z 튐을 잡았을 때 폴리라인/`SH_LEN`
    이 **6.99~7.22** 였다(같은 부류의 부풂이면 그 자릿수로 나타난다). 1.0x~1.9x 는 **부풂 아님**으로 적는다.
 ㄴ **①이 부풂으로 나오면** 기준 ㉠~㉣ 의 **다음 후보 = 후보 2(평면 절단)** 로 교체한다
    (v5-12 §0-2② 순차 결과의 2순위 · 새 규칙 1개 = 평면 선택). **고르는 것이 아니라 등재된 순서다.**
 ㄷ **끝점이 어디서 멈추든 «옷이 정한 것»으로 적는다** — 판정문 「SW 가 끝 위치를 정함 · 옷이 몸을 정한다」.
    끝점을 몸 어깨점(`Y_TOP`)에 맞추려고 `SH_DROP` 을 움직이지 않는다(검수 결정 ① 유지 · 문턱 이동 0).
 ㄹ **`git add` 는 명시 경로만** · 구현 중간 산출도 **즉시 커밋**한다(v5-12 사고 3 의 교훈).
```
