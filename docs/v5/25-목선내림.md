# v5-25 — 목선 채널 게이트에서 내림(보고 값 병기) · S 던짐 자리 사실 · 회귀 3칸 굽기

> 기점 `ce22e0c`(v5-24 HEAD · push 된 sha) · 브랜치 `v5-25-neck-retire`
> 물리 0줄 · `src/` diff = **`s4Gate` 목선 채널 판정 제외**(정본 경로) + **`asm1x` 분리 플래그 두 줄** · ecc 0

```
 ★ 집행 «전» 사실
   ㉠ **2호기가 돌아왔다** — `ssh 2ho hostname` → `DESKTOP-OV72A46` ⟹ ③ 굽기를 집행할 수 있다
      (v5-23·v5-24 는 두 판 연속 도달 불가로 굽기 0 이었다).
   ㉡ `git` 은 Xcode 라이선스 미동의로 멈춰 있어 `DEVELOPER_DIR=/Library/Developer/CommandLineTools` 로
      우회한다(v5-23 부터 같은 사고 · 기계 설정 변경 0).
```

---

## §0 선커밋 — 집행 «전» 등재

### §0-0 회차 프롬프트 원문

```
v5-25 — 목선 채널 게이트에서 내림(보고 값 병기) · S 던짐 자리 사실 · 회귀 3칸 굽기

집행 기계 = 2호기 · 브랜치 v5-25-neck-retire · 기점 = v5-24 HEAD(push 후 sha) · 물리 0줄 ·
src/ diff = s4Gate 목선 채널 «판정 제외 · 값 보고»(정본 경로 · 조립 sha 불변) + S 사실 계기 ·
굽기 3칸 · ecc 0 · §0 선커밋 · 브랜치 인쇄 · 보고 = docs/v5/보고/25.md · 캡처 3칸 · 콘솔 4줄 ·
§0 사무: 귀책 21건째(전략 세션 · 휴지 1.10 = v3-40 기각 어림수를 되살림) ·
백로그 「목선 과신장 채널은 천 신장 한계 상수가 생길 때 재정의」
```

### §0-1 판정문(전략 세션 · v5-24 중간 수령 · 원문)

```
「갈래 C 수용 · 귀책 21건째 · ★ 목선 채널 두 형태 모두 옷 결함을 못 잼:
 몸 기준 = ±5mm 뒤집힘(답 못 하는 질문) · 휴지 1.10 = 근거 없는 수(v3-40 기각) ·
 옛 자는 37칸에서 0건 걸렀음 · 처분 = 게이트 판정에서 내림 · 두 비율 보고 값 병기 ·
 근거 = v4-15 원리 · 옛 29칸 편입 변동 0 이 검증 조건 ·
 결함 포착은 ③a·층3 눌림·어깨 걸림이 분담 · N_sh 세 방향 반증 — S 던짐은 자리부터」
```

### §0-2 §1 집행 4항 (원문)

```
① 채널 내림 구현(diff 원문) · T108·A29 정본 재판정 → 편입 변동 0 확인(변동 있으면 즉시 정지·상신) ·
   조립 sha 불변.
② S 던짐 자리(굽기 0 · f0) — back↔back 최소쌍의 두 정점(행·열·y) · 그 두 행이 무엇인지
   (목선 곡선 행 vs SH_DROP 으로 기운 윗줄 vs 소매/암홀) · off 에서는 왜 안 겹치는지
   (앵커 vs SH_DROP 따로 켠 대조 — 플래그 두 줄 허용) · 처방 0 · 사실만.
③ 회귀 굽기(on) — supima-L · 기본 M(+ S 는 ②로 조립이 서면): 게이트(목선 제외 4채널) ·
   어깨 걸림 · 층3 5행 · 목선 두 비율(보고) · 캡처.
④ 승격 재료 — 108 계획 · 제공 재발행 · v3 동결 예외(자 내림 포함) · S 미해결 시 «그 칸만 보류» 규약.
```

### §0-3 §4 사전 등재 갈래 (원문 · 사후 갈래 신설 0)

```
A = ①(변동 0)+③ pass ⟹ 상신(→ 108)
B = ③ fail ⟹ 값 상신
C = ① 변동 > 0 ⟹ 즉시 정지·상신
D = 조립 sha 변동 ⟹ 정지
E = 그 외 ⟹ 정지
```

---

### §0-4 코드 사실 — 값 보기 «전» 인용

**ㄱ. 현재(v5-24 판본) 목선 판정** — `src/v3/s4Gate.ts`

```ts
  ringExcess: 1,        // v5-24 가 판정에서 내림(보고 값 병기)
  ringRestMax: 1.1,     // v5-24 가 판정으로 올린 «휴지 대비 신장률» 문턱
…
  const ringExcess = ringM / ringAllowM;                  // :141
  /* ★ v5-24 — 목선 판정은 «휴지 대비 신장률»이 한다. */
  if (!(ringRestRatio <= S4_THRESHOLD.ringRestMax))       // :160
    fails.push(`목선 휴지비 … > 1.1 (링 …cm / 휴지 …cm · 옛 초과비 …)`);
```

```
 ★ 이 판은 **그 한 줄을 지우고 판정에서 완전히 뺀다.** 두 비율(`ringExcess`·`ringRestRatio`)과
   딸린 값(`ringM`·`ringBodyM`·`ringAllowM`)은 **계속 산출·인쇄**한다(보고 값 병기 · 삭제 0).
 ★ **문턱 두 개도 지우지 않는다** — `ringExcess: 1`·`ringRestMax: 1.1` 을 남기고 「판정에서 내림」 주석을 단다
   (v3 동결 관행 · 되돌림은 판정 줄 한 줄을 되쓰는 것).
```

**ㄴ. 판정문이 든 두 근거의 값**(v5-23·v5-24 실측 인용 · 이 판이 새로 재지 않는다)

```
 몸 기준(`ringExcess`) — 링을 `y ± 5 mm` 강체 이동하면 **판정이 뒤집힌다**:
   `on supima-L` 1.002145 → **0.999151**(−5 mm) · `on 기본 M` 1.021083 → **0.991968**(−5 mm)
   ⟹ 「경계를 넘느냐」가 **계가 답할 수 있는 질문이 아니다**(v4-15 가 표시 mm 에서 같은 형태로 내린 그 원리).
 휴지 기준(`ringRestRatio`) — 문턱 **1.10** 은 v3-40 이 「어림수」로 **기각**한 수다(`s4Gate.ts:51-52` 원문
   「**1 은 고른 수가 아니라 «정의»다** — 어림수 1.10(v3-24)을 교체했다」) ⟹ v5-24 가 그것을 **되살렸다**
   (판정문 「귀책 21건째」).
 옛 자의 판별력 — 정본 **37칸에서 0건**을 걸렀다(초과비 최대 **0.998283** · v5-24 §1-②).
 ⟹ 두 형태 **모두** 옷 결함을 재지 못한다 ⟹ **판정에서 내린다.**
```

**ㄷ. `asm1x` 의 두 자리**(②의 분리 대조가 붙는 곳)

```ts
  asm1x?: boolean;                                          // :85 (타입) · dressRun.ts:56 도 같다
  const ASM1X = cfg.asm1x === true;                         // :110
  const SH_DROP  = (ASM2 || ASM1X) ? ASM2_SH_DROP : 0;      // :137
  const Y_ANCHOR = (ASM2 || ASM1X) ? Y_NECK : Y_TOP;        // :296
```

**ㄹ. v5-23 이 특정한 S 던짐 자리**(이 판의 ②가 출발하는 사실)

```
 던짐 = `옷 자기 간격 SEP 미달 — 최소쌍 back↔back 1.394mm — 갈래 D`(`c87.5-h155-s40_S` · on)
 최악 쌍 = 삼각형 **14716 ↔ 14719**(둘 다 `back`) · 오프셋 3727/3728/3784 ↔ 3729/3785/3786 ·
 `back` 정점 3,819 = 57 × 67 ⟹ `nuB` 56 · `nvB` 66 ⟹ **`j` = 65·66 · `i` 22 ~ 24** ·
 `dEdge` 22 **>** `N_sh` 17 ⟹ **목선 구간** · 높이 1264.860 ~ 1288.118 mm(`Y_NECK` 1315.769 아래 27.7~50.9)
 off 는 성립 — 최소쌍 **2.261599483527185 mm** · 최악 쌍이 `front↔back`(부류가 다르다)
```

---

### §0-5 형식화 — 값 보기 «전» 등재(사후 신설 0)

**ㄱ. ① 채널 내림**

```
 판정 줄(`s4Gate.ts:159-162`)을 **삭제**하고, 그 자리에 「판정 제외 · 값 보고」 주석을 남긴다.
 `S4Result` 의 필드(`ringExcess`·`ringRestRatio`·`ringM`·`ringBodyM`·`ringAllowM`)는 **그대로** 둔다.
 ⟹ 게이트 판정 채널은 **넷**이 된다: ③a 관통 ≤ 0.5 mm · 자기관통 교차 0 · 정착 창 순변위 ≤ 0.1 mm · pinned 0.
 ★ **검증 조건(판정문)** = 「옛 29칸 편입 변동 **0**」.
   이 판이 실제로 확인할 수 있는 정의역 = **정본 정착 blob 이 있는 칸**(v5-24 실측 37칸) ·
   A포즈 29칸은 정본 정착 blob 이 T포즈 계열뿐이라 **재판정 불가**(v5-24 §2ㄹ 와 같은 한정) ⟹
   **37칸으로 확인하고 그 한정을 적는다.**
   ★ **구조적 예측도 함께 적는다**(값 보기 전) — 채널을 «내리는» 것은 `fails` 에서 한 항목을 빼는 일이므로
     `pass → fail` 은 **원리적으로 불가능**하고, `fail → pass` 는 **목선이 «단독» 실패 사유였던 칸**에서만 난다.
     v5-24 는 그 칸이 **0개**라고 쟀다 ⟹ **변동 0 이 예측이다.** 어긋나면 **갈래 C(즉시 정지)**.
```

**ㄴ. ② 분리 플래그 — 「두 줄」의 형식**(프롬프트가 허용한 범위)

```
 타입을 `asm1x?: boolean` → `asm1x?: boolean | 'anchor' | 'drop'` 로 넓히고 **두 줄**을 더한다:
   const A1X = cfg.asm1x === true || cfg.asm1x === 'anchor';   // 앵커만
   const D1X = cfg.asm1x === true || cfg.asm1x === 'drop';     // SH_DROP 만
 그리고 기존 두 자리를 각각 `A1X`·`D1X` 로 바꾼다:
   SH_DROP  = (ASM2 || D1X) ? ASM2_SH_DROP : 0
   Y_ANCHOR = (ASM2 || A1X) ? Y_NECK : Y_TOP
 ★ `asm1x: true` 면 **둘 다 켜져 v5-22~24 와 항등**이다(회귀 없음) · 키가 없으면 off 그대로.
 ★ 네 판본을 f0 로 잰다 — **off · anchor 만 · drop 만 · 둘 다**. 채널 = `back↔back` 최소쌍 · 최악 쌍의
   패널·(i, j)·y · `nuB`/`nvB`/`N_sh` · 던짐 문언.
 ★ **처방 0** — 어느 쪽이 원인이든 이 판은 고치지 않는다(사실만).
```

**ㄷ. ③ 굽기의 정의역**

```
 회귀 = `supima-L`(`c100-h170-s45_L`) · `기본 M`(`c100-h170-s45_M`) · **S 는 ②로 조립이 서면**만.
 ②는 «사실만» 이고 처방 0 이므로, S 조립이 서는 것은 `anchor` 만/`drop` 만 판본에서일 수 있다 —
 그 경우 **그 판본으로 굽지 않는다**(정본 후보는 `asm1x: true` 다) ⟹ S 는 **보류**로 적고 ④의 규약에 넣는다.
 굽기 인자 = v5-22 와 같은 것(`frames` 400 · `ramp` · 칸마다 그 칸의 A포즈 몸 grid27 a35 · `D_MM` 9) ·
 자산 접두는 **몸 단위로** 가른다(v5-22 §2ㄱ 의 `BODYTAG` 충돌 사고 처치).
```

### §0-6 §0 사무

```
★ **전략 세션 귀책 21건째** 등재 — 판정문 원문 「휴지 1.10 = v3-40 기각 어림수를 되살림」.
  v5-24 발주문이 그 수를 「새 수 0」의 근거로 삼았으나, `s4Gate.ts:51-52` 는 그것을 **v3-40 이 기각한
  어림수**로 적고 있었다 ⟹ 「저장소에 있는 수」이긴 하나 **기각된 수**였다.
★ **백로그 등재** — 「**목선 과신장 채널은 «천 신장 한계 상수»가 생길 때 재정의한다**」(볼트 백로그 절).
★ CC 귀책 `C3` 처치 이행 — 새 계기는 **기준선 한 칸을 먼저 재현**한다.
  이 판의 기준선 = v5-23 이 낸 `on supima-L` 정착의 `ringRestRatio` **1.0427966483091002** ·
  `ringExcess` **1.0021454460806467**.
★ `src/` 수정은 **두 자리뿐** — `s4Gate.ts` 판정 줄 · `garmentScene.ts`(+`dressRun.ts`) 분리 플래그.
  회차 끝에 `git diff ce22e0c -- src/` 를 인쇄한다.
★ 이 판에서 호출한 ecc 에이전트 = 0 · 스킬 = 0 (집행 전 등재 · 종료 시 재확인)
```

---

## §1 집행

진입 인자 = v5-22~24 와 같은 것(칸마다 그 칸의 A포즈 몸 grid27 a35 · `D_MM` 9) · 굽기 = **2호기**(도달 회복).

### §1-① 목선 채널 내림 — **구현 · 편입 변동 0**

**diff 원문**(`git diff ce22e0c -- src/` = 세 파일 · 24+/11−)

```ts
// src/v3/s4Gate.ts — 판정 줄을 «지우고» 주석으로 남겼다(값·문턱 삭제 0)
-  if (!(ringRestRatio <= S4_THRESHOLD.ringRestMax))
-    fails.push(`목선 휴지비 … > 1.1 (링 …cm / 휴지 …cm · 옛 초과비 …)`);
+  // if (!(ringRestRatio <= S4_THRESHOLD.ringRestMax)) fails.push(`목선 휴지비 …`);   ← v5-25 판정 제외
+  /* 사유 셋을 주석에 값으로 남겼다 — ㉠ ±5 mm 뒤집힘(1.002145→0.999151 · 1.021083→0.991968) ·
+     ㉡ 문턱 1.10 은 v3-40 이 「어림수」로 기각한 수 · ㉢ 옛 자는 정본 37칸에서 0건(최대 0.998283) */

// 문턱 둘 다 남기고 «판정에서 내림» 표시
+  /** ★ v5-25 — **판정에서 내림**(보고 값 병기 · 삭제 0). 위 `ringExcess` 와 같은 처분이다. */
   ringRestMax: 1.1,

// 타입 주석 — 역할만 바꿨다(값은 계속 산출·인쇄)
+  /** ★ v5-25 — **보고 값**(판정 제외). v5-24 한 판만 판정 채널이었다. */
   ringRestRatio: number;
```

```
 ⟹ **게이트 판정 채널은 넷이다** — ③a 관통 ≤ 0.5 mm · 자기관통 교차 0 · 정착 창 순변위 ≤ 0.1 mm · pinned 0.
 ★ 두 비율(`ringExcess`·`ringRestRatio`)과 딸린 값(`ringM`·`ringBodyM`·`ringAllowM`)은 **계속 인쇄**한다.
 ★ **되돌림 = 그 한 줄을 되쓰는 것**(문턱을 지우지 않았다).
```

**정본 재판정 — 편입 변동 0**(계기 `scripts/v5Rejudge.ts` · 굽기 0)

| 항목 | 값 |
|---|---|
| 재판정 칸 | **37** |
| 건너뜀 | **71**(정본 정착 blob 없음 69 = 판정 대조 69칸 · 원리적 불가 + `blob n ≠ 장면 n` 2) |
| 옛 pass | **37 / 37** |
| **새 pass** | **37 / 37** |
| **변동 칸** | **0** |
| 휴지비 분포(보고 값) | 최소 1.059141 · p25 1.074616 · 중앙 1.088685 · p75 1.105991 · 최대 1.173295 |

```
 ★ §0-5ㄱ 에 값 보기 «전» 적은 **구조적 예측이 그대로 맞았다** — 채널을 내리는 것은 `fails` 에서 한 항목을
   빼는 일이라 `pass → fail` 은 원리적으로 불가능하고, `fail → pass` 는 «목선 단독» 실패 칸에서만 난다.
   v5-24 가 그 칸을 **0개**로 쟀으므로 **변동 0** 이 예측이었고 실측이 같다 ⟹ **갈래 C 아님.**
 ★ **한정** — A포즈 29칸은 정본 정착 blob 이 T포즈 계열뿐이라 **재판정 불가**다(v5-24 §2ㄹ 와 같은 한정).
   다만 위 구조적 논거는 «칸에 무관»하므로 A29 에서도 `pass → fail` 은 나올 수 없다(값으로는 미확인).
 ★ 휴지비는 **판정에서 내렸으므로** 중앙 1.0887·최대 1.1733 이 «실패»를 뜻하지 않는다 — 보고 값이다.
```

### §1-② S 던짐의 자리 — ★★★ **원인은 «앵커»이고 `SH_DROP` 은 덜어준다**

플래그를 두 줄로 갈라 네 판본을 f0 로 쟀다(`asm1x: true | 'anchor' | 'drop'` · `true` 면 둘 다 = v5-22~24 와 항등).

| 판본 | `n` | 제도 분할 | 던짐 | 최소쌍 mm | 교차 | 최악 쌍 |
|---|---|---|---|---|---|---|
| **off** | 8354 | `N_sh` 15 · `N_nk` 22 · `N_side` 48 · `nuB` 52 · `nvB` 73 | 없음 | **2.2616** | 0 | `front↔back`(정상) |
| **앵커만** | 8354 | (off 와 같다 — 앵커는 제도를 안 바꾼다) | **던짐** | **0.08915** | **6** | **`back↔sleeveL`** |
| **`SH_DROP`만** | 8148 | `N_sh` **17** · `N_nk` 22 · `N_side` **41** · `nuB` **56** · `nvB` **66** | **없음** | **2.25036** | 0 | `front↔back`(정상) |
| 둘 다 | 8148 | (drop 과 같다) | 던짐 | **1.39419** | 0 | `back↔back` |

**최악 쌍의 «자리»**(격자 해독 — 형제 판본이 보고한 제도 분할로 폭을 확정했다 · 추측 0)

```
 앵커만 (nuB 52 · nvB 73 · off 와 `n` 8354 동일 ⟹ 같은 격자)
   A 삼각형 14976 : back(**i0**, j71) y1298.5 · back(i1, j71) y1298.5 · back(**i0**, j72 = nvB−1) y1307.2
   B 삼각형 15929 : sleeveL(off 176) y1275.8 · sleeveL(off 227) y1303.0 · sleeveL(off 226) y1297.7
   붙는 높이 **1275.792 ~ 1307.159 mm**(`Y_NECK` 1315.769 아래 **8.6 ~ 40.0 mm**)
   ★ **`i0` = 패널 가장자리 열 = 암홀 봉제선**(`dEdge` 0) ⟹ **몸판 암홀 가장자리가 소매와 겹친다.**

 둘 다 (nuB 56 · nvB 66 · drop 과 `n` 8148 동일 ⟹ 같은 격자)
   A 삼각형 14716 : back(i22, j65 = nvB−1) y1279.0 · back(i23, j65) y1271.9 · back(i22, j66 = nvB) y1288.1
   B 삼각형 14719 : back(i24, j65) y1264.9 · back(i24, j66) y1273.8 · back(i23, j66) y1280.9
   붙는 높이 **1264.860 ~ 1288.118 mm** · `dEdge` = min(22, 34) = **22 > N_sh 17** ⟹ **목선 구간**
```

```
 ★★★ **가른 답** — `SH_DROP` «만» 켜면 **통과한다**(2.25036 · 최악 쌍이 off 와 같은 `front↔back`) ·
   앵커 «만» 켜면 **가장 심하게 던진다**(0.08915 mm · **교차 6개** · `back↔sleeveL`).
   ⟹ **던짐의 원인은 «앵커 정정»이다.**
 ★★★ 그리고 **`SH_DROP` 이 앵커의 피해를 «덜어준다»** — 둘 다 켜면 0.08915 → **1.39419** 로 15.6배 완화되고
   **교차가 6 → 0** 이 되며, 자리가 **암홀 가장자리 → 목선 구간**으로 옮긴다.
   기전(값이 가리키는 것) — 앵커는 옷을 `Y_NECK` 대역으로 **끌어올려** 맨 윗줄이 몸의 목·어깨 대역에 들어가고
   **암홀 가장자리가 소매와 만난다**. `SH_DROP` 은 어깨 토막을 **내려** 그 만남을 줄인다.
 ★ **「off 에서는 왜 안 겹치는가」의 답** — off 는 옷을 `Y_TOP`(1238.046)에 매달아 맨 윗줄이 몸의 목 대역에
   들어가지 않는다. 최악 쌍도 허리 아래(1116.7~1125.5 mm)의 정상적인 `front↔back` 이다.
 ★ **처방 0** — 이 판은 고치지 않았다(프롬프트 「처방 0 · 사실만」).
 ★ **한정** — `anchor`/`drop` 은 **진단용 값**이다. 정본 후보는 `asm1x: true`(둘 다)이고, S 는 그 판본에서
   **여전히 던진다** ⟹ ③에서 S 는 **보류**다(§0-5ㄷ 그대로).
```

### §1-④ 승격 재료 — **판단 0**

**ㄱ. 108 계획 — v5-24 가 바꾼 순서에서 한 겹이 «빠진다»**

```
 v5-24 는 「자 확정 → 37칸 재판정 → 69칸은 재굽기로만」이었다. 이 판의 ①이 **편입 변동 0** 을 보였으므로
 **재판정 단계가 «확인»으로 줄어든다** — 채널을 내리는 것은 어느 칸의 판정도 나쁘게 만들 수 없다(구조적).
 ⟹ 순서 = **① 채널 내림 확정 → ② 37칸 변동 0 확인(완료) → ③ A포즈 29칸 재굽기 → ④ 제공 재발행**.
 ★ **69칸(판정 대조)은 재굽기 없이도 «판정이 나빠지지 않는다»** — 목선 채널이 사라졌을 뿐이다.
   다만 그 칸들의 «값»은 blob 이 없어 이 판에서 확인할 수 없다(원리적 한계 · v5-24 §2ㅁ).
 ★ 굽기 «칸 수»는 그대로 **108**(정점 39 + 판정 69)이고 순서는 **M→S→L→XL**(v5-22 §1-④ 인용) ·
   시간 **≈17 h** 는 **프롬프트 산정이고 이 판의 측정이 아니다**.
```

**ㄴ. 제공 목록 재발행**

```
 v5-11 §④ 사슬 그대로. ★ **v5-24 가 적은 「편입 11칸 감소」는 이 판으로 «취소»됐다** — 채널을 내렸으니
 감소가 없다(변동 0) ⟹ 재발행은 **편입 집합을 바꾸지 않는다**.
 바뀌는 것은 산출의 «채널 목록»이다 — `l3-gate-*.json` 의 `fails` 에서 목선 줄이 사라지고
 두 비율은 `채널` 에 값으로 남는다 ⟹ 재발행 시 **판본 접미를 새로 끊고 옛 판본은 보존**한다(삭제 0).
```

**ㄷ. v3 동결 예외 등재 항목**(누적 · 이 판이 한 건 더한다)

```
 v5-22 가 4건째(앵커 정정 · `SH_DROP`)를 적고 v5-24 가 5건째(목선 판정 채널 «교체»)를 적었다.
 ★ 이 판은 5건째를 **«내림»으로 고쳐 등재**한다 — 교체(휴지 기준 판정)는 **한 판만 살았다가 기각**됐다.
   등재 문언 셋:
     ① 무엇을 바꾸나 = `s4Gate.ts` 판정 **한 줄 제거**(값·문턱은 남긴다) ⟹ 판정 채널 5 → **4**.
     ② 왜 v3 인가 = 게이트는 **모든 굽기의 판정**이라 플래그 뒤에 둘 수 없다.
     ③ 되돌림 = 그 한 줄을 되쓰는 **한 줄**(문턱을 지우지 않았으므로 복원 0).
 ★ **6건째 후보**로 `asm1x` 분리 플래그(`'anchor'|'drop'`)가 있으나 **진단용**이고 정본 거동을 바꾸지 않는다
   (`true` 면 v5-22~24 와 항등) ⟹ 예외 등재 대상인지는 **전략 세션의 판단**이다.
```

**ㄹ. S 미해결 시 «그 칸만 보류» 규약**(프롬프트가 요구한 항목 · 문언 초안)

```
 **문언(초안)** — 「`asm1x` 승격에서 **조립 자기검사가 던지는 칸은 그 칸만 «보류»**로 등재하고,
 나머지 칸의 승격을 막지 않는다. 보류 칸은 제공 목록에서 «미제공»으로 표기하고 사유(던짐 문언 원문)를 함께 적는다.」
 ★ 이 판의 값이 그 규약을 필요하게 만든 근거 — `c87.5-h155-s40_S` 는 `asm1x: true` 에서 **여전히 던진다**
   (`최소쌍 back↔back 1.39419 mm`) 반면 `supima-L`·`기본 M` 은 조립이 서고 굽기까지 간다.
 ★ **원인이 앵커라는 것이 이 판에서 나왔으므로**(§1-②) 보류는 «영구»가 아니라 **다음 판의 표적**이다 —
   앵커가 맨 윗줄을 목 대역으로 올릴 때 **암홀 가장자리 열(i0)이 소매와 만나는 것**을 어떻게 다룰지가 질문이다.
 ★ **몇 칸이 보류인지는 이 판이 모른다** — 108칸 중 던지는 칸의 전수는 세지 않았다(미집행).
```

### §1-③ 회귀 굽기 — ★ **`supima-L` pass · `기본 M` fail(교차 1)** · S 보류

굽기 = 2호기(DESKTOP-OV72A46 · GTX 1660 Ti · `arch=cuda` · f64) · 잡 `gpu/bake/jobs/v5-25-on.json` ·
`frames` 400 · `ramp` · 총 벽시계 **1023.4 s** · 실패 **0칸**.
★ **조립은 v5-22 와 비트 동일**이라 v5-22 가 2호기에 올린 자산(`asm-v522onA_{L,M}.bin` 등)을 **재사용**했고,
바뀐 **게이트 한 파일만** 보냈다(`src/v3/s4Gate.ts`). 물리가 재현된 것이 값으로 보인다 —
`vs 정본 중앙`이 **68.930630 / 70.765870 mm** 로 v5-22 와 **같은 수**다.

**게이트 — 판정 채널 넷**

| 채널 | 문턱 | `supima-L` | `기본 M` |
|---|---|---|---|
| ③a 관통 최대 mm | ≤ 0.5 | **0.33132481902223526** ✅ | **0.3752726257434258** ✅ |
| 관통 정점 수 | 참고 | 881 | 927 |
| **자기관통 교차** | = 0 | **0** ✅ | **1** ❌ |
| 최소 쌍거리 mm | 참고 | 1.970646652526943 | 1.9798624570389312 |
| 정착 창 순변위 mm | ≤ 0.1 | **0.07229682189324102** ✅ | **0.03930519393313997** ✅ |
| pinned | = 0 | **0** ✅ | **0** ✅ |
| 발산 | — | false | false |
| 봉제 간극 중앙 / 최대 mm | 참고 | 2.0903705401250483 / 3.8480564011988636 | 2.107906669570362 / 4.008345064334165 |
| λ 최대 | 참고 | 1.4609440147599382 | 1.4559966679005811 |
| **`pass`** | — | **true** | **false** |
| `fails` | — | **`[]`** | **`['자기관통 교차 1 ≠ 0']`** |

**목선 두 비율 — 보고 값(판정 제외 확인)**

| 칸 | 옛 초과비 | 링 cm | 허용 cm | 새 휴지비 |
|---|---|---|---|---|
| `supima-L` | **1.0021454460806467** | 49.488944768371766 | 49.38299621269665 | 1.0427966483091002 |
| `기본 M` | **1.0210830840101046** | 49.42449231916582 | 48.40398699492775 | 1.037657 |

```
 ★★★ **v5-22 에서 «목선 하나»로 떨어졌던 `supima-L` 이 통과했다** — 같은 옷·같은 물리(`vs 정본` 동일)에
   **판정만** 바뀌었다. 두 비율은 여전히 1 을 넘지만(1.0021 · 1.0211) **판정에 들어가지 않는다** ⟹
   채널 내림이 뜻대로 작동한다.
 ★ **`기본 M` 은 «자기관통 교차 1» 하나가 남는다** — v5-22 에서도 있던 그 한 개이고,
   v5-22 §1-②ㅁ 가 **겨드랑이 대역 «밖»**임을 쟀다(대역 교차 0 ↔ 전체 1). 자리는 **아직 미특정**이다.
 ⟹ ③은 **1칸 pass · 1칸 fail** ⟹ 사전 등재 **갈래 B**(③ fail ⟹ 값 상신).
```

**층3 5행(중앙 mm) · `pressN` 전부 0**

| 칸 | 목선 | 가슴 | 허리 | 밑단 | 소매 |
|---|---|---|---|---|---|
| `supima-L` | 3.6944 | 4.0668 | 69.2904 | 45.1043 | 6.0533 |
| `기본 M` | 2.2718 | 3.7102 | 61.7286 | 25.7186 | 6.4275 |

`pressN` = **0 / 0**(두 칸 다 「눌림」 정점 0) ⟹ 판정문이 목선 결함 포착을 분담시킨 **층3 눌림 채널은 이 두 칸에서 0** 이다.

**어깨 걸림(정착) · 옷 y 극값**

| 칸 | `Y_TOP` mm | **어깨선 위 옷 정점** | 옷 최고점 mm | 옷 밑단 mm |
|---|---|---|---|---|
| `supima-L` | 1393.4802 | **1002** | 1468.378 | 783.411 |
| `기본 M` | 1403.9385 | **832** | 1471.023 | 820.494 |

★ 편입 규약 **172** 를 두 칸 다 크게 넘는다(v5-22 와 같은 값 — 물리가 재현됐다).

**캡처 4장** — `docs/v5/캡처/25-{L,M}-{1-front,2-side}.png`(정사영 오프스크린 · 화면 판정 0).
★ **S 는 보류**(조립이 `asm1x: true` 에서 던진다) ⟹ 캡처 **0**.

---

## §2 자기검사

```
ㄱ ★★★ **조립 sha 불변 — 통과**(갈래 D 의 자). 기점 `ce22e0c` 워크트리 대조 · `scripts/v5AsmSha.ts` ·
  **T 108/108 동일**(던지는 26칸 문언까지) · **A 29/29 동일**(던짐 0) ⟹ **갈래 D 아님.**
  ★ 이 판은 `asm1x` 의 타입을 넓혔는데도 불변이다 — `true` 면 두 자리가 함께 켜져 v5-22~24 와 항등이고,
    키가 없으면 분기 전체가 죽는다.
ㄴ ★ **`src/` diff = 세 파일 · 24+/11−** — `s4Gate.ts`(판정 줄 제거) · `garmentScene.ts`(분리 플래그 두 줄 +
  두 자리 치환) · `dressRun.ts`(타입·전달). **프롬프트가 허용한 범위**(「목선 채널 판정 제외」 + 「플래그 두 줄」)다.
ㄷ ★★ **①의 검증 정의역이 37칸이다** — A포즈 29칸은 정본 정착 blob 이 T포즈 계열뿐이라 **재판정 불가**
  (v5-24 §2ㄹ 와 같은 한정). 판정문의 「옛 29칸 편입 변동 0」은 **값으로 확인하지 못했다**.
  다만 구조적 논거(채널 제거는 `fails` 에서 항목을 빼는 일)는 칸에 무관하므로 `pass → fail` 은 나올 수 없다.
ㄹ ★ **③의 세 번째 칸(S)은 굽지 않았다** — `asm1x: true` 에서 조립이 던진다(§1-②). §0-5ㄷ 에 값 보기 «전»
  적은 대로 **진단용 `anchor`/`drop` 판본으로는 굽지 않았다**(정본 후보가 아니다).
ㅁ ★ **`기본 M` 의 교차 1개 «자리»를 이 판도 특정하지 않았다** — v5-22 가 겨드랑이 대역 밖임을 쟀을 뿐이다.
ㅂ ★ **굽기는 자산 재사용이다** — 조립을 다시 반출하지 않고 v5-22 가 올린 것을 썼다. 근거 = `asm1x: true`
  경로가 비트 동일(플래그 분리가 `true` 에서 항등)이고, 실측으로 `vs 정본 중앙`·`RAMP_N`·정착 프레임이
  v5-22 와 **같은 수**다. 그래도 «다시 반출해 sha 를 비교»한 것은 아니다(한정).
ㅅ ★ **캡처는 v5-22 것과 바이트 크기가 같다**(114162 / 82986 / 112523 / 81387) — 같은 정착 상태를 같은
  렌더러로 그렸으므로 그렇게 나오는 것이 맞다. 새로 그렸고 자리는 `25-*` 로 갈랐다.
ㅇ **문턱 신설·이동 0** — 이 판은 문턱을 **하나도 쓰지 않았다**(내렸다). 남은 넷은 v3 누적 등재값 그대로다.
ㅈ **물리 0줄 · 처방 0(②) · ecc 에이전트 0 · 스킬 0.**
ㅊ ★ **집행 환경** — `git` 이 Xcode 라이선스 미동의로 멈춰 `DEVELOPER_DIR=/Library/Developer/CommandLineTools`
  로 우회했다(v5-23 부터 세 판 연속 · 기계 설정·저장소 파일 변경 0).
```

---

## §4 판정 — **갈래 B**

```
 A = ①(변동 0)+③ pass ⟹ 상신(→ 108)   … ✗ ③에서 `기본 M` 이 fail 이다
 ★ B = ③ fail ⟹ 값 상신                 … **✔ 이것이다**
 C = ① 변동 > 0 ⟹ 즉시 정지·상신        … ✗ **변동 0**(예측 적중)
 D = 조립 sha 변동 ⟹ 정지                … ✗ **T 108/108 · A 29/29 동일**
 E = 그 외 ⟹ 정지                        … ✗
 사후 갈래 신설 0.

 ① = **통과** — 채널 내림 구현(판정 5 → **4**) · 정본 재판정 **37 pass → 37 pass · 변동 0** ·
      두 비율은 보고 값으로 병기 · 문턱·값 **삭제 0** · 조립 sha 불변.
 ② = **원인을 가렸다** — `SH_DROP` 만 = **통과**(2.25036) · 앵커만 = **최악**(0.08915 · 교차 6 ·
      `back(i0)↔sleeveL` = 암홀 가장자리) · 둘 다 = 1.39419(교차 0 · 목선 구간) ⟹
      **던짐의 원인은 앵커 정정**이고 **`SH_DROP` 이 15.6배 덜어준다**. 처방 0.
 ③ = **1칸 pass · 1칸 fail** — `supima-L` **pass**(v5-22 에서 목선 하나로 떨어진 칸이 통과했다) ·
      `기본 M` **fail**(`자기관통 교차 1 ≠ 0` **하나뿐**) · S **보류**.
 ④ = **적었다** — 108 순서에서 재판정이 «확인»으로 줄고 · 제공 편입 감소는 **취소**되고 ·
      동결 예외 5건째를 «교체»에서 **«내림»으로 정정** · S 보류 규약 초안. 판단 0.

 ★★★ 이 판의 핵심 값 셋
   ㉠ **채널 내림이 편입을 흔들지 않는다** — 37칸 변동 0(구조적 예측 적중).
   ㉡ **판정만 바꿔 `supima-L` 이 통과했다** — 같은 물리(`vs 정본` 68.930630 mm 동일)에 목선 비율은
     여전히 1.0021 이지만 판정 밖이다.
   ㉢ **S 던짐의 원인은 앵커다** — `SH_DROP` 만으로는 통과하고, 앵커만으로는 암홀 가장자리가 소매와 만난다.
```
