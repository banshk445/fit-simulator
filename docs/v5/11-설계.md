# v5-11 — 조립 2세대 «설계서»(어깨 경사 자유도 · 내려 입히기 배치 · 회귀 계획) · 코드 0 · 굽기 0

```
집행 기계  에어(맥) — 이 판은 **코드 0 · 굽기 0 · 물리 0** 이다(2호기 접촉 0)
브랜치     v5-11-asm2-design   기점 9f87ae1(v5-10 HEAD · 확인 = git log -1 이 9f87ae1)
약정       물리 0줄 · `src/` diff **0**(설계 문서 + 사실 계기만) · 굽기 0 · ecc 0 · §0 선커밋 ·
           브랜치 인쇄 · 보고 = docs/v5/보고/11.md · 콘솔 4줄
산출       **docs/v5/설계-조립2세대.md**(설계서 정본) · 이 노트 · 보고 · 색인 · 볼트 · metrics · 귀책 16건째
```

## §0-1 판정문 (전략 세션 · v5-10 §4 수령 · 사용자 결정 · **원문**)

```
갈래 C 수용 · 귀책 16건째(전략 세션): 「상단 행만」 국소판은 행 간격 8.9mm 에 48~82mm 상승이라
산수로 걸렀어야 함 · ★★★ 최종 정체 = 조립(올려 입히기 · v3-21)과 제도(어깨 경사 부재 —
평면 패턴 어깨선이 수평 · 늘림 필요분이 전부 수직 상승)가 함께 만든 정의역 한계 ·
물리·물성 무죄(v5-3~10) · 사용자 결정 = 조립 2세대(㉮): 패턴에 어깨 경사(6번째 자유도 ·
실측표에 없는 항 ⟹ 템플릿 상수로 이름·값 공개 · v5-1 규율) + 배치 «내려 입히기» 전면 재설계 ·
첫 판 = 설계서(코드 0)
```

## §0-2 수령 — 집행 «전»에 읽은 코드 사실 6건

```
 ㄱ ★ **패턴 맨 윗줄이 «단 하나의 높이»로 간다.** `Y_HEM = Y_TOP − L`(`garmentScene.ts:443`) ·
    `bodyPoint` 의 수직 사상이 `y = Y_TOP − (L − py)`(`:602`) ⟹ **`py = L` ↦ `y = Y_TOP` 고정**.
    `Y_TOP` 소비처는 그 파일에 **15곳**(`:41 · 186 · 205 · 212 · 443 · 468 · 538 · 540 · 551 ·
    580 · 581 · 602 · 630 · 740 · 761 · 787 · 953`)이고 전부 이 앵커에 딸려 있다.
 ㄴ ★ **상단 경계의 세 토막이 모두 `py = L` 에 끝난다** —
    `shL = line([−SW/2, L], [−NECK_A, L], N_sh)` · `shR = line([NECK_A, L], [SW/2, L], N_sh)`(`:416-417`) ·
    `neckF` 양 끝 `(±NECK_A, L)`(`:349-352`) · **`armR` 의 위 끝점도 `(SW/2, L)`**(`:347-348`).
    ⟹ 어깨 경사를 넣으면 **`armR` 끝점이 함께 내려가야 한다**(암홀 곡선의 정의역이 바뀐다).
 ㄷ ★ **앞판과 뒤판이 «같은 상단 곡선»을 쓴다** — `coons('front', base, botB, **topB**, lefB, rigB)` ·
    `coons('back', base, botB, **topB**, lefB, rigB)`(`:426-427` · 같은 `topB` 배열).
    ⟹ 제도 관행의 **앞/뒤 경사 차이**(아래 §0-2ㅁ)를 현행 패턴은 **표현할 자유도가 없다**.
 ㄹ ★★★ **실측표의 「총장」과 코드의 앵커가 어긋나 있다.**
    `src/v5/specToPattern.ts:6` 원문 — 「총장 `totalLength` **옷을 눕혀 «뒷목 중심»에서 밑단까지**」.
    그런데 코드는 `L = totalLength/100` 을 `Y_HEM = Y_TOP − L` 로 **«어깨끝 높이»에 매단다**(ㄱ).
    ⟹ 어긋남 = **`Y_NECK − Y_TOP`** = v5-10 §1-① 실측 **82.2598 mm**(mst-over · SW 52) /
       **48.5991 mm**(supima · SW 44.5). **앵커는 «고를» 문제가 아니라 «정정»이다.**
 ㅁ **몸 도출량은 `solveB` 로 «암홀 둘레»에 묶여 있다** —
    `ARM_A = (W − SW)/2`(`:299`) · `ARM_D = solveB(ARM_G/2, b ⟼ arcLen(famArm(ARM_A, b)))`(`:305`) ·
    `CAP_H = solveB(ARM_G/2, b ⟼ arcLen(famCap(CAP_W, b)))`(`:311`) ·
    `NECK_B = solveB(NECK_G/4, b ⟼ arcLen(famNeck(NECK_A, b)))`(`:316`) ·
    `Y_ARM = L − ARM_D`(`:318`) · `SH_LEN = SW/2 − NECK_A`(`:319`) ·
    `panelHalfWidth` 가 `Y_ARM`·`ARM_D`·`ARM_A` 를 읽는다(`:531-534`).
    ⟹ 어깨 경사는 **`SH_LEN`·`armR`·`Y_ARM` 과 동시에** 건드린다(독립 자유도가 아니다).
 ㅂ **v5-10 이 남긴 두 사실** — 능선의 **z 는 산출 불가**(폴리라인이 `SH_LEN` 의 6.99~7.22배) ·
    「상단 행만」 국소판은 하위 엣지가 **10배**(신장 중앙 14.24 / 11.89 ↔ 현행 1.06)로 늘어난다.
```

## §0-3 사전 등재 갈래 (회차 프롬프트 그대로 · 사후 신설 0)

| 항 | 산출 | 비고 |
|---|---|---|
| **①** 제도 — 어깨 경사 자유도 형식화 | 어깨선을 목점→어깨점 **하강 직선**(각도 또는 낙차 `SH_DROP`) · 값의 출처 (a) 제도 관행 상수(**문헌 인용 · 값·범위**) (b) 몸에서 도출 (c) 실측표 확장 · 각 장단·v5-1 규율 적합성 · **몸 도출량과의 상호작용**(어깨선 y 를 읽는 자리 **전수**) | 갈래 없음 · 사실 |
| **②** 배치 — «내려 입히기» 절차 설계 | 능선 위 일치점(**z 산출 불가의 해법 후보 나열 + 값 보기 «전» 선택 기준**) · 앞뒤 패널 **드레이프** 순서 · 소매·암홀 봉제 관계(v4-37 축 이동 유지 여부) · 목선 링 f0 예측 · **램프 역할 변화**(`RAMP_N` 예측) | 설계 |
| **③** 정의역 | 새 조립이 «통하는» 범위 예측(SW 상한 · 드롭숄더 한계 · 팔 윗면 기하 조건) · **현행과의 교집합**(레귤러핏 동일성 판정 채널) | 예측 |
| **④** 회귀·승격 계획 | 플래그 → 진단 3칸 → T포즈 108 sha → A포즈 29칸(≈6 h) → 제공 목록 재발행 → v3 동결 예외 · **각 단계 「되돌리기 한 줄」** · 예상 판 수·시간 | 계획 |
| **⑤** 사전 등재 | **v5-12** 갈래·문턱·정지 조건 **초안**(값 보기 «전» · 회복은 정도로 · 172 는 편입 규약에서만) | 초안 |

**§4** — **A** = ①~⑤ 설계서 완성 ⟹ 상신(전략 세션 검수 → v5-12 발주) /
**B** = 사실 결손 항목(사유) ⟹ 부분 상신. **사후 갈래 신설 0.**

## §0-4 사전 결정 4건 (집행 설계 · 값 보기 «전»)

```
 ㄱ **(a) 문헌 인용은 «1차 출처를 열어» 확인한 것만 적는다.** 검색 요약만 있고 원문을 못 연 값은
    **결손으로 적고 §4 를 B 로 내린다**(문언 창작 0 · 함정 44 의 정신 — 볼트 밖 출처도 같다).
 ㄴ **설계서는 «선택하지 않는다».** ①의 (a)(b)(c) 와 ②의 z 규칙 후보는 **나열 + 판정 기준**까지다.
    고르는 것은 전략 세션·사용자 몫이고, 이 판이 고르면 사후 결정이 된다.
 ㄷ **예측은 «예측»으로 표시한다** — ②③의 수는 전부 「예측(반증 대상)」 표기를 달고,
    v5-10 까지의 **실측값에서 산수로 유도**한 것만 적는다(새 가정 0).
 ㄹ **`git add` 는 명시 경로만**(v5-10 §0 신설 규약) · 커밋 전 `--cached --name-only` 확인.
```

## §0-5 §0 사무 1건

```
 ★ **귀책 16건째 등재**(판정문 지시) — 「전략 세션이 «상단 행만» 국소판을 발주했다 —
   행 간격 8.9 mm 에 48~82 mm 상승이면 **산수로 걸러졌어야** 한다」. 볼트 전략 세션 번호 대장.
```

---

## §1 집행 — 설계서 `docs/v5/설계-조립2세대.md`(**399줄**)로 낸다

이 노트는 «집행 인쇄와 판정»만 담고, ①~⑤의 본문은 **설계서가 정본**이다. 아래는 그 설계서가
**값으로** 세운 것들의 목록이다(수는 전부 설계서·v5-3~10 실측에서 읽어 옮겼다).

### ① 제도 — 어깨 경사 자유도

```
 ㉠ ★★★ **「어깨선이 수평」은 배치가 아니라 «제도»의 성질이다** — `shL`·`shR` 의 두 끝 `py` 가 같고
    (`garmentScene.ts:416-417`) 배치의 수직 사상은 이미 `y = Y_TOP − (L − py)`(`:602`)로 `py` 를 높이로
    옮긴다 ⟹ **제도에 경사를 넣으면 배치가 그대로 따라온다.**
 ㉡ 형식화 = **`SH_DROP` 한 항**(낙차형) + **앵커 정정**(`py = L ↦ Y_NECK` · `Y_HEM = Y_NECK − L`).
    각도형 환산 = **25.19°**(mst · atan(82.2598/174.969)) · **19.48°**(supima).
    ★ 앵커 정정은 «선택»이 아니다 — 실측표의 총장이 **「뒷목 중심에서」**(`specToPattern.ts:6`)인데
      코드가 **어깨끝 높이**에 매달고 있다(어긋남 = `Y_NECK − Y_TOP` = **82.2598 / 48.5991 mm**).
    ★ `SH_DROP = Y_NECK − Y_TOP` 이면 **어깨점이 정확히 몸의 `Y_TOP` 에 앉는다**(산수).
 ㉢ 값의 출처 세 선택지 — **고르지 않았다**(§0-4ㄴ) · 판정 기준 ㉠~㉣ 를 함께 등재.
    (a) **문헌 1차 출처 2건 확인** — Pattern Academy 「뒤 **2 in** · 앞 **1¼ in** · 전 사이즈 공통」 ·
        My Golden Thimble 「표준 **2½ in** · 높은 어깨 **2 in** · 처진 어깨 **3 in**」
        ⟹ **관행 범위 50.8 ~ 76.2 mm**(표준 63.5) · 우리 몸 실측 **48.5991**(SW 44.5) ·
           **82.2598 mm**(SW 52)가 그 범위의 **양 끝 바로 밖**에 놓인다(대조 · 인과 단정 0)
    (b) 몸에서 도출 — 장: 한 식으로 SW 전 구간 · 새 상수 0 / **단: 옷이 몸에 종속된다**(v5 축과 어긋남 · 등재)
    (c) 실측표 확장 — **지금은 불가**(쇼핑몰 표는 눕혀 재므로 경사를 원리적으로 못 담는다)
    ★ **부분 채택 후보**(등재만) — `SH_DROP` 은 템플릿 상수로 두고 **앵커만 정정** ⟹ 남는 오차가
      **14.9 mm**(SW 44.5 · 63.5−48.5991) · **18.8 mm**(SW 52 · 82.2598−63.5) — 그 흡수 여부가 v5-12 측정 대상
 ㉣ ★ **앞/뒤 경사 차이를 현행 패턴은 «표현할 수 없다»** — 앞판·뒤판이 **같은 `topB`** 를 쓴다(`:426-427`)
    ⟹ 문헌의 1¼ ↔ 2 in 차이는 **`topB` 분할 자유도**가 필요하다(2막으로 등재).
 ㉤ **어깨선 y 를 읽는 자리 전수 = 12곳**(설계서 ①-5 표) — 경사가 **10개 자리**를 동시에 건드리고
    그중 `:443` 앵커 하나가 **9개 소비처**를 끌고 간다. 「한 상수 추가」로 끝나지 않는다.
    ★ `ARM_D`·`CAP_H` 는 `solveB` 로 **암홀 둘레에 묶여** 있어 경사와 **독립이 아니다** ⟹ 재도출 필요(v5-12 항목).
```

### ② 배치 — «내려 입히기»

```
 ㉠ **능선 `z` 후보 4종**(측지선 · 평면 절단 · 배치 machinery 차용 · 앞뒤 대칭 평균)과
    **값 보기 «전» 선택 기준 ㉠~㉣**(z 가 규칙으로 결정되는가 · 새 알고리즘·상수 수 · 몸 표면 위인가 ·
    v5-10 직선 하한 **19.2396 / 14.5044 cm** 와 정합하는가). **고르지 않았다.**
 ㉡ 절차 **S0~S6** — 제도 → 목선 앵커(S1) → 능선 일치점(S2) → ★**드레이프(S3 · v5-10 이 «하지 않았던»
    단계**) → **밀어냄(S4)** → 소매(S5 · **현행 유지** · 근거 = v5-9 「소매 관 무죄」) → 램프(S6).
 ㉢ ★ **램프의 역할이 바뀐다** — 어깨 `rest0` 이 0 이면 램프가 감을 것은 암홀·옆선·소매밑뿐이다.
    **`RAMP_N` 예측 89 → ≈52**(mst) · **77 → ≈42**(supima) — 근거는 v5-9 `rampOrder` 인쇄의
    `N_ot` **52 / 42** 라는 **실측**이다(예측이 실측에 얹혀 있다).
 ㉣ **예측 P1~P7** 등재(반증 대상) — P1 링 59.2/60.1 cm · P2 봉제쌍 0.0000 · P3 신장 ≈1.0 ·
    P4 `RAMP_N` · P5 끌어올림 소멸 · P6 어깨위 정점 > 0 · **P7 = S4 의 시작 응력(«미지» — 「작을 것」이라 적지 않았다)**
```

### ③ 정의역

```
 ㉠ ★★★ **몸이 정하는 경계가 값으로 나왔다** — 팔축 `s` 가 처음 양수 되는 x = **20.431 / 20.434 cm** ·
    `+SEP` 여유가 처음 음수 되는 x = **20.793 / 20.795 cm** ⟹ **x\* ≈ 20.4~20.8 cm**
    (**두 옷에서 소수 둘째까지 같다** — 몸이 정하는 값이고 옷과 무관) ⟹ 「봉제선이 몸통 쪽에 머무는」
    SW 상한 ≈ **40.9 ~ 41.6 cm**.
 ㉡ ★★ **현행 두 옷은 둘 다 그 밖이다**(supima 22.25 · mst 26.00 cm) — 즉 **레귤러핏조차 어깨 끝이
    이미 팔 위에 있다**. 그런데 supima 는 오늘 **pass** 다 ⟹ 「팔 위」는 **그 자체로 실패 조건이 아니다**.
 ㉢ ★ **설계 수정 1건이 여기서 나왔다** — 어깨끝 여유가 supima **−0.426 mm** · mst **+0.027 mm**
    ⟹ **`+y` 오프셋으로는 `SEP` 를 못 만든다** ⟹ S4 밀어냄은 **필수**이고 능선점도 **SDF 기울기 방향**
    으로 밀어야 한다.
 ㉣ **SW 상한은 구속하지 않는다(실측)** — 코드가 던지는 상한 **SW > ≈147 cm**(몸 `|x|` 최대
    **73.64404201507568 cm**) · 능선이 단조 하강을 유지하는 상한 **x ≤ ≈38 cm ⟹ SW ≤ ≈76 cm**
    (팔 위 기울기 `Δy/Δx` 평균 **≈ −0.86** ⟹ **≈ 40.7°** · x = 40 cm 에서 **+0.3817 로 부호 반전**).
 ㉤ **레귤러핏 「동일」의 자는 이미 있다** — `CLAUDE.md` v4 트랙 「같은 옷」 3조건(대역 이름 5/5 ·
    게이트 pass·fails 일치 · 표시 5행 중앙 차 ≤ 0.05 mm) ⟹ 새 채널 0 · 새 문턱 0.
    ★ 단 ③번 조건은 **성립하지 않을 것**(예측)이라 「동일」의 뜻 (ㄱ)/(ㄴ)/(ㄷ) 을 **골라야 한다** —
      이 설계서는 고르지 않고, **(ㄷ) 제공 목록 동일**이 제품이 보는 것에 가장 가깝다는 사실만 적었다.
```

### ④ 회귀·승격 계획

```
 **R0~R8 8단계** · 각 단계에 **「되돌리기 한 줄」** 을 달았다(R1·R2 는 `unset ASM2` 하나) ·
 **안전선 = R1·R2 의 비트 불변**(깨지면 R3 로 가지 않는다).
 예상 — **판 수 4~5** · 굽기 **32~35칸** · 굽기 시간 **≈ 6.75 h**(자릿수 · 칸당 실측 평균 758.875 s).
 ★ **조립·계기 시간은 이 판에서 실측했다**(맥) — `v4AsmExport` **3.25 s** · `v4FitReport` **3.69 s** ·
   `v4ProductGate` **3.41 s** ⟹ 칸당 **10.35 s** · 137회 조립 **≈ 445 s** · 29칸 계기 **≈ 206 s**
   ⟹ 굽기 시간의 **0.3% 미만**(한정 = 맥 측정 · 2호기는 다르다).
 ★ **위험 항목 1건 등재** — R6 에서 제공 29칸이 «줄면» 제품이 후퇴한다 ⟹ 처분은 사용자·전략 세션 몫.
```

### ⑤ v5-12 사전 등재 초안

```
 §1 4항(① 비트 불변 3검사 · ② 배치 f0 채널 5종 · ③ 진단 굽기 **3칸**(supima·mst-over·**기본 M**) ·
 ④ 종합) · 문턱은 **전부 기존 자 인용**(바이트/sha 동일 · 몸 여유 ≥ SEP · 신장은 현행 자릿수 ·
 게이트 5채널 불변) · 갈래 **A~E**(D = 비트 불변 실패 ⟹ 즉시 정지) ·
 **「걸림」은 정도로 · 172 는 제공 편입 규약에서만** · 사후 갈래 신설 0.
 정지 조건에 **S4 밀어냄 미수렴**을 덧붙였다(상한은 v5-12 §0 에서 기존 자에서 유도 · 새 상수 0).
```

## §2 자기검사

| 항 | 약정 | 실측 |
|---|---|---|
| 기점 | `9f87ae1`(v5-10 HEAD) | ✅ |
| 코드 0 · 굽기 0 · 물리 0 | | ✅ `git diff --stat 9f87ae1 HEAD -- src/ gpu/ scripts/` = **빈 출력** · 굽기 **0칸** · 2호기 접촉 0 |
| `src/` diff 0 | 설계 문서 + 사실 계기만 | ✅ 새 계기 **0개**(기존 `v5RidgeProbe` 산출 json 과 `numpy` 로 몸 bin 을 직접 읽었다) |
| ecc | 에이전트·스킬 | **0 · 0** |
| §0 선커밋 | 집행 «전» | ✅ `3ec7bec` |
| 브랜치 인쇄 | | ✅ `v5-11-asm2-design` |
| 문헌 인용 | 1차 출처만 | ✅ **2건 확인 · 2건 인용 밖**(401 / 값 없음) · 비례식은 **적지 않았다** |
| 고르지 않기 | ①(a/b/c) · ② z 규칙 · ③ 「동일」 · ④ 위험 처분 | ✅ **4건 전부 부록에 「고르지 않은 것」으로 남겼다** |
| `git add` | 명시 경로만(v5-10 규약) | ✅ 네 커밋 모두 · 커밋 전 `--cached --name-only` 확인 |

**자기신고 — 한정 3건**

```
 1 **문헌 2건을 인용하지 못했다** — `theshapesoffabric.com` **HTTP 401** · `mellysews.com` 은 본문에
   어깨 경사 값이 **없다**. 검색 요약에 있던 **비례식**(어깨너비의 1/10 계열)은 1차 출처를 못 열어
   **값을 적지 않았다**(§0-4ㄱ 대로).
 2 **「어깨 봉제선이 자기 암홀보다 낮아지는 SW」는 재지 않았다** — `ARM_D`(몸·`ARM_G` 도출)와 묶여
   있다 ⟹ **v5-12 §1 측정 항목으로 등재**(추정 0).
 3 **조립·계기 시간은 «맥» 측정이다** — 2호기는 다르다(그 사실을 병기했다).

 미집행 0(설계 5항 전부 산출) · 문턱 이동 0 · 사후 갈래 신설 0 · 화면 판정 0 · 캡처 0(설계판).
 이 판 diff — `docs/v5/설계-조립2세대.md` **신설 399줄** · `docs/v5/11-설계.md` **신설** ·
 `.obsidian-log/전략세션-인수인계.md` **귀책 16건째 +2줄**.
```

## §4 갈래 판정 — **A**

```
 §0 등재 문언 — 「**A** = ①~⑤ 설계서 완성 ⟹ 상신(전략 세션 검수 → v5-12 발주) /
 **B** = 사실 결손 항목(사유) ⟹ 부분 상신」

 ① ~ ⑤ **다섯 항 전부 산출**됐고, 요청된 사실 중 **빈 칸이 없다**:
   ① (a) 문헌 **1차 출처 2건 · 값·범위 50.8~76.2 mm** 확보 · (b)(c) 장단·규율 적합성 · **전수 12곳**
   ② z 후보 **4종 + 선택 기준** · 절차 **S0~S6** · 램프 예측(실측 근거) · 예측 **P1~P7**
   ③ x\* **실측**(20.4~20.8 cm) · SW 상한 **실측**(비구속) · 교집합 판정 채널(기존 자 인용)
   ④ **R0~R8 + 되돌리기 한 줄** · 판 수·시간(굽기 실측 기준 + 조립·계기 **이 판 실측**)
   ⑤ v5-12 갈래 **A~E** · 문턱(기존 자 인용) · 정지 조건
 ⟹ **A**. 한정 3건(§2)은 **요청 항목의 결손이 아니라** ㉠ 인용 밖 2건(값 미기재) ㉡ 다음 판 측정 항목 1건
    ㉢ 측정 기계 한정 1건이다. **전략 세션이 이를 결손으로 보면 B 로 내려도 값은 전부 냈다.**
```
