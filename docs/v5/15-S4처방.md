# v5-15 — S4 붕괴 처방 2후보 가상 실험(f0 · 조립만) → 승자 · 비트 불변 · 진단 굽기 3칸

```
집행 기계  2호기(굽기) + 에어(조립·sha 대조 · 물리 0프레임)
브랜치     v5-15-s4-fix   기점 **c0c0ea6**(v5-14 HEAD · 확인 = git log -1 이 c0c0ea6)
약정       물리 0줄 · `src/` diff = **`redrapeAsm2` 한정** · 두 후보는 **진단 하위 플래그로 공존**
           (정본 off 비트 불변) · 굽기 ≤ 3칸 · ecc 0 · §0 선커밋 · 브랜치 인쇄 ·
           보고 = docs/v5/보고/15.md · 캡처 3칸 · 콘솔 4줄
사무       귀책 **17건째** 등재 + 「**«법선에서 축» 계열 후보 폐쇄**」 등재
```

## §0-1 판정문 (전략 세션 · v5-14 §4 수령 · **원문**)

```
갈래 C 수용 · 귀책 17건째(전략 세션): 국소 좌표계 처방 — n ⊥ ẑ 를 산수로 걸렀어야 함 ·
«법선에서 축» 계열 후보 폐쇄 등재 · 비트 불변 2판 연속 ·
★★★ 원인 = S4 한 걸음(10.23mm) ≈ 행 간격(9.9mm) → 행 건너뜀 → 행 간격 붕괴 → 대각 면 교차 ·
처방은 «고르지 않고» 두 후보를 f0 가상 실험으로 값으로 고른다:
(A) S4 걸음 상한(행 간격 대비 자릿수 아래 · 등재 자 인용 · 손 상수 0) + 봉제선→아래 행 순서 훑기 ·
(B) 표면 추종 S3(접평면 −y 사영 × 행 길이 · 관통 원천 차단) ·
(B)는 산수 사전 점검 필수: 팔 윗면 곡률 반경 < 행 길이 구간 유무 · SEP 밑 판정은 수치 경계(1.95~2.00) 분리
```

## §0-2 수령 — 집행 «전»에 읽은 값·코드 사실 3건

```
 ㄱ **행 간격 실측**(세 칸) — `L/nvB` = **9.8551**(기본 M · nvB 69) · **9.7368**(mst · 76) ·
    **9.7260 mm**(supima · 73).
 ㄴ **등재 자 셋** — `THICK` = **1e-3 m**(`consts.ts:6`) · `SEP = 2·THICK` = **2e-3**(`:8`) ·
    `TOL_SELF` = **1e-4**(`:14`).
    ★ **행 간격 / `THICK` = 9.726 ~ 9.855** ⟹ `THICK` 이 행 간격의 **정확히 «한 자릿수 아래»**다
      (`SEP` 는 1/4.9 로 «반 자릿수»다) ⟹ 판정문의 「행 간격 대비 자릿수 아래 · 등재 자 인용」에
      **가장 정확히 맞는 자는 `THICK`** 이다.
 ㄷ **지금 S4 는 «전 정점 동시 · 한 걸음 = `SEP − d` 전량»이다** — 반복 상한 8 · 수렴 `TOL_SELF` ·
    첫 회 최대이동 **10.2291 mm**(세 칸 동일) ⟹ 행 간격과 같은 자릿수(v5-14 §1-①㉣).
```

## §0-3 사전 등재 갈래 (회차 프롬프트 그대로 · 사후 신설 0)

| 항 | 집행 | 갈래 |
|---|---|---|
| **①** 산수 사전 점검 | (B) 팔 윗면 **SDF 곡률 반경 최소 ↔ 행 길이 9.73~9.86 mm** · (A) 걸음 상한의 **유도**(등재 자 인용 · 새 수 0) · 예상 S4 반복 | 사실 |
| **②** 가상 실험(f0 · 조립만 · **세 칸 × 두 후보 = 6**) | S4 첫 회·최종 최대이동 · **행 간격 최소/중앙**(정상 9.73~9.86 기준) · **앞뒤 면 최소 거리**(≥ `SEP` · **수치 경계 1.95~2.00 분리**) · 봉제쌍 거리 ≈ `SEP` · 관통 정점 · **자기검사 통과 여부** · (B)만: 실루엣 튜브와 만나는 행의 엣지 신장 | **(가)** 한 후보만 세 칸 통과 → 그것 / **(나)** 둘 다 통과 → **S4 이동량이 작은 쪽** / **(다)** 둘 다 실패(값) ⟹ ③~⑤ 착수 0 |
| **③** 승자 경로 off 비트 불변 | **A29 · T108**(던짐 문언 포함) | 통과/깨짐 |
| **④** R3 진단 굽기 3칸 | 게이트 · 어깨 걸림(**정도**) · 링 · 층3 5행(현행 나란히) · `RAMP_N` 실측 · 캡처 앞·옆 | — |
| **⑤** 종합 | v4-17 「같은 옷」 ①②로 레귤러핏 동일 여부 · mst-over 회복 **정도** | 판단 0 |

**§4** — **A** = ②(가)(나)+③+④ ⟹ 상신(화면 판정) / **B** = ②(다) ⟹ 값 상신 /
**C** = ③ 깨짐 ⟹ **즉시 정지** / **D** = 그 외 ⟹ 정지. **사후 갈래 신설 0 · 걸림은 «정도»로.**

## §0-4 두 후보의 형식화와 사전 결정 5건 (값 보기 «전»)

```
 ㄱ **(A) 걸음 상한 + 행 순서 훑기** —
      걸음 상한 = **`THICK`(1 mm)** · 유도 = §0-2ㄴ(행 간격/`THICK` = 9.726~9.855 = 한 자릿수 아래) ·
      **손 상수 0**(등재 자 인용) · 한 걸음 = `min(SEP − d, THICK)` ·
      순서 = **봉제선(맨 위 행)에서 아래로** 행 단위로 훑는다(한 행을 끝내고 다음 행) ·
      반복 상한 = **`NY`(61)** — 이 함수가 **이미 능선 표본 상한으로 쓰는 등재 값**이다(새 상수 0) ·
      수렴 = `TOL_SELF`(0.1 mm) 그대로.
 ㄴ **(B) 표면 추종 S3** — 한 열을 내릴 때 **전역 `−y`** 가 아니라 **접평면 사영**으로 간다:
      `d = normalize(−ŷ − (−ŷ·n̂) n̂)`(`n̂` = 그 점의 몸 SDF 기울기 단위) · 한 걸음 = 그 행의
      **2D 패턴 엣지 길이** · 걸음 «뒤»에 그 점을 **`SEP` 까지 밀어낸다**(관통 원천 차단) ·
      `n̂ ∥ ŷ`(분모 0)면 **전역 `−y` 로 되돌린다**(가드) · S4 는 그대로 둔다(잔여 보정만 한다).
 ㄷ **①의 (B) 점검 기준**(값 보기 «전») — 「곡률 반경 < 행 길이」 구간이 **하나라도 있으면** (B)는
    그 구간에서 **접평면 한 걸음이 표면을 벗어난다** ⟹ 그 사실을 **값으로 적고** ②의 (B) 결과를
    그 사실과 함께 읽는다(**후보를 미리 떨구지 않는다** — 판정문이 「값으로 고른다」고 했다).
 ㄹ **「`SEP` 밑」은 두 부류로 갈라 센다**(판정문 지시) — **수치 경계**(1.95 ~ 2.00 mm)와
    **실질 미달**(< 1.95 mm)을 **따로** 센다.
 ㅁ **두 후보는 하위 플래그로 «공존»한다** — `asm2Fix: 'A' | 'B' | undefined`(기본 `undefined` =
    v5-14 거동) ⟹ **정본 off 비트 불변**은 `asm2` 자체가 꺼져 있으므로 그대로다.
 ㅂ **`git add` 명시 경로만 · 구현 중간 산출 즉시 커밋**(v5-14 사무 ㄴ 규약).
```

---

## §1-⓪ 사무 2건 — 집행 완료

```
 ㄱ **귀책 17건째 등재** — 「국소 좌표계 처방(`b = t × n`)을 발주했다 — `n ⊥ ẑ` 를 «산수»로 걸렀어야
    한다」(실측 89.03~93.29° · 대부분 90.00° · 상단 간격은 정확히 2.0000 mm 로 «작동»하고 있었다).
 ㄴ **「폐쇄된 후보 계열 1건」 등재** — **«면 법선에서 축을 만드는» 처방 계열**(`t × n` · `n` 기반 벌림)은
    **어깨 위 기하에서 전부 전역 `z` 와 같아진다** ⟹ 다시 발주하지 않는다(근거 = `n ⊥ ẑ` 는 그 대역의
    기하 사실). 다른 층위(걸음 크기 · 훑는 순서 · 낙하의 «표면 추종»)로 간다.
```

## §1-① 산수 사전 점검 — **(A) 유도 성립 · (B) 곡률 경고가 «값으로» 나왔다**

### (A) 걸음 상한의 유도 (등재 자 인용 · 손 상수 0)

```
 행 간격(설계) = `L / nvB` = **9.8551**(기본 M) · **9.7368**(mst) · **9.7260 mm**(supima)
 등재 자 — `THICK` **1 mm** · `SEP` **2 mm** · `TOL_SELF` **0.1 mm**
 행 간격 / `THICK` = **9.855 / 9.737 / 9.726** ⟹ `THICK` 이 **정확히 «한 자릿수 아래»**다
 (`SEP` 는 1/4.9 로 «반 자릿수» · `TOL_SELF` 는 1/97 로 두 자릿수 아래 = 반복이 100회 급으로 늘어난다)
 ⟹ **걸음 상한 = `THICK`(1 mm)** 로 정했다(§0-4ㄱ · 값 보기 «전») · 반복 상한 = **`NY`(61)** ·
   수렴 = `TOL_SELF` 그대로. **새 수 0.**
 ★ 예상 반복 = 총 밀어냄 ≈10 mm ÷ 1 mm = **10회 급** ⟹ 실측 **20~21회**(아래 ②) 로 그 자릿수에 들었다.
```

### (B) 곡률 사전 점검 — ★ **기본 M·mst 에서 「R < 행 길이」가 걸린다**

계기 = `R ≈ Δ / ∠(n̂(c), n̂(c + d·Δ))` · `Δ = SEP`(등재 자) · `d` = 접평면 낙하 방향 · 어깨 토막 전 표본.

| 칸 | **R 최소 mm** | R 중앙 mm | 유한 표본 | **R < 행 길이 표본** | 행 길이 mm |
|---|---|---|---|---|---|
| `c100-h170-s45_M` | **3.974** | 15.327 | 22 | **8 / 22** | 9.8551 |
| `mst-over-L` | **7.316** | 27.676 | 6 | **2 / 6** | 9.7368 |
| `supima-L` | **23.421** | 56.252 | 8 | **0 / 8** | 9.7260 |

```
 §0-4ㄷ 이 값 보기 «전»에 적은 기준 — 「곡률 반경 < 행 길이」 구간이 **하나라도 있으면** (B)는
 그 구간에서 **접평면 한 걸음이 표면을 벗어난다** ⟹ 그 사실을 값으로 적고 ②를 함께 읽는다.
 ⟹ **기본 M(8/22) · mst(2/6)에서 걸리고 supima(0/8)에서는 안 걸린다.**
 ★ 이 예보가 ②에서 **그대로 맞았다** — (B)가 던진 «같은 패널» 쌍(back↔back / front↔front)이
   **기본 M 과 supima** 에서 나왔고, 기본 M 의 최근접 정점쌍이 **0.52353 mm** 로 가장 작다.
```

## §1-② 가상 실험 6조합 — **갈래 (다) 둘 다 실패** ⟹ ③~⑤ 착수 0

| 칸 | 후보 | 자기검사(삼각형 쌍) | **최근접 정점쌍 mm** | 그 자리 | S4 반복/상한 | 걸음 상한 mm | S4 최대이동 첫 / 끝 mm |
|---|---|---|---|---|---|---|---|
| `c100-h170-s45_M` | **A** | `front↔back` 0.0004334 | **1.58249** | front(i11,j66)↔back(i11,j67) | **21 / 61** | **1** | **1** / 0.0628 |
| `mst-over-L` | **A** | `front↔back` 0.0001976 | **1.32808** | front(i18,j73)↔back(i17,j73) | **20 / 61** | **1** | **1** / 0.0944 |
| `supima-L` | **A** | `front↔back` 0.00008126 | **0.93036** | front(i46,j70)↔back(i47,j71) | **21 / 61** | **1** | **1** / 0.0790 |
| `c100-h170-s45_M` | **B** | **`back↔back`** 0.0007549 | **0.52353** | front(i43,j69)↔back(i42,j68) | **1 / 8** | — | **0** / **0** |
| `mst-over-L` | **B** | `front↔back` 0.0005646 | **0.93855** | front(i48,j75)↔back(i49,j76) | **1 / 8** | — | **0** / **0** |
| `supima-L` | **B** | **`front↔front`** 0.0009134 | **0.67310** | front(i15,j71)↔front(i13,j73) | **1 / 8** | — | 0.0047 / 0.0047 |

(대조 · v5-14 무처방 — 최근접 정점쌍 **1.594234**(기본 M) · **1.046869**(mst) · **0.435297 mm**(supima) ·
S4 첫 회 최대이동 **10.2291 mm** · 반복 6~7/8.)

### ★★★ ②의 답 — **(다)** · 그러나 두 후보가 **각자 다른 것을 «고쳤다»**

```
 ㉠ **(A) 는 «걸음»을 고쳤다** — S4 첫 회 최대이동이 **10.2291 → 1 mm**(걸음 상한 그대로) 로 내려가고
    **20~21회**(상한 61)에 **0.0628~0.0944 mm** 로 수렴한다 ⟹ ①의 예상 자릿수(10회 급)와 정합.
    **행 건너뛰기는 사라졌다.** 최근접 정점쌍이 supima 에서 **0.435297 → 0.93036 mm**(**2.14배**) ·
    mst **1.046869 → 1.32808**(1.27배) · 기본 M **1.594234 → 1.58249**(≈동일) 로 **개선 방향**이다.
    ⟹ 그런데 **여전히 `SEP` 밑**이고 삼각형 면 거리는 ~0 ⟹ **던진다**.
 ㉡ **(B) 는 «관통»을 고쳤다 — 원천 차단이 값으로 성립한다** — S4 반복이 **1회**이고 최대이동이
    **0 / 0 / 0.0047 mm** 다 ⟹ **S3 가 끝난 시점에 이미 몸 밖 `SEP` 가 확보된다**(S4 가 할 일이 없다).
    ⟹ 판정문의 「(B) 관통 원천 차단」은 **성립**한다.
 ㉢ ★ **그런데 (B) 는 «같은 패널 안»에서 깨진다** — 기본 M 이 **`back↔back`** · supima 가
    **`front↔front`** 로 던지고(v5-12 이래 처음 보는 부류) 최근접 정점쌍이 **0.52353 / 0.67310 mm** 다.
    자리도 **같은 패널의 다른 열·다른 행**(front(i43,j69)↔back(i42,j68) · front(i15,j71)↔front(i13,j73))
    ⟹ **접평면 낙하가 이웃 «열»을 서로 모은다**(곡률이 작은 데서 두 열의 낙하 방향이 수렴한다).
    ①의 곡률 경고(R 최소 **3.974 / 7.316 mm** < 행 길이 **9.86 / 9.74**)가 **그 구간을 미리 지목했다**.
 ㉣ **둘 다 세 칸 통과 실패** ⟹ 사전 등재 「**(다) 둘 다 실패(값) ⟹ ③~⑤ 착수 0**」 ·
    **승자를 고르지 않았다**(둘 중 하나를 억지로 택하지 않는다 — 사후 갈래 신설 0).
 ★ **수치 경계 분리**(§0-4ㄹ · 판정문 지시) — 이 여섯 조합의 최근접 정점쌍은 **0.52353 ~ 1.58249 mm**
   로 **전부 «실질 미달»(< 1.95 mm)** 이다 ⟹ 「1.95~2.00 경계 걸림」은 **한 건도 없다**.
```

## §1-③④⑤ — **착수 0**(사전 등재 (다))

```
 ③ off 비트 불변 · ④ R3 진단 굽기 3칸 · ⑤ 종합 = **전부 착수 0** —
 **굽기 0칸 · 2호기 접촉 0 · 캡처 0장**. 사유는 ②(다) 한 줄이다.
 ★ 다만 이 판의 `src/` diff 는 **전부 `redrapeAsm2` 안**이고 두 후보는 **하위 플래그**(기본
   `undefined`)라 **정본 경로는 건드리지 않았다** — 비트 불변은 v5-12·v5-14 에서 **두 판 연속 통과**했다.
```

## §2 자기검사

| 항 | 약정 | 실측 |
|---|---|---|
| 기점 | `c0c0ea6`(v5-14 HEAD) | ✅ 등재 |
| 물리 0줄 | | ✅ `gpu/` diff **0** · 굽기 **0칸** |
| `src/` diff | `redrapeAsm2` 한정 · 두 후보 **하위 플래그 공존** | ✅ `asm2Fix` 기본 `undefined` · 정본 off 불변 |
| 굽기 / 캡처 | ≤3칸 / 3칸 | **0 / 0**(사전 등재 (다)) |
| ecc | | **0 · 0** |
| §0 선커밋 | | ✅ `aef7d74` |
| 사무 | 귀책 17 + 계열 폐쇄 | ✅ `ac672fd` |
| 갈래 | 사후 신설 0 | ✅ ① 사실 · ② **(다)** · ③④⑤ 착수 0 · **승자 미선정** |
| 문턱 | 신설 0 · 이동 0 | ✅ (A) 걸음 상한은 **등재 자 `THICK`** 에서 유도(값 보기 전 §0-4ㄱ) · (B) 곡률 기준도 §0-4ㄷ |

**자기신고 1건** — ②가 (다)로 갈려 **승자를 고르지 않았고 ③④⑤ 를 착수하지 않았다**(사전 등재대로).
두 후보 코드는 **하위 플래그로 남긴다**(다음 판이 그대로 쓸 수 있다 · 삭제 0).
이 판 diff — `src/v3/garmentScene.ts`(곡률 훅 · 후보 A · 후보 B) · `src/v3/dressRun.ts` +3 ·
`scripts/v5Asm2Probe.ts`(플래그 env) · 계기 신설 0.

## §4 갈래 판정 — **B**

```
 §0-3 등재 — 「**B** = ②(다) ⟹ 값 상신」
 ① = **사실** — (A) 걸음 상한을 **`THICK`** 에서 유도(행 간격/`THICK` = **9.726~9.855** = 한 자릿수 아래) ·
      (B) 곡률 **R 최소 3.974 / 7.316 / 23.421 mm** ↔ 행 길이 **9.73~9.86 mm** ⟹
      「R < 행 길이」 표본 **8/22 · 2/6 · 0/8**(기본 M·mst 에서 걸린다)
 ② = **(다) 둘 다 실패** — 여섯 조합 전부 자기검사 던짐 · 최근접 정점쌍 **0.52353 ~ 1.58249 mm**
      (전부 «실질 미달» · 경계 걸림 0건)
      ★ 그러나 **(A) 는 걸음을 고쳤고**(10.2291 → 1 mm · 20~21회 수렴 · supima 최근접 **2.14배** 개선)
        **(B) 는 관통을 원천 차단했다**(S4 **1회** · 이동 **0 mm**) — **둘이 다른 것을 고쳤다.**
      ★ (B) 의 새 실패 부류 = **«같은 패널» 쌍**(`back↔back` · `front↔front`) = 접평면 낙하가
        이웃 «열»을 모은다 · ①의 곡률 경고가 그 구간을 미리 지목했다.
 ③④⑤ = **착수 0**
 ⟹ **B**. ★ **C 가 아니다**(③을 착수하지 않았다 · 비트 불변은 두 판 연속 통과 상태).
```
