# v5-26 — 기본 M 교차 1 의 자리·기전 · S 암홀↔소매 관계 사실 · (자리가 조립이면) 최소 처방 · 회귀 3칸

> 기점 `27ad7fa`(v5-25 HEAD) · 브랜치 `v5-26-last-two`
> 물리 0줄 · `src/` diff = **`asm1x` 경로 한정**(정본 off 불변 · **처방은 자리가 조립일 때만**) · ecc 0

```
 ★ 집행 «전» 사실
   ㉠ **2호기 도달**(`ssh 2ho hostname` → `DESKTOP-OV72A46`) ⟹ ③ 굽기 집행 가능.
   ㉡ **굽기 메타의 `trail` 은 `(프레임, 순변위)` 뿐**이다(`worker.py:185` `trail.append((frame, net))`) ⟹
      「궤적에서 «언제» 교차가 생기는지」는 **중간 프레임 «위치»가 있어야** 잴 수 있고, 그것은
      `gpu/bake/worker.py`(하네스 · `src/` 밖)에 덤프를 더해 **다시 구워야** 한다.
   ㉢ `git` 은 Xcode 라이선스 미동의로 멈춰 `DEVELOPER_DIR=/Library/Developer/CommandLineTools` 로 우회한다
      (v5-23 부터 네 판 연속 · 기계 설정 변경 0).
   ㉣ **v5-25 캡처 한정** — `25-L/25-M` 은 `22-L/22-M` 과 **md5 가 같다**
      (`b3804b148bcd0cdc04530c1493224963` · `4bf8ef38b34087821a5b1ff650fff76b`).
      정착 상태가 비트 동일이라 같은 그림이 나오는 것이 맞지만, 「새로 그린 캡처」로 읽히면 오해다 ⟹
      **이 판의 §2 에 신고로 남긴다**(v5-25 보고의 「바이트 크기가 같다」를 «md5 가 같다」로 정정).
```

---

## §0 선커밋 — 집행 «전» 등재

### §0-0 회차 프롬프트 원문

```
v5-26 — 기본 M 교차 1 의 자리·기전 · S 암홀↔소매 관계 사실 · (자리가 조립이면) 최소 처방 · 회귀 3칸

집행 기계 = 2호기 · 브랜치 v5-26-last-two · 기점 = v5-25 HEAD 27ad7fa · 물리 0줄 ·
src/ diff = asm1x 경로 «한정»(정본 off 불변 · 처방은 ①②의 사실이 조립 자리를 가리킬 때만 · 물리 자리면 처방 0) ·
굽기 ≤ 3칸 · ecc 0 · §0 선커밋 · 브랜치 인쇄 · 보고 = docs/v5/보고/26.md · 캡처 · 콘솔 4줄
```

### §0-1 판정문(전략 세션 · v5-25 §4 수령 · 원문)

```
「갈래 B 수용 · 채널 내림 변동 0(예측 적중) · supima-L pass ·
 ★ 남은 벽 둘: (a) 기본 M 정착 교차 1(v5-22 부터 · 겨드랑이 밖 · 자리 미특정)
 (b) S = 앵커로 몸판이 4.9cm 올라가며 암홀 첫 열 i0 이 소매 관과 만남(교차 6 · SH_DROP 은 완화) ·
 후보(단정 0): 소매 축 이동은 따라오나 반경/높이 관계가 어긋남 ·
 처방은 자리가 조립이면 최소로, 물리면 0」
```

### §0-2 §1 집행 4항 (원문)

```
① M 교차 1 — 정착 blob 에서 교차 쌍 특정(패널·행·열·y·부위) · f0 에도 있는지 ·
   궤적(20 간격)에서 언제 생기는지 · off(v4 정본 M)에는 없는 이유(앵커·SH_DROP 따로 켠 f0 대조 — 플래그 두 줄).
② S 암홀↔소매 — f0 에서 back(i0)↔sleeveL 쌍의 기하: 암홀 가장자리 열의 높이·반경 vs 소매 관
   (축 AX · R · 축 이동 s) · 앵커 on/off 의 암홀 중심 변화(Δy) 와 s 변화 ·
   「축 방향 추종 · 높이 미추종」 후보의 검증(값) · 세 칸(S·M·L)에서 같은 쌍 거리 표(M·L 은 왜 통과하는지).
③ 처방(①②가 조립 자리를 가리킬 때만 · asm1x 안 · 손 상수 0 · diff 원문) → f0 자기검사 →
   회귀 3칸 굽기(게이트 4채널 · 어깨 걸림 · 층3 · 캡처). 물리 자리면 사실 상신.
④ 승격 재료 갱신 — 108 계획 · «보류 칸» 규약(S 미해결 시).
```

### §0-3 §4 사전 등재 갈래 (원문 · 사후 갈래 신설 0)

```
A = ③ 3칸 pass ⟹ 상신(→ 108)
B = ③ 일부 fail(값) ⟹ 상신
C = 자리 = 물리 ⟹ 사실 상신 · 처분 전략 세션
D = 조립 sha 변동 ⟹ 정지
E = 그 외 ⟹ 정지
```

---

### §0-4 코드 사실 — 값 보기 «전» 인용

**ㄱ. 분리 플래그는 «이미» 있다**(v5-25 가 넣었다 · 이 판은 더하지 않는다)

```ts
  asm1x?: boolean | 'anchor' | 'drop';            // garmentScene.ts:85 · dressRun.ts:56
  const ASM1X = cfg.asm1x === true;
  const A1X = ASM1X || cfg.asm1x === 'anchor';    // 앵커 정정만
  const D1X = ASM1X || cfg.asm1x === 'drop';      // 어깨 경사만
  const SH_DROP  = (ASM2 || D1X) ? ASM2_SH_DROP : 0;
  const Y_ANCHOR = (ASM2 || A1X) ? Y_NECK : Y_TOP;
```

**ㄴ. 소매 관과 축 이동**(②가 견줄 대상 · 원문)

```ts
const SLV = fitSleeve();  const SLV_X0 = SLV.x0, SLV_R = SLV.R;   // 몸에서 SEP 떨어진 등위관
/* v4-37 §1-③ 소매 축 방향 이동(전달 분기 전용) */
if (AO) { … const sAx = ((bx - sx)/cnt)*AX[0] + ((by - sy)/cnt)*AX[1] + ((bz - sz)/cnt)*AX[2];
          for (let v = lo; v < hi; v++) { s.pos[v*3] += sg*sAx*AX[0]; s.pos[v*3+1] += sAx*AX[1]; … } }
```

```
 ★ **`s` 는 «축 방향» 성분뿐이다** — 승인 문언이 「수직 성분 적용 0(램프의 몫)」이라 **높이를 따라가지 않는다**.
   판정문의 후보(「축 방향 추종 · 높이 미추종」)가 가리키는 자리가 바로 이 식이다. ②가 **값으로** 검증한다.
 ★ `__v3gapProbe` 가 `SLV_X0`·`SLV_R`·`sdf` 를 넘긴다(v3-87 도입분) ⟹ 계기는 `src/` **0줄**로 선다.
```

**ㄷ. v5-25 가 남긴 두 벽의 값**(이 판이 출발하는 사실)

```
 (a) `기본 M` 정착 게이트 — `fails` = `['자기관통 교차 1 ≠ 0']` · 교차 **1** · 최소 쌍거리 1.9798624570389312 mm ·
     v5-22 §1-②ㅁ 가 **겨드랑이 대역 «밖»**임을 쟀다(대역 교차 0 ↔ 전체 1) · **자리 미특정**.
 (b) `작은몸 S` f0 — 앵커만 **0.08915 mm · 교차 6 · `back(i0, j71~72) ↔ sleeveL`** ·
     `SH_DROP` 만 **2.25036 통과** · 둘 다 **1.39419 · 교차 0 · 목선 구간** ·
     붙는 높이(앵커만) 1275.792 ~ 1307.159 mm = `Y_NECK`(1315.769) 아래 8.6 ~ 40.0 mm ·
     `Y_TOP` 1238.0458116531372 ⟹ 앵커가 옷을 **`Y_NECK − Y_TOP` = 77.72 mm** 올린다(그 칸).
```

---

### §0-5 형식화 — 값 보기 «전» 등재(사후 신설 0)

**ㄱ. ① 의 네 조각과 «어디까지 하는가»**

```
 ㉠ **정착 교차 쌍 특정** — 정착 blob 에 `triTriHit` 전수 열거(v5-17 이후 같은 격자·같은 식)를 돌려
   교차 삼각형 쌍의 정점을 `(패널 · i · j · y · 부위)` 로 적는다. 격자 폭은 **제도 분할에서 읽는다**
   (던지지 않는 판본이므로 `sc.nuB`·`sc.nvB` 가 직접 나온다 · 역산 0).
 ㉡ **f0 에도 있는가** — 같은 열거를 f0 조립에 돌린다.
 ㉢ **궤적(20 간격)** — `worker.py` 에 **중간 프레임 위치 덤프**를 더하고 M 한 칸을 다시 굽는다(하네스 수정 ·
   `src/` 밖 · v5-9·v5-22 선례). 덤프 주기는 **20 프레임**(프롬프트가 준 값) · 산출은 `frame-<n>.bin`.
   ★ 이것이 이 판에서 **가장 비싼 항목**이다(굽기 1칸 ≈ 9분). 2호기가 도중에 끊기면 **미집행**으로 신고한다.
 ㉣ **off 에는 왜 없는가** — `off` · `anchor` · `drop` · `둘 다` 네 판본을 **f0** 로 열거해 교차 수와 자리를 비교한다
   (플래그는 이미 있다 · `src/` 0줄).
```

**ㄴ. ② 의 채널 — 「축 방향 추종 · 높이 미추종」을 값으로 가른다**

```
 세 칸(`c87.5-h155-s40_S` · `c100-h170-s45_M` · `supima-L`) × 두 판본(off · `asm1x: true`)에서:
   ㉠ **암홀 봉제쌍** `col(몸판, i0, N_side..nvB) ↔ row(소매, N_und, …)` 의 **쌍별 거리**(봉제 대장 그대로) ·
      최소·중앙·최대.
   ㉡ **암홀 가장자리 열의 기하** — 각 행의 `y` · 팔 관 좌표 `(s, ρ, φ)`(축 `AX` · 피벗 `AP` · `axDot` 식 그대로).
   ㉢ **소매 관의 기하** — `SLV_R`·`SLV_X0` · 소매 정점의 `(s, ρ)` 분포.
   ㉣ **앵커 on↔off 의 변화** — 암홀 중심 `Δy` 와 소매 축 이동 `Δs`(소매 정점의 `s` 중앙값 차로 «측정»한다 ·
      코드의 `sAx` 를 다시 계산하지 않는다 · 함정 12).
 ★ **판정 규칙(값 보기 전)** — 「**`Δy` 가 크고 `Δs` 는 작으면** = 「축은 따라오고 높이는 안 따라온다」가 성립 ·
   **둘 다 비슷하게 움직이면** 그 후보는 **반증**」. 단정 0 · 사실만.
```

**ㄷ. ③ 의 착수 조건 — 「자리가 조립인가 물리인가」의 자**

```
 ★ **조립 자리** ⟺ **f0 에 이미 그 교차/근접이 있다**(조립이 만든 것) ·
   ★ **물리 자리** ⟺ **f0 에는 없고 정착에서 생긴다**(램프·충돌 해소가 만든 것).
 ①㉡ 와 ①㉢ 이 그것을 가른다. ⟹
   · `M 교차 1` 이 **f0 에 있으면** 조립 자리 ⟹ ③ 처방 착수(최소 · `asm1x` 안 · 손 상수 0).
   · **f0 에 없으면** 물리 자리 ⟹ **처방 0**(갈래 C).
 ★ S 도 같은 자로 본다 — S 는 f0 에서 던지므로 **조립 자리**가 이미 확정이다.
 ★ 처방을 쓰더라도 **정본 off 는 한 줄도 바뀌지 않아야 한다**(조립 sha T108·A29 로 확인 · 갈래 D 의 자).
```

### §0-6 §0 사무

```
★ 진입 인자 = v5-22~25 와 같은 것(칸마다 그 칸의 A포즈 몸 grid27 a35 · `D_MM` 9).
★ CC 귀책 `C3` 처치 이행 — 새 계기는 **기준선 한 칸을 먼저 재현**한다.
  이 판의 기준선 = v5-25 게이트의 `기본 M` **자기관통 교차 1** · 최소 쌍거리 **1.9798624570389312 mm**.
★ 계기 산출 파일명 접두 = `v5-26-`.
★ **v5-25 캡처 정정을 §2 에 싣는다**(md5 동일 · 「새로 그렸다」가 아니라 「같은 정착 상태를 다시 그렸다」).
★ 회차 끝에 `git diff 27ad7fa -- src/` 를 인쇄한다(처방 0 이면 **빈 출력**이어야 한다).
★ 이 판에서 호출한 ecc 에이전트 = 0 · 스킬 = 0 (집행 전 등재 · 종료 시 재확인)
```
