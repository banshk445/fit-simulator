# v5-22 — 2세대 종결 등재 · 1세대 배치 + 앵커 정정 + `SH_DROP` 승격 후보 · 회귀 3칸 굽기 · 화면

> 기점 `46af833`(v5-21 HEAD) · 브랜치 `v5-22-anchor-drop`
> 물리 0줄 · **`src/` diff = `asm1x` 분리 한정**(기본값 현행 · off 비트 불변) · 굽기 **3칸** · ecc 0

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v5-22-anchor-drop 46af833 → Switched to a new branch
       $ git branch --show-current → v5-22-anchor-drop · HEAD 46af833
 2호기 $ ssh 2ho hostname → DESKTOP-OV72A46 (응답 확인 · 굽기 3칸은 여기서)
```

---

## §0 선커밋 — 집행 «전» 등재

### §0-1 판정문(전략 세션 · v5-21 §4 수령 · 원문)

```
 「갈래 A 수용 · ③(나) 반증(교차 20배 · 밴드 밖 8배 · 원인 = 윗줄 법선 부재 D_MM 9 > band 8.2291) ·
   ★ 2세대 조립 트랙 종결 — 성과 보관(앵커 버그 · SH_DROP · 능선 규칙 · 간극 규약 · 비트 불변 6연속) ·
   오버핏 = 정의역 밖 등재 ·
   ★ 1번 착수: 1세대 배치 그대로 + 앵커 정정(Y_HEM = Y_NECK − L) + SH_DROP 63.5mm(템플릿 상수 · 출처 등재) ·
   예측(반증 대상): 레귤러핏이 스펙 총장대로 걸리고(현행 대비 5~8cm 아래) 어깨선 기울며 게이트 pass 유지 ·
   오버핏 mst 는 여전히 fail(정의역 밖 · 대조만)」
```

### §0-2 §1 집행 4항 (회차 프롬프트 원문)

```
 ① asm1x 경로 — 앵커·SH_DROP 만(1세대 place 그대로) · 어깨선 y 소비처 12곳(v5-11 전수)에 경사 반영
    diff 원문 · off 비트 불변 A29·T108.
 ② 회귀 3칸(on) — supima-L · 기본 M · 작은몸 S(c87.5-h155-s40_S): 게이트 5채널 · 어깨 걸림(정도) ·
    겨드랑이 · 층3 5행(현행 나란히 · 밑단 행이 내려간 만큼 변하는 것이 기대) · 목선 초과비 · RAMP_N ·
    옷 최고점·밑단 y(현행 대비 Δ = 앵커 차 확인) · 캡처.
    갈래: (가) 3칸 pass / (나) fail(칸·채널·값) / (다) 조립 던짐.
 ③ 대조 1칸(예산 안이면) — mst-over-L(on): 정의역 밖 확인(fail 예상 · 값만).
 ④ 승격 재료(판단 0) — 108칸 재굽기 계획(≈17h · 순서 M→S→L→XL) · 제공 목록 재발행 절차 ·
    v3 동결 예외 등재 항목.
 §4 — A = ②(가) ⟹ 상신(화면 판정 → 108 발주) / B = ②(나) ⟹ 값·캡처 상신 / C = ②(다) ⟹ 사실 상신 /
      D = 비트 불변 깨짐 ⟹ 즉시 정지 / E = 그 외 ⟹ 정지. 사후 갈래 신설 0.
```

**★ 단위 명시**(함정 43) — 굽기 = **칸**(3) · 게이트 채널 = 각자 등재 단위(③a **mm** · 교차 **개** ·
목선 초과비 **무차원** · 창 순변위 **mm** · pinned **개**) · 어깨 걸림 = **정점 개수**(규약 172) ·
층3 5행 = **mm**(중앙) · `RAMP_N` = **프레임** · 옷 최고점·밑단 y = **mm** · Δ = **mm** · 캡처 = **장**.

### §0-3 §1-① 형식화 — **값 보기 «전» 등재**(사후 신설 0)

```
 ㄱ **`asm1x` 는 «제도 두 자리»만 켠다** — `SH_DROP`(`garmentScene.ts:131`) 과 `Y_ANCHOR`(`:290`).
    `redrapeAsm2` 는 **부르지 않는다**(`:1379` 는 `ASM2` 로만 열린다 · 이 판에서 그 줄은 **손대지 않는다**).
 ㄴ **자기검사는 «1세대 규칙 그대로»** — v5-16 이 `asm2` 에만 준 「면 교차 0 ∧ 최소쌍 > 0」 완화는
    **타지 않는다**(`:1469·1487` 는 `ASM2` 전용). `asm1x` 는 1세대 배치이므로 **`SEP` 기준 그대로** 본다.
    그 기준에서 던지면 그것이 **갈래 (다)** 이고, 완화로 통과시키지 않는다.
 ㄷ **기본값 현행** — `asm1x` 를 주지 않으면 `SH_DROP = 0` · `Y_ANCHOR = Y_TOP` ⟹ **한 줄도 다르지 않다**.
    off 비트 불변은 **A29 · T108 둘 다** 집행하고 깨지면 **갈래 D(즉시 정지)** 다.
 ㄹ **회귀 3칸 = `supima-L`(`c100-h170-s45_L`) · 기본 `c100-h170-s45_M` · 작은몸 `c87.5-h155-s40_S`** ·
    굽기는 **2호기** · `frames` 400 · `ramp` · 진입 인자는 v5-7b §0-5ㄴ 확정분(grid27 a35 · `D_MM` 9).
 ㅁ **「현행 나란히」의 대조군은 «이 판에서 다시 잰 off 값»이다** — 과거 노트의 수를 옮겨 쓰지 않는다
    (같은 계기·같은 인자로 off/on 두 번 재서 Δ 를 만든다 · 함정 44).
 ㅂ **캡처는 3칸 앞·옆 6장** — `scripts/v4AposeRender.ts`(정사영 오프스크린 · 화면 판정 0) ·
    자리 = `docs/v5/캡처/22-*.png` · git 경유로만 전달한다.
 ㅅ **새 상수 0** — `ASM2_SH_DROP = 0.0635`(v5-12 등재 · 출처 공개분)를 **그대로** 쓴다.
```

### §0-4 §0 사무 — 2세대 종결 3건(전략 세션 판정문 이행)

```
 ㄱ **2세대 종결 문안**(v5-21 §4 ④ (나) 초안)을 볼트에 등재한다.
 ㄴ **정의역 등재(2번 항목)** 문안 초안을 등재한다 — 「오버핏(드롭숄더)은 정의역 밖」.
 ㄷ **「바깥 펼침」 코드 폐기 표시** — `asm2Fix = 'OUT'` 블록에 «폐기(v5-21 반증)» 주석을 단다.
    **코드는 지우지 않는다**(이력 보존 · v3 동결 관행) · 거동 0줄.
 ㄹ 이 판에서 호출한 ecc 에이전트 = 0 · 스킬 = 0 (집행 전 등재 · 종료 시 재확인).
```

```
 §0 선커밋 **1d5b444** — 집행 «전»(자기 기입)
```

---

## §1 집행

진입 인자 = 칸마다 **그 칸의 A-포즈 몸** (grid27 a35) · `D_MM` 9 · 굽기 = **2호기**(DESKTOP-OV72A46 ·
`hostname` 확인 · GTX 1660 Ti · `arch=cuda` · f64).

### §1-① `asm1x` 경로 — diff 원문 · 12곳 감사 · off 비트 불변 **통과**

**ㄱ. diff 원문**(`git diff 46af833 HEAD -- src/` 전량 · **네 자리**뿐이다)

```ts
// src/v3/dressRun.ts — 타입 1줄 + 전달 1줄(키가 없으면 안 넣는다 ⟹ off 불변)
+  asm1x?: boolean;
+    ...(inp.asm1x ? { asm1x: true } : {}),             // v5-22 — 같은 규약

// src/v3/garmentScene.ts — 타입 1줄 + 플래그 1줄 + «제도 두 자리»
+  asm1x?: boolean;
+  const ASM1X = cfg.asm1x === true;   // ★ v5-22 — 제도 두 자리만(배치는 1세대)
-  const SH_DROP = ASM2 ? ASM2_SH_DROP : 0;
+  const SH_DROP = (ASM2 || ASM1X) ? ASM2_SH_DROP : 0;
-  const Y_ANCHOR = ASM2 ? Y_NECK : Y_TOP;
+  const Y_ANCHOR = (ASM2 || ASM1X) ? Y_NECK : Y_TOP;
```

★ `redrapeAsm2` 호출 줄(`:1379` `if (ASM2) redrapeAsm2(B, s.pos);`)은 **한 글자도 손대지 않았다** ⟹
`asm1x` 는 **1세대 `place()` 배치 그대로**다(§0-3ㄱ 그대로).
★ 자기검사도 1세대 규칙 그대로다 — v5-16 완화(`교차 0 ∧ 최소쌍 > 0`)는 `ASM2` 전용 분기라 **타지 않는다**.

**ㄴ. 어깨선 y 소비처 12곳 감사**(표는 v5-11 §①-5 전수 · 「경사 반영」 열은 **이 판의 코드 실측**)

| # | 자리 | v5-11 이 요구한 것 | **현 코드**(v5-22) |
|---|---|---|---|
| 1 | `shL`·`shR` | `L − SH_DROP` 쪽 끝이 내려간다 | ✅ `line([-SW/2, L - SH_DROP], [-NECK_A, L], N_sh)` |
| 2 | `armR` 위 끝점 | 어깨점과 **함께** 내려가야 한다 | ✅ **간접 반영** — `armR = [SW/2 + famArm(…)[0], Y_ARM + famArm(…)[1]]` 이고 `Y_ARM = L − SH_DROP − ARM_D` ⟹ 위 끝 = `L − SH_DROP` |
| 3 | `neckF` 양 끝 | **불변**(목점이 앵커) | ✅ `[x, L − (NECK_B/2)(1+cos…)]` — 손대지 않았다 |
| 4 | `topB` | 등-`py` 행이 아니게 된다(상단 경계 곡선) | ✅ `[...shL.slice(0,-1), ...nk.slice(0,-1), ...shR]` — `shL` 이 경사지므로 자동 |
| 5 | `coons(front/back, …, topB, …)` | 앞뒤 공통 ⟹ **앞/뒤 경사 차 표현 불가** | ⚠️ **그대로**(등재된 한계 · 이 판이 고치지 않는다) |
| 6 | `Y_ARM = L − ARM_D` | 암홀 «높이»가 줄어든다 | ✅ `Y_ARM = L - SH_DROP - ARM_D` |
| 7 | `SH_LEN = SW/2 − NECK_A` | 이름이 어긋난다(실길이 = `hypot`) | ⚠️ **이름 그대로**(수평 길이) · 실길이는 #8 이 `hypot` 으로 센다 |
| 8 | `N_sh = round(SH_LEN/d)` | `hypot` 기준으로 다시 | ✅ `N_sh = max(1, round(hypot(SH_LEN, SH_DROP)/d))` |
| 9 | `panelHalfWidth(py)` | `py` 상한이 `L` 이 아닌 구간의 정의역 확인 | ✅ `Y_ARM` 이 함께 내려가 분기점이 같이 움직인다 — 세 칸에서 **던짐 0**(L·M) / S 는 자기검사에서 던진다(ㄷ) |
| 10 | `Y_HEM = Y_TOP − L` | **`Y_NECK − L`** 로 정정 | ✅ `Y_ANCHOR = (ASM2 \|\| ASM1X) ? Y_NECK : Y_TOP` · `Y_HEM = Y_ANCHOR − L` |
| 11 | `Y_TOP = shoulderTopY()` | **유지** | ✅ 유지 |
| 12 | `neckBaseY()` 탐색 구간 | **유지** | ✅ 유지 |

⟹ **10곳 반영 · 2곳은 등재된 한계로 그대로**(#5 앞뒤 공통 `topB` · #7 이름). 새 상수 0.

**ㄷ. off 비트 불변 — 통과**

```
 기준선 = 기점 `46af833` 을 갈라 낸 워크트리(`../fit-simulator-v522base` · 본체 `src/` 접촉 0) ·
 같은 계기 `scripts/v5AsmSha.ts` · 같은 `D_MM=9`.
 ★ **T 포즈 108 / 108 동일** — 줄 단위 `diff` 빈 차이 · 던지는 **26칸의 문언까지 동일**.
 ★ **A 포즈  29 /  29 동일** — 던지는 칸 **0**.
 ⟹ **갈래 D 가 아니다.** 비트 불변은 **v5-12 · 14 · 16 · 18 · 19 · 22 — 여섯 판 연속**이다.
```

### §1-② f0 몫 — `asm1x` off ↔ on 나란히 (§0-3ㅁ 이행 · 대조군을 이 판에서 다시 쟀다)

계기 `scripts/v5Asm1xF0.ts`(신설 · `src/` 0줄). 게이트 5채널·층3 5행은 **굽기가 내는 값**이라 여기엔 없다.

| 채널 | `supima-L` off → on | `기본 M` off → on | `작은몸 S` off → on |
|---|---|---|---|
| `n`(정점) | 10356 → **10150** | 9490 → **9280** | 8354 → **던짐** |
| `substeps` | 229 → 227 | 231 → 228 | 242 → — |
| `Y_ANCHOR` mm | 1393.480 → **1442.079** | 1403.939 → **1442.089** | 1238.046 → — |
| `SH_DROP` mm | 0 → **63.5** | 0 → **63.5** | 0 → — |
| **옷 최고점 y** mm | 1418.062 → **1442.079**(+24.017) | 1423.414 → **1442.089**(+18.675) | 1290.286 → — |
| **옷 밑단 y** mm | 683.480 → **732.079**(+48.599) | 723.939 → **762.089**(+38.151) | 588.046 → — |
| 옷 세로 길이 mm | 734.582 → 710.000(**−24.582**) | 699.476 → 680.000(**−19.476**) | 702.240 → — |
| 목선 링 3D mm | 495.424 → 509.205(+13.781) | 501.426 → 514.901(+13.476) | 402.125 → — |
| 링/rest | 1.052978 → **1.082268** | 1.065986 → **1.094634** | 1.027612 → — |
| **어깨선 위 옷 정점** | 106 → **314**(+208) | 88 → **206**(+118) | 120 → — |
| 자기 최소쌍 mm | 2.00000 → 2.00000 | 2.00000 → 2.00000 | 2.26160 → **1.394** |
| `RAMP_N` | 77 → 69(−8) | 71 → 64(−7) | 74 → — |
| 제도 `N_sh` / `nuB` / `nvB` | 15 / 56 / 80 → **17 / 60 / 73** | 14 / 54 / 76 → **16 / 58 / 69** | 15 / 52 / 73 → — |

```
 ★★★ **앵커 정정이 값으로 «정확히» 나타난다** — 옷 밑단이 `Y_NECK − Y_TOP` 만큼 «올라간다»:
   supima **+48.599 mm** · M **+38.151 mm**. v5-10 이 등재한 `Y_NECK − Y_TOP` = **48.5991 mm**(SW 44.5) 와
   supima 값이 **소수 세 자리까지 같다** ⟹ 정정이 의도한 그 양이다.
 ★★★ **어깨 걸림 «정도»가 2.3~3.0배로 늘었다** — 어깨선 위 옷 정점 106 → **314**(supima) · 88 → **206**(M).
   (편입 규약 172 기준으로 off 는 106·88 로 «미달»이었고 on 은 **둘 다 넘는다**.)
 ★ **예측과 어긋난 한 가지** — 판정문의 예측은 「현행 대비 **5~8 cm 아래**」였는데 실측은 밑단이
   **+3.8 ~ +4.9 cm «위»**다. 부호가 반대다. 앵커를 «목»으로 올리면 같은 총장이 더 «높이» 걸리므로
   밑단은 올라간다 — 예측 문언의 방향이 뒤집힌 것으로 보이나, **이 판은 사실만 적는다**(판정 0).
 ★ 링/rest 가 1.053 → 1.082(supima) · 1.066 → 1.095(M) 로 **2.9 %p 늘었다** — 목선이 더 펼쳐진다.
   게이트의 목선 초과비는 «몸 정사영» 기준이라 이 값과 다른 채널이다(굽기가 낸다).
 ★★★ **작은몸 S 는 조립이 던진다** — `옷 자기 간격 SEP 미달 — 최소쌍 back↔back 1.394mm — 갈래 D`
   (f0 계기와 조립 반출 **둘 다** 같은 문언으로 던진다) ⟹ 사전 등재 갈래 **②(다)** 가 이 칸에서 성립한다.
   완화로 통과시키지 않았다(§0-3ㄴ 그대로 · 1세대 `SEP` 기준).
```

### §1-④ 승격 재료 — **판단 0**(승격 여부를 CC 가 고르지 않는다)

**ㄱ. 108칸 재굽기 계획**(회차 프롬프트가 준 「≈17 h · 순서 M→S→L→XL」을 그대로 싣는다)

```
 왜 «재굽기»가 필요한가 — `asm1x` 는 `asm2` 와 달리 **1세대 경로의 값을 바꾼다**(`SH_DROP`·`Y_ANCHOR` 는
 플래그 뒤에 있지만, 승격은 그 플래그를 «기본 on» 으로 올리는 것이다) ⟹ **정답지의 조립 위치가 달라진다.**
 v5-10 §1 ㉡ 원문 — 「A포즈 제공 29칸 재굽기 — **필요하다.** 조립 위치가 바뀌면 정착 상태가 바뀐다」.
 ⟹ 정점 대조 **39칸** + 판정 대조 **69칸** = **108칸**(CLAUDE.md v4 트랙 등재 분류).

 순서(프롬프트 지정) — **M → S → L → XL**. 사이즈별로 묶어 굽는다.
   근거(이 판의 값) — `n` 이 M 9,280 / L 10,150 이고 `substeps` 가 228 / 227 이라 **칸당 비용이 사이즈에 따라
   거의 선형**이다. 작은 사이즈부터 돌리면 실패가 «싸게» 드러난다(S 가 이미 f0 에서 던진다 — ㄴ 참고).
 시간 — 프롬프트 산정 **≈17 h**. ★ **이 판이 실측한 것이 아니다**(산정 · 함정 44).
   이 판의 실측 근거는 굽기 2칸의 벽시계(§1-②ㄷ 표)뿐이고, 108칸으로의 외삽은 **다음 판의 몫**이다.
 ★ **칸 간 프로세스 분리는 이미 규약이다**(CLAUDE.md Taichi 함정 ③ · `worker.py` 가 칸마다 자식을 띄운다) ⟹
   JIT 누적 문제는 재굽기 계획에 추가 항목이 아니다.
```

**ㄴ. 제공 목록 재발행 절차**(v5-11 §④ 의 사슬을 그대로 옮긴다 · 새 절차 0)

```
 v5-11 §④ 원문 — 「플래그 → 진단 3칸 → T포즈 108 sha → A포즈 29칸(≈6 h) → **제공 목록 재발행** →
 v3 동결 예외 · **각 단계 「되돌리기 한 줄」**」.
 이 판이 소비한 자리 — 플래그(§1-①) · 진단 3칸(§1-② · **2칸 완주 · 1칸 던짐**) · T포즈 108 sha(§1-①ㄷ) ·
 A포즈 29칸(§1-①ㄷ · sha 대조로 «불변» 확인 — 재굽기가 아니라 조립 sha 다).
 남은 자리 — **A포즈 29칸 «재굽기»**(정착 상태 재생성 · 조립 sha 와 다른 일이다) → **제공 목록 재발행**.
 재발행이 건드리는 것 — 분류 `index-merged-108.v3-91.json` · 제공 `v1-provide-35.v3-91.json`
   (CLAUDE.md v4 트랙 「정답지」 줄에 등재된 두 파일) ⟹ **판본 접미를 새로 끊어** 옛 판본을 남긴다(삭제 0).
 ★ **되돌리기 한 줄** = 기본값을 `asm1x` off 로 되돌리는 한 줄(`dressRun`/계기의 기본값) — 옛 정답지는
   지우지 않으므로 되돌림이 «파일 복원»을 요구하지 않는다.
```

**ㄷ. v3 동결 예외 등재 항목**(v5-11 R7 의 자리 · 문언 초안 · 승인은 전략 세션)

```
 대장 = 볼트 「v3 동결 «예외» 대장」. 선례 — v4-37 §1-③ **소매 축 방향 이동**이 «예외 3건째»다
 (`garmentScene.ts:1404-1424` · 전략 세션 v4-36 §4 승인 문언 인용 방식).
 ⟹ `asm1x` 승격은 **예외 4건째** 후보이고, 등재 문언에 들어갈 항목 셋:
   ① **무엇을 바꾸나** — `garmentScene.ts` 두 자리: `SH_DROP = ASM2_SH_DROP`(제도 어깨 경사 63.5 mm) ·
      `Y_ANCHOR = Y_NECK`(앵커 정정 — 실측표 총장이 «뒷목 중심» 기준이라는 사실의 반영).
   ② **왜 v3 를 건드려야 하나** — 배치는 v3 정본 코드에 있고(v5-10 §4 「배치는 v3 정본 코드다」),
      실측표 해석의 «버그»라서 플래그 뒤에 영구히 둘 수 없다.
   ③ **되돌림** — 두 자리의 `(ASM2 || ASM1X)` 를 `ASM2` 로 되돌리는 **두 줄**. 옛 정답지 보존으로 복원 0.
 ★ 이 판은 **문언 초안만** 적는다(대장 등재·승인은 전략 세션).
```

### §1-② 회귀 굽기 — **②(나) fail · 그리고 한 칸은 ②(다) 던짐**

굽기 = 2호기(DESKTOP-OV72A46 · `hostname` 확인 · GTX 1660 Ti · `arch=cuda` · f64) · `frames` 400 ·
`ramp` · 잡 `gpu/bake/jobs/v5-22-on2.json` · 총 벽시계 **981.1 s** · 실패 **0칸**.
★ 층3 env 에 `ASM1X: '1'` 이 실려 들어간 것을 굽기 메타 `scene` 에서 확인했다(§2 ㄷ 처분의 효과).

**ㄱ. 완주 두 칸 — 정착·비용**

| 채널 | `supima-L`(`v522onA_L`) | `기본 M`(`v522onA_M`) |
|---|---|---|
| `n` | 10,150 | 9,280 |
| `RAMP_N`(실측) | **69** | **64** |
| `rest0` min~max m | 0.0020000000000437237 ~ 0.18915548307527155 | 0.0020000000000395548 ~ 0.17561738558146928 |
| 정착 프레임 | **f190** | **f190** |
| 창 순변위 mm | 0.07229682189324102 | 0.03930519393313997 |
| s/프레임 | 2.328709434210568 | 2.291361662631511 |
| 벽시계 s | 442.4547925000079 | 435.35871589998715 |
| 자기충돌 쌍/서브스텝 | 40,848.0 | 37,191.0 |
| 해소/서브스텝 | 650.4008810572687 | 565.4078947368421 |
| **vs 정본 blob mm** | 중앙 **68.9306295949727** · p99 102.70717945287446 · 최대 106.02119664710014 | 중앙 **70.76587026512115** · p99 101.55518597528844 · 최대 106.84166232255907 |

**ㄴ. 게이트 5채널 — 두 칸 다 `pass false`**

| 채널 | 문턱 | `supima-L` | `기본 M` |
|---|---|---|---|
| ③a 관통 최대 mm | ≤ 0.5 | **0.33132481902223526** ✅ | **0.3752726257434258** ✅ |
| 관통 정점 수 | (참고) | 881 | 927 |
| 자기관통 교차 | = 0 | **0** ✅ | **1** ❌ |
| 최소 쌍거리 mm | (참고) | 1.970646652526943 | 1.9798624570389312 |
| **목선 초과비** | ≤ 1 | **1.0021454460806467** ❌ | **1.0210830840101046** ❌ |
| 링 cm / 허용 cm | — | 49.488944768371766 / 49.38299621269665 | 49.42449231916582 / 48.40398699492775 |
| 정착 창 순변위 mm | ≤ 0.1 | 0.07229682189324102 ✅ | 0.03930519393313997 ✅ |
| 보조 장치(pinned) | = 0 | **0** ✅ | **0** ✅ |
| 발산 | — | false | false |
| 봉제 간극 중앙 / 최대 mm | (참고) | 2.0903705401250483 / 3.8480564011988636 | 2.107906669570362 / 4.008345064334165 |
| λ 최대 | (참고) | 1.4609440147599382 | 1.4559966679005811 |

```
 `fails` 원문 —
   supima-L : ["목선 초과비 1.0021 > 1 (링 49.49cm > 허용 49.38cm)"]
   기본 M   : ["자기관통 교차 1 ≠ 0", "목선 초과비 1.0211 > 1 (링 49.42cm > 허용 48.40cm)"]
 ★ **다섯 채널 중 세 채널은 두 칸 모두 통과**다(관통 · 정착 · pinned).
 ★★★ **떨어뜨린 것은 «목선 초과비» 하나**(M 은 교차 1 이 더 붙었다).
   supima-L 은 링 **49.489 cm ↔ 허용 49.383 cm** — 차가 **1.06 mm** 다(초과비 1.0021).
   M 은 링 49.424 ↔ 허용 48.404 — 차 **10.2 mm**(초과비 1.0211).
 ★ 허용 `C_allow = C_body + 2π(THICK + TOL_SELF)` 는 **몸에 정사영한 닫힌 둘레** 기준이다
   (`s4Gate.ts:51`) ⟹ 앵커를 목으로 올리면 목선이 **더 좁은 몸 둘레** 위에 앉아 허용이 줄어든다.
   f0 에서 이미 링/rest 가 1.053 → 1.082(supima) · 1.066 → 1.095(M) 로 늘어 있었고(§1-② f0 표),
   굽기에서 그 값이 문턱을 넘겼다.
```

**ㄷ. 층3 5행 — on 실측**(`sepMm` 2 · `bandHalfMm` 9)

| 행 | `supima-L` n / p25 / **중앙** / p75 mm | `기본 M` n / p25 / **중앙** / p75 mm |
|---|---|---|
| 목선 | 54 / 1.0450314319401437 / **3.694429205563228** / 8.214405422538288 | 54 / 1.0000000447715156 / **2.271847244252619** / 6.635882407113522 |
| 가슴 | 248 / 1.049281314648991 / **4.066782162031521** / 14.107264129053807 | 238 / 1.0051671214209994 / **3.7101871936601487** / 14.011332891464695 |
| 허리 | 233 / 23.079898004875165 / **69.29042564858443** / 90.53906802576577 | 223 / 18.15622324665093 / **61.728586993099256** / 74.0101538790064 |
| 밑단 | 122 / 31.192559600538335 / **45.1043114609863** / 52.74861837172856 | 118 / 13.33530228039645 / **25.71858463332665** / 41.1484253431398 |
| 소매 | 1122 / 1.0213119753646207 / **6.053287708967557** / 23.665072434234602 | 1020 / 1.0549867088849947 / **6.427472139916171** / 25.82228128709412 |

대역 높이 — `supima-L` 가슴 131.20738124847412 cm · 허리 103.22565499061783 · 밑단 79.38706874847406 ·
`yArm` 131.2151937484741 · 몸 가슴둘레 94.52415959690823 cm · 허리 65.53116340311274 ·
`기본 M` 가슴 131.20635318756104 · 허리 103.22462692970475 · 밑단 79.38604068756099 ·
`yArm` 131.21416568756104 · 94.51733260197157 / 65.53116102134912.
`pressN` = **0 / 0**(두 칸 다 「눌림」 정점 0) · `snugN`/`looseN` 은 위 표의 행별 n 에서 갈린다.

```
 ★ **「현행 나란히」의 off 쪽 5행은 «미집행»이다** — §0-3ㅁ 는 「과거 노트의 수를 옮겨 쓰지 말고 같은 계기로
   off/on 두 번 재라」고 등재했는데, 층3 5행의 off 쪽은 **A포즈 정착 상태**가 있어야 나오고 그것은
   **굽기 한 칸**을 더 쓴다. 이 판의 굽기 예산은 3칸이고 **2칸 완주 + 1칸 던짐**으로 소진됐다.
   ⟹ off 5행은 적지 않았다(과거 노트에서 옮겨 쓰지도 않았다 · §2 ㅁ 에 한정으로 신고).
 ★ 대신 **이 판이 «직접 잰» off↔on 거리**가 있다 — 굽기 메타의 `vs 정본 blob mm`
   중앙 **68.93 / 70.77 mm** · 최대 106.02 / 106.84 mm. 옷 전체가 그만큼 다른 자리에 선다.
```

**ㄹ. 어깨 걸림 «정도» · 옷 최고점·밑단 y — 정착 상태**

| 채널 | `supima-L` | `기본 M` |
|---|---|---|
| `Y_TOP`(옷 어깨끝 x 에서 잰 몸 높이) mm | 1393.4802 | 1403.9385 |
| **어깨선 위 옷 정점(정착)** | **1002** | **832** |
| 〃 (f0) | 314 | 206 |
| 〃 off f0 | 106 | 88 |
| 옷 최고점 y mm(정착) | 1468.378 | 1471.023 |
| 옷 밑단 y mm(정착) | 783.411 | 820.494 |
| 옷 세로 길이 mm(정착) | 684.967 | 650.529 |

```
 ★★★ **어깨 걸림이 편입 규약(172)을 크게 넘는다 — 1002 / 832.** off f0 는 106 / 88 로 미달이었다.
   ⟹ 판정문의 예측 중 「**어깨선 기울며 걸린다**」 부분은 **값으로 성립**한다.
 ★ 밑단은 정착에서 f0 보다 더 올라간다(L 732.079 → 783.411 · M 762.089 → 820.494 mm) —
   램프가 옷을 몸에 감아 올리는 몫이다.
```

**ㅁ. 겨드랑이 대역**(계기 `scripts/v4Armpit.ts` · 같은 정착 blob)

| 채널 | `supima-L` | `기본 M` |
|---|---|---|
| 대역 자기관통 교차 | **0** | **0** |
| 대역 최소 쌍거리 mm | 1.9957656265168064 | 1.9927409048798979 |
| 대역 관통 최대 mm | 0.256258330034794 | 0.22039016146797288 |
| 대역 관통 정점 | 9515 | 3954 |
| 대역 몸거리 최소 mm | 0.7437416699652061 | 0.7796098385320271 |
| 전체 교차(대조) | 0 | **1** |

★ **M 의 교차 1 개는 겨드랑이 대역 «밖»이다**(대역 교차 0 · 전체 1). 자리는 이 판이 특정하지 않았다.

**ㅂ. 작은몸 S — ②(다) 조립 던짐**

```
 `c87.5-h155-s40_S` · `asm1x` on ⟹ **조립이 던진다**:
   `옷 자기 간격 SEP 미달 — 최소쌍 back↔back 1.394mm — 갈래 D`
 f0 계기(`v5Asm1xF0.ts`)와 조립 반출(`v4AsmExport.ts`) **둘 다** 같은 문언으로 던졌다.
 off 는 같은 칸에서 최소쌍 **2.26160 mm** 로 통과한다 ⟹ **앵커·경사가 그 칸에서 뒤판을 서로 붙인다.**
 완화로 통과시키지 않았다(§0-3ㄴ 그대로 — `asm2` 전용 완화는 타지 않는다).
 ⟹ **굽기 착수 0**(그 칸) · 캡처 0(그 칸).
```

### §1-③ 대조 1칸 `mst-over-L` — **착수 0**

```
 예산 근거 — 회차 프롬프트가 「③ 대조 1칸(**예산 안이면**)」으로 조건을 걸었고, 굽기 3칸 예산은
 **2칸 완주 + 1칸 던짐**으로 소진됐다(그중 한 칸은 하네스 결함으로 **두 번** 구웠다 · §2 ㄷ).
 ⟹ **착수 0.** 오버핏이 정의역 밖이라는 것은 §0 사무 ㄴ 문안으로 볼트에 등재했다(값 대조는 미집행).
```

### §1-④ — 위에 적었다(승격 재료 · 판단 0)

---

## §2 자기검사

```
ㄱ ★ **`BODYTAG` 충돌을 굽기 «전»에 잡았다.** `v4AsmExport.ts:24` 은 `BODYTAG = TAG.replace(/_[^_]*$/,'')`
  이고 `worker.py:89` 는 `sdf-{body}.bin` 을 그 이름으로 찾는다 ⟹ **몸이 다른 칸에 같은 접두를 쓰면
  SDF 가 덮인다.** 처음 접두를 `v522on` 하나로 두어 M(416×479×84)과 S(363×519×89)의 SDF 가 같은 파일에
  겹쳐 썼고, 굽기 전에 발견해 **몸 단위로 접두를 갈랐다**(`v522onA` = c100-h170-s45 · `v522onB` = c87.5-h155-s40).
  ★ 잘못된 `v522on_*` 산출은 **지우지 않았다**(추적 밖 자산 · 삭제 0) — 잡에서 참조하지 않는다.
ㄴ ★★ **«조용한 off 반출» 사고 — 잡았고, 값으로 막았다.** 처음 2호기로 보낸 조립 3개의 정점 수가
  **off 값**(10,356 / 9,490 / 8,354)이었다. 같은 형태의 대조에서는 `ASM1X=1` 이 정상 작동한다
  (같은 칸 M: on **9,280** ↔ off **9,490**) ⟹ **원인은 미규명**이다(재현 실패).
  ★ 처분 — 굽기를 **중지**시키고 다시 반출하며 **칸마다 정점 수로 on 을 검증**했다(10,150 / 9,280) ·
    원격 헤더도 `n = 10,150` 으로 확인 · 굽기 로그의 `RAMP_N 69 / 64` 도 f0 의 on 값과 일치한다.
  ★ **교훈(다음 판 등재 후보)** — 「조립 반출은 «정점 수»로 플래그가 먹었는지 매번 확인한다」.
    v5-19 의 「기준선 한 칸 재현」(CC 귀책 `C3` 처치)과 같은 계열이고, 자산 반출에도 그 자를 댄다.
ㄷ ★★★ **하네스 결함을 «부류»째 닫았다 — CC 귀책 `C1` 의 재발.**
  물리 400프레임은 첫 시도에 완주했고(634.4 s) 실패는 **층3**였다:
    `subprocess.CalledProcessError: Command '['npx','tsx','scripts/v4FitReport.ts']' returned non-zero exit status 1`
  원인 = `worker.py:197` 의 헤더→env 사상 표가 **화이트리스트**다:
    `"scene": {k: BH[j] for k, j in (("BODY_BIN","body"),("ARM_AXIS_JSON","armAxisJson"),
                                    ("ARM_ORIGIN_JSON","armOriginJson")) if BH.get(j)}`
  조립 헤더는 `asm1x: true` 를 **적고 있었는데**(v5-9 §0 사무 ㄱ 의 `SCENEARGS`) 표가 넘기지 않아
  층3 가 **off 장면**(n 10,356)을 세우고 구운 위치(n 10,150)와 어긋났다 — v5-8 사고 5(정점 수가 몸에
  딸려 있다)와 **같은 부류**이고, 그때의 처분이 «표»였기 때문에 **표에 없는 새 플래그가 또 샜다**.
  ★ 처분 두 겹(커밋 `2869ad2` · `src/` 접촉 **0** · 물리 0줄):
    ① 사상 표에 `ASM1X`·`ASM2`·`ASM2FIX` 등재(계기가 `'1'` 을 보므로 값은 문자열).
    ② `SCENE_KEYS` + `check_scene_keys` — 조립 헤더가 **모르는 장면 키**를 적으면 조용히 흘리지 않고 던진다.
  ★ 효과 확인 — 재굽기 메타의 `scene` 에 `"ASM1X": "1"` 이 들어 있다(실패 0칸 · 층3 완주).
ㄹ ★ **2호기 cmd 출력 인코딩 사고 3회**(코드 결함 아님) — 워커 로그의 `—`(U+2014)가 cp949 에서 터졌다.
  `py -X utf8` 은 **부모만** 고치고 자식(칸마다 새 프로세스)은 여전히 cp949 였다 ⟹
  `set "PYTHONIOENCODING=utf-8"` 로 **환경변수**를 주어야 자식이 물려받는다. 중간에 내가 붙인
  `chcp 65001 >nul` 과 `set PYTHONUTF8=1 &`(끝 공백이 값에 들어갔다)가 각각 한 번씩 더 실패시켰다.
  ★ **원격 코드·잡 변경 0** — 환경변수만으로 풀었다.
ㅁ ★ **층3 5행의 off 쪽 미집행**(§1-②ㄷ) — §0-3ㅁ 를 지키려면 off 재굽기 한 칸이 필요하고 예산 밖이다.
  과거 노트의 수를 **옮겨 쓰지도 않았다**. 대신 이 판이 직접 잰 `vs 정본 blob` 중앙 68.93 / 70.77 mm 를 냈다.
ㅂ ★ **③ 대조 1칸 미집행**(예산 조건부 항목).
ㅅ ★ **M 의 교차 1개의 «자리»를 특정하지 않았다**(겨드랑이 대역 밖이라는 것만 안다).
ㅇ ★ **판정문 예측과 부호가 반대인 항목 1건** — 「현행 대비 5~8 cm 아래」 ↔ 실측 밑단 **+3.8 ~ +4.9 cm 위**.
  사실만 적고 판정하지 않았다(판단 0).
ㅈ **문턱 신설·이동 0** — 쓴 수는 `ASM2_SH_DROP`·`SEP`·`THICK`·`TOL_SELF`·`S4_THRESHOLD` 다섯 채널 ·
  편입 규약 172 뿐이고 전부 이미 등재된 양이다. 손 상수 0.
ㅊ **물리 0줄** · `src/` diff = **네 자리**(타입 2 · 플래그 1 · 제도 2줄) · `redrapeAsm2` 호출 줄 무접촉.
ㅋ **이 판에서 호출한 ecc 에이전트 = 0 · 스킬 = 0.**
```

---

## §4 판정 — **갈래 B**

```
 A = ②(가) ⟹ 상신(화면 판정 → 108 발주)  … ✗ 두 칸 다 `pass false`
 ★ B = ②(나) ⟹ 값·캡처 상신               … **✔ 이것이다**
 C = ②(다) ⟹ 사실 상신                    … 부분 성립(작은몸 S 던짐) — **B 가 더 넓다**(값·캡처가 있다)
 D = 비트 불변 깨짐 ⟹ 즉시 정지            … ✗ T 108/108 · A 29/29 동일(6판 연속)
 E = 그 외 ⟹ 정지                          … ✗
 사후 갈래 신설 0.

 ① = **통과** — `src/` diff 네 자리 · 12곳 중 10 반영(2 는 등재된 한계) · off 비트 불변 **T108 · A29**.
 ② = **(나)** — 회귀 3칸 중 **2칸 완주 · 1칸 조립 던짐**. 완주 두 칸은 **게이트 5채널 중 3채널 통과**
      (관통 0.331 / 0.375 mm ≤ 0.5 · 정착 0.072 / 0.039 mm ≤ 0.1 · pinned 0)이고
      **목선 초과비 하나에 떨어졌다**(1.0021 / 1.0211 > 1) · M 은 교차 1 이 더 붙었다.
      작은몸 S 는 **②(다)**(`최소쌍 back↔back 1.394mm`).
 ③ = **착수 0**(예산 조건부).
 ④ = **적었다**(108칸 계획 · 제공 목록 재발행 절차 · v3 동결 예외 4건째 문언 초안 · 판단 0).

 ★★★ 이 판의 핵심 값 셋
   ㉠ **앵커 정정은 «의도한 그 양»이다** — f0 밑단이 `Y_NECK − Y_TOP` 만큼 올라간다:
      supima **+48.599 mm** ↔ v5-10 등재값 **48.5991 mm**(소수 세 자리 일치) · M **+38.151 mm**.
   ㉡ **어깨 걸림은 «성립»한다** — 정착에서 어깨선 위 옷 정점 **1002 / 832**(off f0 106 / 88 · 규약 172).
   ㉢ **막은 것은 «목선»이다** — 앵커를 목으로 올리면 목선이 더 좁은 몸 둘레 위에 앉아 **허용이 줄고**
      (M 허용 48.40 cm ↔ off 기준선보다 작다) 초과비가 1 을 넘는다. supima 는 **1.06 mm** 차다.
```
