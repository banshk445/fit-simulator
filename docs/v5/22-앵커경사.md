# v5-22 — 2세대 종결 등재 · 1세대 배치 + 앵커 정정 + `SH_DROP` 승격 후보 · 회귀 3칸 굽기 · 화면

> 기점 `46af833`(v5-21 HEAD) · 브랜치 `v5-22-anchor-drop`
> 물리 0줄 · **`src/` diff = `asm1x` 분리 한정**(기본값 현행 · off 비트 불변) · 굽기 **3칸** · ecc 0

### ★ 브랜치 전환 인쇄 (집행 «전»)

```
 에어  $ git checkout -b v5-22-anchor-drop 46af833 → Switched to a new branch
       $ git branch --show-current → v5-22-anchor-drop · HEAD 46af833
 2호기 $ ssh 2ho hostname → DESKTOP-OV72A46 (응답 확인 · 굽기 3칸은 여기서)
```

---

## §0 선커밋 — 집행 «전» 등재

### §0-1 판정문(전략 세션 · v5-21 §4 수령 · 원문)

```
 「갈래 A 수용 · ③(나) 반증(교차 20배 · 밴드 밖 8배 · 원인 = 윗줄 법선 부재 D_MM 9 > band 8.2291) ·
   ★ 2세대 조립 트랙 종결 — 성과 보관(앵커 버그 · SH_DROP · 능선 규칙 · 간극 규약 · 비트 불변 6연속) ·
   오버핏 = 정의역 밖 등재 ·
   ★ 1번 착수: 1세대 배치 그대로 + 앵커 정정(Y_HEM = Y_NECK − L) + SH_DROP 63.5mm(템플릿 상수 · 출처 등재) ·
   예측(반증 대상): 레귤러핏이 스펙 총장대로 걸리고(현행 대비 5~8cm 아래) 어깨선 기울며 게이트 pass 유지 ·
   오버핏 mst 는 여전히 fail(정의역 밖 · 대조만)」
```

### §0-2 §1 집행 4항 (회차 프롬프트 원문)

```
 ① asm1x 경로 — 앵커·SH_DROP 만(1세대 place 그대로) · 어깨선 y 소비처 12곳(v5-11 전수)에 경사 반영
    diff 원문 · off 비트 불변 A29·T108.
 ② 회귀 3칸(on) — supima-L · 기본 M · 작은몸 S(c87.5-h155-s40_S): 게이트 5채널 · 어깨 걸림(정도) ·
    겨드랑이 · 층3 5행(현행 나란히 · 밑단 행이 내려간 만큼 변하는 것이 기대) · 목선 초과비 · RAMP_N ·
    옷 최고점·밑단 y(현행 대비 Δ = 앵커 차 확인) · 캡처.
    갈래: (가) 3칸 pass / (나) fail(칸·채널·값) / (다) 조립 던짐.
 ③ 대조 1칸(예산 안이면) — mst-over-L(on): 정의역 밖 확인(fail 예상 · 값만).
 ④ 승격 재료(판단 0) — 108칸 재굽기 계획(≈17h · 순서 M→S→L→XL) · 제공 목록 재발행 절차 ·
    v3 동결 예외 등재 항목.
 §4 — A = ②(가) ⟹ 상신(화면 판정 → 108 발주) / B = ②(나) ⟹ 값·캡처 상신 / C = ②(다) ⟹ 사실 상신 /
      D = 비트 불변 깨짐 ⟹ 즉시 정지 / E = 그 외 ⟹ 정지. 사후 갈래 신설 0.
```

**★ 단위 명시**(함정 43) — 굽기 = **칸**(3) · 게이트 채널 = 각자 등재 단위(③a **mm** · 교차 **개** ·
목선 초과비 **무차원** · 창 순변위 **mm** · pinned **개**) · 어깨 걸림 = **정점 개수**(규약 172) ·
층3 5행 = **mm**(중앙) · `RAMP_N` = **프레임** · 옷 최고점·밑단 y = **mm** · Δ = **mm** · 캡처 = **장**.

### §0-3 §1-① 형식화 — **값 보기 «전» 등재**(사후 신설 0)

```
 ㄱ **`asm1x` 는 «제도 두 자리»만 켠다** — `SH_DROP`(`garmentScene.ts:131`) 과 `Y_ANCHOR`(`:290`).
    `redrapeAsm2` 는 **부르지 않는다**(`:1379` 는 `ASM2` 로만 열린다 · 이 판에서 그 줄은 **손대지 않는다**).
 ㄴ **자기검사는 «1세대 규칙 그대로»** — v5-16 이 `asm2` 에만 준 「면 교차 0 ∧ 최소쌍 > 0」 완화는
    **타지 않는다**(`:1469·1487` 는 `ASM2` 전용). `asm1x` 는 1세대 배치이므로 **`SEP` 기준 그대로** 본다.
    그 기준에서 던지면 그것이 **갈래 (다)** 이고, 완화로 통과시키지 않는다.
 ㄷ **기본값 현행** — `asm1x` 를 주지 않으면 `SH_DROP = 0` · `Y_ANCHOR = Y_TOP` ⟹ **한 줄도 다르지 않다**.
    off 비트 불변은 **A29 · T108 둘 다** 집행하고 깨지면 **갈래 D(즉시 정지)** 다.
 ㄹ **회귀 3칸 = `supima-L`(`c100-h170-s45_L`) · 기본 `c100-h170-s45_M` · 작은몸 `c87.5-h155-s40_S`** ·
    굽기는 **2호기** · `frames` 400 · `ramp` · 진입 인자는 v5-7b §0-5ㄴ 확정분(grid27 a35 · `D_MM` 9).
 ㅁ **「현행 나란히」의 대조군은 «이 판에서 다시 잰 off 값»이다** — 과거 노트의 수를 옮겨 쓰지 않는다
    (같은 계기·같은 인자로 off/on 두 번 재서 Δ 를 만든다 · 함정 44).
 ㅂ **캡처는 3칸 앞·옆 6장** — `scripts/v4AposeRender.ts`(정사영 오프스크린 · 화면 판정 0) ·
    자리 = `docs/v5/캡처/22-*.png` · git 경유로만 전달한다.
 ㅅ **새 상수 0** — `ASM2_SH_DROP = 0.0635`(v5-12 등재 · 출처 공개분)를 **그대로** 쓴다.
```

### §0-4 §0 사무 — 2세대 종결 3건(전략 세션 판정문 이행)

```
 ㄱ **2세대 종결 문안**(v5-21 §4 ④ (나) 초안)을 볼트에 등재한다.
 ㄴ **정의역 등재(2번 항목)** 문안 초안을 등재한다 — 「오버핏(드롭숄더)은 정의역 밖」.
 ㄷ **「바깥 펼침」 코드 폐기 표시** — `asm2Fix = 'OUT'` 블록에 «폐기(v5-21 반증)» 주석을 단다.
    **코드는 지우지 않는다**(이력 보존 · v3 동결 관행) · 거동 0줄.
 ㄹ 이 판에서 호출한 ecc 에이전트 = 0 · 스킬 = 0 (집행 전 등재 · 종료 시 재확인).
```

```
 §0 선커밋 **1d5b444** — 집행 «전»(자기 기입)
```

---

## §1 집행

진입 인자 = 칸마다 **그 칸의 A-포즈 몸** (grid27 a35) · `D_MM` 9 · 굽기 = **2호기**(DESKTOP-OV72A46 ·
`hostname` 확인 · GTX 1660 Ti · `arch=cuda` · f64).

### §1-① `asm1x` 경로 — diff 원문 · 12곳 감사 · off 비트 불변 **통과**

**ㄱ. diff 원문**(`git diff 46af833 HEAD -- src/` 전량 · **네 자리**뿐이다)

```ts
// src/v3/dressRun.ts — 타입 1줄 + 전달 1줄(키가 없으면 안 넣는다 ⟹ off 불변)
+  asm1x?: boolean;
+    ...(inp.asm1x ? { asm1x: true } : {}),             // v5-22 — 같은 규약

// src/v3/garmentScene.ts — 타입 1줄 + 플래그 1줄 + «제도 두 자리»
+  asm1x?: boolean;
+  const ASM1X = cfg.asm1x === true;   // ★ v5-22 — 제도 두 자리만(배치는 1세대)
-  const SH_DROP = ASM2 ? ASM2_SH_DROP : 0;
+  const SH_DROP = (ASM2 || ASM1X) ? ASM2_SH_DROP : 0;
-  const Y_ANCHOR = ASM2 ? Y_NECK : Y_TOP;
+  const Y_ANCHOR = (ASM2 || ASM1X) ? Y_NECK : Y_TOP;
```

★ `redrapeAsm2` 호출 줄(`:1379` `if (ASM2) redrapeAsm2(B, s.pos);`)은 **한 글자도 손대지 않았다** ⟹
`asm1x` 는 **1세대 `place()` 배치 그대로**다(§0-3ㄱ 그대로).
★ 자기검사도 1세대 규칙 그대로다 — v5-16 완화(`교차 0 ∧ 최소쌍 > 0`)는 `ASM2` 전용 분기라 **타지 않는다**.

**ㄴ. 어깨선 y 소비처 12곳 감사**(표는 v5-11 §①-5 전수 · 「경사 반영」 열은 **이 판의 코드 실측**)

| # | 자리 | v5-11 이 요구한 것 | **현 코드**(v5-22) |
|---|---|---|---|
| 1 | `shL`·`shR` | `L − SH_DROP` 쪽 끝이 내려간다 | ✅ `line([-SW/2, L - SH_DROP], [-NECK_A, L], N_sh)` |
| 2 | `armR` 위 끝점 | 어깨점과 **함께** 내려가야 한다 | ✅ **간접 반영** — `armR = [SW/2 + famArm(…)[0], Y_ARM + famArm(…)[1]]` 이고 `Y_ARM = L − SH_DROP − ARM_D` ⟹ 위 끝 = `L − SH_DROP` |
| 3 | `neckF` 양 끝 | **불변**(목점이 앵커) | ✅ `[x, L − (NECK_B/2)(1+cos…)]` — 손대지 않았다 |
| 4 | `topB` | 등-`py` 행이 아니게 된다(상단 경계 곡선) | ✅ `[...shL.slice(0,-1), ...nk.slice(0,-1), ...shR]` — `shL` 이 경사지므로 자동 |
| 5 | `coons(front/back, …, topB, …)` | 앞뒤 공통 ⟹ **앞/뒤 경사 차 표현 불가** | ⚠️ **그대로**(등재된 한계 · 이 판이 고치지 않는다) |
| 6 | `Y_ARM = L − ARM_D` | 암홀 «높이»가 줄어든다 | ✅ `Y_ARM = L - SH_DROP - ARM_D` |
| 7 | `SH_LEN = SW/2 − NECK_A` | 이름이 어긋난다(실길이 = `hypot`) | ⚠️ **이름 그대로**(수평 길이) · 실길이는 #8 이 `hypot` 으로 센다 |
| 8 | `N_sh = round(SH_LEN/d)` | `hypot` 기준으로 다시 | ✅ `N_sh = max(1, round(hypot(SH_LEN, SH_DROP)/d))` |
| 9 | `panelHalfWidth(py)` | `py` 상한이 `L` 이 아닌 구간의 정의역 확인 | ✅ `Y_ARM` 이 함께 내려가 분기점이 같이 움직인다 — 세 칸에서 **던짐 0**(L·M) / S 는 자기검사에서 던진다(ㄷ) |
| 10 | `Y_HEM = Y_TOP − L` | **`Y_NECK − L`** 로 정정 | ✅ `Y_ANCHOR = (ASM2 \|\| ASM1X) ? Y_NECK : Y_TOP` · `Y_HEM = Y_ANCHOR − L` |
| 11 | `Y_TOP = shoulderTopY()` | **유지** | ✅ 유지 |
| 12 | `neckBaseY()` 탐색 구간 | **유지** | ✅ 유지 |

⟹ **10곳 반영 · 2곳은 등재된 한계로 그대로**(#5 앞뒤 공통 `topB` · #7 이름). 새 상수 0.

**ㄷ. off 비트 불변 — 통과**

```
 기준선 = 기점 `46af833` 을 갈라 낸 워크트리(`../fit-simulator-v522base` · 본체 `src/` 접촉 0) ·
 같은 계기 `scripts/v5AsmSha.ts` · 같은 `D_MM=9`.
 ★ **T 포즈 108 / 108 동일** — 줄 단위 `diff` 빈 차이 · 던지는 **26칸의 문언까지 동일**.
 ★ **A 포즈  29 /  29 동일** — 던지는 칸 **0**.
 ⟹ **갈래 D 가 아니다.** 비트 불변은 **v5-12 · 14 · 16 · 18 · 19 · 22 — 여섯 판 연속**이다.
```

### §1-② f0 몫 — `asm1x` off ↔ on 나란히 (§0-3ㅁ 이행 · 대조군을 이 판에서 다시 쟀다)

계기 `scripts/v5Asm1xF0.ts`(신설 · `src/` 0줄). 게이트 5채널·층3 5행은 **굽기가 내는 값**이라 여기엔 없다.

| 채널 | `supima-L` off → on | `기본 M` off → on | `작은몸 S` off → on |
|---|---|---|---|
| `n`(정점) | 10356 → **10150** | 9490 → **9280** | 8354 → **던짐** |
| `substeps` | 229 → 227 | 231 → 228 | 242 → — |
| `Y_ANCHOR` mm | 1393.480 → **1442.079** | 1403.939 → **1442.089** | 1238.046 → — |
| `SH_DROP` mm | 0 → **63.5** | 0 → **63.5** | 0 → — |
| **옷 최고점 y** mm | 1418.062 → **1442.079**(+24.017) | 1423.414 → **1442.089**(+18.675) | 1290.286 → — |
| **옷 밑단 y** mm | 683.480 → **732.079**(+48.599) | 723.939 → **762.089**(+38.151) | 588.046 → — |
| 옷 세로 길이 mm | 734.582 → 710.000(**−24.582**) | 699.476 → 680.000(**−19.476**) | 702.240 → — |
| 목선 링 3D mm | 495.424 → 509.205(+13.781) | 501.426 → 514.901(+13.476) | 402.125 → — |
| 링/rest | 1.052978 → **1.082268** | 1.065986 → **1.094634** | 1.027612 → — |
| **어깨선 위 옷 정점** | 106 → **314**(+208) | 88 → **206**(+118) | 120 → — |
| 자기 최소쌍 mm | 2.00000 → 2.00000 | 2.00000 → 2.00000 | 2.26160 → **1.394** |
| `RAMP_N` | 77 → 69(−8) | 71 → 64(−7) | 74 → — |
| 제도 `N_sh` / `nuB` / `nvB` | 15 / 56 / 80 → **17 / 60 / 73** | 14 / 54 / 76 → **16 / 58 / 69** | 15 / 52 / 73 → — |

```
 ★★★ **앵커 정정이 값으로 «정확히» 나타난다** — 옷 밑단이 `Y_NECK − Y_TOP` 만큼 «올라간다»:
   supima **+48.599 mm** · M **+38.151 mm**. v5-10 이 등재한 `Y_NECK − Y_TOP` = **48.5991 mm**(SW 44.5) 와
   supima 값이 **소수 세 자리까지 같다** ⟹ 정정이 의도한 그 양이다.
 ★★★ **어깨 걸림 «정도»가 2.3~3.0배로 늘었다** — 어깨선 위 옷 정점 106 → **314**(supima) · 88 → **206**(M).
   (편입 규약 172 기준으로 off 는 106·88 로 «미달»이었고 on 은 **둘 다 넘는다**.)
 ★ **예측과 어긋난 한 가지** — 판정문의 예측은 「현행 대비 **5~8 cm 아래**」였는데 실측은 밑단이
   **+3.8 ~ +4.9 cm «위»**다. 부호가 반대다. 앵커를 «목»으로 올리면 같은 총장이 더 «높이» 걸리므로
   밑단은 올라간다 — 예측 문언의 방향이 뒤집힌 것으로 보이나, **이 판은 사실만 적는다**(판정 0).
 ★ 링/rest 가 1.053 → 1.082(supima) · 1.066 → 1.095(M) 로 **2.9 %p 늘었다** — 목선이 더 펼쳐진다.
   게이트의 목선 초과비는 «몸 정사영» 기준이라 이 값과 다른 채널이다(굽기가 낸다).
 ★★★ **작은몸 S 는 조립이 던진다** — `옷 자기 간격 SEP 미달 — 최소쌍 back↔back 1.394mm — 갈래 D`
   (f0 계기와 조립 반출 **둘 다** 같은 문언으로 던진다) ⟹ 사전 등재 갈래 **②(다)** 가 이 칸에서 성립한다.
   완화로 통과시키지 않았다(§0-3ㄴ 그대로 · 1세대 `SEP` 기준).
```
