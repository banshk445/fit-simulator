# v5-23 — 목선 채널의 정의역 사실(on/off 나란히) · 링 신장·높이·간격 · S 던짐 사실 · 처방 0

> 기점 `f382603`(v5-22 HEAD) · 브랜치 `v5-23-neck-ruler`
> 물리 0줄 · **`src/` diff 0(계기만 · v5-22 산출 재사용)** · 굽기 0 · ecc 0 · **처방 0 · 단정 0 · 판단 0**

```
 ★ 집행 «전» 사무 — 이 세션에서 `git` 이 Xcode 라이선스 미동의로 멈춰 있었다(`/usr/bin/git` shim).
   `sudo` 없이 `DEVELOPER_DIR=/Library/Developer/CommandLineTools` 로 우회했다(git 2.50.1) ·
   저장소 파일 변경 0 · 기계 설정 변경 0.
```

---

## §0 선커밋 — 집행 «전» 등재

### §0-0 회차 프롬프트 원문

```
v5-23 — 목선 채널의 정의역 사실(on/off 나란히) · 링 신장·높이·간격 · S 던짐 사실 · 처방 0

집행 기계 = 2호기 · 브랜치 v5-23-neck-ruler · 기점 = v5-22 HEAD f382603 · 물리 0줄 ·
src/ diff 0(계기만 · v5-22 산출 재사용) · 굽기 0(가능하면 · S 조립만 f0) · ecc 0 · §0 선커밋 ·
브랜치 인쇄 · 보고 = docs/v5/보고/23.md · 콘솔 4줄 · §0 사무: 귀책 20건째(전략 세션 · 앵커 방향 설명 오류)
```

### §0-1 판정문(전략 세션 · v5-22 §4 + 화면 수령 · 원문)

```
「갈래 B 수용 · 화면 합격(두 벌 · 어깨선 기울고 밑단 스펙 길이) · 앵커 정정 양 일치 · 귀책 20건째 ·
 어깨 걸림 성립 · ★ 목선 초과비 단독 실패 — 자의 정의역 질문:
 off(v4-37 계열)는 링이 어깨로 흘러내려 56cm(휴지 대비 +19%)인데 넓은 자리라 통과 ·
 on 은 제자리(목 밑동) 49.5cm(+5%)인데 좁은 자리라 1mm 초과 ⟹
 허용식 C_allow = C_body(링 높이) + 2π(THICK+TOL) 은 «링이 몸에 얹혀 몸이 벌린 만큼» 전제 ·
 봉제 장력에 떠 있는 옷깃은 전제 밖 후보 · 단정 0 · 문턱 이동 0 · 자의 «전제 성립 여부»를 값으로」
```

### §0-2 §1 집행 4항 (원문)

```
① 링 사실(두 칸 × on/off) — 정착 링 둘레 · 휴지 대비 신장률(정점별 분포 · 균일/국소) ·
   링 정점 높이(앞·뒤·옆 y · 몸 목 밑동 링 높이 대비) · 링↔몸 최소 거리 분포(«얹힘» = SEP 근처 정점 비율 ·
   «떠 있음» = 그 이상) · 허용값 산출 높이의 몸 둘레 기울기(dC/dy — 높이 ±5mm 에 허용이 얼마나 변하는지).
② 자의 전제 판정(값 보기 전 규칙) — 「링 정점의 «얹힘» 비율」이 off 에서 높고 on 에서 낮으면 =
   허용식 전제가 on 에서 불성립(자의 정의역 밖) · 반대면 = on 의 실제 과신장(옷 결함). 사실만.
③ S 던짐 — 작은몸 S(on) 조립 자기검사 던진 쌍(패널·자리·값) · off 에서는 성립하는지 ·
   SH_DROP/앵커 중 무엇이 그 쌍을 만들었는지(둘을 따로 켠 f0 대조 · 굽기 0).
④ 처방 후보 나열(판단 0) — (ㄱ) 목선 채널 정의 보완(예: 얹힌 정점만으로 허용 산출 ·
   또는 신장률 상한을 휴지 기준으로 — 각각 «새 수 0 으로 유도 가능한지») (ㄴ) 옷깃 자체 문제면
   그 자리(봉제 장력 방향) · 각 후보의 회귀 영향(T108·A29 게이트 결과 변동 여부 — 시뮬레이션 0 · 산출 재판정만).
```

### §0-3 §4 사전 등재 갈래 (원문 · 사후 갈래 신설 0)

```
A = ①②③④ ⟹ 상신(처분은 전략 세션)
B = 계기 불가(사유) ⟹ 상신
```

---

### §0-4 코드 사실 — 값 보기 «전» 인용

**ㄱ. 허용식과 링 둘레 — `src/v3/s4Gate.ts:111-126` 원문**

```ts
const polyLen = (ix: number[]) => ix.slice(1).reduce((t, v, k) =>
  t + Math.hypot(pos[v*3] - pos[ix[k]*3], pos[v*3+1] - pos[ix[k]*3+1],
                 pos[v*3+2] - pos[ix[k]*3+2]), 0);
/** 구 채널 — 삭제하지 않고 병기(v3-40 §0-4). **판정에 쓰지 않는다.** */
const ringRestRatio = (polyLen(P.neckF) + polyLen(P.neckB)) / P.ringRest;

/* v3-40 §1 — 목선 «초과비». 링을 «닫힌 고리»로 보고, 그 링이 놓인 몸 자리의 둘레와 견준다. */
const loop = [...P.neckF, ...[...P.neckB].reverse()];
const closed = (pts) => pts.reduce((t, _, k) => {
  const a = pts[k], b = pts[(k+1) % pts.length];
  return t + Math.hypot(a[0]-b[0], a[1]-b[1], a[2]-b[2]); }, 0);
const ringM      = closed(loop.map((v) => [pos[v*3], pos[v*3+1], pos[v*3+2]]));
const ringBodyM  = closed(loop.map((v) => bd.nearestBodyPoint(pos[v*3], pos[v*3+1], pos[v*3+2])));
const ringAllowM = ringBodyM + 2 * Math.PI * (THICK + TOL_SELF);
const ringExcess = ringM / ringAllowM;
```

```
 ★★★ **판정문의 「C_body(링 높이)」를 코드가 그렇게 쓰지 않는다 — 정정 사실 하나.**
   `ringBodyM` 은 «어떤 높이의 몸 둘레»가 아니라 **링 정점 각각의 `nearestBodyPoint` 를 순서대로 이은
   닫힌 다각형의 길이**다. ⟹ 링이 몸에서 떨어져 있으면 그 사영 다각형이 «몸 위에서 더 좁은 자리로 모일» 수
   있고, 그러면 허용이 **높이와 무관하게** 줄어든다. 이 판의 ①은 그 사영 다각형을 직접 잰다.
 ★ `ringRest = 2 · S.LEN_NECK`(`dressRun.ts:161`) · `ringRestRatio` 는 **판정 아님**(구 채널 · 병기).
 ★ 문턱 = `S4_THRESHOLD.ringExcess = 1`(`s4Gate.ts:54` — 「1 은 고른 수가 아니라 «정의»다」).
```

**ㄴ. 「얹힘」의 자 — 등재된 양에서 «유도»한다**(새 수 0)

```
 허용식이 더하는 양은 `2π(THICK + TOL_SELF)` 다. 닫힌 고리에서 둘레에 `2πr` 이 더해지는 것은
 **반경이 `r` 만큼 커진 것**과 같다 ⟹ 이 식은 **「링이 몸 표면에서 반경 방향으로 `THICK + TOL_SELF`
 떨어져 «얹혀» 있다」**를 전제한다.
 ⟹ **얹힘 판정자 `d_rest := THICK + TOL_SELF = 1.1 mm`**(등재 두 수의 합 · 손 상수 0):
     **얹힘** ⟺ 링 정점의 몸 거리 `d ≤ d_rest` · **떠 있음** ⟺ `d > d_rest`.
 ★ 프롬프트 문언은 「«얹힘» = SEP 근처」라고 적었으나, **코드의 식이 더하는 양은 `THICK + TOL_SELF`**
   이므로 그 값을 자로 쓴다. `SEP`(2 mm) 기준 비율도 **함께** 인쇄해 둘 다 볼 수 있게 한다(판정 아님).
 ★ 이것은 **문턱 신설이 아니다** — 허용식이 이미 쓰는 수를 «읽어» 판정자로 쓴 것이고, 게이트 문턱은
   건드리지 않는다(문턱 이동 0 · 판정문 지시 그대로).
```

**ㄷ. `dC/dy` 의 계기** — 프롬프트가 준 **±5 mm** 를 그대로 쓴다(탐침 폭 · 문턱 아님).
링 정점 전체를 `y ± 5 mm` 로 **강체 평행이동**한 뒤 같은 식(`ringBodyM` + `2π(THICK+TOL_SELF)`)으로
허용을 다시 산출해 차를 낸다. 링 모양·둘레는 건드리지 않는다(허용의 «높이 민감도»만 본다).

**ㄹ. off 정착 상태의 자리** — `public/v3diag/v3-77/settled-<칸>.bin`
(`worker.py:82` · `load.py:51` 이 읽는 그 파일 · v5-22 굽기의 「vs 정본 blob」 기준이 이것이다).

---

### §0-5 이 판의 제약 아래 «집행 가능/불가» — 값 보기 «전» 가른다

```
 ★ 집행 가능(굽기 0 · `src/` 0줄)
   ㉠ **on 정착 링 사실** — v5-22 가 구운 정착 blob 두 개가 로컬에 있다
      (`gpu/bake/results/v5-22-on2/v522onA_{L,M}.bin` · n 10,150 / 9,280).
   ㉡ **off 정착 링 사실 — «정본 blob»으로** (`settled-c100-h170-s45_{L,M}.bin`).
      ★ **한정(미리 등재)** — 정본 blob 은 **T포즈**이고 v5-22 on 은 **A포즈**다. 포즈가 다르므로
        on↔off 대조에는 **자세 차가 섞인다**. 그 사실을 표에 명시하고, 포즈가 같은 대조는 f0 로 따로 낸다.
   ㉢ **f0 on ↔ f0 off 링 사실**(두 칸 · 같은 A포즈 몸 · 같은 인자) — 포즈가 같은 유일한 대조다.
   ㉣ **③의 앞 두 항** — S(on) 던진 쌍의 패널·자리·값 · off 에서 성립하는지.
   ㉤ **④ 후보 나열과 «산출 재판정»** — 시뮬레이션 0 · 이미 있는 산출로 새 자를 다시 적용해 본다.
 ★ **집행 불가 — 사유를 먼저 적는다**
   ㉥ **③의 「`SH_DROP`/앵커를 따로 켠 f0 대조」** — `asm1x` 는 **두 자리를 «함께»** 켠다
      (`SH_DROP = (ASM2||ASM1X) ? … : 0` · `Y_ANCHOR = (ASM2||ASM1X) ? … : …`).
      따로 켜려면 **플래그를 하나 더 만들어야 하고 그것은 `src/` 수정**이다 ⟹ 이 판의 약정
      「`src/` diff 0」과 **정면으로 충돌**한다. ⟹ **미집행으로 신고**하고 대신 ㉣ 를 값으로 낸다.
      (다음 판이 플래그 하나를 열면 바로 잴 수 있다 — 등재만 해 둔다.)
   ㉦ **off 정착의 «A포즈» 판본** — 굽기 한 칸이 필요하고 이 판은 「굽기 0」이다 ⟹ 미집행.
 ⟹ ①의 「두 칸 × on/off」는 **세 겹으로** 낸다: on 정착(A) · off 정착(T · 포즈 한정) · f0 on↔off(A).
```

### §0-6 §0 사무

```
★ **전략 세션 귀책 20건째** 등재 — 판정문 원문 「귀책 20건째(전략 세션 · **앵커 방향 설명 오류**)」.
  v5-22 발주문의 예측이 「레귤러핏이 … 현행 대비 **5~8 cm 아래**」였는데 실측은 밑단이 **+3.8~4.9 cm 위**였다
  (v5-22 §1-② f0 표 · supima +48.599 mm · M +38.151 mm). 앵커를 «목»으로 올리면 같은 총장이 더 높이
  걸리므로 밑단은 올라간다 — 방향이 뒤집힌 설명이었고, 판정문이 스스로 귀책으로 등재했다.
★ 진입 인자 = 칸마다 그 칸의 A포즈 몸(grid27 a35) · `D_MM` 9(v5-22 와 «같은» 인자 — 산출 재사용).
★ 계기 산출 파일명 접두 = `v5-23-`.
★ CC 귀책 `C3` 처치 이행 — 새 계기는 **기준선 한 칸을 먼저 재현**한다.
  이 판의 기준선 = v5-22 게이트의 `목선 초과비` **1.0021454460806467**(supima-L) · 링 **49.488944768371766 cm** ·
  허용 **49.38299621269665 cm**. 계기가 같은 blob 에서 이 셋을 재현해야 한다.
★ `src/` 는 **한 줄도 고치지 않는다** — 회차 끝에 `git diff f382603 -- src/` 가 비어 있음을 확인해 적는다.
★ 이 판에서 호출한 ecc 에이전트 = 0 · 스킬 = 0 (집행 전 등재 · 종료 시 재확인)
```

---

## §1 집행

계기 = `scripts/v5NeckRuler.ts`·`scripts/v5SThrow.ts` **신설**(둘 다 `src/` **0줄**) ·
진입 인자 = v5-22 와 **같은** 것(칸마다 그 칸의 A포즈 몸 grid27 a35 · `D_MM` 9) · 굽기 **0**.

**§0-6 처치 이행 — 기준선 재현 «비트 일치»**: 계기가 `on 정착 supima-L` 에서
초과비 **1.0021454460806467** · 링 **49.488944768371766 cm** · 허용 **49.38299621269665 cm** 를 냈다 —
v5-22 게이트 값과 **자릿수 전부 같다** ⟹ 계기가 게이트 식(`s4Gate.ts:111-126`)을 정확히 재현한다.

### §1-① 링 사실 — 세 겹(§0-5 등재 그대로)

**ㄱ. 정착 상태**(게이트가 판정하는 그 상태)

| 판본 | 포즈 | 링 cm | `ringBody` cm | 허용 cm | **초과비** | `restRatio`(판정 아님) | **얹힘 `d_rest`** | 얹힘 `SEP` | 몸거리 중앙 mm | 신장 중앙 | `dC/dy` cm/10mm |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `on supima-L` | A | 49.4889 | 48.6918 | 49.3830 | **1.002145** | 1.042797 | **14/54 = 25.93%** | 20/54 = 37.04% | 3.6944 | 1.0344 | −1.8078 |
| `on 기본 M` | A | 49.4245 | 47.7128 | 48.4040 | **1.021083** | 1.037657 | **19/54 = 35.19%** | 27/54 = 50.00% | 2.2718 | 1.0199 | −1.8318 |
| `off 기본 M`(정본) | **T** | 52.3077 | 52.1890 | 52.8802 | **0.989174** | 1.082992 | **31/54 = 57.41%** | 41/54 = 75.93% | **1.0641** | **1.1058** | −1.4430 |

몸거리 분포 mm — `on L` 최소 0.7470 · p25 1.0450 · 중앙 3.6944 · p75 8.2144 · 최대 24.2554 · 평균 6.1503 /
`on M` 0.7190 · 1.0000 · 2.2718 · 6.6359 · 17.9862 · 4.6493 /
`off M(T)` 0.8449 · 0.9985 · **1.0641** · 1.8761 · **6.0765** · **1.7896**.

링 높이 mm — `on L` 최소 1419.692 · 최대 1468.378 · 앞 1425.412 · 뒤 1419.692 · 옆 1465.227 ·
`Y_NECK` 1442.079 · **중앙이 `Y_NECK` 대비 +7.932** /
`on M` 1423.642 · 1471.023 · 1427.288 · 1423.642 · 1455.131 · `Y_NECK` 1442.089 · **+8.736** /
`off M(T)` 1398.375 · 1449.586 · 1413.870 · 1398.375 · 1442.563 · `Y_NECK` 1441.194 · **−7.644**.

신장률(3D 엣지 / 2D 패턴 엣지 · 엣지 52개) —
`on L` 최소 1.0003 · p25 1.0169 · **중앙 1.0344** · p75 1.0707 · 최대 1.1520 · >1 비율 100.00% /
`on M` 0.9368 · 1.0088 · **1.0199** · 1.0748 · 1.1436 · 88.46% /
`off M(T)` 0.9981 · 1.0354 · **1.1058** · 1.1281 · **1.1916** · 96.15%.

**ㄴ. f0 상태**(같은 A포즈 · 같은 인자 — 포즈가 같은 유일한 on↔off 대조)

| 판본 | 링 cm | `ringBody` cm | 허용 cm | 초과비 | `restRatio` | 얹힘 `d_rest` | 몸거리 중앙 mm | 신장 중앙 | 링 중앙 − `Y_NECK` mm |
|---|---|---|---|---|---|---|---|---|---|
| `on supima-L` | 88.7508 | 69.4992 | 70.1904 | 1.264430 | 1.082268 | **0/54** | 26.8310 | 1.0959 | −36.426 |
| `off supima-L` | 91.7762 | 80.5781 | 81.2693 | 1.129286 | 1.052978 | **0/54** | 27.8357 | 1.0221 | −85.025 |
| `on 기본 M` | 86.6128 | 71.2042 | 71.8953 | 1.204707 | 1.094634 | **0/54** | 18.8558 | 1.0688 | −36.426 |
| `off 기본 M` | 88.7217 | 78.2747 | 78.9658 | 1.123546 | 1.065986 | **0/54** | 26.1641 | 1.0334 | −74.577 |

```
 ★ f0 에서는 **on·off 둘 다 얹힘 0%** 다(몸거리 중앙 18.9 ~ 27.8 mm) — 배치는 «실루엣 기둥» 위에 놓는 것이고
   아직 아무것도 몸에 얹히지 않았다 ⟹ **f0 는 «얹힘»을 가르지 못한다.** 그래서 ①을 세 겹으로 냈다.
 ★ 그런데 f0 에서도 **같은 기전이 보인다** — `off` 의 링이 **더 긴데**(91.78 > 88.75 · L) 초과비는 **더 낮다**
   (1.129 < 1.264). 사영 둘레가 더 넓기 때문이다(`ringBody` 80.58 > 69.50). ⟹ 채널을 움직이는 것은
   **링 길이가 아니라 «사영 둘레»** 다.
 ★ **한정(§0-5ㄴ 에 미리 등재)** — `off 정착`은 **T포즈 정본 blob** 이고 `on 정착`은 **A포즈**다.
   `off 정착`의 A포즈 판본은 굽기 한 칸이 필요해 **미집행**이다(「굽기 0」).
   판정문이 인용한 off 값(「56cm · +19%」)은 **v4-37 계열 A포즈**의 수이고, 이 판이 잰 T포즈 정본은
   **52.31 cm · `restRatio` 1.083(+8.3%)** 다 — 같은 옷의 다른 자세이므로 **두 수를 섞지 않는다**.
```

**ㄷ. 허용의 높이 민감도 `dC/dy`**(링을 `y ± 5 mm` 강체 이동 · 프롬프트 탐침 폭 · 링 모양·둘레 불변)

| 판본 | 허용(+5 mm) cm | 허용(0) cm | 허용(−5 mm) cm | **초과비(+5)** | 초과비(0) | **초과비(−5)** |
|---|---|---|---|---|---|---|
| `on supima-L` 정착 | 47.7232 | 49.3830 | 49.5310 | 1.037001 | 1.002145 | **0.999151** |
| `on 기본 M` 정착 | 47.9929 | 48.4040 | 49.8247 | 1.029830 | 1.021083 | **0.991968** |
| `off 기본 M` 정착(T) | 52.1804 | 52.8802 | 53.6233 | 1.002440 | 0.989174 | 0.975465 |

```
 ★★★ **판정이 «링 높이 5 mm» 안에서 뒤집힌다** — `on` 두 판본 모두 링을 **5 mm 내리면 통과**한다
   (1.002145 → **0.999151** · 1.021083 → **0.991968**). 반대로 5 mm 올리면 더 크게 실패한다.
   허용의 기울기는 **10 mm 당 −1.44 ~ −1.83 cm** 다(f0 에서는 −2.07 ~ −4.09 cm).
 ★ 즉 이 채널의 통과/실패는 **링이 목 밑동에서 몇 mm 위아래에 앉느냐**에 달려 있다. 사실만 적는다.
```

### §1-② 자의 전제 판정 — 사전 규칙이 **«전제 불성립» 쪽을 가리킨다**

```
 사전 규칙(§0-2 ② · 값 보기 «전») —
   「「링 정점의 «얹힘» 비율」이 **off 에서 높고 on 에서 낮으면** = 허용식 전제가 on 에서 불성립
    (자의 정의역 밖) · **반대면** = on 의 실제 과신장(옷 결함)」

 실측(같은 칸 `기본 M` · 정착) —
   **off 57.41%(31/54) > on 35.19%(19/54)**  … `d_rest = 1.1 mm` 자
   **off 75.93%(41/54) > on 50.00%(27/54)**  … `SEP = 2 mm` 자(병기)
   몸거리 중앙 **off 1.0641 mm ↔ on 2.2718 mm** · 평균 **1.7896 ↔ 4.6493** · 최대 **6.0765 ↔ 17.9862**
 ⟹ **규칙이 발화하는 쪽 = 「허용식 전제가 on 에서 불성립」.**

 ★ **보강 사실 — 「on 의 과신장」 쪽은 값이 반대다.** 휴지 대비 신장은 **on 이 «덜»** 늘어나 있다:
   `restRatio` **on 1.037657 / 1.042797 ↔ off 1.082992** · 엣지 신장 중앙 **on 1.0199 / 1.0344 ↔ off 1.1058** ·
   최대 **on 1.1436 / 1.1520 ↔ off 1.1916**.
   ⟹ **on 은 링이 더 여유로운데도 떨어지고, off 는 더 늘어났는데도 통과한다.**
 ★ **가른 것은 허용값이다** — `ringBody`(사영 둘레) **on 47.7128 ↔ off 52.1890 cm**.
   링 자체는 on 이 **2.88 cm 짧은데**(49.42 ↔ 52.31) 허용은 **4.48 cm 짧다**(48.40 ↔ 52.88).
 ★ **한정** — 두 판본의 **자세가 다르다**(A ↔ T). 다만 ①ㄴ 의 f0 대조(같은 A포즈)에서도
   **사영 둘레가 채널을 움직인다**는 같은 기전이 보이므로, 방향은 자세 차만으로 설명되지 않는다.
   **단정은 하지 않는다**(판정문 「단정 0」 그대로).
```

### §1-③ S 던짐 — 쌍과 자리

```
 던짐 문언(원문) — `옷 자기 간격 SEP 미달 — 최소쌍 back↔back 1.394mm — 갈래 D`
 최악 쌍 = 삼각형 **14716 ↔ 14719** · **둘 다 `back` 패널** · `min` **1.3941902517730405 mm** · `hits` **0**(교차 0)
 정점(패널 · 오프셋 · y / x / z mm) —
   A 14716 : back 3727 (1278.965 / −43.931 / −104.565) · back 3728 (1271.878 / −36.704 / −108.968) ·
             back 3784 (1288.118 / −41.766 / −100.832)
   B 14719 : back 3729 (1264.860 / −32.067 / −109.237) · back 3786 (1273.796 / −31.279 / −108.034) ·
             back 3785 (1280.923 / −38.731 / −103.251)
 붙는 높이 = **1264.860 ~ 1288.118 mm** · x 는 −31 ~ −44 mm(왼쪽) · z 는 −100 ~ −109 mm(뒤쪽)
```

**자리를 격자로 푼다**(추측 0 — 정점 수와 등재 제도식으로만):

```
 `back` 패널 정점 수 = **3,819** = (nuB+1)(nvB+1) = **57 × 67** ⟹ `nuB = 56` · `nvB = 66`.
   ★ 등재 제도식과 맞는다 — off S 는 `N_sh` 15 · `N_nk` 22 · `N_side` 48 · `N_arm` 25(`nuB` 52 · `nvB` 73)이고,
     on 은 `N_sh = round(hypot(SH_LEN, SH_DROP)/d)` 로 **17** 이 되어 `nuB = 2·17+22 = 56` ·
     `Y_ARM = L − SH_DROP − ARM_D` 로 `N_side` 가 **41** 이 되어 `nvB = 41+25 = 66` 이다.
 오프셋 → (i, j) — 3727 → (**i 22**, **j 65**) · 3728 → (i 23, j 65) · 3729 → (i 24, j 65) ·
   3784 → (i 22, **j 66**) · 3785 → (i 23, j 66) · 3786 → (i 24, j 66).
 ⟹ **붙는 자리 = 봉제선 행 `j = nvB`(66)와 그 바로 아래 행 `nvB−1`(65) · 열 `i` 22 ~ 24.**
 ★★★ `dEdge(i) = min(i, nuB − i)` = min(22, 34) = **22 > N_sh(17)** ⟹ 이 열은 **어깨 토막이 아니라 «목선 구간»** 이다
   (어깨 토막 = `i ≤ 17` 또는 `i ≥ 39`). 즉 **S 의 던짐은 «목선» 자리에서 뒤판이 제 위로 접힌 것**이다.
 ★ 높이도 목선대다 — 1264.9 ~ 1288.1 mm 는 `Y_NECK`(1315.769) 아래 **27.7 ~ 50.9 mm** ·
   `Y_TOP`(1238.046) 위 **26.8 ~ 50.1 mm**.
```

**off 에서는 성립한다**(같은 칸 · 같은 인자 · `asm1x` 없음):

```
 던짐 **없음** · 최소쌍 **2.261599483527185 mm**(> `SEP` 2 mm) · `hits` **0** ·
 최악 쌍 = 삼각형 6136 ↔ 13728 = **front ↔ back**(정상적인 앞뒤 간격) · 정점 오프셋 3127 / 3128 / 3180 ·
 붙는 높이 **1116.720 ~ 1125.475 mm**(허리 아래) ⟹ **off 의 최악 자리는 목선이 아니다.**
 ⟹ `asm1x` 가 **최악 쌍의 «부류»를 바꿨다**: `front↔back`(설계상 마주보는 쌍) → `back↔back`(자기접힘).
```

```
 ★ **미집행 — 「`SH_DROP`/앵커를 따로 켠 f0 대조」**(§0-5 ㉥ 에 값 보기 «전» 등재).
   `asm1x` 는 두 자리를 **함께** 켜고, 따로 켜려면 플래그를 하나 더 만들어야 하며 그것은 `src/` 수정이다 ⟹
   이 판의 약정 「`src/` diff 0」과 충돌한다. **다음 판이 플래그 하나를 열면 바로 잴 수 있다.**
   ★ 다만 이 판의 값이 **한쪽을 가리킨다** — 붙는 자리가 **목선 구간**이고 높이가 **`Y_NECK` 바로 아래** 라는 것은
     «앵커를 `Y_NECK` 으로 올린 것»과 같은 자리다. `SH_DROP` 이 건드리는 자리는 **어깨 토막 열**(`i ≤ 17`·`≥ 39`)이다.
     **단정은 하지 않는다**(따로 켜 보지 않았다).
```

### §1-④ 처방 후보 나열 — **판단 0**

**(ㄱ)-1 「얹힌 정점만으로 허용 산출」**

```
 자는 새로 안 만든다 — `d_rest = THICK + TOL_SELF`(허용식이 이미 쓰는 수) 로 얹힌 정점을 고른다.
 ★ 그런데 **«닫힌 둘레»를 부분 집합으로 어떻게 닫을지는 새 규칙이 필요하다** — 얹힌 정점은
   `on M` 19/54 · `on L` 14/54 로 **고리를 끊어 놓는다**(연속 구간이 아니다) ⟹ 남은 구간을 어떻게 이을지가
   새 식이다. **「새 수 0」은 되지만 「새 식 0」은 아니다.**
```

**(ㄱ)-2 「신장률 상한을 휴지 기준으로」 — ★ 새 수 0 · 새 식 0 으로 «이미» 유도된다**

```
 근거 — `s4Gate.ts:53` 원문: 「구 채널(휴지 대비 비율)은 삭제하지 않고 `ringRestRatio` 로 **병기**한다(판정 아님)」 ·
 `:51-52` 가 「어림수 **1.10**(v3-24)을 교체했다」고 적었다 ⟹ **자와 문턱이 둘 다 이미 저장소에 있다.**
 그 자로 이 판의 세 정착 판본을 **재판정**하면:
   `on supima-L` **1.042797** ≤ 1.10 ✅ · `on 기본 M` **1.037657** ≤ 1.10 ✅ · `off 기본 M(T)` **1.082992** ≤ 1.10 ✅
 ⟹ **세 판본 모두 통과**한다. ★ 이것은 **문턱 이동이 아니라 «폐기된 구 채널의 복귀»** 이므로
   판정문의 「문턱 이동 0」과 충돌하는지 여부는 **전략 세션의 판단**이다(CC 는 고르지 않는다).
```

**(ㄱ)-3 「정의역 게이팅」**(얹힘 비율이 낮으면 이 채널을 적용하지 않는다)

```
 ★ **새 수가 필요하다** — 「얹힘 비율 몇 % 미만」의 자가 저장소에 없다. 이 판의 값(off 57.41% ↔ on 35.19%)
   사이에 선을 그으면 **결과에 맞춘 문턱**이 된다(함정 14) ⟹ CC 는 그 선을 제안하지 않는다.
```

**(ㄴ) 옷깃 자체 문제인가 — 근거가 약하다**(사실만)

```
 신장률 분포가 `on` 에서 **중앙 1.0199 / 1.0344 · 최대 1.1436 / 1.1520** 이고 `off` 는 **중앙 1.1058 · 최대 1.1916** 다 ⟹
 **국소 과신장도 on 이 off 보다 작다.** 「봉제 장력에 떠 있는 옷깃」이 **떠 있다는 것은 값이 지지**하지만
 (몸거리 중앙 2.27 ↔ 1.06 mm · 최대 17.99 ↔ 6.08 mm), **그 자체가 «결함»이라는 근거는 이 판에 없다.**
```

**회귀 영향 — 「산출 재판정만」**(시뮬레이션 0 · 굽기 0)

```
 로컬 게이트 산출 **43개**(`gpu/oracle/export/l3-gate-*.json`)를 다시 읽어 판정했다:
   **통과 33 · 실패 10** · 실패 중 **목선이 사유에 든 칸 5** · ★ **목선 «단독» 사유 칸 0**
   통과 칸 초과비 — 최소 **0.987804** · 중앙 **0.989420** · **최대 0.997411**(`c87.5-h155-s45_XL`)
   실패 칸 초과비 — 최소 0.989138 · 중앙 1.015835 · 최대 1.130614
 ⟹ ★ **목선 자를 «완화»하는 방향의 후보라면 기존 43 산출의 판정은 한 칸도 바뀌지 않는다** —
   통과 33칸은 완화로 깨지지 않고(초과비 최대 0.9974 < 1), 실패 10칸은 **목선 단독이 0** 이라 여전히 실패한다.
 ★ **한정** — 43 은 **로컬에 있는 전부**이고 **T108·A29 전량이 아니다**. 전량 재판정은 나머지 산출이 있어야 한다
   (2호기 · 이 판에서 도달 불가 · §2 ㄹ).
 ★ **(ㄱ)-2 를 «조이는» 방향으로 읽으면**(휴지 기준으로 교체) 43칸의 `ringRestRatio` 가 필요한데
   **게이트 json 이 그 값을 인쇄하지 않는다**(`S4Result` 에는 있으나 산출에 없다) ⟹ **재판정 불가**(미집행).
```

---

## §2 자기검사

```
ㄱ ★ **`src/` diff = 0 을 확인했다** — `git diff f382603 -- src/` 가 **빈 출력**이다(§2-① 에 기록).
  이 판이 만든 것은 계기 둘(`v5NeckRuler.ts`·`v5SThrow.ts`)뿐이고, 게이트 식은 **`s4Gate.ts` 의 식을 그대로**
  다시 쓴 것이다(기준선 재현이 **비트 일치**로 그것을 증명한다).
ㄴ ★ **「얹힘」 자를 프롬프트 문언과 «다르게» 골랐고 그 사유를 값 보기 전에 적었다**(§0-4ㄴ) —
  프롬프트는 「`SEP` 근처」라 했으나 **허용식이 더하는 양은 `2π(THICK + TOL_SELF)`** 이므로
  `d_rest = THICK + TOL_SELF = 1.1 mm` 를 자로 썼다. **`SEP` 기준 비율도 함께 인쇄**해 둘 다 볼 수 있게 했다
  (두 자 어느 쪽으로 읽어도 ②의 방향은 같다: off 57.41 / 75.93% ↔ on 35.19 / 50.00%).
ㄷ ★★ **미집행 1 — ③의 「`SH_DROP`/앵커 따로 켜기」**: `src/` 수정을 요구해 이 판 약정과 충돌한다
  (§0-5 ㉥ 에 값 보기 «전» 등재). 대신 던진 쌍의 자리를 격자로 풀어 **목선 구간**임을 값으로 냈다.
ㄹ ★★ **미집행 2 — `off 정착`의 A포즈 판본**: 굽기 한 칸이 필요하고 이 판은 「굽기 0」이다(§0-5 ㉦).
  ★ 게다가 `settled-c100-h170-s45_L.bin`(정본 blob)이 **로컬에 없고** 이 판 도중 **2호기가 도달 불가**가 됐다
  (`ssh: connect to host desktop-ov72a46 port 22: Operation timed out`) ⟹ `off 정착`은 **`기본 M` 한 칸만** 냈다.
  그래서 ①의 「두 칸 × on/off」는 **on 정착 2 · off 정착 1 · f0 on/off 4 = 7 판본**으로 냈다.
ㅁ ★ **자세 차가 섞인 대조가 하나 있다** — `off 정착`은 T포즈, `on 정착`은 A포즈다. 그 사실을 표에 명시했고,
  판정문이 인용한 off 값(56 cm)과 이 판의 T포즈 값(52.31 cm)을 **섞지 않았다**.
ㅂ ★ **(ㄱ)-2 의 재판정이 43칸에서 불가**하다 — 게이트 json 이 `ringRestRatio` 를 인쇄하지 않는다.
  **계기를 고쳐 다시 굽는 일은 하지 않았다**(굽기 0 · `src/` 0줄).
ㅅ ★ **③의 (i, j) 는 «역산»이다** — 패널 정점 수 3,819 = 57 × 67 분해와 등재 제도식(`N_sh` = `round(hypot/d)` ·
  `Y_ARM = L − SH_DROP − ARM_D`)이 맞물리는 것으로 확인했다. 훅이 `nu`·`nv` 를 직접 주지 않아 **추론 한 겹**이 있다.
ㅇ ★ **집행 환경 사고 1건(코드·데이터 무관)** — 이 세션에서 `git` 이 Xcode 라이선스 미동의로 멈췄다.
  `sudo` 가 이 세션에서 불가해 **`DEVELOPER_DIR=/Library/Developer/CommandLineTools`** 로 우회했다(git 2.50.1) ·
  기계 설정·저장소 파일 변경 **0**.
ㅈ **문턱 신설·이동 0** — 쓴 수는 `THICK`·`TOL_SELF`·`SEP`·`ringExcess 문턱 1`·구 채널 문턱 **1.10**(등재값 인용) ·
  탐침 폭 ±5 mm(프롬프트가 준 값) 뿐이다. **손 상수 0.**
ㅊ **물리 0줄 · 처방 0 · 단정 0 · 판단 0 · 굽기 0.**
ㅋ **이 판에서 호출한 ecc 에이전트 = 0 · 스킬 = 0.**
```

### §2-① `src/` diff 0 · 전략 세션 귀책 20건째

```
 ★ `git diff f382603 -- src/` = **빈 출력**(확인).
 ★ **전략 세션 귀책 20건째 등재** — 판정문 원문 「귀책 20건째(전략 세션 · **앵커 방향 설명 오류**)」.
   v5-22 발주문 예측 「현행 대비 **5~8 cm 아래**」 ↔ 실측 밑단 **+48.599 / +38.151 mm 위**(v5-22 §1-② f0 표).
   앵커를 목으로 올리면 같은 총장이 더 높이 걸리므로 밑단은 올라간다 — 판정문이 스스로 귀책으로 등재했다.
```

---

## §4 판정 — **갈래 A**

```
 ★ A = ①②③④ ⟹ 상신(처분은 전략 세션)   … **✔ 이것이다**
   B = 계기 불가(사유) ⟹ 상신              … ✗ 계기 둘 다 작동했고 **기준선을 비트 일치로 재현**했다
 사후 갈래 신설 0.

 ① = **7 판본으로 냈다**(on 정착 2 · off 정착 1 · f0 on/off 4) — 링 둘레 · 사영 둘레 · 허용 · 초과비 ·
      휴지 대비 신장(분포) · 링 높이(앞·뒤·옆 · `Y_NECK` 대비) · 링↔몸 거리 분포 · 얹힘 비율(두 자) · `dC/dy`.
 ② = **사전 규칙이 「허용식 전제가 on 에서 불성립」 쪽을 가리킨다** —
      얹힘 **off 57.41% > on 35.19%**(`SEP` 자로도 75.93% > 50.00%) ·
      보강 = **on 이 «덜» 늘어나 있다**(`restRatio` 1.038 / 1.043 ↔ 1.083 · 엣지 신장 중앙 1.020 / 1.034 ↔ 1.106) ⟹
      「on 의 실제 과신장(옷 결함)」 쪽은 값이 지지하지 않는다. **자세 차 한정 · 단정 0.**
 ③ = **던진 쌍과 자리를 특정했다** — `back↔back` 삼각형 **14716↔14719** · **`j` = `nvB`·`nvB−1` · `i` 22~24** =
      **목선 구간**(`dEdge` 22 > `N_sh` 17) · 높이 1264.9~1288.1 mm(`Y_NECK` 아래 27.7~50.9) ·
      **off 는 성립**(최소쌍 2.2616 mm · 최악 쌍이 `front↔back` 으로 «부류»가 다르다) ·
      `SH_DROP`/앵커 따로 켜기는 **미집행**(`src/` 0줄 약정과 충돌 · 사유 사전 등재).
 ④ = **후보 셋을 «새 수 0 으로 유도 가능한지»와 함께 나열**했다 —
      (ㄱ)-2 「휴지 기준(구 채널 `ringRestRatio` · 옛 문턱 1.10)」만이 **새 수 0 · 새 식 0** 이고
      그 자로 재판정하면 **세 판본 다 통과**한다 · (ㄱ)-1 은 새 식 필요 · (ㄱ)-3 은 새 수 필요(제안 0) ·
      회귀 영향 = 완화 방향이면 **로컬 43칸 판정 불변**(목선 «단독» 실패 칸 **0** · 통과 칸 초과비 최대 0.9974).

 ★★★ 이 판의 핵심 값 셋
   ㉠ **채널을 움직이는 것은 링 길이가 아니라 «사영 둘레»다** — on 은 링이 2.88 cm 짧은데 허용이 4.48 cm 짧다.
   ㉡ **판정이 링 높이 5 mm 안에서 뒤집힌다** — on 두 판본 다 `y − 5 mm` 에서 통과(0.999151 · 0.991968).
   ㉢ **S 의 던짐은 «목선» 자리다**(어깨 토막이 아니다) — 목선 초과비 실패와 **같은 대역**에서 난다.
```
