# v5-17 — 교차의 «자리» 분포(기준선 529/446/512) · 처방 후보 f0 시험 · 교차 0 이면 첫 굽기

```
집행 기계  2호기(굽기 · 교차 0 달성 시에만) + 에어(조립 · 물리 0프레임)
브랜치     v5-17-crossings   기점 **71d816e**(v5-16 HEAD · 확인 = git log -1 이 71d816e)
약정       물리 0줄 · `src/` diff = `redrapeAsm2` 하위 플래그 **한정**(off 비트 불변) ·
           굽기 ≤ 3칸(**교차 0 달성 시에만**) · ecc 0 · §0 선커밋 · 브랜치 인쇄 ·
           보고 = docs/v5/보고/17.md · 캡처(굽기 시) · 콘솔 4줄
```

## §0-1 판정문 (전략 세션 · v5-16 §4 수령 · **원문**)

```
갈래 C 수용 · 귀책 18건째(전략 세션): 「근접이지 교차 아님」 전제 — 계기 반출력(C2) 위에 세웠으나
검증 없이 처방한 책임 · 완화는 교차 0 뒤로(순서) · ★ 교차 수백 = S4 «각자 밀어내기»가 메시를 제 위로
접는 성격 ⟹ 처방은 S3 배치가 «메시 결(행·열 간격) 보존 + 몸 밖» 을 동시에 만족해야 ·
후보 선택은 분포를 «먼저» 보고 · 비트 불변 3판 연속 · (A) 기본 승격 수용
```

## §0-2 수령 — 집행 «전»에 읽은 코드 사실 2건

```
 ㄱ ★ **①(교차 전수 열거)은 `src/` 0줄로 된다** — `garmentScene.ts:1238` 의 인쇄 훅
    `__v3gapProbe` 가 **`pos` · `tris` · `panels[{name, base}]` · `n`** 을 이미 넘기고,
    삼각형 교차 판정기 **`triTriHit`**(`instruments.ts:135`)이 내보내져 있다 ⟹ 계기를
    **`scripts/` 쪽에** 쓰면 된다(`minPairDist` 의 격자 후보 생성 방식을 그대로 따른다 · `:66-116`).
 ㄴ **(ㄷ) 의 «첫 행»은 `N_side` 다** — 패널 행은 `j = 0`(밑단 `py = 0`) → `j = nvB`(`py = L`) 이고
    `N_side = round(Y_ARM / d)`(`:409`) 이므로 **`j = N_side` 가 `py = Y_ARM`**(암홀 깊이 · 실루엣 튜브
    이음 자리)이다 ⟹ 블렌드 구간은 `j ∈ (N_side, nvB]` 로 **자리가 이미 정해져 있다**(새 수 0).
```

## §0-3 사전 등재 갈래 (회차 프롬프트 그대로 · 사후 신설 0 · ㄹ 만 ① 뒤 추가 등재 허용)

| 항 | 집행 | 갈래 |
|---|---|---|
| **①** 교차 분포(굽기 0 · 세 칸) | 교차 쌍 **전수 열거** — 패널 조합(front↔back / 같은 패널 / 소매 관련) · **행 `j` 분포**(봉제선부터 몇 행 · 튜브 이음 행 근처인지) · **열 `i` 분포**(팔 윗면 `s>0` 인지 몸통인지) · **S3 직후 vs S4 직후 교차 수**(어느 단계가 만드는지) | **(가)** 지배 자리 특정(한두 부류가 **80% 이상**) / **(나)** 분산 |
| **②** 후보 사전 등재 | (ㄱ)(ㄴ)(ㄷ) 를 **값 보기 «전»**에 형식화 · 각각 하위 플래그 · (ㄹ)은 ①(가)일 때만 §0 추가 등재 | — |
| **③** 후보 f0 시험 | 세 칸 × 후보 수 — **교차 수(전수)** · 최근접 정점쌍 · **행 간격** 최소/중앙 · **열 간격** 최소/중앙 · 관통 · S4 반복 · 봉제쌍 · 링/rest | 표 + **선택** |
| **④** 교차 0 달성 시 | off 비트 불변(A29·T108) → 굽기 3칸 → 「같은 옷」 대조. **미달성 시 착수 0** + 최소 후보의 잔여 교차 분포 상신 | — |

**선택 규칙**(값 보기 «전») — **세 칸 교차 수 «합»이 최소인 후보** · **0 이 아니면 굽기 착수 0**.

**§4** — **A** = 교차 0 + 굽기 3칸 ⟹ 상신(화면 판정) / **B** = 교차 감소·잔여(값·분포) ⟹ 상신 /
**C** = ①(나) ∧ 전 후보 무효 ⟹ 사실 상신 / **D** = 비트 불변 깨짐 ⟹ **즉시 정지** / **E** = 그 외 ⟹ 정지.

## §0-4 후보 세 개의 형식화 (값 보기 «전» · 각각 하위 플래그 · 새 상수 0)

```
 (ㄱ) **`FIX='AB'` — (A)+(B) 결합**
      S3 = **표면 추종**(접평면 사영 `d = normalize(−ŷ − (−ŷ·n̂)n̂)` × 그 행의 2D 엣지 길이 · 걸음 뒤
      `SEP` 까지 밀어냄 · v5-15 (B) 그대로) · S4 = **걸음 상한 `THICK` + 봉제선→아래 행 순서 ·
      반복 상한 `NY`**(v5-15 (A) 그대로). **두 코드가 이미 있으므로 «한 줄 조건»이다.**
 (ㄴ) **`FIX='ABI'` — (ㄱ) + 열 간격 항**
      S3 뒤에 **행마다** 같은 행의 이웃 열 거리를 **2D 패턴 거리로 투영**한다:
        각 행 `j` 에서 `i = 0…nu−1` 에 대해 `L2 = |uv(i+1,j) − uv(i,j)|`(면내 rest) ·
        `L3 = |p(i+1,j) − p(i,j)|` · 두 점을 중점 기준으로 **`L3 → L2` 가 되게 대칭 이동**
        (한 점당 `(L2 − L3)/2`) · **행 단위로 3회 훑는다**(반복 수 3 = `NY/20` 이 아니라 **등재 자 없음**
        ⟹ **수렴 판정 `TOL_SELF`** 로 돌리고 상한은 `NY` 로 둔다 · 새 상수 0) · 그 뒤 `SEP` 밀어냄.
 (ㄷ) **`FIX='BLEND'` — S3 블렌드(봉제선 ↔ 실루엣 튜브 첫 행)**
      **재료 둘** — ㉠ `place()` 가 이미 놓은 **튜브 위치**(오늘 자기검사를 통과하는 그 배치) ·
      ㉡ 상단 행에서 내린 **표면 추종 드레이프**(ㄱ 의 S3).
      **블렌드** — `j ∈ (N_side, nvB]` 에서 `w = (j − N_side)/(nvB − N_side)` ·
      `p = (1 − w)·튜브 + w·드레이프` ⟹ `j = N_side` 에서 **정확히 튜브**(w=0) ·
      `j = nvB` 에서 **정확히 능선 행**(w=1) · `j ≤ N_side` 는 **튜브 그대로**(손대지 않는다) ·
      블렌드 뒤 각 정점을 **`SEP` 등위면으로 사영**(밀어냄 한 번) · S4 는 **잔여만**.
      ★ 근거 = 판정문 「S3 배치가 «메시 결 보존 + 몸 밖»을 동시에」 — 튜브는 **결이 보존된 배치**이고
        (오늘 통과한다) 블렌드는 그 결을 **상단으로 이어 붙인다**.
 ★ **선택은 ③의 «교차 수 합»이 한다**(값 보기 전 규칙) — 이 절은 **고르지 않는다**.
```

---

## §1-① 교차 분포 — **갈래 (가) 지배 자리 특정**

계기 = 신설 `scripts/v5CrossDist.ts`(**`src/` 0줄** · `__v3gapProbe` 수신 + `triTriHit` + `minPairDist`
격자 방식 · 정점 공유 삼각형은 교차로 세지 않는다). 기준선은 **(A)** 경로다.
★ **교차 수가 v5-16 과 정확히 일치**한다(529 / 446 / 512) ⟹ 두 계기가 같은 것을 센다(교차 검산).

| 칸 | 교차 | 삼각형 | `nvB` | **`back↔front`** | 같은 패널 | 소매 | `j` 범위 | 상단 5행 / 12행 |
|---|---|---|---|---|---|---|---|---|
| `c100-h170-s45_M` | **529** | 17,808 | 69 | **510 (96.4%)** | 19 (3.6%) | 0 | 42~68 | 272 / 326 |
| `mst-over-L` | **446** | 22,872 | 76 | **378 (84.8%)** | 46 (10.4%) | 22 (4.9%) | 53~75 | 264 / 264 |
| `supima-L` | **512** | 19,520 | 73 | **454 (88.7%)** | 52 (10.2%) | 6 (1.2%) | 50~72 | 215 / 235 |

**행 `j` 분포**(`nvB − j` · 상위) — 기본 M **nvB−3 128 · nvB−13 89 · nvB−2 75 · nvB−12 54 · nvB−14 46 ·
nvB−4 40 · nvB−1 21** / mst **nvB−4 153 · nvB−14 76 · nvB−5 47 · nvB−3 42 · nvB−13 33 · nvB−15 31 ·
nvB−1 22** / supima **nvB−14 124 · nvB−4 107 · nvB−13 74 · nvB−3 70 · nvB−15 30 · nvB−5 21 · nvB−17 18**.
**열 `i` 분포**(상위) — 기본 M **i5 45 · i52 45 · i53 37 · i4 31 · i57 27 · i51 24** / mst
**i58 54 · i8 39 · i9 38 · i47 37 · i59 34 · i55 24** / supima **i58 48 · i55 42 · i5 39 · i53 36 ·
i54 31 · i59 29**.

```
 ㉠ ★ **지배 부류 = «앞뒤 사이»** — `back↔front` 가 **84.8 ~ 96.4%** ⟹ 사전 등재 기준(한두 부류가
    **80% 이상**)을 **세 칸 모두** 넘긴다 ⟹ **①(가)**.
 ㉡ ★ **행이 «두 띠»에 몰린다** — ㉮ **상단 띠 `nvB−1~5`**(봉제선 아래 1~5행) ·
    ㉯ **아래 띠 `nvB−12~17`**. 두 띠 밖은 드물다(`nvB−19` 12 · `nvB−24` 12 가 최대).
    ★ ㉯ 는 **실루엣 튜브 이음 행 근처**다 — `nvB = N_side + N_arm` 이고 `N_arm = round(LEN_ARM/d)`
      이므로 `nvB − N_arm = N_side`(= `py = Y_ARM`) ⟹ 관측된 12~17 이 그 자리와 겹친다(§0-2ㄴ).
 ㉢ ★ **열은 «어깨 토막»에 몰린다** — 상위 열이 전부 `i` 1~9(어깨L) 와 `i` 47~60(어깨R)이다 ·
    몸통 중앙 열은 상위에 없다 ⟹ 교차는 **어깨선이 만든 자리**에서 난다.
 ㉣ **S3 직후 vs S4 직후** — 이 계기는 자기검사가 부르는 `assemble` 의 **마지막 상태**를 받으므로
    두 단계를 «따로» 세지 못했다(**미측정** · 추정 0). 대신 ③이 **S3 를 바꾸는 후보들**로 그 답을
    간접적으로 준다(아래 AB / BLEND 의 극단적 차이).
```

## §1-③ 후보 f0 시험 — **선택 = (ㄷ) BLEND**(규칙대로) · 그러나 **0 이 아니다**

| 후보 | 기본 M | mst-over | supima | **합** | S4 반복 | 최근접 정점쌍 mm |
|---|---|---|---|---|---|---|
| **A**(기준선) | 529 | 446 | 512 | **1487** | 21 · 20 · 21 | 1.58249 · 1.32808 · 0.93036 |
| **(ㄱ) AB** | **29,530** | **24,785** | **18,816** | **73,131** | 1 · 1 · 1 | 0.52353 · 0.93855 · 0.67310 |
| **(ㄴ) ABI** | 454 | 669 | 720 | **1843** | 15 · 12 · 11 | 1.70982 · 1.23872 · 1.07839 |
| **★ (ㄷ) BLEND** | **466** | **176** | **366** | **★ 1008** | 10 · 13 · 11 | 1.32775 · **2.0** · 0.92080 |

```
 선택 규칙(§0-3 · 값 보기 «전») = 「세 칸 교차 수 **합**이 최소인 후보 · **0 이 아니면 굽기 착수 0**」
 ⟹ **(ㄷ) BLEND**(합 **1008** = 기준선의 **67.8%**) · **0 이 아니므로 ④ 착수 0.**

 ㉠ ★★★ **(ㄱ) AB 가 교차를 «49배» 폭증시킨다**(1487 → **73,131**). v5-15 는 (B) 가 **관통을 원천
    차단**한다고만 재고 **교차를 세지 않았다**(그때 `hits` 를 안 읽었다 · 대장 `C2`) ⟹ 이 판이 처음
    그 대가를 값으로 본다: **표면 추종은 관통을 없애는 대신 메시를 제 위로 접는다**(판정문의 성격 진단이
    S4 가 아니라 **S3 표면 추종**에도 그대로 적용된다).
 ㉡ **(ㄴ) ABI 는 기준선보다 나쁘다**(1843 > 1487) — 열 간격 항이 기본 M 은 개선(529 → 454)하지만
    mst(446 → 669) · supima(512 → 720)를 악화시킨다 ⟹ **열만 잡으면 행이 깨진다**.
 ㉢ ★ **(ㄷ) BLEND 만 «세 칸 모두» 기준선 이하**다 — 466 / **176** / 366(mst 는 **39.5%** 로 최대 개선) ·
    mst 의 최근접 정점쌍이 **정확히 2.0 mm = `SEP`** 다(그 칸에서는 앞뒤 간격이 설계값에 정확히 선다).
```

### ★★★ 잔여 교차의 «성격»이 바뀌었다 — 앞뒤 **−87.6%**

| 부류 | (A) 기준선 합 | **(ㄷ) BLEND 합** | 변화 |
|---|---|---|---|
| **`back↔front`**(앞뒤) | **1342** | **167** | **−87.6%** |
| 같은 패널(`back↔back` + `front↔front`) | 117 | **775** | +562% |
| 소매 관련 | 28 | 66 | — |
| 합 | 1487 | **1008** | **−32.2%** |

BLEND 잔여의 자리 — 기본 M **back↔back 224(48.1%) · back↔front 147(31.5%) · front↔front 95(20.4%)** ·
mst **back↔back 87(49.4%) · front↔front 49(27.8%) · 소매 22(12.5%) · 앞뒤 0** ·
supima **front↔front 192(52.5%) · back↔back 128(35.0%) · back↔front 20(5.5%)**.
행 — 기본 M `nvB−3 87 · nvB−4 62 · nvB−1 61 · nvB−5 49 · nvB−6 46` / mst `nvB−1 40 · nvB−5 28 ·
nvB−4 20 · nvB−6 19` / supima `nvB−1 56 · nvB−4 52 · nvB−8 44 · nvB−5 40`.

```
 ㉠ ★★★ **BLEND 가 「앞뒤 교차」를 거의 없앴다** — 1342 → **167**(mst 는 **0**) ⟹ ①㉠ 이 지목한
    **지배 부류가 해소**됐다.
 ㉡ ★★★ **아래 띠(`nvB−12~17`)가 사라졌다** — BLEND 잔여의 행이 **`nvB−1~8`** 에만 있다 ⟹
    ①㉡㉯(튜브 이음 행 근처)가 **정확히 고쳐졌다**(BLEND 가 `j ≤ N_side` 를 튜브 그대로 두고
    그 위만 이어 붙인 결과 · 설계 의도와 값이 일치한다).
 ㉢ ★ **남은 것은 «한 패널이 제 위로 접히는» 교차다**(같은 패널 **68.5 / 77.2 / 87.5%**) ·
    자리는 **봉제선 아래 1~8행 · 어깨 토막 열**(i 4~5 · 49~55 · 58)이다.
 ⟹ **문제의 층위가 «앞뒤 사이»에서 «한 패널 안»으로 내려갔다**(새 사실 · 다음 판의 표적).
```

## §1-④ — **착수 0**(교차 0 미달성)

```
 사전 등재 「교차 0 달성 시 … · **미달성 시 착수 0** + 최소 후보의 잔여 교차 분포를 상신」 ⟹
 **off 비트 불변 미실행 · 굽기 0칸 · 2호기 접촉 0 · 캡처 0장** · 잔여 분포는 위에 전량 적었다.
 ★ 한정(자기신고) — 이 판의 `src/` diff 는 **전부 `redrapeAsm2` 안 + `asm2Fix` 타입 확장** 이고
   `asm2` 가 꺼지면 그 함수가 호출되지 않으므로 off 경로는 형태상 불변이지만, **이 판에서 값으로
   확인하지 않았다**(비트 불변은 v5-12·v5-14·v5-16 세 판 연속 통과 상태 · 다음 판 필수 항목).
```

## §2 자기검사

| 항 | 약정 | 실측 |
|---|---|---|
| 기점 | `71d816e`(v5-16 HEAD) | ✅ 등재 |
| 물리 0줄 | | ✅ `gpu/` diff **0** · 굽기 **0칸** |
| `src/` diff | `redrapeAsm2` 하위 플래그 한정 | ✅ 그 함수 + `asm2Fix` 타입 확장(2파일) |
| 굽기 | ≤3칸(교차 0 시에만) | **0칸**(미달성) |
| ecc | | **0 · 0** |
| §0 선커밋 | | ✅ `e38158d`(사무 귀책 18 포함) |
| 갈래 | 사후 신설 0 | ✅ ① **(가)** · ③ **BLEND 선택**(규칙대로) · ④ 착수 0 · **(ㄹ) 추가 등재 안 했다** |
| 선택 규칙 | 값 보기 «전» | ✅ §0-3 에 「합 최소 · 0 아니면 굽기 착수 0」을 적고 그대로 적용 |

**자기신고 2건** — ㉠ **①의 「S3 직후 vs S4 직후」는 미측정**이다(계기가 `assemble` 의 최종 상태만
받는다 · 추정 0 · ③이 간접 답을 준다) · ㉡ **off 비트 불변을 이 판에서 재지 않았다**(사전 등재대로 ·
근거와 함께 다음 판 필수 항목으로 남겼다).
이 판 diff — `src/v3/garmentScene.ts`(후보 ㄱㄴㄷ) · `src/v3/dressRun.ts`(타입) ·
`scripts/v5CrossDist.ts` **신설**(계기 · `src/` 0줄로 전수 열거) · `v5Asm2Probe.ts`·`v4AsmExport.ts`(FIX 확장).

## §4 갈래 판정 — **B**

```
 §0-3 등재 — 「**B** = 교차 감소·잔여(값·분포) ⟹ 상신」
 ① = **(가)** 지배 자리 특정 — `back↔front` **84.8~96.4%** · 행 **두 띠**(`nvB−1~5` · `nvB−12~17`) ·
      열 **어깨 토막**(i 1~9 · 47~60)
 ③ = **(ㄷ) BLEND 선택**(합 **1008** = 기준선 67.8% · 규칙대로) · ★ **(ㄱ) AB 는 교차 49배 폭증**(73,131) ·
      **(ㄴ) ABI 는 기준선보다 나쁘다**(1843)
 ★★★ **앞뒤 교차 1342 → 167(−87.6%) · 아래 띠 소멸** ⟹ 지배 부류와 한 띠가 **해소**됐다 ·
      남은 것은 **«한 패널이 제 위로 접히는» 교차**(같은 패널 68.5~87.5% · 봉제선 아래 1~8행)
 ④ = **착수 0**(교차 0 미달성)
 ⟹ **B**. ★ **A 가 아니다**(교차 0 아님) · **C 가 아니다**(①(가)이고 BLEND 가 유효했다) ·
    **D 가 아니다**(비트 불변을 깬 증거 없음 — 다만 이 판에서 재지 않았다 · §1-④ 한정).
```
