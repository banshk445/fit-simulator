# v5-14 — 어깨선 국소 좌표계(`t × n` 벌림 · S3 국소 낙하) · T포즈 108 재확인 · 진단 굽기 3칸

```
집행 기계  2호기(굽기) + 에어(조립·sha 대조 · 물리 0프레임)
브랜치     v5-14-local-frame   기점 **4934876**(v5-13 HEAD · 확인 = git log -1 이 4934876)
약정       물리 0줄 · `src/` diff = **`redrapeAsm2` 한정**(off 비트 불변) · 굽기 ≤ 3칸 · ecc 0 ·
           §0 선커밋 · 브랜치 인쇄 · 보고 = docs/v5/보고/14.md · 캡처 3칸 앞·옆 · 콘솔 4줄
사무       ㄱ 설계서에 「닫힌 = 간극 0(거리 `SEP`)」 반영(현재 **0건** — 미반영 확인) ·
           ㄴ `CLAUDE.md` 에 「구현 중간 산출 즉시 커밋」 등재(현재 **0건** — 미등재 확인)
```

## §0-1 판정문 (전략 세션 · v5-13 §4 수령 · **원문**)

```
갈래 B 수용 · 능선 자 성립(부풂 아님 · 규칙 불변) · 호길이 사상 성립(대칭 · 세 칸 팔 윗면 — 실물 정합) ·
★★★ 던짐 = 면 교차 · 원인 = 전역 z 벌림이 기울어진 능선에서 면에 수직이 아님 ·
처방 = 국소 좌표계: b = t × n(t 능선 접선 · n 몸 SDF 기울기) 로 ±SEP/2 벌림 ·
S3 첫 행들도 국소 아래(b 벌림 후 n 바깥·−y 합성 — CC 형식화 · 손 상수 0)로 낙하 ·
㉡(같은 i 앞뒤 z 차 행별 추적)을 처방 «전» 사실로 먼저 · 능선 규칙 불변
```

## §0-2 수령 — 집행 «전»에 읽은 코드 사실 3건

```
 ㄱ **지금 벌림은 «전역 z» 한 줄이다** — `redrapeAsm2` 의 `ZHALF = SEP/2` 를
    `q = [r3[0], r3[1], r3[2] + (isFront ? ZHALF : −ZHALF)]` 로 준다(v5-12 정정 2) ⟹
    능선이 기울어도 벌림 방향이 **바뀌지 않는다**(판정문이 지목한 자리).
 ㄴ **S3 는 «전역 −y» 낙하다** — `pos[vb] = pos[vt]` 에 `y` 만 `− len` 한다 ⟹ 앞뒤 열이 **같은 (x,z)**
    를 공유하며 내려가고, 행이 하나 밀리면 서로의 «행 사이»로 들어간다(v5-13 §1-③㉡ 실측).
 ㄷ **`n`(몸 SDF 기울기)은 이미 S4 가 쓴다** — 중앙차분 `sampleSdf(bodyG, ·±h, …)` 세 축 ⟹
    **같은 식을 그대로** 국소 좌표계에 쓴다(새 식 0 · `t` 는 능선 폴리라인의 접선).
```

## §0-3 사전 등재 갈래 (회차 프롬프트 그대로 · 사후 신설 0)

| 항 | 집행 | 갈래 |
|---|---|---|
| **①** ㉡ 사실(처방 «전») | 세 칸에서 같은 `i` 의 앞뒤 열 간격을 **행별로** 찍어 어느 행·어느 `s`(팔축 투영)에서 `SEP` 밑으로 가는지 + 그 자리의 **능선 기울기**(`t·ŷ` · `n` 과 `z` 의 각) | **(가)** 「기울기 클수록 먼저 교차」 상관 성립 / **(나)** 불성립(다른 원인 · 값) |
| **②** 국소 좌표계 구현 | 벌림 `b = normalize(t × n)` · S3 낙하 방향 = 국소(형식화 등재) · **조립 자기검사 통과가 조건** · f0 채널(링/rest · 봉제쌍 ≈ `SEP` · 앞뒤 면 ≥ `SEP` · 관통 · S4 수렴 · 어깨끝 `s`) | **(가)** 세 칸 통과 / **(나)** 던짐(값) |
| **③** off 비트 불변 | A포즈 **29/29** + **T포즈 108/108 재확인**(v5-13 한정 해소) | 통과/깨짐 |
| **④** R3 진단 굽기 3칸 | 게이트 · 어깨 걸림(**정도**) · 링 · 층3 5행(현행 나란히 · **차 보고만**) · `RAMP_N` 실측(예측 **≈52/42** 대조) · 캡처 | — |
| **⑤** 종합 | v4-17 「같은 옷」 ①②로 레귤러핏(supima·기본 M) 동일 여부 · mst-over 회복 **정도** | 판단 0 |

**§4** — **A** = ②(가)+③+④ ⟹ 상신(화면 판정 · R4 이후 결정) / **B** = ②(나) ⟹ 값 상신 /
**C** = ①(나) ⟹ 사실 상신 · **② 착수 0** / **D** = ③ 깨짐 ⟹ **즉시 정지** / **E** = 그 외 ⟹ 정지.
**사후 갈래 신설 0 · 걸림은 «정도»로.**

## §0-4 형식화와 사전 결정 4건 (값 보기 «전»)

```
 ㄱ **국소 좌표계 형식화**(새 상수 0 · 새 식은 외적 하나) —
      `t` = 능선 폴리라인의 **단위 접선**(그 호길이 자리의 두 이웃 표본 차 · 정규화) ·
      `n` = **몸 SDF 기울기 단위 벡터**(S4 가 쓰는 그 중앙차분 · `h = sdfSpec.h`) ·
      `b = normalize(t × n)` — 능선을 따라가며 «면의 앞뒤» 를 가리키는 축이다.
      **벌림** = 앞판 `+b·(SEP/2)` · 뒤판 `−b·(SEP/2)`(부호는 `b·ẑ > 0` 이 «앞»이 되도록 한 번 맞춘다 ·
      그 판정은 `AXIS_Z` 기준 `ẑ = (0,0,1)` 과의 내적 부호 하나 · 손 상수 0).
      **S3 낙하** = 전역 `−y` 대신 **국소 아래** `d = normalize(−ŷ + (−ŷ·n)·n·(−1))` … ★ 이 식은
      「`−ŷ` 를 몸 표면에 «접하게» 만든 것」이다: `d = normalize(−ŷ − (−ŷ·n) n)`(법선 성분 제거) ·
      `n` 과 `−ŷ` 가 평행하면(분모 0) **전역 `−y` 로 되돌린다**(가드 · 새 상수 0).
 ㄴ **①의 판정 기준**(값 보기 «전») — 「상관 성립」 = **기울기(|t·ŷ|)가 큰 `i` 에서 «먼저»**
    (= 더 높은 `j` 에서) `SEP` 밑으로 내려가는 것. 세 칸 중 **둘 이상**에서 단조 경향이면 (가) ·
    그 외는 (나). **순위 상관을 수로 적고** 문턱은 새로 만들지 않는다.
 ㄷ **②는 조립 자기검사 통과가 «조건»이다** — 던지면 (나)이고 **④⑤ 착수 0**.
 ㄹ **`git add` 명시 경로만 · 구현 중간 산출은 «즉시» 커밋**(v5-12 사고 3 · 이 판 사무 ㄴ 으로 등재한다).
```

---

## §1-⓪ 사무 2건 — 집행 완료

```
 ㄱ **설계서에 「닫힌 = 간극 0」 반영** — `docs/v5/설계-조립2세대.md` 의 **S2 절**을 고쳤다(0건 → 반영):
    「이미 닫힌」은 **거리 0 이 아니라 «거리 = rest = `SEP`»(간극 0)** 이고, 근거(봉제 주석 원문)와
    실측(`front↔back 0.000mm` 던짐)을 달았다 · **채널 기대값도 정정**(「거리 ≈ `SEP` · 간극 ≈ 0」) ·
    v5-10 의 「0.0000 mm」가 **자기검사 밖 가상값**이었음을 병기.
 ㄴ **`CLAUDE.md` 에 「구현 중간 산출 즉시 커밋」 등재**(0건 → 등재) — v5-13 준비 중 `src/` 를 기점
    판본으로 되돌려 정정 2건이 덮인 사고(v5-12 §1-① 사고 3)를 근거로 달고, 「작업트리를 건드리는 명령
    앞에서 `git status --porcelain` 을 먼저 보고 미커밋분은 커밋하거나 `git stash` 로 격리한다」를 적었다.
```

## §1-① ㉡ 사실 — **갈래 (나) 불성립** · ★★★ 판정문의 원인 가설이 **기각**된다

계기 = `redrapeAsm2` 의 인쇄 훅에 **행별 추적**을 더했다(ASM2 전용 · 동작 0) — 같은 `i` 의 앞뒤 간격을
`j = nvB` 에서 아래로 훑어 **`SEP` 밑으로 가는 첫 `j`** 와, 그 `i` 의 **능선 기울기 `|t·ŷ|`** ·
**`n`↔`ẑ` 각** · 실패 자리의 **같은행/대각 거리와 행 간격**을 함께 낸다.

### ㉠ 상단 행은 «완벽하다» — 전역 `z` 벌림이 이미 작동한다

```
 세 칸 · 모든 `i` 에서 **상단 간격 = 2.0000 mm** (= `SEP` 정확히) ⟹ `±SEP/2` 전역 `z` 벌림은
 상단 행에서 **의도대로 작동한다**. 벌림 «크기»는 문제가 아니다.
```

### ㉡ ★★★ `n` 이 `ẑ` 와 **수직**이다 ⟹ **`t × n` ≈ `±ẑ`** ⟹ **처방이 무효다**

| 칸 | `n`↔`ẑ` 각(좌 · 어깨 토막 전체) |
|---|---|
| `c100-h170-s45_M` | 89.57 · 92.79 · 91.47 · 92.88 · 90.39 · 90.09 · 89.03 · **90.00 × 9** |
| `mst-over-L` | 89.73 · **90.00 × 20** |
| `supima-L` | 91.04 · **90.00** · 93.29 · **90.00 × 14** |

```
 산수 — `n` 이 `ẑ` 와 수직이면 `n = (n_x, n_y, 0)` 이고 능선 접선 `t` 도 z 성분이 거의 0 이므로
   `t × n = (t_y·0 − 0·n_y, 0·n_x − t_x·0, t_x n_y − t_y n_x) = (0, 0, ·)` ⟹ **`b ∥ ẑ`**.
 ⟹ 판정문의 처방(「`b = t × n` 으로 ±SEP/2 벌림」)은 이 기하에서 **전역 `z` 벌림과 «같은 축»**이다
    ⟹ **처방이 원인을 건드리지 않는다**(무효 처방 · 값으로 기각).
 ⟹ 판정문의 원인 문언(「전역 z 벌림이 기울어진 능선에서 면에 수직이 아님」)도 **기각**된다 —
    `n ⊥ ẑ` 이므로 `z` 벌림은 **이미 «면의 법선에 수직인 평면» 안**에 있다(= 앞뒤를 가르는 방향이다).
```

### ㉢ 「기울기 클수록 먼저 교차」 — **상관 없음**(§0-4ㄴ 기준 미달 ⟹ (나))

| 칸 | 기울기 `|t·ŷ|` 범위 | `SEP` 밑 첫 `j` 의 `nvB−j` | 단조 경향 |
|---|---|---|---|
| `c100-h170-s45_M` | 0.1857 ~ 1.0000 | 0 · 14 · 12 · 12 · 1 · 13 · −1 · −1 · −1 · 2 · 3 · 2 · −1 · 2 · 14 · −1 | **없음** |
| `mst-over-L` | 0.1436 ~ 1.0000 | 14 · −1 · −1 · 13 · −1 · 13 · 2 · −1 · 4 · 3 · 4 · −1 · 3 · −1 · −1 · −1 · −1 · 13 · −1 · −1 · −1 | **없음** |
| `supima-L` | 0.2004 ~ 0.3773 | 13 · 14 · 12 · 3 · −1 · −1 · −1 · −1 · 4 · −1 · −1 · 3 · 3 · 13 · −1 · 12 · −1 | **없음** |

(`−1` = 그 `i` 는 끝까지 `SEP` 밑으로 안 간다.) §0-4ㄴ 이 **값 보기 «전»**에 적은 자 —
「기울기가 큰 `i` 에서 **더 높은 `j`** 에서 먼저 내려간다」가 **세 칸 중 둘 이상에서 단조 경향」** —
**한 칸도 성립하지 않는다**(기울기 최대 1.0000 인 `i=0` 은 세 칸 다 `−1` = 아예 안 내려간다).
⟹ **①(나) 불성립.**

### ㉣ ★★★ 그 대신 **진짜 원인이 값으로 잡혔다 — S4 한 걸음이 «한 행»을 건너뛴다**

실패 자리의 세 수(좌측 · 앞 7개)

| 칸 | `i` | 첫 `j` | **같은행 mm** | **대각 mm** | **행 간격 mm** |
|---|---|---|---|---|---|
| `c100-h170-s45_M` | 15 | 69(=nvB) | 2.0000 | 6.2129 | — |
| | 14 | 55 | 2.1500 | **1.5387** | **1.0325** |
| | 13 | 57 | 1.9787 | 1.8927 | 25.3965 |
| | 10 | 56 | 2.0149 | **1.4688** | **1.7870** |
| `mst-over-L` | 20 | 62 | 2.0446 | **1.0486** | **1.0945** |
| | 17 | 63 | 2.0636 | **1.7301** | **1.5381** |
| | 15 | 63 | 2.0211 | **1.6132** | **2.7080** |
| `supima-L` | 15 | 59 | 1.9786 | **1.1957** | **0.8706** |
| | 13 | 70 | 4.0554 | **0.4353** | **2.2199** |
| | 16 | 60 | 1.8725 | 3.0021 | 23.5523 |

```
 ㉠ **같은 행 간격은 유지된다** — 실패 자리에서도 **1.8169 ~ 4.0554 mm**(≈ `SEP`) ⟹ **벌림은 산다**.
 ㉡ **실패는 «대각»에서 난다** — 0.4353 ~ 1.9551 mm ⟹ 앞판의 한 정점이 뒤판의 **이웃 행**과 붙는다.
 ㉢ ★★★ **그 자리의 «행 간격»이 붕괴해 있다** — **0.8706 · 1.0325 · 1.0945 · 1.5381 · 1.7870 ·
    2.2199 · 2.7080 mm** (정상 = `L / nvB` ≈ **9.9 mm**) ⟹ 한 열의 두 이웃 행이 **1 mm 안으로** 모였다.
 ㉣ ★★★ **S4 의 한 걸음이 그만큼이다** — S4 최대이동 **첫 회 10.2291 mm**(세 칸 동일)이고
    행 간격이 **≈9.9 mm** 다 ⟹ **밀어냄 한 번이 «한 행»을 통째로 건너뛸 수 있다.**
    그래서 이웃 행이 서로 지나쳐 **앞뒤가 «행 사이»로 엇갈린다**(v5-13 §1-③㉡ 의 그 자리).
 ⟹ **원인은 «벌림 방향»이 아니라 «S4 밀어냄의 한 걸음 크기»다**(값 근거: 10.2291 ≈ 9.9 mm ·
    실패 자리 행 간격 0.87~2.71 mm · 같은행 간격은 유지).
 ★ 보조 사실 — `SEP` 밑 첫 `j` 로 잡힌 자리 중 일부는 **같은행이 1.95~2.00 mm** 인 «경계 걸림»이다
   (`SEP` = 2.0 mm 바로 밑 · 수치 경계) ⟹ 실질 실패는 **행 간격이 붕괴한 자리**다(위 굵은 값).
```

## §1-② 국소 좌표계 구현 — **착수 0**(사전 등재대로)

```
 §0-3 이 「**C** = ①(나) ⟹ 사실 상신 · **② 착수 0**」을 걸었고 ①이 (나)로 나왔다 ⟹
 **`b = t × n` 벌림·국소 낙하를 구현하지 않았다.** §0-4ㄱ 의 형식화는 **초안으로 남긴다**(사후 확대 0).
 ★ 그리고 ㉡ 가 그 처방이 **이 기하에서 무효**임을 값으로 보였다 — 구현했어도 전역 `z` 와 같았다.
```

## §1-③ off 비트 불변 — **통과**(v5-13 한정 **해소**)

```
 A포즈 **29 / 29 동일** · **T포즈 108 / 108 동일**(기준선 = v5-12 에서 «커밋된 v5-11 코드»로 뜬 표 ·
 같은 계기 `scripts/v5AsmSha.ts` · 줄 단위 대조 · 던지는 26칸의 **문언까지 동일**)
 ⟹ v5-13 §1-④ 의 한정(「T포즈 108칸 미재확인」)이 **이 판에서 해소**됐다.
 ★ 이 판의 `src/` diff 는 **전부 `redrapeAsm2` 안의 인쇄 훅**(플래그 off 면 호출되지 않는 함수)이다.
```

## §1-④⑤ R3 진단 굽기 · 종합 — **착수 0**

```
 ②가 착수 0 이므로 굽기·캡처·종합도 **착수 0** — **굽기 0칸 · 2호기 접촉 0 · 캡처 0장**.
 게이트·어깨 걸림·링·층3 5행·`RAMP_N` 실측·「같은 옷」 대조는 **전부 미집행**이고 사유는 이 한 줄이다.
```

## §2 자기검사

| 항 | 약정 | 실측 |
|---|---|---|
| 기점 | `4934876`(v5-13 HEAD) | ✅ 등재 |
| 물리 0줄 | | ✅ `gpu/` diff **0** · 굽기 **0칸** |
| `src/` diff | `redrapeAsm2` 한정 · off 비트 불변 | ✅ **A 29/29 · T 108/108 동일** |
| 굽기 / 캡처 | ≤3칸 / 3칸 앞·옆 | **0칸 / 0장**(사전 등재 C) |
| ecc | | **0 · 0** |
| §0 선커밋 | | ✅ `501495c` |
| 사무 | ㄱ 설계서 · ㄴ CLAUDE.md | ✅ 둘 다(미등재 확인 후) |
| 브랜치 인쇄 | | ✅ `v5-14-local-frame` |
| 갈래 | 사후 신설 0 | ✅ ① **(나)** · ②③④⑤ 착수 0/통과 — 이름을 새로 만들지 않았다 |
| 문턱 | 신설 0 · 이동 0 | ✅ ①의 자를 **값 보기 전** §0-4ㄴ 에 적었고 그대로 적용했다 |

**자기신고 1건** — ①이 (나)로 갈리면서 **②의 구현을 하지 않았다**(사전 등재대로) ⟹ 판정문의 처방이
**집행되지 않았다**. 그 사유는 ㉡ 의 값(`n`↔`ẑ` ≈ 90° ⟹ `b ∥ ẑ`)이고, **처방을 임의로 바꾸지 않았다**.
이 판 diff — `src/v3/garmentScene.ts`(인쇄 훅 2회 추가) · `CLAUDE.md` +6 · 설계서 S2 절 교체 · 계기 신설 0.

## §4 갈래 판정 — **C**

```
 §0-3 등재 — 「**C** = ①(나) ⟹ 사실 상신 · **② 착수 0**」
 ① = **(나) 불성립** — 「기울기 클수록 먼저 교차」가 **한 칸도** 성립하지 않는다(기울기 최대 1.0000 인
      `i=0` 이 세 칸 다 «안 내려간다») ⟹ §0-4ㄴ 기준 미달
 ② = **착수 0** · ③ = **통과**(A 29/29 · **T 108/108** — v5-13 한정 해소) · ④⑤ = **착수 0**
 ⟹ **C**. ★ **D 가 아니다**(비트 불변 유지) · **B 가 아니다**(②를 착수하지 않았다).
 ★★★ 이 판이 낸 두 가지 — ㉠ **판정문의 원인·처방이 값으로 기각**된다(`n ⊥ ẑ` ⟹ `t × n ∥ ẑ`) ·
   ㉡ **진짜 원인이 특정**됐다(S4 한 걸음 **10.2291 mm** ≈ 행 간격 **9.9 mm** ⟹ 한 행 건너뛰기 ·
   실패 자리 행 간격 **0.87~2.71 mm** · 같은행 간격은 **1.82~4.06 mm** 로 유지).
```
