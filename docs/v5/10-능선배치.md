# v5-10 — 어깨선 «능선 배치»(f0 봉제쌍 일치) 가상 실험 → 진단 굽기 2칸 · 회귀 «영향 범위» 사실

```
집행 기계  2호기 DESKTOP-OV72A46(win32-x64 · GTX 1660 Ti 6144MiB · driver 610.88 · CUDA · Python 3.12.10)
브랜치     v5-10-ridge-placement   기점 1602914(v5-9 HEAD · 확인 = git log -1 이 1602914)
약정       물리 0줄 · `src/` diff = **garmentScene 어깨 배치 «진단 플래그 경로»**(기본값 = 현행 ·
           **정본 경로 비트 불변**이 조건) · 굽기 ≤ 3칸 · ecc 0 · §0 선커밋 · 브랜치 인쇄 · 캡처 · 콘솔 4줄
보고       docs/v5/보고/10.md            노트(이 파일) docs/v5/10-능선배치.md
```

## §0-1 판정문 (전략 세션 · v5-9 §4 수령 · **원문**)

```
갈래 A 수용(③ 정도 0) · ★★★ 원인 확정 = 어깨 봉제 닫힘 경로가 몸의 팔을 관통(SW 52 · 팔축 +64~80mm)
— «올려 입히기»(v3-21) 의 정의역 밖 · 소매 관 무죄 · (B) 순서·가중 사망(차단 이동 · supima 파괴) ·
2배 링은 판별자 아님(설계 의도 등재) ·
★ 처방 방향 = 실물 착의 방식: 어깨 봉제쌍을 f0 에 «이미 닫힌» 상태로 몸의 어깨 능선
(목 밑동→어깨점→팔 윗면) 위에 배치 · 앞뒤 패널은 거기서 아래로 · 하위 행 배치 불변(국소) ·
예측(반증 대상): f0 목선 링 ≈ rest 자릿수 · 끌어올림 불필요 · SW 52 걸림 회복 «정도» > 0 ·
supima 는 pass 유지 · 사고: git add -A 금지 등재
```

## §0-2 수령 — 집행 «전»에 읽은 코드 사실 5건

```
 ㄱ ★ **능선의 두 끝이 이미 코드에 있다.**
    `garmentScene.ts:176-186 shoulderTopY()` — 「몸 표면이 `|x| = SW/2` 에 «닿는» 가장 높은 y」를
    **±`sdfSpec.h` 띠**로 뽑아 `Y_TOP` 을 만든다 ⟹ **능선의 «바깥» 끝이 곧 `Y_TOP` 이다.**
    `:197-234 neckBaseY()` + `:235 NECK_RING = ringOf(planeSection(0,1,Y_NECK), SEP)` +
    `:238 NECK_A = NECK_RING.vmax` ⟹ **능선의 «안» 끝이 목 밑동 링의 어깨 쪽 점이다.**
    ⟹ 능선은 **새 기준을 고르지 않고** 이 둘을 잇는 곡선으로 형식화할 수 있다(§0-4ㄱ).
 ㄴ **상단 행의 구성이 세 토막이다** — `:416-418`
      `shL = line([−SW/2, L], [−NECK_A, L], N_sh)` · `nk = resample(neckF, N_nk)` ·
      `shR = line([NECK_A, L], [SW/2, L], N_sh)` · `topB = [...shL.slice(0,-1), ...nk.slice(0,-1), ...shR]`
    ⟹ 상단 행 인덱스 `i` 는 **0 = 어깨 끝(−x)** → `N_sh` = 목(−) → `N_sh+N_nk` = 목(+) →
       `nuB = 2·N_sh+N_nk` = 어깨 끝(+x) 다(v5-9 §1-① 의 순번 정정과 **같은 방향**).
 ㄷ **배치 machinery 가 전부 «내보내져» 있다** — `createScene` 반환(`:951-963`)에
    `Y_TOP · Y_NECK · NECK_RING · NECK_A · SH_LEN · LEN_NECK · at · build · place ·
     supportAt · boundaryOf · arcOn · bodyPoint · HSUP_Y · SLAB · yOf · NY · DELTA · GAP_SIDE` 가 있다
    ⟹ ②의 **가상 배치는 «코드 0줄»로** 계기에서 만들 수 있다(src/ 손대지 않는다).
 ㄹ **면내 rest 는 «2D 패턴(uv)»에서 나온다** — `assemble` 이 `makeInplane(tris, uv, …)` 로 만든다
    (`:883`). ⟹ **상단 행을 옮겨도 rest 는 변하지 않는다** — 바뀌는 것은 «시작 변형»이다.
    그 크기가 ②의 「하위 행과의 최대 엣지 신장」 채널이다.
 ㅁ ★ **어깨 봉제가 닫혀서 시작하면 `RAMP_N` 이 «다른 봉제»가 정한다.**
    v5-9 실측 — 어깨 rest0 최대 **243.948 mm** 가 전 그룹 최대이고 그다음이 **암홀 143.122 mm** ·
    `RAMP_N = ceil((max rest0 − SEP)/(G·DT²))`(`worker.py:125`) ⟹ 어깨가 0 이 되면
    `RAMP_N` 이 **89 → ≈52**(암홀 기준)로 내려간다. **예산·정착 프레임이 함께 움직인다**(사실).
```

## §0-3 사전 등재 갈래 (회차 프롬프트 그대로 · 사후 신설 0)

| 항 | 집행 | 갈래 |
|---|---|---|
| **①** 능선 정의(몸에서 · 손 상수 0) | 목 밑동 링 어깨 쪽 점 → 어깨 피벗 P → 팔축 AX 방향 `SW/2` 까지 «최상단» 곡선 규칙 형식화 + supima·mst-over 봉제선 길이 ↔ 능선 길이 비교 | (갈래 없음 · 사실) |
| **②** 가상 실험(코드 0 · f0 만) | 봉제쌍을 능선 위 **일치점**에 놓고 **상단 행만** 옮긴 가상 배치 — 목선 링 둘레 · 몸 관통 정점 수·깊이 · 봉제쌍 거리 0 확인 · 하위 행과의 최대 엣지 신장 | **(가)** 관통 band 안 + 신장 허용 자릿수 / **(나)** 초과(값) ⟹ **③ 착수 0** |
| **③** 진단 굽기(②(가) 전제 · 2칸 · 플래그 · 정본 0) | supima-L(**회귀** · pass 유지가 조건) · mst-over-L(회복 정도 · 어깨위 정점 · 최고점 · 링 · 교차 · 게이트) | **(가)** supima pass 유지 + mst 회복>0 / **(나)** supima 깨짐(값) / **(다)** mst 무변화 |
| **④** 회귀 영향 범위(판단 0) | T포즈 108 조립 sha 영향(플래그 off = 0 이 조건) · A포즈 제공 29칸 재굽기 필요·시간(칸당 실측) · 「정본 승격」 결정 항목 목록 | 판단 0 |

**§4** — **A** = ②(가)+③(가)+④ ⟹ 상신(다음 = 정본 승격 판정) / **B** = ③(나) ⟹ 값 상신 /
**C** = ②(나) 또는 ③(다) ⟹ 사실 상신 / **D** = 그 외 ⟹ 정지. **회복은 «정도»로.**

## §0-4 형식화와 diff 초안 (값 보기 «전»)

### ㄱ 능선 `ridge(x)` — **규칙은 `shoulderTopY` 의 것을 그대로 일반화한다**

```
 정의역  x ∈ [NECK_A, SW/2]  (둘 다 «몸에서 도출된» 값 · 손 상수 0)
 띠      B(x) = { 몸 정점 v : |x_v| ∈ [x − h, x + h] },  h = sdfSpec.h
         ★ 이 띠는 `garmentScene.ts:181` 이 `Y_TOP` 을 뽑을 때 쓴 **그 띠**다(폭·중심 규칙 동일).
 능선점  y(x) = max { y_v : v ∈ B(x) } · z(x) = 그 최대를 낸 정점의 z
 검산    x = SW/2 에서 y(x) 는 **`Y_TOP` 과 같아야 한다**(같은 식·같은 띠) ⟹ §1-① 에서 값으로 확인
 옷의 자리 ridge_cloth(x) = ( ±x, y(x) + SEP, z(x) )
         ★ 오프셋 방향을 «고르지» 않았다 — `SEP` 는 v3 가 등재한 유일한 간극 척도이고(배치 주석 `:189`)
           능선은 «최상단»이라 `+y` 가 그 점에서 몸에서 멀어지는 방향이다. **실제 여유는 측정해서 적는다**
           (`exactBodyDist` · ②의 관통 채널) — 「SEP 가 확보된다」고 **가정하지 않는다**.
 목선 토막 목선 정점은 **목 밑동 링**에 놓는다 —
         `arcOn( boundaryOf( supportAt(Y_NECK, SLAB), DELTA, 1 ), !front ).at(px)` · y = Y_NECK
         ★ `bodyPoint` 와 **같은 machinery**이고 다른 것은 ㉠ 높이(`Y_NECK`) ㉡ 배율 **1**(클램프 없음)뿐이다.
           `DELTA` 는 배치가 이미 «푼» 부풀림이다(새 수 0).
 능선 길이 LEN_RIDGE = ∫ |d ridge| (x 를 `h` 간격으로 훑어 폴리라인 호길이)
 봉제선 길이 SH_LEN = SW/2 − NECK_A (`:319` 원문) ⟹ **늘림 필요 여부 = LEN_RIDGE / SH_LEN**
```

### ㄴ ② 가상 배치 — **src/ 0줄** (계기가 `S` 의 내보낸 함수만 부른다)

```
 상단 행(front·back 의 j = nvB) 만 옮긴다 — 하위 행은 **건드리지 않는다**(판정문 「하위 행 배치 불변」).
   i ∈ [0, N_sh]              ⟹ x = SW/2 − (SW/2 − NECK_A)·(i/N_sh) 의 `ridge_cloth(x)` · **x 부호 −**
   i ∈ (N_sh, N_sh+N_nk)      ⟹ 목선 토막(위 식 · front/back 각각)
   i ∈ [N_sh+N_nk, nuB]       ⟹ 같은 식 · **x 부호 +**
   ★ 어깨 토막은 **front 와 back 에 «같은 점»**을 준다 ⟹ 봉제쌍 거리 **0**(판정문 「이미 닫힌 상태」).
 재는 것 — ㉠ 목선 링 둘레(닫힌 고리 · `s4Gate.ts:118-123` 정의 · 예측 ≈ `ringRest = 2·LEN_NECK`)
   ㉡ 몸 여유 = `sgn·exactBodyDist`(상단 행 전 정점) — **음수 = 관통** · 개수·최대 깊이·중앙
   ㉢ 봉제쌍 거리 최대(0 확인) ㉣ 상단 행 ↔ 하위 행 엣지 신장률 = |p_top − p_below| / |uv_top − uv_below|
     (분모는 **2D 패턴 거리** = 면내 rest · §0-2ㄹ) — 중앙·최대·최대 자리
   ㉤ 대조 = **현행 배치**의 같은 넷(같은 계기·같은 프레임 f0)
 ★ (가)의 「band 안」 = 몸 여유가 **`−SEP` 보다 크다**(= 관통 깊이가 `SEP` 미만) — v3 가 쓰는
   충돌 두께 척도 그대로다(새 문턱 0 · 관통 게이트 ③a 의 0.5mm 는 «정착» 문턱이라 f0 에 걸지 않는다).
   「신장 허용 자릿수」 = **현행 배치의 같은 채널과 같은 자릿수**(현행 값을 먼저 재서 기준으로 쓴다 ·
   결과에 맞춰 옮기지 않는다 · 함정 14).
```

### ㄷ ③ 진단 플래그 diff 초안과 **승인문**

```
 ㉠ `src/v3/garmentScene.ts` — `place()`(`:793-809`)에 **분기 하나**를 더한다.
    스위치 = `cfg.ridgeTop`(기본 `undefined`) ⟹ 켜지지 않으면 **줄 하나도 다르게 돌지 않는다.**
      if (RIDGETOP && (panel.name === 'front' || panel.name === 'back') && j === nvB) { …능선/목링… }
      else { 기존 `bodyPoint` 경로 그대로 }
 ㉡ `src/v3/dressRun.ts` — `prepare()` 가 `ridgeTop` 을 `createScene` 으로 **넘겨만** 준다(선택 인자).
 ㉢ `scripts/v4AsmExport.ts` — `RIDGETOP=1` env 를 읽어 `prepare` 에 넘기고 헤더에 `ridgeTop: true` 를 적는다.
 ★ **커널·물리 0줄** · rest·uv·위상·정점 수 **불변**(옮기는 것은 «초기 위치»뿐이다).
 **승인** — 회차 머리가 이 경로를 명시했다(「`src/` diff = garmentScene 어깨 배치 «진단 플래그 경로»」).
 집행 전에 **비트 불변 검사 2건**을 건다:
   ⓐ 플래그 off 재-export 의 **asm 본문 바이트 동일**(v5-8 §0-4 와 같은 검사)
   ⓑ 플래그 off 재-export 의 **scene·scene-bend·scene-seam payload sha 동일**(rest·위상 불변 증명)
 ★ ⓐⓑ 중 하나라도 어긋나면 **③ 착수 0 · 정지**(갈래 D).
```

## §0-5 사전 결정 5건 (값 보기 «전» · CC 집행 설계)

```
 ㄱ **진입 인자는 v5-7b §0-5ㄴ 확정분 그대로** — `BODY_BIN`/`ARM_AXIS_JSON`/`ARM_ORIGIN_JSON` =
    `gpu/oracle/export/grid27/l3ap-{body,body,origin}-c100-h170-s45-a35.{bin,json,json}` ·
    모든 계기 산출에 `_args` 기록(v5-7b §0-5ㄱ 규약).
 ㄴ **판정문의 「어깨 피벗 P → 팔축 AX 방향」은 «검산»으로만 쓴다** — 능선은 §0-4ㄱ 의 몸 표본 규칙이
    정본이고, 피벗·축(자산의 뼈 값)은 **능선점이 팔 쪽인지**(`s = (p − 피벗)·방향`)를 v5-9 §1-① 채널로
    되재는 데 쓴다. 두 규칙을 섞어 새 곡선을 만들지 않는다.
 ㄷ **③의 2칸은 supima-L(회귀) · mst-over-L(회복)** — v5-9 와 같은 칸·같은 몸·같은 축.
    예산 ≤ 3칸 중 2칸을 쓰고 나머지 1칸은 **남긴다**(변주는 이 판에서 하지 않는다).
 ㄹ **발산 판정은 계기가 던지는 것으로** — `degenerate > 0` · NaN · 굽기 예외 · 정착 미도달(400 상한).
    「나빠졌다」는 발산이 아니다(v5-7b §0-5ㅁ 과 같은 규칙).
 ㅁ **`git add` 는 «명시 경로만»** — 이 판부터 `git add -A` · `git add .` 를 쓰지 않는다(§0-6 사무).
```

## §0-6 §0 사무 1건 (집행 «전» 등재 · 집행은 선커밋 «뒤»)

```
 ★ **`CLAUDE.md` 에 「`git add -A` 금지 · 명시 경로만」 등재** — v5-9 사고 1·2 의 처분(판정문 지시).
   근거 = v5-9 에서 `git add -A` 가 «의도적 추적 밖» 14파일을 커밋에 담고, 그 정정에서 글롭이
   원래 추적되던 json 480개를 인덱스에서 뺐다(둘 다 CC 귀책 · `docs/v5/9-어깨봉제.md` §2 1·2).
```

---

## §1-⓪ 집행 인쇄 · 사무 1건

```
 브랜치  v5-10-ridge-placement · §0 선커밋 **1512687**(집행 «전») · 사무 ab8496c · 계기 = 이 커밋
 기계    ①②는 **에어(맥)** 에서 돈다 — 물리 0프레임 · GPU 0(오프스크린 raster 뿐) ·
         ③은 2호기 몫이었으나 **②(나) ⟹ 착수 0**(아래)
 사무    `CLAUDE.md` 에 「**`git add -A` · `git add .` 를 쓰지 않는다 — 명시 경로만**」 등재(7줄) ·
         근거 = v5-9 사고 1·2(추적 밖 14파일 커밋 · 정정 글롭이 추적 json 480개를 인덱스에서 뺐다)
```

## §1-① 능선 정의 — **사실** (판정 0)

계기 = 신설 `scripts/v5RidgeProbe.ts`(측정만 · **`src/` 0줄** · `S` 가 내보낸 함수·값만 부른다).

### ㉠ 형식화 보충 2건 (값을 만들기 «전»에 코드에서 드러났다)

```
 보충 1 — **정의역에 `y ≤ Y_NECK` 상한이 필요하다.** §0-4ㄱ 의 「띠 안 최상단」을 그대로 쓰면
   목 쪽(x = NECK_A ≈ 8.5 cm)에서 **머리 꼭대기**를 집는다(머리가 그 x 띠에 있다).
   `Y_NECK` 은 `neckBaseY()` 도출값이고, `x = SW/2` 에서는 `Y_TOP < Y_NECK` 이라 **검산이 유지된다**.
 보충 2 — **`supportAt` 은 `|x| > SW/2` 를 버린다**(`garmentScene.ts:485`) ⟹ 목 밑동 링을 그 함수로
   만들면 **어깨폭 안**만 본다. 이 판은 그것을 그대로 쓴다(배치가 쓰는 그 링이다 · 새 규칙 0).
```

### ㉡ 검산 — **통과**

| 칸 | `Y_TOP` m | `ridge(SW/2)` 몸 y m | **차 mm** | `Y_NECK` m | `NECK_A` cm | `h` mm | `DELTA` mm |
|---|---|---|---|---|---|---|---|
| mst-over-L(SW 52) | 1.3598263263702393 | 1.3598263263702393 | **0** | 1.4420861371069447 | 8.5031 | 3.6146 | 27.2392 |
| supima-L(SW 44.5) | 1.3934800624847412 | 1.3934800624847412 | **0** | 1.4420791 | 8.5057 | 3.6146 | 29.1777 |

⟹ 능선의 바깥 끝이 **`shoulderTopY()` 와 같은 값**이다(같은 식·같은 띠 · 차 0).

### ㉢ ★★★ **능선 규칙은 `y` 는 정하지만 `z` 를 «정하지 못한다»**

```
 어깨 위는 z 방향으로 «평지»라 `argmax y` 의 z 가 임의로 튄다. 값 —
   z 폭 **100.093 mm** · 인접 표본 z 차 **중앙 17.111 mm · 최대 74.625 mm**(mst · V1)
 ⟹ 폴리라인 호길이가 **126.3291 cm**(`SH_LEN` 17.4969 의 **7.2201배**)로 부푼다. 이것은 어깨선 길이가
    아니라 **잡음의 길이**다.
 ★ 그래서 z 를 저장소의 기존 척도로 정하는 **변종 V2** 를 함께 냈다 —
   고원 = { v ∈ B(x) : y_v ≥ y(x) − SEP } · z(x) = (min z + max z)/2
   (`SEP` 는 v3 가 등재한 **유일한 간극 척도** · 새 상수 0)
 ⟹ V2 도 부푼다 — 길이 **124.6811 cm**(비 **7.1259**) · 인접 z 차 중앙 **19.486** · 최대 **74.625 mm**.
   ⟹ **호길이는 이 규칙으로 «산출 불가»다**(추정치로 채우지 않는다 · CLAUDE.md 계기 규약).
```

| 칸 | 규칙 | 폴리라인 길이 cm | 능선/봉제선 | z 폭 mm | 인접 z차 중앙 mm | 최대 mm |
|---|---|---|---|---|---|---|
| mst-over-L | V1 argmax z | 126.3291 | **7.2201** | 100.093 | 17.111 | 74.625 |
| mst-over-L | V2 고원 z중앙 | 124.6811 | **7.1259** | 100.110 | 19.486 | 74.625 |
| supima-L | V1 | 97.1434 | **7.0679** | 100.093 | 17.176 | 67.369 |
| supima-L | V2 | 96.0978 | **6.9918** | 100.110 | 19.486 | 67.421 |

### ㉣ ★★★ 「늘림 필요 여부」는 **잘 정의된 하한**으로 답한다

```
 호길이는 못 내지만 **두 끝점의 직선 거리**는 잘 정의되고 **V1·V2 가 소수 넷째까지 같다**
 (끝점은 z 잡음의 영향을 거의 받지 않는다 — 아래 값).
   mst-over  목쪽 (8.503 cm, 1.43981, −0.02453) ↔ 어깨끝 (26.000 cm, 1.35983, −0.02668)
             직선 **19.2396 cm** ↔ `SH_LEN` **17.4969 cm** ⟹ **하한 비 1.09960**
   supima    목쪽 (8.506 cm, 1.43981, −0.02453) ↔ 어깨끝 (22.250 cm, 1.39348, −0.02368)
             직선 **14.5044 cm** ↔ `SH_LEN` **13.7443 cm** ⟹ **하한 비 1.05531**
 ★★ **초과분의 정체가 «수직 상승»이다.** `Δx` 가 `SH_LEN` 과 **정확히 같다**(정의상
   `SH_LEN = SW/2 − NECK_A` · `:319`) ⟹ 직선 거리가 `SH_LEN` 을 넘는 양은 **전부**
   `Y_NECK − Y_TOP` = **82.2598 mm**(mst) / **48.5991 mm**(supima) 에서 온다.
   즉 **평면 패턴의 어깨 이음선은 «수평 직선»인데 몸의 능선은 목 쪽으로 그만큼 «올라간다»**.
 ⟹ 「늘림 필요 여부」 = **필요하다 · 최소 9.96% / 5.53%**(하한 · 실제 호길이는 더 길다).
   이것은 **SW 의 함수**다(mst 82.26 mm ↔ supima 48.60 mm — SW 가 커질수록 상승이 크다).
 ★ 팔축 되재기(§0-5ㄴ) — 능선점의 `s` 는 목쪽 **−114.49 mm** 에서 어깨끝 **+74.98 mm** 까지 «단조»로
   올라간다(mst · `r` 8.57 → 32.87 mm) ⟹ 능선의 바깥 3분의 1이 **피벗보다 팔 쪽**이다(v5-9 ①과 정합).
```

## §1-② 가상 실험 (f0 · 코드 0줄) — **갈래 (나) 초과** ⟹ **③ 착수 0**

사전 등재 ②의 정의 그대로 — **상단 행만** 옮긴다(어깨 토막 = 능선 · 목선 토막 = 목 밑동 링 ·
앞뒤 어깨는 **같은 점** ⟹ 봉제쌍 거리 0) · **하위 행 접촉 0**.

| 채널 | 현행 배치(f0) | **능선 V1 · 상단 행 전부** | **능선 V2 · 상단 행 전부** | [보조] V2 · 어깨 토막만 |
|---|---|---|---|---|
| **mst-over-L** | | | | |
| 목선 링 cm | 97.2594 | **59.2017** | **59.2017** | 104.9603 |
| 링/`ringRest`(47.0421) | 2.0675 | **1.2585** | **1.2585** | 2.2312 |
| 상단 행 정점 | 130 | 130 | 130 | 130 |
| **여유 음수 개수** | **0** | **48** | **48** | 48 |
| 여유 최소 mm | +22.2874 | **−11.0409** | **−11.3424** | −11.3424 |
| **관통 깊이 최대 mm** | **0** | **11.0409** | **11.3424** | 11.3424 |
| 어깨 봉제쌍 거리 최대 / 중앙 mm | 243.9596 / 194.3379 | **0 / 0** | **0 / 0** | 0 / 0 |
| **하위행 엣지 신장 중앙** | **1.0587** | **14.5015** | **14.2375** | 9.8715 |
| 하위행 엣지 신장 최대 | 1.3156 | **24.1373** | **24.1373** | 22.5132 |
| 신장 > 2 인 정점 | **0** | **130 / 130** | **130 / 130** | 80 / 130 |
| 최대 자리 | 어깨R i=64 | 목선 i=32 | 목선 i=32 | 어깨L i=17 |
| **supima-L** | | | | |
| 목선 링 cm | 91.7762 | **60.0925** | **60.0925** | 93.5453 |
| 링/`ringRest`(47.0498) | 1.9506 | **1.2772** | **1.2772** | 1.9882 |
| 상단 행 정점 | 114 | 114 | 114 | 114 |
| **여유 음수 개수** | **0** | **40** | **36** | 36 |
| 여유 최소 mm | +21.9849 | **−6.5356** | **−7.4239** | −7.4239 |
| **관통 깊이 최대 mm** | **0** | **6.5356** | **7.4239** | 7.4239 |
| 어깨 봉제쌍 거리 최대 / 중앙 mm | 211.1720 / 183.0758 | **0 / 0** | **0 / 0** | 0 / 0 |
| **하위행 엣지 신장 중앙** | **1.0663** | **11.8215** | **11.8878** | 7.9605 |
| 하위행 엣지 신장 최대 | 1.3561 | **19.8120** | **19.8120** | 19.2582 |
| 신장 > 2 인 정점 | **0** | **114 / 114** | **114 / 114** | 64 / 114 |
| 최대 자리 | 목선 i=16 | 목선 i=28 | 목선 i=28 | 어깨R i=43 |

### ★★★ ②의 답 — **(나) 초과** (두 채널 · 두 규칙 · 두 옷에서 일관)

```
 §0-4ㄴ 이 값 보기 «전»에 적은 (가)의 두 조건 —
   ㉠ 「관통 band 안」 = 몸 여유가 **−SEP(−2 mm) 보다 크다**
      실측 관통 깊이 최대 **11.0409 / 11.3424 mm**(mst) · **6.5356 / 7.4239 mm**(supima)
      ⟹ **SEP 의 3.3~5.7배** · 음수 정점 **48/130 · 36~40/114** ⟹ **초과**
   ㉡ 「신장 허용 자릿수」 = **현행 배치의 같은 채널과 같은 자릿수**
      현행 중앙 **1.0587 / 1.0663** · 최대 **1.3156 / 1.3561**
      능선 중앙 **14.2375~14.5015 / 11.8215~11.8878** · 최대 **24.1373 / 19.8120**
      ⟹ **자릿수가 하나 다르다**(중앙 13.4~13.7배 · 최대 14.6~18.3배) · 신장 > 2 인 정점이 **전부**
      ⟹ **초과**
 ⟹ **갈래 (나)** · **③ 착수 0**(사전 등재 그대로 · 굽기 0칸).

 ★ **예측 대조** — §0-1 판정문이 등재한 넷 중
   ① 「f0 목선 링 ≈ rest 자릿수」 = **부분 성립**. 링/rest 가 **2.0675 → 1.2585**(mst) ·
     **1.9506 → 1.2772**(supima) 로 내려간다. 「≈ rest」(1.0)에는 **못 닿는다**(1.26~1.28).
   ② 「끌어올림 불필요」 = **어깨 봉제쌍 거리 0 으로 성립**(최대 0.0000 mm) — 어깨는 **이미 닫혀서** 시작한다.
   ③④ (SW 52 회복 · supima pass 유지) = **미측정**(③ 착수 0).
 ★★ **왜 초과인가 — 값이 원인을 가리킨다.** 상단 행이 `Y_TOP` 에서 `Y_NECK` 으로 **82.26 / 48.60 mm**
   올라가는데 하위 행은 **그 자리에 있다**. 행 간격은 `L / nvB` = 74 cm / 83 ≈ **8.9 mm** 이고
   면내 rest 는 **2D 패턴 거리**(§0-2ㄹ)라 변하지 않는다 ⟹ 그 한 엣지가 8.9 mm → 80~90 mm,
   즉 **10배 안팎**으로 늘어난다. 최대 자리가 **목선 중앙**(i=32 / i=28)인 것이 그 증거다.
 ★ [보조] **어깨 토막만** 옮기면(목선 불변) 신장 중앙이 **14.24 → 9.87**(mst) · **11.89 → 7.96**
   (supima)로 내려가지만 **여전히 자릿수가 다르고**(>2 인 정점 80/130 · 64/114) 링은 2배로 돌아간다
   (2.2312 / 1.9882) ⟹ **둘 다 산다**(이 열은 **갈래 판정에 쓰지 않았다** — 사전 등재 ②는 「상단 행만」 =
   어깨+목선 전부다 · 다음 판 설계용 사실이다).
 ★ 「하위 행 배치 불변(국소)」이라는 판정문의 전제가 **이 실험에서 값으로 깨진다** —
   상단 행만 옮기는 처방은 **국소일 수 없다**(바로 아래 한 행이 10배 늘어난다).
```

## §1-③ 진단 굽기 — **착수 0** (사전 등재대로)

```
 §0-3 이 ②(나)에 「**③ 착수 0**」을 걸었고 ②가 (나)로 나왔다 ⟹ **굽기 0칸 · 플래그 미구현**.
 §0-4ㄷ 의 diff 초안(`place()` 분기 · `prepare` 선택 인자 · `RIDGETOP` env)은 **초안 그대로 남긴다** —
 코드에 넣지 않았다(사후 확대 0). 2호기 접촉 0 · 예산 3칸 **전부 미사용**.
```

## §1-④ 회귀 «영향 범위» 사실 (판단 0)

```
 ㉠ **T포즈 108 조립 sha 영향 — «측정 불가»(미집행)** · 사유 = ③ 착수 0 ⟹ 플래그를 코드에 넣지 않았다.
    추정치로 채우지 않는다. 초안의 형태상 «기대»는 0 이다(분기 조건이 `cfg.ridgeTop` 이고 기본값이
    `undefined` 이며 분기 자리가 `j === nvB` 하나다) — 그러나 **그 기대를 값으로 확인하지 않았다.**
    확인 방법은 등재해 둔다 — ⓐ 플래그 off 재-export asm 본문 바이트 대조 ·
    ⓑ `scene`·`scene-bend`·`scene-seam` payload sha 대조(v5-9 §1-⓪ 에서 쓴 그 계기 `payload.py`).
 ㉡ **A포즈 제공 29칸 재굽기 — 필요하다.** 조립 위치가 바뀌면 정착 상태가 바뀌므로 제공 목록의
    근거(게이트 pass + 어깨위 정점 ≥ 172)가 **전부 다시 계산돼야 한다**(v4-52 §1-② 편입 규약).
    **칸당 실측 기준**(이 캠페인에서 «측정된» 벽시계 · 기억·역산 0) —
      v5-8 `mstoverNR_XL` **989.1 s** · `supimaNR_L` **550.1 s** ·
      v5-9 `mstoverRO_XL` **799.1 s** · `supimaRO_L` **697.2 s**  ⟹ 4칸 평균 **758.875 s**
    ⟹ 29칸 직렬 = **29 × 758.875 = 22,007.4 s ≈ 6.11 h**(칸마다 자식 프로세스 · v4-17 §1-① 규약).
    ★ **한정** — 이 네 칸은 **v5 실측표 옷**(정점 12,144 / 10,356)이고 제공 29칸은 **차트 사이즈 옷**이다
      ⟹ 위 값은 **자릿수 기준**이지 29칸의 실측이 아니다. 29칸 각각의 실측은 이 판이 재지 않았다.
    ★ 재굽기 전에 **조립 export 29회**(칸당 `v4AsmExport.ts`)와 **층3 계기 29회**가 붙는다(이 판 미측정).
 ㉢ **「정본 승격」 결정에 필요한 항목 목록**(사실 · 우선순위 0):
    1 플래그 off 비트 불변 ⓐⓑ 실측(㉠)
    2 ②의 두 초과를 **없애는** 배치 설계 — 「상단 행만」이 아니라 **하위 행까지 다시 놓는** 판
      (판정문의 「앞뒤 패널은 거기서 아래로」가 그 뜻이면 ②의 정의역이 좁았다 · 사실로 적는다)
    3 평면 패턴의 어깨 이음선에 **수직 상승 9.96% / 5.53%(하한)** 을 넣을지 — 제도 변경이면
      `SH_LEN`·`topB`(`garmentScene.ts:319`·`:416-419`)가 바뀌고 **패턴 자체가 바뀐다**
    4 v3 **동결 예외** 등재(배치는 v3 정본 코드다)
    5 108칸 계정 재굽기 예산·순서(㉡) + v3 정답지 대조 기준 재수립
    6 제공 29칸 목록이 **바뀌는지**(편입 규약은 그대로 두고 값만 다시 계산)
    7 어깨 봉제 `rest0` 이 0 이 되면 `RAMP_N` 을 **다른 봉제가 정한다**(§0-2ㅁ · 89 → ≈52 · 예산 변동)
```
