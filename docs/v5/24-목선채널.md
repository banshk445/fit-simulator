# v5-24 — 목선 채널 자 교체(휴지 기준 · 기존 상수) · 옛 산출 재판정 · S 이산화 수정 · 회귀 3칸 굽기

> 기점 `de771be`(v5-23 HEAD) · 브랜치 `v5-24-neck-channel`
> 물리 0줄 · `src/` diff = **`s4Gate` 목선 채널 정의**(정본 경로 포함) + **`N_sh` 산식 한 줄** · ecc 0

```
 ★ 집행 «전» 사실 두 개(값 보기 전 등재)
   ㉠ **정본 정착 blob 39칸**이 로컬에 있다(`public/v3diag/v3-77/settled-*.bin`) — CLAUDE.md 가 등재한
      「정점 대조 **39칸**(blob 존재) + 판정 대조 **69칸**(blob 부재)」의 앞 39 다 ⟹ ②의 재판정 정의역은
      **blob 이 있는 39칸**이고 나머지 69칸은 **blob 이 없어 원리적으로 재판정 불가**다(자산 문제가 아니다).
   ㉡ **2호기가 도달 불가**다 — `ssh: connect to host desktop-ov72a46 port 22: Operation timed out`
      (v5-23 도중부터 계속). ④ 굽기 3칸은 **이 기계 없이는 집행할 수 없다**.
   ㉢ `git` 은 Xcode 라이선스 미동의로 멈춰 있어 `DEVELOPER_DIR=/Library/Developer/CommandLineTools` 로
      우회한다(v5-23 사무 그대로 · 기계 설정 변경 0).
```

---

## §0 선커밋 — 집행 «전» 등재

### §0-0 회차 프롬프트 원문

```
v5-24 — 목선 채널 자 교체(휴지 기준 · 기존 상수) · 옛 산출 재판정 · S 이산화 수정 · 회귀 3칸 굽기

집행 기계 = 2호기 · 브랜치 v5-24-neck-channel · 기점 = v5-23 HEAD de771be · 물리 0줄 ·
src/ diff = s4Gate 목선 채널 정의(정본 경로 포함 — 자 교체이므로 off 게이트 «결과»가 바뀔 수 있음 ·
조립 sha 는 불변) + asm1x N_sh 산식 한 줄 · 굽기 3칸 · ecc 0 · §0 선커밋 · 브랜치 인쇄 ·
보고 = docs/v5/보고/24.md · 캡처 3칸 · 콘솔 4줄
```

### §0-1 판정문(전략 세션 · v5-23 §4 수령 · 원문)

```
「갈래 A 수용 · ★ 목선 채널 = 이 상황을 못 재는 자: 코드 허용값(최근접점 다각형)이 v3-40 정의(링 높이 몸 둘레)와
 다르고 · 옷깃이 뜨면 허용이 붕괴 · ±5mm 에 판정 뒤집힘 = v4-15 원리(계가 답 못 하는 질문 → 폐기) ·
 on 이 덜 늘어났는데 떨어짐 · 옛 자는 «옷깃이 흘러내려 앉는» 옛 체제의 자 ·
 처분 = 자 교체: 휴지 대비 신장률 ≤ 기존 상수((ㄱ)-2 · 새 수 0 · 새 식 0) ·
 함정 14 아님(독립 근거 = 뒤집힘 · 측정 대상 정정 · v4-37·v5-16 계열) ·
 검증 = 옛 산출 T108·A29 재판정으로 편입 변동 사실 · S 던짐 = SH_DROP 이산화(dEdge 22 > N_sh 17) → N_sh 산식 한 줄」
```

### §0-2 §1 집행 5항 (원문)

```
① 자 교체 구현 — s4Gate 목선 채널: 휴지 기준 신장률(정의·상수 출처 등재 · diff 원문) ·
   옛 채널은 «보고 값»으로 병기(판정에서 내림 · 이력).
② 옛 산출 재판정(굽기 0) — T108·A29 정본 blob 에 새 자 → pass/fail 변동 표(칸 · 옛 R · 새 비율) ·
   편입 집합 변동(29 → ?) · «자만 바뀌었으니 조립·blob sha 불변» 확인.
③ N_sh 수정(asm1x) — 작은몸 S 조립 자기검사 통과 · f0 채널.
④ 회귀 3칸 굽기(on) — supima-L · 기본 M · 작은몸 S: 게이트(새 자) · 어깨 걸림 · 층3 · 링 휴지비 · 캡처.
⑤ 승격 재료 — 108 재굽기 계획 · 제공 재발행 · v3 동결 예외 등재 항목(자 교체 포함).
```

### §0-3 §4 사전 등재 갈래 (원문 · 사후 갈래 신설 0)

```
A = ④ 3칸 pass ⟹ 상신(→ 108 발주)
B = fail(칸·채널·값) ⟹ 상신
C = ② 에서 옛 편입이 크게 흔들림(사실) ⟹ 값 상신 · 처분 전략 세션
D = 조립 sha 변동 ⟹ 즉시 정지
E = 그 외 ⟹ 정지
```

---

### §0-4 코드 사실 — 값 보기 «전» 인용

**ㄱ. 현행 목선 채널 전량**(`src/v3/s4Gate.ts`)

```ts
/** v3 누적 등재 문턱 — **이 파일이 새로 정하는 수는 0이다.** */
export const S4_THRESHOLD = {
  penMaxM: 5e-4, crossings: 0,
  /** v3-40 §1 — 목선 «초과비» R = C_ring / C_allow ≤ **1**.
   * C_allow = C_body + 2π(THICK + TOL_SELF) · C_body = 링을 몸에 정사영한 닫힌 둘레.
   * **1 은 고른 수가 아니라 «정의»다** — 어림수 1.10(v3-24)을 교체했다.
   * 구 채널(휴지 대비 비율)은 삭제하지 않고 `ringRestRatio` 로 **병기**한다(판정 아님). */
  ringExcess: 1,
  settleNetM: TOL_SELF, pinned: 0,
} as const;
…
const ringRestRatio = (polyLen(P.neckF) + polyLen(P.neckB)) / P.ringRest;
…
if (!(ringExcess <= S4_THRESHOLD.ringExcess))
  fails.push(`목선 초과비 ${ringExcess.toFixed(4)} > 1 (링 … cm > 허용 … cm)`);
```

```
 ★ **교체할 자와 문턱이 둘 다 «이미 이 파일 안에» 있다** — 자 = `ringRestRatio`(계산은 이미 매 판 돌고 있고
   `S4Result` 에도 실린다) · 문턱 = 주석이 인용한 **어림수 1.10(v3-24)**.
   ⟹ **새 수 0 · 새 식 0**(판정문 「(ㄱ)-2 · 새 수 0 · 새 식 0」 그대로).
 ★ `ringRest = 2 · S.LEN_NECK`(`dressRun.ts:161`) — 휴지 둘레는 **패턴 목선 길이의 두 배**다(앞·뒤).
```

**ㄴ. `N_sh` 산식**(`garmentScene.ts:474`)

```ts
const N_sh = Math.max(1, Math.round(Math.hypot(SH_LEN, SH_DROP) / d));
```

```
 ★ v5-23 §1-③ 실측 — S(on) 던짐의 자리는 `j = nvB · nvB−1` · `i` 22~24 이고 `dEdge` **22 > N_sh 17** ⟹
   **어깨 토막이 아니라 «목선 토막»** 이다. 판정문은 그 원인을 「`SH_DROP` 이산화」로 읽고 **`N_sh` 산식 한 줄**을 지시했다.
 ★ **판정문이 «식»을 주지 않았다** ⟹ CC 가 **등재된 양만으로** 후보를 형식화하고 **값으로 고른다**(§0-5ㄷ).
```

---

### §0-5 형식화 — 값 보기 «전» 등재(사후 신설 0)

**ㄱ. ① 자 교체의 형식**

```
 새 판정 = **`ringRestRatio ≤ 1.10`**(자·문턱 둘 다 위 ㄱ 에서 인용).
 옛 판정 `ringExcess ≤ 1` 은 **판정에서 내리고 «보고 값»으로 병기**한다 — `S4_THRESHOLD.ringExcess` 는
 **지우지 않고** 「판정에서 내림(v5-24)」 주석을 달아 남긴다(v3 동결 관행 · 삭제 0).
 `fails` 문언도 새 자로 바꾼다: `목선 휴지비 <값> > 1.10 (링 …cm / 휴지 …cm)`.
 ★ **정본 경로를 건드린다** — `asm2`/`asm1x` 플래그 뒤가 아니다 ⟹ **off 게이트의 «결과»가 바뀔 수 있다**
   (프롬프트가 그렇게 등재했다). 바뀌지 «않아야» 하는 것은 **조립 sha** 뿐이다(갈래 D 의 자).
```

**ㄴ. ② 재판정의 정의역과 방법**

```
 정의역 = **정본 정착 blob 이 있는 39칸**(위 집행 전 사실 ㉠). 나머지 69칸은 **blob 이 없어 재판정 불가**다
 (게이트는 위치가 있어야 돈다) ⟹ 「T108 전량 재판정」은 **원리적으로 39칸까지**다. 그 사실을 표에 적는다.
 방법 = 각 칸에서 `prepare()`(off · 그 칸의 몸)로 장면을 세우고 정본 blob 을 얹어 **`runS4Gate` 를 그대로** 부른다 ·
 옛 `ringExcess` 와 새 `ringRestRatio` 를 **나란히** 적고 pass/fail 변동을 센다.
 ★ **A포즈 29칸**은 정본 정착 blob 이 `v3-77`(T포즈) 계열뿐이라 **이 판에서 재판정 불가**다 — 미집행으로 신고한다.
 ★ **「조립·blob sha 불변」 확인** = `scripts/v5AsmSha.ts` 로 T108·A29 표를 떠 기점 `de771be` 와 **줄 단위 대조**.
   자 교체는 조립을 건드리지 않으므로 **동일해야 한다**. 다르면 **갈래 D(즉시 정지)**.
```

**ㄷ. ③ `N_sh` 후보와 선택 규칙 — 값 보기 «전» 고정**

```
 세 후보 모두 **`SH_DROP = 0`(off)이면 현행 식과 «항등»** 이다 ⟹ **off 비트 불변이 구조적으로 보장**된다
 (그래도 값으로 확인한다).
   ㉠ 높이 걸음 ≤ `SEP`    : `N_sh = max(round(hypot(SH_LEN,SH_DROP)/d), ceil(SH_DROP/SEP))`
   ㉡ 높이 걸음 ≤ `THICK`  : `… , ceil(SH_DROP/THICK))`      … v5-15 (A)「걸음 상한 THICK」선례
   ㉢ 높이 걸음 ≤ `d`      : `… , ceil(SH_DROP/d))`           … 가장 약한 상한(대조군)
 ★ 셋 다 **새 수 0** — `SEP`·`THICK`·`d` 는 등재된 양이다.
 선택 규칙 = 「**세 칸(supima-L · 기본 M · 작은몸 S) 조립 자기검사가 전부 통과**하는 후보 중
   **정점 수 증가가 가장 작은 것**」. 동률이면 ㉢ → ㉠ → ㉡ 순(상한이 느슨한 쪽 우선).
   통과하는 후보가 **없으면 채택 0 · 사실만 상신**(처방 0).
```

**ㄹ. ④ 굽기의 전제**

```
 2호기 도달이 회복되어야 집행할 수 있다. 회복되지 않으면 ④는 **미집행**이고, ①②③⑤의 값만으로 상신한다.
 그 경우의 갈래는 **A 도 B 도 아니다**(둘 다 ④의 결과를 요구한다) ⟹ 사전 등재 갈래 중 **E(그 외 ⟹ 정지)** 이거나,
 ②가 크게 흔들리면 **C** 다. **값을 본 뒤에 갈래를 만들지 않는다.**
```

### §0-6 §0 사무

```
★ 진입 인자 = v5-22·v5-23 과 같은 것(칸마다 그 칸의 몸 · `D_MM` 9).
★ CC 귀책 `C3` 처치 이행 — 새 계기는 **기준선 한 칸을 먼저 재현**한다.
  이 판의 기준선 = v5-22 게이트의 `목선 초과비` **1.0021454460806467**(on supima-L 정착) ·
  v5-23 이 낸 `ringRestRatio` **1.042797**(같은 판본).
★ 계기 산출 파일명 접두 = `v5-24-`.
★ `src/` 수정은 **두 자리뿐**이다 — `s4Gate.ts` 목선 채널 · `garmentScene.ts:474` `N_sh` 한 줄.
  회차 끝에 `git diff de771be -- src/` 를 인쇄해 그 둘만 바뀐 것을 보인다.
★ 이 판에서 호출한 ecc 에이전트 = 0 · 스킬 = 0 (집행 전 등재 · 종료 시 재확인)
```

---

## §1 집행

### §1-① 자 교체 — **구현 완료** (`src/` diff = `s4Gate.ts` **하나뿐**)

**diff 원문**(요지 · `git diff de771be -- src/` = `s4Gate.ts` 26+/8−, 다른 파일 0):

```ts
// 문턱 — 옛 것은 «지우지 않고» 판정에서만 내렸다
   ringExcess: 1,                    // ★ v5-24 — 판정에서 «내림»(보고 값으로 병기 · 삭제 0)
+  ringRestMax: 1.1,                 // ★ v5-24 — 새 판정 채널(휴지 대비 신장률)

// 판정
-  if (!(ringExcess <= S4_THRESHOLD.ringExcess))
-    fails.push(`목선 초과비 ${ringExcess.toFixed(4)} > 1 (링 …cm > 허용 …cm)`);
+  if (!(ringRestRatio <= S4_THRESHOLD.ringRestMax))
+    fails.push(`목선 휴지비 ${ringRestRatio.toFixed(4)} > ${S4_THRESHOLD.ringRestMax} `
+      + `(링 …cm / 휴지 …cm · 옛 초과비 …)`);

// 타입 주석 — 두 채널의 «역할»을 맞바꿨다(값은 둘 다 계속 산출·인쇄)
-  /** 구 채널 · 참고(판정 아님 · 옛 문턱 1.10) */ ringRestRatio: number;
+  /** ★ v5-24 — **판정 채널**(휴지 대비 신장률 · 문턱 `ringRestMax` 1.10). v3-40~v5-23 은 참고였다. */
```

```
 ★ **새 수 0 · 새 식 0** — 자 `ringRestRatio = (앞+뒤 목선 폴리라인) / ringRest` 는 이 파일이 **이미 매 판 계산**해
   `S4Result` 로 내놓던 값이고, 문턱 **1.10** 은 `ringExcess` 주석이 인용한 **v3-24 어림수** 그대로다.
 ★ 사유를 **코드 주석에 값으로** 적었다(사영 다각형 붕괴 · ±5 mm 판정 뒤집힘 1.002145→0.999151 ·
   1.021083→0.991968 · 덜 늘어난 쪽이 떨어짐 1.0377/1.0428 ↔ 1.0830) ⟹ **측정 대상 정정**이고
   결과에 맞춘 문턱 이동이 아니다(판정문 「함정 14 아님」 그대로).
 ★ **기준선 재현** — `on supima-L` 정착에서 옛 자 **1.0021454460806467**(v5-22 게이트와 비트 일치) ·
   새 자 **1.0427966483091002**(v5-23 값) · 새 문턱으로 **통과**.
```

### §1-② 옛 산출 재판정 — ★★★ **편입이 크게 흔들린다**

계기 `scripts/v5Rejudge.ts`(신설) — 정본 blob 을 얹고 **`runS4Gate` 를 그대로** 부른다(게이트 식 재작성 0).

| 항목 | 값 |
|---|---|
| 재판정한 칸 | **37** |
| 건너뛴 칸 | **71** — 정본 정착 blob 없음 **69**(판정 대조 69칸 · 원리적 불가) + **blob n ≠ 장면 n 2칸** |
| **옛 pass** | **37 / 37**(옛 자로는 **한 칸도 떨어지지 않는다**) |
| **새 pass** | **26 / 37** |
| **변동** | **11칸 · 전부 `pass → fail`** (`fail → pass` **0**) |
| 옛 초과비 분포 | 최소 **0.987804** · 중앙 **0.989700** · **최대 0.998283** |
| 새 휴지비 분포 | 최소 **1.059141** · p25 **1.074616** · 중앙 **1.088685** · p75 **1.105991** · 최대 **1.173295** |

**pass → fail 11칸**(칸 · 옛 초과비 → 새 휴지비 · 링 cm / 휴지 cm):

| 칸 | 옛 초과비 | **새 휴지비** | 링 cm | 휴지 cm |
|---|---|---|---|---|
| `c100-h170-s45_XL` | 0.996790 | **1.100619** | 53.16 | 47.53 |
| `c100-h185-s45_S` | 0.989338 | **1.119709** | 58.89 | 51.63 |
| `c100-h185-s45_XL` | 0.989032 | **1.106258** | 58.11 | 51.64 |
| `c100-h185-s50_XL` | 0.989823 | **1.124213** | 59.04 | 51.56 |
| `c122.5-h155-s40_S` | 0.989789 | **1.115648** | 50.89 | 44.80 |
| `c122.5-h155-s40_L` | 0.989420 | **1.105991** | 50.39 | 44.79 |
| `c122.5-h155-s45_L` | 0.988046 | **1.155725** | 47.40 | 40.14 |
| `c122.5-h155-s50_XL` | 0.988854 | **1.172694** | 48.16 | 40.14 |
| `c122.5-h170-s40_XL` | 0.989700 | **1.173295** | 52.66 | 43.91 |
| `c122.5-h170-s50_L` | 0.991017 | **1.168417** | 58.52 | 49.01 |
| `c122.5-h170-s50_XL` | 0.990376 | **1.161716** | 58.13 | 49.01 |

```
 ★★★ **옛 자는 이 37칸에서 판별력이 «0» 이다** — 초과비 최대가 **0.998283** 으로 문턱 1 에 한 칸도 닿지 않는다.
   ⟹ 정본 체제에서 그 채널은 **아무도 거르지 않고 있었다**(v5-23 §1-④ 의 43칸 재판정과 같은 방향:
   목선 «단독» 실패 칸 0).
 ★★★ **새 자는 11칸을 거른다**(29.7%). 휴지비 중앙이 **1.088685** 라 문턱 **1.10** 이 분포의 **p75 근처**를 자른다 ⟹
   판별력은 생겼으나 **편입 집합이 크게 흔들린다**.
 ★ **흔들리는 칸의 성격** — `c122.5`(가슴 122.5)가 **6칸** · `c100-h185`(키 185)가 **3칸** ·
   `XL` 사이즈가 **6칸** ⟹ **큰 몸 · 큰 사이즈**에 몰린다. 휴지(패턴 목선)가 작은데 링이 큰 조합이다.
 ★ **건너뛴 2칸의 사실** — `blob n 9888 ≠ 장면 n 9948` · `blob n 11118 ≠ 장면 n 10846` ⟹
   정본 blob 과 현재 제도가 **정점 수부터 다르다**(옛 판본에서 만든 blob). 이 판은 그 둘을 재판정하지 않았다.
```

### §1-③ `N_sh` 수정 — ★ **통과하는 후보가 «없다» · 채택 0**(§0-5ㄷ 규칙 그대로)

세 후보 모두 `SH_DROP = 0` 이면 현행 식과 항등이다(off 불변은 구조적 보장).

| 후보 | 식 | `supima-L` n / 최소쌍 mm | `기본 M` n / 최소쌍 | `작은몸 S` n / 최소쌍 |
|---|---|---|---|---|
| **현행**(대조) | `max(1, round(hypot/d))` | 10150 / **2.0 통과** | 9280 / **2.0 통과** | 8148 / **1.39419 던짐** |
| ㉢ 높이 걸음 ≤ `d` | `…, ceil(SH_DROP/d)` | 10150 / **2.0 통과** | 9280 / **2.0 통과** | 8148 / **1.39419 던짐** |
| ㉠ 높이 걸음 ≤ `SEP` | `…, ceil(SH_DROP/SEP)` | 14590 / **1.95733 던짐** | 13760 / **1.89265 던짐** | 12168 / **1.21605 던짐** |
| ㉡ 높이 걸음 ≤ `THICK` | `…, ceil(SH_DROP/cfg.THICK)` | — (미실행) | 22720 / **1.15383 던짐** | 20744 / **0.81733 던짐** |

```
 ★ ㉢ 는 **현행과 항등**이다(`ceil(SH_DROP/d)` = ceil(63.5/9) = 8 ≤ `round(hypot/d)` = 17) ⟹ 아무것도 바뀌지 않는다.
 ★★★ ㉠·㉡ 은 **세 칸을 «다» 깨뜨린다** — 통과하던 `supima-L`·`기본 M` 까지 던지게 만든다.
   ★ **단조다** — 열을 촘촘히 할수록 `back↔back` 최소쌍이 내려간다:
     `S` 1.39419(N_sh 17) → **1.21605**(㉠) → **0.81733**(㉡) · `M` 2.0 → **1.89265** → **1.15383**.
   ⟹ **기전** — `N_sh` 를 키우면 어깨 토막의 **열 간격이 좁아지고**, 자기검사는 그 좁아진 이웃 열을
     «붙음»으로 읽는다. 열 간격이 `d` = 9 mm 일 때 있던 여유가 사라진다.
 ⟹ ★ **판정문의 처방 방향(「`SH_DROP` 이산화 → `N_sh` 산식 한 줄」)은 이 세 후보 아래서 «반증»된다.**
   세 칸을 다 통과시키는 후보가 없으므로 **채택 0** 이고 `garmentScene.ts` 는 **기점과 바이트 동일**로 되돌렸다
   (`git diff de771be -- src/v3/garmentScene.ts` = 0줄).
 ★ **한정** — ㉡ 의 `supima-L` 은 돌리지 않았다(㉠ 에서 이미 던졌고 ㉡ 은 더 촘촘하다 · 방향이 단조로 확인됐다).
 ★ **처방 0** — 다른 방향(예: `N_sh` 를 «줄이는» 쪽)은 **등재한 후보가 아니므로 시험하지 않았다**(사후 신설 0).
```

### §1-④ 회귀 3칸 굽기 — **집행 불가**(미집행)

```
 2호기가 판 내내 도달 불가다 — `ssh: connect to host desktop-ov72a46 port 22: Operation timed out` ·
 `ping desktop-ov72a46.tail2666a5.ts.net` → **2 패킷 전송 · 0 수신 · 100% 손실**.
 ⟹ ④의 게이트(새 자)·어깨 걸림·층3·링 휴지비·**캡처 3칸**은 **전부 미집행**이다.
 ★ §0-5ㄹ 에 값 보기 «전» 적어 둔 그대로다 — 이 경우 갈래는 **A 도 B 도 아니다**.
```

### §1-⑤ 승격 재료 — **판단 0**(자 교체가 더해진 판)

```
 ㄱ **108 재굽기 계획** — v5-22 §1-④ 의 것을 그대로 두되 **한 항목이 늘었다**:
    자 교체는 **정본 경로**를 바꾸므로 «재굽기» 없이도 **판정이 바뀐다**(이 판 ②가 값으로 보였다: 37 → 26).
    ⟹ 순서가 바뀐다 — **① 자 교체를 먼저 확정하고 ② 그 자로 108 을 재판정한 뒤 ③ 재굽기**가 맞다.
    ★ 다만 **69칸은 blob 이 없어 재판정이 불가**하므로, 그 69칸은 **재굽기로만** 판정할 수 있다.
 ㄴ **제공 목록 재발행** — v5-11 §④ 사슬 그대로. 이 판의 값이 더하는 것: 재발행 대상이 **편입 11칸 감소**를
    반영해야 한다(정점 대조 39칸 중 재판정 가능한 37칸 기준 · 실제 제공 35칸과의 관계는 이 판이 확인하지 않았다).
 ㄷ **v3 동결 예외 등재 항목** — v5-22 가 적은 **4건째**(앵커·`SH_DROP`)에 **5건째**가 붙는다:
    **목선 판정 채널 교체**(`ringExcess` → `ringRestRatio`). 등재 문언에 들어갈 셋 —
      ① 무엇을 바꾸나 = `s4Gate.ts` 판정 한 줄 + 문턱 한 줄(값은 둘 다 계속 인쇄) ·
      ② 왜 v3 인가 = 게이트가 v3 정본 코드이고 플래그 뒤에 둘 수 없다(**모든 굽기의 판정**이다) ·
      ③ 되돌림 = 판정 줄을 옛 자로 되쓰는 **한 줄**(옛 문턱은 지우지 않았다).
 ★ **비용 사실** — 이 판이 잰 것: 재판정은 **굽기 0** 으로 37칸 · 계기 한 번에 끝났다.
   재굽기 시간은 이 판이 재지 않았다(2호기 부재).
```

---

## §2 자기검사

```
ㄱ ★★★ **조립 sha 불변 확인 — 통과**(갈래 D 의 자). 기점 `de771be` 워크트리 대조 · 같은 계기 `v5AsmSha.ts` ·
  **T 포즈 108 / 108 동일**(던지는 26칸 문언까지) · **A 포즈 29 / 29 동일**(던짐 0) ⟹
  「자만 바뀌었으니 조립·blob sha 불변」이 값으로 성립한다. **갈래 D 아님.**
ㄴ ★ **`src/` diff 는 `s4Gate.ts` 하나뿐**이다(26+/8−). `garmentScene.ts` 는 ③ 후보를 전부 되돌려
  **기점과 바이트 동일**이다(`git diff de771be -- src/v3/garmentScene.ts` = 0줄) ⟹
  프롬프트가 허용한 「`asm1x` `N_sh` 산식 한 줄」은 **쓰지 않았다**(후보가 전부 반증됐으므로).
ㄷ ★★ **미집행 1 — ④ 회귀 3칸 굽기와 캡처 3칸.** 2호기가 판 내내 도달 불가다
  (`ssh … Operation timed out` · `ping` 100% 손실). §0-5ㄹ 에 값 보기 «전» 등재한 상황 그대로다.
ㄹ ★★ **미집행 2 — A포즈 29칸 재판정.** 정본 정착 blob 이 `v3-77`(T포즈) 계열뿐이라
  A포즈 정착 상태가 없다(§0-5ㄴ 에 사전 등재).
ㅁ ★ **재판정 정의역의 «원리적» 한계** — 108칸 중 **69칸은 정본 정착 blob 이 없다**(CLAUDE.md 가 등재한
  「판정 대조 69칸(blob 부재)」). 게이트는 위치가 있어야 도는 자이므로 그 69칸은 **재굽기로만** 판정할 수 있다.
  ⟹ 이 판의 「T108 재판정」은 **37칸까지**이고 그것이 자산 부족이 아니라 **정의역 사실**이다.
ㅂ ★ **건너뛴 2칸** — `blob n 9888 ≠ 장면 n 9948` · `blob n 11118 ≠ 장면 n 10846`.
  정본 blob 이 **옛 판본 제도**로 만들어져 현재 제도와 정점 수가 다르다. 이 판은 그 둘을 재판정하지 않았고
  **맞추려 하지도 않았다**(그것은 자산 재생성이고 이 판의 정의역 밖이다).
ㅅ ★ **③ 의 후보 한 칸 미실행** — ㉡ 의 `supima-L`. ㉠ 에서 이미 던졌고 ㉡ 은 더 촘촘하며
  최소쌍이 **단조 감소**하는 것이 두 칸에서 확인됐다. **방향 판정에는 영향이 없다**(자기신고).
ㅇ ★ **다른 방향의 `N_sh` 후보는 시험하지 않았다** — 등재한 셋 밖이다(사후 신설 0). 값이 가리키는 방향
  (`N_sh` 를 «줄이는» 쪽)은 **사실로만** 적었다.
ㅈ ★ **자 교체는 정본 경로를 바꿨고 off 게이트 판정이 실제로 바뀌었다**(11칸). 프롬프트가 그렇게 등재했으므로
  약정 위반은 아니나, **승격 전에 전략 세션의 처분이 필요한 규모**다(§4 C 로 상신한다).
ㅊ **문턱 신설·이동 0** — 새 문턱 **1.10** 은 `s4Gate.ts` 주석이 인용한 **v3-24 어림수**이고 이 파일이
  새로 정한 수가 아니다. 자 `ringRestRatio` 도 이미 계산되던 값이다. **손 상수 0.**
ㅋ **물리 0줄** · 굽기 0 · 처방 0(③ 채택 0) · **ecc 에이전트 0 · 스킬 0.**
ㅌ ★ **집행 환경** — `git` 이 Xcode 라이선스 미동의로 멈춰 `DEVELOPER_DIR=/Library/Developer/CommandLineTools`
  로 우회했다(v5-23 과 같은 사고 · 기계 설정·저장소 파일 변경 0).
```

---

## §4 판정 — **갈래 C**

```
 A = ④ 3칸 pass ⟹ 상신(→ 108 발주)        … ✗ ④ 미집행(2호기 도달 불가)
 B = fail(칸·채널·값) ⟹ 상신                … ✗ 같은 이유로 ④의 값이 없다
 ★ C = ② 에서 옛 편입이 크게 흔들림(사실) ⟹ 값 상신 · 처분 전략 세션   … **✔ 이것이다**
 D = 조립 sha 변동 ⟹ 즉시 정지              … ✗ **T 108/108 · A 29/29 동일**
 E = 그 외 ⟹ 정지                            … ✗ C 가 먼저 선다(②가 값으로 흔들림을 보였다)
 사후 갈래 신설 0.

 ① = **완료** — 자 교체 구현 · `src/` diff = `s4Gate.ts` 하나 · **새 수 0 · 새 식 0** ·
      기준선 재현(옛 1.0021454460806467 = v5-22 게이트 · 새 1.0427966483091002 = v5-23).
 ② = **★ 크게 흔들린다** — 옛 pass **37/37** → 새 pass **26/37** · **11칸 pass → fail**(fail → pass 0) ·
      옛 자는 37칸에서 **한 칸도 거르지 않았다**(초과비 최대 **0.998283**) ·
      새 휴지비 중앙 **1.088685** 라 문턱 **1.10** 이 **p75 근처**를 자른다 ·
      흔들리는 칸은 **큰 몸·큰 사이즈**에 몰린다(`c122.5` 6 · `c100-h185` 3 · XL 6).
 ③ = **후보 전부 반증 · 채택 0** — `N_sh` 를 키우면 열 간격이 좁아져 `back↔back` 최소쌍이 **단조 감소**
      (S 1.39419 → 1.21605 → **0.81733** · M 2.0 통과 → 1.89265 → **1.15383**) ⟹
      ㉠·㉡ 은 **통과하던 두 칸까지 깨뜨린다** · ㉢ 은 현행과 **항등**이다.
 ④ = **미집행**(2호기 도달 불가) · 캡처 **0장**.
 ⑤ = **적었다** — 108 재굽기의 **순서가 바뀐다**(자 교체 확정 → 37칸 재판정 → 69칸은 재굽기로만) ·
      제공 재발행 · **v3 동결 예외 «5건째»**(목선 판정 채널 교체) 문언 초안 · 판단 0.

 ★★★ 이 판의 핵심 값 셋
   ㉠ **옛 자는 정본 체제에서 판별력이 0 이었다**(37칸 초과비 최대 0.998283) — v5-23 의 43칸 재판정
     (「목선 «단독» 실패 칸 0」)과 같은 방향이 더 큰 표본에서 확인됐다.
   ㉡ **새 자는 11칸(29.7%)을 거른다** — 판별력은 생겼으나 **편입 집합이 흔들린다**. 문턱 1.10 이
     휴지비 분포의 p75 근처에 있다는 것이 그 이유다.
   ㉢ **`N_sh` 를 촘촘히 하는 방향은 반대다** — 열 간격이 `SEP` 쪽으로 내려가면 자기검사가 그것을 «붙음»으로 읽는다.
```
