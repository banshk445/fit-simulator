# 30 — 3/5 종결 등재 + 정본 승격 + blob 처분 (v3-49 · 사무 판 · **물리 0프레임**)

v3-49. 브랜치 `v3-49-close-3of5`(사전 등록). 호출한 ecc 에이전트/스킬: **0 / 0**

## git 스탬프

```
 브랜치  v3-49-close-3of5           워크트리  /Users/banshk/projects/fit-simulator
 기점    325b05d (v3-48 HEAD)       직전 회차  325b05d
 커밋    514b76d  §0 판정 기준 등록 (집행 «전»)
         (본문)   ㉠ 승격 · ㉡ 등재 · ㉢ blob 처분
 검사    npx tsc -b rc=0 · npm run build 통과 · `src/v3/` diff 0줄 · **물리 0프레임**
```

---

## §0 판정 기준 — 실행 «전»에 등록

> 이 절을 커밋한 «뒤»에만 집행으로 간다. 이후 결과를 보고 어떤 수도 고치지 않는다.

### §0-1 배경 (전략 세션 판정 수령분)

```
 **사용자 화면 판정 = 합격** · **전략 세션 = 3/5 「브라우저 실행 + 렌더」 종결 «선언»**.
 이 판은 **선언을 저장소에 등재**하고, **후보 2종을 정본으로 승격**하고, **blob 처분을 고정**하는
 **사무 판**이다. **물리 실행 0프레임 · `src/v3` 코드 0줄**(패널 라벨 정리 제외).
 **선언 근거는 전부 등재분이다 — 이 판이 새로 재는 값 0.**
```

### §0-2 ㉠ 정본 승격

```
 C-브라우저 기준선의 **정본 구성**을 갱신한다:
   gray  = **구 d9** (`faf4873f` · f220) — **유지**
   swim  = **신 d9** (`557bc446` · f260) — «후보» → «**정본**» 승격
   sweat = **신 d8** (`51d74c66` · f330) — «후보» → «**정본**» 승격
 구 정본(swim d10 · sweat d9)은 «**전 정본**»으로 강등하되 **원문·파일 무삭제**.
 기준선 문서(`20-브라우저접속.md` 계열)에 승격 이력을 **날짜·sha·근거 판(v3-47·48)** 과 함께
 «**추가**» 기록한다 — **기존 절 무수정**.
 패널 선택칸 라벨에서 «(후보)» 표기를 **정본 표기로 갱신**(**표시 로직 0줄**).
```

### §0-3 ㉡ 3/5 종결 등재

```
 `00-현재상태.md` 맨 위에 **종결 블록**:
   「3/5 브라우저 실행 + 렌더 — **종결** (v3-35 개시 · v3-49 종결 · **14판**)
    최종 구성 · **남은 알려진 한계 9항**(v3-48 §6-3 링크) · **#104 링크**
    **사용자 화면 합격 + 전략 세션 선언(이 턴) 귀속 명기**」
 `전략세션-인수인계.md` 의 v3 절 갱신: **다음 단계 = 4/5 파생 치수·핏 리포트 v3 자립 통합**.
 **5/5(성능)는 그 뒤.** `v3-노트-색인.md` 단계 지도 갱신.
```

### §0-4 ㉢ blob 처분 — **재현성 부채 해소**

```
 정본 3종 + 전 정본 2종의 상태 blob 처분을 고정한다:
  1) **sha256 전체 값**을 `metrics-log` 에 등재(이미 있는 것은 **확인만**)
  2) `.gitignore` 예외 «또는» 별도 보관 중 «**저장소 크기 대비**» 판단해 택1:
     blob 5종 **합계 크기를 실측**하고, **≤ 25MB** 면 **git LFS 없이 저장소 예외 등재**
     (`public/v3diag/` 의 **해당 파일만** `!` 예외), 초과면 **압축 후 판단**,
     그래도 크면 **재생성 절차 문서화로 갈음**(§5-1 형식 · 프레임·sha 명기)
  3) 어느 쪽이든 «**재현 절차 + 기대 sha**»를 한 문서에 모은다(`docs/v3/부록-상태재현.md`)
```

### §0-5 ㉣ 갈래 (사전 등록)

| 갈래 | 조건 | 귀결 |
|---|---|---|
| **A** | 승격 + 등재 + blob 처분 완료 | 3/5 종결 «**등재 완료**» 보고 · **4/5 착수 대기** |
| **B** | blob 크기 초과로 **재생성 문서화 갈음** | 그 사실 명기하고 **A 와 같이 진행** |
| **E** | 판정 불가 | 정지 · 사실만 |

### 이 판에서 바꾸지 않는 것

물리·조립·게이트·표시층·브리지 코드 **0줄** · 문턱 **0** · 벽 **120분** · **원문 무삭제** ·
v2 경로 · **한계 9항 문언**(초안 그대로 — 수정은 **전략 세션 승인 필요**).

### 순서

```
 §0 커밋 → ㉠ 승격 → ㉡ 등재 → ㉢ blob → 보고
```

---

## §1 ㉠ 정본 승격 — **집행 완료**

### §1-1 승격 결과

| 원단 | **정본(2026-08-23~)** | 페이로드 sha | 프레임 | d | 근거 판 | 지위 변화 |
|---|---|---|---|---|---|---|
| gray | **d9** | `faf4873f…` | 220 | 9mm | v3-38 확정 · **v3-47 d8 탈락**(③a 0.5317) | **유지** |
| swim | **d9 «신»** | `557bc446…` | 260 | 9mm | **v3-47 §3-2**(③a 0.4972 · 61.2분) | 후보 → **정본** |
| sweat | **d8 «신»** | `51d74c66…` | 330 | 8mm | **v3-48 §3**(③a 0.4752 · 66.07분) | 후보 → **정본** |

| 전 정본 | 페이로드 sha | 프레임 | 강등 사유 | 처분 |
|---|---|---|---|---|
| swim **d10** | `4964ea1c…` | 180 | v3-47 에서 d9 가 전 채널 통과 | **파일·기록 무삭제** |
| sweat **d9** | `bb22ce73…` | 190 | v3-48 에서 d8 이 전 채널 통과 | **파일·기록 무삭제** |

```
 **승격 귀속** = **사용자 화면 판정 «합격» + 전략 세션 «3/5 종결 선언»**.
   v3-47·48 이 못박은 조항(「정본 승격은 사용자 화면 합격 + 전략 선언 뒤」)의 **집행**이고,
   **CC 가 스스로 올린 것이 아니다**. **CC 자체 화면 판정 0 · 이 판이 새로 잰 값 0.**
 **해상도 사다리 = 원단별 확정**(#106 · #108): gray **d9** · swim **d9** · sweat **d8**.
 **denim 은 #78 처분 그대로 제품 v1 제외** — 이 판이 손대지 않았다.
```

### §1-2 기록 자리 — **«추가»이고 수정이 아니다**

```
 `docs/v3/20-브라우저접속.md` **맨 끝에 「v3-49 «추가»」 절을 붙였다.**
 그 문서의 §5(기준선 정본화)·#76(「3종으로 확정 — gray d9 · swim d10 · sweat d9」)은
 **한 글자도 고치지 않았다** — 승격은 «그 뒤에 일어난 이력»으로 덧붙는다.
 ⟹ **v3-38 시점의 정본이 무엇이었는지가 문서에서 사라지지 않는다.**
```

### §1-3 패널 라벨 — **표시 로직 0줄**

```
 `V3Panel.tsx` 의 `SETTLED` **라벨 문자열만** 바꿨다(선택 항목 수·순서·url·물리 인자 **전부 그대로**):
   "gray d9 (정착 220)"        → "gray d9 **정본** (정착 220)"
   "swim d10 (정착 180)"       → "swim d10 (**전 정본** · 정착 180)"
   "sweat d9 (정착 190)"       → "sweat d9 (**전 정본** · 정착 190)"
   "swim d9 신 (정착 260 · **후보**)"  → "swim d9 **정본** (정착 260)"
   "sweat d8 신 (정착 330 · **후보**)" → "sweat d8 **정본** (정착 330)"
 **표시 로직·렌더·캡처 경로 0줄.** 파일명 접두사 규약도 그대로다.
```

---

## §2 ㉡ 3/5 종결 등재 — **집행 완료**

```
 **자리 3곳**
  · `.obsidian-log/00-현재상태.md` **맨 위**에 **★★ 종결 블록** 신설
    (선언 귀속 · 범위 · 최종 구성 표 · 한계 9항 · #104 · blob 처분 · **다음 = 4/5**)
  · `.obsidian-log/전략세션-인수인계.md` **v3 절 교체** — 「현재 위치 = 3/5 종결 · **다음 = 4/5
    파생 치수·핏 리포트 v3 자립 통합** · 5/5 는 그 뒤」. **v3-48 절은 원문 무삭제로 아래 누적**
  · `.obsidian-log/v3-노트-색인.md` **단계 지도 갱신** — 「브라우저·렌더 ✅ 종결(3/5)」 ·
    **「핏 리포트·핏 맵 ←지금(4/5)」** · 3/5 경로 요약 인용문 추가 · 30·부록 행 추가
```

### §2-1 판 수 — **사실 병기 1건**

```
 §0-3 이 등재한 문언은 「v3-35 개시 · v3-49 종결 · **14판**」이다.
 **문서상 회차는 v3-35 ~ v3-49 = 15개**다. 어긋남의 정체는 **세는 대상**이다:
   **작업 판 = v3-35 ~ v3-48 = 14판** · v3-49 = **종결 사무 판**(물리 0프레임 · 새로 잰 값 0).
 ⟹ **「14판」을 그대로 두고 «작업 판 수»라는 읽기를 명기**했다. **문언 수정 0 · 창작 0.**
 (선례: v3-47 §0 이 metrics 라벨 어긋남을 「정본을 쓰고 어긋남을 사실로 병기」로 처리했다.)
```

### §2-2 한계 9항 — **문언 수정 0 · 그중 1항 «해소»**

```
 정본 초안은 `docs/v3/29-sweat재실측과S5.md` **§6-3** 이고 **한 글자도 고치지 않았다**
 (§0 「수정은 전략 세션 승인 필요」 조항 그대로).
 다만 **9항(정착 blob 이 저장소 밖)** 은 **이 판의 ㉢ 이 해소**했다 ⟹ **실질 8항**.
 **초안 문언은 두고, «해소됨»을 등재 쪽에 적었다**(초안을 고쳐 8항으로 만들지 않았다).
```

---

## §3 ㉢ blob 처분 — **갈래 A**(저장소 추적 · LFS 없음)

### §3-1 크기 실측 → 택1

| 파일 | 크기[B] | 프레임 | d | 정점 |
|---|---|---|---|---|
| `settled-gray-d9.bin` | 499,379 | 220 | 9mm | 10402 |
| `settled-swim-d9-new.bin` | 499,379 | 260 | 9mm | 10402 |
| `settled-sweat-d8-new.bin` | 635,987 | 330 | 8mm | 13248 |
| `settled-swim-d10.bin` | 416,529 | 180 | 10mm | 8676 |
| `settled-sweat-d9.bin` | 499,379 | 190 | 9mm | 10402 |
| **합계** | **2,550,653 B = 2.43 MB** | — | — | — |

```
 **2.43 MB ≤ 25 MB** ⟹ §0-4 의 첫째 갈래 그대로 **저장소 예외 등재**.
 **압축 검토·LFS 검토 불필요**(문턱을 한 자릿수 아래로 밑돈다) ⟹ **갈래 B 불발화.**
```

### §3-2 `.gitignore` — **디렉터리형이 `!` 를 막는다**(실측 확인)

```
 1차 시도: `public/v3diag/` 를 그대로 두고 `!public/v3diag/settled-*.bin` 5줄을 붙였다
   ⟹ **안 듣는다.** `git check-ignore -v` 가 5개 전부 **`.gitignore:13 public/v3diag/`** 로 답했다.
   기전: **디렉터리가 제외되면 git 은 그 안으로 내려가지 않으므로** 안쪽 파일의 `!` 예외가
   평가되지 않는다.
 2차(채택): **`public/v3diag/*`(내용물 무시) + `!` 예외 5줄.**
   ⟹ 5개만 `A`(추가) 로 잡히고, 프로브·중간 blob(`d10-gray-220.bin` · `d11-gray-220.bin` ·
     `denim-d12.bin` · `node-gray-f0.bin` · `sweat-d9.bin` · `mathfp.js`)은 **계속 무시**된다
     (`git check-ignore` 로 확인).
 **무시 «범위»는 바뀌지 않았다** — 바뀐 것은 예외를 걸 수 있느냐뿐이다.
```

### §3-3 `docs/v3/부록-상태재현.md` — **한 문서에 모았다**

```
 담긴 것: 처분 근거 · **정본 3종 + 전 정본 2종의 페이로드 sha «전체 값» + blob sha + 크기 + 헤더** ·
 **페이로드 sha ↔ blob sha 의 차이**(파일명에 쓰는 것은 페이로드 쪽 — 헤더의 `frame` 이 재발행으로
 바뀌기 때문) · **확인 명령 2개** · **재생성 절차**(원단당 61~66분 · 프레임·d 표 · 게이트 통과 확인 ·
 Node 대안과 «채널 상이» 조항) · **§5-1 「재생성이 sha 를 그대로 낼 것인가는 보장하지 않는다」**.
 ⟹ §0-4 의 3) 「어느 쪽이든 재현 절차 + 기대 sha 를 한 문서에」 **충족**.
```

---

## §4 ㉣ 갈래 판정 — **A**

| 갈래 | 조건 | 실측 | 성립 |
|---|---|---|---|
| **A** | 승격 + 등재 + blob 처분 완료 | **3/3 완료**(㉠ 승격 2종 · ㉡ 등재 3곳 · ㉢ 저장소 추적 5종) | **예** |
| B | blob 크기 초과로 재생성 문서화 갈음 | **2.43MB ≪ 25MB** — 초과 없음 | 아니오 |
| E | 판정 불가 | — | 아니오 |

**3/5 「브라우저 실행 + 렌더」 = 등재 완료 · 4/5 착수 대기.**

### §4-1 이 판이 «닫은» 것 / «연» 것

```
 닫음  **3/5 종결이 저장소에 등재됐다**(현재상태 · 인수인계 · 색인 3곳)
       **정본 2종 승격** — C-브라우저 기준선이 **원단별 사다리로 확정**(gray d9 · swim d9 · sweat d8)
       **재현성 부채 해소** — blob 5종 저장소 추적 + 재현 부록 ⟹ **한계 9항 → 실질 8항**
       **`.gitignore` 디렉터리형이 `!` 를 막는다**를 실측으로 확인(다음에 같은 함정을 안 밟는다)
 엶    **4/5 파생 치수·핏 리포트 v3 자립 통합** — 착수 0
       **한계 8항이 그대로 열려 있다** — 특히 **swim ③a 여유 0.56%**(재현 편차에 뒤집힐 수 있다)
       **재생성 sha 보장 없음** — 파일 보존이 1순위다(부록 §5-1)
```

---

## §5 회귀

```
 **물리 0프레임** — 이 판은 솔버를 한 번도 돌리지 않았다(계기 실행은 진단 벌 재현 1회뿐)
 **`src/v3/` 전량 `325b05d`(v3-48 HEAD) 대비 diff 0줄** — 물리·조립·게이트·표시층·브리지
 변경 목록
   · `src/components/V3Panel.tsx` — **라벨 문자열 5개 + 주석 1줄**(표시 로직 0줄)
   · `.gitignore` — 디렉터리형 → `public/v3diag/*` + `!` 예외 5줄 (+ 주석)
   · `public/v3diag/settled-*.bin` **5종 «추가 추적»**(파일 내용 변경 0바이트)
   · 문서 — `docs/v3/30-종결등재.md`(신설) · `docs/v3/부록-상태재현.md`(신설) ·
     `docs/v3/20-브라우저접속.md`(**맨 끝 «추가»** · 기존 절 무수정) ·
     볼트 3종 · `docs/metrics-log.md`
 **문턱·게이트·SEP·원단 물성·벽 120분 변경 0** · denim 처분 유지 · v2 경로 무영향
 **npx tsc -b rc=0** · **npm run build 통과**
 **구 정본 진단 벌 재현**(브리지 off · `settled-gray-d9.bin`):
   끊김 front/side/back **7 / 118 / 7** · 팔 숨김 후 side **76 (65.6‰)** · 실루엣 **9735**
   — v3-44/45/47/48 등재분과 **일치** ⟹ 승격이 **상태 파일을 건드리지 않았다**는 확인이기도 하다
```

---

## §6 자기 점검

| # | 항목 | 답 |
|---|---|---|
| 1 | §0 커밋 선행 | **예** — `514b76d`(§0)가 승격·등재·blob 집행 커밋보다 앞선다(git 이력) |
| 2 | `src/v3` diff 0줄 · 물리 0프레임 | **예** — `src/v3/` 0줄. 솔버 실행 0회(진단 벌 재현 계기 1회는 **물리 0프레임** 경로) |
| 3 | 구 정본 무삭제 · 승격 이력이 «추가»인가 | **예** — `20-브라우저접속.md` §5·#76 **무수정** · 맨 끝 «추가» 절 · 전 정본 blob 2종 **파일 보존**(오히려 이번에 저장소로 «들어갔다») |
| 4 | 종결 블록에 선언 귀속이 명기됐는가 | **예** — 「**사용자 화면 판정 합격 + 전략 세션 선언**, v3-49 는 **등재만**」을 현재상태·인수인계·이 노트 §1-1 **세 곳에** 적었다 |
| 5 | blob 5종의 sha·크기·처분이 한 문서에 모였는가 | **예** — `docs/v3/부록-상태재현.md`(페이로드 sha **전체 값** + blob sha + 크기 + 헤더 + 확인 명령 + 재생성 절차 + **보장 없음** 명기) |
| 6 | #109 신설 | **예** — 「종결의 형식: **선언은 사용자·전략 세션 몫 · CC 는 등재만** · **종결 = 한계 없음이 아니라 한계가 등재됨**」. 부수로 **`.gitignore` 디렉터리형이 `!` 를 막는다**를 같은 항목에 실측으로 붙였다 |
| 7 | 이 판에서 호출한 ecc 에이전트·스킬 | **0 / 0** |
