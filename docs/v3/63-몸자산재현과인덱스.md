# v3-82 — 몸 자산 재현 «절차 분리» · 정착 체제 코드 사실 · 몸 인덱스 27

**물리 0프레임 · 옷 재생 0 · 몸 굽기 다운로드 0(측정용 산출만)**

| | |
|---|---|
| 브랜치 | `v3-82-body-asset-index` |
| 기점 | **`f71a48b`** (= v3-81 HEAD) |
| 호출 ecc 에이전트/스킬 | **0** |

---

## §0 수령 (집행 «전» 커밋)

### §0-1 판정문 (전략 세션 · v3-81 §4 수령 · 원문)

```
 「갈래 C 수령 · 화면 4/4 합격 · 재굽기 108 = 안 함 · 1몸 재굽기 = 이 판 결과 뒤 승혁 ·
   「비트 재현 불가」 = **미성립(절차 미분리)** · 기전 후보 = 미수렴 몸 굽기(확정 아님) ·
   몸 인덱스 27 신설」
```

★ **v3-81 의 「비트 재현 불가」는 이 판에서 «정정»된다** — §1-①′ 참조. 지적이 옳았다.

### §0-2 바꾸지 않는 것

```
 물리 코어 4파일 diff 0 · **`Mannequin.tsx` diff 0** · 몸 blob 27개 **무변조** ·
 `index-merged-108.json` 0 · `v1-provide-35/37` 0 · 문턱 0 · **재굽기 0** · 화면 **0줄**
```

---

## §1′ 집행

### §1-①′ `unitScale` 인자 분리 — **갈래 (나)**

**ㄱ 좌표계와 곱해지는 자리 (코드 사실)**

| 자리 | 사실 |
|---|---|
| `src/components/bodyInjectBake.ts:113` | `restore = inverse(sk.bindMatrix) · sk.matrixWorld` |
| `:118-121` | 정점마다 `applyBoneTransform` → `applyMatrix4(restore)` ⟹ **바인드 프레임 · 월드 «배율» 포함** |
| ⟹ | blob 좌표는 world 도 순수 local 도 아니다 — **바인드 프레임에 조상 배율이 실린 값**이다 |
| `Mannequin.tsx:295-301` | `unitScale = (DEFAULT_BODY_SIZE.height/100) ÷ rawHeight` · **`rawHeight ≤ 0.001` 이면 «1»**(폴백) |
| `:308` | `scene.scale.setScalar(unitScale)` ⟹ `sk.matrixWorld` 에 들어간다 |
| `V3Panel.tsx:423-427` | `bl = new Blob([r.verts.buffer])` → `URL.createObjectURL(bl)` → `a.download` |

```
 ⟹ v3-81 의 가로채기(`URL.createObjectURL` 인자)는 **이미 「다운로드 직전 바이트 그대로」**였다.
   («앞»이 아니라 «그 자리»다 — 이 판에서 확인했고, 그래서 (가)는 성립하지 않는다.)
```

**ㄴ 새 페이지 «첫» 적재에서 생산 경로 1회 — 저장 blob 과 정점별 비교**

| 대상 | 결과 |
|---|---|
| **26몸** | 재생성 = **`1.038266 × 저장` + y절편 3.75~4.48mm** · **최대 잔차 0.0001mm** |
| | 배율 **1.038266 은 26몸에서 «유일값»**이다(몸마다 다르지 않다) |
| **1몸 `c87.5-h155-s40`** | **상사 변환으로 겹치지 않는다** — 최적 배율 0.917053 에서도 **잔차 95.17mm** |
| 축별(기준몸 `c100-h170-s45`) | x·y·z **전부 1.038266** · 잔차 0.0001mm ⟹ **균일 배율**이다 |
| 축별(타깃몸) | x **0.9148** / y **1.0270** / z **1.0068** · 잔차 21.97 / 12.77 / 6.89mm ⟹ **균일 배율이 아니다** |

```
 ⟹ **갈래 (나)** — 「전 정점 정확히 ×1.03827」이 **26몸에서 성립**한다.
 ★ **자리 등재**: 실측 `Scene.scale.x = 1.000000` · 기하 local 높이 **1.7651m** ·
   **1 / 1.038266 = 0.963144 = 1.70 / 1.7651** = `Mannequin.tsx:299` 의 `unitScale` **그 값**.
   ⟹ **오늘 굽기에는 `unitScale` 이 안 실렸고 저장 자산에는 실렸다.** 자리는 **`:295-301` 두 줄**이다.
   그 값이 1 이 되는 길은 둘뿐이다 — ① `rawHeight ≤ 0.001` **조용한 항등 폴백**(#121 계열) ·
   ② 이미 배율이 걸린 씬을 재서 `rawHeight = 1.70` ⟹ 몫이 1. **어느 쪽인지 이 판은 가르지 않는다**
   (`Mannequin.tsx` 수정 0 · 처방 0 · 귀속 0). 두 구성(`?v2=1&v3=1` · `?canon=1&v3=1`)에서
   **둘 다 1** 이었다 ⟹ 「마네킹 인스턴스 2개라서」는 **아니다**(단독 인스턴스에서도 1).
```

```
 ★★ **v3-81 「비트 재현 불가」 정정**: 26몸은 **인자 하나(1.038266)와 y절편**으로 저장분에 겹친다 ⟹
   **재현 «절차»의 문제**이지 재현 불가가 아니다. 전략 세션의 「미성립(절차 미분리)」이 옳았다.
   남는 것은 **그 몸 1개**뿐이고, 그것은 배율이 아니라 **형상이 다르다**.
```

### §1-②′ 정착 체제 — **갈래 (나)**

**snap 의 «자리»와 성격 (코드 사실)**

| 자리 | 사실 |
|---|---|
| `Mannequin.tsx:343` | `const snap = !targetChanged && mannequinPoseRef.maxScaleResidual <= POSE_SETTLE_EPS` |
| `:352` | `const to = (cur, target) => (snap ? target : lerp(cur, target, t))` |
| ⟹ | **`snap` 은 «인자»가 아니다** — `useFrame` 안에서 «계산»되는 클로저 변수다. **넘기는 호출자가 없다** |
| `:315` | `t = 1 − 0.001^delta` ⟹ **프레임 delta 에 의존**한다 |
| `mannequinRef.ts:75` | `POSE_SETTLE_EPS = 1e-4` (수렴 문턱 · 새 문턱 0) |
| `V3Panel.tsx:413-419` | 상한 **600프레임** · 판정 = `poseStopped() ∧ maxScaleResidual ≤ EPS` **연속 2회** |
| `Mannequin.tsx:391-405` | **잔차 = 목표 대비 편차의 «채널 최대»**(높이·팔·다리·어깨·몸통둘레·상쇄 전부) · 단위는 **배율**(무차원) |
| `:417` | `scaleStillFrames` 는 **프레임 간 변화량 0** 여부 — 잔차와 «다른 양»이다 |

```
 ⟹ 「잔차 8.60e-2」는 **배율 단위**이고 길이가 아니다(v3-77 등재값).
```

**체제 표 — 어느 경로 · 어느 snap · 어느 가시성**

| 실행 | 경로 | 전진 분포 | snap | 탭 |
|---|---|---|---|---|
| v3-77 §1 (자산 생성) | 하네스 「그리드 몸 굽기(27칸)」 | **4~64 · 1칸 600** | 미기록 | 미기록 |
| v3-81 시도 1 | **콘솔 단독 호출**(한 몸씩) | **전부 600** | 값 미기록 · 잔차 4~6e-2 ⟹ **false** | hidden |
| v3-81 시도 2 | 그리드 버튼(재적재 후) | **전부 3** | — | hidden |
| **v3-82 (이 판)** | 그리드 버튼 · **새 적재 1회** | **26칸 3 · 1칸 600** | `[dress·P26]` 로그 **52건**(snap=true 발화) | hidden |

```
 ★ **재현됐다**: 이 판의 새 적재에서 **`c87.5-h155-s40` 만 전진 600 · 잔차 3.37e-2**,
   나머지 26칸은 **전진 3 · 잔차 0.00e+0**. v3-77 의 「1칸 600 · 나머지 소수 프레임」과 **같은 모양**이다.
 ★ 그 몸은 **순회의 «첫» 칸**이다(`grid.bodies()` 정렬 첫 항목) ⟹ **직전 정착 상태가 없다** ⟹
   그 프레임에 `targetChanged = true` 이고 잔차도 크다 ⟹ **`snap = false`** ⟹
   `delta` 가 작은 동기 전진에서 lerp 가 600프레임 안에 수렴하지 못한다.
 ⟹ **갈래 (나)** — 「snap=false ⟹ **미수렴 굽기가 구조적으로 가능**」. 후보 확정 재료를 **값으로** 냈다.
   굽기 시점 잔차 3.37e-2 ≫ 문턱 1e-4 ⟹ **그 칸은 문턱을 통과하지 못한 채 구워진다**(상한 도달).
 ★ **처방 0 · 귀속 0.** 「첫 칸이라서」는 **관측된 정합**이고 단정이 아니다 —
   순서를 바꿔 재현하는 것은 **이 판의 정의역 밖**이다(재굽기 0).
 ★ §4 의 C 조건(「snap 인자 자체가 없음」)은 **문면상 성립**한다(위 `:352`).
   다만 §1-② 가 요구한 것은 **그 경로의 snap «값»**이고, 그 값은 **측정됐다**(false) ⟹
   **(나)로 답한다.** 두 사실을 **함께** 등재한다 — 문면을 고르지 않고 둘 다 남긴다.
```

### §1-③′ 몸 인덱스 27 — `docs/v3/body-index-27.json` **신설**

```
 읽기 전용 정본 · `index-merged-108.json` **수정 0** · 몸 blob **무변조**.
 행마다: id · 목표 높이(h/100) · blob 실측 높이 · 잔차(실측−목표) · sha256 · mtime ·
        굽기 프레임/잔차(v3-77 등재값 인용 · 그 회차가 개별 값을 남긴 칸은 하나뿐이라 나머지는 **「미등재」**)
```

| 문턱 | 값 |
|---|---|
| 정의(**먼저** 등재) | 26몸(`c87.5-h155-s40` 제외 · **정의역 서로소**)의 \|잔차\| **최대값** |
| 값 | **0.0020 m** |
| 26몸 잔차 범위 | **−0.0014 ~ +0.0020 m** |
| **초과** | **`c87.5-h155-s40` · 잔차 +0.0898 m** ← §2 사전 등재 기대와 **일치** |

### §1-④ 재굽기 0 · 화면 0줄 — **성립**

---

## §2′ 자기검사

| # | 항목 | 결과 |
|---|---|---|
| ㉠ | ① 표 | **갈래 (나)** · 26몸 상사 일치(잔차 0.0001mm) · 1몸 불일치(95.17mm) |
| ㉡ | ② 표 | **갈래 (나)** · 체제 4행 · snap 자리 `:343`(인자 아님) |
| ㉢ | ③ 27행 + 문턱 출처 | **27행** · 문턱 0.0020m(26몸 최대) · 초과 1 |
| ㉣ | 몸 blob 27 sha 전후 동일 | **변경 0건**(git 미변경) |
| ㉤ | 정본 3파일 diff | **0** (`index-merged-108` · `v1-provide-35` · `v1-provide-37`) |
| ㉥ | `Mannequin.tsx` diff · 물리 코어 diff · 화면 diff | **0 · 0 · 0** |
| ㉦ | `tsc -b` · 회귀(v3-81 §2) | 통과 · **전량 통과** |

**§3 캡처 — 0장**(이 판은 화면을 안 바꾼다 · 사전 등재 그대로).

---

## §4′ 판정 — **갈래 A**

```
 A = ①②③ 전량(①② 는 갈래 이름까지) ⟹ 상신
   ① **(나)** · ② **(나)** · ③ 27행 + 문턱 등재 · ④ 재굽기 0 · 화면 0줄 · §2 전량 · 캡처 0(사전 등재)
 ★ B 는 불성립(가로채기 지점은 이미 다운로드 직전이었다) ·
   C 의 문면은 성립하나 ② 는 **값이 측정돼 (나)로 답할 수 있다** ⟹ 두 사실을 함께 등재하고 **A** 로 답한다.
 사후 갈래 신설 0 · 기대 사후 변경 0.
```

### §4-1 넘기는 것

| # | 항목 | 몫 |
|---|---|---|
| ㉠ | **1몸 재굽기** — `c87.5-h155-s40` 만 형상이 다르고 문턱 45배 초과 | **승혁**(판정문 예고분) |
| ㉡ | `unitScale` 이 1 이 되는 두 길 중 어느 것인가 — **가르려면 `Mannequin.tsx` 를 봐야 한다**(이 판 잠금) | 전략 세션 |
| ㉢ | **재굽기 절차**: 재현하려면 `unitScale` 이 실린 상태여야 하고, **순회 첫 칸은 미수렴한다** ⟹ 두 조건을 절차에 넣어야 한다 | 전략 세션 |
| ㉣ | 몸 인덱스 27 을 **굽기 파이프라인이 «생산 시점»에 쓰게 할지** | 전략 세션 |
