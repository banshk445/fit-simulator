# v3-92 — 제품 화면 2차: 드래그 회전 · 말로 된 리포트 · 문언

| | |
|---|---|
| 브랜치 | `v3-92-screen-2` |
| 기점 | **`8bdee12`** (= v3-91 HEAD) |
| §0 커밋 | **`94652a0`** — 집행 «전» 별도 커밋(sha 만 그 뒤 한 줄로 채웠다) |
| 호출 ecc 에이전트/스킬 | **0** |

---

## §0 수령·사무 (집행 «전» 별도 커밋)

### §0-1 판정문 (전략 세션 · v3-91 §4 수령 · 원문)

```
 「갈래 C 수령 · **소매 처방 완결**(착용불가 5 → 보류 5 · 제공 35 불변) ·
   **정본 = 108.v3-91 · 35.v3-91 채택** · 화면 3/3 합격 ·
   목선 초과비 4칸 · ②(가) · ③(가) = **백로그** ·
   **조립 트랙 종결** · **v3 물리·조립 «동결»**(이후 수정은 **v4 엔진**에서)」
```

### §0-2 사무 — **조립 트랙 종결 · v3 동결 · 백로그 신설**

`00-현재상태.md` 에 「조립 트랙 종결 · v3 동결」 블록 · 인수인계에 「백로그」 절을 **순서대로** 세운다.

### §0-3 바꾸지 않는 것

```
 물리 · 조립 · 게이트 · 정본 · `framing` 규칙 · `match` · `provide`(규칙) · 문턱 ·
 **계산 채널 0줄**
```

### §0-4 사전 등재 기대

| # | 기대 |
|---|---|
| **①** | 드래그 회전 — 좌우 = **수직축 회전** · 상하 **±15° 제한** · 중심 = **몸 중심**(v3-80 프레이밍 규칙 **불변**) · 앞·옆·뒤 버튼은 **스냅**(≤ 0.3s) · **관성 없음** |
| **②** | 카드 5개 본문을 **기존 3대역으로 말로**(새 수 0) · mm 은 **오른쪽 배지에만** · 경계·색·판정은 v3-54 규칙 **인용** |
| **③** | 회색 문언 2종 + 제공 0칸 문언 교체 · **사유 본문·측정 원문 접힘 그대로** · **매칭·착지 배너 불변** |

**§2 자기검사** — 매칭 12/12 · 버튼 108/108(**정본 v3-91** raw 재도출) · 착지 27/27 · ARM_G 9/혼입 0 ·
프레이밍 v3-80 ㉠㉡ **재현**(회전 0°) · **회전 후 프리셋 스냅 각도 = 프리셋 3종 카메라 자세와 «동일»**(값) ·
canon 3종 sha 불변 · 정본 diff 0 · 물리 코어 diff 0 · `tsc`

### §0-5 갈래 (집행 «전» 등재)

| 갈래 | 조건 | 처분 |
|---|---|---|
| **A** | ①②③ + §2 + 캡처 5/5 | 상신 |
| B | ① 회전과 프레이밍 규칙이 **충돌**(코드 사실) | ① 착수 0 · 나머지 · 상신 |
| C | 그 외 | 정지 |

**사후 갈래 신설 0.**

---

## §1′ 집행

### §1-①′ 드래그 회전 — **프레이밍 규칙과 «충돌 없음»**

```
 구조: `renderProduct` 는 `view.dir`(보는 «방향»)만 읽고, **거리는 `framing.cameraDistanceM()`** 이
   따로 정한다(v3-80). 회전 중심도 그 함수가 쓰는 **몸 bbox 중심** 그대로다 ⟹
   각도를 돌려도 **프레이밍은 한 글자도 안 바뀐다**. 실제로 `productView.ts` · `framing.ts` **diff 0**.
 ⟹ §0-5 **B(충돌)는 불성립**.
 구현은 «화면 쪽»에만 있다 — `dirOf(yaw, pitch)` 가 방향을 만들고 프리셋 이름은 그대로 쓴다.
   좌우 = 수직축(yaw) · 상하 = **±15° 제한** · **관성 없음**(포인터를 떼면 그 각도에서 멎는다) ·
   감도는 **캔버스 크기에서 뜬다**(폭 840 에 360° · 높이 672 에 180°) — **손 상수 0**.
```

**프리셋 스냅 각도 = 프리셋 카메라 자세와 «동일»** (§2 대조 · 값)

| 프리셋 | `PRODUCT_VIEWS.dir` | `dirOf(yaw, 0)` | 최대차 |
|---|---|---|---|
| front-p | `[0, 0, -1]` | yaw **0°** → `[0.000000, 0.000000, -1.000000]` | **0.00e+0** |
| side-p | `[-1, 0, 0]` | yaw **90°** → `[-1.000000, 0.000000, -0.000000]` | **6.12e-17** |
| back-p | `[0, 0, 1]` | yaw **180°** → `[-0.000000, 0.000000, 1.000000]` | **1.22e-16** |

```
 **3/3 일치**(부동소수 최말단). 스냅은 **짧은 쪽으로** 돌고 **0.3s**(smoothstep · 관성 아님)에 끝난다.
```

```
 ★ **집행 중 결함 1건 — 고쳤다**: 스냅을 `requestAnimationFrame` 으로만 돌렸더니
   **비가시 탭에서 각도가 45° 에 멎었다**(캡처로 확인 · v3-71 §2 「rAF 0회/초」와 같은 자리).
   ⟹ **애니메이션은 «있으면 좋은 것»이고 «도달»이 계약**이다 — 같은 시간 뒤에 목표를 그냥 놓는
     `setTimeout` 을 함께 건다. 재촬영에서 **뒤 스냅이 정확히 섰다**.
```

### §1-②′ 말로 된 리포트 — **새 수 0**

```
 카드 본문에서 **숫자를 뺐다**(대역·경계·색·판정은 v3-54 규칙 그대로 «인용» · 계산 0줄):
   눌림(<0)        「옷이 몸을 누릅니다」
   밀착(0 ~ sepMm) 「옷이 몸에 붙습니다」
   여유(> sepMm)   「옷과 몸 사이가 떠 있습니다」
 mm 은 **오른쪽 배지에만** 남는다(자리 그대로). 겉면 범례(「밀착 0~2.0mm」)도 그대로다.
 실측(캡처 4) — 다섯 카드가 「옷이 몸에 붙습니다 / 옷과 몸 사이가 떠 있습니다 ×4」로 나온다.
```

### §1-③′ 문언

| 자리 | 전 | 후 |
|---|---|---|
| 회색(보류·경계) | 검증을 통과하지 못해 제공하지 않습니다 | **이 사이즈는 아직 준비 중입니다** |
| 회색(착용불가) | 이 사이즈는 만들 수 없습니다 · **사유 보기** | **이 사이즈는 아직 준비 중입니다** · **자세히** |
| 제공 0칸 몸 | 이 몸에는 제공되는 사이즈가 없습니다 | **이 몸에 맞는 준비된 사이즈가 아직 없습니다** |

```
 **분류는 그대로다** — `gray` 가 여전히 착용불가/검증을 가르고, 「자세히」가 붙는가는
 `detail` 유무가 정한다. **사유 본문·측정 원문 접힘 그대로 · 매칭·착지 배너 불변.**
 `provide.ts` diff = **문언 2줄 + 주석**(규칙·분류 0줄).
```

---

## §2′ 자기검사

| # | 항목 | 결과 |
|---|---|---|
| ㉠ | 매칭 · 버튼 · 착지 | **12/12 · 108/108 · 27/27**(정본 **v3-91** raw 재도출) |
| ㉡ | ARM_G · 혼입 | **9 / 0** · ㉡㉢ 금지어 **0** |
| ㉢ | 프레이밍 v3-80 ㉠㉡(회전 0°) | **0.110% · 0.91176**(편차 0.000%) · 잘림 **0/27** |
| ㉣ | 프리셋 스냅 각도 = 프리셋 자세 | **3/3 일치**(최대차 ≤ 1.22e-16) |
| ㉤ | 정본 diff | **0** |
| ㉥ | 물리·조립·게이트·`framing`·`match`·`productView` diff | **0** |
| ㉦ | `provide.ts` | **문언만**(규칙 0줄) |
| ㉧ | `tsc -b` | 통과 |

---

## §3 캡처 5장 — `~/Desktop/v3-92-캡처/`

| # | 파일 | 화면 |
|---|---|---|
| 1 | `1-기본몸M-앞.png` | 기본몸 M · yaw 0° |
| 2 | `2-드래그45도.png` | 같은 칸 · **드래그 45°** |
| 3 | `3-뒤-스냅.png` | 같은 칸 · **「뒤」 스냅**(정확히 180°) |
| 4 | `4-마른몸-착지L-말로된카드.jpg` | 마른몸 착지 L · **말로 된 카드 5개** |
| 5 | `5-제공0칸-새문언.jpg` | 제공 0칸 몸 · **새 문언 3종** |

```
 ★ **캡처 경로 한정 1건** — 이 세션의 크롬 창이 **폭 500px** 이라 캔버스(840px)가 화면에서 잘린다.
   `resize_window` 가 듣지 않았다(실측 `innerWidth` 500 유지) ⟹ 1~3 은 **캔버스 원본을
   v3-86 수신기로 받아** 저장했다(840×672 PNG · sha 대조 통과). 4~5 는 패널(440px)이라 화면 캡처다.
   **보이는 것을 바꾸지 않았고 «받는 경로»만 바꿨다.**
 ★ 캡처 4 의 몸을 바꿨다 — 발주는 「마른몸 착지 L」인데 `85·158·39`(= `c87.5-h155-s40`)는
   **v3-91 뒤 제공 0칸**이라 카드가 안 나온다 ⟹ **착지 L 이 서는 마른몸** `85·168·44`
   (= `c87.5-h170-s45` · 착지 **L**)로 잡았다. **사실로 등재**한다.
```

---

## §4′ 판정 — **갈래 A**

```
 ①②③ + §2 전량 + 캡처 5/5 ⟹ **A**. B(회전↔프레이밍 충돌)는 **불성립**(diff 0 으로 증명).
 사후 갈래 신설 0 · 계산 채널 0줄 · 문턱 0 · 정본 0.
```

### §4-1 넘기는 것

| # | 항목 | 몫 |
|---|---|---|
| ㉠ | **화면 판정**(회전 감도 · 스냅 0.3s · 말로 된 문장 · 새 문언) — **보기 판단은 사용자 몫** | 사용자 |
| ㉡ | 백로그 6 「UI 재설계」 중 **«리포트 말로»만** 이 판에서 닫혔다 — 캔버스 주인공·밝은 톤·입력 3칸은 미착수 | 전략 세션 |
| ㉢ | **크롬 창 폭 500px** — 캡처가 수신기 경유를 요구한다(사용자가 창을 넓히면 화면 캡처로 충분) | 사용자 조치 |
