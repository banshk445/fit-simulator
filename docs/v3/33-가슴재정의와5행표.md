# 33 — chestY 재정의(규격 의미론) + 높이 도출 + 5행 표 (v3-52 · **물리 0프레임**)

v3-52. 브랜치 `v3-52-fit-table`(사전 등록). 호출한 ecc 에이전트/스킬: **0 / 0**

## git 스탬프

```
 브랜치  v3-52-fit-table            워크트리  /Users/banshk/projects/fit-simulator
 기점    cb48ba7 (v3-51 HEAD)       직전 회차  cb48ba7
 커밋    9bcdcb9  §0  판정 기준 (집행 «전»)
         64abf32  §1  chestY 재정의 «규칙» (**수치 대입 0**)
         (본문)   §2~§6  도출 · 검증 · 5행 표 · 게이트 · 갈래 A
 검사    npx tsc -b rc=0 · npm run build 통과 · **물리 0프레임 · 솔버·게이트 0줄**
```

---

## §0 판정 기준 — 실행 «전»에 등록

### §0-1 배경 (전략 세션 판정 수령분)

```
 v3-51 정지 수령. 전략 판정:
 **(2) chestY «정의» 교체** — 「내부 최대」는 **우리가 만든 가정**이었고 **이 몸이 반증**했다.
    **의류 치수 규격(ISO 8559 계열)의 의미론을 채택**한다: 가슴둘레 = «**겨드랑이 밑**» 수평 최대 둘레.
    v3-51 §3-1 의 **단조 증가 사실과 정합** ⟹ 최대는 **순수 몸통 대역의 상단**.
 **(1) 팔 배제 기준 = «불필요해진다»** — **팔 대역에서 재지 않는다.** **골격 0 · v2 임포트 0.**
 **사후 조정 방지**: 변경 근거 = **외부 규격 의미론**이다(**데이터는 «옛 규칙의 반증»만 제공**).
 새 규칙은 **수치 «전» 커밋**. 실패 시 차기 후보 = **v3 골격 층**(이 판에서 **착수 0**).
 **대역 반폭 처방**: v2 의 ±2.5cm 대신 «**그 원단의 격자 간격 d**»(등재 사다리값).
```

### §0-2 ㉠ 재정의 도출 (커밋 규율: §0 → 규칙(**수치 0**) → 구현·수치)

```
 `Y_ARM`  = `Y_LOW` 위에서 단면 성분 **1 → >1 «첫» 전이**(이분 · `TOL_SELF`) — **가랑이와 같은 계열**
 `chestY` = **`Y_ARM` 직하의 «순수 몸통 대역» 상단** (`C_chest` = 그 높이의 몸통 껍질 둘레)
 `waistY` = **(`Y_LOW`, `chestY`) 내 C(y) «내부» 최소** (v3-51 §1-3 **원 규칙 그대로** · **끝 붙음이면 갈래 B**)
 계기: **`v3BodyLevels` 재사용**(§1 기계 그대로 · **규칙 절만 교체**).
 **한계 사전 등재**: 처진 팔 포즈에서 `Y_ARM` 이 **해부학적 겨드랑이보다 낮게** 잡힐 수 있다
 (**첫 출현이 손끝일 가능성**) — **보수적 오차** · **값으로 확인해 등재**
 (전이 높이의 성분들의 **|x| 위치를 인쇄** — **팔 원위인지 근위인지 «사실»만**).
```

### §0-3 ㉡ 검증 (사전 등록)

```
 **결정성 2회** · **순서 `Y_NECK` > `chestY` > `waistY` > `Y_LOW`** · **C(chestY) > C(waistY)** ·
 **v2 `bodyMeasure` 값 나란히 표**(**참고 채널 · 판정 불사용 · `V2DIMS` 미사용**) ·
 **`waistY` 가 v3-51 관찰(≈108cm)과 «같은 자리»인지 확인**(다르면 **사실만**).
```

### §0-4 ㉢ 5행 표 구현 — **v3 패널**(색 착수 0)

```
 **값**: 정착 blob **정점별 부호거리 c**(`bodyG` SDF · **`v3O4` 계열** · **물리 0프레임**)
 **국면**: 눌림 **c < 0** · 밀착 **0 ≤ c ≤ SEP** · 여유 **c > SEP** (**#110 Q1**)
 **5행**: 목선(`s4Gate` 링) · 가슴(`chestY ± d`) · 허리(`waistY ± d`) · 밑단(hem 사슬) ·
        소매(`v3SeamShoulder` 패널 대역). **대역 반폭 = 그 원단의 `d`**(**도출 근거 명기**).
 **행 산출**: 중앙 · p25~p75 · **국면별 개수**. **경계(`SEP` = 2mm)와 그 도출을 화면에 명기**(**G5**).
 **자기검사(G6)**: 리포트 층 c ↔ `s4Gate` `bodyClearance` **동일 blob 대조** — **최대차·일치율 표시**.
 **판정**: **G1**(grep) · **G2**(0프레임) · **G3**(도출 목록) · **G4**(정본 3종 전량 · NaN/불가 0) ·
        **G5** · **G6** — **전부 이 판에서 판정**. **색(후보 2 잔여)은 다음 판.**
```

### §0-5 ㉣ 갈래 (사전 등록)

| 갈래 | 조건 | 귀결 |
|---|---|---|
| **A** | 재정의 성립 + 검증 통과 + 표가 3종에서 서고 G1~G6 통과 | **표 스냅샷과 함께 상신** |
| **B** | **재정의도 불성립**(끝 붙음·전이 부재) | 정지 · **차기 후보(골격 층) 상신** |
| **C** | **G 위반 발견** | 정지 · 상신 |
| **E** | 판정 불가 | 정지 · 사실만 |

### 이 판에서 바꾸지 않는 것

**물리 0프레임** · **솔버·게이트 0줄** · 문턱·정본 **0** · v2 경로 **0** · **`V2DIMS` off** ·
**색·캡처 착수 0** · **v2 값 판정 사용 0**.

### 순서

```
 §0 커밋 → 규칙 커밋(수치 0) → ㉠ 도출 → ㉡ 검증 → ㉢ 표+게이트 → 등재·상신
```

---

## §1 규칙 확정 — **수치 대입 0**

> 구현·실행 «전»에 커밋한다(v3-40 규율 · v3-51 과 같은 순서). **아래에 숫자는 하나도 없다.**

### §1-1 chestY 재정의 — **근거는 «규격 의미론»이지 데이터가 아니다**

```
 **바꾸는 것은 «정의»다.** 옛 정의(「몸통 둘레의 «내부» 최대」)는 **v3-51 §1-3 이 우리가 «가정»한 것**이고,
 v3-51 §3-2 ③ 이 **이 몸에서 그 형태가 없음을 반증**했다.
 **새 정의의 근거는 «의류 치수 규격의 의미론»(ISO 8559 계열) 이다** —
 「**가슴둘레 = 겨드랑이 «밑»의 수평 최대 둘레**」.
 **데이터가 새 정의를 «정당화»하지 않는다** — 데이터가 한 일은 **옛 정의의 반증**뿐이다.
 (이 구분을 명시하는 이유: 결과를 보고 규칙을 고치는 것[함정 14 계열]과 «정의의 출처를 밖에서
  가져오는 것»은 다르다. 출처가 저장소 «밖»의 규격이므로 **사후 조정이 아니다**.)
 **부수 효과 — 팔 배제 기준이 «불필요»해진다**: 정의역이 겨드랑이 «아래»로 잘리므로
 **팔이 평면에 들어오는 대역에서 아예 재지 않는다**. ⟹ **골격 0 · v2 임포트 0**(v3-51 갈래 C 해소).
```

### §1-2 도출 규칙

```
 **기계는 v3-51 §1-1 그대로다**(평면 정확 단면 → union-find 성분 → 축점 몸통 고리 →
 monotone chain 볼록 껍질 둘레 C(y)). **규칙 «절»만 바꾼다.**

 **`Y_ARM`** = `Y_LOW` «위»로 올라가며 단면 성분이 **1 → >1** 로 바뀌는 **«첫» 전이 높이**.
    **가랑이(`Y_LOW`)와 «같은 계열»의 위상 사실**이고, `TOL_SELF` 까지 **이분**한다.
    (아래에서 위로 «첫» 전이를 잡는다 — v3-51 이 «위에서 아래로 첫 성분 ≥3» 을 잡아
     팔 융합 대역을 집었던 것과 **방향이 반대**이며, 그 방향 차이가 규칙 교체의 내용이다.)
 **`chestY`** = **`Y_ARM` 직하의 «순수 몸통 대역» 상단** = **이분 구간의 «아래쪽 끝»**
    (성분이 아직 1인 마지막 높이). **새 수 0** — 이분이 이미 `TOL_SELF` 까지 좁힌 값이다.
    `C_chest` = 그 높이의 몸통 껍질 둘레.
    **규격 의미론과의 대응**: C(y) 가 그 대역에서 «어떻게 움직이든» 정의는 **「겨드랑이 밑」**이다.
    v3-51 §3-1 의 **단조 증가 사실은 「그러므로 최대가 상단에 온다」는 «정합 확인»**이고,
    **정의의 근거가 아니다**(위 §1-1).
 **`waistY`** = 구간 **(`Y_LOW`, `chestY`)** 안 C(y) 의 **«내부» 최소** —
    **v3-51 §1-3 의 원 규칙 그대로**다. **끝에 붙으면 갈래 B**(격자 이웃 비교 · 문턱 0).
```

### §1-3 한계 — **사전 등재**

```
 처진 팔 포즈에서 **`Y_ARM` 이 «해부학적 겨드랑이»보다 낮게 잡힐 수 있다** —
 아래에서 올라갈 때 평면에 **먼저 들어오는 것이 «손끝»** 일 수 있기 때문이다.
 그 경우 `chestY` 는 **실제 가슴보다 아래**가 되고, **둘레는 «작게»** 나온다 ⟹ **보수적 오차**.
 **값으로 확인해 등재한다**: 전이 높이에서 «새로 생긴 성분»들의 **|x| 위치를 인쇄**하고,
 **팔 원위(손·전완)인지 근위(상완·겨드랑이)인지 «사실»만** 적는다. **보정하지 않는다.**
```

### §1-4 5행 표 규칙

```
 **값 c** = 정착 blob 의 **정점별 몸 부호거리**(`sampleSdf(bodyG, ·)`) — **`v3O4` 와 같은 계기**.
   **물리 0프레임**(주입만).
 **국면**(#110 Q1): **눌림 c < 0** · **밀착 0 ≤ c ≤ `SEP`** · **여유 c > `SEP`**.
 **행 5개와 그 정의역**
   ① **목선** = `s4Gate` 가 쓰는 링(`neckF` ∪ `neckB`) — **같은 집합**을 쓴다(함정 12 규범).
   ② **가슴** = **몸통 패널**(front · back) 정점 중 |y − `chestY`| ≤ **d**
   ③ **허리** = 몸통 패널 정점 중 |y − `waistY`| ≤ **d**
   ④ **밑단** = 밑단 사슬(앞판 j=0 행 ∪ 뒤판 j=0 행) — 하네스 `hemIx` 와 **같은 정의**
   ⑤ **소매** = 소매 패널(`slv`) 정점 전량
 **대역 반폭 = 그 원단의 `d`** — **도출 근거**: `d` 는 그 원단의 **격자 간격**(등재 사다리값)이고,
   **표본 한 칸**이다. 대역을 그보다 좁히면 **한 줄도 안 잡힐 수 있고**, 넓히면 **다른 높이가 섞인다**.
   v2 의 ±2.5cm 는 **v2 슬라이스 간격에서 온 v2 문언**이라 v3 에서 쓸 근거가 없다(전략 처방).
   ⟹ **새 수 0**(원단마다 값이 다르고, 전부 등재 사다리에서 온다).
 **행 산출**: **중앙 · p25~p75 · 국면별 개수 · 표본 수/정의역 수**.
 **G5**: 화면이 **경계 `SEP` = 2mm 와 그 도출**(`SEP = 2 × THICK`)을 **스스로 밝힌다**.
 **G6 자기검사**: **같은 blob** 에서 리포트 층의 c 와 `s4Gate` 의 `bodyClearance` 를 대조해
   **최대차**와 **부호 일치율**을 낸다. **v2 의 `sleeveSignAgreePct` 를 대체한다**(#110 Q3).
```

### §1-5 이 규칙이 쓰는 수 — **손 상수 0 확인**

| 쓰는 수 | 출처 | 새 수인가 |
|---|---|---|
| `AXIS_Z` · `Y_TOP` · `Y_NECK` | `garmentScene` 몸 도출(등재) | 아니오 |
| `TOL_SELF` | `consts.ts`(v3-12) | 아니오 |
| `SEP` = 2 × `THICK` | `consts.ts`(#110 Q1) | 아니오 |
| **대역 반폭 = `d`** | **원단별 등재 사다리값**(gray 9 · swim 9 · sweat 8 mm) | 아니오 |
| y 격자 1cm | 등재 이산화 규약 · **진단 전용**(이분·정밀화가 `TOL_SELF` 까지) | 아니오(판정량 아님) |
| — | **그 외 없음** | — |

---

## §2 ㉠ 재정의 도출 — **성립**

계기 `scripts/v3BodyLevels.ts`(§1 규칙의 인쇄) · 기계·규칙 정본은 **`src/v3/bodyLevels.ts` 하나**다
(브라우저 워커와 Node 가 **같은 모듈**을 쓴다 — 함정 22「같은 것을 두 계기가 다르게 본다」 예방).

```
 몸 정점 15882 · 삼각형 28880 · 축 z 1.605cm
 등재량 Y_TOP 146.870 · Y_NECK 150.046cm · TOL_SELF 0.1mm · 격자 1.0cm(진단 전용)
 **Y_LOW** (가랑이 · 성분 1→2)          **82.830cm**
 **Y_ARM** (팔 첫 출현 · 성분 1→>1)     **135.370cm**
 **chestY**(= Y_ARM 직하 순수 몸통 상단) **135.362cm** · **C 99.200cm**
 **waistY**(= (Y_LOW, chestY) 내 C 내부 최소) **107.585cm** · **C 68.039cm**
 허리 격자 색인 **24/51** — 끝이 아니다(**내부 극값 · 갈래 B 불발화**)
```

| 검증 항목(§0-3) | 결과 |
|---|---|
| **결정성 2회** | **동일** — 서명 `0.828304896/1.353695521/1.353617396/1.075849976/0.992002335/0.680387515` 2/2 |
| **순서** `Y_NECK > chestY > waistY > Y_LOW` | **성립** (150.046 > 135.362 > 107.585 > 82.830) |
| **물리 정합** `C(chestY) > C(waistY)` | **성립** (99.200 > 68.039) |
| **v3-51 관찰(≈108cm)과 같은 자리인가** | **예** — v3-51 진단표의 격자 최소 108cm ↔ 이 판의 정밀 **107.585cm**. **같은 자리다** |

### §2-1 §1-3 한계 — **실제로 발화했다**(보수적 오차 · 보정 0)

```
 `Y_ARM` 에서 «새로 생긴» 성분 **2개**, 각각 **선분 6개 · |x| 78.312~78.414cm**.
 이 몸의 어깨 반폭은 18cm 대이고 팔 끝은 |x| 86~88cm 대다(v3-51 진단표) ⟹
 **새로 생긴 성분은 «팔 원위»(손·손끝) 다. «겨드랑이»가 아니다.**
 ⟹ **`Y_ARM` 이 해부학적 겨드랑이보다 «낮게» 잡혔다** — §1-3 이 사전 등재한 그대로다.
 결과: `chestY` 가 실제 가슴보다 아래이고 **둘레가 «작게» 나온다 ⟹ 보수적 오차**.
 **보정하지 않았다.** 사실만 등재한다.
```

### §2-2 v2 참고 병기 — **판정 불사용**(§0-3 조항 · `V2DIMS` 미사용)

v2 값은 **기존 하네스**(`PATTERNCORE=1 npm run check:pattern`)의 인쇄를 그대로 인용한다.
**새 파일 0 · v3 경로에 v2 임포트 0.**

| 채널 | **v3(이 판)** | v2 `bodyMeasure` | 차 |
|---|---|---|---|
| 가슴 높이 | **135.36cm** | 125.54cm | +9.82cm |
| 가슴 둘레 | **99.20cm** | 84.01cm | +15.19cm |
| 허리 높이 | **107.58cm** | 103.04cm | +4.54cm |
| 허리 둘레 | **68.04cm** | 64.90cm | +3.14cm |
| (참고) 상한 | `Y_ARM` **135.37cm** | `shoulderJointY` 137.54cm | −2.17cm |

```
 **판정에 쓰지 않았다.** 차이의 출처는 **정의와 정의역이 다르다**는 것이다 —
 v2 는 **골격으로 팔을 뗀 정점 집합**에 **2.5cm 슬랩**을 씌워 재고, v3 는 **평면 정확 단면의
 몸통 고리**를 **볼록 껍질**로 잰다. **어느 쪽이 옳은지 이 판은 판정하지 않는다**(#110: v2 일치는
 게이트가 아니다).
 **관찰 1건(검증 0)**: 제품 UI 의 「가슴둘레」 기본값은 **100cm** 이고 v3 의 `C(chestY)` 는 **99.20cm** 다.
 **두 수의 관계는 이 판이 확인하지 않았다** — 우연일 수 있다.
```

---

## §3 ㉢ 5행 표 — **정본 3종 전부 «선다»**

계산 `src/v3/fitReport.ts`(신설) · 표시 `V3Panel` · 배선 `v3DressWorker`.
**Node(`scripts/v3FitTable.ts`)와 브라우저가 «같은 모듈»을 쓰고 값이 일치**한다(아래 3종 대조).

**경계 2.0mm**(SEP = 2 × THICK · v3-13 등재) · **대역 반폭 = 그 원단의 d**(gray/swim **9.0mm** · sweat **8.0mm**)
**높이**: 가슴 y135.36cm(둘레 99.20) · 허리 y107.58cm(둘레 68.04) · 겨드랑이 y135.37cm

| 원단 | 부위 | 중앙[mm] | p25~p75 | 눌림/밀착/여유 | 표본/정의역 |
|---|---|---|---|---|---|
| **gray d9** | 목선 | **1.00** | 1.00~2.78 | 0 / 41 / 15 | 56/56 |
| | 가슴 | 4.64 | 1.02~8.90 | 0 / 101 / 166 | 267/267 |
| | 허리 | **8.90** | 8.90~8.90 | 0 / 0 / 239 | 239/239 |
| | 밑단 | **8.90** | 7.67~8.90 | 0 / 8 / 108 | 116/116 |
| | 소매 | 5.40 | 1.06~8.90 | 0 / 370 / 752 | 1122/1122 |
| **swim d9** | 목선 | 1.12 | 1.00~1.94 | 0 / 43 / 13 | 56/56 |
| | 가슴 | 5.37 | 1.20~8.90 | 0 / 86 / 183 | 269/269 |
| | 허리 | **8.90** | 8.90~8.90 | 0 / 0 / 247 | 247/247 |
| | 밑단 | **8.90** | 7.46~8.90 | 0 / 8 / 108 | 116/116 |
| | 소매 | 5.73 | 1.37~8.90 | 0 / 330 / 792 | 1122/1122 |
| **sweat d8** | 목선 | 1.08 | 1.00~2.20 | 0 / 48 / 16 | 64/64 |
| | 가슴 | 5.32 | 1.03~8.90 | 0 / 102 / 179 | 281/281 |
| | 허리 | **8.90** | 8.90~8.90 | 0 / 0 / 268 | 268/268 |
| | 밑단 | **8.90** | 8.90~8.90 | 0 / 7 / 125 | 132/132 |
| | 소매 | 4.02 | 1.00~8.90 | 0 / 562 / 806 | 1368/1368 |

**브라우저 ↔ Node 대조: 3종 전부 «같은 값»**(같은 모듈이므로 구조적으로 성립 · 실측으로 확인).

### §3-1 ★ **자기검사(G6)가 계기 한계를 «잡아냈다»** — 이 판의 가장 중요한 사실

```
 자기검사 실측  gray 표본 10402 · **최대차 106.973mm** · 부호 일치율 **100.00%**
               swim 표본 10402 · **최대차 105.552mm** · 부호 일치율 **100.00%**
               sweat 표본 13248 · **최대차 108.898mm** · 부호 일치율 **100.00%**
 **원인 — SDF 밴드 포화(코드 사실)**: `deriveSpacing` 이 굽는 밴드는 **band = THICK + 2h**
 (`bodySdf.ts:124`)이고 이 몸에서 **8.902mm** 다. 그 밖의 정점은 `sampleSdf` 가 **밴드 상한을 돌려준다.**
 ⟹ 표의 **8.90mm 는 «측정»이 아니라 «상한»** 이다(허리 행이 중앙·p25·p75 전부 8.90 인 것이 그 증상).
 **이미 등재된 같은 사실**: v3-26 §3-1 이 O3 간극중앙을 **4원단 전부 8.902mm · 판별력 0** 으로
 처분했고, v3-48 §4 가 그 값을 **계기 동일성 보조 확인**으로 재확인했다. **새 발견이 아니라
 «그 사실이 핏 리포트에서 어떻게 나타나는가»가 이 판의 신규분이다.**
 **국면 판정은 «유효»하다** — 경계 `SEP` = **2mm ≪ 8.902mm** 라 눌림/밀착/여유의 세 칸은
 밴드 «안»에서 갈린다. **포화되는 것은 «여유» 쪽 분위수 값뿐이다.**
 ⟹ **읽는 규칙**: 8.90mm 는 «**≥ 8.90mm**»로 읽는다. **개수는 믿고, 여유 쪽 분위수는 하한으로 본다.**
 **부호 일치율 100%** 는 「눌림이 한 점도 없다」와 같은 말이다(3종 전부 눌림 0).
 **이 판은 c 의 정의를 바꾸지 않았다** — §1-4 가 `sampleSdf` 로 못박았고, 결과를 보고 고치면
 **함정 14**다. 고칠지 여부는 **전략 세션 몫**이고 아래 §5-1 에 질문으로 올린다.
```

### §3-2 게이트 판정 — **G1~G6 전량 통과**

| # | 게이트 | 판정 | 근거 |
|---|---|---|---|
| **G1** | v2 임포트 0 · v2 데이터 읽기 0 · `V2DIMS` 미사용 | **통과** | `grep -rn "from '../lib/"` **0건**. `dimsOverride` 를 넘기는 곳은 **`scripts/v3S4.ts:135` 하나**이고 v3 리포트 경로(`dressRun.prepare`)는 **넘기지 않는다** ⟹ `V2DIMS` **false**. v2 참고값은 **기존 하네스 인쇄를 인용**했을 뿐 코드에 들어가지 않았다 |
| **G2** | 물리 0프레임 | **통과** | 표는 **정착 blob 주입 + `frames:0`** 경로에서 난다(패널 「정착 상태 표시」). 솔버 스텝 **0** |
| **G3** | 손 상수 0 | **통과** | §1-5 표 — `AXIS_Z`·`Y_TOP`·`Y_NECK`·`TOL_SELF`·`SEP`·**대역 반폭 `d`**(등재 사다리) + 1cm 격자(진단 전용). **그 외 0** |
| **G4** | 정본 3종 전부 산출 · NaN·불가 행 0 | **통과** | 3종 × 5행 = **15행 전부 산출** · 표본/정의역 **전부 동일**(누락 0) |
| **G5** | 경계 자기 공개 | **통과** | 화면이 「경계 **2.0mm** (SEP = 2 × THICK(옷 두께 1mm) — v3-13 등재 · 자기충돌 분리 거리와 «같은 수»)」와 **대역 반폭·도출**을 함께 인쇄 |
| **G6** | 자기검사 값 표시 | **통과** | 화면이 「표본 N · 최대차 …mm · 부호 일치율 …%」 인쇄. **그 값이 §3-1 의 한계를 실제로 드러냈다** |

**스냅샷**: `docs/captures/v3-52-핏표/` — 표 확대 1장 + 패널 전경 1장(**sweat d8 정본** · `51d74c66`).
**색은 착수 0**(후보 2 의 잔여 · 다음 판).

---

## §4 ㉣ 갈래 판정 — **A**

| 갈래 | 조건 | 실측 | 성립 |
|---|---|---|---|
| **A** | 재정의 성립 + 검증 통과 + 표가 3종에서 서고 G1~G6 통과 | **4/4** | **예** |
| B | 재정의도 불성립(끝 붙음·전이 부재) | 전이·내부 극값 **둘 다 있다**(허리 색인 24/51) | 아니오 |
| C | G 위반 발견 | **G1~G6 전량 통과** | 아니오 |
| E | 판정 불가 | — | 아니오 |

### §4-1 이 판이 «닫은» 것 / «연» 것

```
 닫음  **chestY 가 규격 의미론으로 닫혔다**(135.362cm · C 99.200cm) — **팔 배제 기준이 «불필요»해졌다**
       (v3-51 갈래 C 해소 · **골격 0 · v2 임포트 0**)
       **waistY 107.585cm** — v3-51 관찰(≈108cm)과 **같은 자리** · **내부 극값**(색인 24/51)
       **5행 표가 정본 3종 전부에서 선다** · **G1~G6 전량 통과** · **브라우저 ↔ Node 값 일치**
       **자기검사(G6)가 실제로 «작동»했다** — 계기 한계를 값으로 드러냈다
 엶    **SDF 밴드 포화(8.902mm)** — 「여유」 쪽 분위수가 **측정이 아니라 상한**이다(§3-1)
       **`Y_ARM` 이 해부학적 겨드랑이보다 낮다**(첫 출현 = 손 원위 · 보수적 오차 · 보정 0)
       **핏 맵 «색»** — 후보 2 의 잔여(다음 판)
```

---

## §5 회귀

```
 **물리 0프레임** — 솔버 스텝 0. 표는 정착 blob 주입 경로에서 난다
 **솔버·게이트 0줄** — `solver.ts` · `s4Gate.ts` · `garmentScene.ts` **diff 0**
 신설 `src/v3/bodyLevels.ts`(도출) · `src/v3/fitReport.ts`(표) · `scripts/v3FitTable.ts`(Node 인쇄)
 수정 `scripts/v3BodyLevels.ts`(규칙 절 교체 · 기계는 공유 모듈로 이동) ·
      `src/workers/v3DressWorker.ts`(**`kind:'fit'` 1블록**) · `V3Panel.tsx`(**표시만**)
 **하네스 유입 0** — `scripts/v3S4.ts` · `s4Gate.ts` · `solver.ts` · `garmentScene.ts` 어디서도
   `fitReport`·`bodyLevels` 를 임포트하지 않는다(grep 0건)
 문턱 0 · 정본 0 · v2 경로 0 · 색·캡처(제품) 착수 0
 **npx tsc -b rc=0** · **npm run build 통과**
 **구 정본 진단 벌 재현** 끊김 **7/118/7** · 팔 숨김 후 **76(65.6‰)** · 실루엣 **9735** — 등재분과 일치
```

### §5-1 전략 세션에 올리는 질문 **1건**

```
 **「여유」 쪽 분위수가 SDF 밴드(8.902mm)에서 포화한다.** 국면 판정은 유효하지만
 「얼마나 여유가 있는가」는 8.90mm 위로 못 읽는다. 선택지(**CC 는 고르지 않았다**):
   ㉮ **그대로 두고 «≥ 8.90mm»로 읽는다**(문서·화면 표기만 · 코드 0)
   ㉯ **리포트 층의 c 를 «정확 거리»로 바꾼다**(`exactBodyDist` — 게이트가 쓰는 것 · 비용 증가)
   ㉰ **SDF 밴드를 넓힌다**(굽기 비용·메모리에 직결 — v3-13 예산 조항)
 **§1-4 를 결과에 맞춰 고치지 않았다**(함정 14). 결정 뒤 다음 판에서 집행한다.
```

---

## §6 자기 점검

| # | 항목 | 답 |
|---|---|---|
| 1 | §0 → 규칙 → 수치 git 순서 | **예** — `9bcdcb9`(§0) → `64abf32`(§1 규칙 · **수치 0**) → 구현·실행 |
| 2 | 물리 0프레임 · 손 상수 0(대역 반폭 `d` 도출 포함) | **예** — 솔버 0스텝. 대역 반폭 = **그 원단의 격자 간격 `d`**(등재 사다리값 · 원단마다 다르다) · 근거는 「표본 한 칸」이고 §1-4 가 **수치 «전»에** 적었다 |
| 3 | 규칙 변경 근거가 «규격 의미론»으로 등재됐는가 | **예** — §1-1 이 「근거 = ISO 8559 계열 의미론 · **데이터는 옛 정의의 반증만 제공**」을 **구현 전에** 못박았다 |
| 4 | v2 값 판정 사용 0 · G1 grep 원문 | **예** — v2 는 §2-2 **참고 표**뿐이고 판정 항목 어디에도 안 들어간다. grep: `from '../lib/` **0건** · `dimsOverride` 를 넘기는 곳은 `scripts/v3S4.ts:135` **하나**(v3 리포트 경로 아님) |
| 5 | 자기검사 값이 실제로 표에 나오는가 | **예** — 화면·Node 둘 다 「표본 · 최대차 · 부호 일치율 · 정의역」을 인쇄한다(스냅샷 참고). **그 값이 §3-1 의 밴드 포화를 잡아냈다** |
| 6 | #111 갱신 · #112 신설 | **#111 갱신 예**(「위상만으로는 팔을 못 뗀다」에 **「그래서 규격 의미론으로 정의를 옮겼고 팔 배제가 불필요해졌다」**를 이어 적었다) · **#112 신설 예**(5행 표 · **밴드 포화**) |
| 7 | 이 판에서 호출한 ecc 에이전트·스킬 | **0 / 0** |
