# v3-86 — 옆 틈 G 조사(사실만) · 27몸 재생성 sha 대조 · 반출 경로 승격

| | |
|---|---|
| 브랜치 | `v3-86-side-gap-audit` |
| 기점 | **`2dc025d`** (= v3-85 HEAD) |
| §0 커밋 | **`9da11cc`** — 집행 «전» 별도 커밋(sha 만 그 뒤 한 줄로 채웠다) |
| 호출 ecc 에이전트/스킬 | **0** |

---

## §0 수령 (집행 «전» 별도 커밋)

### §0-1 판정문 (전략 세션 · v3-85 §4 수령 · 원문)

```
 「갈래 E 수령 · 화면 3/3 합격 ·
   ㉠ **«제공 감소» 불성립**(옛 편입 2칸은 v3-81 에서 이미 자격 상실 · **실 변화 0**) ·
   **v3-85 정본 채택** · ㉡ 26몸 체제 = **v3-82 ㄴ 으로 검정 완료** · 형식 종결만 ·
   ㉢ **새 사실 = 1.5489m 몸에 XL 도 조립 실패** → **옆 틈 G 탐색 조사** · **귀속 0** ·
   ㉣ 수신기 **정식 승격** · 함정 36 수용」
```

★ **㉠ 정정 수령** — CC 가 v3-85 §4-1 에 「자산이 옳아졌는데 제공이 줄었다」로 올린 것은
  **불성립**이다. 그 몸은 v3-81 에서 이미 **제공 0칸**이었고(제공 목록 35 에 없었다),
  분류가 「편입 → 착용불가」로 바뀌었을 뿐 **화면이 주는 것은 처음부터 없었다**. **실 변화 0.**
  CC 가 「편입 ≠ 제공」(v3-78 §6)을 스스로 등재해 놓고 그 구분을 놓쳤다 — **CC 귀책**.

### §0-2 바꾸지 않는 것

```
 물리 코어 4파일 · **조립/탐색 코드(읽기·계기만)** · `Mannequin` · `framing`/`match`/`provide` ·
 **정본 전부**(108 원본 · v3-85 · 35 · 37 · `body-index`) · **몸 blob 27** · **문턱 0** · **처방 0**
```

### §0-3 사전 등재 기대

| # | 갈래 |
|---|---|
| **①** | (가) **하나 이상이 손 상수** / (나) 전부 유도 / (다) 그 외 |
| **②** | (가) 전량 `c87.5` 블록 / (나) `c87.5` 다수 + 타 블록 소수(**칸 명시**) / (다) 무작위 |
| **③** | (가) **실제 틈은 존재하는데 탐색 창 밖** / (나) **실제 틈이 없다**(옷이 몸을 관통·겹침) / (다) 그 외 |
| **④** | **27/27 동일**. 미달 시 칸·좌표차 상신(**귀속 0**) |

```
 ② 기대 건수 = **20** (= 16 + 4) · 함께 **보류 41칸 사유 분포**(부수)
 ③ 계기 1칸 = `c87.5-h155-s40_XL` · **대조군** `c100-h170-s45_M`(편입 칸) · **물리 0 · 저장 0**
```

### §0-4 갈래 (집행 «전» 등재)

| 갈래 | 조건 | 처분 |
|---|---|---|
| **A** | ①~⑤ 전량 | 상신(①②③ **갈래 이름까지**) |
| B | ③ 조립을 디버그로 «분리 실행» 못 함(코드 사실) | ③ 착수 0 · 나머지 · 상신 |
| C | ④ 미달 | 값 상신 · 나머지 집행 |
| D | 그 외 | 정지 |

**사후 갈래 신설 0.**

---

## §1′ 집행

### §1-①′ 옆 틈 G 코드 사실 — **갈래 (가)**(손 상수 있음)

| 항목 | 값 | 자리 | 출처 |
|---|---|---|---|
| **G 의 정의** | `GAP_SIDE` — 옆선 패널 배치의 **좌우 여유**. 패널 폭 조건 `need = 4·panelHalfWidth(py) + 2·GAP_SIDE` 에 들어간다 | `garmentScene.ts:524` | — |
| **G 초기값** | `SEP` = **2mm** (= 2 × THICK) | `:520` · `consts.ts:8` | **(나) 유도** |
| **판정 기준** | 비인접 삼각형 **최소 쌍거리 ≥ `SEP`** (`minPairDistLite`) | `:723-726` | **(나) 유도** |
| **탐색 스텝** | `GAP_SIDE *= **1.5**` | `:730` | **★ (다) 손 상수** |
| **회수** | `for (i = 0; i < **8**; i++)` | `:724` | **★ (다) 손 상수** |
| 탐색 시작점 | 매 회차 `fitDelta()` → `assemble(D_FIXED)` 재조립 | `:725-726` | 유도 |

```
 ⟹ **갈래 (가)** — 「하나 이상이 손 상수」. **두 개**다: 배율 **1.5** · 회수 **8**.
 그 둘이 정하는 탐색 폭: G 는 2mm 에서 **2 × 1.5⁷ = 34.17mm** 까지만 간다.
 ★ **이 판은 그 수를 바꾸지 않는다**(처방 0 · §0-2).
```

### §1-②′ 실패 분포 — **갈래 (다)**

**「옆 틈 G 미발견」 20칸**(사전 등재 기대 20 · **일치**)

| 가슴 | 키 | 어깨 | 실패 사이즈 |
|---|---|---|---|
| 87.5 | 155 | 40 | S M L XL |
| 87.5 | 170 | 40 | S M L |
| 87.5 | 185 | 40 | S XL |
| 100 | 155 | 40 | M XL |
| 122.5 | 155 | 40 | XL |
| 122.5 | 155 | 45 | S M |
| 122.5 | 155 | 50 | S M L |
| 122.5 | 170 | 45 | S |
| 122.5 | 170 | 50 | M |
| 122.5 | 185 | 45 | XL |

| 축 | 분포 |
|---|---|
| 가슴 | **87.5 : 9 · 122.5 : 9 · 100 : 2** |
| 키 | 155 : **12** · 170 : 5 · 185 : 3 |
| 어깨 | 40 : **12** · 45 : 4 · 50 : 4 |
| 사이즈 | S 6 · M 6 · XL 5 · L 3 |

```
 ⟹ **갈래 (다)**. (가)「전량 c87.5」 ✗ · (나)「c87.5 «다수» + 타 블록 소수」도 ✗ —
   **c87.5 와 c122.5 가 «동수»(9 : 9)** 이고 c100 이 2 다.
 ★ 무작위도 아니다(사실만 적는다): **가슴 «양 끝» 블록에 18/20 이 몰리고 가운데는 2** ·
   **키 155 가 12/20** · **어깨 40 이 12/20**. **기전 귀속 0.**
```

**부수 — 보류 41칸 사유 분포**

| 사유 | 칸 |
|---|---|
| 게이트 미통과 | **35** |
| 시간 상한 도달 | 3 |
| 비정상 장기 실행(상한 미발화 · 강제 종료) | 1 |
| 프레임 상한 도달 | 1 |
| 상한 도달 | 1 |

**착용불가 31칸 전체** — 옆 틈 G **20** · 소매산 **9** · 목 밑동 **2**

### §1-③′ 계기 1칸 — **갈래 (나)**

**실패 칸 `c87.5-h155-s40_XL`** (`CELL=… npx tsx scripts/v3GapProbe.ts` · 조립까지 · 물리 0 · 저장 0)

| 회차 | GAP_SIDE(mm) | 측정 최소쌍거리(mm) | 기준(mm) | 부족분(mm) | DELTA(mm) |
|---|---|---|---|---|---|
| 0 | 2.000 | **0.0000** | 2.000 | 2.0000 | 20.389 |
| 1 | 3.000 | **0.0000** | 2.000 | 2.0000 | 20.389 |
| 2 | 4.500 | **0.0000** | 2.000 | 2.0000 | 20.389 |
| 3 | 6.750 | **0.0000** | 2.000 | 2.0000 | 20.389 |
| 4 | 10.125 | **0.0000** | 2.000 | 2.0000 | 20.389 |
| 5 | 15.188 | **0.0000** | 2.000 | 2.0000 | 20.389 |
| 6 | 22.781 | **0.0000** | 2.000 | 2.0000 | 20.389 |
| 7 | **34.172** | **0.0000** | 2.000 | 2.0000 | 20.389 |

**대조군 `c100-h170-s45_M`(편입 칸)**

| 회차 | GAP_SIDE(mm) | 측정 최소쌍거리(mm) | 부족분(mm) |
|---|---|---|---|
| 0 | 2.000 | 1.8585 | 0.1415 |
| 1 | 3.000 | **2.8189** | **−0.8189**(통과) |

```
 ★★ **G 를 ×17.09 키워도 측정 채널이 «전혀» 움직이지 않는다** — 0.0000 → 0.0000mm(변화 **0.0000**).
   대조군은 G 를 ×1.5 하자 최소쌍거리가 **+0.96mm** 움직여 곧바로 통과한다.
 ⟹ **갈래 (나)** — 「실제 틈이 «없다»」. 최소 쌍거리가 **정확히 0** 이라는 것은
   비인접 삼각형이 **접촉/겹쳐 있다**는 뜻이고, `GAP_SIDE` 를 34mm 까지 키워도 안 벌어진다.
 ★ **문면의 괄호는 한정한다** — (나)의 「옷이 «몸»을 관통」은 **이 계기가 재는 양이 아니다**.
   `minPairDistLite(sc0.s.pos, sc0.tris)` 는 **옷↔옷** 비인접 삼각형 거리다 ⟹
   **확인된 것은 «옷 자기 겹침»이고, 몸 관통 여부는 미확인**으로 남긴다. **귀속 0 · 처방 0.**
 ★ 부수 사실: `DELTA` 도 8회 내내 **20.389mm 로 고정**이다(`fitDelta()` 가 매 회차 같은 값을 낸다).
```

### §1-④′ 27몸 재생성 sha 대조 — **1/27** ⟹ **기대 미달**

```
 절차: 드라이런 1회로 sha 대조(1/27 동일) → **좌표차를 재려면 정점이 필요**하므로
   **승격된 수신기**(`--out <scratchpad>` · 저장소 밖)로 **26몸을 받아** 저장분과 정점 비교.
   ★ 유일하게 sha 가 같은 칸은 **`c87.5-h155-s40`** — v3-85 에서 «이 코드로» 구운 새 몸이다.
```

| 항목 | 값 |
|---|---|
| sha 동일 | **1 / 27** (그 새 몸 하나) |
| 정점 비교(26몸) | **최대 좌표차 0.000119 mm** — 26몸 **전부 같은 값** |
| 중앙 좌표차 | **0.000000 ~ 0.000001 mm** |
| 다른 좌표 비율 | 51.3 ~ 53.6 % |

```
 ⟹ **비트 재현은 안 되고, 수치 재현은 된다.** 0.000119mm = **1.19e-7 m** ≈ float32 **2 ULP**.
   게이트 높이 문턱(0.0020m)의 **1/17,000** · ③b 문턱(약 1mm)의 **1/8,400**.
 ★ 이 값이 v3-82~85 를 관통한 「비트 재현 불가」 논점을 **수로 닫는다** —
   재현 실패의 규모는 **최말단 비트**이지 자산 차이가 아니다. **귀속 0 · 처방 0.**
 ★ 27번째 칸은 본순회에서 ㉮ 로 떨어져 저장 0 이고(재시도 통과), 위 in-page 대조에서
   **sha 동일**로 이미 확인됐다 — 정점 비교 표본이 26 인 이유다.
```

### §1-⑤′ 반출 경로 승격 — **완료**

```
 `scripts/v3Receiver.ts` **신설** — 포트 고정(5199 기본) · `POST /put?name=&sha256=` ·
   **sha 대조 후에만 저장**(불일치 시 **저장 0** + 409) · 파일명은 `basename` 으로 잘라
   상위 경로 탈출 차단 · **기대 수 도달 시 자동 종료**(상주 0).
 `V3Panel` 의 **동봉(base64) 방식 제거** — 이제 바이트는 수신기로 간다.
 `CLAUDE.md` 에 **「자산 반출 = 수신기」** 절 등재(다운로드에 기대지 않는 이유 + 진입).
 ★ 이 판의 §1-④ 가 그 경로의 **첫 정식 사용**이었고 **26/26 sha 대조 통과**로 작동을 증명했다.
```

---

## §2′ 자기검사

| # | 항목 | 결과 |
|---|---|---|
| ㉠ | ①②③ 표·갈래 | (가) · (다) · (나) |
| ㉡ | ④ | **1/27**(기대 미달) · 좌표차 0.000119mm |
| ㉢ | **정본 전부 diff** | **0** (108 원본 · v3-85 · 35 · 37 · 35.v3-85 · `body-index-27`) |
| ㉣ | 몸 blob 27 변경 | **0건** |
| ㉤ | 물리 코어 · `Mannequin`/`framing`/`match`/`provide` diff | **0 · 0** |
| ㉥ | `garmentScene.ts` diff | **삭제 0줄** — 순수 추가(계기 6줄) |
| ㉦ | 회귀 | `tsc` 통과 · v3-81 §2 **전량 통과** · v3-80 프레이밍 **통과** · v3-81 canon 잘림 **0/3** |

**§3 캡처 — 0장**(사전 등재 그대로).

---

## §4′ 판정 — **갈래 C**

```
 §0-4 **C = ④ 미달 ⟹ 값 상신 · 나머지 집행**. ④ 가 **1/27** 이므로 C 다.
 ①②③⑤ 는 **전량 집행**했고 갈래도 이름으로 답했다((가)·(다)·(나)).
 사후 갈래 신설 0 · 문턱 이동 0 · 처방 0.
```

### §4-1 넘기는 것

| # | 항목 | 몫 |
|---|---|---|
| **㉠** | **옆 틈 탐색이 이 20칸에서 «무력»하다** — G 를 ×17 해도 채널이 0.0000 에서 안 움직인다. 손 상수 두 개(1.5 · 8)를 키워도 소용없다는 뜻이므로 **처방은 그 축이 아니다** | 전략 세션 |
| ㉡ | 최소쌍거리 0 의 정체 — **옷↔옷 겹침**은 확인, **몸 관통 여부는 미확인**(계기가 안 잰다) | 전략 세션 |
| ㉢ | 분포가 **가슴 양 끝 + 어깨 40 + 키 155** 에 몰린다(18/20 · 12/20 · 12/20) · **귀속 0** | 전략 세션 |
| ㉣ | **비트 재현 불가의 규모 = 2 ULP(0.000119mm)** ⟹ 「재현 불가」를 자산 문제로 다루지 않아도 되는지 | 전략 세션 |
