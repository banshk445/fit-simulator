# 57 — 본 그리드 굽기 · 105칸 무인 순회 (v3-78 · 승인 예산 **58시간** · 사용자 소유)

v3-78. 브랜치 `v3-78-grid-bake`(사전 등록 · **이 CC 세션 전용**). 호출한 ecc 에이전트/스킬: **0 / 0**

## git 스탬프

```
 브랜치  v3-78-grid-bake            워크트리  /Users/banshk/projects/fit-simulator
 기점    2e7f78f (v3-77 HEAD)       직전 회차  2e7f78f
```

---

## §0 판정 수령 — **집행 «전» 커밋**

### §0-1 본 굽기 **go** 수령

```
 **근거**(전략 세션): **스모크 3/3 분류 «정확»** + **재개 «양방향» 검증**.
 ⟹ **105칸 본 굽기 개시**(v3-77 에서 처리된 3칸은 **자동 건너뜀** · 총 계정은 **108칸**).
```

### §0-2 처분 등재 **3건**

```
 ① **착용 불가 = 「ARM_G 제도 기본값」 «조건부» 판정**
    현행 착용 불가는 **전 사이즈 공통 `ARM_G` 0.4439** 위에서 나온 결과다(v3-77 §0-2 한계).
    ⟹ **실측 암홀을 수령하면 «재판정» 대상**이다. **제품 «한계»로 등재**하고,
       이번 굽기의 착용 불가는 **「현행 ARM_G 조건에서」**라는 단서와 함께 읽는다.
 ② **몸 «잔차 초과» 칸 = 편입 «유지» + 잔차 값 사실 등재**
    굽기 상한(600프레임)에 걸린 몸은 **막지 않는다**. 다만 **잔차 값을 그 칸에 병기**한다.
    ★ **계통적 «몰림»이 발견되면 집계에 «별도 열»**로 낸다(예: 특정 축 끝에 몰린다면).
    현행 알려진 초과 = **`c87.5-h155-s40`(잔차 8.60e-2)** — v3-77 §1′-3.
 ③ **병행 세션 «금지»** — 굽기 중 **이 머신의 다른 CC 작업 0**.
    근거: 벽시계가 판정 채널의 일부이고(v3-74~77 실측), **부하가 섞이면 시간 값이 오염된다**.
```

### §0-3 실행 절차 등재

```
 · **`caffeinate -i` 로 감싼다**(유휴 시스템 잠자기 차단 · 굽기가 끝나면 함께 끝난다).
 · **전원 연결**(AC) — 배터리 절전이 개입하지 않게 한다.
 · **개시 시퀀스 준수**(v3-73 §0-2) — 몸 굽기는 **v3-77 에서 이미 끝났다**(27칸 전량 `body-*.bin`).
   ⟹ 이 판은 **브라우저를 쓰지 않는다**. Node 오케스트레이터만 돈다.
 · **진행 로그 1줄/칸** — 칸 id · 시각 · 분류 · f · 초 · 게이트 · sha.
```

### §0-4 §2 집계 (사전 등록)

```
 **분류표** — 편입 n / **착용불가 n**(사유·자리별 «소계») / **보류**(정착 미도달) n ⟹ **108칸 전량 계정**
 **시간** — 칸별 실측 · 합계 · **사이즈별 / 축별 분포**(**산정 0 · 실측만**)
 **몸 잔차 초과 목록** — **계통성 여부를 «사실»로 병기**
 **sha 전량** — **파일 ↔ 인덱스 대조**
```

### §0-5 §4 갈래 (사전 등록)

| 갈래 | 조건 | 귀결 |
|---|---|---|
| **A** | **108칸 전량 계정**(분류 3종 합 = 108) | **집계 상신**(제품 화면 판 발주 준비) |
| **B** | 오케스트레이터 **반복 고장**(재개로도 진행 불능) | 정지 · 사실만 |
| **E** | 판정 불가 | 정지 · 사실만 |

### 이 판에서 바꾸지 않는 것

**`solver` / `garmentScene` / `s4Gate` / `bodyLevels` diff 0** · **문턱·정본 0** · **정본 3종 불변** ·
**제품 화면 0줄** ·
★ **오케스트레이터 «수정 금지»** — 스모크 검증분 **그대로** 쓴다.
**결함을 발견하면 «정지·상신»이고, 도중 수리는 0이다.**

---

## §0′ 개정 — 프레임 상한 조정 · 예산 개방 (**정지 → 개정 커밋 → 재개**)

### §0′-1 「오케스트레이터 수정 금지」 **예외 개방**

```
 **전략 세션 처분 · 절차 준수 조건**: **이 개정 커밋에 등재된 변경 «한 건»만** 허용.
 **그 외 수리 0 유지.** 이 절이 그 한 건의 «등재 자리»다.
```

### §0′-2 상한 규칙 등재 (**손 상수 0**)

```
 **규칙**: **「프레임 상한 = «기측정 v1 체제 정착 f» 최대 × 2」**
 **현재 기측정 최대 = 200** — 근거 칸:
   기준 칸(v3-74) **f = 180** · 스모크 `c100-h170-s45_M` **f = 180** ·
   스모크 `c87.5-h155-s40_XL` **f = 200** ⟹ **최대 200**
 ⟹ **상한 = 200 × 2 = 400**(종전 900).
 **자동 변경 0** — 정착 f 최대가 갱신돼도 **다음 «개정 커밋»에서만** 이 수를 고친다.
 구현: `scripts/v3GridRun.ts` — `const SETTLED_F_MAX = 200; const CAP = … ?? SETTLED_F_MAX * 2;`
```

### §0′-3 소급 판정 — **영향 0**(값으로 확인)

| 편입 칸 | f | 분 | 게이트 |
|---|---|---|---|
| `c87.5-h155-s40_XL` | **200** | 38.6 | pass |
| `c87.5-h155-s40_L` | **190** | 33.1 | pass |
| `c100-h170-s45_M` | **180** | 28.4 | pass |

```
 ⟹ **편입 전량 f ≤ 200 < 400** ⟹ **상한 400 으로 낮춰도 소급 영향 «0»**. 재실행 0.
```

### §0′-4 ★★ **개정 전제에 대한 반증 사실 1건**(은폐 0)

```
 §0′ 지시문은 690분 칸(`c87.5-h155-s40_M`)을 **「보류 유지 · 재실행 0」**으로 처분했다.
 **실측을 열어 보니 그 칸은 «상한에 걸린 칸이 아니다»**:
   `c87.5-h155-s40_M` — **f = 190 · 690.1분 · 「게이트 미통과」**(목선 초과비 1.0039)
   ⟹ **190프레임에서 «정착에 도달»했고**, 그 190프레임이 **690분** 걸린 것이다.
 ⟹ **상한을 400으로 낮춰도 이 칸은 «빨라지지 않는다»** — 문제는 **프레임 수가 아니라
    «프레임당 시간»**이었다.
 ★ **그 시간의 정체** — 그 구간이 **맥이 자던 11.8시간 구간**과 겹친다(v3-78 진행 보고 실측:
    경과 11.82h · CPU 54.6분 · **가동률 7.7%** · `pmset` 로그 「Wake … due to … **lid**」).
    **`sec` 는 «벽시계»로 잰다** ⟹ **690.1분은 «잠자기가 섞인» 오염값**이다.
 **처분은 그대로다**(보류 유지 · 재실행 0). **바뀌는 것은 «그 값을 어떻게 읽는가»**다.
```

### §0′-5 ★ 209시간 추정 — **정정**

```
 §0′ 지시문은 「209시간 추정은 c87.5 블록 편중 + 이상치 평균의 «과대추정 가능»」이라 적었다.
 **원인을 값으로 특정한다**: **이상치 1개(690.1분)가 «잠자기 오염»**이다.
   물리 돈 칸 8개의 벽시계[분]: **26.0 / 28.4 / 33.1 / 27.3 / 33.1 / 36.5 / 38.6 / 690.1**
   ⟹ **오염값 1개를 빼면 26.0 ~ 38.6 대역 · 평균 31.9분**
   ⟹ 남은 100칸 × 31.9분 ≈ **53시간** — **승인 예산 58시간 «안»**이다.
 ⟹ **209시간은 과대추정이 맞고, 그 원인은 «표본 편중»보다 «잠자기 오염»이 크다.**
 **산정이 아니라 실측 원칙 유지** — 블록별 실측이 나오면 갱신한다.
 **전제**: 뚜껑을 열고 AC 를 유지해야 이 대역이 유지된다(가동률 100% 가정).
```

### §0′-6 함정 14 **경계** 등재

```
 **목선 초과비 문턱 «무변경»**. 보류 5건(**1.0011 / 1.0039 / 1.0083 / 1.0164 / 1.0199**)은
 **보류 유지**한다. **결과에 맞춰 문턱을 움직이지 않는다**(함정 14).
 ★ **작은 몸 계통성은 «지금» 판정하지 않는다** — 현재 보류 5건이 **전부 `c87.5` 블록**이지만
   **그 블록만 돌았기 때문**일 수 있다(표본 편중). **§2 집계의 «별도 열»에서 판정**한다. **처분 0.**
```

### §0′-7 예산 처분 · 로그 구분

```
 **예산 «개방»**(사용자) — **완주까지 계속** · **매일 진척 확인**.
 **④ 로그 구분** — 보류 사유를 **「게이트 미통과」 / 「상한 도달」 / 「정착 미도달」** 셋으로 가른다.
   **상한에 닿아 멈춘 것**과 **상한 «안»에서 정착을 못 본 것**은 다른 사실이기 때문이다.
   (이 표기 변경은 상한 조정에 «딸린» 것이고, §0′-1 이 허용한 **그 한 건 안**이다.)
```

### §0′-8 정지 시점 상태 — **미완 칸 미기록 재확인**

```
 정지 «전» 완료 **9칸** ⟹ 정지 «후» 완료 **9칸**(동일) ⟹ **돌던 칸은 기록되지 않았다**
 (v3-77 §2′-3 1단계와 같은 성질이 본 굽기에서도 성립).
 분류: **편입 3 · 보류 5 · 착용불가 1**.
```
