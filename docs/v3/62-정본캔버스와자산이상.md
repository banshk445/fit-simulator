# v3-81 — 정본 화면 캔버스 · 제공 목록 정정(35) · 자산 이상 조사

**물리 0프레임 · 재생 전용**

| | |
|---|---|
| 브랜치 | `v3-81-canon-and-asset` |
| 기점 | **`b464217`** (= `v3-80-product-polish` HEAD) |
| 워크트리 | `/Users/banshk/projects/fit-simulator` |
| 호출 ecc 에이전트/스킬 | **0** |

---

## §0 수령 (집행 «전» 커밋)

### §0-1 판정문 (전략 세션 · v3-80 §5 수령 · 원문)

```
 「갈래 A 수령 · 화면 합격 8/8 · ㉠ V3Product.tsx 잠금 해제(캔버스 폭 1줄 한정) ·
   ㉡ c87.5-h155-s40 제공 제외 37→35(분류 편입 유지 · 재굽기 미결 = 승혁) ·
   ㉢ 등재만 · ㉣ 문구 정본 확정」
```

### §0-2 바꾸지 않는 것

```
 물리 0프레임 · solver/garmentScene/s4Gate/bodyLevels **diff 0** · `framing.ts` **규칙 0** ·
 `match.ts` **0** · `index-merged-108.json` **수정 0** · blob **무변조** · 문턱 **0** ·
 **재굽기 0** · v2 **무삭제**
```

---

## §1-③ㄱ 코드 사실 — **몸 «높이»가 어디서 나오는가** (집행 «전» 등재)

| 자리 | 사실 |
|---|---|
| `src/components/Mannequin.tsx:295-301` | `unitScale = DEFAULT_BODY_SIZE.height/100 ÷ rawHeight` — glb 원본을 **기준 신장(170cm)** 에 맞춘다 |
| `:308` | `scene.scale.setScalar(unitScale)` (마운트 1회) |
| `:316` | `heightMultiplier = bodySize.height / DEFAULT_BODY_SIZE.height` — **키 축의 유일한 입력** |
| `:352` | `to(cur, target) = snap ? target : lerp(cur, target, t)` — **한 프레임에 도달하지 않는다** |
| `:354` | `outer.scale.setScalar(to(outer.scale.x, heightMultiplier))` — **균일 스케일** |

```
 ⟹ 몸 높이[m] = **1.70 × outer.scale.x**(그 시점의 «현재» 값) ·
   `heightMultiplier` 는 **목표**일 뿐이고 `lerp` 라 **수렴 «전»에 구우면 큰 몸이 나온다.**
 산술: h155 목표 = 155/170 = 0.91176 ⟹ 1.5500m (실측 `c100-h155-*` = **1.5500** 일치)
       `c87.5-h155-s40` = **1.6398m** ⟹ 그 시점 `outer.scale.x` ≈ **0.9646**
       (1.0 과 0.91176 «사이» — 미수렴 구간의 값이다)
 ★ **이 판은 여기까지 «사실»만 적는다** — 처방 0 · 귀속 0(§0-1 ㉢ 「등재만」).
```

---

## §2 자기검사 — **사전 등재**(집행 «전» 커밋 · 기대를 결과로 맞추지 않는다)

### §2-1 ② 제공 목록 정정의 «기대»

```
 `v1-provide-35.json` = 37 에서 **`c87.5-h155-s40_L` · `c87.5-h155-s40_XL` 둘만 제거**.
 `v1-provide-37.json` 은 **무삭제 · 대조 전용**이다(수정 0).
 그 몸의 4버튼 기대:
   S · M — **「검증을 통과하지 못해 제공하지 않습니다」**(현행 유지 · 보류)
   L · XL — **같은 문언**(**착용불가 «아님»** · 사유 열람 **없음**)
     ★ 분류 정본에서 이 둘은 **「편입」으로 남는다**(§0-2 수정 0) ⟹
       「편입인데 제공 목록에 없음」 = **경계 칸과 같은 처리**(v3-78 §6 규칙 ③ 그대로).
       **새 규칙 0** — 이미 등재된 갈래를 타는 것이다.
   착지 = **없음** · 제공 0칸 몸 **7 → 8**
```

### §2-2 검사 항목

| # | 기대 |
|---|---|
| ㉠ | 매칭 **12/12** |
| ㉡ | 버튼 상태 **108/108** — 기대는 **raw 35** 에서 독립 도출 |
| ㉢ | 착지 **27/27** — 기대표 = v3-80 §3-2 에서 **`c87.5-h155-s40` 행만 「없음」**으로 |
| ㉣ | ARM_G **9 / 혼입 0** |
| ㉤ | 프레이밍 v3-80 ㉠㉡ **재현**(0.110% · 0.91176) |
| ㉥ | **canon 3종 × 3뷰 잘림 0**(프로그램 산출) · 3종 sha·정점 불변 |
| ㉦ | G1 0 · 분류 리터럴 0 · `frames` 0 · 물리 코어 diff 0 · `tsc` 통과 |

### §2-3 착지 기대표 — v3-80 §3-2 에서 **한 행만** 바뀐다

```
 c87.5-h155-s40 : L(제공 L·XL) → **없음**
 제공 0칸 몸 **8개**: c87.5-h155-s40 · c87.5-h155-s50 · c87.5-h170-s50 · c100-h155-s40 ·
   c122.5-h170-s45 · c122.5-h185-s40 · c122.5-h185-s45 · c122.5-h185-s50
 나머지 26행은 **v3-80 §3-2 등재분과 동일**하다.
```

## §4 갈래 (집행 «전» 등재)

| 갈래 | 조건 | 처분 |
|---|---|---|
| **A** | ①②③ 전량 + §2 전량 + 캡처 4/4 | 상신(③ 은 갈래 이름까지) |
| B | ① 이 캔버스 1줄로 안 닫힘(코드 사실) | ① 착수 0 · 나머지 집행 · 상신 |
| C | ③ **(다)** | 사실만 상신 |
| D | 그 외 | 정지 |

**사후 갈래 신설 0 · 기대 사후 변경 0.**

---

## §1′ 집행

### §1-①′ 정본 화면 캔버스 — **한 줄**

```
 `V3Product.tsx` 의 코드 변경은 **`480, 672` → `840, 672` «한 줄»**이다(나머지 +3줄은 주석).
 840 의 출처 = v3-80 `v3FramingCheck` 실측 **834.5px** · v1 화면과 **같은 값**.
```

**회귀 — 3종 sha·정점 «불변»**

| 원단 | sha | 정점 | 등재값과 |
|---|---|---|---|
| gray d9 | `faf4873f…` | 10402 | **일치** |
| swim d9 | `557bc446…` | 10402 | **일치** |
| sweat d8 | `51d74c66…` | 13248 | **일치** |

**잘림 — 프로그램 산출**(`MODE=canon npx tsx scripts/v3FramingCheck.ts`)

```
 정본 화면의 몸 = 주입 0 ⟹ `parseGlb` 원본 메시.
 실측 bbox — 높이 **1.7651m** · 가로 **1.7796m** · 깊이 **0.2822m**
   ★ 하드코딩 BEXT `[1.78, 1.765, 0.282]`(v3-70 「기본 몸 캐시」)와 **같은 자리**다(교차 확인).
 480 폭: front-p x −55.1~535.1 **잘림** · side-p 잘림 없음 · back-p x −61.2~541.2 **잘림** ⟹ **2/3**
 840 폭: front-p 124.9~715.1 · side-p 370.4~468.4 · back-p 118.8~721.2 ⟹ **0/3** ✅
 ★ 몸은 3종이 «같은 메시»다(원단만 다르다) ⟹ **3종 × 3뷰 = 잘림 0**.
```

### §1-②′ 제공 목록 정정 — **35**

```
 `v1-provide-35.json` **신설** — 37 에서 `c87.5-h155-s40_L` · `_XL` **둘만** 제거.
 `v1-provide-37.json` **diff 0**(무삭제 · 대조 전용) · `index-merged-108.json` **diff 0**.
 제외 사유는 파일 «안» 메타 한 줄로 담았다 ⟹ 35 파일은 **객체**(`메타` + `provide`)다.
 소비처는 배열·객체 **둘 다** 받는다(한 줄) — 37 은 배열 그대로 읽힌다.
```

**그 몸의 4버튼 — 사전 등재 기대와 «일치»**

| 사이즈 | 분류 정본 | 버튼 | 문언 | 사유 열람 |
|---|---|---|---|---|
| S · M | 보류 | 회색 | 검증을 통과하지 못해 제공하지 않습니다 | 없음 |
| **L · XL** | **편입(유지)** | 회색 | **같은 문언** | **없음** |

```
 ⟹ 「편입인데 제공 목록에 없음」 = **경계 칸과 같은 갈래**(v3-78 §6 규칙 ③) · **새 규칙 0**.
```

### §1-③′ 자산 이상 조사 — **사실만**(처방 0 · 귀속 0)

**ㄱ 몸 생성 경로** — §1-③ㄱ 등재분(집행 «전» 커밋 `65ccd30`) 그대로.

**ㄷ v3-78 「굽기 몸 잔차 초과 1칸」이 이 몸인가 — 「예」**(v3-77 §1′-3 원문)

```
 「★ **상한 초과 1칸**(굽되 사실 남김 · 함정 25):
    **`c87.5-h155-s40` — 전진 600프레임 · 잔차 8.60e-2** ⟹ **그 몸은 슬라이더 목표에
    «완전히» 도달하지 않았을 수 있다**. **공교롭게 그 몸이 스모크 ㉡ 의 몸이다**(사실 병기).
    나머지 26칸은 4~64프레임에 잔차 0.」
 ⟹ **이 판이 새로 발견한 것이 아니다.** v3-77 이 이미 등재했고 v3-78 §0 ②가
   「몸 잔차 초과 칸 = 편입 «유지» + 잔차 값 사실 등재」로 처분했다.
 ★ 인덱스 정합: `index-merged-108.json` 은 **몸 치수도 몸 blob sha 도 «담지 않는다»**
   (필드 = `at · f · gate · sec · sha · status` · `sha` 는 **옷 상태** 페이로드다) ⟹
   **인덱스만으로는 몸 이상을 검출할 수 없다**(구조 사실).
```

**ㄹ 4칸 blob 높이 — 「전부 1.6398」**(구조상 필연)

```
 몸 파일은 **27개**(칸 108개가 아니다) — `body-<bodyId>.bin` 하나를 **4 사이즈가 «공유»**한다.
 ⟹ `c87.5-h155-s40` 의 S/M/L/XL **네 칸 전부** 같은 1.6398m 몸으로 구워졌다.
```

**ㅁ 굽기 머신·시각**

```
 몸 blob 파일 mtime — **2026-08-26 09:06**(맥 브라우저 하네스 · v3-77 §1 굽기).
 칸 4개의 인덱스 기록 — `at` 01:57:41 / 13:27:46 / 14:00:51 / 00:50:49 ·
   **`host` 필드 «없음»** ⟹ 맥 기록이다(2호기 행만 `host=DESKTOP-OV72A46` 를 갖는다).
```

**ㄴ 「지금」 다시 돌린 결과 — ★ 두 번 쟀고 «둘 다» 저장분과 다르다**

*시도 1 — 단독 호출(콘솔에서 한 몸씩)*

| 입력 | 전진 | 잔차 | 높이 |
|---|---|---|---|
| c87.5-h155-s40 | **600(상한)** | 4.87e-2 | **1.6944** |
| c87.5-h155-s45 | 600(상한) | 4.22e-2 | 1.6695 |
| c100-h155-s45 | 600(상한) | 6.41e-2 | 1.6528 |

```
 세 몸 모두 **상한에 걸렸다** ⟹ v3-77 의 체제(26칸이 4~64프레임에 잔차 0)와 **다르다**.
 ⟹ **이 측정은 생산 경로를 재현하지 못했다.** 좌표를 바꿔 다시 쟀다.
```

*시도 2 — **생산 경로 그대로**(하네스 「그리드 몸 굽기(27칸)」 버튼 · 소스 수정 0 ·
페이지 안에서 Blob 을 가로채 높이만 재고 다운로드는 막았다)*

```
 27칸 **전부 전진 3프레임 · 잔차 0.00e+0** (v3-77 은 4~64프레임 + 1칸 600 이었다 — **체제가 또 다르다**)
```

| 저장 ↔ 재생성 | 비율 |
|---|---|
| **26칸** | **1.03827** (유일값 하나 · 몸마다 다르지 않다) |
| **1칸 `c87.5-h155-s40`** | **0.98067** |

```
 ★ 그 공통 인자 **1.03827 = 1.7651 / 1.70** — glb 원본 높이 ÷ 기준 신장,
   곧 `Mannequin.tsx:299` 의 **`unitScale` 의 역수**와 «같은 값»이다.
 ★ 공통 인자로 정규화하면:
     c87.5-h155-s45 : 1.6081 / 1.03827 = **1.5488** = 저장 1.5489 ✅
     c100-h155-s45  : 1.6093 / 1.03827 = **1.5500** = 저장 1.5500 ✅
     c100-h170-s45  : 1.7651 / 1.03827 = **1.7000** = 저장 1.7000 ✅
     **c87.5-h155-s40 : 1.6081 / 1.03827 = 1.5488 ≠ 저장 1.6398** ← **이 몸만 어긋난다**
 ★ 오늘 재생성에서는 **s40 이상이 재현되지 않는다** — s40 = s45 = s50 = **1.6081** (같다).
```

**갈래 판정 — (다) 그 외**

```
 (가)의 문면은 「재생성이 **1.5489**」이고 실측은 **1.6081**이다 ⟹ **문면대로 (가)가 아니다.**
 (나)의 문면은 「재생성도 1.6398」이고 실측은 1.6081이다 ⟹ **(나)도 아니다.**
 ⟹ **(다)**. **사후 갈래 신설 0 · 기대 사후 변경 0**(문면을 고쳐 (가)로 만들지 않았다).
 ★ 다만 «가리키는 방향»은 등재한다: 공통 인자 1.03827 을 나누면 그 몸의 재생성이
   **형제값 1.5489 와 일치**하고, 26칸은 전부 일치한다 ⟹ **저장 blob 쪽이 어긋나 있다**는
   해석과 정합한다((가)의 «취지»). **단정하지 않는다 · 귀속 0 · 처방 0.**
 ★ 함께 남는 «미해결» 사실 2건:
   ① 오늘 재생성이 저장분과 **전 칸 상수 배**로 다르다 ⟹ **현재 코드로는 저장 자산을
      비트 재현할 수 없다**(재굽기 결정에 직접 걸린다 — 승혁 몫).
   ② 정착 체제가 실행마다 다르다 — v3-77 4~64프레임 / 시도1 전부 600 / 시도2 전부 3.
      **원인 귀속 0.**
```

---

## §2′ 자기검사 — **실측 전량**

| # | 기대 | 실측 |
|---|---|---|
| ㉠ | 매칭 12/12 | **12/12** ✅ |
| ㉡ | 버튼 108/108(raw 35 독립 도출) | **108/108** ✅ |
| ㉢ | 착지 27/27 · 제공 0칸 **8** | **27/27 · 8개** ✅ (§2-3 목록과 동일) |
| ㉣ | ARM_G 9 / 혼입 0 | **9 / 0** · ㉡㉢ 금지어 **0** ✅ |
| ㉤ | 프레이밍 v3-80 재현 | **0.110% · 0.91176**(편차 0.000%) ✅ |
| ㉥ | canon 3종 sha·정점 · 잘림 0 | **3/3 일치 · 잘림 0/3** ✅ |
| ㉦ | 경계 | G1 0 · 분류 리터럴 0 · `frames` 0 · 물리 코어 diff 0 · `framing.ts`·`match.ts` diff 0 · 정본 2종 diff 0 · `tsc` 통과 ✅ |

---

## §3 캡처 — `~/Desktop/v3-81-캡처/` (4장)

| # | 파일 | 화면 |
|---|---|---|
| 1 | `1-canon-gray-front-손잘림해소.jpg` | `?canon=1` gray front — **손 잘림 해소** |
| 2 | `2-canon-sweat-side.jpg` | `?canon=1` sweat side — sha `51d74c66…` · 정점 13248 |
| 3 | `3-v1-제공0칸-c87.5-h155-s40.jpg` | 입력 85·158·39 → **4버튼 전부 검증 문언** · 착장 없음 |
| 4 | `4-v1-기본몸M-회귀.jpg` | 기본몸 M 회귀 — 변화 없음 |

**보기 판정은 하지 않는다 — 사용자 몫.**

---

## §4′ 판정 — **갈래 C**

```
 §4 사전 등재: **C = ③ (다) ⟹ 사실만 상신**. ③ 이 (다)로 떨어졌으므로 **C** 다.
 ★ ①·② 와 §2 와 캡처는 **전량 완료**했다(A 의 나머지 조건은 충족) — 갈래를 가른 것은
   **③ 의 결과 하나**이고, 그 자리에 «없는 갈래»를 만들지 않았다.
```

### §4-1 넘기는 것

| # | 항목 | 몫 |
|---|---|---|
| ㉠ | **저장 자산을 현재 코드로 비트 재현할 수 없다**(전 칸 상수 배 1.03827) | 승혁(재굽기 결정) |
| ㉡ | 정착 체제가 실행마다 다르다(4~64 / 600 / 3 프레임) · **귀속 0** | 전략 세션 |
| ㉢ | 인덱스가 **몸 치수·몸 sha 를 담지 않는다** ⟹ 몸 이상을 인덱스로 못 잡는다 | 전략 세션 |
| ㉣ | 35 파일이 **객체**가 된 것(메타 한 줄 요구의 귀결) | 등재만 |
