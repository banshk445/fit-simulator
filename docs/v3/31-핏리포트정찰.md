# 31 — 4/5 개시: 핏 리포트 지형 정찰 + 요건 초안 (v3-50 · **물리 0프레임 · 코드 0줄**)

v3-50. 브랜치 `v3-50-fitreport-scout`(사전 등록). 호출한 ecc 에이전트/스킬: **0 / 0**

## git 스탬프

```
 브랜치  v3-50-fitreport-scout      워크트리  /Users/banshk/projects/fit-simulator
 기점    48ffc6b (v3-49 HEAD)       직전 회차  48ffc6b
 커밋    7458611  §0 판정 기준 등록 (실행 «전»)
         (본문)   ㉠ 재고 · ㉡ 대조 · ㉢ 초안
 검사    npx tsc -b rc=0 · npm run build 통과 · **코드 diff 0 · 물리 0프레임**
```

---

## §0 판정 기준 — 실행 «전»에 등록

> 이 절을 커밋한 «뒤»에만 정찰로 간다. 이후 결과를 보고 어떤 수도 고치지 않는다.

### §0-1 배경 (전략 세션 판정 수령분)

```
 **3/5 종결 등재 확정**(v3-49 · #109). **4/5 = 파생 치수·핏 리포트의 v3 «자립» 통합.**
 이 판은 **정찰**이다 — **구현 0 · 물리 0프레임 · 코드 0줄**.
 산출은 «**지형 보고 + 요건 초안**»이고, **요건 «확정»은 전략 세션 몫**이다.
 **방향 고정(전략 판정)**: **v2 는 «접착»으로 틀린 시뮬레이션이었다**(v3 재건의 근거) ⟹
 4/5 의 목표는 «**v2 값 일치**»가 아니라 «**v3 자립 + 물리 정합**».
 **v2↔v3 대조는 «참고 채널»이며 게이트가 될 수 없다.**
```

### §0-2 ㉠ v2 핏 경로 재고 조사

```
 제품(v2 경로)이 사용자에게 내는 **핏 관련 산출물 «전량»**을 목록화한다:
   **항목명 · 정의(무엇을 재는가) · 코드 자리(파일·함수) · 입력(무엇에 의존하는가) ·
   v2 시뮬 의존 여부**(정착 상태를 쓰는가 / 패턴·몸만 쓰는가)
 **v3-30~32 등재분**(파생 치수의 **v2 은밀 의존** 발견·조치 이력)을 재인용해
 **당시 조치가 어디까지 갔고 무엇이 남았는지 «현재 코드»로 확인**한다(**추정 0**).
```

### §0-3 ㉡ v3 공급 대조표

```
 ㉠ 의 각 항목에 대해:
   **v3 정착 상태(정본 3종) + 몸 메시 + 패턴에서 계산 가능한가** ·
   **기존 계기**(`v3SeamShoulder` · `v3O4` · `s4Gate` · 하네스 `bodyObs` 등)로 **이미 재는가** ·
   **새 계기가 필요한가** · **원리적으로 v3 에 없는 입력이 필요한가**(있다면 무엇)
 파생 치수 4종(목선반폭·목선둘레·소매산·암홀깊이)은 **v3 조립이 이미 낸다**(등재분) —
 **확인만 하고 재지 않는다.**
```

### §0-4 ㉢ 4/5 요건 «초안» (등록은 전략 세션이)

```
 초안에 담을 것:
   **완결 조건 후보**(무엇이 서면 4/5 종결인가) · **게이트 후보**(안전·정합 채널 — **v2 일치 제외**) ·
   **사용자 화면/수치 판정 지점** · **예상 판 수와 비용 규모** ·
   **리스크**(swim 0.56% 재실행 유발 여부 포함 — **4/5 가 브라우저 재실행을 요구하는지**)
 **판정하지 않는다 — 초안까지만.**
```

### §0-5 ㉣ 갈래 (사전 등록)

| 갈래 | 조건 | 귀결 |
|---|---|---|
| **A** | 재고 + 대조표 + 초안 | 상신 (**요건 확정 대기**) |
| **B** | v3-30~32 조치와 현재 코드가 **어긋남**(은밀 의존 잔존 발견) | **그 사실을 최우선 등재**하고 나머지 정찰은 계속 · 상신 |
| **E** | 판정 불가 | 정지 · 사실만 |

### 이 판에서 바꾸지 않는 것

**코드 0줄 · 물리 0프레임** · 문턱·게이트·기준선 **0** · **v2 경로 0** · **정본 구성 0**.

### 순서

```
 §0 커밋 → ㉠ 재고 → ㉡ 대조 → ㉢ 초안 → 등재·상신
```

---

## §1 ㉠ v2 핏 경로 재고 — **사용자에게 나가는 핏 산출물 전량**

**코드 자리를 지목한다. 추정 서술 0** — 아래 줄 번호는 `48ffc6b`(v3-49 HEAD) 시점이다.

### §1-1 산출물 3종 — **경로가 «둘»이다**

| # | 항목 | 정의(무엇을 재는가) | 코드 자리 | 입력 | **v2 시뮬 의존** |
|---|---|---|---|---|---|
| **A1** | **핏 맵 색**(제품 기본 경로) | 옷 정점 → 몸 표면 **부호거리**[cm]. 산출 불가 표본은 `FIT_MAP_UNKNOWN_CM` | `src/workers/garmentWorker.ts:86` `computeFitCm` → `:503-512` 전송 → `src/components/Garment.tsx:1348` `setFit` → `weldedGarmentGeometry.ts:80` 정점색 | **v1/v2 워커의 정착 좌표**(`front`/`back`) + `wholeBodyCollisionMesh` | **예 — 정착 좌표 없이는 값이 없다** |
| **A2** | **핏 맵 색**(patternCore 경로) | 같은 부호거리. 몸통은 앞/뒤 «반쪽 시트», 소매는 **전신 메시** | `src/lib/patternDressCore.ts:1037-1043` `fitPerVertexOut` → `DressButton.tsx:321-327` 이벤트 → `PatternPreview.tsx:619-622` `vertexColors` | **v2 stage2b 정착 좌표**(`sim.positions`) | **예** |
| **B** | **핏 리포트 표**(수치 · 사용자 화면) | 부위 5행 × {중앙 · p25~p75 · 눌림/밀착/여유 개수} + **경계 `marginMm`** + **소매 부호 일치율** | 계산 `patternDressCore.ts:947-1005 · 1069-1080` · 표시 `DressButton.tsx:364-405` | 같은 정착 좌표 + `margins().meshMarginM` | **예** |

```
 **경로가 둘이라는 것이 이 재고의 첫 사실이다.** `showFitMap` 하나가 **두 렌더 경로**를 켠다:
   · **제품 기본**(v2 = `Garment.tsx` + `garmentWorker`) — **색만** 나온다. **표는 없다.**
   · **patternCore**(기본 off · `PatternPreview` + `DressButton`) — **색 + 표** 둘 다 나온다.
 ⟹ **지금 제품 기본 경로의 사용자는 «핏 리포트 표»를 못 본다.** 표는 플래그 뒤에 있다.
```

### §1-2 핏 리포트 표의 «정의» — 값·문턱·대역

```
 **값**   `signedClearance` = 옷 정점 → 몸 표면 **부호거리**(+ 가 뜬 것 · `patternDressCore.ts:945`)
 **문턱** **새로 만든 것이 0**이다 — 경계는 물리가 흡착 목표로 쓰는 껍질 거리
        (`margins().meshMarginM` → `fit.marginMm`)이고 **화면이 그 값을 스스로 밝힌다**
        (`DressButton.tsx:374`). v1 히트맵의 0/1/3cm 는 코드가 「눈대중 초기값」이라 적어 둔 값이라
        **재사용하지 않았다**(같은 파일 주석).
 **국면** `med ≤ 0` **눌림** · `0 < med ≤ marginMm` **밀착** · `med > marginMm` **여유**
        (`DressButton.tsx:366-370`)
 **5행** 목선(`ringIdx`) · **가슴**(`bandByY(body.chestY)`) · **허리**(`bandByY(body.waistY)`) ·
        밑단(`hemIdx`) · **소매**(`sleeveIdx`, 기준면 = **전신 메시**)
        가슴·허리 대역 반폭 **±2.5cm** — 「대역은 **몸에서 뜬다**(새 손 상수 0)」로 등재
        (`patternDressCore.ts:958-959`)
 **자기검사 1종** `sleeveSignAgreePct` — 소매 행의 **부호 신뢰율**.
        `signedClearance`(면 **와인딩 의존**)와 `insideParity`(레이 패리티 · 와인딩 무관)가
        같은 안/밖을 말한 비율. **이 마네킹은 뒤집힌 면이 있다**(`bvhFromArrays.ts:120` 실측)
        ⟹ 낮으면 **소매 행을 믿지 말라**고 화면이 스스로 말한다(`DressButton.tsx:378-381`)
```

### §1-3 «핏 인접» 수치 — 표에는 없지만 같은 결과에 실려 있다

`PatternDressMetrics`(같은 파일 `:170~285`)가 함께 내는 값 중 **핏 의미가 있는 것**:

```
 `ringLenCm`(목선 실측 둘레) · `hemFrontCm` / `hemBackCm`(밑단) · **`cuffCm`(소맷부리 착장 후 실측)** ·
 `cuffDraftCm`(제도값) · **`cuffEaseCm`(여유)** · `covPct`(노출 비율) · `maxStrain` ·
 `maxSeamGapMm` · `selfIntersections` · `insideCount`/`insideTotal`(관통) ·
 `pen.byPanel` / `pen.torsoByYCm` / `pen.sleeveByArcCm` / `pen.sleeveCross`(관통 자리 · **인쇄 전용**)
 ⟹ **콘솔로만 나간다.** 사용자 화면에 오르는 것은 §1-1 의 3종뿐이다.
```

### §1-4 **파생 치수 4종은 사용자 화면에 «없다»**

```
 `Controls.tsx` · `PatternPreview.tsx` 에 「목선 / 암홀 / 소매산」 표시 **0건**
 (유일한 문자열 매치는 `Controls.tsx:432` 의 **DEV 토글 이름**「암홀 용접」이고 치수 표시가 아니다).
 ⟹ 파생 치수는 **제도 내부값**이지 산출물이 아니다. **4/5 의 대상은 «핏 리포트»쪽이다.**
```

---

## §2 «은밀 의존» 잔존 판정 — **아니오**(잔존 없음) · 사실 2건 병기

**§0-2 가 요구한 「현재 코드로 확인」이고 추정 0이다.**

| 검사 | 명령/자리 | 결과 |
|---|---|---|
| v3 가 `pattern-meta.json` 을 읽는가 | `grep -rn "pattern-meta" src/ scripts/` | **읽기 0** — `scripts/v3S4.ts` 의 3건은 **주석·인쇄 문자열**이고, 그 파일의 `readFileSync` 는 **GLB(:63 계열)와 체크포인트(:583)** 뿐이다. 실제 읽는 곳은 **v2 하네스** `scripts/dressPattern.ts:52` 하나다 |
| v3 코드가 v2 `src/lib` 을 임포트하는가 | `grep -rn "from '../lib/…'" src/v3/ scripts/v3*.ts` | **0건**. `src/v3/*.ts` 의 임포트는 **자기 모듈 8개뿐**(`bodySdf` · `consts` · `dressRun` · `garmentScene` · `glb` · `instruments` · `solver` · `wangStretch`) |
| v3-33 의 #56 종결 문언과 현재 코드가 맞는가 | `docs/v3/18-파생치수도출.md:1007` | **맞는다** — 「파일 읽기 0 · 6축 추종 · S5 유지 · S4 회수 · **부분 접속 1/5 닫음**」 |

```
 ⟹ **갈래 B 는 발화하지 않는다.** 다만 **사실 2건**을 병기한다(둘 다 «의존»은 아니다):
 **① `V2REF` 는 소스에 리터럴로 박혀 있다** — `scripts/v3S4.ts:125-126`
    (`neckHalfWidthCm 8.12` · `armholeGirthCm 44.39` · `capHeightCm 12.31` · `necklineGirthCm 48.27` ·
     `sleeveWidthCm 18.0` · `seam{38,62,124,16}`). **파일을 읽지 않는 «대조 인쇄용 상수»**이고
    기본 판정 경로에 값을 넘기지 않는다.
 **② `V2DIMS=1` 스위치가 파생 4종을 v2 값으로 «고정»한다** — 같은 파일 `:117` · `:135`
    `dimsOverride`. **기본 off** 이고 「판정 대상이 아니라 **비교 대상**을 만드는 장치」로 등재돼 있다.
    **4/5 가 「v2 일치는 게이트가 아니다」로 고정된 이상, 이 스위치는 «참고 채널»에 머물러야 한다**
    — 게이트 경로에 들어가면 §0-1 의 방향 고정을 어긴다. **리스크 항목으로 §3 에 올린다.**
```

---

## §3 ㉡ v3 공급 대조표

**질문 4개를 항목마다 답한다**: v3 정착 상태 + 몸 + 패턴으로 **계산 가능한가** ·
**기존 계기가 이미 재는가** · **새 계기가 필요한가** · **v3 에 원리적으로 없는 입력이 있는가**.

| 항목(§1) | v3 계산 가능? | 기존 계기가 이미 재는가 | 새 계기 | v3 에 없는 입력 |
|---|---|---|---|---|
| **A1/A2 핏 맵 색**(정점별 부호거리) | **가능** — `bodyG` SDF + 정착 `pos` | **재는 것에 «가깝다»**: `s4Gate` 의 `makeBodyDistance().bodyClearance` 가 **최대 관통만** 뽑고, `v3O4` 가 **정점 전체 거리 배열**을 이미 만든다(`sampleSdf` 루프) | **작다** — 배열을 «내보내는» 층만 필요 | **없음** |
| **B-목선 행** | **가능** | `s4Gate` 가 `neckF`/`neckB` 링 · `ringM`·`ringBodyM`·`ringExcess` 를 낸다 | 링 정점의 **부호거리 분위수**만 추가 | **없음** |
| **B-가슴 행** | **가능하되 «몸 축»이 필요** | **아니오** | **필요** — `body.chestY` 에 해당하는 **v3 쪽 높이**를 몸에서 떠야 한다 | **`chestY`·`waistY` 의 v3 도출** ← **유일한 실질 결손** |
| **B-허리 행** | 같음 | **아니오** | 같음 | 같음 |
| **B-밑단 행** | **가능** | 하네스 `hemObs()` 가 **밑단 둘레·파장**을 낸다(v3-26 O1·O2) | 밑단 사슬의 **부호거리 분위수**만 추가 | **없음** |
| **B-소매 행** | **가능** | `v3SeamShoulder` 가 패널별(`sleeveR`/`sleeveL`) 대역을 이미 가른다 | 소매 정점의 부호거리 분위수 | **없음** |
| **B-`marginMm`(경계)** | **가능** | **이미 있다** — v3 의 대응 상수는 `SEP = 2×THICK = 2mm`(`consts.ts:8`) · v3-26 O4 가 **그 문턱으로** 접촉을 센다 | 없음 | **없음**(다만 **값이 다르다** — 아래 상자) |
| **B-`sleeveSignAgreePct`** | **필요 없어진다** | — | **없음** | — (아래 상자) |
| **§1-3 인접 수치**(목선·밑단·소맷부리 둘레 · 관통 · 신장 · 시접) | **전부 가능** | **대부분 이미 잰다** — `s4Gate`(관통 `penMaxM`/`penCnt` · `lambdaMax` · 시접 갭 · `ringExcess`) · 하네스 `hemObs` · `v3SeamShoulder` | 소맷부리 폐곡선 둘레 1종 | **없음** |
| **파생 치수 4종** | **이미 낸다** — v3 조립이 매 실행에 인쇄(`8.928 / 49.315 / 12.918 / 21.495cm` · v3-47 §1 · v3-48 §2 에서 **d 무관 확인**) | **예** | 없음 | **없음** |

```
 **㉮ 결손은 «한 곳»이다 — `chestY` · `waistY`.**
   v2 는 `bodyMeasure` 슬라이스(1cm)로 가슴·허리 높이를 잡는다. v3 에는 그 규칙이 없다.
   다만 **v3 는 이미 «몸에서 높이를 뜨는» 기계를 갖고 있다** — `scripts/v3S4.ts` 가 `Y_TOP`(어깨끝)·
   `Y_NECK`(목 밑동 · 벽비율 이분법)을 **손 상수 0 으로** 도출한다(#56 종결분).
   ⟹ **같은 계열의 도출을 가슴·허리에 한 번 더 하는 일**이지, 없는 층을 새로 세우는 일이 아니다.
   **이 판은 그 규칙을 «제시하지 않는다»**(정찰이다 · 처방 0).

 **㉯ `marginMm` 은 «값이 다르다» — 이것이 4/5 의 가장 큰 정의 문제다.**
   v2 경계 = 흡착 껍질 `meshMarginM`(물리가 흡착 목표로 쓰는 거리) ·
   v3 대응 = `SEP = 2mm`(옷–옷 분리 거리 · 두께의 2배).
   **둘은 «같은 이름의 다른 수»다.** v3 는 «접착»을 버렸으므로(§0-1 방향 고정)
   **흡착 껍질이라는 개념 자체가 없다.**
   ⟹ **「밀착/여유의 경계를 v3 에서 무엇으로 정의할 것인가」가 요건 확정의 핵심 질문이다.**
   후보는 §4-2 에 «질문»으로 올린다 — **이 판은 고르지 않는다.**

 **㉰ `sleeveSignAgreePct` 는 v3 에서 «필요가 사라진다».**
   그 채널은 **면 와인딩 의존 부호**를 감시하려고 만든 자기검사다(뒤집힌 면이 있는 마네킹).
   v3 는 부호를 **SDF**(`sampleSdf`)에서 얻고, 그 부호 기준은 v3-13 이 «수밀 필드»로 세운 것이다.
   ⟹ **와인딩 의존이 원리적으로 없다.** 다만 **없어진다고 «단정»하지 않는다** — 4/5 가
   **같은 자리에서 대체 자기검사 1종을 세워야 하는지**는 요건 질문이다(§4-2).
```

---

## §4 ㉢ 4/5 요건 «초안» — **판정하지 않는다. 확정은 전략 세션 몫.**

### §4-1 완결 조건 «후보»

```
 **후보 1 (좁은 판)** — **핏 리포트 표의 v3 자립**
   정본 3종의 정착 상태에서 **5행 표 + 경계 + 자기검사**를 **v2 코드 임포트 0 · v2 데이터 읽기 0** 으로 낸다.
   화면은 **v3 패널**에 붙인다(제품 기본 경로 이식은 5/5 로 미룬다).
   ⟹ 완결 = 「**v3 만으로 표가 선다**」
 **후보 2 (넓은 판)** — 후보 1 + **핏 맵 색의 v3 자립**(정점별 부호거리 배열 → v3 표시 층)
   ⟹ 완결 = 「**표 + 색이 v3 만으로 선다**」
 **후보 3 (가장 넓은 판)** — 후보 2 + **제품 «기본» 경로 전환**(사용자가 보는 핏이 v3 산출)
   ⟹ 완결 = 「사용자가 보는 핏이 v3 것이다」. **이것은 4/5 가 아니라 5/5 성격일 수 있다** — 질문.
 **CC 의 관찰(판정 아님)**: 후보 1 은 이 판의 대조표에서 **결손이 `chestY`/`waistY` 하나뿐**이라
 가장 작고, 후보 3 은 «접속 전환»이라 성격이 다르다.
```

### §4-2 요건 확정 전에 **답이 필요한 질문 4개**

```
 **Q1 경계의 정의** — v3 에서 「밀착/여유」의 경계는 무엇인가?
    v2 의 `meshMarginM`(흡착 껍질)은 **v3 에 대응물이 없다**(㉯).
    이 판은 **후보를 고르지 않는다** — 다만 결정이 **없으면 표가 서지 않는다**는 사실만 등재한다.
 **Q2 `chestY`/`waistY` 도출 규칙** — v3 가 몸에서 뜰 것인가(`Y_TOP`/`Y_NECK` 계열), 아니면
    **사용자 입력(가슴둘레 슬라이더)** 에서 올 것인가? **손 상수는 금지**(규범).
 **Q3 자기검사** — `sleeveSignAgreePct` 자리에 v3 는 무엇을 둘 것인가(㉰). 「없음」도 답이 될 수 있으나
    **명시적으로 등재**돼야 한다.
 **Q4 완결 범위** — §4-1 의 후보 1·2·3 중 어디까지가 4/5 인가.
```

### §4-3 게이트 «후보» — **v2 일치는 넣지 않았다**

**§0-1 방향 고정 그대로. 아래는 전부 «안전·정합» 채널이다.**

| # | 게이트 후보 | 근거(등재분) |
|---|---|---|
| G1 | **v2 코드 임포트 0 · v2 데이터 읽기 0** | #56 의 종결 형식 그대로(파일 읽기 0 · 임포트 0). **자동 검사 가능**(grep) |
| G2 | **물리 0프레임** — 표·색은 **정착 blob 주입**으로만 산출 | v3-48 §4 O4 계기 전례(`v3O4`) · 재실행 유발 0 |
| G3 | **손 상수 0** — 대역·경계·높이가 전부 몸/등재 상수에서 도출 | 규범(문턱 절) · #56 「손 상수 0」 |
| G4 | **정본 3종 전부에서 산출 가능**(NaN·산출 불가 행 0) | 정본 = v3-49 승격분 |
| G5 | **`marginMm` 을 화면이 스스로 밝힌다** | v2 가 이미 지킨 형식(`DressButton.tsx:374`) — **계승** |
| G6 | **자기검사 1종이 값으로 나온다**(Q3 의 답이 무엇이든) | P11 §4 형식 계승 |
| — | ~~v2 값과 ±x% 이내~~ | **게이트 아님.** v2↔v3 대조는 **참고 채널**(§0-1) |

### §4-4 판정 지점

```
 **수치 판정**  게이트 G1~G6 (CC 자체 판정 가능 — 전부 객관 채널)
 **화면 판정**  핏 맵 «색»이 들어갈 때만 필요하다(후보 2·3). **표만이면 화면 판정은 불필요**하고
              수치 대조로 닫힌다 — **후보 1 이 사용자 시간을 가장 적게 쓴다**(관찰 · 판정 아님)
 **사용자 재판정 대상 후보**  「색이 핏을 읽히게 하는가」(후보 2·3에서만)
```

### §4-5 예상 판 수·비용 «규모»

**등재 실측에서 유추한 «규모»이고 약속이 아니다.**

| 후보 | 예상 판 수 | 브라우저 재실행 | 비용 규모 |
|---|---|---|---|
| 후보 1 | **2~3판** | **0회**(G2) | 정찰 1 + 구현·게이트 1~2. 물리 실행 0 |
| 후보 2 | +1~2판 | **0회** | 표시 층 + 화면 판정 1회 |
| 후보 3 | +2판 이상 | **미상** | 제품 경로 전환 = 접속 성격(3/5 가 14판이었다) |

### §4-6 리스크

```
 **R1 `V2DIMS` 스위치의 오용** — `scripts/v3S4.ts:117·135` 가 파생 4종을 v2 값으로 고정할 수 있다.
   기본 off 이지만, 4/5 에서 「대조가 잘 맞는다」를 좇다 **게이트 경로에 들어가면**
   §0-1 의 방향 고정을 어긴다. ⟹ **G1 에 「`V2DIMS` 미사용」을 명시 조항으로 넣을 것을 제안**(초안).
 **R2 swim ③a 여유 0.56% 는 «건드리지 않는다»** — G2(물리 0프레임)를 지키면 **재실행이 없고**
   따라서 **이 리스크는 4/5 에서 발화하지 않는다**. 후보 3(제품 경로 전환)이 새 정착을 요구하면
   그때 되살아난다 ⟹ **후보 3 을 4/5 에 넣을지의 판단 재료**(v3-48 §6-3 한계 3항).
 **R3 경계 정의(Q1)가 미결이면 표가 안 선다** — 이것이 4/5 의 **선행 차단**이다.
   #56(파생 치수)이 1/5 의 선행 차단이었던 것과 **같은 자리**다.
 **R4 경로가 둘이라는 사실**(§1-1) — 「핏 리포트」를 고칠 때 **어느 경로의 것인지**가 매번 갈린다.
   제품 기본 경로에는 **표가 아예 없다**. 요건 문언이 이 둘을 갈라 적지 않으면 범위가 흐려진다.
```

---

## §5 ㉣ 갈래 판정 — **A**

| 갈래 | 조건 | 실측 | 성립 |
|---|---|---|---|
| **A** | 재고 + 대조표 + 초안 | **3/3 산출**(§1 재고 3종+인접 · §3 대조표 10행 · §4 초안) | **예** |
| B | v3-30~32 조치와 현재 코드 어긋남(은밀 의존 잔존) | **잔존 «아니오»**(§2 · 검사 3종) | 아니오 |
| E | 판정 불가 | — | 아니오 |

**상신 — 요건 «확정»(특히 Q1~Q4)은 전략 세션 몫이다.**

### §5-1 이 판이 «닫은» 것 / «연» 것

```
 닫음  **은밀 의존 잔존 «아니오»** — 검사 3종으로 판정(파일 읽기 0 · v2 임포트 0 · #56 문언 일치)
       **핏 산출물은 3종이고 «경로가 둘»이다** — 제품 기본에는 **표가 없다**
       **결손은 한 곳뿐**(`chestY`/`waistY`) — 나머지는 기존 계기에 이미 있거나 작은 추가다
       **파생 치수 4종은 사용자 화면에 없다** ⟹ 4/5 의 대상은 핏 리포트 쪽이다
 엶    **Q1 경계 정의**(선행 차단 · v3 에 흡착 껍질이 «없다») · **Q2 `chestY`/`waistY` 도출** ·
       **Q3 자기검사 대체** · **Q4 완결 범위(후보 1/2/3)**
       **`V2DIMS` 오용 리스크**(R1) — 게이트 문언에 미사용 조항 제안
```

---

## §6 회귀

```
 **코드 0줄 · 물리 0프레임** — 이 판은 솔버도 계기도 «돌리지 않았다»(읽기·grep 만).
 `git diff`(문서 제외) **0** · `npx tsc -b` **rc=0** · `npm run build` **통과**
 문턱·게이트·기준선·v2 경로·정본 구성 **전부 0**.
```

---

## §7 자기 점검

| # | 항목 | 답 |
|---|---|---|
| 1 | §0 커밋 선행 | **예** — `7458611`(§0)이 재고·대조·초안 커밋보다 앞선다(git 이력) |
| 2 | 코드 0줄 · 물리 0프레임 | **예** — 변경은 `docs/v3/31-핏리포트정찰.md` 등 문서뿐. 솔버·계기 실행 **0회** |
| 3 | ㉠ 이 코드 자리 지목인가(추정 서술 0) | **예** — 항목마다 **파일:줄**을 적었다(`garmentWorker.ts:86` · `Garment.tsx:1348` · `patternDressCore.ts:1037-1043` · `DressButton.tsx:364-405` 등) |
| 4 | v2 일치를 게이트 후보에 넣지 않았는가 | **예** — §4-3 표의 마지막 줄에 **「게이트 아님」으로 명시 배제**하고 **참고 채널**로 남겼다. 나아가 **R1** 로 `V2DIMS` 오용을 리스크 등재 |
| 5 | 은밀 의존 잔존 여부가 명시 판정됐는가 | **예 — «아니오»(잔존 없음)**. 검사 3종의 명령·자리·결과를 §2 표에 적었고, **의존은 아니나 사실인 2건**(`V2REF` 리터럴 · `V2DIMS` 스위치)을 병기했다 |
| 6 | 이 판에서 호출한 ecc 에이전트·스킬 | **0 / 0** |
