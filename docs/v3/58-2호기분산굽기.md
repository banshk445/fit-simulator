---
tags: [회차, v3, 그리드, 분산굽기]
회차: v3-78B
날짜: 2026-08-27
기점: v3-78 HEAD `6e0723b`
브랜치: `v3-78b-second-baker` (2호기 Windows 전용)
갈래: A (스모크 성립 · 연속 실행 개시)
---

# v3-78B — 2호기 분산 굽기 (역순 구간 · 출처 등재 · 병합 규약)

## 헤더 (git 스탬프)

```
워크트리   C:\Users\banba\fit-simulator (win32-x64 · DESKTOP-OV72A46)
브랜치     v3-78b-second-baker
기점 커밋  6e0723b (origin/v3-78-grid-bake HEAD · 맥 순회 브랜치)
이 회차    b86c84c (§0 개정 2건 + 수령 검증) · 58547d4 (§2 병합기) · d9f5dd3 (등재) · <§1-3 커밋>
런타임     node v25.9.0 · win32-x64 · npm 11.12.1 · tsc -b rc=0
맥 개입    0 (fetch 만 · 원격 쓰기 0 · 맥 파일 수정 0)
```

## §0-0 1차 보고 = **착수 0**(같은 판의 앞선 집행분 · 원문 보존)

첫 집행에서 **정지·상신**했다. 사유 2건, 둘 다 회신으로 해소됐다.

| # | 사유 | 해소 |
|---|---|---|
| 1 | 기점 `v3-78` 이 **원격에 없었다**(fetch rc=0 · `v3-78*` 0개 · 최신 = `v3-77-grid-pipeline` 2e7f78f) | 맥이 `origin/v3-78-grid-bake` 푸시 ⟹ `6e0723b` 확보 |
| 2 | 몸 파일 27개가 **git 에 없다** — `.gitignore` 가 `public/v3diag/*` 전량 무시(예외 = 정본 정착 blob 5개). 오케스트레이터는 몸 파일이 없으면 그 칸을 **건너뛴다**(무진행) | ㉠ **맥 전달** 확정(전략 처분) · 2호기 자체 굽기 **기각** |

★ **㉡(2호기 자체 굽기) 기각 근거**(전략 처분 등재): 몸 배열이 머신에 따라 갈릴 수 있다는
**미등재 항을 회피**한다 · 정본 몸은 **한 벌** · 출처는 **sha**. ⟹ 이 회차에서 **몸 굽기 0회**.

## §0-1 환경 전제 확인 (착수 전)

| 항목 | 값 | 판정 |
|---|---|---|
| node (2호기) | **v25.9.0** · win32-x64 | 맥 등재분(`v25.9.0` · darwin/arm64 · metrics-log:12020)과 **동일 메이저·패치** ✅ |
| `package.json` engines | `"node": "25.9.0"` | 일치 ✅ |
| `npx tsc -b` | **rc=0** | ✅ |
| 가시화 계기 → 동기 굽기 시퀀스 | **해당 없음** — 몸 조달이 ㉠(맥 전달)이라 이 회차에서 **브라우저 굽기 0회** | — |
| git identity | 2호기에 **미설정이었다** — 저장소 한정으로 맥과 같은 값 설정 | 계기 사실 |
| `core.autocrlf` | 기본 `true`(Windows) ⟹ 저장소 한정 **`input`** 으로 교체(줄끝 오염 회피) | 계기 사실 |

## §0-2 몸 27칸 수령 검증 (27/27 일치)

```
경로   public/v3diag/v3-77/body-*.bin   (맥 원 경로와 같은 하위 구조)
개수   27 / 27
크기   각 190,584 B (전 칸 동일)
대조   sha256sum -c body-shas.txt  ⟹  27개 전부 OK · 불일치 0
```

매니페스트 `body-shas.txt`(맥 산출)는 **저장소에 등재**했다 — 다음 재현이 같은 몸을 썼는지
파일 하나로 판별된다.

## §0-3 오케스트레이터 개정 2건 (「수정 금지」 **2차 예외**)

전략 세션 개정 절차로 열린 **딱 2건만** 넣었다. `b86c84c`.

| # | 내용 | 형태 |
|---|---|---|
| ① | **실행 구간 인자** | `REV=1`(칸 목록 역순) · `SLICE=a:b`(역순 적용 **후** 인덱스 · 끝 배타) |
| ② | **칸 기록 머신 출처** | `host` · `os` · `node` 3필드 + 진행 로그 1줄/칸에 `@host` |

- **목록 내용 무변경** — `cells()` 정본 108칸의 **순서·부분만** 고른다(새 칸 0 · 명명 0 · 문턱 0).
- ②의 **부재 = 맥**: 맥은 이 개정을 받지 않은 오케스트레이터로 돌고 있으므로 맥 기록에는
  세 필드가 **없다**. 그 부재 자체가 병합의 **판별자**다(§2 ㉣).
- 그 외 오케스트레이터 수정 **0** · `solver`/`garmentScene`/`s4Gate` diff **0**.

## §0-4 개정 3건째 — **칸 «시간» 상한 신설**(§0″ · 커밋 `4949265`)

절차는 지시대로 **정지 → 개정 커밋 → 재개**로 했다.

```
규칙   「상한 = **그 머신의 기측정 «정착 도달» 칸 최대 벽시계 × 2**」  ← 맥의 프레임 상한 규칙과 같은 형태
도출   2호기 기측정 최대 = 7865.0s(131.1분) = `c122.5-h185-s40_XL`  ⟹  **SEC_CAP 15730s = 262.2분**
표기   초과 시 **보류 · 「시간 상한 도달」**(기존 3분 표기의 4번째)
우선   정착 봤으면 게이트 판정 확정 → 시간 상한 → 프레임 상한 → 정착 미도달
```

- **자동 변경 0**(다음 개정 커밋에서만) · **소급 0**(기록된 9칸 재판정 0) · env `SECS=` 재정의만 허용.
- ★ **도출값의 측정 조건 병기** — 7865.0s 는 **게임이 물리 6코어를 점유한 구간**에서 잰 값이다(§4-5).
  무경합에서는 이 상한이 그만큼 **느슨하다**. **조이지 않는다** — 결과에 맞춰 문턱을 움직이지 않는다(함정 14).
- ★ **검사 granularity** — 상한은 `N_WIN` **창 경계에서만** 본다.
  `N_WIN = round(1/(DAMP·DT)) = round(1/(6·1/60)) = **10프레임**` ⟹ 실제 정지는 최대 한 창
  (오염 조건 41.4s/프레임 기준 **≈6.9분**) 넘길 수 있다.
- ★ **발화 경로 미실증** — 상한을 실제로 넘긴 칸이 아직 없다. 인위 재현(`SECS=1`)은 **실칸에 거짓 기록**을
  남기므로 하지 않았다. 확인은 인자 반영까지: 헤더 `시간 상한 262.2분` · `SECS=600` ⟹ `10.0분`.
- **정지 비용** — 진행 중이던 `c122.5-h185-s40_L` **74분을 버렸다**(미완이라 index 미기록 ·
  v3-77 §2′-3 「미완 미기록」 규약이 그대로 작동). 재개 시 **완료 9칸 전부 건너뜀** 확인.

## §1 실행 — 역순 순회

```
진입   REV=1 npx tsx scripts/v3GridRun.ts
개시   2026-08-27 12:25:13 (KST)
첫 칸  c122.5-h185-s50_XL      끝 칸  c87.5-h155-s40_S      대상 108칸(완료분 건너뜀)
상한   400프레임 (v3-78 §0′ 개정분 그대로 · 이 회차에서 손대지 않았다)
```

### §1-1 스모크

「역순 첫 칸 1개 완주 → 게이트·체크포인트·출처 필드 확인」이 규약이었다.
**실제로는 구간 인자 단위 확인(`REV=1 SLICE=0:3`)이 3칸을 완주했다** — 셋 다 **착용불가**로
물리 0프레임·즉시 종료라 시간 비용은 없었다. 사실대로 적는다(1칸 대신 3칸).

| 확인 항목 | 결과 |
|---|---|
| 역순 목록 | 첫 칸 `c122.5-h185-s50_XL` · 끝 칸 `c87.5-h155-s40_S` — `cells()` 정본의 정확한 역순 ✅ |
| 체크포인트 | 칸마다 `index.json` 즉시 기록 · 재실행 시 **건너뜀** 동작 확인 ✅ |
| 출처 필드 | 기록 3필드 + 로그 `@DESKTOP-OV72A46` ✅ |
| 게이트 | **착용불가 분기라 미확인** ⟹ 첫 «물리» 칸에서 확인한다(§1-3) |

### §1-2 착용불가 연속 8칸 — `c122.5-h185` 대역

역순 첫 두 몸(`c122.5-h185-s50` · `c122.5-h185-s45`)의 **8칸 전부 착용불가**.

| 칸 | 사유 | 수치 |
|---|---|---|
| c122.5-h185-s50_XL | 소매산 실수해 없음 | 암홀 반쪽 22.20 < 소매산 반폭 **22.85** cm |
| c122.5-h185-s50_L | 〃 | 22.20 < **25.42** |
| c122.5-h185-s50_M | 〃 | 22.20 < **29.13** |
| c122.5-h185-s50_S | 〃 | 22.20 < **28.37** |
| c122.5-h185-s45_XL | **옆 틈 G를 8회 안에 못 찾았다** | — |
| c122.5-h185-s45_L | 소매산 실수해 없음 | 22.20 < **22.58** |
| c122.5-h185-s45_M | 〃 | 22.20 < **26.12** |
| c122.5-h185-s45_S | 〃 | 22.20 < **29.93** |

★ **교차 머신 재현 1건** — `c122.5-h185-s50_S` 는 맥이 v3-77 스모크 ㉢에서 이미 돌린 칸이고,
맥 기록은 「암홀 반쪽 **22.20cm** < 소매산 반폭 **28.37cm**」였다. 2호기가 **같은 두 수**를 냈다.
⟹ **분류·도출 수치 수준의 교차 머신 일치 1건**(blob 비트 동일은 여전히 **미검증 · 미보장**).
이 칸은 **중복 칸**이므로 병합에서 **맥 우선** 규칙을 받는다(2호기분은 참고 등재).

★ 관측(판정 아님) — 소매산 반폭이 사이즈 순으로 **단조가 아니다**(XL 22.85 < L 25.42 < S 28.37 < M 29.13).
암홀 반쪽 22.20cm 는 **전 사이즈 공통 `ARM_G`** 에서 나오는 값이라 **몸이 바뀌어도 그대로**다
(v3-77 §0-2 한계 — 차트가 암홀을 주지 않는다). **기전 귀속 0 · 처방 0.**

### §1-3 첫 물리 칸 — `c122.5-h185-s40_XL` · **보류**(게이트 미통과)

```
결과   **보류** · f=190 · **7865.0s = 131.1분** · gate **fail(③a 관통 0.5794mm > 0.5mm)**
사유   「게이트 미통과」 — 상한(400) 도달이 아니라 **정착은 봤고 게이트에서 떨어졌다**
개시   12:25:32   종료   14:36:37 (KST)   상한   400프레임(v3-78 §0′ · 손대지 않음)
```

**스모크 확인 항목 마감** — §1-1 에서 미확인으로 남겼던 게이트 경로가 여기서 확인됐다.

| 확인 항목 | 결과 |
|---|---|
| 게이트 실행 | `runS4Gate` 발화 · **실패 채널·수치까지 표기**(③a 관통 0.5794mm > 0.5mm) ✅ |
| 보류 3분 표기(v3-78 §0′ ④) | 「상한 도달」 아닌 **「게이트 미통과」** 로 정확히 갈렸다 ✅ |
| 체크포인트 | 칸 종료 즉시 `index.json` 기록 ✅ |
| 출처 필드 | `host`·`os`·`node` + 로그 `@DESKTOP-OV72A46` ✅ |
| **blob·sha 경로** | **여전히 미확인** — 편입 칸이 아니라 `settled-*.bin` 을 쓰지 않는다. **첫 «편입» 칸에서 확인한다** |

★ **처리량 사실 1건(n=1 · 산정 아님)** — 같은 성격의 칸이 맥에서 **28.4 / 38.6분**이었는데
2호기는 **131.1분**이다 ⟹ **약 3.4~4.6배 느리다**. 단, **프레임 수는 같은 대역**이다
(맥 편입 f=180 / 200 ↔ 2호기 f=190) — **정착 프레임이 아니라 벽시계만 갈린다**.
⟹ 하드웨어 차이로 보이나 **기전 귀속 0** · **남은 칸 수·완주 시각 산정 0**(#81).
분담 규약의 전제(「양 끝에서 만난다」)는 **2호기 쪽 진도가 훨씬 느리다**는 사실과 함께 읽어야 한다.

★ 관측(판정 아님) — 첫 물리 칸의 실패가 **문턱 근방**이다(0.5794 vs 0.5mm · 초과 **16%**).
**처방 0 · 문턱 0 · 재시도 0.**

## §2 병합기 — 작성·단위 확인까지 (실행 0)

`scripts/v3GridMerge.ts` · `58547d4` · **기본 드라이런**(`OUT=` 없으면 파일 0개 기록).

규약은 §0 사전 등재분을 그대로 옮겼다(새로 정하는 것 0):

| 규칙 | 구현 |
|---|---|
| ㉠ 중복 칸 = **맥 우선** | `merged[id] = mac[id]` · 2호기분은 `dup[]` 에 **원문 보존**(폐기 아님) |
| ㉡ 유효성 = blob 의 게이트 통과(머신 무관) | 병합은 **판정을 다시 하지 않는다** |
| ㉢ 비트 동일 **미보장** | 중복 칸 sha 가 달라도 «불일치»로 세지 않고 **사실로만** 센다 |
| ㉣ 출처 판별 | 파일(1차) · `host` 유무(2차 · 부재 = 맥) · 어긋나면 **보고만**(자동 교정 0) |

계정 검증: **분류 합 = 108**(편입+착용불가+보류+**미기록**) · 편입 칸 `settled-<id>.bin`
**sha 파일 대조**(식은 오케스트레이터와 동일 — 헤더 `hl` 뺀 본문만 해싱).

**단위 확인 — 합성 fixture 6케이스 · 실데이터 0**

| 케이스 | 기대 | 결과 |
|---|---|---|
| 맥 단독 편입(정상) | 일치 | ✅ |
| 중복 칸(둘 다 편입 · payload 다름) | 맥 채택 · sha «다름»을 사실로만 | ✅ |
| 2호기 단독 편입(정상) | 일치 | ✅ |
| 편입인데 blob 부재 | ✗ 검출 | ✅ |
| 편입인데 파일 변조 | ✗ 검출(파일 sha ↔ 기록 sha 병기) | ✅ |
| 2호기 기록에 `host` 부재 | 출처 어긋남 보고(교정 0) | ✅ |
| 계정 합 | 9칸 기록 + 99 미기록 = **108/108** | ✅ |
| 드라이런 | 파일 쓰기 0 | ✅ |

★ **계기 사실 1건** — `WINDIR` 은 **Windows 시스템 환경변수**(`C:\WINDOWS`)라 인자명으로 쓸 수 없다.
첫 판에서 실제로 충돌해 blob 경로가 `C:\WINDOWS/settled-….bin` 으로 나갔다 ⟹
인자를 `IDX_MAC` / `IDX_WIN` / `BLOB_MAC` / `BLOB_WIN` 으로 교체했다.
**실행(양쪽 완주 후 · 별도 지시)은 하지 않았다.**

## §3 하지 않은 것 (미집행 명시)

- **맥 순회 개입 0** — `git fetch` 만 했다. 원격 쓰기 0 · 맥 파일 수정 0 · 원격 트리거 0.
- **병합 «실행» 0** — 스크립트 작성·단위 확인까지. 실데이터 대상 실행은 별도 지시 대기.
- **몸 굽기 0회** · 브라우저 계기 0회 · 화면 판정 0.
- 문턱·정본 **0** · 프레임 상한 **손대지 않음**(v3-78 §0′ 400 그대로) · 새 채널 0 · 처방 0.
- 개정 2건 **외** 오케스트레이터 수정 0.

## §4 오염·한계 (사실 등재)

1. **부하 오염(경미)** — 굽기 시작 직후 `tsc -b` 1회와 병합기 단위 확인이 **같은 머신에서 겹쳤다**.
   겹친 구간에 돌던 칸은 **전부 착용불가(물리 0프레임)** 라 `sec` 오염 대상이 아니다.
   이후로는 v3-78 §0 ③(병행 세션 금지)의 취지를 2호기에도 적용한다.
2. **절전 — 차단 확인**. 맥에서 「690분 칸 = 잠자기 오염」이 이미 등재됐다(v3-78 §0′ 반증분).
   2호기는 **사용자 조치로 껐고**, 확인은 값으로 했다: 활성 구성표(Turbo)의 `SUB_SLEEP STANDBYIDLE`
   **AC/DC 둘 다 `0x00000000`(안 함)**. ⟹ 이 순회에는 잠자기 오염 경로가 없다.
3. **시간 대조 불가** — 2호기 `sec` 은 맥 값과 **직접 비교 불가**(다른 하드웨어). 칸 간
   상대 비교만 유효하다.
4. **비트 동일 미보장** — 사전 등재분 그대로. **검증 시도 0.**

5. **★ 부하 오염(중대) — 굽기 12:25~15:25 구간에 «게임»이 같은 머신에서 돌았다.**
   실측: `Overwatch` pid 6472 가 **609%**(물리 6코어 전량) · 굽기 node 는 105%(1코어) ·
   총 프로세서 사용률 58% · 주파수 카운터가 **베이스 2592MHz** 에 붙어 있었다.
   ⟹ **131.1분(7865.0s)이라는 유일한 물리 칸 값은 이 조건에서 잰 값이다.**
   종료 후 실측: 총 사용률 **12%** · `% Processor Performance` **161%**(≈**4.18GHz**) ·
   경쟁 프로세스 없음. **게임 중의 `% Processor Performance` 샘플은 남기지 못했다** ⟹
   「몇 배 빨라졌는가」는 **다음 «무경합» 칸이 끝나야** 값으로 말할 수 있다. **현재는 미확정.**
   ★ 파생: 「맥 대비 3.4~4.6배 느리다」도 **오염 측정 기반**이다 — 무경합 값으로 **정정 예정**.
   ★ 규약 관련: v3-78 §0 ③(병행 세션 금지)의 근거가 정확히 이 오염이다. 2호기에도 적용한다.
6. **재개 손실 1건** — §0″ 개정 절차(정지→커밋→재개)로 `c122.5-h185-s40_L` **74분 폐기**.
   그 칸은 재개 후 **무경합 조건에서 처음부터** 다시 돈다(오염 없는 첫 물리 칸이 된다).

## §5 병렬 샤딩 4-way (§0‴ · 커밋 `c6d61a6`)

### §5-1 조건 ① — 단독 무경합 기준값과 **속도 정정**

```
c122.5-h185-s40_L   보류 · f=240 · 6926.6s = 115.4분 · fail(③a 관통 0.5184mm / 자기관통 교차 14)
                    ⟹ **28.86 s/프레임**(단독 · 무경합 · par 없음)
```

| 조건 | s/프레임 | 맥 대비 |
|---|---|---|
| 맥 | 9.47(28.4분÷180) · 11.58(38.6분÷200) | 1× |
| 2호기 **오염**(게임 병행) | 41.39(7865.0÷190) | 3.6~4.4× |
| 2호기 **무경합 단독** | **28.86** | **2.5~3.0×** |

⟹ **종전 등재 「3.4~4.6배」는 오염 측정 기반이었다. 「2.5~3.0배」로 정정한다.**
게임 종료 효과는 **1.43배**(41.39 → 28.86).

### §5-2 샤드 배치 (역순 · 몸 경계 정렬 · 16칸씩)

| 샤드 | `SLICE` | 첫 칸 → 끝 칸 |
|---|---|---|
| 1 | 8:24 | `c122.5-h185-s40_XL` → `c122.5-h170-s40_S` |
| 2 | 24:40 | `c122.5-h155-s50_XL` → `c100-h185-s50_S` |
| 3 | 40:56 | `c100-h185-s45_XL` → `c100-h170-s45_S` |
| 4 | 56:72 | `c100-h170-s40_XL` → `c100-h155-s40_S` |

- 경계는 **4의 배수**라 몸 단위로 갈린다(bodyCache 재사용 유지).
- 역순 **[72,108) 은 남겼다** — 맥이 순방향으로 도는 구역이다. 소진 시 재지정(코드 변경 0).
- 네 프로세스 모두 시작 시 **완료분 10칸을 합집합으로 보고 건너뜀** 확인.

### §5-3 부하 실측

| 채널 | 단독 | 5 프로세스(샤드 4 + 검증 1) |
|---|---|---|
| 클럭 | 167% ≈ 4.34GHz | **146% ≈ 3.80GHz**(전 코어 터보 −12.5%) |
| CPU 총 사용률 | 12% | 48~54% |
| RAM 여유 | 14.3GB | 12.6GB(프로세스당 463~518MB) |
| 칸당 감속 | 1× | **1.33× (f=200 칸) · 1.62× (f=240 검증 칸)** |

### §5-4 샤딩 산출 4칸

| 칸 | 샤드 | 결과 | f | 시간 | 게이트 |
|---|---|---|---|---|---|
| `c100-h185-s45_XL` | 3 | **편입** | 200 | 127.9분 | pass · sha `4b60c341…` |
| `c100-h170-s40_XL` | 4 | **편입** | 200 | 128.6분 | pass · sha `f06c2a88…` |
| `c122.5-h185-s40_M` | 1 | 보류 | 230 | 156.2분 | fail(③a 0.5133mm) |
| `c122.5-h185-s40_S` | 1 | 착용불가 | — | 즉시 | 암홀 22.20 < 소매산 23.43cm |

**2호기 첫 편입 2칸**이다(그 전까지 편입 0). 소계 **14칸** — 착용불가 9 · 보류 3 · 편입 2.

### §5-5 조건 ④ 검증 1 (대체 채널) — **전 자리 일치**

대조 방향을 먼저 정했다: 기준 기록이 **단독**에서 나왔으므로 재실행을 **부하 아래**서 돌려야
「샤딩이 값을 바꾸는가」를 잰다(단독↔단독이면 결정성만 잰다).

| 자리 | 단독 기록 | 부하 하 재실행(par 5) | 판정 |
|---|---|---|---|
| 분류 | 보류 | 보류 | 일치 |
| f | 240 | 240 | 일치 |
| ③a 관통 | 0.5184mm > 0.5mm | 0.5184mm > 0.5mm | 일치 |
| 자기관통 교차 | 14 ≠ 0 | 14 ≠ 0 | 일치 |
| 사유 | 게이트 미통과 | 게이트 미통과 | 일치 |
| `sec` | 6926.6s | 11246.2s | 불일치 — **정상**(강등된 채널) |

⟹ **병렬 부하가 값 채널을 바꾸지 않는다.**
★ **등재 문언(지시분)**: 「**대체 채널 = 게이트 수치 재현 · blob 비트 동일의 «충분조건 아님»**」 —
게이트 수치는 스칼라 몇 개이고 blob 은 정점 배열 전량이다. **조건 ④ 종결은 원안(첫 편입 칸
`c100-h185-s45_XL` blob sha 단독 대조)까지 통과해야 한다 — 미집행.**
- 검증 실행은 별도 인덱스로 격리했고(**본 계정 제외**), 끝난 뒤 파일명을 샤드 패턴 밖
  `verify-c122.5-h185-s40_L.json` 으로 옮겼다 — **건너뜀 합집합·병합 입력에서 확실히 빠진다.**
- 샤딩 산출 blob 사본을 스크래치패드에 보존했다(재실행이 덮어써도 대조 가능).

### §5-6 사용자 요청 정지 (20:50)

게임을 위해 **샤드 4개 정지**(CPU 54% → 14%). 검증 1은 완료 직전이라 1코어로 마쳤다(12:07:40 UTC).
폐기된 진행 중 칸 4개 — 샤드 1 약 78분 · 샤드 2 **약 174분** · 샤드 3 약 46분 · 샤드 4 약 45분.
**기록 14칸은 무손실**(칸 경계 체크포인트). 재개 시 완료 칸 건너뜀 · 중단 칸만 처음부터.

## 다음 수

- 연속 실행 계속(역순) · 아침 진척에 **2호기 소계 병기**.
- 양쪽 완주 후 병합 실행은 **별도 지시**.

### §5-7 재개 (23:08 KST) — 무경합 복귀 · 건너뜀 합집합 14칸

정지(20:50) → 재개(23:08:53~23:08:59)까지 **2시간 18분** 놀렸다. 인자는 정지 전과 **같은 값**이다
(`REV=1 SHARD=n PAR=4 SLICE=8:24 / 24:40 / 40:56 / 56:72`) — 오케스트레이터 수정 **0** ·
프레임 상한 400 · 시간 상한 262.2분 **그대로** · 문턱 0 · 새 채널 0.

**재개 전 무경합 확인**(값으로 했다 · 게임 종료 상태):

| 채널 | 재개 직전(단독) | 4 샤드 가동 중 |
|---|---|---|
| `% Processor Performance` | **170% ≈ 4.41GHz** | **143% ≈ 3.71GHz**(전 코어 터보 −16%) |
| CPU 총 사용률 | 5% | 59% |
| RAM 여유 | 14.6GB | 12.7GB(굽기 프로세스 449~475MB × 4) |
| 경쟁 프로세스 | **없음**(상위 8개에 게임 0 · Overwatch 부재) | 〃 |

⟹ §4-5 오염 조건(게임 609% 점유 · 베이스 2592MHz 고정)은 **해소**됐다. 이 구간 산출은
§5-1 무경합 기준(28.86 s/프레임 단독)과 **같은 계열**이며, 병렬 감속만 §5-3 형태로 얹힌다.

**건너뜀 합집합** — 네 샤드 헤더 전부 `완료분 **14칸**`(정지 전 10칸 + 샤딩 산출 4칸).
합집합이 샤드 간에 실제로 전파됨을 재개가 다시 확인했다(§0‴ 규약 작동).

| 샤드 | 건너뛴 칸 | **재개 후 첫 실행 칸** | 남은 칸 |
|---|---|---|---|
| 1 | 4(보류 3 · 착용불가 1) | `c122.5-h170-s50_XL` | 12 |
| 2 | 0 | `c122.5-h155-s50_XL` | 16 |
| 3 | 1(편입) | `c100-h185-s45_L` | 15 |
| 4 | 1(편입) | `c100-h170-s40_L` | 15 |

- 정지 때 폐기된 진행 중 4칸(§5-6)은 **처음부터** 다시 돈다 — 미완은 기록되지 않았으므로
  건너뜀 대상이 아니다(v3-77 §2′-3 「미완 미기록」 규약 그대로).
- 2호기 소계 **변동 없음 — 14칸**(착용불가 9 · 보류 3 · 편입 2). 재개 자체는 산출 0.

★ **조건 ④ 원안은 여전히 미집행** — 첫 편입 칸 `c100-h185-s45_XL` 의 blob sha **단독** 대조는
샤드가 도는 동안 할 수 없다(단독 조건이 아니게 된다). **샤드 정지 구간에서만 집행 가능** ·
현재 지위 = **미집행**(§5-5 대체 채널만 통과).

### §5-8 전력 설정 상향 (23:3x · 사용자 조치) — **칸 도중 변경 = 혼합 조건 4칸**

사용자가 CPU 전력을 올렸다. 굽기 인자·코드는 **손대지 않았다**(오케스트레이터 수정 0).

| 채널 | 전(23:29 · 단일 표본) | 후(23:40 · 5표본 평균) |
|---|---|---|
| `% Processor Performance` | 146% ≈ 3.80GHz | **157.2% ≈ 4.08GHz**(158·157·157·157·157) |
| 샤드당 코어 | 1.05 × 4 | 1.05 × 4 (무변화) |
| 전원 구성표 | Turbo | Turbo(동일) |
| 노출된 설정 | — | **`PROCTHROTTLEMIN` AC = `0x00000064`**(최소 프로세서 상태 100%) |

- `PROCTHROTTLEMAX` · `PERFBOOSTMODE` · `PERFEPP` 는 이 구성표에서 **숨김**이라 값으로 못 읽었다 —
  「무엇이 바뀌었나」의 완전한 목록은 **미확인**. 확인된 것은 위 한 줄과 **클럭 실측**뿐이다.
- 측정 방법이 전후로 다르다(단일 표본 ↔ 5표본 평균) — 그래도 후자는 표본 산포가 1%p라
  **+7~8%** 라는 방향·크기는 값으로 선다.
- 단독 무경합 170%(§5-7)에는 못 미친다 — 4코어 동시 부하의 전 코어 터보 한계가 그 아래다.

★ **혼합 조건 4칸** — 변경이 칸 **도중**에 들어갔다. 23:09 개시분 4칸(`c122.5-h170-s50_XL` ·
`c122.5-h155-s50_XL` · `c100-h185-s45_L` · `c100-h170-s40_L`)은 앞 ~30분이 146%, 나머지가 157%다.
⟹ **이 4칸의 `sec` 은 두 조건 어느 쪽의 깨끗한 측정도 아니다.** §5-1 속도표 갱신의 근거로는
**그 다음 칸부터** 쓴다. 재실행으로 조건을 맞추지는 **않았다**(미완 폐기 비용 4칸 × ~30분).
★ **추정(확정 아님)** — 단일 스레드 CPU 바운드라 속도가 클럭에 대체로 비례한다고 보면
물리 칸 128분 → **약 119분** · 남은 58칸 벽시계 32h → **약 30h**. **검증은 완주 칸 `sec` 로 한다.**

### §5-9 샤드 2 **세그폴트**(01:04:55 · exit 139) — 계기 사실

```
증상   /c/Program Files/nodejs/npx: line 65:  1275 Segmentation fault  "$NODE_EXE" "$NPX_CLI_JS" "$@"
칸     c122.5-h155-s50_XL  (23:08:55 개시 → 01:04:55 사망 · **116.0분 폐기** · 미완이라 index 미기록)
```

| 확인 항목 | 값 |
|---|---|
| 나머지 샤드 | 1·3·4 **정상**(같은 시각 코어 점유 1.05~1.06 · 116분 무중단) |
| 시스템 RAM | 여유 **13.4GB** — 시스템 OOM 아님 |
| Windows 오류 보고(이벤트 1000/1001/1026) | **없음** — 원인 모듈 미확인 |
| 프로세스 RSS | 사망 직전 관측 없음(직전 표본 461~502MB로 나머지와 동급) |

★ **원인 미확정.** 세그폴트는 네이티브 층에서 나는 신호인데, 이 판에서 얻은 증거로는
「어느 층인가」를 못 가른다. **기전 귀속 0 · 처방 0 · 코드 수정 0.**
★ **이 칸의 이력이 특이하다** — `c122.5-h155-s50_XL` 은 정지 전 판에서도 **약 174분** 돌다가
사용자 정지로 폐기됐다(§5-6). 두 판 합쳐 **290분을 쓰고 기록 0**이다.
**「이 칸이 유별난가」는 아직 판정 못 한다** — 재기동으로 세 번째 시도를 건다.
판별 기준을 미리 적어 둔다: **같은 칸에서 또 죽으면 「칸 결부」** · 다른 칸에서 죽으면 **「산발」**.

**조치** — 인자 동일(`REV=1 SHARD=2 PAR=4 SLICE=24:40`)로 **재기동**(01:0x). 건너뜀 규약대로
기록된 칸만 건너뛰고 이 칸은 처음부터 돈다. **기록 손실 0**(칸 경계 체크포인트).

### §5-10 재개 후 산출 4칸 · 속도 (혼합 조건 · **정정 보류**)

| 칸 | 샤드 | 결과 | f | 시간 | s/프레임 |
|---|---|---|---|---|---|
| `c100-h185-s45_L` | 3 | 보류 | 180 | 86.1분 | 28.69 |
| `c100-h185-s45_M` | 3 | 착용불가 | — | 즉시 | — |
| `c100-h170-s40_L` | 4 | **편입** | 200 | 93.6분 | **28.08** |
| `c122.5-h170-s50_XL` | 1 | **편입** | 190 | 109.6분 | 34.61 |

**소계 18칸** — 착용불가 10 · 보류 4 · **편입 4**.

★ **같은 몸 대조 1건**(가장 센 근거) — `c100-h170-s40` 의 XL(par 5 · 상향 전) **38.58** ↔
L(par 4 · 상향 후) **28.08** ⟹ **1.37배**. 몸·f 가 같으므로 단가 차이로는 설명되지 않는다.
★ **그러나 몸이 바뀌면 단가가 실제로 다르다** — 같은 조건에서 34.61(`c122.5-h170-s50_XL`) ↔
28.08(`c100-h170-s40_L`) = **1.23배**. ⟹ **s/프레임을 몸 간에 섞어 비교하면 안 된다.**
★ **par 4 ↔ 전력 상향은 아직 못 가른다** — 둘이 같은 시점에 바뀌었다. **귀속 0.**
★ 위 4칸은 전부 §5-8 **혼합 조건**이다 ⟹ §5-1 속도표 정정은 **온전한 새 조건 칸**
(`c100-h185-s45_S` · 00:35:12 개시)이 끝난 뒤에 한다. **현재 정정 보류.**

★ **분류 전제 하나가 깨졌다** — `c122.5-h185-s50` 은 4칸 전부 착용불가였는데
**`c122.5-h170-s50_XL` 은 편입**이다. ⟹ 「c122.5 대역 ≈ 착용불가」로 잡았던 **잔여 시간 추정 전제 폐기**.
키가 내려가면 통과한다는 **관측**이다(기전 귀속 0). 남은 c122.5 칸 상당수가 물리 칸이 된다 ⟹
남은 55칸 벽시계 추정 **약 22~27시간**(샤드 2 지배 · 세그폴트 재발 시 더).

### §5-11 속도 정정 — 같은 몸 3점 사다리(**1.60배**) · §5-1 표의 몸 혼합 결함

새 조건(par 4 · 전력 상향) **순수** 칸이 나왔다. `c100-h170-s40_M` · 편입 · f=180 ·
4330.5s(72.2분) · **24.06 s/프레임** · 00:42:43 개시(전력 상향 23:3x **이후**) ⟹ 혼합 아님.

**같은 몸 `c100-h170-s40` 3점 사다리** — 조건만 바뀌었고 몸·패턴은 같다:

| 사이즈 | 조건 | f | 시간 | s/프레임 | 기준 대비 |
|---|---|---|---|---|---|
| XL | par 5 · 상향 전 | 200 | 128.6분 | 38.58 | 1× |
| L | par 4 · **혼합** | 200 | 93.6분 | 28.08 | 1.37배 |
| **M** | par 4 · **새 조건 순수** | 180 | 72.2분 | **24.06** | **1.60배** |

- 중간값 28.08 이 두 값 사이에 앉는 것이 **혼합 조건 해석과 일치**한다(앞 26분 옛 · 뒤 67분 새).
- **par 4 ↔ 전력 상향의 분해는 여전히 불가**(동시에 바뀌었다). **총 이득 1.60배만 값으로 선다.**

★ **§5-1 표의 결함 등재** — 「2호기 단독 무경합 **28.86**」은 `c122.5-h185-s40_L` 에서,
「맥 9.47 · 11.58」은 또 다른 칸에서 나온 값이다. **몸이 다르면 프레임 단가가 1.23배까지 다르다**
(§5-10 확인) ⟹ **그 표는 몸을 섞어 비교하고 있다.** 「맥 대비 2.5~3.0배」도 같은 결함을 물려받는다.
**정정 방식은 값을 고쳐 쓰는 게 아니라 «비교 단위»를 몸으로 묶는 것이다**:
- **같은 몸 안에서만** s/프레임을 비교한다.
- 교차 머신 속도비는 **맥이 같은 몸을 돌린 칸이 나온 뒤** 다시 낸다. 그전까지 「2.5~3.0배」는
  **참고값**으로 강등한다(폐기 아님 · 근거 병기).

**소계 19칸** — 착용불가 10 · 보류 4 · **편입 5**. 남은 53칸(샤드 1/2/3/4 = 11/16/12/13).
잔여 벽시계 추정 **약 17~25시간**(샤드 2 지배 · 착용불가 비율이 폭의 대부분 · 세그폴트 재발 시 더).

**§5-11 보강 — 두 번째 같은 몸 사다리(독립 확인)**

`c100-h185-s45_S` · 편입 · f=210 · 5307.3s(88.5분) · **25.27 s/프레임** · 00:35:12 개시(새 조건 순수).

| 몸 | 상향 전(par 5) | 새 조건 순수(par 4) | 이득 |
|---|---|---|---|
| `c100-h170-s40` | 38.58 (XL) | 24.06 (M) | 1.60배 |
| `c100-h185-s45` | 38.36 (XL) | **25.27 (S)** | **1.52배** |

- 새 조건 순수 칸 2개가 **24.06 · 25.27**(산포 5% 이내)로 모인다 ⟹ 조건 이득 **1.5~1.6배**는
  서로 다른 두 몸에서 각각 성립한다. **분해(par 4 ↔ 전력)는 여전히 불가.**
- ★ **몸 `c100-h185-s45` 4칸 완결** — XL 편입 · L 보류 · M 착용불가 · S 편입.
  **한 몸 안에서 네 사이즈가 세 분류로 갈린 첫 사례**다(관측 · 기전 귀속 0).

**소계 20칸** — 착용불가 10 · 보류 4 · **편입 6**. 남은 52칸(샤드 1/2/3/4 = 11/16/11/13).

### §5-12 몸 `c122.5-h170-s50` 4칸 완결 · 균일 조건 몸별 단가 · 착용불가 사유 분포

| 사이즈 | 결과 | f | 시간 | s/프레임 | 비고 |
|---|---|---|---|---|---|
| XL | **편입** | 190 | 109.6분 | 34.61 | 혼합 조건(§5-8) |
| L | **편입** | 180 | 92.7분 | **30.91** | 새 조건 순수 |
| M | 착용불가 | — | 즉시 | — | 옆 틈 G를 8회 안에 못 찾았다 |
| S | 착용불가 | — | 즉시 | — | 암홀 22.20 < 소매산 반폭 **22.59** cm |

**균일 조건(par 4 · 전력 상향 · 순수) 몸별 프레임 단가** — 이제 조건을 섞지 않고 비교한다:

| 몸 | s/프레임 | `c100-h170-s40` 대비 |
|---|---|---|
| `c100-h170-s40` | 24.06 | 1× |
| `c100-h185-s45` | 25.27 | 1.05배 |
| `c122.5-h170-s50` | **30.91** | **1.28배** |

⟹ §5-10에서 혼합 조건으로 어림한 **1.23배**가 균일 조건에서 **1.28배**로 확인됐다.
**단가는 몸의 성질이다**(관측). 무엇이 단가를 올리는지는 **안 봤다 — 기전 귀속 0.**

★ **한 몸 안에서 분류가 갈린 사례 2건째** — `c100-h185-s45`(편입 2 · 보류 1 · 착용불가 1)에 이어
`c122.5-h170-s50`(편입 2 · 착용불가 2). **사이즈는 판정을 가른다.**
★ **S 의 낙차** — 같은 소매(s50)의 소매산 반폭이 h185 에서 **28.37**, h170 에서 **22.59** 다.
암홀 반쪽 22.20 은 `ARM_G` 고정값이라 **키가 판정을 뒤집는다**(v3-77 §0-2 한계 그대로 · 처방 0).

**착용불가 12칸 사유 분포**(누적):

| 사유 | 건수 |
|---|---|
| 소매산 실수해 없음(암홀 < 소매산 반폭) | 9 |
| 옆 틈 G를 8회 안에 못 찾았다 | 2 |
| 목 밑동(벽/지붕 전이)을 못 찾는다 | 1 |

**소계 23칸** — 착용불가 12 · 보류 4 · 편입 7. 남은 49칸(샤드 1/2/3/4 = 8/16/11/13).
누적 착용불가 비율 **52%**(12/23) ⟹ 잔여 벽시계 추정 **약 13~25시간**(전부 물리면 25h ·
비율 유지면 13h · **샤드 2 지배**).

### §5-13 몸 `c100-h170-s40` 4칸 완결(**편입 3**) · 샤드 2 크래시 구간 통과

| 사이즈 | 결과 | f | 시간 | s/프레임 | 게이트 |
|---|---|---|---|---|---|
| XL | **편입** | 200 | 128.6분 | 38.58 | pass · sha `f06c2a88…` (상향 전) |
| L | **편입** | 200 | 93.6분 | 28.08 | pass · sha `b2fd2396…` (혼합) |
| M | **편입** | 180 | 72.2분 | 24.06 | pass · sha `482d696f…` (순수) |
| S | 보류 | 200 | 75.6분 | 22.67 | fail(③a 관통 **1.3717mm** / 자기관통 교차 12) · 순수 |

★ **편입 3칸짜리 몸이 처음이다.** 지금까지 완결된 두 몸은 분류가 고루 갈렸는데(§5-11 보강 · §5-12)
이 몸은 **S 하나만** 떨어졌다.
★ **S 의 관통이 자릿수가 다르다** — 1.3717mm 는 다른 보류 칸(0.5133 · 0.5184 · 0.5235mm)의
**약 2.6배**다. 문턱 0.5mm 근처에서 갈린 칸이 아니다. **기전 귀속 0 · 처방 0.**
★ **같은 몸 · 순수 조건 산포 = 6%**(22.67 ↔ 24.06) ⟹ **몸 간 1.28배 차이는 이 잡음 밖**이다.
「단가는 몸의 성질」 판정 유지.

**샤드 2 세그폴트 — 「산발」쪽 증거 1건**(확정 아님)
재기동분(01:0x 개시)이 같은 칸 `c122.5-h155-s50_XL` 에서 **124.7분 생존**(03:10 관측 · 코어 1.05 정상).
직전 사망은 **116.0분**이었다 ⟹ **같은 시간대를 넘겼다.** 다만 **완주 전이라 판정은 미확정** —
§5-9 의 판별 기준대로 **완주하면 「산발」 확정**, 그전에 죽으면 다시 본다.

**소계 24칸** — 착용불가 12 · 보류 5 · 편입 7. 남은 48칸(샤드 1/2/3/4 = 8/16/11/12).

### §5-14 **§5-12·§5-13 판정 정정** — 「단가 = 몸의 성질」은 **너무 이른 단정이었다**

`c100-h185-s40_XL` · 편입 · f=190 · 5505.1s(91.8분) · **28.97 s/프레임**(순수 조건)이 나오면서
앞선 판정이 유지되지 않는다. **순수 조건 5점 전량**:

| 칸 | 가슴 | 키 | 소매 | 사이즈 | s/프레임 |
|---|---|---|---|---|---|
| `c100-h170-s40_S` | 100 | 170 | 40 | **S** | 22.67 |
| `c100-h170-s40_M` | 100 | 170 | 40 | **M** | 24.06 |
| `c100-h185-s45_S` | 100 | 185 | 45 | **S** | 25.27 |
| `c100-h185-s40_XL` | 100 | 185 | 40 | **XL** | **28.97** |
| `c122.5-h170-s50_L` | 122.5 | 170 | 50 | **L** | 30.91 |

★ **정정 내용**:
- §5-12 「**단가는 몸의 성질이다**」 ⟹ **철회.** 근거였던 3점은 몸과 사이즈가 **같이** 변했다.
- §5-13 「같은 몸 순수 산포 6% = 잡음」 ⟹ **철회.** 같은 몸 안에서 **S 22.67 < M 24.06** 은
  사이즈 순서와 같은 방향이다. **잡음이 아니라 신호일 수 있다.**
- `c100-h185-s40_XL` 은 **c100 대역인데 28.97** 로 c122.5 값(30.91)에 붙는다 ⟹
  「c100 ↔ c122.5」 구분만으로는 설명이 안 된다.

★ **현재 지위 = 판정 불가.** 몸과 사이즈가 데이터에서 **분리되지 않는다**(5점 전부 조합이 다르다).
「몸 간 1.28배」도 **사이즈 효과가 섞인 값**으로 강등한다. **기전 귀속 0 · 처방 0 · 문턱 0.**
★ **분리 조건**(뒤에 자동으로 채워진다) — 같은 몸의 네 사이즈가 **전부 순수 조건 물리 칸**으로
완결되면 사이즈 효과가 단독으로 보인다. **순회를 바꾸지 않는다** — 기다리면 나온다.
★ **잔여 시간 추정에 미치는 영향** — 몸별 단가로 세운 추정이 근거를 잃는다.
순수 조건 실측 폭 **22.67~30.91 s/프레임**을 그대로 폭으로 쓴다.

**소계 25칸** — 착용불가 12 · 보류 5 · **편입 8**. 남은 47칸(샤드 1/2/3/4 = 8/16/10/12).

### §5-15 샤드 2 완주 — **세그폴트 「산발」 확정** · **f=360** · 프레임 상한 여유 11%

```
c122.5-h155-s50_XL   **편입** · f=360 · 12968.2s = 216.1분 · 36.02 s/프레임 · pass · sha ba21dc6f55ecfcd8…
c122.5-h155-s50_L    착용불가 — 옆 틈 G를 8회 안에 못 찾았다
c122.5-h155-s50_M    착용불가 — 옆 틈 G를 8회 안에 못 찾았다
```

★ **① 세그폴트 판정 = 「산발」**(§5-9 사전 등록 기준 충족). 같은 칸이 **세 번째 시도에서 완주**했다.

| 시도 | 구간 | 결과 |
|---|---|---|
| 1차 | 17:51~20:50 | 약 174분 → 사용자 정지로 폐기 |
| 2차 | 23:09~01:05 | 116.0분 → **세그폴트(exit 139)** |
| 3차 | 01:06~04:42 | **216.1분 → 편입** |

⟹ **칸 결부 아님.** 원인은 **여전히 미확정**(§5-9 그대로) · 처방 0 · 코드 수정 0.
같은 칸을 세 판 돌린 총 비용 **약 506분**, 그중 기록으로 남은 건 3차뿐이다.

★ **② 느렸던 이유 = 정착 지연**(매달림 아님). 36.02 s/프레임은 c122.5 XL 대역(35.31 · §5-14)과
같은 값이다. **속도는 정상 · 프레임 수가 많았다.**

★ **③ 프레임 상한 400 의 여유가 11% 로 줄었다** — 정착 f 기록 분포:

| f | 180 | 190 | 200 | 210 | 230 | 240 | **360** |
|---|---|---|---|---|---|---|---|
| 칸 | 3 | 3 | 5 | 1 | 1 | 1 | **1** |

- v3-78 §0′ 의 상한 400 은 「**정착 f 최대 200 × 2**」로 도출됐다. 그 입력은 **이미 240 으로 밀렸고**
  이제 **360**이다. 같은 규칙을 다시 적용하면 720 이 된다.
- **개정하지 않는다.** 상한 변경은 정지 → 개정 커밋 → 재개 절차 사안이고, **결과를 보고 문턱을
  움직이는 것과 구분**되어야 한다(함정 14). **이 회차에서 상한 무변경 · 소급 0.**
- ★ **위험 등재** — 400 을 넘는 칸이 나오면 「프레임 상한 도달」 보류로 기록되는데, 그것은
  **물리적 결론이 아니라 상한 인공물**이다. 지금 여유는 **40프레임(11%)**뿐이다.
  발생하면 그 칸은 **재판정 대상**으로 표시해야 한다.

★ **④ 시간 상한은 머신 조건에 종속이다**(반사실 등재 · 판정 아님) — 이 칸은 216.1분으로
262.2분 상한을 안 건드렸다. 그러나 **옛 조건**(par 5 · 전력 상향 전 · 약 1.5배)이었다면
**약 324분** ⟹ **발화했을 것이다.** 같은 칸이 조건에 따라 「편입」도 「시간 상한 보류」도 된다.
⟹ **느린 조건에서 잰 상한은 거짓 보류를 만들 수 있다.** 상한 자체는 **건드리지 않는다**(§0-4 유지).

**착용불가 사유 분포 갱신**(14칸): 소매산 실수해 없음 9 · **옆 틈 G 4** · 목 밑동 1.
**소계 29칸** — 착용불가 14 · 보류 6 · **편입 9**. 남은 43칸(샤드 1/2/3/4 = 7/13/10/12).

### §5-16 중간 집계 (31칸 · 편입 10) — **분류가 가슴 치수로 갈린다**

`c122.5-h155-s50_S` 착용불가(옆 틈 G)로 몸 `c122.5-h155-s50` 완결(**편입 1 · 착용불가 3**) ·
`c100-h155-s50_XL` **편입**(f=200 · 103.8분 · 31.15 s/프레임 · sha `da1bbf32…`)로 **편입 10칸** 도달.

**★ 분류 × 가슴 치수 교차표**(기록 31칸 · 관측 · 기전 귀속 0):

| 대역 | 착용불가 | 물리 진행(편입 + 보류) |
|---|---|---|
| `c122.5` | **14** | 6 |
| `c100` | **1** | 10 |

- 착용불가 15칸 중 **14칸이 c122.5**다. c100 유일 사례는 `c100-h185-s45_M`(목 밑동).
- **사유별로도 갈린다** — 「소매산 실수해 없음」 9건 **전부 c122.5** · 「옆 틈 G」 5건 **전부 c122.5**.
- v3-77 §0-2 한계(차트가 암홀을 주지 않는다 · 암홀 반쪽 22.20cm 는 `ARM_G` 고정)와 방향이 같다.
  **처방 0 · 문턱 0** — 관측만 등재한다.

**★ 순수 조건 7점 — 사이즈 축**(§5-14 「분리 불가」 지위는 **유지**):

| 사이즈 | s/프레임 |
|---|---|
| S | 22.67 · 25.27 |
| M | 24.06 |
| L | 30.91 |
| XL | 28.97 · 31.15 · 35.31 |

- S·M ≈ 24 · L·XL ≈ 31 로 **사이즈 방향의 기울기가 보인다**(약 1.3배).
- **그러나 같은 사이즈 XL 안에서도 몸에 따라 28.97~35.31 = 1.22배** 벌어진다 ⟹
  **사이즈 효과와 몸 효과는 아직 분리되지 않는다.** §5-14 분리 조건(한 몸 4사이즈 순수 완결) 유지.

**잔여 재추정** — 착용불가가 c122.5 에 몰리므로 **벽시계 지배가 샤드 2 → 샤드 3·4 로 옮겨간다**:

| 샤드 | 남음 | 대역 | 성격 |
|---|---|---|---|
| 1 | 7 | 전부 `c122.5-h170` | 착용불가 다수 예상 ⟹ 빠름 |
| 2 | 12 | `c122.5-h155` 8 + `c100-h185-s50` 4 | 혼재 |
| 3 | 11 | 전부 `c100` | **대부분 물리** |
| 4 | 11 | 전부 `c100` | **대부분 물리** |

⟹ **잔여 약 15~19시간**(샤드 3·4 지배 · 칸당 1.3~1.6시간 기준).
**소계 31칸** — 착용불가 15 · 보류 6 · **편입 10**. 남은 41칸.

### §5-17 **2차 세그폴트**(07:25:13) — 칸/시간 결부 아님 · **샤드 2 편중** 관측

```
증상   /c/Program Files/nodejs/npx: line 65:  1450 Segmentation fault  "$NODE_EXE" "$NPX_CLI_JS" "$@"
칸     c122.5-h155-s45_L  (06:47:07 개시 → 07:25:13 사망 · **38분 폐기** · 미완이라 index 미기록)
```

| | 1차 | 2차 |
|---|---|---|
| 시각 | 01:04:55 | 07:25:13 |
| 칸 | `c122.5-h155-s50_XL` | **`c122.5-h155-s45_L`**(다름) |
| 칸 경과 | 116.0분 | **38분**(다름) |
| 프로세스 수명 | 116분 | **379분** |

⟹ **칸 결부 아님 · 시간 결부 아님**(§5-15 「산발」 판정 유지 · 1차 칸은 3차 시도에서 완주했다).

★ **새 관측 — 두 번 다 «샤드 2» 다.** 같은 기간 샤드 1·3·4 는 **496분(8.3시간) 무중단**이다
(07:25 관측 · 코어 점유 1.06~1.07 정상 · 시스템 RAM 여유 13.2GB ⟹ OOM 아님).

| 후보 | 상태 |
|---|---|
| 우연(넷 중 하나에 2연속 = 약 6%) | **배제 못 함** |
| 샤드 2 구간(`c122.5-h155` 대역) 결부 | **배제 못 함** — 관통 13.3114mm 파탄 칸도 이 대역이다 |
| 프로세스/런타임 산발 | **배제 못 함** |

⟹ **판정 불가 · 기전 귀속 0 · 처방 0 · 코드 수정 0.** 표본이 2건이다.

**조치 — 재기동 + 진단 계기 1건 추가**(07:2x)
- 인자 **무변경**(`REV=1 SHARD=2 PAR=4 SLICE=24:40`) · 오케스트레이터·문턱·상한 **손대지 않았다**.
- 추가분: `NODE_OPTIONS=--report-on-fatalerror --report-directory=<scratchpad>`.
  **계산에 영향 0**(치명 오류 시 보고서만 쓴다) · 3차 크래시가 나면 **원인 층을 값으로 잡기 위함**.
  ★ 한계: SIGSEGV 가 네이티브 층에서 나면 이 보고서가 **안 나올 수 있다**. 나오면 이득, 안 나와도 손해 0.
- **누적 크래시 비용**: 1차 116분 + 2차 38분 = **154분 폐기**(기록 손실은 0 · 칸 경계 체크포인트).

**소계 36칸** — 착용불가 15 · 보류 9 · 편입 12. 남은 36칸(샤드 1/2/3/4 = 6/11/9/10).

### §5-18 **§5-14 분리 조건 충족** — 사이즈 효과 ↔ 가슴 효과 분리 (순수 조건 16칸)

`c100-h185-s40_S` **편입**(f=180 · 74.0분 · 24.68 s/프레임 · sha `1b39f48e…`)로
몸 `c100-h185-s40` **네 사이즈가 전부 순수 조건 물리 칸**으로 완결됐다 ⟹ §5-14 분리 조건 충족.

**① 몸 고정 · 사이즈만 변화**(완결 사다리 · 단조):

| 사이즈 | S | M | L | XL |
|---|---|---|---|---|
| s/프레임 | **24.68** | 25.14 | 27.16 | **28.97** |

⟹ **사이즈 효과 = 1.17배**(S→XL). 같은 몸·같은 조건이므로 **다른 요인이 섞이지 않았다.**

**② 사이즈 고정 · 가슴만 변화**(순수 조건 전량):

| 사이즈 | `c100` | `c122.5` | 비 |
|---|---|---|---|
| S | 22.67 · 24.68 · 25.27 | (없음) | — |
| M | 24.06 · 24.99 · 25.14 | 29.02 | **1.17배** |
| L | 27.16 · 27.74 | 30.91 · 31.18 | **1.13배** |
| XL | 28.97 · 31.15 | 33.88 · 35.31 · 36.02 | **1.17배** |

⟹ **가슴 효과 = 1.13~1.17배.** 사이즈 효과와 **같은 크기 대역**이다.

**③ 키·소매 효과는 작다** — c100 안에서 사이즈를 고정했을 때 산포:
S 1.11배 · M 1.05배 · L 1.02배 · XL 1.08배. ⟹ 단가를 움직이는 주 요인은 **가슴 · 사이즈** 둘이다.

★ **§5-14 「분리 불가」 지위 해제.** §5-12 에서 철회했던 「단가는 몸의 성질」은 **절반만 맞았다** —
몸(가슴)도 사이즈도 각각 실재하고, 크기가 비슷해서 섞여 보였던 것이다. **기전 귀속은 여전히 0**
(접촉 수·정점 수 따위는 **안 봤다**). 관측 등재까지가 이 회차의 범위다.

**④ 병렬 페널티 재측정**(부수 산출) — §5-1 단독 무경합 **28.86**(c122.5 · L · 상향 전)과
순수 병렬 c122.5 L(**30.91 · 31.18**)의 비 = **1.07~1.08배**.

| 조건 | 칸당 페널티 | 실효 처리량 |
|---|---|---|
| 옛(par 5 · 상향 전) | 1.33배 | 약 3.0배 |
| **현(par 4 · 상향 후)** | **1.07~1.08배** | **약 3.7배** |

※ 두 값의 몸이 완전히 같지는 않다(`c122.5-h185-s40` ↔ `c122.5-h170-s45/s50`) — **어림값**이다.

**소계 40칸** — 착용불가 16 · 보류 10 · **편입 14**. 남은 32칸(샤드 1/2/3/4 = 4/11/8/9).

## §6 일시 정지 (10:31 KST · 지시분) — 진행 중 칸 완주 후 정지

### §6-1 정지 절차와 계기 사실 2건

지시: **「샤드 4개 일시 정지 · 진행 중 칸은 완주 후 정지」**.

★ **계기 사실 ① — 오케스트레이터에 정지 훅이 없다.** 칸 사이에서 중단 신호를 보지 않으므로
「칸 경계에서 스스로 서기」가 불가능하다 ⟹ **각 칸의 기록을 확인한 뒤 프로세스를 내리는** 방식으로 집행했다.
★ **계기 사실 ② — 셸을 내려도 node 자식이 살아남는다.** 하네스의 작업 정지가 bash 래퍼만 종료시키고
`tsx`→`node` 트리는 계속 돌았다(정지 후에도 4개 생존 확인). ⟹ `taskkill /T /F` 로 **트리째** 종료했다.
- ★ 그때 **어느 pid 가 어느 샤드인지 밖에서 특정할 수 없었다** — 네 프로세스의 **명령줄이 동일**하고
  (샤드 번호는 **환경변수**로 넘겨서 명령줄에 안 잡힌다) 기동 시각도 셋이 **23:09:06.986 로 동일**했다.
  ⟹ **추측 종료를 하지 않았다**(멀쩡한 샤드의 진행 중 칸을 날릴 위험). 넷을 함께 내리는 순서로 바꿨다.
  ★ **부수 효과 = 이득** — 그 덕에 마지막까지 **par 4 가 유지**되어 남은 세 칸이 **순수 조건**으로 기록됐다.
  개별 정지였다면 par 4→3→2 로 떨어져 그 칸들이 혼합 조건이 됐을 것이다.

### §6-2 지시 시점 진행 중이던 4칸 — 전부 완주·기록

| 샤드 | 칸 | 결과 | f | 시간 | s/프레임 |
|---|---|---|---|---|---|
| 1 | `c122.5-h170-s40_XL` | **편입** | 190 | 112.7분 | 35.58 · sha `65e3782b…` |
| 3 | `c100-h170-s50_XL` | **편입** | 200 | 95.8분 | 28.74 · sha `6b5bbe26…` |
| 2 | `c122.5-h155-s45_L` | **편입** | 280 | 158.2분 | 33.90 · sha `9bb003da…` |
| 4 | `c100-h155-s50_S` | 보류 | **400** | 155.3분 | 23.30 · **상한 도달** |

- 지시 이후 샤드 2가 **착용불가 3칸**을 추가로 처분했다(`c122.5-h155-s45_M`·`_S` · `c122.5-h155-s40_XL` ·
  전부 옆 틈 G · **물리 0프레임 · 비용 0**). 사실대로 등재한다.
- **폐기분 3칸** — 지시 이후 시작된 칸이라 정지와 함께 버렸다: 샤드 1 약 32분 · 샤드 2 약 26분 ·
  샤드 3 약 30분 = **약 88분**. **기록 손실 0**(칸 경계 체크포인트).

### §6-3 ★ 프레임 상한 400 **첫 발화** — §5-15 위험의 실현

```
c100-h155-s50_S   보류 · f=400 · 9318.3s(155.3분) · **상한 도달**
                  fail(③a 관통 0.5125mm > 0.5mm / **정착 창 순변위 0.4878mm > 0.1mm**)
```

- **정착하지 못한 채 상한에서 잘렸다**(순변위가 문턱의 4.9배). ⟹ **게이트 수치는 물리적 결론이 아니다.**
- §5-15 에서 「400 을 넘는 칸이 나오면 상한 인공물 · **재판정 대상**으로 표시해야 한다」고 사전 등재했다.
  **그 조건이 실현됐다** ⟹ 이 칸은 **재판정 대상**이다. 상한·문턱은 **이 회차에서 건드리지 않는다**(함정 14).
- 정착 f 분포의 꼬리: 180~240 에 17칸 · 250 · 280 · 300 · 360 · **400(미정착)**.

### §6-4 정지 시점 총계

| 항목 | 값 |
|---|---|
| 기록 | **47칸** (착용불가 19 · 보류 11 · **편입 17**) |
| 남은 칸 | **25칸** — 샤드 1/2/3/4 = **3 / 7 / 7 / 8** |
| 착용불가 편중 | c122.5 **18** ↔ c100 **1** |
| 정지 시각 | **10:31:06 KST** · 굽기 프로세스 잔존 **0** |

- 인덱스 5개(`index.json` + `index-shard-1..4.json`)를 **`git add -f` 로 등재**했다 —
  `.gitignore:15 public/v3diag/*` 가 무시하던 파일이다. 크기 2.2~2.9KB.
  **blob(`settled-*.bin` · 각 553KB)은 등재하지 않는다** — 무시 규칙의 근거(용량)가 유효하다.

## §7 맥 병합 수령 + ⓑ 재배정 실행 (12:00~)

### §7-1 `503adf6` 수령 — 병합 충돌 1건 해소

`git pull origin v3-78-grid-bake` ⟹ **`scripts/v3GridRun.ts` 충돌**(양쪽이 시간 상한을 각자 개정).

| 측 | 값 | 근거 |
|---|---|---|
| 2호기(HEAD) | `SETTLED_SEC_MAX = 7865.0` ⟹ **262.2분** | 이 머신 기측정 정착 도달 최대 131.1분 × 2 |
| 맥(`503adf6`) | `59.3 * 60` ⟹ **118.6분** | 맥 기측정 최대 59.3분 × 2(잠자기 오염 칸 제외) |

★ **해소 = 2호기 값 유지.** 맥 주석 스스로 「**규칙만 공유하고 수는 머신마다 각자 뜬다**」고 적고 있고,
「충돌은 맥의 시간 상한 로직 보존 우선」 조항은 **맥 파일이 2호기 값에 덮이지 않게** 하는 것이다.
⟹ **이 파일은 2호기 머신이므로 2호기 도출값을 쓴다.** 맥 값은 **주석으로 보존**(무삭제).
`npx tsc -b` **rc=0** · 병합 커밋 `57060f6`.

### §7-2 ⓑ 4줄 — **저장소에 없어서 §0⁗-5 에서 도출**

맥 노트는 ⓑ 를 「보고 본문에 낸다」고만 적었고 **명령줄 자체는 저장소에 없다**. 배정값
(2호기 몫 = 전체 목록 인덱스 **71~74 · 84~86** · 구간 `71:87`)으로 **겹침 없이** 4분할했다.
★ **`REV` 없음** — 맥 §0⁗ 정정대로 `SLICE` 는 `cells()` **정본(정방향) 목록** 기준이다.

```powershell
$env:SHARD='1'; $env:PAR='4'; $env:SLICE='71:73'; npx tsx scripts/v3GridRun.ts   # pid 26004
$env:SHARD='2'; $env:PAR='4'; $env:SLICE='73:75'; npx tsx scripts/v3GridRun.ts   # pid 16736
$env:SHARD='3'; $env:PAR='4'; $env:SLICE='84:86'; npx tsx scripts/v3GridRun.ts   # pid 24332
$env:SHARD='4'; $env:PAR='4'; $env:SLICE='86:87'; npx tsx scripts/v3GridRun.ts   # pid 24616
```

★ **`Start-Process -PassThru` 로 샤드별 pid 를 확보**했다 ⟹ §6-1 의 「pid 특정 불가」가 **해소**된다.

### §7-3 ★ 합집합이 맥 59칸을 **인식하지 못한다**(값으로 확인)

네 샤드 첫 줄이 전부 **`완료분 47칸`** — 106(47+59) 도 97(맥 집계 합집합) 도 아니다.

**원인**: `git ls-tree MERGE_HEAD public/v3diag/` = **정본 blob 5개뿐 · 인덱스 파일 0개**.
맥 인덱스는 `.gitignore` 대상이라 **푸시된 적이 없다**. `082842b` 의 「2호기 인덱스 5개 반입」은
맥이 **우리 파일을 자기 머신으로** 가져간 것이고 **반대 방향은 오지 않았다**.

**이 배정에서는 손해 0**(구간 `71:87` 실측):

| 구분 | 칸 수 | 내역 |
|---|---|---|
| 기록 있음 | 9 (75~83) | **전부 2호기 자신의 기록** — 맥 것이 아니다 |
| 기록 없음 | 7 (71~74 · 84~86) | **맥이 배정한 2호기 몫과 정확히 일치** |

★ **남은 위험** — 2호기에 **맥 구간이나 다른 구간**을 주면 맥이 끝낸 칸을 **그대로 재계산**한다.
병합된 합집합 정규식이 `/^index-shard-.+[.]json$/` 로 넓어졌으므로,
**맥이 자기 인덱스를 `index-shard-mac.json` 류로 커밋·푸시하면 즉시 합집합에 들어온다.** — **요청 대상.**

### §7-4 ⓑ 산출 (5/7 완료 시점)

| # | 칸 | 결과 | f | 시간 | s/프레임 |
|---|---|---|---|---|---|
| 71 | `c100-h185-s50_XL` | **편입** | 210 | 107.9분 | 30.84 · sha `49952f01…` |
| 72 | `c122.5-h155-s40_S` | **편입** | 190 | 87.9분 | 27.76 · sha `49a50bb8…` |
| 73 | `c122.5-h155-s40_M` | 보류 | 210 | 105.8분 | 30.23 · fail(관통 1.0394mm / 교차 7) |
| 74 | `c122.5-h155-s40_L` | **편입** | 200 | 104.5분 | 31.36 · sha `d16197cf…` |
| 86 | `c122.5-h170-s40_L` | 보류 | **400** | 228.3분 | 34.25 · fail(관통 4.4238mm / 교차 2) |

★ **f=400 두 칸의 «성격»이 다르다**(기록의 `reason` 으로 갈린다):

| 칸 | f | `reason` | 정착 | 지위 |
|---|---|---|---|---|
| `c100-h155-s50_S`(§6-3) | 400 | **상한 도달** | 실패(순변위 0.4878mm > 0.1mm) | **상한 인공물 · 재판정 대상** |
| `c122.5-h170-s40_L` | 400 | **게이트 미통과** | **도달** | **정상 보류 · 재판정 아님** |

⟹ §0-4 우선순위(「정착 봤으면 게이트 판정 확정 → 시간 상한 → 프레임 상한」)가 **값으로 작동**한다.
「상한 인공물이 특정 대역에 몰린다」는 방향은 **아니다**(사전 가설 철회).

★ **시간 상한 초과 관측(미확정)** — 진행 중인 `c122.5-h170-s40_S` 가 **281분**으로 상한 262.2분을
**19분 넘겼다**. 검사(line 144 `performance.now() - t0 >= SEC_CAP`)는 **물리 루프 창 경계에서만** 돌고
**루프 이후 게이트 평가·blob 기록 구간은 검사 밖**이다 ⟹ 그 구간에 있는 것으로 **설명은 되지만
밖에서 확정할 수 없다**. **관측만 등재 · 기전 귀속 0 · 코드 수정 0.**

**2호기 누계 52칸** — 편입 **20** · 보류 13 · 착용불가 19. ⓑ 잔여 2칸(진행 1 · 대기 1).

## §8 `c122.5-h170-s40_S` 강제 종료 — 「개입 0」 해제 (지시분 · 18:26)

### §8-1 판정 근거와 집행

**지시**: 「강제 종료 승인 — **전제였던 «상한 유계»가 미발화로 깨졌다**」. ⟹ 개입 0 해제.

```
개시        11:55:50 (ⓑ 배정 샤드 3 첫 칸)
강제 종료   18:26:05  ·  taskkill /PID 24332 /T /F  (트리 7 프로세스 종료 성공)
종료 직전   경과 23,431s = **390.5분**  ·  CPU 23,424s  ·  **코어 점유 1.00**
```

| 상한 | 값 | 발화 여부 |
|---|---|---|
| 시간 상한 | 262.2분 | **미발화**(1.49배 초과 · 128분 넘김) |
| 프레임 상한 | 400 | **미발화**(이 대역 단가 34 s/f 환산 시 **약 690프레임** 상당) |

### §8-2 「상한 미발화 사유」 — **확정 불가 · 추정 병기(귀속 0)**

★ **확정된 사실은 둘뿐이다**: ㉠ 두 상한 모두 발화하지 않았다 · ㉡ **코어를 1.00으로 꽉 쓰고 있었다**
(⟹ 교착·I/O 대기·잠자기가 **아니다** — 계속 계산 중이었다).

**추정(검사되지 않음 · 기전 귀속 0)**:
- 시간 상한 검사는 `scripts/v3GridRun.ts` **line 144**(`performance.now() - t0 >= SEC_CAP`)에 있고
  **물리 루프 `while (frame < CAP)` «안»에만** 있다. ⟹ **루프 밖 구간**(드레싱 · 옆 틈 G 탐색 ·
  게이트 평가 · blob 기록 등)에 들어가 있었다면 **두 상한 어느 것도 볼 수 없다**.
- 이 추정이 맞다면 「상한」은 **물리 루프에만 걸린 유계**이고 **칸 전체의 유계가 아니다**.
  ★ 이는 **v3-78 §0′·v3-78B §0″ 상한 규약의 «적용 범위» 문제**다 — **개정 사안이며 이 회차에서 고치지 않는다.**
- **f 는 미상**이다(오케스트레이터가 기록을 남기지 못했다) ⟹ 어느 구간이었는지 **밖에서 특정 불가**.

### §8-3 index **수기 등재** — 보류 사유 4번째 신설

```json
"c122.5-h170-s40_S": {
 "status": "보류",
 "sec": 23415.0,
 "reason": "비정상 장기 실행(상한 미발화 · 강제 종료)",
 "manual": "수기 등재 — 오케스트레이터 산출 아님 · f 미상 · sec 은 프로세스 경과 실측 · par 4→1 혼합 · 재실행 0"
}
```

- **오케스트레이터 산출이 아니다** ⟹ `manual` 필드로 **판별 가능**하게 남겼다. `f` 없음 · `sha` 없음.
- ★ **재실행 0**(지시) — **`SECS` 축소 재실행은 기각**됐다. **원인 미상 상태의 재시도 금지**가 근거다.
- 이 칸은 **편입도 착용불가도 아니며**, 물리 결론이 아니라 **집행 기록**이다. 병합·집계에서 그렇게 읽어야 한다.

### §8-4 마지막 칸 — **외부 시간 상한 집행**(임시 절차 · 코드 수정 0)

`c122.5-h170-s40_M` 을 **단독**(PAR 미설정)으로 즉시 착수했다(18:26:45 · 최상위 pid 26600 ·
헤더 `대상 1칸 · 완료분 53칸` ⟹ 수기 등재분이 합집합에 정상 반영됨).

```
감시   외부 프로세스가 60초 주기로 확인
집행   개시 + 15,730s(262.2분) = **22:48:55 도달 시 taskkill /T /F**
기록   그 경우 「시간 상한 도달(외부 집행)」 보류로 **수기 등재**
```

★ **코드는 손대지 않았다.** 상한이 루프 밖에서 무력하다는 §8-2 추정에 대한 **임시 우회**이며,
**정규 해법은 개정 절차**(상한 적용 범위 재정의)로 다뤄야 한다 — 이 회차 범위 밖.
