# v3-90 — 소매 제약만 재발주(규칙 유도 집합) · bb 그리드 자리 · arcOn 모듈로 사실

| | |
|---|---|
| 브랜치 | `v3-90-sleeve-probe-ruleset` |
| 기점 | **`d6ebede`** (= v3-89 HEAD · 복원 상태 · `garmentScene.ts` diff **0** 확인) |
| §0 커밋 | **`585479f`** — 집행 «전» 별도 커밋(sha 만 그 뒤 한 줄로 채웠다) |
| 호출 ecc 에이전트/스킬 | **0** |

---

## §0 수령 (집행 «전» 별도 커밋)

### §0-1 판정문 (전략 세션 · v3-89 §4 수령 · 원문)

```
 「갈래 B 수용 · 복원 적법 · **귀책 = 전략 세션**(기대 집합 «열거» · **규칙으로 등재했어야**) ·
   **원리 확정**(제약이 답을 민다) · ㉰(나)·㉱(다) 수용 ·
   **arcOn 모듈로 = fb 옆선 2칸 기전 «후보»**(귀속 0) · **bb 7 별개** · 한정 수용」
```

★ **귀책 등재** — v3-89 의 ㉮ 위반은 **전략 세션 귀책**이다(기대를 «칸 열거»로 걸었다).
  이 판은 그 교훈대로 **집합을 «규칙»으로** 걸고, 집행 «전»에 원소를 뽑아 크기를 확인한다.

### §0-2 허용 diff

```
 v3-89 와 **동일한 3자리** — `:625-635` probe 항 1개 · `:733` 문언 · `:526` fb 인쇄 계기 1줄
 그 외 **0**(RMIN · 브래킷 · 손 상수 · 물리 코어 · 정본 · blob 27 · 화면 전부 불변)
```

### §0-3 사전 등재 (집행 «전» · **규칙으로**)

```
 ㉮ **물림 집합 K** = { 칸 | **R/RMIN < 1 + SEP / (2·CAP_W)** }
    · 집행 «전»에 계산해 **원소를 열거**한다.
    · 기대 **|K| = 8**(소매 7 + `c100-h155-s40_XL`).
    · **|K| ≠ 8 이면 착수 «전» 정지·상신.**
 ㉯ **108 − |K| 칸**: 조립 정점 **비트 동일** · 해시 동일.
 ㉰ **K**: v3-89 실측과 **수치·판정 동일**
    (조립 통과 **5** = 소매 4 + `c100-h155-s40_XL` · `back↔back` **3**).
```

**② bb 7칸 사전 등재 갈래** — (가) **2D 에서도 근접**(패턴 자체) / (나) **2D 는 멀고 3D 사상이 접는다**
(배율 < 1 또는 부호 반전) / (다) 그 외

### §0-4 갈래 (집행 «전» 등재)

| 갈래 | 조건 | 처분 |
|---|---|---|
| **A** | ㉮㉯㉰ 전량 | 상신 |
| B | **㉯ 위반** | 복원 · diff 0 · 상신 |
| C | **㉰ 불일치** | 값 상신 |
| D | 그 외 | 정지 |

**사후 갈래 신설 0.**

---

## §1′ 집행

### §1-⓪′ ㉮ 물림 집합 K — **집행 «전» 규칙으로 뽑았다** · **|K| = 8**

`npx tsx scripts/v3SetK.ts`(신설 · **코드 변경 0** · 조립만) · 규칙 `R/RMIN < 1 + SEP/(2·CAP_W)`

| 칸 | R/RMIN | 문턱 | CAP_W(cm) | K |
|---|---|---|---|---|
| `c87.5-h155-s40_S` | 1.000000 | 1.007208 | 13.87 | **★** |
| `c87.5-h155-s40_M` | 1.000000 | 1.007219 | 13.85 | **★** |
| `c87.5-h155-s40_L` | 1.000000 | 1.007374 | 13.56 | **★** |
| `c87.5-h155-s40_XL` | 1.000000 | 1.007378 | 13.55 | **★** |
| `c87.5-h170-s40_M` | 1.000000 | 1.006429 | 15.55 | **★** |
| `c87.5-h170-s40_L` | 1.000000 | 1.006494 | 15.40 | **★** |
| `c87.5-h170-s40_S` | 1.000282 | 1.006393 | 15.64 | **★** |
| `c100-h155-s40_XL` | **1.005094** | 1.007584 | 13.19 | **★** |
| `c87.5-h170-s40_XL` | 1.007764 | 1.006580 | 15.20 | (밖 · 가장 가깝다) |
| `c100-h155-s40_L` | 1.018480 | 1.007415 | 13.49 | |

```
 **|K| = 8** — 사전 등재(소매 7 + `c100-h155-s40_XL`)와 **원소까지 일치** ⟹ **착수**.
 계기 미발화 **11칸** = 소매산 9 + 목 밑동 2(조립 «이전» 단계에서 던진다 ⟹ K 판정 대상 밖).
 ★ v3-89 가 «열거»로 놓친 `c100-h155-s40_XL` 을 **규칙이 스스로 집어냈다**(경계 밖 최근접은
   `c87.5-h170-s40_XL` r = 1.007764 vs 문턱 1.006580 — **여유 0.0012**).
```

### §1-①′ 재발주분(3자리) — 적용

```
 v3-89 와 **동일한 3자리**: `:625-635` probe 항 1개 · `:733` 문언 · `:526` fb 인쇄 계기
 `git diff --stat` = **1 file · +27 / −1** · 삭제 줄은 **문언 1줄뿐**
 ★ `RMIN`·브래킷(`hi *= 1.15` · 40회)·`x0 += 0.005` 는 **grep 으로 변경 0 확인**
```

### §1-②′ ㉯ 회귀 — **100 / 100 비트 동일** ✅

| 항목 | 값 |
|---|---|
| 대조 대상(108 − \|K\|) | **100칸** |
| 패턴해시 **동일** | **100** |
| 상이 | **0** |
| 최소쌍 패널·값 상이 | **0** |

### §1-③′ ㉰ K 8칸 — **5 통과 · back↔back 3** ✅

| 칸 | 전 | 후 | R/RMIN | 호 | 최소쌍(mm) | 패널 쌍 |
|---|---|---|---|---|---|---|
| `c87.5-h155-s40_M` | 던짐 | **조립** | 1.0072 | 1.9857π | 2 | sleeveR↔sleeveR |
| `c87.5-h155-s40_L` | 던짐 | **조립** | 1.0074 | 1.9854π | 2 | sleeveR↔sleeveR |
| `c87.5-h155-s40_XL` | 던짐 | **조립** | 1.0074 | 1.9854π | 2 | sleeveR↔sleeveR |
| `c87.5-h170-s40_M` | 던짐 | **조립** | 1.0064 | 1.9872π | 2.33905 | back↔back |
| `c100-h155-s40_XL` | 던짐 | **조립** | 1.0076 | 1.9849π | 2 | sleeveR↔sleeveR |
| `c87.5-h155-s40_S` | 던짐 | 던짐 | 1.0072 | 1.9857π | **1.27781** | **back↔back** |
| `c87.5-h170-s40_S` | 던짐 | 던짐 | 1.0064 | 1.9873π | **1.47426** | **back↔back** |
| `c87.5-h170-s40_L` | 던짐 | 던짐 | 1.0065 | 1.9871π | **1.96197** | **back↔back** |

```
 조립 통과 **5/8**(기대 5) · 던짐 3칸 전부 `back↔back`(기대 3) · 값은 v3-89 와 **소수점까지 동일**.
 조립 분류 — 전 **77/108** → 후 **82/108**.
 ★ **세 기대가 모두 맞았다** — 집합을 규칙으로 걸자 v3-89 에서 갈래를 B 로 만든 그 한 칸이
   **집합 «안»으로 들어왔고**, 회귀는 **100/100 비트 동일**이 됐다.
```

### §1-④′ ② bb 7칸 그리드 자리 — **갈래 (나)**

| 칸 | 패널 | 삼각형 A (i,j) | 삼각형 B (i,j) | Δi | Δj | 3D 거리(mm) | 격자 간격(mm) | 2D 추정 거리(mm) | 3D/2D |
|---|---|---|---|---|---|---|---|---|---|
| `c122.5-h170-s50_M` | back | (40,73) | (42,74) | **2** | **1** | 1.371 | 16.19 | 36.20 | 0.0379 |
| `c122.5-h155-s50_S` | back | (38,72) | (40,73) | **2** | **1** | 1.745 | 15.48 | 34.62 | 0.0504 |
| `c122.5-h155-s45_S` | back | (38,71) | (40,71) | **2** | **0** | 1.557 | 15.70 | 31.41 | 0.0496 |
| `c122.5-h185-s45_XL` | back | (43,80) | (45,81) | **2** | **1** | 1.801 | 16.49 | 36.88 | 0.0488 |
| `c87.5-h185-s40_S` | back | (38,73) | (40,73) | **2** | **0** | 1.444 | 16.87 | 33.74 | 0.0428 |
| `c87.5-h185-s40_XL` | back | (47,75) | (47,76) | **0** | **1** | 0.003067 | 17.48 | 17.48 | 0.000175 |
| `c87.5-h170-s40_S` | back | (37,73) | (39,73) | **2** | **0** | 1.474 | 16.17 | 32.34 | 0.0456 |

```
 격자 폭은 `tris` 에서 **되찾았다**(패널 안 index 차의 «1 이 아닌 최솟값» = nu+1 · 새 상수 0).
 ★★ 7칸 전부 **같은 `back` 패널 «안»**이고 **Δi = 0~2 · Δj = 0~1** — **격자에서 한두 칸 이웃**이다.
   2D 추정 거리 **17.5 ~ 36.9mm** ↔ 3D 거리 **0.003 ~ 1.80mm** ⟹ **3D/2D = 0.000175 ~ 0.0504**.
 ⟹ **(나) 2D 는 멀고 3D 사상이 접는다.** (가)「2D 에서도 근접」은 **20~5700배 차이로 부정**된다.
 ★ 방향이 한쪽으로 쏠린다 — **Δi = 2 가 5/7**(호 방향으로 두 칸 건너). `at()` 이 호 위를
   진행하다 **그 자리에서 되돌아온다**는 그림과 정합한다. **기전 귀속 0 · 처방 0.**
```

### §1-⑤′ ③ arcOn 모듈로 — **원문 · 좌표 · 반경 자유도**

`src/v3/garmentScene.ts:489-491` **원문**:

```ts
      at: (sArc: number): [number, number] => {
        let a2 = sArc % total;
        if (a2 < 0) a2 += total;
```

```
 ⟹ **클램프가 아니라 «모듈로»** — 호 길이를 넘어서면 **되감겨 처음으로 돌아온다**.
   `bodyPoint` 는 `A[k].at(front ? px : -px)`(`:565`)로 px 를 **호 길이 좌표**로 쓴다 ⟹
   패널 반폭이 호 반절을 넘는 높이에서는 **앞뒤판이 서로의 자리로 넘어간다**.
 실측 정합(v3-89 §1-⑦′): `front↔back` 2칸의 그 높이 **ratio 1.1472 · 1.2667 (> 1)** —
   `need(4·panelHalfWidth + 2·GAP_SIDE) > base(호 둘레)` 인 체제다. **후보로 등재**(귀속 0).
 ★ **몸판에는 소매 `R` 같은 «반경 자유도»가 «없다»** — 배치는 `boundaryOf(h, delta, scale)`(`:469-475`)
   이고 자유 파라미터는 **δ(오프셋)** 와 **scale(=`Math.max(1, need/base)`)** 뿐이다.
   소매처럼 「반지름을 키워 호를 줄이는」 손잡이가 **구조적으로 없다** ⟹
   **소매에 쓴 처방을 몸판에 그대로 옮길 수 없다.** 사실만 적는다(**처방 0**).
```

---

## §2′ 자기검사

| # | 항목 | 결과 |
|---|---|---|
| ㉠ | ㉮ 열거 | **\|K\| = 8** · 원소 일치(집행 «전» 산출) |
| ㉡ | ㉯ 표 | **100 / 100 비트 동일** · 상이 0 |
| ㉢ | ㉰ 표 | **5 통과 · bb 3** · v3-89 와 수치 동일 |
| ㉣ | 정본 전부 diff · blob 27 | **0 · 0** |
| ㉤ | `garmentScene.ts` diff | **3자리** · 삭제 줄 **1**(문언 교체분) · RMIN·브래킷·손 상수 **변경 0** |
| ㉥ | 물리 코어 · 화면 diff | **0 · 0** |
| ㉦ | 회귀 | `tsc` 통과 · v3-81 §2 **전량 통과** · v3-80 프레이밍 **통과** · v3-81 canon 잘림 **0/3** |

**§3 캡처 — 0장**(사전 등재 그대로).

---

## §4′ 판정 — **갈래 A**

```
 ㉮ **|K| = 8**(원소 일치) · ㉯ **100/100 비트 동일** · ㉰ **5 통과 · bb 3** ⟹ **A**.
 ② **(나)** · ③ 사실 등재 완료. 사후 갈래 신설 0 · 문턱 이동 0 · 처방 0.
 ★ **집합을 «규칙»으로 건 것이 갈래를 갈랐다** — 같은 처방이 v3-89 에서는 B(열거가 1칸 짧았다),
   v3-90 에서는 **A** 다. 코드는 **같다**.
```

### §4-1 넘기는 것

| # | 항목 | 몫 |
|---|---|---|
| **㉠** | **처방이 서 있다** — 조립 77 → **82/108** · 회귀 100/100 비트 동일. 채택·병합 판단 | 전략 세션 |
| ㉡ | **bb 7칸 = 배치 사상의 접힘**(2D 17~37mm → 3D 0.003~1.8mm · Δi = 2 쏠림). 다음 표적 | 전략 세션 |
| ㉢ | **몸판에는 반경 자유도가 없다** ⟹ 소매 처방을 그대로 못 옮긴다. 대안 축(δ · scale · 호 재매개화)은 **미조사** | 전략 세션 |
| ㉣ | `arcOn` 모듈로 = **fb 옆선 2칸의 기전 후보**(ratio > 1 체제) · **귀속 0** | 전략 세션 |
