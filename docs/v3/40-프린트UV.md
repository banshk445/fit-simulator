# 40 — 5/5-α 프린트 UV: 정규화 층 + 패널 분리 렌더 (v3-59 · **물리 0프레임 · `garmentScene` diff 0**)

v3-59. 브랜치 `v3-59-print-uv`(사전 등록 · **이 CC 세션 전용**). 호출한 ecc 에이전트/스킬: **0 / 0**

## git 스탬프

```
 브랜치  v3-59-print-uv             워크트리  /Users/banshk/projects/fit-simulator
 기점    b2db8b6 (v3-58 HEAD)       직전 회차  b2db8b6
 커밋    e72ef7a  §0  Q1~Q4 수령 + 판정 기준 (집행 «전»)
         d75658d  §1·§2  정규화 층 + G7 계기 (**판정 «전»**)
         (본문)   §2 집행 · §3 렌더 · §4 게이트 · 갈래 A
 검사    npx tsc -b rc=0 · npm run build 통과 · **물리 0프레임 · garmentScene diff 0**
```

---

## §0 판정 수령 — **집행 «전» 커밋**

### §0-1 수령분

```
 **Q1** = 완결 범위 **β** · **착수 순서 α 먼저**(성능 축은 **Q2 답 대기** · **㉢ 은 v1 기각**)
 **Q3** = 최종 상태 «**대체**»(**이 판 무관** · **이관 규약만 등재**:
   **v2 무삭제** · **이관 완료 시 하네스 전용 강등**)
 **Q4** = **G1 유지**(**정의역 명기**) · **G2 «재정의»**(**사전 등록 실행만** · **정본 승격은 게이트 전량**) ·
   **G7 신설**(**UV 계약 «성질» 3건**) · **R5/R8 처분 = 매핑 «불제작»** ·
   **UI 는 v3 프리셋 «직접» 노출**(**전환 판 몫**)
 **이 판의 물음**: 「**v3 옷에 프린트가 올바로 얹히는가**」 — **판정은 사용자 화면 + 전략 세션**
```

### §0-2 §1 정규화 층 — **신설 모듈**(`garmentScene` diff 0)

```
 **`src/v3/printUv.ts` 신설(순수)**: `assemble()` 출력의 `uv`(**패턴 미터**)와 **패널 경계**를 받아
 **v2 계약**(`patternGarment.ts:306-321` «**문언**» 준거 · **임포트 0**)대로 정규화:
   **패널별 bbox** → **공통 `scale` = max(모든 패널 max(폭, 높이))** →
   **u = (x − xMin) / scale** · **v = 1 − (y − yMin) / scale**
 **손 상수 0** — 전부 **그 상태의 bbox 에서 도출**.
 **`garmentScene.ts` 는 주석 포함 diff 0**(**회귀 앵커**) — **소비만 한다.**
```

### §0-3 §2 G7 자기검사 — **판정 «전» 등재**

```
 **①** `u·v ∈ [0,1]` **전 정점**(패널별 **위반 수 = 0**)
 **②** **축척 보존**: **무작위 아닌 «등재 가능한» 표본**(각 패널 **시접 구간 양끝** 등 **결정적 선택**)에서
   **패턴 거리 / uv 거리 = `scale`** · **편차를 수치로 등재**
   (**문턱 = `TOL_SELF` 계열 · 새 수 0** · **사전 등록 후 집행**)
 **③** **v 방향**: 각 패널 **어깨측 정점의 v < 밑단측 정점의 v**(**텍스처 위 = 어깨**)
```

### §0-4 §3 패널 분리 렌더 — **표시 층**

```
 패널 **4개를 각각 지오메트리로**(v2 `PatternPreview.tsx:359-374` 와 **같은 구조** · **코드 임포트 0**)
 **앞판 = 프린트 텍스처** · **뒤판/소매 = 단색**(v2 계약의 «앞판 프린트» **형식 그대로**)
 **프린트 원본 = v2 경로가 쓰는 «기존 자산 파일»**(**데이터 읽기는 임포트가 아니다** · v3-13)
 **프린트 폭·중심 = `uMax` 도출**(`PatternPreview.tsx:166-174` **문언 준거** · **새 수 0**)
 **핏 맵 색과의 공존**: **프린트 모드 ↔ 핏 색 모드 «토글»**(**동시 적용 안 함** · **#89 렌더 기여 갱신**)
 **blob 무변조**: **표시 전후 정본 blob sha 동일 «실측»**(v3-54 §3-2 형식)
```

### §0-5 §4 게이트 + 캡처

```
 **G1**(v2 임포트 0 · grep) · **G2**(물리 0프레임 · blob 주입만) · **G3**(손 상수 0) ·
 **G4**(정본 3종 전량) · **G7**(§2 3건) **전량 재판정**
 캡처: **3종 × front/side/back-p 프린트 on = 9장** + **핏 색 토글 대조 front 3장** · **CC 화면 판정 0**
```

### §0-6 §5 갈래 (사전 등록)

| 갈래 | 조건 | 귀결 |
|---|---|---|
| **A** | G 전량 통과 · 캡처 산출 | **사용자 화면 판정 상신**(「프린트가 올바로 얹히는가」) |
| **B** | **G7 위반** | 정지 · **위반 수치 «사실만»** · **처방 0** |
| **E** | 판정 불가 | 정지 · 사실만 |

### 이 판에서 바꾸지 않는 것

**물리 0프레임** · **`solver.ts` / `garmentScene.ts` / `s4Gate.ts` diff 0** · 문턱·정본 **0** ·
**`SEP`·밴드 도출 0** · **v2 경로 0줄** · **`V2DIMS` off** · **핏 색 채널 무변경**(토글 공존만).

### 순서

```
 §0 커밋 → §1 → §2 등재 커밋 → §2 집행 → §3 → §4 → §5 판정 · 상신
```

---

## §1 정규화 층 — **`src/v3/printUv.ts` 신설**(순수 · `garmentScene` diff 0)

```
 입력  `assemble()` 의 **`uv`(패턴 미터 · n×2)** + **패널 경계**(`panels` 의 `base`·`count`)
 규약  **v2 계약의 «문언» 준거**(`patternGarment.ts:306-321` 을 **읽고 옮겼을 뿐 · 코드 임포트 0** · G1):
         **scale = max(모든 패널의 max(폭, 높이))**   ← **공통** ⟹ 패널 간 **축척 보존**
         **u = (x − b.xMin) / scale**
         **v = 1 − (y − b.yMin) / scale**            ← 패턴 y 가 아래로 증가 ⟹ 뒤집는다(텍스처 «위» = 어깨)
 **손 상수 0** — `scale` · `xMin` · `yMin` 이 전부 **그 상태의 bbox 에서 도출**된다.
 순수성 `node:` 0 · 파일 0 · `process` 0 · console 0 · **three 임포트 0**(표시 층이 소비).
 **`garmentScene.ts` 는 주석 포함 diff 0** — 이 모듈은 그 «출력»만 읽는다(**회귀 앵커 유지**).
```

**정규화 결과**(3종 **동일** — 제도가 `d` 와 무관하기 때문 · v3-47 §1 「파생 치수 d 무관」과 정합):

| 패널 | bbox(패턴) | **uMax** | **vMax** |
|---|---|---|---|
| front | 55.00 × 70.00cm | **0.7857** | **1.0000** |
| back | 55.00 × 70.00cm | 0.7857 | 1.0000 |
| sleeveR / sleeveL | 34.51 × 22.00cm | 0.4929 | 0.3143 |

**공통 `scale` = 70.000cm**(= 총장) — **uv 1.0 이 70cm**다. **화면이 이 값을 밝힌다**(G5 형식).

---

## §2 G7 자기검사 — **3종 × 3건 전부 통과**

계기 `scripts/v3PrintUvCheck.ts`(**판정 «전» 커밋** `d75658d`) · **물리 0프레임**.

| 검사 | 정의 | gray | swim | sweat |
|---|---|---|---|---|
| **①** `u·v ∈ [0,1]` 위반 수 | 패널별 전 정점 | **0 / 10402** | **0 / 10402** | **0 / 13248** |
| **②** 축척 보존 편차 | «패턴 거리/scale − uv 거리»의 max · 표본 = **각 패널 «모서리 쌍» 4개**(결정적) | **2.036e-8** | **2.036e-8** | **2.036e-8** |
| — | 문턱 = **`TOL_SELF` / `scale`** = **1.429e-4**(**등재 상수의 단위 환산 · 새 수 0**) | 통과 | 통과 | 통과 |
| **③** v 방향 | 패턴 y **최대**(어깨측)의 v **<** 최소(밑단측)의 v | **0 불성립** | **0** | **0** |

```
 **② 의 편차가 문턱의 «1/7000»** 이다 — 정규화가 **아핀 변환**이므로 축척 보존은 원리적으로 성립하고,
 남는 것은 **부동소수 오차**뿐이다(2e-8). **이 검사는 «구현이 규약대로인가»를 잡는다.**
 **③ 실측**: front·back 은 어깨측 **v 0.0000** ↔ 밑단측 **v 1.0000**(패널 높이 = scale) ·
   sleeve 는 **0.6857 ↔ 1.0000**. **텍스처 «위»가 어깨**임이 값으로 선다.
 **표본 규칙을 «사전 등록»했다** — 무작위가 아니라 **각 패널의 i·j 격자 끝점 4쌍**이고,
   그 규칙이 계기 주석과 §0-3 에 **집행 전에** 적혔다.
```

---

## §3 패널 분리 렌더 — **표시 층**

```
 `src/v3/productView.ts` 에 **`PrintInput`** 을 받는 경로를 붙였다(**표시 층 · 물리 0줄**):
   패널마다 «자기 정점만» 담은 지오메트리를 만들고 **인덱스를 패널 기준으로 옮긴다**.
   삼각형은 **세 정점이 모두 그 패널인 것만** 담는다 ⟹ **시접 브리지 스트립은 빠진다**
   (스트립은 **패널을 가로지르는** 삼각형이다 · v3-45 등재) — **사실 등재**.
 **앞판 = 텍스처 · 나머지 = 단색** — v2 계약의 「앞판 = 대표색 + 가슴 프린트 / 뒤판·소매 = 단색」
   **형식 그대로**(`PatternPreview.tsx:128` · `:359-374` 와 **같은 구조 · 코드 임포트 0**).
 **프린트 원본** = **v2 경로가 쓰는 기존 자산**(`docs/test-assets/print-tee.png`)을
   `public/v3print/` 로 **복사**해 쓴다 — **데이터 읽기이지 코드 임포트가 아니다**(v3-13 규약).
 **프린트 폭·중심** — v2 는 `uMax` 에서 도출한다(`PatternPreview.tsx:166-174`).
   이 판은 **UV 를 계약대로 내는 데까지**이고 **합성 상수를 새로 만들지 않았다**(**새 수 0**).
   화면이 **앞판 `uMax` 0.7857 을 인쇄**한다.
 **핏 색과의 공존** — **토글이 배타적**이다: 프린트 on 이면 핏 색 체크박스가 **비활성**되고
   렌더에 **정점색을 넘기지 않는다**(`V3Panel.tsx` · **동시 적용 0**). **핏 색 채널 무변경.**
 **blob 무변조 실측** — 표시 전후 `settled-gray-d9.bin` **`8dec6eca…`** ·
   `settled-sweat-d8-new.bin` **`dcaceda3…`** **동일**.
```

### §3-1 렌더 기여 목록 갱신 (**#89 형식**)

```
 ① **패널 분리 + 프린트 텍스처**(v3-59 신설 · **표시 전용** · **앞판만**) ← **이번에 추가 · 핏 색과 배타**
 ② 정점색 = 핏 맵 색(v3-54 · **프린트 on 이면 안 넘어간다**)
 ③ 시접 브리지 스트립(v3-45 · **프린트 모드에서는 빠진다** — 패널을 가로지르므로)
 ④ 정점 노멀 보간 스무딩(v3-42 §4) · ⑤ 원근·조명·톤매핑(v3-43 `DISPLAY`)
```

---

## §4 게이트 재판정 — **전량 통과**

| # | 게이트 | 판정 | 근거 |
|---|---|---|---|
| **G1** | v2 임포트 0 · 데이터 읽기 · `V2DIMS` 미사용 | **통과** | `grep -rn "from '../lib/" src/v3/ src/workers/v3DressWorker.ts scripts/v3PrintUvCheck.ts` **0건**. **정의역 명기**(Q4): 「**코드 임포트 0**」이고 **자산 파일 복사·문언 준거는 임포트가 아니다**(v3-13) |
| **G2** | **물리 0프레임**(재정의: **사전 등록 실행만**) | **통과** | 표·UV·색 전부 **정착 blob 주입 + `frames:0`** · **솔버 스텝 0** · 이 판의 실행은 **§2 사전 등록분뿐** |
| **G3** | 손 상수 0 | **통과** | `scale`·`xMin`·`yMin` = **그 상태 bbox** · ② 문턱 = **`TOL_SELF`/`scale`** · **새 수 0** |
| **G4** | 정본 3종 전량 | **통과** | G7 **3종 × 3건** · 캡처 **3종 × 3뷰** |
| **G7** | **UV 계약 성질 3건**(신설) | **통과** | §2 표 |

**캡처** `docs/captures/v3-59-프린트UV/` — **프린트 on 9장**(3종 × front/side/back-p) +
**핏 색 토글 대조 front 3장**. **CC 자체 화면 판정 0.**

---

## §5 갈래 판정 — **A**

| 갈래 | 조건 | 실측 | 성립 |
|---|---|---|---|
| **A** | G 전량 통과 · 캡처 산출 | **G1·G2·G3·G4·G7 전량** · 캡처 **12장** | **예** |
| B | G7 위반 | 위반 **0** | 아니오 |
| E | 판정 불가 | — | 아니오 |

### §5-1 이 판이 «닫은» 것 / «연» 것

```
 닫음  **v3-58 §2-1 이 지목한 «정규화 층» 결손이 메워졌다** — `printUv.ts` 하나 · `garmentScene` diff 0
       **G7 신설·통과** — 범위·축척 보존·v 방향이 **값으로** 선다(편차 2e-8 = 문턱의 1/7000)
       **패널 분리 렌더** — 앞판만 텍스처 · 핏 색과 **배타 토글** · **blob 무변조 실측**
       **공통 scale = 70.00cm = 총장** · 앞판 **uMax 0.7857** — 화면이 밝힌다
 엶    **시접 브리지가 프린트 모드에서 빠진다**(패널을 가로지르는 삼각형) — **사실 등재 · 처방 0**
       **프린트 «합성»**(폭·중심·크롭)은 이 판 범위 밖 — v2 는 `uMax` 에서 도출한다(문언만 인용)
       **사용자 화면 판정** — 「프린트가 «올바로» 얹히는가」
       **Q2(성능 목표)** 미결 · **R5/R8**(원단 UI) = **전환 판 몫**(매핑 불제작 처분)
```

---

## §6 회귀

```
 **물리 0프레임** · **`solver.ts` · `garmentScene.ts` · `s4Gate.ts` diff 0**(`b2db8b6` 대비 · **주석 포함**)
 **v2 경로 0줄** — `src/lib/` · `PatternPreview.tsx` · `Garment.tsx` · `garmentWorker.ts` **diff 0**
 신설 `src/v3/printUv.ts` · `scripts/v3PrintUvCheck.ts`(**판정 «전» 커밋**) ·
      `public/v3print/print-tee.png`(**자산 복사** · 코드 아님)
 수정 `src/v3/productView.ts`(**표시 층** · `PrintInput` 경로) · `V3Panel.tsx`(토글·범례) ·
      `v3DressWorker.ts`(**`kind:'uv'` 1블록**)
 문턱·정본 0 · `SEP`·밴드 도출 0 · `V2DIMS` 미사용 · **핏 색 채널 무변경**
 **npx tsc -b rc=0** · **npm run build 통과** · **blob sha 동일**(2종 실측)
```

---

## §7 자기 점검

| # | 항목 | 답 |
|---|---|---|
| 1 | §0 커밋 선행 · `garmentScene` diff 0 | **예** — `e72ef7a`(§0) → `d75658d`(§1 모듈 + §2 계기 · **판정 전**) → 집행. `garmentScene` **주석 포함 0줄** |
| 2 | v2 계약을 «임포트»했는가 | **아니오** — **문언 준거**(주석에 `patternGarment.ts:306-321` 을 인용) · `grep` **0건**. **자산은 «복사»**(v3-13: 데이터 읽기 ≠ 임포트) |
| 3 | G7 표본이 «결정적»인가 | **예** — **각 패널 i·j 격자 끝점 4쌍**. **규칙을 집행 «전»에** §0-3 과 계기 주석에 적었다. **무작위 0** |
| 4 | 손 상수 | **0** — `scale`·bbox 는 **그 상태**에서 · ② 문턱은 **`TOL_SELF`/`scale`**(등재 상수의 단위 환산) |
| 5 | 핏 색과 동시 적용했는가 | **아니오** — **배타 토글**(프린트 on ⟹ 핏 색 비활성 · 정점색 미전달). **#89 목록 갱신** |
| 6 | 이 판에서 호출한 ecc 에이전트·스킬 | **0 / 0** |
