# 18 — 파생 치수 자체 도출 (#56 · 부분 접속 1/5)

v3-31. 선행 차단 해소 — v3가 파생 치수 4종을 `pattern-meta.json`(v2 제도의 산출물)에서
읽던 것을 **몸에서 직접 도출**한다.

호출한 ecc 에이전트/스킬: **0**

> **git 스탬프** · 브랜치 `v2-stage2a` · 워크트리 `/Users/banshk/projects/fit-simulator`
> · 기점 `42059be`(v3-30 metrics) · §0 등록 커밋 `44371b4` · 도출 커밋 `55dc3a6`

---

## §0 판정 기준 — **도출 «전»에 등록**

> **이 절은 §2 도출을 «한 줄도 쓰기 전»에 별도 커밋으로 고정한다.**
> §2·§3 결과를 보고 이 절을 고치지 않는다. 순서는 git 이력이 증거다
> (이 커밋 → 그다음이 도출 커밋).

**이 판의 성패는 「기준선이 보존되는가」다.** 파생값이 v2와 달라도 좋다 —
착장 결과가 v3-26 기준선의 잡음 바닥 안이면 통과다.

### ㉠ 주 기준 — 착장 관측량 O1~O4가 잡음 바닥 안

기본 치수(현 `pattern-meta.json`의 `garmentDims`)에서 v3 자체 도출값으로 제도해
**d15 · gray**로 착장하고, v3-26 §1이 등록한 관측량 4종을 낸다.

| 관측량 | 정의 | **잡음 바닥**(v3-26 §1 등록분) |
|---|---|---|
| O1 | 밑단 둘레 [cm] | 0.00 cm |
| O2 | 밑단 주름 파장 [cm] | 0.00 cm |
| O3 | 몸–옷 간극 중앙값 [mm] | 0.000 mm |
| O4 | 접촉 정점 비율 [%] | **0.25 pp** |

- **잡음 바닥을 재측정하지 않는다.** 위 등록값을 그대로 쓴다.
- 대조 기준선 = **v3-26 §2 d15 · gray의 O1~O4 실측값**(같은 하네스 · 같은 원단 ·
  같은 해상도). 차이가 위 바닥 안이면 ㉠ 통과.
- O4만 바닥이 0이 아닌 이유: 접촉 판정이 이산량이라 정점 하나가 들고 나면
  계단이 생긴다(v3-26 §1 등재분). 나머지 셋은 결정적 재실행에서 비트 동일이었다.

### ㉡ 부 기준 — 파생값 자체의 일치 (㉠과 **나눠** 판정)

4종 각각 **v3 도출값 vs `pattern-meta.json` 값**을 표로 낸다.

| 파생 치수 | `pattern-meta.json` 값 |
|---|---|
| `neckHalfWidthCm` (목선 반폭) | 8.12 cm |
| `armholeGirthCm` (암홀 둘레 앞+뒤) | 44.39 cm |
| `capHeightCm` (소매산 높이) | 12.31 cm |
| `necklineGirthCm` (목선 둘레 앞+뒤) | 48.27 cm |

- **다르면 그 크기를 적는다.** ㉡의 불일치는 그 자체로 실패가 아니다 —
  ㉠이 통과하면 **갈래 B**(파생값 차이 등재)로 간다.
- ㉡을 근거로 ㉠의 문턱을 움직이지 않는다. 역도 같다.

### ㉢ S4 시험 — 동반 확인

③a 관통 ≤ 0.5mm · 자기관통(삼각형 교차) 0 · 시접 자기검사 · λmax · 정착 도달 ·
보조 장치 0(invMass=0 정점 0 · 앵커 0 · 핀 0 · 흡착 0). **문턱은 누적분 그대로.**

### ㉣ 순서 조항

1. 이 절을 커밋한다 (**도출 코드 0줄**)
2. §1 판독 → §2 도출 → §3 검증 → §4 치수 변경
3. 결과가 어떻든 ㉠㉡㉢의 문턱·잡음 바닥·대조 대상을 바꾸지 않는다
4. 갈래에 없는 결과면 **H(판정 불가)**로 적고 정지한다

---

## §1 v2가 무엇을 뽑는지 «판독» — 코드 임포트 0

읽은 곳: `src/lib/patternDraft.ts:320-375`(목선·어깨·암홀 깊이) · `:376-432`(암홀 둘레·소매산) ·
`src/lib/bodyMeasure.ts:69-121,327,436,511`(몸 계측량의 정의). **읽기만 했고 임포트·복사 0.**

### §1-1 v2가 푸는 기하 문제 4종

| 파생 치수 | 입력 | 찾는 것 | v2의 조건 |
|---|---|---|---|
| `neckHalfWidthCm` | `body.neckBaseFrontM`, `neckBaseGirthM` | 목선 반폭 w | 앞 목선 곡선(4분 타원, 반축 w × (w+1cm))의 길이 × 2 = 몸 목밑 **앞호** × 여유배율. 60회 이분법 |
| `necklineGirthCm` | `neckBaseFrontM + neckBaseBackM` | 목선 둘레 | 앞 목선 + 뒤 목선. 여유배율 = 1 + 2cm/목밑둘레 ⟹ 결과적으로 **목밑둘레 + 2cm** |
| `armholeGirthCm` | `body.chestGirthM` | 암홀 둘레 | 암홀 깊이 = **가슴둘레/4 + 3cm** 로 «먼저» 정하고(교재 계열 상수 `:47 =4`, `:49 =0.030`), 그 깊이로 그린 앞·뒤 암홀 곡선 길이의 합 |
| `capHeightCm` | `armholeGirthM`, `g.sleeveWidthM` | 소매산 높이 | 소매산 3차곡선(밑변 = 소매폭)의 호장 = (암홀 둘레 + 이즈)/2 가 되는 높이. 60회 이분법 (“walking the sleeve”) |

**v2가 몸에서 쓰는 값**: `neckBaseFrontM` · `neckBaseBackM` · `neckBaseGirthM` ·
`chestGirthM` · `ridgeTopYAt(x)` · `shoulderSpanM`. 전부 `bodyMeasure.ts`의 **측지 링 ·
능선 폴리라인 · 각도 bin 최근접 볼록껍질**에서 나온다.

### §1-2 v3가 가진 몸 표현 — 「같은 답이 나올 수 있는 표현인가」

| | v2 | v3 |
|---|---|---|
| 몸 | `bodyMeasure`/`bodyGeodesic`의 측지 링 + 능선 폴리라인 | **삼각형 메시 원본**(15,882정점 · 28,880삼각형) + **복셀 SDF**(h 3.951mm · 대역 8.9mm) |
| 링 | 표면 위 측지 곡선 | **평면 «정확» 단면**(삼각형 엣지 교차) + **볼록 껍질 지지함수** |
| 여유 | 등재 상수 2종(목선 2cm · 암홀 3cm) + 교재 나눗수 4 | **SEP = 2×두께 = 2mm** (v3가 이미 등재한 유일한 간극 척도) |

**판단**: 목선 2종은 «같은 답이 나올 수 있는» 표현이다 — 목밑 링은 v3의 평면 단면 +
볼록 껍질로 «직접» 잡힌다. **암홀 둘레는 아니다**(§1-3).

### §1-3 «베끼지 않았음»의 근거 — 그리고 암홀이 왜 안 되는가

v3는 v2의 조건식(앞호 = 목선 앞곡선 길이 · 가슴/4+3cm · walking the sleeve)을 **쓰지
않는다.** v3의 원리는 하나다:

> **옷의 «구멍»은 몸의 대응 «링»을 옷 두께(SEP)만큼 부풀린 것이다.**
> 볼록체를 δ 부풀리면 둘레가 «정확히» 2πδ 는다 — 여유를 새로 고를 필요가 없다.

이 원리로 **목선 반폭 · 목선 둘레 · 소매산 높이** 3종이 나온다. **암홀 둘레는 나오지
않는다** — 값으로 낸 근거:

```
 몸에서 잴 수 있는 팔 관련 링(+SEP)
   어깨끝 x=22.5cm 수직 평면 단면          34.51 cm   ← 소매 대역 «최대»이기도 하다
   바깥쪽 x=24.5~44.5cm                    23.9~32.4 cm (단조 감소)
 필요한 값(v2)                              44.39 cm
 «몸에는 44.39cm 링이 없다.»
 그리고 실수해 조건: 암홀 반쪽 ≥ 소매산 반폭.  34.51/2 = 17.26 ≤ 암홀둘레/2
   ⟹ 팔 링을 그대로 암홀에 쓰면 «소매산 높이 = 0»(퇴화)
 이유: 이 옷의 암홀은 T자세 마네킹의 겨드랑이(어깨끝 아래 3.4cm)보다 훨씬 깊다
   (암홀 깊이 21.5cm). 그 차이는 **옷의 «성격»(박시 티셔츠)**이지 몸의 계측량이 아니다.
   v2도 몸에서 «재지» 않고 교재 관계식(가슴/4 + 3cm)으로 «정한다».
```

⟹ **암홀 둘레는 파생 치수가 아니라 «옷 사양 입력»으로 승격한다**(총장·품·어깨·소매길이와
같은 층위). 기본값 44.39cm — v3-18~30이 돌린 값 그대로다. **갈래 D의 대안 조항**이다.

---

## §2 v3 자체 도출

구현: `scripts/v3S4.ts` §1-A(옷 치수 입력) · §1-B(몸 링 계기) · §1-C(파생 치수 도출).
**`pattern-meta.json` 읽기 0** — `readFileSync(META)`와 `type Meta`를 통째로 지웠다.

### §2-1 계기 — 몸 링 3종

```
 planeSection(nx,ny,c)  평면 n·(x,y)=c 와 메시의 «정확한» 교선점(삼각형 엣지 보간).
                        슬랩 표본이 아니다 — 메시가 성길 때 링이 끊기는 것을 없앤다.
 ringOf(pts, δ)         교선점의 볼록 껍질 지지함수 → 경계 둘레 · 축별 반폭.
                        볼록체를 δ 부풀리면 둘레가 «정확히» 2πδ 는다.
 wallFrac(y, h)         높이 y 근방 표면 «면적» 중 |n_y| ≤ 1/√2 인 비율.
                        1/√2 = 45° = 벽과 지붕의 «정확한 반». 고른 수가 아니라 분할점이다.
```

### §2-2 도출 4종

| 값 | 도출 | 손 상수 |
|---|---|---|
| `Y_TOP` 어깨끝 높이 | 몸 표면이 \|x\| = 어깨/2 에 닿는 최고 y (허용폭 = SDF 복셀 h) | 0 |
| `Y_NECK` 목 밑동 | `Y_TOP` 위로 «첫» 국소 최소 둘레 높이(= 목)에서 아래로 내려가며 `wallFrac`이 **1/2**을 가르는 높이. 표본 간격 = 복셀 h | 0 |
| **`NECK_A`** 목선 반폭 | `Y_NECK` 수평 단면 볼록 껍질 **+SEP** 의 x 반폭 | 0 |
| **`NECK_G`** 목선 둘레 | 같은 링의 둘레 | 0 |
| `CAP_TUBE` 소매 통둘레 | \|x\| ∈ [어깨/2, 어깨/2+소매길이] 의 수직 평면 단면(최고 연결성분 · 분리 간격 = SDF 대역폭) 중 **최대** +SEP | 0 |
| **`CAP_H`** 소매산 높이 | `arcLen(famCap(CAP_TUBE/2, h)) = 암홀둘레/2` 의 해(이분법). 실수해 조건 `암홀둘레/2 ≥ CAP_TUBE/2` 를 «먼저» 검사하고 위반이면 **정지**(갈래 D) | 0 |
| **`ARM_G`** 암홀 둘레 | **도출하지 않는다 — 옷 사양 입력**(§1-3). 기본 44.39cm | — |

**전역 최소가 아니라 국소 최소인 이유**: 머리 꼭대기가 몸 전체의 최소 둘레(17.1cm)라
전역 탐색은 정수리를 집는다. 목은 어깨와 턱 «사이»의 국소 최소다(값으로 확인: 어깨끝
위로 첫 국소 최소 = y 1.520 · 둘레 39.3cm).

### §2-3 실패 처리 — «말없는 기본값 폴백» 0 (함정 25)

세 지점 전부 이름 붙은 예외로 **정지**한다. 폴백 값은 하나도 없다.

```
 어깨끝 높이를 못 찾는다 — 갈래 D
 목 단면 국소 최소 둘레를 못 찾는다 — 갈래 D
 목 밑동(벽/지붕 전이)을 못 찾는다 — 갈래 D
 소매 대역 팔 단면을 못 찾는다 — 갈래 D
 소매산 실수해 없음 — 암홀 반쪽 …cm < 소매산 반폭 …cm — 갈래 D
```

§4에서 `BSX=1.10`(몸 x +10%)이 실제로 3번째 예외로 정지했다 — **동작이 값으로 확인됐다.**

### §2-4 결정성

전부 순수 기하다 — 난수 0, 시간 의존 0, 부동소수 순서 고정(정점·삼각형 순회 순서가
GLB 순서). 같은 GLB·같은 입력에서 같은 값이 나온다(§3 두 실행에서 비트 동일 확인).

---

## §3 검증 — 기본 치수

### §3-1 ㉡ 파생값 대조 — **통과**

```
 파생 치수      v3 자체 도출     v2(pattern-meta)      차        비율
 목선 반폭         8.52 cm          8.12 cm       +0.40 cm    +5.0%
 목선 둘레        48.04 cm         48.27 cm       −0.23 cm    −0.5%
 소매산 높이      12.92 cm         12.31 cm       +0.61 cm    +4.9%
 암홀 둘레        44.39 cm(입력)   44.39 cm        0.00 cm     0.0%   ← 도출 아님(§1-3)
 몸에서 잰 링:  Y_TOP 1.4687m · Y_NECK 1.5019m · 목 밑동 링 48.04cm/x반폭 7.88cm(+SEP 전)
                소매 대역 팔 최대 34.51cm @ x=22.5cm
```

**서로 다른 원리로 잰 값이 0.23~0.61cm 안에서 만난다.** v2는 측지 링 + 교재 관계식,
v3는 평면 단면 + 볼록 껍질 + SEP 부풀림이다.

### §3-2 ㉠ 착장 관측량 — **실패(갈래 C)**

`d15 · gray · 서브스텝 산정 · 같은 하네스`. 대조군은 **같은 코드에 `V2DIMS=1`**로
파생 4종만 v2 값으로 고정한 실행이다 — 치수 외의 모든 것이 같다.

```
 관측량            v3 도출        v2 치수        차        잡음 바닥      판정
 O1 밑단 둘레     110.45cm      110.45cm      0.00 cm      0.00cm        ✓
 O2 파장           36.82cm       27.61cm    **+9.21 cm**   0.00cm      **✗**
                  (영교차 6)    (영교차 8)
 O3 간극 중앙      8.902mm       8.902mm     0.000 mm      0.000mm       ✓
 O4 접촉          23.77%        23.18%     **+0.59 pp**    0.25pp      **✗**
```

**대조군의 타당성 확인**: `V2DIMS=1` 실행이 O2 = 27.61cm(영교차 8)로 **v3-26 §3-1의
gray 등록값과 비트 일치**한다. 대조 기준선이 옳다.

⟹ **§0 ㉠ 실패 — 갈래 C.** 기준·잡음 바닥을 **고치지 않는다.**

### §3-3 ㉢ S4 시험 — **전부 통과**(양쪽 다)

```
                     v3 도출        v2 치수      문턱
 정착                 180프레임      180프레임    도달 필요        ✓ ✓
 ③a 관통(절대)        0.3663mm      0.4641mm    ≤ 0.5mm         ✓ ✓
 ③b 관통비            0.507         0.950       [0.5,1.25] 참고   ✓ ✓
 자기관통(삼각형 교차)   0             0          0                ✓ ✓
 시접 간극 중앙        2.22mm        2.23mm      2mm 근방         ✓ ✓
 목선 원주비          1.0957        1.1021      ≤ 1.10           ✓ ✓
 λmax                1.3659        1.3648      —                ✓ ✓
 보조 장치(invMass=0)   0             0          0                ✓ ✓
 발산                  0             0          0                ✓ ✓
 벽시계               378초         543초        —
```

**도출 치수로 만든 옷은 «물리적으로 멀쩡하다».** 깨진 것은 안전이 아니라 **기준선
재현**이다 — 다른 옷이 나왔고, 그 차이가 잡음 바닥보다 크다.

### §3-4 ㉠ 실패의 정체 — **O2는 «세는» 양이다** (계기 한계 · 신설)

```
 파생값 차이는 0.23~0.61cm 인데 O2 차이는 9.21cm 다.
 O2 는 밑단 주름의 «영교차 수»(8 → 6)로 파장을 낸다. 주름 개수는 정수다 —
 한 개가 들고 나면 파장이 27.61 → 36.82 로 «계단»으로 뛴다(연속량이 아니다).
 v3-26이 등록한 O2 잡음 바닥 0.00cm 는 **같은 입력 재실행의 바닥**이지
 **입력 섭동의 바닥이 아니다.** 0.4cm 목선 변화가 주름 하나를 없앨 수 있다.
 O4(+0.59pp)도 같은 계열 — 접촉 판정은 정점 단위 이산량이고, 주름 하나가
 사라지면 접촉 정점이 무리로 움직인다.
```

⟹ **v3-26의 「원단이 갈린다」 판정 중 O2 조항은 이 취약성을 안고 있다.**
0.4cm 치수 섭동이 원단 간 차이(9.19cm)와 «같은 크기»의 O2 변화를 만든다.
O4 조항은 인접 차 1.35~2.84pp 이므로 0.59pp 섭동에 뒤집히지 않는다(여유 2.3배).
**S5 성립은 유지되나 O2 단독 근거는 약해진다** — 전략 세션 판정 대상으로 올린다.

---

## §4 치수 변경 동작

**비용 산정 먼저**(회차 프롬프트 조항): d15·gray 착장 1점 = **378~543초**. 축은 6개
(옷 4 + 몸 2)이므로 전 축 착장은 40~55분이다. §3이 **갈래 C**로 정지 조건에 걸렸으므로
**착장은 §3의 2점에서 멈추고**, 이 절은 **파생값 추종(순수 기하 · 즉시)**만 낸다.
**돌리지 않은 것**: 축별 착장 완주 확인 6점. 갈래 F를 함께 적는다.

### §4-1 파생값 추종 — 6축 각 1점

```
 축                   목선반폭   목선둘레   소매산   암홀깊이   팔최대단면
 기준(기본)             8.52cm   48.04cm  12.92cm  21.495cm   34.51cm
 총장   0.70→0.80      8.52     48.04    12.92    21.495     34.51     (무관 — 옳다)
 품     0.55→0.62      8.52     48.04    12.92    20.137     34.51     암홀 깊이만
 어깨   0.45→0.40      9.25     50.41    10.36    20.602     38.19     **4종 전부**
 소매   0.22→0.30      8.52     48.04    12.92    21.495     34.51     (무관 — 옳다)
 몸 z   ×1.15          8.53     51.05    11.07    21.495     37.26     목선둘레·소매산
 몸 x   ×1.10          ── 정지: 「목 밑동(벽/지붕 전이)을 못 찾는다 — 갈래 D」 ──
```

**읽는 법**
- **총장·소매길이가 파생값을 안 움직이는 것이 옳다** — 목 밑동 링과 팔 단면은 그
  치수와 기하적으로 무관하다. v2도 같다.
- **어깨를 좁히면 4종이 전부 움직인다** — `Y_TOP`(어깨끝 높이)이 바뀌어 목 밑동 탐색
  시작점이 옮기고, 소매 대역 x 시작점도 옮긴다. 연쇄가 옳게 걸려 있다.
- **몸 z를 15% 키우면 목선 둘레 +6.3% · 소매산 −14%** — 몸이 두꺼워지면 팔 통둘레가
  커지고(34.51→37.26), 암홀 둘레가 고정이므로 소매산이 낮아진다. 기하적으로 옳다.
- **몸 x +10%는 «정지»한다** — 몸 어깨가 옷 어깨(45cm)보다 넓어져 `Y_TOP`이 목 밑동
  위로 올라가고 벽/지붕 전이가 구간 밖으로 나간다. **말없는 폴백 없이 이름 붙은
  예외로 멈춘다**(§2-3 · 함정 25). 갈래 E의 「안전 범위」다.

### §4-2 안전 범위 — 몸 x 축

```
 BSX   1.00      1.02      1.04      1.06      1.08      1.10
 Y_TOP 1.4687   1.4671    1.4023    1.4729    1.4729    ── 정지 ──
 Y_NECK 1.5019  1.4989    1.4990    1.5003    1.5005
 목선둘레 48.04  51.45     51.90     51.19     51.61
 ⟹ 안전 범위 = BSX ≤ 1.08 (옷 어깨 45cm 고정 기준). 1.10에서 이름 붙은 정지.
```

### §4-3 계기 한계 — **목 밑동 도출이 «한 복셀»에 민감하다** (신설)

```
 BSX 1.00 → 1.02 (몸 2% 변화)에서
   Y_NECK   1.5019 → 1.4989   (−3.0mm ≈ SDF 복셀 h 3.951mm 의 0.76배 = «한 표본»)
   목선 둘레 48.04 → 51.45cm  (+7.1%)
 목 밑동 대역은 단면 둘레가 «가파르다»(1.505m 45.95cm → 1.500m 53.15cm).
 벽/지붕 전이를 복셀 간격으로 표본하고 선형 보간하므로 한 표본의 이동이 3.4cm 를 만든다.
 «기준(문턱 1/2 · 45°)은 그대로 두고», 근찾기 정밀도만 올리는 것이 다음 수다
 (wallFrac 위에서 이분법 — 이 판에서는 §0 순서 조항 때문에 손대지 않았다).
 Y_TOP 자체도 불안정하다(BSX 1.02→1.04 에서 1.4671→1.4023) — 몸이 넓어지면
 |x|=어깨/2 가 어깨 능선에서 «위팔»로 옮겨 붙는다.
```

### §4-4 `pattern-meta.json` 읽기 — **0**

```
 grep -rn "pattern-meta" scripts/v3*.ts src/v3/     ⟹ 주석 3곳뿐(§6 회귀에 원문)
 readFileSync(META) 삭제 · type Meta 삭제 · const META 삭제 · meta.* 참조 0
 남은 v2 유래 «수»는 대조표용 V2REF 4종 + seamCounts 4종 — **출력에만 쓴다**
 (제도·배치·봉제·착장의 어느 경로도 읽지 않는다).
```

---

## §5 갈래 판정 — 실행 «전» 고정분에 답한다

| 갈래 | 판정 | 근거 |
|---|---|---|
| **A** §0 통과 + 추종 + 완주 | **불성립** | §0 ㉠ 이 O2·O4 에서 실패했다 |
| **B** 파생값은 다른데 §0 통과 | **불성립** | 파생값은 잘 맞았으나(0.23~0.61cm) §0 이 실패했다 |
| **C** §0 기준 실패 | **성립 (주)** | O2 **+9.21cm**(바닥 0.00) · O4 **+0.59pp**(바닥 0.25). **기준·바닥 변경 0** · S4·S5 재판정 필요 |
| **D** 원리적으로 같은 답이 안 나온다 | **성립 (부분 · 암홀 1종)** | 몸에는 44.39cm 링이 없고, 팔 링을 쓰면 소매산이 퇴화한다(§1-3). **대안 = 옷 사양 입력으로 승격**(구현 완료) |
| **E** 치수 변경 시 깨진다 | **성립** | 몸 x 축 **BSX ≤ 1.08** 이 안전 범위. 1.10 은 이름 붙은 정지(§4-2) |
| **F** 비용이 커서 축을 다 못 본다 | **성립** | §4 축별 **착장 완주 6점 미실행**(축당 378~543초 · 총 40~55분). C 정지 조항이 먼저 걸렸다 |
| **G** 판정창 이탈 | 불성립 | — |
| **H** 판정 불가 | 불성립 | — |

⟹ **C(주) + D(암홀 1종) + E + F.**

**#56 은 «해소»되지 않았다 — 절반이다.** `pattern-meta.json` 의존은 **끊겼고**(읽기 0)
치수 변경도 **추종한다**. 그러나 **기준선이 재현되지 않는다** — 도출값으로 만든 옷은
물리적으로 멀쩡하지만(㉢ 전부 통과) v3-26 기준선과 **다른 옷**이다.

**전략 세션이 판단할 갈림 2건**
1. **기준선을 어디에 둘 것인가.** 「v2 치수 재현」을 계속 기준선으로 쓸지, 아니면
   **v3 도출 치수를 새 기준선으로 선언**하고 S5 4종을 그 위에서 다시 뜰지.
   후자면 재실행 비용은 4종 × 378~543초 ≈ **25~36분**이다.
2. **O2 조항의 처분.** O2는 주름 «개수»(정수)로 파장을 내므로 0.4cm 섭동이
   원단 간 차이와 같은 크기의 변화를 만든다(§3-4). v3-26 S5 성립의 O2 근거를
   유지할지, O4 단독으로 좁힐지(O4는 여유 2.3배로 버틴다).

---

## §6 회귀 — 전량 재실행

```
 v3:s1    갈래 A — ①②③③′ 전부 통과 (잔류속도 1.96e-8m/s · 총질량 일치 true)   불변
 v3:s2    갈래 B — ②-B(ke≠B, B_eff=ke/4). 등재된 상태 그대로                  불변
 v3:s3    갈래 A — ①②④ 통과                                                불변
 v3:s3b   ④b #24 비용표 산출 정상(every 1/4/16 = 108249/101672/98139 ms)        불변
 v3:body  [v3-16 §2 안전장치] 0.753 / 0.620 / 0.732 / 0.669 — **비트 일치**
          구 계열 보존 YES · 일치 구간 [0.5,1.25] YES · 단조성 YES              불변
 npx tsc -b   rc=0
 npm run build ✓ built in 560ms
 pattern-meta 읽기 경로  주석 3곳 + 출력 문자열 1곳뿐 · 실행 경로 **0**
```

**제도 층을 고쳤는데 다른 경로가 하나도 안 깨졌다** — `patternCore`(v2)와 v3 하네스는
분리돼 있고, `pattern-meta.json` 은 v3S4 «혼자» 읽고 있었다.

---

## §7 잔여 갱신

| # | 항목 | 상태 |
|---|---|---|
| **56** | 파생 치수의 v2 의존 | **절반 해소** — 파일 읽기 0 · 치수 추종 ✓ / **기준선 미재현**(갈래 C) · 암홀 1종은 입력 승격(갈래 D) |
| **58** | **O2(밑단 파장)가 이산량이다** *(신설)* | 영교차 «개수»로 파장을 내므로 0.4cm 치수 섭동이 27.61↔36.82cm 계단을 만든다. v3-26 잡음 바닥 0.00cm 는 **재실행 바닥**이지 **섭동 바닥이 아니다** |
| **59** | **목 밑동 도출이 한 복셀에 민감하다** *(신설)* | 몸 2% 변화 → `Y_NECK` −3.0mm(≈한 표본) → 목선 둘레 +7.1%. 기준(45°·1/2)은 두고 **근찾기 정밀도**를 올리는 것이 다음 수 |
| **60** | **`Y_TOP`(어깨끝 높이)이 불안정하다** *(신설)* | 몸이 넓어지면 \|x\|=어깨/2 가 어깨 능선에서 위팔로 옮겨 붙는다(BSX 1.02→1.04 에서 1.4671→1.4023) |
| **55** | v2 빠른 모드 / v3 정밀 모드 이중 설계 | 미결(이월) |
| **57** | 입력 규약 어긋남 3건 | 미결(이월). `sleeveWidthM`은 이 판에서 **소매 통둘레를 몸에서 도출**하며 «쓰이지 않는 값»으로 굳었다 |
| — | **부분 접속 1/5** | **미완** — 2/5(조립 추출)로 넘어가려면 갈림 1(기준선 처분)이 먼저다 |

**정정**: v3-17 §1 「치수는 데이터로 읽는다」 — 그 「데이터」가 **v2 제도의 산출물**이었다.
v3-31이 파일 읽기를 없앴고, 남은 v2 유래 값은 **대조표 출력용 `V2REF` 뿐**이다.

호출한 ecc 에이전트/스킬: **0**

---
---

# v3-32 — #59 근찾기 정밀화 + 새 기준선 확립 (부분 접속 1/5 완결)

호출한 ecc 에이전트/스킬: **0**

## §0 전략 세션 판정 4건 등재 + 순서 조항 + §2 판정 기준

> **이 절은 §1 수리 코드를 «한 줄도 쓰기 전»에 커밋한다.** git 이력이 순서의 증거다.

### §0-1 전략 세션 판정 4건 (수령분 · 원문 요지)

1. **v3-31 §0 ㉠은 «설계 결함»이었다 — 귀책 전략 세션.** 잡음 바닥은 **같은 입력
   재실행**의 값인데 ㉠은 **입력이 다른** 두 실행을 비교했다. **바닥의 정의역 밖**이다.
   #50·#53·#58과 같은 계열. **판정은 뒤집지 않는다**(갈래 C 유지) — 결함만 등재한다.
2. **O2는 삭제하지 않고 «정의역»을 못박는다**: 「같은 치수·같은 입력에서 **원단만**
   다를 때만 유효 · **입력 섭동 비교에는 쓸 수 없다**」. **S5는 흔들리지 않는다** —
   v3-26 §0-2가 이미 「①②의 주 채널은 **O4** · O1~O3는 기록만」으로 등록했다.
3. **암홀의 «옷 사양» 승격 승인.** 근거가 값으로 섰다(§1-3).
4. **v3 도출 치수를 새 기준선으로 선언한다. 단 #59를 «먼저» 고친다** — 정밀화하면
   기본 치수의 파생값도 움직이므로, 지금 세우면 두 번 세운다.

### §0-2 순서 조항

```
 이 커밋(§0 등록 · 수리 코드 0줄)
   → §1 수리 + 수렴 문턱 «도출» (§2 실행 전 커밋)
   → §2 안정성 검증
   → §2 통과 시에만 §3 새 기준선.  §2 실패면 §3 착수 «0».
 §2·§3 결과를 보고 이 절의 어떤 수도 고치지 않는다.
```

### §0-3 §2 판정 기준 — **X를 «지금» 도출한다**

**㉮ 민감도** — 몸을 x축으로 `s = 1+ε` 배 할 때 목선 둘레 변동

```
 판정:  |ΔP| / P  ≤  X(ε)          X(ε) = ε · (2π·R_x / P)
 도출:  볼록체의 둘레는 코시 공식으로 P = ∫₀^2π h(θ) dθ (h = 지지함수).
        x축만 s배 하면  h_s(θ) = max(s·x sinθ + z cosθ)
                              = max( (x sinθ + z cosθ) + (s−1)·x sinθ )
        ⟹ |h_s(θ) − h(θ)| ≤ (s−1)·max|x| = ε·R_x   (모든 θ에서)
        ⟹ |ΔP| ≤ 2π·ε·R_x        ⟹ |ΔP|/P ≤ ε·(2π R_x / P)
        **높이가 고정이면 이것은 «수학적 상한»이다.** 상한을 넘는 분은 몸이 아니라
        «Y_NECK 표본이 움직인 것»이다 — 그것이 #59의 정체다.
 이 몸의 계수:  R_x = 7.88cm(목 밑동 링 x반폭 · SEP 전) · P = 48.04cm
                2π R_x / P = 2π×7.88/48.04 = **1.031**
 ⟹  X(0.02) = 2.06%   X(0.04) = 4.12%   X(0.06) = 6.19%   X(0.08) = 8.25%
 대조 대상: v3-31 §4-2와 **같은 점** BSX = 1.00 / 1.02 / 1.04 / 1.06 / 1.08
 v3-31 실측: ε=0.02 에서 **+7.1%** ⟹ 상한 2.06% 의 **3.4배** (수리 전)
```

**㉯ 결정성** — 같은 입력 2회에서 파생값 4종 **비트 동일**.

**㉰ 부수 채널(판정 아님 · 원인 분리용)** — `Y_NECK` 자체의 변동을 함께 적는다.
㉮가 실패했을 때 **표본 잡음인지 전이면의 실제 이동인지**를 가른다.

**㉱ 파생값 변화는 판정이 아니다** — v3-31 §3-1 값(8.52 / 48.04 / 12.92cm) 대비
**크기만 적는다**. 정밀화의 목적이 값을 움직이는 것이므로 «바뀌는 것이 정상»이다.

### §0-4 이 판에서 바꾸지 않는 것

전이 문턱 **1/2**(45°) · 잡음 바닥(O4 0.25pp 등) · v3-26 원문 · O2의 판정 사용(금지) ·
v2 코드 임포트 0 · 흡착 0 · CC 자체 화면 판정 0.

---

## §1 #59 수리 — 근찾기 정밀화 (**§2 실행 «전» 커밋**)

**기준은 손대지 않았다** — 벽/지붕 전이 문턱은 **1/2**(45°) 그대로다. 바꾼 것은 둘이다:
① 벽 비율의 **측도**(대역 평균 → 그 자리의 정확한 값) ② **근찾기**(복셀 표본 + 선형
보간 → 구간 포착 + 이분법).

### §1-1 왜 대역이 한계였는가

```
 v3-31: wallFrac(y, half = h) — 두께 2h ≈ 7.90mm «대역»의 삼각형 «면적» 비율.
        대역 자체가 분해능 하한이다. 근찾기를 아무리 정밀히 해도 그 아래로 못 간다.
```

### §1-2 «교선 길이» 가중은 틀렸다 — 실측으로 기각

첫 구현은 평면 y=c 와 삼각형의 **교선 길이**로 가중했다. **편향이 있다**:
수평에 가까운 면(지붕)은 수평면과 거의 나란해 **교선이 거의 안 생긴다** — 지붕이
통째로 빠져 벽 비율이 위로 편향된다.

```
 교선 길이 가중:  Y_NECK 1.4904m (−11.5mm) · 목선 둘레 **64.95cm** · 반폭 13.95cm
                  ⟹ 어깨 한복판을 집는다. **기각.**
```

### §1-3 옳은 측도 — «단위 높이당 면적»

```
 면 위에서 높이가 dy 오를 때 진행 거리 dl = dy / |n_h|
   (n_h = 법선의 «수평 성분» 크기 · 수직 벽 |n_h| = 1 · 수평 지붕 |n_h| = 0)
 ⟹ dA = ds · dy / |n_h|
 ⟹ 가중치 = 교선길이 × |n| / √(n_x² + n_z²)
 대역 → 0 극한에서 v3-31의 «대역 면적»과 «같은 양»이고, 대역은 사라진다.
 완전 수평면(n_h = 0)은 이 평면과 만나지 않으므로 자연히 빠진다.
```

### §1-4 수렴 문턱 도출 — 새 상수 0

```
 문턱 = TOL_SELF = 1e-4 m = **0.1mm**
 근거: v3-12가 등재한 「한 서브스텝의 재수렴 폭」이고, **v3가 두 기하를 «같다»고
       보는 길이**다(S3b ② 허용오차 · v3-19 정착 문턱도 이 값에서 환산했다).
       목 밑동 높이를 그보다 촘촘히 정할 이유가 없다 — 아래로는 v3 어디서도
       구별하지 않는 척도다.
 실측: 포착 칸 3.95mm(= 복셀 h) → 이분법 **6회** → 3.95/2⁶ = 0.062mm < 0.1mm
       |dP/dy| = 0.91 cm/mm  ⟹  **둘레 정밀도 0.91mm** (49.32cm의 0.18%)
```

### §1-5 실패 처리 — v3-31 §2-3 그대로 + 1종 추가

```
 목 단면 국소 최소 둘레를 못 찾는다 — 갈래 D
 목 밑동(벽/지붕 전이)을 못 찾는다 — 갈래 D
 **목 밑동 구간 포착 실패 — 위쪽 끝이 이미 1/2 아래다 — 갈래 D**   (신설)
 말없는 폴백 **0**.
```

### §1-6 #60(`Y_TOP`) 처분 — **이 판에서 고치지 않는다 · 이월**

```
 사유(값): v3-31 §4-2에서 Y_TOP 이 6.5mm 튀는(1.4687→1.4023) 구간에서도
           Y_NECK 은 1.4989~1.5005 (1.6mm 폭) 로 «안정»했다. 목 밑동 탐색은
           Y_TOP 을 «스캔 시작점»으로만 쓰고 위로 올라가며 첫 국소 최소를 잡으므로
           시작점이 낮아지면 더 스캔할 뿐이다 ⟹ **#60은 §2를 막지 않는다**(갈래 F 불성립).
 규모:     Y_TOP 은 «배치 높이»이기도 하다. 고치면 옷이 놓이는 높이가 바뀌어
           §3 기준선이 다시 흔들린다 — 기준선을 두 번 세우지 말라는 §0-1 판정 4와
           같은 이유로 **이 판 밖**이다.
 이월:     #60 유지. 재개 조건 = 몸 슬라이더(체형 변경)를 제품에 붙이는 판.
```

### §1-7 §1 산출 — 기본 치수의 파생값(§2 ㉱ · 판정 아님)

```
                     v3-31(수리 전)   v3-32(수리 후)     차
 Y_NECK               1.5019 m        1.5005 m       −1.4 mm
 목선 반폭             8.52 cm         8.93 cm       +0.41 cm
 목선 둘레            48.04 cm        49.32 cm       +1.28 cm
 소매산 높이          12.92 cm        12.92 cm        0.00 cm  (목선과 무관 — 옳다)
 암홀 둘레            44.39 cm(입력)  44.39 cm(입력)   0.00 cm
 v2 대비: 목선 반폭 +0.81cm(+9.9%) · 목선 둘레 +1.05cm(+2.2%) · 소매산 +0.61cm(+4.9%)
 부수 정정: §1 출력의 「x반폭」이 지지함수 «u축»(=z)을 찍고 있었다 — v축(=x)으로 고쳤다.
           도출에 쓰는 `NECK_A`는 처음부터 v축이었으므로 **값에는 영향이 없다**(출력만).
```

---

## §2 안정성 검증 — **§0-3 등록 기준 통과**

### §2-1 ㉮ 민감도 — 몸 x축 `s = 1+ε`

```
 BSX    Y_TOP     Y_NECK     목선 둘레   |ΔP|/P    X(ε) 상한   판정   반폭     소매산
 1.00   1.4687   1.5005      49.32cm      —          —         —    8.93cm  12.92cm
 1.02   1.4671   1.5005      49.82cm     1.01%      2.06%      ✓    9.08cm  13.40cm
 1.04   1.4023   1.5005      50.39cm     2.17%      4.12%      ✓    9.25cm  13.37cm
 1.06   1.4729   1.5006      50.92cm     3.24%      6.19%      ✓    9.41cm  13.34cm
 1.08   1.4729   1.5006      51.48cm     4.38%      8.25%      ✓    9.58cm  13.32cm
 1.10   ── 정지: 「목 밑동(벽/지붕 전이)을 못 찾는다 — 갈래 D」 (v3-31과 같다) ──
```

**㉮ 4/4 통과.** 실측이 상한의 **약 0.53배**로 일관된다 — 단면이 원에 가까우면
둘레 ≈ π(a+b) 이고 a만 ε배 하면 Δ ≈ ε·a/(a+b) ≈ 0.5ε 이다. **몸이 변한 만큼만 변한다.**

```
 수리 전후 (같은 점 · ε = 0.02)
   v3-31  Y_NECK 1.5019 → 1.4989 (−3.0mm)   목선 둘레 +7.1%   ⟹ 상한의 **3.4배** ✗
   v3-32  Y_NECK 1.5005 → 1.5005 ( 0.0mm)   목선 둘레 +1.01%  ⟹ 상한의 0.49배 ✓
 Y_NECK 변동 폭(BSX 1.00~1.08):  3.0mm → **0.1mm**  (30배 축소)
```

### §2-2 ㉰ 부수 채널 — 원인 분리

`Y_NECK`이 전 대역에서 **0.1mm 안**에 있다 ⟹ v3-31의 +7.1%는 **전이면의 실제 이동이
아니라 표본 잡음**이었다. #59의 귀속이 값으로 확정된다.

### §2-3 ㉯ 결정성 — 통과

같은 입력 2회 실행의 §1 출력 블록 md5 **`f516f71bfa2ced9c5474aa2b8f88316c` 동일**.

### §2-4 ㉱ 파생값 변화 (판정 아님)

§1-7 표 참고. 목선 둘레 +1.28cm · 반폭 +0.41cm · 소매산 0.00cm.
**안전 범위는 v3-31과 같다 — `BSX ≤ 1.08`.**

---

## §3 새 기준선 **B-v3** — d15 · 4종 · v3 도출 치수

**v3-26 기준선은 삭제하지 않는다.** 이름을 붙여 병기한다:
**「A-v2 = v2 치수 기준선」(v3-26 · d11)** ↔ **「B-v3 = v3 도출 치수 기준선」(이 절 · d15)**.

### §3-1 관측량 4종 — B-v3 등재

```
 원단     k[N/m]     ρ      B[N·m]    sub  정착  프레임  벽시계    s/f
 gray       69.0  0.187  2.3192e-5   139   180    180    349초   1.94
 denim    2027.8  0.324  6.4200e-5   ...   190    190   1594초   8.39
 sweat      25.0  0.224  5.9470e-5   ...   190    190    252초   1.33
 swim      209.2  0.204  6.0240e-5   ...   180    180    645초   3.58
 총 벽시계 2840초 = **47.3분** (전략 세션 추정 25~36분 대비 +31% · CPU 경합 없음)

 원단     O1 밑단둘레   O2 파장(영교차)    O3 간극중앙   **O4 접촉**
 gray      110.45cm     36.82cm (6)      8.902mm      **23.88%**
 denim     110.42cm     27.61cm (8)      8.902mm      **18.37%**
 sweat     110.42cm     36.81cm (6)      8.902mm      **24.25%**
 swim      110.43cm     36.81cm (6)      8.902mm      **21.93%**
 발산 0 · 4종 전부 정착(상한 600 미도달)
```

### §3-2 S5 재판정 — **O4 주 채널 · O2는 기록만**(§0-1 판정 2)

```
 ① 갈리는가   O4 정렬  18.37 < 21.93 < 23.88 < 24.25
              인접 차  **3.56 / 1.95 / 0.37 pp**  — 전부 잡음 바닥 0.25pp 초과  ✓
 ② 순서       denim < swim < gray < sweat  — **v3-26(A-v2)과 완전 일치**  ✓
 O2(기록만)   {denim} ↔ {gray, sweat, swim} 두 무리. A-v2에서는 {gray,denim} ↔ {sweat,swim}
              였다 — **무리가 바뀐다.** #58 정의역 조항대로 **판정에 쓰지 않았다**
 O1·O3        A-v2와 같이 판별력 없음(O1 인접차 0.01~0.02cm · O3 4종 완전 동률)
```

**⟹ S5는 새 기준선에서도 선다.** 여유도 커졌다 — 최소 인접 차가 A-v2의 1.35pp 대비
0.37pp로 **줄었지만** 바닥 0.25pp 대비 여전히 1.5배다(A-v2 d15는 0.50pp였다).

### §3-3 S4 시험 4종 — **2종에서 각 1조항 실패**

```
 원단   ③a 관통(≤0.5mm)   ③b 비   교차  시접중앙   목선(≤1.10)   λmax   정착  보조
 gray   **0.7915  ✗**     0.851    0    2.22mm     1.0848 ✓    1.3697  180 ✓  0 ✓
 denim    0.3944  ✓       0.614    0    2.18mm     1.0450 ✓    1.2131  190 ✓  0 ✓
 sweat    0.3323  ✓       0.774    0    2.38mm   **1.1515 ✗**  1.4873  190 ✓  0 ✓
 swim     0.3696  ✓       0.496    0    2.21mm     1.0764 ✓    1.2995  180 ✓  0 ✓
```

**A-v2 치수의 같은 해상도(d15)와 대조** — 어느 것이 새로 생겼고 어느 것이 고쳐졌는가:

```
 목선 원주비 (문턱 1.10)
   v2 치수 d15 (v3-27 §2-5):  gray 1.1043 ✗ · denim 1.0549 ✓ · sweat 1.1728 ✗ · swim 1.2868 ✗
   v3 도출  d15 (이 절)    :  gray 1.0848 ✓ · denim 1.0450 ✓ · sweat 1.1515 ✗ · swim 1.0764 ✓
   ⟹ **실패 3종 → 1종.** 목선을 몸에서 뽑으니 목선이 «덜 늘어난다» — 도출의 목적 그대로다
 ③a 절대 관통 (문턱 0.5mm)
   v2 치수 d15 (v3-28)     :  gray 0.3957 ✓ · denim 0.3119 ✓ · sweat 0.4319 ✓ · swim 0.3672 ✓
   v3 도출  d15 (이 절)    :  gray **0.7915 ✗** · denim 0.3944 ✓ · sweat 0.3323 ✓ · swim 0.3696 ✓
   ⟹ **gray 1종이 새로 생겼다.** ③b 비 0.851 ∈ [0.5,1.25] 이므로 «모형은 설명한다» —
     깨진 것은 «물리 안전» 조항이다. 두께 1mm의 0.79배(문턱은 절반 = 0.5mm)
 v3-31의 같은 원단·같은 해상도(수리 «전» 도출 치수): gray 관통 0.3663 ✓ · 목선 1.0957 ✓
   ⟹ gray ③a 실패는 **§1 수리(목선 둘레 48.04 → 49.32cm)와 함께 나타났다**
```

**문턱은 하나도 조정하지 않았다.**

---

## §4 갈래 판정 — 실행 «전» 고정분에 답한다

| 갈래 | 판정 | 근거 |
|---|---|---|
| **A** §1 수리 + §2 통과 + §3에서 4종이 O4로 갈리고 순서 유지 | **조건 3항 «전부» 성립** | §1 수리 완료 · §2 ㉮ 4/4·㉯ 통과 · §3 인접 차 3.56/1.95/0.37pp > 0.25 · 순서 denim<swim<gray<sweat 일치 |
| **B** §3에서 순서가 바뀐다 | 불성립 | v3-26과 완전 일치 |
| **C** §2 실패 | 불성립 | 등록 상한 X(ε)의 0.49~0.53배 |
| **D** 수렴 문턱 도출 실패 | 불성립 | `TOL_SELF` 0.1mm에서 도출(§1-4) |
| **E** 어느 원단이 «정지 예외»에 걸린다 | 불성립 | 4종 전부 완주 · 예외 발화 0 |
| **F** #60이 §2를 막는다 | 불성립 | Y_TOP 6.5mm 요동에도 Y_NECK 0.1mm(§1-6) |
| **G** 판정창 이탈 | — | 아래 참고 |
| **H** 어디에도 안 걸린다 → 판정 불가 | **성립(병기)** | **§3이 등록한 S4 시험이 2종에서 각 1조항 실패했는데, 그 결과에 대응하는 갈래가 A~H에 없다** |

### ⟹ **A(조건 성립) + H(S4 결과가 갈래 밖 — 판정 불가) · «종결 선언 0» · 정지**

**A를 단독으로 선언하지 않는 이유.** A의 귀결은 「#56 종결 · #59 종결 · 새 기준선 확립 ·
**2/5로 진행**」이다. 그런데 §3이 등록한 S4 조항 중 **gray의 ③a(물리 안전)가 실패**한다
(0.7915mm > 0.5mm). 갈래 A의 조건문에 S4가 없다는 이유로 «안전 조항이 깨진 채 종결»을
선언하는 것은 상시 규약(「갈래에 없는 결과면 판정 불가로 적고 정지」)에 어긋난다.

**개별 종결 판단** — 전략 세션 몫이나, 값은 갈라 둔다:

```
 #59  **종결 가능** — 이 판의 표적이었고 §2가 등록 기준으로 통과했다(민감도 3.4배 초과 →
      0.49배 · Y_NECK 변동 30배 축소 · 결정성 동일). S4 실패와 «독립»이다
 #56  **미결** — 파일 의존은 끊겼고 치수도 추종하나, 새 기준선의 S4가 2종에서 깨진다
 B-v3 **조건부 확립** — S5 재판정(①②)은 통과했으므로 «원단 축»의 기준선으로는 선다.
      «S4 축»의 기준선으로는 gray 1종이 미달이다
```

### 전략 세션이 판단할 갈림 3건

1. **gray ③a 실패의 처분.** 절대 관통 0.7915mm는 두께(1mm)의 0.79배이고 ③b 비는
   0.851로 **모형 구간 안**이다(즉 「예상되는 크기」다). 문턱 0.5mm는 v3-29가
   「두께의 절반」으로 도출했다. ㉠ 해상도를 낮춰(d13·d11) 회수할지 ㉡ 문턱의 도출을
   다시 볼지 ㉢ 원단별 안전 한계로 등재할지(#51 계열). **CC는 문턱을 건드리지 않았다.**
2. **sweat 목선 1.1515 실패의 처분.** v2 치수 d15에서는 **3종이 실패**했고 도출 치수로
   **1종으로 줄었다** — 방향은 옳으나 sweat(k=25, 가장 무른 원단)는 남는다.
   원단별 한계(#51)로 등재할지, 목선 도출에 원단 의존을 넣을지.
3. **O2의 무리 변화 기록.** A-v2 {gray,denim}↔{sweat,swim} → B-v3 {denim}↔{gray,sweat,swim}.
   판정에는 안 썼으나(#58 정의역) **B-v3 기록으로 남는다.**

---

## §5 회귀 — 전량 재실행 · 불변

```
 v3:s1    갈래 A — ①②③③′ 전부 통과                                    불변
 v3:s2    갈래 B — ②-B(ke≠B, B_eff=ke/4). 등재된 상태 그대로             불변
 v3:s3    갈래 A — ①②④ 통과                                          불변
 v3:s3b   ④b #24 비용표 산출 정상                                       불변
 v3:body  [v3-16 §2 안전장치] 0.753 / 0.620 / 0.732 / 0.669 — **비트 일치**
          구 계열 보존 YES · 구간 [0.5,1.25] YES · 단조성 YES              불변
 npx tsc -b   rc=0
 npm run build ✓ built in 492ms
 pattern-meta.json  실행 경로 읽기 **0** 유지 (주석 2곳 + 출력 문자열 2곳뿐)
```

---

## §6 잔여 갱신

| # | 항목 | v3-31 | v3-32 |
|---|---|---|---|
| **59** | 목 밑동 도출이 한 복셀에 민감 | 신설 | **수리 완료 · 종결 가능**(전략 세션 확인 대기). 측도를 «단위 높이당 면적»으로 정확화 + 이분법(문턱 `TOL_SELF` 0.1mm 도출). 민감도 상한의 3.4배 → **0.49배** · Y_NECK 변동 3.0 → **0.1mm** |
| **56** | 파생 치수의 v2 의존 | 절반 해소 | **여전히 미결** — 파일 읽기 0 · 치수 추종 ✓ · S5 재판정 ✓ / **새 기준선의 S4가 2종에서 실패** |
| **58** | O2가 이산량이다 | 신설 | **정의역 못박음 등재**(전략 세션 판정 2): 「같은 치수·같은 입력에서 원단만 다를 때만 유효 · **입력 섭동 비교 금지**」. 이 판에서 판정에 **쓰지 않았다** |
| **60** | `Y_TOP` 불안정 | 신설 | **이월**(사유 = §1-6: §2를 막지 않고, 고치면 배치 높이가 바뀌어 기준선을 두 번 세운다). 재개 조건 = 몸 슬라이더를 제품에 붙이는 판 |
| **61** | **gray가 ③a(물리 안전)에서 실패한다** *(신설)* | — | d15 · 도출 치수에서 **0.7915mm > 0.5mm**. ③b 비 0.851은 구간 안(모형은 설명한다) · v2 치수 d15는 0.3957 통과 · 수리 전 도출 치수도 0.3663 통과 ⟹ **§1 수리(목선 48.04→49.32cm)와 함께 나타났다** |
| **62** | **A-v2 / B-v3 두 기준선이 공존한다** *(신설)* | — | **A-v2**(v2 치수 · v3-26 · d11) ↔ **B-v3**(v3 도출 치수 · v3-32 · d15). **v3-26 원문 무삭제.** 앞으로 어느 쪽 기준선인지 «이름으로» 적는다 |
| **51** | 안전 한계가 원단마다 다르다 | — | **보강** — 목선 원주비 실패가 v2 치수 3종 → 도출 치수 1종(sweat)으로 줄었다. 남는 것은 가장 무른 원단이다 |
| **55·57** | 이중 모드 · 입력 규약 어긋남 3건 | 이월 | 이월 |
| — | **부분 접속 1/5** | 미완 | **미완 유지** — 2/5(조립 추출)는 갈림 1·2의 처분 뒤 |

**§0 ㉠ 설계 결함 등재(전략 세션 자기 귀책 · 판정 4건 중 1)**: v3-31 §0 ㉠은
**잡음 바닥의 정의역 밖**에서 판정했다 — 바닥은 «같은 입력 재실행»의 값인데
㉠은 «입력이 다른» 두 실행을 비교했다. #50·#53·#58과 같은 계열이다.
**v3-31의 갈래 C 판정은 뒤집지 않는다**(결함만 등재).

호출한 ecc 에이전트/스킬: **0**

---
---

# v3-33 — #61 회수 시도 + 한계 등재 (부분 접속 1/5 닫기)

호출한 ecc 에이전트/스킬: **0**

## §0 전략 세션 판정 4건 + 판정 기준 (**§1 실행 «전» 커밋**)

### §0-1 전략 세션 판정 4건 (수령분)

1. **#59 종결 승인.** 이 판의 표적이었고 등록 기준으로 통과했으며 S4 실패와 **독립**이다.
2. **갈래 A에 S4를 안 넣은 것은 «전략 세션 귀책»** — 등재한다.
   **앞으로 「기준선 확립」 갈래에는 S4 조항을 반드시 포함**한다.
   (v3-32가 A의 조건을 다 만족하고도 종결을 선언할 수 없었던 원인이다.)
3. **gray ③a — 문턱을 내리지 않고 «설정»을 보수적으로 바꿔 회수**한다.
   물리적 파탄이 아니다(0.7915mm < 두께 1mm · ③b 0.851로 **모형이 설명한다**).
   원인도 정합한다 — 목선 +1.28cm로 옷이 더 내려앉아 **더 날카로운 특징**에 닿았다.
   **안전이 속도보다 우선.**
4. **sweat 목선은 «원단 조건부 한계»로 등재**(#44 연장). 재도출은 **별도 판**.

### §0-2 판정 기준 — 실행 «전» 고정

```
 ㉠ 회수 판정   d13 · gray · v3 도출 치수에서 **③a 절대 관통 ≤ 0.5mm**.  **문턱 변경 0**
 ㉡ 동반 조항   그 실행의 나머지 S4 — 자기관통(삼각형 교차) 0 · 시접 · λmax · 정착 ·
                보조 장치 0.  ③b 비 [0.5,1.25]는 **참고**(v3-29가 정의역을 v3-16 계열로
                한정했다 — 옷 장면 적용은 미해결)
 ㉢ S5 유지     d13으로 내리면 4종을 다시 봐야 하는지 **§1 결과로 판단**한다
 ㉣ 순서       §1(gray 1점) → §2(판단) → §3(필요 시 4종).
                **§1 결과를 보고 ㉠을 고치지 않는다**
```

### §0-3 4종 재실행 비용 — **미리 산정**(㉢ 조항)

```
 기준  B-v3 d15 4종 실측 총 2840초 (gray 349 · denim 1594 · sweat 252 · swim 645)
 배율  v3-27 §1-3 gray 실측  d11 1630초 / d13 1038초 / d15 610초 / d18 307초
       d15 → d13 = 1038/610 = **1.702배**      d15 → d11 = 1630/610 = **2.672배**
 산정  4종 d13 ≈ 2840 × 1.702 = **4,834초 ≈ 81분**
       4종 d11 ≈ 2840 × 2.672 = **7,588초 ≈ 126분**
 (같은 기계·같은 하네스 가정. CPU 경합 시 늘어난다 — v3-26이 등재한 함정)
```

### §0-4 이 판에서 바꾸지 않는 것

③a 문턱 0.5mm · 목선 문턱 1.10 · 잡음 바닥(O4 0.25pp 등) · O2의 판정 사용(금지) ·
v3-26 원문 · v2 코드 임포트 0 · 흡착 0 · CC 자체 화면 판정 0.

