# 41 — 5/5-α 프린트 «합성 층»: 대표색 + 가슴 프린트 + 브리지 복원 (v3-60 · **물리 0프레임**)

v3-60. 브랜치 `v3-60-print-composite`(사전 등록 · **이 CC 세션 전용**). 호출한 ecc 에이전트/스킬: **0 / 0**

## git 스탬프

```
 브랜치  v3-60-print-composite      워크트리  /Users/banshk/projects/fit-simulator
 기점    65f3339 (v3-59 HEAD)       직전 회차  65f3339
 커밋    42f677d  §0  화면 판정 수령 + 판정 기준 (집행 «전»)
         (본문)   §1 계약 특정·이식 · §2 게이트·자기검사 · §3 캡처 · §4 갈래 E
 검사    npx tsc -b rc=0 · npm run build 통과 · **물리 0프레임 · printUv/garmentScene diff 0**
```

---

## §0 판정 수령 — **집행 «전» 커밋**

### §0-1 화면 판정 수령

```
 **UV 층 = «합격»**(기하 정립 · **3종 동일** · **왜곡 0** · **G7 정합**).
 **제품 표시 = «미완» · α 완결 «보류».** 사유 **3건 전부 v3-59 «등재 범위 밖» 항목**:
   **①** **합성 층 부재**(자산 «원본»이 앞판 «전면»에 그대로)
   **②** **앞·뒤 «색 불일치»**
   **③** **브리지 부재**
 **부수 등재**: **어깨 집힘(#104)이 «색 대비»로 가시화** — **새 결함 아님 · #104 «병기»만**.
```

### §0-2 브리지 처분 수령

```
 프린트 모드에서 **스트립 = «대표색 단색»의 «별도» 지오메트리로 복원**.
 **텍스처 불가 근거** = **스트립 UV 가 «단일 패널 정규화 공간»에 속하지 않는다**
   (스트립은 **패널을 가로지른다** — v3-59 §3 등재) · **v3-45 «목적 보존»**.
```

### §0-3 §1 합성 층 — **v2 계약 «문언» 준거**(코드 임포트 0 · **새 수 0**)

```
 v2 합성 계약을 **코드에서 «값으로» 특정**한다 — `PatternPreview.tsx:125-190`:
   **①** **대표색의 «출처»**(**도출식인가 자산 메타인가** — **문언 그대로 인용** ·
        **손으로 색을 고르지 말 것**)
   **②** **가슴 프린트의 폭·중심·크롭**: `:166-174` **`uMax` 도출식 그대로**
        (v1 상수 상속분은 «**상속 사실**»을 **주석에 등재**하고 **그대로 쓴다** — **새 수 0**)
 **`src/v3/printComposite.ts` 신설(순수)**: **대표색 + 앞판 «가슴 영역» 프린트** 합성 텍스처 생성.
   **뒤판·소매·브리지 = 같은 대표색.** **앞판 «가슴 밖» 영역도 대표색.**
 **`productView` 표시 층만 수정** · **`printUv.ts` / `garmentScene.ts` diff 0**
   (**G7 재판정으로 «무변경» 확인**)
```

### §0-4 §2 게이트

```
 **G1**(v2 임포트 0) · **G2**(물리 0프레임) · **G3**(**손 상수 0** — **대표색·폭·중심 전부 «출처» 명기**) ·
 **G4**(정본 3종) · **G7**(**3건 «재판정»** — §1 이 **UV 를 안 건드렸다는 «값» 근거**) ·
 **blob 무변조 sha**
 **자기검사 1건 «신설»**: **브리지 스트립 수 = v3-45 등재값(258 / 232 / 258)과 «일치»** ·
   **프린트 모드 on/off 에서 «동일»**
```

### §0-5 §3 캡처

```
 **3종 × front/side/back-p 합성 on = 9장** + **브리지 on/off 대조 side 2장(gray)** · **CC 화면 판정 0**
```

### §0-6 §4 갈래 (사전 등록)

| 갈래 | 조건 | 귀결 |
|---|---|---|
| **A** | G 전량 + 자기검사 통과 · 캡처 산출 | **사용자 화면 판정 상신**(**α 완결 «후보»**) |
| **B** | **대표색 «출처»가 v2 코드에서 특정 불가**(**손 상수만 발견**) | **정지** · **그 사실 상신** · **색 «임의 선정» 0** |
| **E** | 판정 불가 | 정지 · 사실만 |

### 이 판에서 바꾸지 않는 것

**물리 0프레임** · **`solver` / `garmentScene` / `s4Gate` / `printUv` diff 0** · **핏 색 채널 0줄** ·
문턱·정본 **0** · **v2 경로 0줄** · **`V2DIMS` off**.

### 순서

```
 §0 커밋 → §1 계약 특정 → §1 구현 → §2 → §3 → §4 판정 · 상신
```

---

## §1 합성 층 — **v2 계약 «값으로» 특정 + 이식**

### §1-1 계약 특정 (§0-3 ① · **갈래 B 불발화**)

```
 **대표색의 «출처» = `borderRepresentativeColor(image)`**(`garmentTextureComposite.ts:43-100`) —
   **테두리 띠(`BORDER_BAND_FRACTION` 0.08)의 «최빈 색 버킷»(`BUCKET` 24) 평균**.
   알파 < 10 제외 · rgb > 235(흰 배경) 제외. **버킷 없으면 rgb(128,128,128) 폴백.**
 ⟹ **손 상수가 아니라 «도출식»이다** ⟹ **갈래 B(대표색 출처 특정 불가) 불발화 · 색 임의 선정 0.**
 **`averageGarmentColor` 는 쓰지 않는다** — v1 이 «실패로 등재»한 경로다(v2 주석 `:11-15` · 70 §5-1).
 **②** 폭·중심: `PRINT_WIDTH_FRACTION` **0.45** · `PRINT_TOP_FRACTION` **0.25** ·
   `panelW = OUTPUT_SIZE(512) × uMax` · `destX = (panelW − destW)/2`
   (`:389-393`) · 하단 초과 시 **종횡비 지켜 축소 + «고지»**(`:396-400`).
   **v2 주석이 「실측 아님 · 눈대중 초기값」으로 그 «지위»를 이미 등재**했고, **그 지위째 상속**한다
   (**새 수 0**).
```

### §1-2 이식 — `src/v3/printComposite.ts` 신설(순수 · **코드 임포트 0**)

```
 옮긴 것: **① 대표색** · **② 프린트 bbox**(거리 문턱 55 · 프레임 비율 0.9) ·
   **③ 배치(폴백 경로)** · **④ 재스캔 G2′**(아래 정정분)
 옮기지 «않은» 것: **성분별 «개별 배치»(P34)** — `garmentRegion`(**업로드 크롭 메타**) 위에서
   각 성분을 제 자리로 옮기는 정련이다. v3 의 원본은 **저장소 자산 파일**이라 그 메타가
   **원리적으로 없고**, v2 도 그때 **«폴백» 경로**(`?autofit=1` · v1)를 탄다 ⟹ **경로 선택이지 누락이 아니다.**
```

### §1-3 ★ **판독 정정 1건 — 구현 «커밋 전»에 고쳤다**

```
 1차 판독에서 **재스캔 G2′(78회차)** 를 「`garmentRegion` 위의 정련」으로 분류해 **«옮기지 않음»에 넣었다.**
 **틀렸다.** 실행이 드러냈다 — 첫 자산에서 **프레임 문턱이 «발동»해 프린트가 통째로 버려졌다.**
 v2 주석(`:203-208`)이 그 사태를 정확히 적어 두었다: 「배경이 통째로 «프린트»로 잡혀 bbox 가
 프레임 전체가 된다 … **색으로는 못 가른다** … 그래서 **위상**을 쓴다: 후보 화소를 **8-연결 성분**으로
 나누고 **프레임 «경계에 닿는» 성분을 제외**한다 … **새 상수 0**」.
 ⟹ **재스캔은 `garmentRegion` 과 «무관»하고 bbox «자체»를 고친다** ⟹ **옮겼다.**
 **구현을 «커밋하기 전»에 고쳤다** — 결과에 맞춘 조정이 아니라 **계약 판독 오류의 정정**이고,
 그 사실을 **코드 주석과 이 절에 남긴다**(은폐 0).
```

### §1-4 ★ 자산 선택 — **값으로 골랐다**(임의 선정 0)

| 자산 | 크기 | 대표색 | 프린트 bbox | 프레임 문턱 | 재스캔 |
|---|---|---|---|---|---|
| `print-tee.png` | 750×1000(알파 62.3%) | rgb(34,31,37) | **없음** | **발동(프린트 버림)** | 성분 115 · **경계접촉 0 제외** |
| **`print-tee-white.webp`** | 750×1000 | rgb(34,31,37) | **40% × 43%** | **미발동** | 성분 37 · **경계접촉 1 제외** |

```
 **전자는 «검정 옷의 하이라이트»가 대표색과 거리 55 를 넘어 프레임 전역에 퍼진다**
 (재스캔이 돌았지만 **경계에 닿는 성분이 0** — 배경이 아니라 «옷 자체»가 원인이라 위상으로도 못 가른다).
 **이것은 v2 계약이 «이미 등재한 한계»다** — 「색으로는 못 가른다」(v2 :203-206).
 ⟹ **후자를 쓴다.** **자산이 계약 «전제»를 만족하는지를 값으로 판정한 것**이고 **상수 선정이 아니다.**
```

---

## §2 게이트 + 자기검사

### §2-1 게이트 — **전량 통과**

| # | 게이트 | 판정 | 근거 |
|---|---|---|---|
| **G1** | v2 임포트 0 | **통과** | `grep -rn "from '../lib/" src/v3/` **0건**. 계약은 **문언 준거** · 자산은 **복사**(v3-13) |
| **G2** | 물리 0프레임 | **통과** | 정착 blob 주입 · 솔버 스텝 0 |
| **G3** | 손 상수 0 | **통과** | 대표색 = **도출식** · 폭·중심 = **v1 상속분(지위째)** · **새 수 0** |
| **G4** | 정본 3종 | **통과** | 합성·렌더·캡처 3종 |
| **G7** | UV 3건 **재판정** | **통과** | **3/3**(편차 2.036e-8) · **`printUv.ts`·`garmentScene.ts` diff 0** ⟹ **§1 이 UV 를 안 건드렸다는 «값» 근거** |
| — | blob 무변조 | **통과** | 표시 전후 sha 동일 |

### §2-2 ★ 자기검사 — **기준값이 «현 정본»의 것이 아니다**

**실측**(계기 = `seamBridgeIndices` + `bridgeStripCount` · 0프레임):

| 원단(정본) | **실측 스트립** | §0-4 가 지목한 v3-45 등재값 | 일치 |
|---|---|---|---|
| gray **d9** | **258** | 258 | **예** |
| swim **d9 «신»** | **258** | 232 | **아니오** |
| sweat **d8 «신»** | **290** | 258 | **아니오** |

```
 **불일치의 정체 — 해상도 승격**(값으로 확인):
   v3-45 등재값 **258 / 232 / 258** 은 **구 정본**(gray d9 · swim **d10** · sweat **d9**)의 것이다
   (`metrics-log.md:22563`).
   **v3-49 가 정본을 승격**했고(swim d10→**d9** · sweat d9→**d8**) **시접 쌍이 함께 바뀌었다** —
   v3-47 §1 표: 시접 쌍 **242 → 268**(swim) · **268 → 300**(sweat).
   v3-48 §6-1 계기 출력도 sweat **구 d9 = 스트립 258** · **신 d8 = 스트립 290** 을 이미 찍었다.
 ⟹ **실측이 틀린 것이 아니라 «기준값이 다른 시점의 것»이다.**
 **기준값을 사후에 바꾸지 않았다**(함정 14) — **§0-4 문언 그대로 판정**하고 **불일치를 사실로 적는다**.
 **자기검사의 둘째 조항(프린트 모드 on/off 동일)은 «구조적으로» 성립한다** —
   스트립 배열은 **상태(`sc.seams`)에서 나오고** 표시 층은 그것을 **읽기만** 한다
   (`productView` 는 `bridgeIdx` 를 «받아» 그릴 뿐이다) ⟹ 프린트 모드와 **무관**.
```

---

## §3 캡처

```
 `docs/captures/v3-60-프린트합성/` — **합성 on 9장**(3종 × front/side/back-p)
 + **브리지 대조 2장**(gray side · A/B). **CC 자체 화면 판정 0.**
 **대조 2장에 대한 사실**: 두 장이 «육안으로 구분되지 않는다» — side 뷰에서 스트립은
   **시접선 위의 얇은 띠**라 이 배율에서 차이가 안 보인다. **토글이 걸렸는지 CC 는 판정하지 않는다**
   (자체 화면 판정 0) — **수치 채널(§2-2)이 그 자리를 대신한다.**
```

---

## §4 갈래 판정 — **E (판정 불가)** · 정지 · **사실만**

| 갈래 | 조건 | 실측 | 성립 |
|---|---|---|---|
| A | G 전량 **+ 자기검사 통과** · 캡처 산출 | G 전량 통과 · 캡처 11장 · **자기검사 «일치» 조항이 2/3 불일치** | **아니오** |
| B | 대표색 출처 특정 불가 | **특정됨**(`borderRepresentativeColor` 도출식) | 아니오 |
| **E** | 판정 불가 | **자기검사의 «기준값»이 현 정본의 것이 아니다** ⟹ 「일치」 판정이 성립하지 않는다 | **예** |

```
 **A 를 선언하지 않았다.** 게이트는 전량 통과했고 합성도 섰지만,
 **사전 등재된 자기검사 조항이 «그대로는» 통과하지 못했다.**
 **기준값을 고쳐 A 로 만들지 않았다**(함정 14) — 고쳐야 할 것은 **기준값의 «출처»**이고
 그 판단은 **전략 세션 몫**이다.
 **합성·브리지·게이트의 실측은 전부 유효하다** — 캡처도 산출됐다. **정지는 «판정»에 대한 것**이다.
```

### §4-1 이 판이 «닫은» 것 / «연» 것

```
 닫음  **합성 층이 섰다** — 대표색 **rgb(34,31,37)** · 프린트 **302×430px** · 앞·뒤·소매 **같은 색** ·
       **앞판 가슴에만** 프린트(v3-59 의 「원본이 전면에」 미완 해소)
       **브리지 복원** — 대표색 단색 별도 지오메트리(**텍스처 0 · UV 0** · v3-45 목적 보존)
       **대표색 출처가 «도출식»** — 갈래 B 불발화 · **색 임의 선정 0**
       **G1~G4 · G7 전량 통과** · **`printUv`·`garmentScene` diff 0**
       **자산 선택을 «값으로»** 했다(문턱 발동 ↔ 미발동 실측)
       **판독 정정 1건**(재스캔 G2′)을 **커밋 전에** 고치고 은폐 없이 등재
 엶    **자기검사 기준값의 출처** — v3-45(구 정본) ↔ 현 정본(v3-49 승격분)
       **브리지 토글의 «화면» 확인** — side 뷰에서 육안 구분 불가(**수치로 대신함**)
       **앞·뒤 색 «불일치»(§0-1 ②)** — 이제 **같은 대표색**을 쓰므로 해소로 «보이나»
         **CC 는 화면 판정을 하지 않는다** ⟹ **사용자 판정 대상**
       **어깨 집힘 색 대비 가시화**(#104 병기분) — 합성 색이 바뀌었으므로 **다시 볼 대상**
```

---

## §5 회귀

```
 **물리 0프레임** · **`solver`·`garmentScene`·`s4Gate`·`printUv` diff 0**(`65f3339` 대비 · 주석 포함)
 **핏 색 채널 0줄**(배타 토글만) · **v2 경로 0줄** · 문턱·정본 0 · `V2DIMS` 미사용
 신설 `src/v3/printComposite.ts` · 자산 `public/v3print/print-tee-white.webp`(복사)
 수정 `productView.ts`(표시 층 · `solid`·`bridgeIdx`) · `V3Panel.tsx`(합성 배선·범례)
 **npx tsc -b rc=0** · **npm run build 통과** · **G7 재판정 3/3**
```

---

## §6 자기 점검

| # | 항목 | 답 |
|---|---|---|
| 1 | §0 커밋 선행 | **예** — `42f677d`(§0) → 계약 특정 → 구현 |
| 2 | 대표색을 손으로 골랐는가 | **아니오** — `borderRepresentativeColor` **도출식**을 옮겼고 값은 **자산에서 나온다**(rgb 34,31,37) |
| 3 | 판독 정정을 은폐했는가 | **아니오** — §1-3 에 「1차 판독이 틀렸다 · 실행이 드러냈다 · **커밋 전에** 고쳤다」를 적었고 **코드 주석에도 남겼다** |
| 4 | 자산 선택이 임의인가 | **아니오** — **두 자산의 실측 표**(§1-4)로 골랐다. 전자는 **v2 가 이미 등재한 한계**에 걸린다 |
| 5 | 자기검사 실패를 A 로 읽었는가 | **아니오** — **E(판정 불가)로 정지**했고 **기준값을 고치지 않았다** |
| 6 | 이 판에서 호출한 ecc 에이전트·스킬 | **0 / 0** |
