# 50 — 전용 «베이크 마운트» 신설 · 백화 판별 · ㉯ 재집행 (v3-71 · **물리 0프레임**)

v3-71. 브랜치 `v3-71-bake-mount`(사전 등록 · **이 CC 세션 전용**). 호출한 ecc 에이전트/스킬: **0 / 0**

## git 스탬프

```
 브랜치  v3-71-bake-mount           워크트리  /Users/banshk/projects/fit-simulator
 기점    f686b6a (v3-70 HEAD)       직전 회차  f686b6a
 커밋    (§0 → §1 → §2 → §3 순서로 채운다)
```

---

## §0 판정 수령 — **집행 «전» 커밋**

### §0-1 ㉮′ 분기 유지 **승인** + **체제 경계 등재**

```
 **승인**: v3-70 §2-㉮′ 의 처분(BEXT 분기 유지)을 그대로 둔다.
 **★ 체제 경계 — 한 줄 등재**:
   **「정본 3종 = «캐시 BEXT» · 이후 «모든 새 칸» = «실측 BEXT»(도출 h · 도출 ③b)」**
 ⟹ 두 체제가 **공존**한다. 섞지 않는다. 새 칸의 h·③b 문턱은 **그 몸에서 도출**된다.
```

### §0-2 G1 «경계» 판정 수령

```
 **하네스 층**(`V3Panel` · 플래그 화면)의 **v2 참조 = «위반 아님»**.
 **제품 경로**(`src/v3/**` · `V3Product` · `FitReportTable`)의 **v2 임포트 0 유지**가 조건이다.
 ⟹ **G1 문언에 «경계»를 명시 등재**한다(v3-70 §2-㉱ 가 값으로 올린 것을 문언으로 승격).
```

### §0-3 백화 처분 수령 — **저우선 «해제»**

```
 **수복 대상 = «살아있는 몸»**이다. **«v2 화면»이 아니다.**
 **전용 베이크 마운트**가 (i) **굽기 파이프라인 앞단 «부품»**이자 (ii) **백화 «판별 실험»**이다.
 **v2 화면 자체는 이 판에서 수정 «0줄»**.
```

### §0-4 하드코딩 BEXT = **어림** 사실 수용

```
 **귀책 0** — v3-13 시점의 **예산 계산용 어림값**이다. 사실만 남는다.
```

### §0-5 §1 신설 (사전 등록)

```
 **`BakeMount` 신설**(하네스 층 · **제품 화면 노출 0**)
   · **명시적 «픽셀» 크기의 R3F 캔버스** — 근거는 v3-70 §2-㉯′ 관측
     「캔버스 CSS 크기 지정 없음 · **300×150 기본값 고착**」. **크기를 CSS 로 못박는다**
     (**값과 근거를 주석에 등재**).
   · `Mannequin` + **슬라이더 `bodySize` 연결**.
 **굽기 «시점» = P26 정착 성립 «후»** — 잔차 ≤ `POSE_SETTLE_EPS` ·
   **`Mannequin` 의 기존 정착 검출을 재사용**(조항 인용) · **정착 «전» 굽기 금지**
   (**조용한 조기 굽기 0**).
 **연결부는 v3-70 §1-1 그대로** — `parseGlb` 기준 위치 + `applyBoneTransform` · **규약 ①②③ 유지**.
```

### §0-6 §2 백화 «판별» (사전 등록 · **원인 단정 없이 «위치»만 좁힌다**)

```
 **㉠ 3값 등재** — `BakeMount` 에서 ⓐ **마네킹 ref 생존 여부** ⓑ **캔버스 «실측» 크기**
    ⓒ **`useFrame` 구동 여부**.
 **생존 ⟹** 백화 = **v2 씬/레이아웃 «국소»**로 **위치** 등재(**기전 귀속 0**) ·
    **v2 수복 «불요» 종결**(하네스 강등 유지 · **재개 조건 문언을 「베이크 마운트로 «대체됨»」으로 갱신**).
 **사망 ⟹** **`Mannequin` 층**으로 위치 등재 · **정지 · 관측 상신**(**갈래 B**).
```

### §0-7 §3 ㉯ 재집행 (사전 등록 · **물리 0프레임 · 조립까지**)

```
 **㉰ 기본 슬라이더 굽기** — 주입 배열 ↔ `parseGlb` 배열 **max|Δ| «실측» 등재**
    (**비트 동일이면 그리 적고**, 아니면 **값만** — **문턱 창작 0** · **판정은 전략 세션**).
 **㉱ 가슴둘레 100 → 110cm 굽기**
    ⓐ **가슴 대역**(등재 가슴 y 밴드) **실측 둘레 배율** — **기대 1.10 병기**
    ⓑ **BEXT · h · band · ③b 문턱 · Y_TOP · 목선 · AXIS_Z** 도출값 — **기본 몸 값과 병기**
    ⓒ **방향 판정만** — 둘레 증가 ∧ BEXT y·z 증가 ∧ 문턱이 그 h 를 따라감.
       **크기의 «허용 오차»는 정하지 않는다.**
```

### §0-8 §4 갈래 (사전 등록)

| 갈래 | 조건 | 귀결 |
|---|---|---|
| **A** | §2 **생존** · §3 ㉱ **방향 전량 성립** · 값 전량 등재 | **완료 상신**(민감도 판 준비 완료) |
| **B** | §2 **사망**(`BakeMount` 에서도 ref null) | 정지 · 관측 상신 · **수정 0줄** |
| **E** | 판정 불가 | 정지 · 사실만 |

### 이 판에서 바꾸지 않는 것

**물리 0프레임** · **`solver` / `garmentScene` / `s4Gate` diff 0** · **정본 0** ·
**v2 화면 수정 0줄** · **문턱 0** · **색·표시·표 채널 0줄**.

### 순서

```
 §0 커밋 → §1 → §2 판별 → §3 → §4 판정 · 상신
```

---

## §1 베이크 마운트 신설 — 변경 파일 **3개**

```
 **신설** `src/components/BakeMount.tsx` — 전용 R3F 캔버스 + `Mannequin`.
   **캔버스 크기를 «픽셀»로 못박았다**: `style={{ width: 320px, height: 440px }}`.
   근거 주석에 v3-70 §2-㉯′ 관측(「크기 지정 없음 · 300×150 고착」)을 그대로 실었다.
   **표시 층 값**이고 물리·문턱 채널이 아니다(캡처 프리셋 300×420 과 같은 계열).
 **수정** `src/components/V3Panel.tsx` — 마운트 + 버튼 3개(㉠ 판별 · ㉰ 기본 굽기 · ㉱ 가슴 110 굽기).
   굽기 시점은 **`awaitMannequinSettled`(P23 §1 조항)를 그대로 재사용** — **새 문턱 0**.
 **수정** `src/components/bodyInjectBake.ts` — §3 실측 결손을 **주석으로 등재**(코드 로직 변경 0).
```

**기점 `f686b6a` 대비 diff 0 확인**: `solver` **0** · `garmentScene` **0** · `s4Gate` **0** ·
**`dressRun` 0** · `V3Product` **0** · `FitReportTable` **0** · **`Mannequin` 0** ·
**`FitCanvas` 0** · **`Controls` 0** · `App` **0** ⟹ **v2 화면 수정 0줄**(§0-3 준수).
`tsc -b` **rc=0** · `build` **rc=0**.

---

## §2 백화 «판별» — ★ 위치가 **v2 도 Mannequin 도 아니다**

### §2-㉠ 3값 (사전 등록 채널)

| 시점 | 마네킹 ref | 캔버스 실측 | `useFrame` | 스케일 잔차 |
|---|---|---|---|---|
| 마운트 직후(비가시 탭) | **사망(null)** | **300×150**(지정 320×440) | **0** | Infinity |
| **스크린샷 1장 «후»** | **생존** | **640×880**(css 320×440) | 1 | **0.00e+0** |

⟹ **§0-6 의 「생존」 갈래** — **갈래 B 불발화**.

### §2-㉡ ★★ 위치 특정 — **비가시 탭 ⟹ R3F 가 «초기화조차» 안 된다**

실측 사슬(전부 이 판에서 잰 값 · **기전 «단정»은 하지 않는다**):

```
 ① `document.visibilityState = **"hidden"**` — Chrome 확장 구동이라 탭이 «앞»에 없다(사용자 조작 아님)
 ② 같은 1초 창에서  **`requestAnimationFrame` = 0회**  ↔  **`MessageChannel` = 949회**
 ③ **`ResizeObserver` 발화 0건**(2초 관찰 · 대상 = 베이크 마운트 상자)
 ④ R3F `<Canvas>` 는 **크기가 측정돼야 루트를 만든다** ⟹ **`_roots` 가 비어 있다**
 ⑤ 그래서 `frameloop="never"` + `advance()` **수동 펌프**는
    **`advance` 1,620,000회에 `useFrame` 0회**였다(**예외 0건** — 조용히 아무 것도 안 한다)
 ⑥ **스크린샷 1장**을 찍으면 페인트가 유발돼 **RO 가 발화하고 루트가 생긴다** ⟹ 위 표의 2행
```

```
 ⟹ **백화는 「v2 씬/레이아웃 국소」가 «아니다»** — §0-6 이 미리 적어 둔 두 선택지 중
    **어느 쪽도 아니고**, **「환경: 비가시 탭」**이라는 **제3의 위치**다.
    같은 원인이 **v2 캔버스(972×853 상자에 300×150)** 와 **베이크 마운트**에 **동형으로** 나타난다.
 **처분 제안 0** — 위치만 좁혔다. **원인 귀속·수정은 하지 않았다**(v2 화면 0줄).
 **§0-6 의 「재개 조건 문언 갱신」**: 「베이크 마운트로 «대체됨»」이라 적을 수 없다 —
    **베이크 마운트도 같은 조건에 걸린다**. 대신 **「비가시 탭에서는 R3F 경로 전체가 서지 않는다」**를
    등재한다(v2·베이크 마운트 **공통**).
```

### §2-㉢ 펌프 시도 — **기각**(구현 되돌림 · 사실만 남김)

```
 `MessageChannel` 은 비가시 탭에서도 **949회/초** 돈다 ⟹ 구동원으로는 «쓸 수 있다».
 그러나 **루트가 없으면 `advance` 는 빈 집합을 돈다**(⑤) ⟹ **펌프는 원인보다 «뒤»에 있다**.
 ⟹ `frameloop="never"` 와 펌프를 **되돌리고** 기본 루프를 쓴다. **남는 것은 위 값들이다.**
 (부수: 이 값들은 **v3-69 결손 4 「무인 N칸 굽기」**의 전제도 흔든다 —
  **무인 굽기는 «가시 탭»을 요구하거나 R3F 를 벗어나야 한다**. 판정은 전략 세션 몫.)
```

---

## §3 ㉯ 재집행 — ★★ **㉰ 에서 «좌표계 결손» 발견 · ㉱ 미집행**

### §3-㉰ 기본 슬라이더 굽기 — **정착은 성립 · 배열은 «붕괴»**

```
 [v3-71 굽기] 가슴 **100cm** · 정착 **성립** · 프레임 **5** · 잔차 **0.000e+0**
   ⟹ **조기 굽기 0**(§0-5 조항 충족 · `awaitMannequinSettled` 그대로)
 [v3-71 굽기] 스킨드 메시 **찾음** · 정점 **15882** · `parseGlb` 배열과 **비트 «상이»**
   · **max|Δ| = 1.7693e+5 mm**
 [v3-71 굽기] 오류 — **「목 단면 국소 최소 둘레를 못 찾는다 — 갈래 D」**(조립이 던졌다)
```

**★★ 값의 의미 — 「모양은 맞고 «프레임»이 틀렸다」**

```
 **1.7693e+5 mm = 176.93 m = 1.7693 m × «정확히 100»**.
 1.7693 m 는 이 몸의 y 범위와 **같은 자릿수**다(BEXT y 1.765) ⟹ **배율 100 의 좌표계 어긋남**.
 ⟹ **v3-70 §1-1 연결부 설계에 «프레임 복원» 결손이 있다.**
    `applyBoneTransform` 의 출력은 **GLB 프레임이 아니다** — v3-70 의 「좌표계는 GLB 프레임 그대로다」
    라는 주석이 **틀렸다**(그 회차에는 검증할 몸이 없었다).
 ★ **v3-70 에서 «안 걸린» 이유**: 마네킹이 **부재**해 이 경로가 **항등 통과**(`skinned:false`)였다
    ⟹ **v3-70 §2-㉮ 의 «비트 동일»은 «진짜 등가»가 아니었다** — 커넥터를 **한 번도 통과하지 않았다**.
    **이 판이 그것을 값으로 뒤집는다.** (v3-70 원문 무삭제 · 여기가 정정의 자리 · **귀책 = CC**)
 **기전 가설은 적지 않는다**(단정 0). **다음 판의 첫 과제 = «프레임 복원식» 도출.**
```

### §3-㉱ 가슴 100 → 110cm — **미집행**

```
 ㉰ 가 **조립 단계에서 던지므로** 같은 경로로는 ⓐ 둘레 배율 · ⓑ 도출값 · ⓒ 방향 판정을
 **낼 수 없다**. **추정으로 채우지 않는다**(계기 규약). **미집행으로 적는다.**
```

---

## §4 판정 — **갈래 E**(판정 불가 · 정지 · 사실만)

| 사전 등록 조건 | 실측 | 충족 |
|---|---|---|
| **§2 생존** | 마네킹 ref **생존**(스크린샷 1장 후) | ✅ |
| **§3 ㉱ 방향 전량 성립** | **미집행** — ㉰ 에서 좌표계 붕괴 · 조립 throw | ❌ |
| 값 전량 등재 | §2 6항 · §3 ㉰ 3값 등재 | ✅ |

```
 **갈래 A 불성립**(㉱) · **갈래 B 불성립**(§2 는 «생존») ⟹ **어느 갈래에도 해당 없음** ⟹ **E · 정지**.
```

**이 판이 «세운» 것**
① `BakeMount` — **크기를 픽셀로 못박은** 전용 마운트(v2 화면 0줄) ·
② **정착 «후»에만 굽는 경로**(기존 조항 재사용 · 새 문턱 0 · 실측 「정착 성립 · 프레임 5 · 잔차 0」) ·
③ **백화 위치 특정** — v2 도 Mannequin 도 아닌 **「비가시 탭 ⟹ RO 미발화 ⟹ R3F 루트 미생성」**.

**★ 상신하는 «사실» 3건**
① **백화 = 환경 조건**(제3의 위치). **v2 와 베이크 마운트에 «동형»으로 나타난다** ⟹
   §0-6 의 「베이크 마운트로 대체됨」 문언은 **쓸 수 없다**. 무인 굽기(결손 4)의 전제도 흔든다.
② **연결부 «프레임 복원» 결손** — max|Δ| **1.7693e+5 mm**(정확히 100배) · 조립 **갈래 D throw**.
③ **v3-70 §2-㉮ 의 「비트 동일」은 커넥터를 통과하지 않은 값이었다**(마네킹 부재 ⟹ 항등 통과).
   **v3-70 판정 자체는 「배관 등가」로는 유효**하나 **「연결부 등가」로는 «무효»**다. **귀책 = CC.**

**다음 판의 첫 과제**: **프레임 복원식 도출**(②). 그것 없이는 ㉱ 도 민감도 판도 서지 않는다.
**CC 화면 판정 0.**
