# 50 — 전용 «베이크 마운트» 신설 · 백화 판별 · ㉯ 재집행 (v3-71 · **물리 0프레임**)

v3-71. 브랜치 `v3-71-bake-mount`(사전 등록 · **이 CC 세션 전용**). 호출한 ecc 에이전트/스킬: **0 / 0**

## git 스탬프

```
 브랜치  v3-71-bake-mount           워크트리  /Users/banshk/projects/fit-simulator
 기점    f686b6a (v3-70 HEAD)       직전 회차  f686b6a
 커밋    (§0 → §1 → §2 → §3 순서로 채운다)
```

---

## §0 판정 수령 — **집행 «전» 커밋**

### §0-1 ㉮′ 분기 유지 **승인** + **체제 경계 등재**

```
 **승인**: v3-70 §2-㉮′ 의 처분(BEXT 분기 유지)을 그대로 둔다.
 **★ 체제 경계 — 한 줄 등재**:
   **「정본 3종 = «캐시 BEXT» · 이후 «모든 새 칸» = «실측 BEXT»(도출 h · 도출 ③b)」**
 ⟹ 두 체제가 **공존**한다. 섞지 않는다. 새 칸의 h·③b 문턱은 **그 몸에서 도출**된다.
```

### §0-2 G1 «경계» 판정 수령

```
 **하네스 층**(`V3Panel` · 플래그 화면)의 **v2 참조 = «위반 아님»**.
 **제품 경로**(`src/v3/**` · `V3Product` · `FitReportTable`)의 **v2 임포트 0 유지**가 조건이다.
 ⟹ **G1 문언에 «경계»를 명시 등재**한다(v3-70 §2-㉱ 가 값으로 올린 것을 문언으로 승격).
```

### §0-3 백화 처분 수령 — **저우선 «해제»**

```
 **수복 대상 = «살아있는 몸»**이다. **«v2 화면»이 아니다.**
 **전용 베이크 마운트**가 (i) **굽기 파이프라인 앞단 «부품»**이자 (ii) **백화 «판별 실험»**이다.
 **v2 화면 자체는 이 판에서 수정 «0줄»**.
```

### §0-4 하드코딩 BEXT = **어림** 사실 수용

```
 **귀책 0** — v3-13 시점의 **예산 계산용 어림값**이다. 사실만 남는다.
```

### §0-5 §1 신설 (사전 등록)

```
 **`BakeMount` 신설**(하네스 층 · **제품 화면 노출 0**)
   · **명시적 «픽셀» 크기의 R3F 캔버스** — 근거는 v3-70 §2-㉯′ 관측
     「캔버스 CSS 크기 지정 없음 · **300×150 기본값 고착**」. **크기를 CSS 로 못박는다**
     (**값과 근거를 주석에 등재**).
   · `Mannequin` + **슬라이더 `bodySize` 연결**.
 **굽기 «시점» = P26 정착 성립 «후»** — 잔차 ≤ `POSE_SETTLE_EPS` ·
   **`Mannequin` 의 기존 정착 검출을 재사용**(조항 인용) · **정착 «전» 굽기 금지**
   (**조용한 조기 굽기 0**).
 **연결부는 v3-70 §1-1 그대로** — `parseGlb` 기준 위치 + `applyBoneTransform` · **규약 ①②③ 유지**.
```

### §0-6 §2 백화 «판별» (사전 등록 · **원인 단정 없이 «위치»만 좁힌다**)

```
 **㉠ 3값 등재** — `BakeMount` 에서 ⓐ **마네킹 ref 생존 여부** ⓑ **캔버스 «실측» 크기**
    ⓒ **`useFrame` 구동 여부**.
 **생존 ⟹** 백화 = **v2 씬/레이아웃 «국소»**로 **위치** 등재(**기전 귀속 0**) ·
    **v2 수복 «불요» 종결**(하네스 강등 유지 · **재개 조건 문언을 「베이크 마운트로 «대체됨»」으로 갱신**).
 **사망 ⟹** **`Mannequin` 층**으로 위치 등재 · **정지 · 관측 상신**(**갈래 B**).
```

### §0-7 §3 ㉯ 재집행 (사전 등록 · **물리 0프레임 · 조립까지**)

```
 **㉰ 기본 슬라이더 굽기** — 주입 배열 ↔ `parseGlb` 배열 **max|Δ| «실측» 등재**
    (**비트 동일이면 그리 적고**, 아니면 **값만** — **문턱 창작 0** · **판정은 전략 세션**).
 **㉱ 가슴둘레 100 → 110cm 굽기**
    ⓐ **가슴 대역**(등재 가슴 y 밴드) **실측 둘레 배율** — **기대 1.10 병기**
    ⓑ **BEXT · h · band · ③b 문턱 · Y_TOP · 목선 · AXIS_Z** 도출값 — **기본 몸 값과 병기**
    ⓒ **방향 판정만** — 둘레 증가 ∧ BEXT y·z 증가 ∧ 문턱이 그 h 를 따라감.
       **크기의 «허용 오차»는 정하지 않는다.**
```

### §0-8 §4 갈래 (사전 등록)

| 갈래 | 조건 | 귀결 |
|---|---|---|
| **A** | §2 **생존** · §3 ㉱ **방향 전량 성립** · 값 전량 등재 | **완료 상신**(민감도 판 준비 완료) |
| **B** | §2 **사망**(`BakeMount` 에서도 ref null) | 정지 · 관측 상신 · **수정 0줄** |
| **E** | 판정 불가 | 정지 · 사실만 |

### 이 판에서 바꾸지 않는 것

**물리 0프레임** · **`solver` / `garmentScene` / `s4Gate` diff 0** · **정본 0** ·
**v2 화면 수정 0줄** · **문턱 0** · **색·표시·표 채널 0줄**.

### 순서

```
 §0 커밋 → §1 → §2 판별 → §3 → §4 판정 · 상신
```
