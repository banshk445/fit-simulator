# 44 — 5/5-α 자산 «재수령» · 합성 완주 (v3-64 · **물리 0프레임 · 합성 로직 diff 0 «기대»**)

v3-64. 브랜치 `v3-64-print-final2`(사전 등록 · **이 CC 세션 전용**). 호출한 ecc 에이전트/스킬: **0 / 0**

## git 스탬프

```
 브랜치  v3-64-print-final2         워크트리  /Users/banshk/projects/fit-simulator
 기점    22a6e2c (v3-63 HEAD)       직전 회차  22a6e2c
 커밋    (§0 → §1 → §2 → §3 순서로 채운다)
```

---

## §0 수령 확인 — **집행 «전» 커밋**

### §0-1 자산 존재 — **확인**

```
 `docs/test-assets/print-graphic.png`  **존재**
   크기 **20,606 B** · **1200 × 1200** · **colorType 6(RGBA) · depth 8** · mtime **2026-08-24 23:49**
   **sha256 `47bf97da178773fab83434a57565770d24432041a4c4af3f69815a567e0921f6`**
 ⟹ §0 의 「없으면 즉시 갈래 B」 조건은 **불발화**한다.
```

### §0-2 ★ 사실 1건 — **git 커밋은 «없다»**

```
 §0 은 「**`git log` 로 «사용자 커밋» 확인**」을 요구했다. **실측**:
   `git log --all --oneline -- docs/test-assets/print-graphic.png` ⟹ **0건**
   `git status --porcelain docs/test-assets/` ⟹ **`?? docs/test-assets/print-graphic.png`**(**untracked**)
 ⟹ **파일은 «워킹트리»에만 있고 저장소에 «커밋된 적이 없다»**.
 **처분**: **존재 조건은 충족**했으므로 진행하고, **이 판이 자산을 커밋한다**
   (**저장소에 «넣는» 것은 «편집»이 아니다** — 바이트를 바꾸지 않는다 · sha256 을 위에 등재했고
    커밋 뒤 **동일함을 재확인**한다). **«사용자 커밋»이라 적지 않는다** — **CC 커밋**이다(귀속 정확).
```

### §0-3 전략 세션 «사전 검증값» 수령 — **대조 채널**(판정 아님)

```
 ㉠ **알파 유** · ㉡ **61.8% / 73.6%** · ㉢ **테두리 투명**
 **판정은 §1 의 «CC 재검사»가 정본이다** — 전략 세션 값은 **대조 채널**로 병기한다.
```

### §0-4 §1 자산 검사 — **v3-62 §2 규칙 문언 그대로 «3회째»**(변경 0)

```
 **㉠ 알파 보유** · **㉡ 불투명 bbox 가로·세로 < 90%** · **㉢ 테두리 1px 전부 투명**
 실측 → **전략 세션 사전 검증값과 대조 병기**
 **통과 ⟹ 채택** / **미달 ⟹ 갈래 B**(값 상신 · **편집 0**)
```

### §0-5 §2 합성 완주 · §3 캡처

```
 **§2** `printComposite` 에 **채택 자산 적용** — **합성 로직 diff 0 «기대»**(**자산 경로 참조만** ·
   **어긋나면 diff 내용 «사실 등재»**).
   게이트 **G1~G4 · G7 · blob 무변조 · 스트립 자기검사**
   (**v3-63 §0-② 재등재 기준 — gray 258 · swim 258 · sweat 290**).
 **§3** **3종 × front/side/back-p 9장** + **back 반점**(v3-60 관찰: **gray 목선 아래 · swim 어깨·등**)
   **재현 여부를 «캡처 픽셀»로 확인 · 사실만 등재**(**재현 시 좌표 병기 · 귀속 0 · 처방 0**).
```

### §0-6 §4 갈래 (사전 등록)

| 갈래 | 조건 | 귀결 |
|---|---|---|
| **A** | 자산 통과 · 합성 성립 · 게이트 전량 | **캡처 상신**(α 완결 «후보») |
| **B** | **자산 부재 또는 미달** | 정지 · 값 상신 |
| **E** | 판정 불가 | 정지 · 사실만 |

### 이 판에서 바꾸지 않는 것

**물리 0프레임** · **`solver` / `garmentScene` / `s4Gate` / `printUv` diff 0** · **핏 색 채널 0줄** ·
문턱·정본 **0** · **v2 경로 0줄** · **`V2DIMS` off**.

### 순서

```
 §0 확인·커밋 → §1 → §2 → §3 → §4 판정 · 상신
```

---

## §1 자산 검사 — **v3-62 §2 규칙 문언 그대로 «3회째»**(변경 0) ⟹ **적합 1건 · 채택**

`scripts/v3AssetScan.ts` **diff 0**(v3-62 신설 그대로 · 규칙 문언 변경 **0**).

| 자산 | α<250 | 불투명 bbox | 테두리 불투명 | ㉠ | ㉡ | ㉢ | 판정 |
|---|---|---|---|---|---|---|---|
| **`print-graphic.png`** | **74.1%** | **61.8% × 68.0%** | **0.0%** | ✓ | ✓ | ✓ | **적합 · 채택** |
| `print-tee.png` | (v3-62 등재) | 100% × 100% | — | ✓ | **✗** | — | 미달 |
| `print-tee-white.webp` | (v3-62 등재) | 99.x% × 9x% | — | ✓ | **✗** | — | 미달 |

**면적비 41.99%** — 적합 후보 중 **최소**(규칙의 tie-break 조항 그대로).

### §1-① ★ 대조 병기 — 전략 세션 «사전 검증값»과 **세로가 어긋난다**

```
 ㉡  전략 세션 **61.8% / 73.6%**   vs   **CC 실측 61.8% × 68.0%**
     가로 **일치**(61.8%) · 세로 **5.6%p 차이**(73.6 → 68.0)
 ⟹ **판정은 불변**(양쪽 다 90% 미만 ⟹ ㉡ 통과). **어긋남은 사실로 남긴다 · 귀책 0.**
     둘 중 어느 쪽이 옳은지는 **이 판의 정의역 밖**이다 — §0-3 이 「판정은 CC 재검사가 정본」이라
     미리 고정했으므로 **CC 값으로 진행**한다.
```

---

## §2 합성 완주 — **합성 로직 diff 0 «기대»가 실제로 성립**

### §2-1 diff 실측 (기점 `22a6e2c` 대비)

```
 변경 파일 **1개뿐** — `src/components/V3Panel.tsx` (**+7 / −3** · 자산 경로 + 선택 이력 주석 무삭제)
 **diff 0 확인**: `printComposite.ts` · `printUv.ts` · `garmentScene.ts` · `solver.ts` · `s4Gate.ts`
 ⟹ §0-5 의 **「기대」가 «실측»으로 확정**됐다. 합성 층은 **자산 경로 참조만** 바뀐다는 v3-60 설계가 유지된다.
```

`npx tsc -b` **rc=0** · `npm run build` **rc=0**.

### §2-2 합성 실측 (콘솔 정본)

```
 [v3-60 합성] 대표색 **rgb(128, 128, 128)** · 프린트 bbox **750×891@(225,206)**
              **프레임 문턱 미발동** · **재스캔 미실행(1패스 통과)** · **하단 축소 없음**
 패널 범례    UV 축척 **70.00cm = uv 1.0** · 앞판 uMax **0.7857** · 프린트 **750×891px**
```

### §2-3 ★★ 사실 1건 — **대표색 rgb(128,128,128) 은 «폴백»이다**

```
 `borderRepresentativeColor`(v2 `garmentTextureComposite.ts:43-100`)는 **테두리 띠 0.08** 안의
 화소를 **BUCKET 24** 로 모아 최빈 버킷을 쓴다. 제외 규칙: **α<10 제외** · **rgb>235 제외**.
 이 자산은 **테두리 불투명 0.0%**(§1) ⟹ 테두리 띠가 **전부 α=0** ⟹ **버킷이 하나도 안 생긴다**
 ⟹ **폴백 `rgb(128,128,128)` 이 나온다**(v2 계약이 「테두리도 전부 배경/흰색뿐 — 드문 경우」로
    미리 적어 둔 바로 그 경로다).
 ⟹ **그래픽 전용 자산에서는 이것이 «구조적으로 불가피»하다.** 규칙 ㉢(테두리 투명)을 통과하는
    자산은 **정의상** 대표색 버킷을 만들 수 없다 — ㉢ 과 「대표색이 옷 색을 준다」는 **양립하지 않는다**.
 **결과**: 옷 몸판이 **중간 회색**으로 나온다. **처방 0 · 귀책 0** — 사실만 등재한다.
    (「옷 색을 어디서 받을 것인가」는 **이 판의 정의역 밖**이다.)
```

### §2-4 게이트 — **전량 통과**

| 게이트 | 채널 | 값 | 판정 |
|---|---|---|---|
| **G1** | v3 → `lib/` 임포트 | **0건**(grep) | ✅ |
| **G2** | tsc `-b` | **rc=0** | ✅ |
| **G3** | build | **rc=0** | ✅ |
| **G4** | 물리 프레임 | **0** | ✅ |
| **G7** | UV ①②③ | v3-59 판정 유지(`printUv` **diff 0**) | ✅ |
| **blob 무변조** | `settled-gray-d9` / `settled-sweat-d8-new` sha256 | **`8dec6eca…` / `dcaceda3…`** | ✅ |
| **스트립 자기검사** | gray / swim / sweat | **258 / 258 / 290** (idx 1548 / 1548 / 1740) | ✅ **기준 일치** |

---

## §3 캡처 9장 + **back 반점 재현 확인**

### §3-1 캡처

`docs/captures/v3-64-합성완주/` — **9장**(3 정본 × `front` / `side` / `back-p`).
파일명에 **정본 sha 셀 병기**(#82 확장 규약): `gray-d9-f220-faf4873f` · `swim-d9-f260-557bc446` ·
`sweat-d8-f330-51d74c66`.

### §3-2 back 반점 — **재현 여부 «판정 불가»**(사실 등재 · 귀속 0 · 처방 0)

**계기**: back-p 캡처 3장의 화소를 직접 읽어 **회색 옷 대역**(|r−g|<12 ∧ |g−b|<12 ∧ 70≤r≤175)
안에서 **밝은 화소(r≥160)** 를 센다.

| 캡처 | 옷 대역 | 밝은 화소 | 비율 | bbox |
|---|---|---|---|---|
| gray d9 `faf4873f` | 58,019 px | **916 px** | 1.58% | **(92,261)~(822,1004)** |
| swim d9 `557bc446` | 58,988 px | **903 px** | 1.53% | **(92,261)~(822,1004)** |
| sweat d8 `51d74c66` | 56,499 px | **874 px** | 1.55% | **(92,261)~(822,1004)** |

**★ 판정 불가 · 근거 2건**

```
 ⓐ **계기가 «반점»과 «조명 하이라이트»를 못 가른다.** 세 캡처의 밝은 화소 bbox 가
    **소수점까지 동일**(92,261)~(822,1004) 하고 비율도 1.53~1.58% 로 붙어 있다 —
    이는 **옷 실루엣 전역에 퍼진 조명 하이라이트**의 시그니처이지 국소 반점이 아니다.
    v3-60 이 말한 「gray **목선 아래**」·「swim **어깨·등**」 같은 **국소성**이 이 채널에는 없다.
 ⓑ **애초에 «같은 조건»이 아니다.** v3-60 관찰은 대표색 **rgb(34,31,37)**(검정 옷)에서 났고,
    이 판은 §2-3 대로 대표색 **rgb(128,128,128)**(중간 회색)이다. 반점의 정의 자체가
    **대표색 대비**에 의존하므로 **대비가 4배 가까이 달라진 화면에서 재현 여부를 물을 수 없다.**
 ⟹ **「재현됐다」고도 「사라졌다」고도 적지 않는다.** 위 표는 **관측값**으로만 남는다.
    (**CC 화면 판정 0** — 보기 판단은 캡처를 받은 쪽이 한다.)
```

---

## §4 판정 — **갈래 A**

| 조건 | 실측 | 충족 |
|---|---|---|
| 자산 통과 | ㉠✓ ㉡✓ ㉢✓ · 적합 1건 채택 | ✅ |
| 합성 성립 | 대표색·bbox 산출 · 문턱 미발동 · 재스캔 미실행 | ✅ |
| 게이트 전량 | G1~G4 · G7 · blob · 스트립 258/258/290 | ✅ |

⟹ **갈래 A** — **캡처 9장 상신**(α 완결 «후보»).

**갈래 A 안에서 함께 상신하는 «사실» 3건** (판정을 바꾸지 않는다):
① §1-① 전략 세션 ㉡ 세로 값과 **5.6%p 어긋남** ② §2-3 대표색이 **폴백**이라 옷이 중간 회색 ·
㉢ 과 「대표색=옷 색」이 **양립 불가** ③ §3-2 back 반점 **판정 불가**(조건 상이 + 계기 미분해).

**α 완결 선언 여부는 전략 세션이 한다** — 이 판은 선언하지 않는다.
