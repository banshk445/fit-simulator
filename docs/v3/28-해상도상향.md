# 28 — 해상도 한 단계 (d 사다리 상향 · 코드 0줄 · 새 벽 120분)

v3-47. 브랜치 `v3-47-resolution-up`(사전 등록). 호출한 ecc 에이전트/스킬: **0 / 0**

## git 스탬프

```
 브랜치  v3-47-resolution-up          워크트리  /Users/banshk/projects/fit-simulator
 기점    9e13443 (v3-46 HEAD)         직전 회차  9e13443
 커밋    509404b  §0 판정 기준 등록 (실행 «전»)
         a130570  ㉥ 신 해상도 후보 상태 + 패널 선택 항목 1줄
 검사    npx tsc -b rc=0 · npm run build 통과 · `src/v3/` diff 0줄
```

---

## §0 판정 기준 — 실행 «전»에 등록

> 이 절을 커밋한 «뒤»에만 실행으로 간다. 이후 결과를 보고 어떤 수도 고치지 않는다.

### §0-1 배경 (전략 세션 판정 수령분)

```
 사용자 결정: **㉰ 해상도 판** · 「시간을 많이 늘려도 돼」 ⟹ **벽 재가격**.
   **새 벽 = 120분** (**사용자 승인 · 소유자 재가격** — **도출 상수가 아니라 UX 예산**이며,
   **귀속을 이렇게 등재한다**. **구 60분 등재 이력은 무삭제**)
 대상: 제품 3종을 각자 한 단계 곱게 — **gray d9→d8 · swim d10→d9 · sweat d9→d8**.
   **denim 제외 확정 유지** · **Node B-v3-d11 검증 경로 무영향** · **파생 치수 무영향**.
 성격: **물리·조립 코드 0줄.** `d` 는 **등재 사다리 입력값** ⟹ **원복 구조가 아니라
   «구 정본 유지 · 신 상태 후보» 구조**다. 채택은 **사용자 화면 합격 + 전략 선언 뒤**.
 어깨 집힘 서류(v3-44~46)는 **이 판의 대조 입력**이다 — **같은 채널로 전후를 잰다**.
```

### §0-2 ㉠ 조립 — 신 해상도 3종

```
 **조립 2회**(리로드 사이) — n · 삼각형 · sub · 램프 **동일** 확인. **시접 쌍 수 등재**.
 sub · n 을 **v3-38 표 형식으로 등재**(비용 모형 검증용 대조 — **판정 아님**).
```

### §0-3 ㉡ 프로브 — 벽 판정 (§1′ 조항 · **새 벽**)

```
 3종 «각각» **단독 20프레임** → **s/f 실측** → 예상 = s/f × **정착 프레임 상한**(등재 도출식).
 **어느 원단이든 예상 > 120분 ⟹ 그 원단 전량 착수 0 · 그 사실 등재 · 나머지는 진행**
 (**전 원단 초과 시 갈래 B 정지**).
```

### §0-4 ㉢ 전량 + 하드 게이트

```
 **단독** · 정착 판정은 **등재 조항**(`settleNetM` · 창 순변위) 그대로 — **프레임 수는 실행이 정한다**.
 **하드 게이트**(원단별 · 하나라도 실패한 원단은 «**후보 탈락**» — **그 원단만 구 해상도 유지**):
   자기교차 **0** · 시접 교차 **0** · 발산 **0** · ③a **≤ 0.5mm** · `ringExcess` **≤ 1** ·
   실측 **≤ 120분**
 **코드 0줄이므로 원복은 없다** — 탈락 원단은 **사실만 등재**.
```

### §0-5 ㉣ S5 — 3종 인접 차 (신 해상도 통과분으로)

```
 O4 인접 차 **≥ 0.25pp** · **순서 유지**. 통과 원단이 3종 미만이면 **혼합 구성(신+구)**으로
 재판정하고 **그 사실을 명기**. **붕괴 시 신 해상도 전체 «후보 탈락» · 상신**.
```

### §0-6 ㉤ 대조 채널 — 어깨 집힘 서류 (**기록 · 판정 아님**)

```
 v3-44/45/46 의 4채널을 신 상태에서 재측정해 «**구 해상도 값과 나란히**» 표로:
   ① 시접 ±2열 **농축비**  ② 상위 5% **소매 점유**
   ③ **sideXplus 잔여 끊김**(브리지 on · 팔 숨김)  ④ **코너 확폭 최대**
 **추가 기록**: ③a **여유율** · **시간 여유율**(새 벽 대비) · **능선 위 법선 이격 중앙**.
```

### §0-7 ㉥ 캡처 + 상신

```
 **통과 구성**으로 제품 **9장** + 대조 **3장**(v3-45 규약 · **브리지 on** · **신 해상도 sha 파일명**).
 **CC 자체 화면 판정 0.** 사용자 재판정 대상 = **(가) 어깨 집힘 인상 변화**.
```

### §0-8 ㉦ 갈래 (사전 등록)

| 갈래 | 조건 | 귀결 |
|---|---|---|
| **A** | 3/3 게이트 통과 · S5 유지 | 캡처 상신 · 신 해상도 «**후보**»(정본 승격은 합격 후) |
| **B** | 프로브 **전 원단** 벽 초과 | 착수 0 · 상신 |
| **C** | **일부 원단 탈락** | 통과분만 후보 · **혼합 구성 명기** · 캡처는 **통과 구성**으로 · 상신 |
| **D** | S5 붕괴 | 후보 **전체 탈락** · 상신 |
| **E** | 판정 불가 | 정지 · 사실만 |

### §0-9 부채 (유지분 재등재)

```
 · **denim 검증 경로 재측정**
 · **swim / sweat 시접 굽힘 ③a 미측정**(v3-46 신규분 — **이 판 무관 · 유지**)
```

### 이 판에서 바꾸지 않는 것

물리·조립·게이트·표시층·브리지 코드 **0줄** · 문턱 전 종목 **0** · `SEP` **0** ·
원단 물성 **0** · **구 정본 상태 무삭제** · v2 경로 · **CC 자체 화면 판정 0** · **denim 처분**.

### 순서

```
 §0 커밋 → ㉠ 조립 → ㉡ 프로브 → ㉢ 전량+게이트 → ㉣ S5 → ㉤ 대조 → ㉥ 캡처 → 등재
```

---

## §1 ㉠ 조립 — 신 해상도 3종 (**조립 2회 동일 3/3** · 스텝 0회)

| 채널 | gray d9→**d8** | swim d10→**d9** | sweat d9→**d8** |
|---|---|---|---|
| 조립 2회 동일 | **예** | **예** | **예** |
| 정점 | 10402 → **13248** (×1.273) | 8676 → **10402** (×1.199) | 10402 → **13248** (×1.273) |
| 삼각형 | 20012 → **25604** | 16636 → **20012** | 20012 → **25604** |
| **sub**(멤/굽) | 226 → **258** (258/142) | 345 → **377** (377/169) | 160 → **207** (142/207) |
| 램프 | 82 → **77** | 81 → **82** | 82 → **77** |
| 시접 쌍 | 268 → **300** | 242 → **268** | 268 → **300** |
| 제약 총 | **63868** | **49906** | **63868** |
| 파생 치수[cm] | **8.928 / 49.315 / 12.918 / 21.495 — 3종 전부 동일 · 구 해상도와도 동일** | | |

```
 **파생 치수 무영향 확인** — 목선반폭·목선둘레·소매산·암홀깊이가 d 와 무관하게 같다.
 sub 는 정점 수보다 «덜» 는다(×1.14 / ×1.09 / ×1.29 vs 정점 ×1.27 / ×1.20 / ×1.27) —
 비용 모형 대조용 기록이고 **판정이 아니다**.
```

---

## §2 ㉡ 프로브 — **3/3 벽 통과** (새 벽 **120분**)

단독 20프레임 · `scripts/v3Run.ts`. 구 해상도 s/f 는 **v3-46 이 같은 하네스에서 실측한 값**이다(#103).

| 원단 | 정착 프레임 | 구 s/f | **신 s/f** | 비 | **예상 = s/f × 정착** | 벽 120분 |
|---|---|---|---|---|---|---|
| gray d8 | 220 | 10.21 | **16.10** | 1.577 | **59.0분** | ✓ |
| swim d9 | 180 | 12.93 | **20.30** | 1.570 | **60.9분** | ✓ |
| sweat d8 | 190 | 7.98 | **14.50** | 1.817 | **45.9분** | ✓ |

```
 **프로브의 정의역 한계 — 실측으로 드러났다(§3-3 · #107)**: 20프레임은 «드레이프 «전»» 구간만 본다.
 전량 실행은 프레임이 갈수록 느려질 수 있고, 이 판의 sweat Node 실행이 그 형태를 보였다.
 **프로브는 벽 판정의 «하한»이지 상한이 아니다.**
```

---

## §3 ㉢ 전량 + 하드 게이트

경로: v3-38 과 같은 **브라우저 워커 + `s4Gate.ts`** · **단독**.
프레임 수는 §0-4 「실행이 정한다」에 따랐고 도출을 아래에 적는다.

### §3-1 gray d8 — **후보 탈락**(③a)

**1차 220프레임**(구 해상도의 정착 프레임 그대로)

| 채널 | 문턱 | 구 d9 등재 | **실측** | 판정 |
|---|---|---|---|---|
| ③a 관통 | ≤0.5mm | 0.3768 ✓ | **0.5317mm** | ✗ |
| 정착 `settleNetM` | ≤0.1mm | — | **0.1342mm** (settled **false**) | ✗ |
| 자기교차 / 발산 / pinned | 0 | 0 | **0 / false / 0** | ✓ |
| `ringExcess` | ≤1 | — | **0.9882** | ✓ |
| 시간 | ≤120분 | 53.7분(구) | **49.7분** | ✓ |

**정착이 안 된 상태의 ③a 는 과도기 값**이라 구 d9(settled=true)와 같은 채널이 아니다.
그래서 프레임을 늘려 다시 돌렸다 — **프레임 도출**: 벽 120분의 **75%(90분)** 를 프레임 예산으로 쓴다
(실행 간 요동이 2배까지 있다는 등재 사실 때문에 여유를 둔다) ÷ 실측 0.226분/프레임 ⟹ **390**.

**2차 390프레임 상한 · 실제 330프레임에서 종료**

| 채널 | **실측** | 판정 |
|---|---|---|
| **③a 관통** | **0.5317156289mm** (1차 0.5317156072 · **최악 정점 v11079 동일** · 좌표 소수 9자리까지 동일) | **✗** |
| 창 순변위(진행 통지) | f=290~320 **0.0320 / 0.0349 / 0.0271 / 0.0287mm** — 전부 ≤ 0.1mm ⟹ **정착 성립** | ✓ |
| 게이트 `settleNetM` | **`null`** — `before` 를 `frames−N_WIN`=380 에서 잡게 돼 있는데 330 에서 끝나 **미포착**(계기 한계) | 판정 불가 |
| 자기교차 / 발산 | 0 / false | ✓ |
| `ringExcess` | 0.9882 | ✓ |
| 시간 | **89.6분** | ✓ |
| 상태 sha | `d3288cca d5181cef …` (1차 `08f2438b f099da6f …`) | — |

```
 **③a 가 프레임 220 ↔ 330 에서 같은 값·같은 정점이다** ⟹ **정적 조건**이고 더 돌려도 안 바뀐다.
 ⟹ **gray d8 후보 탈락(③a 0.5317mm > 0.5mm). 3차 실행 불필요** — 회차를 아꼈다.
 **관측 1건(원인 미상)**: 2차 실행이 `stopped: true` 로 프레임 330 에서 끝났다. `cancel` 을 보내는 곳은
 취소 버튼뿐이고 **CC 의 도구 호출에서 취소가 나가지 않았다**. 원인은 특정하지 못했다 — 사실만 적는다.
```

### §3-2 swim d9 — **전 채널 통과** (`pass: true` · `fails: []`)

프레임 **260**(= 90분 예산 ÷ 프로브 0.338분/프레임 ⟹ 266 → 260).

| 채널 | 문턱 | 구 d10 등재 | **실측** | 판정 |
|---|---|---|---|---|
| **③a 관통** | ≤0.5mm | 0.3653 ✓ | **0.4972mm** | **✓** |
| 자기교차 | 0 | 0 | **0** | ✓ |
| 발산 | 0 | 0 | **false** | ✓ |
| `ringExcess` | ≤1 | — | **0.9908** | ✓ |
| **정착** | ≤0.1mm | — | **0.0000015mm** (settled **true**) | ✓ |
| pinned | 0 | — | **0** | ✓ |
| **시간** | ≤120분 | 44.4분(병렬·판정 불가) | **61.2분** | ✓ |
| `lambdaMax` | 참고 | 1.3500 | 1.3469 | — |
| 시접 갭 중앙/최대 | 참고 | — | 2.124 / 6.162mm | — |
| 관통 정점 `penCnt` | 참고 | — | 788 | — |
| 최악 정점 | 참고 | — | v4616 @ (0.0464, 1.5047, 0.0415) | — |
| 상태 blob sha | — | — | `786f3fc0 8165c757 …` | — |
| 상태 **페이로드** sha | — | — | **`557bc446 9a0f1b57 …`** | — |

**③a 여유율 0.0028mm(0.56%)** — 문턱에 매우 가깝다. **시간 여유율 58.8분(49.0%)**.

### §3-3 sweat d8 — **미측정**(환경 제약 · 판정 불가)

```
 ① **브라우저 1차 유실** — 프레임 110/370 까지 진행 후 페이지가 리로드됐다.
    사실: vite 클라이언트 재로드 **오전 3:01:51** · **저장소 쓰기 0**(미커밋은 실행 전 만든
    `scripts/v3Asm.ts` 하나이며 앱이 임포트하지 않는다) · vite 프로세스 생존.
    **원인 미상.** 같은 형태의 유실이 swim 1차에서도 있었다(재로드 **오전 1:29:42** · 진행 80/260).
 ② **Node 재시도 — 시간 채널 «오염»**: 탭 의존을 없애려 `scripts/v3Gate.ts`(워커의 실행·게이트
    «그 줄»을 옮긴 계기 · 물리 0줄)로 돌렸다. 실측:
       f=20 **5.6분**(16.8 s/f) → f=40 **53.8분** → f=60 **124.0분** → f=80 **179.8분**
    같은 시각 `duetexpertd`(macOS 시스템 데몬)가 **99.3% CPU 를 15시간 38분째** 점유(load 2.62).
    §1′/#79: 경쟁 부하가 있으면 **시간 채널은 판정 불가** ⟹ 이 실행으로 벽을 판정하지 않는다. **중단**했다.
    (등재 뒤 재확인: `contactsd` 83% · `WindowServer` 57% · load **6.19** — 부하 지속)
 ⟹ **sweat d8 은 «미측정»이다. 통과도 탈락도 아니다.** 추정치를 적지 않는다.
    구성상 **구 해상도 d9 를 유지**한다.
```

---

## §4 ㉣ S5 — **미실행 · 판정 불가**

```
 §0-5 는 «3종 인접 차»를 요구한다. 신 해상도 통과분은 **swim 1종**뿐이고 sweat 는 **미측정**이다.
 혼합 구성으로 재판정하는 길이 §0-5 에 열려 있으나, 그러려면 **구 2종의 O4 를 같은 실행 조건에서
 다시 내야** 한다 — 이 판은 그 실행을 하지 않았다.
 ⟹ **S5 판정 미실행.** 갈래 D 는 발화도 기각도 되지 않았다. **부채로 등재한다.**
```

---

## §5 ㉤ 대조 채널 — 어깨 집힘 서류 (**기록 · 판정 아님**)

구 해상도 값은 v3-44/45 등재분, 신 해상도 값은 이 판의 상태에서 **같은 계기**(`v3SeamShoulder` · 브리지 on)로 냈다.

| # | 채널 | gray d9 → **d8**(탈락) | swim d10 → **d9**(통과) |
|---|---|---|---|
| ① | 시접 ±2열 **농축비** | 2.42 → **2.17** ↓ | 2.37 → **2.22** ↓ |
| ② | 상위 5% **소매 점유** | 58% → **53%** ↓ | 50% → **46%** ↓ |
| ③ | **sideXplus 잔여 끊김**(브리지 on · 팔 숨김) | 28.5 → **30.5‰** ↑ | 31.0 → **19.8‰** ↓ |
| ④ | **코너 확폭 최대** | 5.13 → **4.90mm** ↓ | 4.58 → **6.16mm** ↑ |
| — | 시접 갭 중앙(rest 대비) | 2.15 (1.073배) → **2.11 (1.053배)** | 2.13 (1.063배) → **2.12 (1.062배)** |
| — | 능선 위 옷 정점 | 776 (7.46%) → **949 (7.16%)** | 762 (8.78%) → **900 (8.65%)** |
| — | **능선 위 법선 이격 중앙** | 1.62 → **1.70mm** | 4.97 → **4.45mm** |
| — | **③a 여유율** | −6.3%(초과) | **0.56%**(0.0028mm) |
| — | **시간 여유율**(새 벽 대비) | 25.3%(89.6/120) | **49.0%**(61.2/120) |

```
 **엇갈린다.** ①② 는 두 원단 모두 «감소»(집힘이 시접·소매에 덜 몰린다)이지만
 ③ 은 gray 만 악화 · ④ 는 swim 만 악화다. **개선 «양»도 작다**(① −6~−10% · ② −4~−5pp).
 **이 판은 이 표로 판정하지 않는다** — §0-6 이 「기록 · 판정 아님」으로 못박았고,
 좋고 나쁨은 **사용자 화면 판정 몫**이다.
```

---

## §6 ㉥ 캡처 — **통과 구성 12장** + 탈락 기록 4장

```
 **통과 구성** = **swim d9 «신»** + gray d9 «구» + sweat d9 «구» (혼합 구성 · 갈래 C)
 경로  docs/captures/v3-47-해상도상향/
   v3-47-swim-d9-f260-**557bc446**-{front-p,side-p,back-p}.png   ← **신 해상도 후보**
   v3-47-gray-d9-f220-faf4873f-{front-p,side-p,back-p}.png       ← 구 정본
   v3-47-sweat-d9-f190-bb22ce73-{front-p,side-p,back-p}.png      ← 구 정본
   …-compare-side.png ×3 (좌 제품 side-p / 우 같은 상태의 진단 래스터 sideXplus)
 **탈락 기록** docs/captures/v3-47-해상도상향/탈락-gray-d8/ — gray d8 4장(f320 · `08f2438b`)
 규약  v3-45 형식 그대로 · **브리지 on** · 제품 900×1260 · 대조 1800×1260
 **무변조 3/3** — 표시 전후 payload·pos sha 동일(`557bc446…` / `faf4873f…` / `bb22ce73…`)
 파일명 접두사는 패널이 `v3-45-` 로 발행하므로 **저장소에 넣을 때 `v3-47-` 로 바꿔 적었다**(코드 0줄)
 **CC 자체 화면 판정 0.** 사용자 재판정 대상 = **(가) 어깨 집힘 인상 변화**
```

### §6-1 재현 절차

```
 1. npm run dev → http://localhost:5173/?v3=1
 2. «정착 상태» 선택칸에 **「swim d9 신 (정착 260 · 후보)」** 가 추가돼 있다(구 3종 그대로)
 3. [정착 상태 표시](물리 **0프레임**) → [제품 표시] → 시접 브리지 체크(기본 켜짐)
 4. [front-p] [side-p] [back-p] → [캡처 3장] · [대조 1장]
 주입 파일  public/v3diag/settled-swim-d9-new.bin (신) · settled-{gray-d9,swim-d10,sweat-d9}.bin (구 · 무삭제)
```

---

## §7 ㉦ 갈래 판정 — **C (일부 원단 탈락 · 혼합 구성)**

| 갈래 | 조건 | 실측 | 성립 |
|---|---|---|---|
| A | 3/3 게이트 통과 · S5 유지 | gray 탈락 · sweat 미측정 · S5 미실행 | 아니오 |
| B | 프로브 전 원단 벽 초과 | 3/3 통과(59.0 / 60.9 / 45.9분) | 아니오 |
| **C** | **일부 원단 탈락** | **gray d8 탈락 · swim d9 통과 · sweat d8 미측정** | **예** |
| D | S5 붕괴 | **미실행 — 판정 불가** | 판정 불가 |
| E | 판정 불가 | — | 아니오 |

**혼합 구성**: **swim 만 신 해상도 d9 «후보»** · gray·sweat 는 **구 해상도 유지**.
**정본 승격은 사용자 화면 합격 + 전략 선언 뒤다** — 이 판은 후보까지다.

### §7-1 이 판이 «닫은» 것 / «연» 것

```
 닫음  **gray d8 은 안 된다** — ③a 0.5317mm 가 프레임 220↔330 에서 «같은 값·같은 정점»(정적 조건)
       **파생 치수는 d 와 무관**하다(3종 × 2해상도 전부 8.928/49.315/12.918/21.495)
       프로브 3/3 이 새 벽 안(59.0 / 60.9 / 45.9분)
 엶    **swim d9 의 ③a 여유가 0.56%(0.0028mm)** — 문턱에 붙어 있다. 재현 편차에 뒤집힐 수 있다
       **sweat d8 미측정** — 환경이 가라앉은 뒤 90분이 필요하다
       **S5 미실행** — 혼합 구성 판정에는 구 2종 O4 재실행이 필요하다
       **프로브 정의역 한계**(#107) — 20프레임은 드레이프 «전» 구간만 본다
```

---

## §8 부채

```
 유지  denim 검증 경로 재측정 · swim/sweat 시접 굽힘 ③a 미측정(v3-46 신규분)
 **신규**
  · **sweat d8 전량 미측정** — 시스템 부하가 가라앉은 뒤 재실행(≈90분)
  · **S5 3종 인접 차 미실행** — 혼합 구성 판정에는 구 gray·sweat O4 재실행이 필요
  · **브라우저 장시간 실행의 유실** — 2회 관측(01:29:42 · 03:01:51 재로드 · 저장소 쓰기 0 · 원인 미상).
    장시간 실행은 **Node 경로**(`scripts/v3Gate.ts`)가 탭 의존이 없어 안전하나, **시간 채널은
    시스템 부하를 함께 재야** 한다
```

---

## §9 회귀

```
 **코드 diff — 구성·계기 경로 외 0줄**
   물리·조립·게이트·표시층·브리지: `src/v3/` 전량 v3-46 HEAD 대비 **0줄**
   변경분 3건은 전부 구성·계기다:
     · `scripts/v3Asm.ts`(신설 · 계기 · 조립 2회 확인)
     · `scripts/v3Gate.ts`(신설 · 계기 · 워커의 실행·게이트 줄을 옮긴 Node 경로)
     · `src/components/V3Panel.tsx` **1줄 + 주석**(정착 상태 선택 항목 추가 · **표시 로직 0줄**)
   `public/v3diag/settled-swim-d9-new.bin` 신설 — **구 정본 3종 무삭제**
 **문턱·게이트·SEP·원단 물성 변경 0** · denim 처분 유지 · v2 경로 무영향
 npx tsc -b rc=0 · npm run build 통과
 **구 정본 상태의 진단 벌 값 재현**(브리지 off · `settled-gray-d9.bin`):
   끊김 front/side/back **7 / 118 / 7** · 팔 숨김 후 side **76(65.6‰)** · 실루엣 **9735** — v3-44/45 와 일치
 **하네스 비트 동일**: 코드 0줄이므로 자동 성립 — 위 진단 벌 재현으로 확인
```

---

## §10 자기 점검

| # | 항목 | 답 |
|---|---|---|
| 1 | §0 커밋이 실행보다 앞서는가 | **예** — `509404b`(§0) → 이후 전부 |
| 2 | 코드 diff 0줄인가(구성·계기 외) | **예** — `src/v3/` 전량 0줄. 변경 3건은 계기 2 + 패널 선택 항목 1줄(표시 로직 0줄) |
| 3 | 벽 120분의 귀속이 등재됐는가 · 구 60분 무삭제인가 | **예** — §0-1 에 「사용자 승인 · 소유자 재가격 · 도출 상수가 아니라 UX 예산」으로 명기. 구 60분 등재(v3-38·v3-46)는 **손대지 않았다** |
| 4 | 판정에 쓴 시간이 전부 단독·실측인가 | **gray·swim 예**(단독 브라우저 실행 · 89.6분 / 61.2분 실측). **sweat 아니오** ⟹ 그래서 **판정하지 않고 «미측정»으로 적었다**(§3-3) |
| 5 | ㉤ 대조표가 구/신 나란히인가 | **예** — §5 |
| 6 | #105 · #106 신설 | **예** — #105(벽 재가격의 귀속) · #106(해상도 후보 결과) · **#107 추가**(프로브 정의역 한계) |
| 7 | 이 판에서 호출한 ecc 에이전트·스킬 | **0 / 0** |
