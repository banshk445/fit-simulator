# 35 — 채널 확정 + 문언 회수 + 핏 맵 색 (v3-54 · 4/5 완결 «후보» 판 · **물리 0프레임**)

v3-54. 브랜치 `v3-54-fit-color2`(사전 등록). 호출한 ecc 에이전트/스킬: **0 / 0**

## git 스탬프

```
 브랜치  v3-54-fit-color2           워크트리  /Users/banshk/projects/fit-simulator
 기점    fab0994 (v3-53 HEAD)       직전 회차  fab0994
 커밋    c929f9e  §0  판정 기준 (집행 «전»)
         c5aabcf  §1-2 채널 확정 · S5 교차 확인 · 문언 정정
         (본문)   §3~§7  색 · 게이트 · 캡처 · 갈래 C
 검사    npx tsc -b rc=0 · npm run build 통과 · **물리 0프레임 · 솔버·게이트 0줄**
```

---

## §0 판정 기준 — 실행 «전»에 등록

### §0-1 배경 (전략 세션 판정 수령분)

```
 v3-53 **B·C 정지 수령** · **§0-1a `exactBodyDist` 판단 «승인»**(수단 과잉 지정은 **전략 귀책**).
 **Q1 = ㉮**: **정확 거리 채널을 «국면 판정 포함» 정본 채택.** 「국면 개수 불변」 조항 **철회** —
   **근거는 «정의»다**(경계 `SEP` 는 **길이**이고 · 길이 비교는 **거리의 정의**로 하며 ·
   구 채널은 **근사**이고 그 **측정 오차가 경계 판정을 뒤집기 충분**하다).
   조항의 **전제(두 채널의 경계 일치)가 «측정»으로 반증**됐다. **1~4정점 변화 = «교정»으로 등재.**
   **㉯ 기각**(두 채널 공존 = **함정 22**).
 **Q2 = 문언 정정 + 회수 «승인».**
```

### §0-2 ㉠ 채널 확정 (사무 + 확인 1건)

```
 `fitReport` **국면 판정 = 정확 거리(정본)**. **구 채널은 «자기검사 전용»으로 강등 표기.**
 **v3-52 #112 의 표를 신 채널 값으로 «정본 갱신»**(구 값 병기 · **무삭제**).
 **S5 교차 확인**: `v3O4` 를 **정확 거리 판으로 재산출**(3 정본 blob · **0프레임**) —
   **순서(swim < gray < sweat)** · **인접 차(≥ 0.25pp)** 유지 여부.
   **유지 ⟹ 「검증 채널(SDF)·리포트 채널(정확)」 분리 등재 · 진행**
   **뒤집힘 ⟹ 갈래 B 정지 · 상신**
```

### §0-3 ㉡ 문언 정정 + 회수 (Q2)

```
 `instruments.ts` 「**SDF 오차 ≤ h의 5%**」 → **실측 문언으로 교체**(v3-53 §2-1 인용 ·
 **원문 무삭제** · **정정 이력 명기**).
 **회수 조사**: 저장소 «전체»에서 ① **그 문언 인용처** ② **`sampleSdf` 값을 «거리»로 직접 읽는 용법**
 을 목록화. 각각에 **「영향 없음(근거) / 재검 필요」** 판정.
 **재검 필요가 나오면 그 항목은 «착수하지 말고» 상신**(이 판 정의역 밖).
```

### §0-4 ㉢ 핏 맵 색 (v3-53 §0-3 문언 그대로)

```
 **분기점**: **−0.5mm**(게이트 하한) · **0** · **`SEP`(2mm)** · **여유 상한 = 그 상태 c 상위 분위수**
 (**CC 확정 · 근거 명기 · 손 상수 0 · 범례 공개**).
 **색상은 표시 재량** · **#89 갱신** · **blob 무변조** · **브리지 공존 규칙 명기**.
```

### §0-5 ㉣ 게이트 + 캡처 + 상신

```
 **G1~G6 전량 재판정**(3종). **캡처 9장(색 on) + 표 스냅샷 3장.** **CC 화면 판정 0.**
 사용자 판정: **「색이 핏을 읽히게 하는가」**. 통과 시 **4/5 완결 조건 충족** — **선언은 전략 세션.**
```

### §0-6 ㉤ 갈래 (사전 등록)

| 갈래 | 조건 | 귀결 |
|---|---|---|
| **A** | S5 유지 · 회수 «영향 없음» · 색·캡처 산출 · G 전량 | **사용자 화면 판정 상신** |
| **B** | **S5 뒤집힘** | 정지 · 상신 |
| **C** | 회수에서 «**재검 필요**» 발견 | **그 목록 상신** · 색은 산출돼 있으면 **캡처까지 내되 «완결 후보» 주장 0** |
| **E** | 판정 불가 | 정지 · 사실만 |

### 이 판에서 바꾸지 않는 것

**물리 0프레임** · **솔버·게이트 0줄** · 문턱·정본 **0** · **`SEP`·밴드 도출 0** · v2 경로 **0** ·
**`V2DIMS` off** · **높이 도출·행 정의 0** · **철회된 조항 «외» v3-53 §0 문언 0**.

### 순서

```
 §0 커밋 → ㉠ 확정+S5 → ㉡ 정정+회수 → ㉢ 색 → ㉣ 게이트+캡처 → 등재·상신
```

---

## §1 ㉠ 채널 확정 — **집행 완료**

### §1-1 국면 판정의 정본 = **정확 거리**(Q1 ㉮)

```
 `src/v3/fitReport.ts` 주석에 **철회 근거를 «정의»로** 적었다(코드 옆에 남는다):
   경계 `SEP` 는 **길이**이고 · 길이 비교는 **거리의 정의**로 한다 ·
   구 채널은 **근사**이며 그 오차(밴드 안 실측 0.943~0.975mm)가 **2mm 경계를 뒤집기 충분**하다 ·
   **1~4정점 차이는 «오류»가 아니라 «교정»**이다.
 ⟹ **데이터가 근거가 아니라 «정의»가 근거다**(v3-52 §1-1 의 chestY 재정의와 같은 형식).
 **구 채널(`sampleSdf`)은 «자기검사 전용»으로 강등** — `sdfRows` · `self` 에만 남고 **판정에 쓰지 않는다**.
 **삭제 0**(무삭제 규약).
```

### §1-2 #112 표 — **신 채널로 정본 갱신**(구 값 병기 · 무삭제)

| 원단 | 부위 | **정본(정확 거리)** | 구 값(SDF · 자기검사 전용) |
|---|---|---|---|
| **gray d9** | 목선 | **1.04** (0.99~2.84) | 1.00 (1.00~2.78) |
| | 가슴 | **4.64** (1.03~16.06) · 0/101/166 | 4.64 (1.02~8.90) · 0/101/166 |
| | 허리 | **45.16** (21.15~77.19) · 0/0/239 | 8.90 (8.90~8.90) · 0/0/239 |
| | 밑단 | **24.08** (8.07~45.76) · 0/8/108 | 8.90 (7.67~8.90) · 0/8/108 |
| | 소매 | **5.38** (1.06~16.71) · 0/**374**/**748** | 5.40 (1.06~8.90) · 0/370/752 |
| **swim d9** | 허리 | **28.65** (17.74~75.36) | 8.90 |
| | 밑단 | **21.86** (7.67~42.25) | 8.90 |
| | 소매 | **5.69** (1.33~14.21) · 0/**331**/**791** | 5.73 · 0/330/792 |
| **sweat d8** | 허리 | **51.86** (20.76~79.25) | 8.90 |
| | 밑단 | **30.27** (15.61~50.60) | 8.90 |
| | 가슴 | **5.28** (1.03~20.64) · 0/**103**/**178** | 5.32 · 0/102/179 |
| | 소매 | **3.98** (1.01~22.09) · 0/**563**/**805** | 4.02 · 0/562/806 |

```
 **눌림은 3종 전부 0** 이고 **부호 일치율 100%** 다 — 갈린 것은 «밀착↔여유» 경계뿐이다(교정 등재).
```

### §1-3 ★ S5 교차 확인 — **유지**(갈래 B 불발화)

| 구성 원단 | **정확 거리 O4** | SDF O4(검증 채널) | 차 |
|---|---|---|---|
| swim 신 d9 | **19.12%** | 19.07% | +0.05pp |
| gray 구 d9 | **22.40%** | 22.35% | +0.05pp |
| sweat 신 d8 | **23.19%** | 23.15% | +0.04pp |

```
 **순서 swim < gray < sweat 유지** ✓ (v3-26 §3-3 등재 순서의 3종 제한과 같다)
 **인접 차 3.28pp / 0.79pp** ≥ 잡음 바닥 **0.25pp** ✓
 **두 채널의 차(0.04~0.05pp)가 잡음 바닥(0.25pp)의 «1/5 이하»** ⟹
   **S5 판정은 채널 교체의 영향을 받지 않는다.**
 ⟹ **#115 «채널 분리» 등재**: **검증 채널 = SDF**(S5·O4·하네스 접촉 계수) ·
   **리포트 채널 = 정확 거리**(핏 리포트·색). **둘은 서로를 대체하지 않고, 서로의 교차 검증이다.**
```

---

## §2 ㉡ 문언 정정 + 회수 (Q2)

### §2-1 정정 — `src/v3/instruments.ts` (**원문 무삭제 · 이력 명기**)

```
 구 문언 「SDF 오차 ≤ **h의 5%**, 거르기 문턱 10h 로 **두 자릿수 여유**」
 실측    밴드 안 최대차 **0.943 / 0.961 / 0.975mm** = **h의 23.9~24.7%**(v3-53 §2-1)
 새 문언 「격자 SDF 오차는 **최대 h의 약 25%**(실측 ≤ 0.98mm)이고, 거르기 문턱 10h 가 그 **약 40배**라
        «거르기»에는 충분하다. **거리 «값»으로는 쓰지 말 것**」
 ⟹ **오차 «크기» 문언은 틀렸다(약 5배). «결론»(거르기 안전)은 유지된다** — 그 구분을 주석에 적었다.
```

### §2-2 회수 조사 — **grep 전량** · 항목별 판정

**① 그 문언 인용처**: `src/v3/instruments.ts:146` **한 곳뿐**(grep `h의 5%`). → **정정 완료**.

**② `sampleSdf` 값을 «거리»로 직접 읽는 용법** — `grep -rn "sampleSdf" src/ scripts/` 전량:

| # | 자리 | 용법 | 판정 |
|---|---|---|---|
| 1 | `src/v3/instruments.ts:229` | `bodyClearance` **후보 거르기**(`g > CELL` skip) | **영향 없음** — `CELL` = 10h = **39.5mm**, 최대 오차 0.98mm 의 **40배**. 관통은 `signed < THICK`(1mm)이라야 하므로 놓칠 수 없다 |
| 2 | **`src/v3/solver.ts:131-135`** | **물리 접촉 거리·법선** | **★ 재검 필요** — §2-3 |
| 3 | `src/v3/garmentScene.ts:577 · 630` | `fitDelta()`·소매 R 도출의 **최소 여유 탐색** | **영향 없음(조건부)** — δ 는 «**초기 배치**»이고 결과는 **게이트가 정착 상태에서** 판정한다. 정본 3종이 그 δ 로 통과했다. **다만 δ 의 «의미»(정확히 그만큼 띄운다)는 최대 ~1mm 어긋날 수 있다 — 사실 병기** |
| 4 | `src/v3/fitReport.ts` | **자기검사 전용**(강등 완료) | **영향 없음** — 판정에 안 쓴다 |
| 5 | `scripts/v3O4.ts:28` · `scripts/v3S4.ts:748` | **S5 O3/O4 검증 채널** | **영향 없음 — 값으로 확인**(§1-3: 차 0.04~0.05pp ≪ 바닥 0.25pp) |
| 6 | `scripts/v3S4.ts:866 · 1036 · 1049 · 1183 · 1238` | `sampleSdf ≤ SEP` **접촉 계수** | **영향 없음 — 값으로 확인**(§1-3 과 같은 계열 · 같은 문턱 SEP) |
| 7 | `scripts/v3SeamShoulder.ts:84` · `scripts/v3Artifact.ts:339` | **이미 «혼합»** — `g > CELL` 이면 SDF, 아니면 **정확 거리** | **영향 없음** — 근방에서 이미 정확 거리를 쓴다(코드 사실) |
| 8 | `scripts/v3Body.ts` · `scripts/v3S3.ts:138` | **SDF 자체를 검증하는 하네스** | **영향 없음** — 오차를 «재는» 쪽이다 |
| 9 | `src/lib/sdfCollision.ts` · `scripts/paramSweep.ts` · `scripts/dressPattern.ts` | **v2 경로** — v2 자체 SDF 필드(다른 굽기·다른 상수) | **정의역 밖** — 이 판의 문언은 **v3 `bodySdf`** 에 대한 것이다 |

### §2-3 ★ **재검 필요 1건 — `src/v3/solver.ts` 물리 접촉**

```
 `solver.ts:131` 이 `d = sampleSdf(...)` 를 **접촉 거리로 직접 쓴다**(법선도 그 중심차분이다).
 ⟹ **물리가 보는 «몸 표면»이 실제 메시에서 최대 ~1mm 어긋난다.**
 **음(−)의 방향은 게이트가 잡는다** — ③a 가 **정확 거리로** 관통을 재고 **≤ 0.5mm** 를 요구한다
   (정본 3종 전부 통과) ⟹ **파묻히는 쪽은 이미 상한이 있다**.
 **양(+)의 방향은 게이트 채널이 «없다»** — 옷이 SDF 가 생각하는 것보다 최대 ~1mm 더 «떠» 있어도
   현행 게이트는 그것을 재지 않는다. **이 사실은 v3-53 의 정정 «전»에는 등재되지 않았다.**
 ⟹ **§0-3 조항대로 «착수하지 않고» 상신한다.** 이 판은 `solver.ts` 를 **한 줄도 고치지 않았다.**
 **부수 확인(단정 0)**: 이것이 «결함»인지 «허용 오차»인지는 **접촉 모델의 정의 문제**이고,
   판단은 전략 세션 몫이다. **기전 귀속 0 · 처방 0.**
```

⟹ **§0-6 갈래 C 성립**(회수에서 «재검 필요» 발견).

---

## §3 ㉢ 핏 맵 색 — **산출 완료**

### §3-1 분기점 — **전부 등재량 또는 «그 상태»에서** (손 상수 0)

| 분기점 | 값 | 출처 |
|---|---|---|
| 눌림 하한 | **−0.5mm** | `s4Gate.S4_THRESHOLD.penMaxM` **그대로 임포트**(새 수 0) |
| 눌림/밀착 | **0** | 부호 |
| 밀착/여유 | **`SEP` = 2.0mm** | `consts.ts`(#110 Q1) |
| 여유 상한 | **그 상태 c 의 p95** — gray **82.4mm** · swim **80.5mm** · sweat **86.8mm** | **그 상태에서 뜬다** |

```
 **p95 의 근거**: 상한을 **p100** 으로 두면 «가장 멀리 뜬 몇 정점»(밑단 자락 · 최대 110mm 대)이
 그라데이션 전체를 눌러 **대부분이 한 색으로 뭉친다**. p95 는 **그 꼬리만 잘라내는 표시 층 절단**이고,
 값은 «그 상태»에서 뜨므로 **원단·해상도마다 다르다**(위 표가 그 증거다).
 **판정 채널이 아니며 범례가 mm 를 밝힌다**(G5).
```

### §3-2 렌더 기여 목록 갱신 (**#89 형식**)

```
 이 상태의 «화면»에 기여하는 층(위에서 아래로):
   ① **정점색 = 핏 맵 색**(v3-54 신설 · **표시 전용** · `fitReport.color.rgb`) ← **이번에 추가**
   ② 시접 브리지 스트립(v3-45 · 표시 전용 · **정점을 만들지 않는다**)
   ③ 정점 노멀 보간 스무딩(v3-42 §4)
   ④ 원근 카메라 · 조명 3 · 톤매핑(v3-43 `DISPLAY`)
 **브리지 공존 규칙**: 브리지는 «기존 정점»을 새 삼각형으로 이을 뿐 **정점을 만들지 않는다**(v3-45 등재)
   ⟹ **정점색이 그대로 스트립에도 이어진다.** **스트립 전용 색을 따로 두지 않았다** — 그래서
   시접 자리가 «다른 색»으로 튀지 않는다.
 **blob 무변조**: `geom()` 이 위치·인덱스·색을 **전부 사본**으로 만든다(`Float32Array.from`) ⟹
   상태 배열에 한 바이트도 쓰지 않는다. **실측 확인**: 표시 전후 `settled-gray-d9.bin`
   sha256 `8dec6eca…` **동일**.
```

---

## §4 ㉣ 게이트 + 캡처

| # | 게이트 | 판정 | 근거 |
|---|---|---|---|
| **G1** | v2 임포트 0 · v2 데이터 읽기 0 · `V2DIMS` 미사용 | **통과** | `grep -rn "from '../lib/" src/v3/ src/workers/v3DressWorker.ts` **0건** |
| **G2** | 물리 0프레임 | **통과** | 표·색 전부 **정착 blob 주입 + `frames:0`** 경로 · 솔버 스텝 0 |
| **G3** | 손 상수 0 | **통과** | §3-1 표 — 분기점 4개가 전부 등재량 또는 «그 상태» · 대역 반폭 = `d` · p95 는 **표시 층 절단**으로 명기 |
| **G4** | 정본 3종 전부 산출 · NaN·불가 0 | **통과** | 3종 × 5행 = **15행** · 표본 = 정의역 · 색 정점 **10402 / 10402 / 13248 전량** |
| **G5** | 경계 자기 공개 | **통과** | 범례가 **눌림 <0 · 밀착 0~2.0mm · 여유 2.0~82.4mm(p95) · 눌림 하한 −0.5mm(게이트 ③a 문턱)** 를 인쇄 |
| **G6** | 자기검사 값 표시 | **통과** | 표본·최대차·부호 일치율·**등재 잡음 초과 표본**·밴드 상한을 화면이 인쇄 |

**캡처**: `docs/captures/v3-54-핏색/` — **제품 9장(색 on · 3종 × front/side/back-p)** + **표 3장**.
**CC 자체 화면 판정 0.**

---

## §5 ㉤ 갈래 판정 — **C**(회수에서 «재검 필요» 발견)

| 갈래 | 조건 | 실측 | 성립 |
|---|---|---|---|
| A | S5 유지 · **회수 «영향 없음»** · 색·캡처 · G 전량 | **회수에 «재검 필요» 1건**(`solver.ts`) | 아니오 |
| B | S5 뒤집힘 | **유지**(순서·인접 차 전부) | 아니오 |
| **C** | 회수에서 «재검 필요» 발견 | **`src/v3/solver.ts` 물리 접촉 1건** | **예** |
| E | 판정 불가 | — | 아니오 |

```
 **갈래 C 의 조항 그대로 집행했다**: 「그 목록 상신 · 색은 산출돼 있으면 **캡처까지 내되
 «완결 후보» 주장 0**」 ⟹ **캡처 12장을 냈고, 4/5 완결 조건 충족을 «주장하지 않는다».**
 사용자 화면 판정은 **받을 수 있다**(색이 산출됐다) — 다만 **합격해도 완결 선언의 전제는
 «재검 1건의 처분»이 먼저다.**
```

### §5-1 이 판이 «닫은» 것 / «연» 것

```
 닫음  **채널 확정** — 국면 판정 = 정확 거리(정본) · 구 채널은 자기검사 전용 강등(**삭제 0**)
       **S5 유지** — 두 채널 차 0.04~0.05pp ≪ 바닥 0.25pp ⟹ **채널 분리 등재(#115)**
       **문언 정정 완료**(원문 무삭제) · **회수 조사 전량**(9항목)
       **핏 맵 색 산출** — 분기점 4개 전부 도출 · 범례 공개 · **blob 무변조 실측 확인**
       **브리지 공존** — 스트립이 정점을 안 만들어 색이 그대로 이어진다
 엶    **★ `solver.ts` 물리 접촉의 SDF 오차** — 음의 방향은 ③a 가 잡지만 **양의 방향은 게이트 채널이 없다**
       **`garmentScene` δ 의 의미가 ~1mm 어긋날 수 있다**(사실 병기 · 게이트가 결과를 잡으므로 영향 없음)
       **4/5 완결** — 재검 처분 뒤
```

---

## §6 회귀

```
 **물리 0프레임** · **솔버·게이트 0줄** — `solver.ts` · `s4Gate.ts` · `garmentScene.ts`
   **`fab0994` 대비 diff 0**(재검 필요로 지목한 `solver.ts` 도 **한 줄도 안 고쳤다**)
 변경  `src/v3/fitReport.ts`(채널 확정 표기 + 색) · `src/v3/productView.ts`(정점색 입력 · 표시 층) ·
       `src/components/V3Panel.tsx`(색 토글·범례 · 표시만) · `scripts/v3O4.ts`(교차 확인 병기) ·
       `src/v3/instruments.ts`(**주석 정정만** · 코드 0줄)
 **G1** `from '../lib/` grep **0건** · `V2DIMS` 미사용 · **하네스 유입 0**
 문턱 0 · 정본 0 · `SEP`·밴드 도출 0 · v2 경로 0 · 높이 도출·행 정의 0
 **npx tsc -b rc=0** · **npm run build 통과**
 **구 정본 진단 벌 재현** 끊김 **7/118/7** · 실루엣 **9735** — 등재분과 일치
 **blob 무변조** `settled-gray-d9.bin` sha256 **`8dec6eca…` 동일**(표시 전후)
```

---

## §7 자기 점검

| # | 항목 | 답 |
|---|---|---|
| 1 | §0 커밋 선행 · 물리 0프레임 · 솔버 0줄 | **예** — `c929f9e`(§0) 선행 · 솔버 스텝 0 · `solver.ts` diff **0** |
| 2 | 조항 철회 근거가 «정의»로 등재됐는가 | **예** — `fitReport.ts` 주석과 §1-1 에 「경계 `SEP` 는 **길이** · 길이 비교는 **거리의 정의**로 · 구 채널은 **근사**」로 적었다. **데이터는 «전제의 반증»만** 제공했다 |
| 3 | S5 교차 확인 결과와 채널 분리 등재 | **예** — §1-3(19.12/22.40/23.19% · 순서·인접 차 유지 · 두 채널 차 0.04~0.05pp) · **#115 채널 분리** |
| 4 | 회수 목록이 전량인가 · 판정 근거 병기 | **예** — `grep -rn "sampleSdf" src/ scripts/` 전량을 **9항목**으로 묶고 각각 근거를 적었다. **재검 필요 1건**(`solver.ts`)은 **착수 0 · 상신** |
| 5 | 색 분기점 도출·범례·#89 갱신 · G1~G6 | **예** — §3-1(4분기점 · p95 근거) · 범례 화면 공개 · §3-2 렌더 기여 4층 · §4 G1~G6 **전량 통과** |
| 6 | #114 · #115 · #113 갱신 | **#114 신설 예**(핏 맵 색) · **#115 신설 예**(검증 채널/리포트 채널 분리) · **#113 갱신 예**(문언 정정 + **회수 결과 · 재검 1건**) |
| 7 | 이 판에서 호출한 ecc 에이전트·스킬 | **0 / 0** |
