# 52 — 몸 축 «민감도» 측정 (v3-73 · GARMENT_V1 · gray · 동기 굽기 · **측정 전용 · 정본 아님**)

v3-73. 브랜치 `v3-73-body-sensitivity`(사전 등록 · **이 CC 세션 전용**). 호출한 ecc 에이전트/스킬: **0 / 0**

## git 스탬프

```
 브랜치  v3-73-body-sensitivity     워크트리  /Users/banshk/projects/fit-simulator
 기점    accebf0 (v3-72 HEAD)       직전 회차  accebf0
 커밋    (§0 → §1 등재 → 집행 → §3 순서로 채운다)
```

---

## §0 판정 수령 — **집행 «전» 커밋**

### §0-1 ㉮″ Δ 판정 수령 + **체제 경계 «확장» 등재**

```
 **판정 수령**: v3-72 의 **max|Δ| 591.87mm = 「정의 차이」**(결함 아님).
 **★ 체제 경계 «확장»**(v3-71 §0-1 의 한 줄을 넓힌다):
   **「정본 3종 = `parseGlb` 몸(**T포즈**) + **캐시 BEXT**
     · v1 칸 **전량** = **구운 마네킹 몸**(**슬라이더 체계**) + **실측 BEXT**(도출 h · 도출 ③b)」**
 ⟹ 두 체제는 **몸의 «정의»부터 다르다**. 값을 가로질러 비교하지 않는다.
```

### §0-2 잔여 결손 처분 — **절차 등재**(코드 0)

```
 **「개시 시퀀스」** = ① **가시화 계기 1회**(스크린샷 1장 ⟹ ResizeObserver 발화 ⟹ **R3F 루트 생성**)
   → ② **루트 생성 확인**(㉠ 판별 3값) → ③ **동기 굽기 N회**(`stepFrames` · rAF 무관).
 **코드 변경 0** — v3-72 가 만든 경로를 **절차로 고정**하는 것이 이 등재다.
```

### §0-3 Q 답 등재 (사용자 소유분 수령)

```
 **Q2′ 굽기 예산 = 12시간**(벽시계 · 누적).
 **Q3′ `GARMENT_V1`** = { **L 0.71** · **W 0.51** · **SW 0.44** · **SLEN 0.20** · **ARM_G 제도 기본값** }
   **SLEN 도출식 등재**: **화장 42cm − SW/2 = 42 − 22 = 20cm ⟹ 0.20 m**
     (화장 = 목 옆점~소매 끝 · SW/2 = 어깨너비의 반 = 어깨점까지의 거리 ⟹ 나머지가 소매길이)
   **ARM_G 한계 등재**: 사이즈 차트가 **암홀 둘레를 주지 않는다**(v3-69 결손 3 ㉡) ⟹
     **제도 기본값 `0.4439`(`dressRun.ts:53`)를 그대로 쓴다**. **차트에서 도출한 값이 아니다.**
 **소매통(`sleeveWidth`) = v1 «미사용» 확정**(#57 · v3-69 §2-3 재확인분과 일치).
 **Q4′ 원단 = `gray`** (k 69 · ρ 0.187 · B 2.3191698e-5 · `consts.ts:20`).
```

### §0-4 산출물 분류

```
 **「측정 «전용» · «정본» 아님」**(v3-70 §0-C 그대로) ⟹ **Node 채널 허용** ·
 20프레임 브라우저↔Node 비트 대조는 **정본 편입에만** 걸린다.
```

---

## §1 측정 설계 — **집행 «전» 등재**

### §1-1 기준 칸

```
 **기본 슬라이더 몸**(동기 굽기) × **GARMENT_V1** × **gray**.
 ★ **착장 캡처 front / side 2장** — **전략 세션 화면 판정용**(포즈·조립 확인). **CC 화면 판정 0.**
```

### §1-2 축 칸 — **끝값 창작 0**

축별 **단독** 변경 · 양끝 = **v2 슬라이더 등재 min/max**(`src/components/Controls.tsx:128-137` 인용).

| 축 | 기본 | **min** | **max** | 출처 |
|---|---|---|---|---|
| 가슴둘레 | 100 | **70** | **140** | `Controls.tsx:129` |
| 어깨너비(몸) | 45 | **35** | **55** | `Controls.tsx:134-137` |
| 키 | 170 | **140** | **200** | `Controls.tsx:128` |
| 팔 길이 | 60 | **40** | **80** | `Controls.tsx:130` |
| 다리 길이 | 85 | **60** | **110** | `Controls.tsx:131` |

### §1-3 실행 순서 = **예산 «절단» 순서**

```
 기준 → 가슴둘레± → 어깨너비± → 키± → 팔길이± → 다리길이±
 **누적 12시간 도달 시 그 지점에서 «절단»** · **미실행 칸은 «미실행»으로 등재**(추정 0).
```

### §1-4 칸 절차

```
 동기 굽기 → `bodyVerts` 주입 → **실측 BEXT 체제** → 정착 →
 게이트 **③a · ③b · crossings · settleNet**(**문턱 변경 0** · **③b = «그 칸» h × 25%**)
```

### §1-5 게이트 «실패» 칸 처분

```
 **실패도 그 축의 «데이터»다** — 실패 사실 · 게이트 · 값을 등재하고 **순회를 계속한다**(**정지 아님**).
```

### §2 판정 채널 (**새 채널 0**)

```
 **「가른다」의 정의** = **5행 핏 표에서 «어느 행이든» 국면(눌림 / 밀착 / 여유)이 기준 칸 대비 «바뀜»**.
 **값 변화(중앙 · p25~p75)는 «전 행» 병기** — **판정과 분리**한다.
 **보조 채널**(사실 등재만 · 판정 아님): **cov · O4 · 게이트 여유**.
```

### §3·§4

```
 **§3 등재** `docs/v3/52`(이 노트) — **축 × 칸 판정표**(가른다 / 안 가른다 / 실패 / **미실행**) ·
   **칸별 도출값** · **벽시계 «실측»**.
```

| 갈래 | 조건 | 귀결 |
|---|---|---|
| **A** | 순회 완료(**전량 또는 예산 절단**) · 판정표 + 기준 칸 캡처 산출 | **판정표·캡처 상신** |
| **B** | **동기 굽기·주입 3칸 «연속» 불능** | 정지 · 사실만 |
| **E** | 판정 불가 | 정지 · 사실만 |

### 이 판에서 바꾸지 않는 것

**`solver` / `garmentScene` / `s4Gate` diff 0** · **문턱·정본 0** · **기존 정본 3종 불변** ·
**제품 화면 0줄** · **v2 코드 무삭제** · **`Mannequin` 로직 0줄**.

### 순서

```
 §0 커밋 → §1 등재 커밋 → 기준 칸(캡처 포함) → 순회 → §2 판정 → §3 → §4 상신
```

---

## §2′ 집행 — ★★ **판정 채널이 이 «체제»에서 서지 않는다**

### §2′-1 계기 신설

```
 **신설** `scripts/v3Sensitivity.ts` — 한 «칸» 드라이버(Node · 측정 전용).
   구운 몸 정점 × `GARMENT_V1` × `gray` → **정착 → 게이트(③a · crossings · settleNet · ringExcess)
   → 5행 핏 표**. **문턱 변경 0 · 새 채널 0** · ③b 는 **그 칸의 h × 25%** 를 인쇄해 대조 가능하게 둔다.
 **이 판의 코드 변경은 이 파일 «하나»뿐이다** — 제품 화면 0줄 · `Mannequin` 0줄 ·
   `solver` / `garmentScene` / `s4Gate` **diff 0**.
```

### §2′-2 ★★ **`deriveLevels` 가 «구운 몸»에서 산출 불가** — 두 칸이 **서로 다른 사유**로

| 몸 | `Y_TOP` | `AXIS_Z` | 높이 도출 |
|---|---|---|---|
| **원본 GLB**(T포즈 · 정본 체제) | 146.710cm | 1.605cm | `Y_LOW` 82.84 · `Y_ARM` 135.37 · **chestY 135.36** · **waistY 107.59cm** · 허리 격자점 **52개** |
| **구운 몸 가슴100**(A포즈 · v1 체제) | 137.993cm | 1.543cm | **산출 불가** — 「**허리 구간 격자점 1개** — 극값 판정 불가 · 갈래 B」 |
| **구운 몸 가슴110**(A포즈 · v1 체제) | 140.305cm | 1.830cm | **산출 불가** — 「**C 최소가 구간 «끝»에 붙는다**(내부 극값 없음) · 갈래 B」 |

```
 ⟹ **5행 핏 표가 «행 자체»를 못 낸다.** §2 가 등재한 판정 정의는
    「**어느 행이든 국면(눌림/밀착/여유)이 기준 칸 대비 바뀜**」인데 **비교할 행이 없다**.
 ⟹ **축 × 칸 «판정표»를 만들 수 없다.**
 **원인 방향은 «단정하지 않는다»** — 다만 두 사유가 모두 `bodyLevels` 의 **「성분 1 → >1 전이」**
    규칙(`bodyLevels.ts:113-130`)에 걸려 있고, **A포즈에서는 팔이 몸통에 가까워진다**는 사실이
    같은 자리에 있다(v3-72 §1-3: BEXT x **1.7796 → 1.0890**). **기전 귀속 0 · 처방 0.**
 ★ **체제 경계(§0-1)의 «두 번째 귀결»** — 몸의 정의가 달라지면 **몸에서 도출되는 «판정 채널»도
    같이 갈린다**. 첫 귀결(h · ③b 문턱)은 v3-72 가 값으로 확인했고, **이것이 그 확장분**이다.
```

### §2′-3 기준 칸 — 게이트 «중간» 값(정착 미도달 · 진행 중)

```
 조립 정점 **10458** · 삼각형 **20120** · 준비 **2.3s**
 도출 — BEXT [1.088971, 1.700000, 0.271880]m · **h 3.260478mm** · band 7.520955mm ·
        **③b 문턱 0.815119mm** · Y_TOP 137.9934cm · AXIS_Z 1.5426cm · **목선둘레 46.9149cm**
 실제 진행 **50프레임**(청크 10 × 5 · 472.9s) — 값은 **실측 그대로**:
   프레임  관통[mm]  교차  순변위[mm]   ringExcess   링[cm] / 허용[cm]
    10     0.2060    0    211.4348     1.0479       95.17 / 90.83
    20     0.2148    0    205.3820     1.0150       93.30 / 91.92
    30     0.2379    0    193.8700     1.0030       93.40 / 93.11
    40     0.2476    0    169.6157     1.0173       93.51 / 91.92
    50     0.2013    0    159.1520     1.0287       93.76 / 91.14
 ⟹ **③a 관통은 0.20~0.25mm 로 문턱(0.5mm) «안»** · **교차 0(전 구간)** ·
    **settleNet 211 → 159mm 로 «감소 중»**(정착 진행) ·
    **ringExcess 는 1.003~1.048 사이에서 «진동»하며 문턱 1 을 넘는다**(50프레임 내 미해소).
 **정착 미도달 · 순회 정지로 «중단»**(예산 소진이 아니다 — §3′ 사유).
```

### §2′-3a ★ 계기 정정 1건 (같은 판에서 값으로 잡았다 · 은폐 0)

```
 1차 드라이버는 청크마다 **`f=10` 에 고정**돼 찍혔다 — `runFrames` 의 반환 `frame` 은
 **그 호출이 돈 «개수»**이지 누적 절대 프레임이 아니고(`frames` 인자가 개수 · `startFrame` 은 램프 전용),
 1차 구현이 `frame = r.frame` 이었기 때문이다. **물리는 정상으로 돌았고**(위 표의 값이 계속 갈린다)
 **프레임 «번호»만 안 늘었다**. `frame += step` 으로 고쳤다. **위 표의 프레임 열은 정정 후 기준이다.**
```

```
 ★ **`GARMENT_V1` × 이 몸의 사실 1건** — **목선 초과비가 «시작부터» 문턱을 넘는다**.
   `SW 0.44` · 목선둘레 **46.9149cm** 인 몸에 링이 **95.17cm** 로 벌어져 있다(허용 90.83cm).
   **처방 0 · 귀책 0** — §1-5 대로 **그 칸의 «데이터»로 등재**한다.
```

### §2′-4 ★ 벽시계 «실측» — 예산 산정의 입력

```
 **10프레임 = 93.6초 ⟹ 약 9.4 s/frame**(Node · gray d9 · 조립 정점 10458 · 이 기계 · 단독 아님).
 **정착에 몇 프레임이 필요한지는 «이 칸에서» 아직 모른다** ⟹ **칸당 시간을 «산정하지 않는다»**(#81).
 **대조**(체제가 다르므로 «가로질러 비교하지 않는다»): 정본 3종은 **브라우저** 61~66분/원단
   (v3-49 부록 §5 · DEFAULT_GARMENT · T포즈 몸).
```

---

## §3′ 순회 — **미실행**(추정 0)

| 순서 | 칸 | 상태 |
|---|---|---|
| 1 | **기준**(기본 슬라이더 · GARMENT_V1 · gray) | **부분 실행 50프레임** — 도출값·게이트 5점 등재 · **정착 미도달 · 중단** |
| 2~3 | 가슴둘레 **70** / **140** | **미실행** |
| 4~5 | 어깨너비 **35** / **55** | **미실행** |
| 6~7 | 키 **140** / **200** | **미실행** |
| 8~9 | 팔길이 **40** / **80** | **미실행** |
| 10~11 | 다리길이 **60** / **110** | **미실행** |

```
 **미실행 사유**: §2′-2 — **판정 채널이 서지 않아 순회를 «열어도» 판정표가 나오지 않는다**.
   칸을 더 굽는 것은 **게이트 값만** 쌓고 **「가른다」를 못 가린다**.
 **예산 절단이 아니다** — 12시간을 쓰기 «전»에 **채널 부재**로 막혔다. **누적 벽시계 «미소진»**.
 **기준 칸 착장 캡처 2장 = 미산출** — 정착이 미도달이고, 정착 blob 을 화면에 싣는 배선
   (`SETTLED` 항목에 `bodyVerts` · `garment` 를 함께 싣는 경로)이 **아직 없다**.
   **이 판에서 만들지 않았다** — 갈래 E 로 정지하므로 **배선을 늘리지 않는다**.
```

---

## §4 판정 — **갈래 E**(판정 불가 · 정지 · 사실만)

| 사전 등록 조건 | 실측 | 충족 |
|---|---|---|
| **A** 순회 완료(전량 또는 예산 절단) · **판정표** + 기준 칸 캡처 | **판정표 «생성 불가»**(§2′-2) · 캡처 미산출 | ❌ |
| **B** 동기 굽기·주입 **3칸 «연속» 불능** | 굽기·주입·**조립 모두 성공**(정점 10458 · throw 0) | ❌ |
| **E** 판정 불가 | **판정 채널 부재** | ✅ |

```
 **갈래 A 불성립** — 순회를 완주해도 **판정표가 정의되지 않는다**.
 **갈래 B 불성립** — 굽기·주입은 **된다**(불능이 아니다).
 ⟹ **E · 정지 · 사실만.**
```

**★ 상신하는 «사실» 4건**

```
 ① ★★ **`deriveLevels` 가 v1 체제(구운 A포즈 몸)에서 산출 불가** — 두 몸이 **서로 다른 사유**로
    걸린다(격자점 1개 / 내부 극값 없음). ⟹ **5행 핏 표 = 판정 채널이 «없다»**.
    **체제 경계의 두 번째 귀결**이다(첫 귀결 = h·③b 문턱 · v3-72).
 ② **`GARMENT_V1` × 기준 몸에서 목선 초과비가 시작부터 실패**(1.0479 > 1 · 링 95.17 > 허용 90.83cm).
    ③a 관통은 **0.2060mm 로 문턱 안**이고 **교차 0**이다. **처방 0 · 그 칸의 데이터.**
 ③ **벽시계 실측 9.4 s/frame**(Node · 조립 정점 10458). **칸당 시간 산정 0**(#81) ·
    **12시간 예산은 «미소진»**(채널 부재로 앞에서 막혔다 · 실제 소진 **약 8분**).
 ④ **기준 칸 캡처 미산출** — 배선 부재(`SETTLED` 에 `bodyVerts`·`garment` 를 싣는 경로).
    **정지 판단이라 배선을 늘리지 않았다.**
```

**다음 판이 먼저 풀어야 하는 것**: **판정 채널**. 선택지는 이 판의 정의역 «밖»이므로 **제안하지 않는다** —
다만 값으로 말할 수 있는 것은 **「`bodyLevels` 의 성분 전이 규칙이 A포즈 몸을 전제하지 않았다」**는
사실 하나다(원본 T포즈에서는 **격자점 52개**로 넉넉히 선다). **CC 화면 판정 0.**
