# v3-13 — #25 실제 몸을 충돌체로 (S4 유일 선행) (2026-08-18)

> **git 스탬프** — 브랜치 `v2-stage2a` · 워크트리 `/Users/banshk/projects/fit-simulator`
> · 기점 커밋 `3827276` (v3-12 metrics 추기) · 커밋 해시는 이 파일이 담긴 커밋.
> **호출한 ecc 에이전트·스킬: 0.**
>
> **갈래 판정 = C (주) + I (부수)**
> - **C**: ④에서 **관통이 0이 아니다** — 구 0.0767mm · 원기둥 0.0383mm(평면 0).
>   **표현이 물리를 바꿨다.** 다만 «원인이 특정»됐다: 삼선형 보간의 곡률 오차와
>   두 자리까지 일치한다(예측 h²/4R = 0.078 · h²/8R = 0.039 · 평면 0).
> - **I(부수)**: 대역이 **곡률로 갈린다** — 평면은 «정확»(0.000000mm)하고 곡면만 어긋난다.
> - **#25는 해소되지 않았다.** S4 착수 조건 미충족 ⟹ 정지.
>
> **얻은 것**: 원본 **수밀 확정**(χ=2) · 부호 **불일치 0.000%**(v1 2.95% 대비) ·
> 굽기 **294ms** · 질의 **175ns** · **마찰은 완전 보존**(tanθ_c/μ 1.0010/1.0003/1.0000).
>
> 진입: `npm run v3:body` · `SDFPATH=1 SKIP4=1 npm run v3:s3`.

---

## §1 원본 수밀성 — **직접 쟀다** (v3-10 §1-2는 인용이었다)

`public/models/mannequin.glb`를 **glTF 2.0 규격대로 직접 파싱**했다.
**데이터 읽기이지 코드 임포트가 아니다** — `src/lib/meshCollision.ts`를 부르지 않는다.
그 파일의 `excludeArms`(53줄)·`splitFrontBack`(113줄)은 구멍을 «내는» 쪽이고,
이 판이 재려는 것은 그것을 **거치기 전**의 원본이다.

컨테이너: `extensionsRequired=[]`(압축 없음) · 메시 1 · 프리미티브 1 · 정점 15882 · 삼각형 28880.

```
 기준                     정점    삼각형   열린엣지  비다양체  와인딩불일치  연결성분  퇴화삼각
 원시 인덱스             15882   28880      2774        0          0        61        0
 위치 용접(비트 동일)     14442   28880         0        0          0         1        0
 위치 용접(1µm)          14442   28880         0        0          0         1        0

 오일러 지표: V 14442 − E 43320 + F 28880 = χ = 2   ⟹  종수 g = 0
 엣지 길이[mm]: 최소 1.06 · p05 2.86 · 중앙 12.43 · p95 20.94 · 최대 40.34
 bbox[m]: x −0.890~0.890 · y 0.004~1.769 · z −0.114~0.168
```

**⟹ 원본은 수밀이다. 갈래 D 불성립.** 부호가 **원리적으로** 정의된다.

> **계기 정의역이 결론을 뒤집는 자리였다.** 원시 인덱스로 보면 열린 엣지 2774 · 연결 성분
> 61로 **「비수밀」로 읽힌다.** 그것은 glTF가 UV·법선 이음매에서 «같은 위치의 정점»을
> 쪼개 저장하기 때문이고, 기하가 아니라 저장 형식의 성질이다. **위치 용접 기준을 함께
> 내지 않았으면 정반대 결론이 나왔다.** χ=2는 그것과 «독립»인 확인이다 — 손잡이·터널이
> 있으면 열린 엣지가 0이어도 χ < 2가 된다.

**구멍은 v2가 낸 것이다**: 원본이 χ=2인데 v3-10이 등재한 597/189는 `splitFrontBack`·
`excludeArms`의 산출물이다. ⟹ **v3는 그 두 함수를 거치지 않으면 비수밀 문제가 없다.**

---

## §2 설계 결정 5건

### 1. 표현 — **복셀 SDF** (메시 질의 아님)

S3의 해석 충돌체는 `sdf(c,x,y,z,out) → 부호 있는 거리` **하나**의 인터페이스로 들어온다
(`solver.ts` `Collider`/`sdf`). 복셀 격자는 그것을 그대로 만족하면서 질의가 O(1)이고,
무엇보다 **부호 문제를 「굽는 단계 한 번」으로 국한한다.** 메시 질의로 가면 매 질의마다
부호를 정해야 하고(최근접 면 법선은 모서리·오목부에서 뒤집힌다 — v1 Stage 1a 부호 모순
2.95%), 서브스텝이 **1211**인 이 솔버에서 그 비용과 위험이 «매 질의»로 곱해진다.

### 2. 격자 간격 — **도출**(손 상수 0)

```
band   = thickness + 2h          (접촉 판정 띠 + 삼선형 스텐실)
margin = band + 2h
n_k    = floor((L_k + 2·margin)/h) + 1
메모리 = 4 bytes × nx·ny·nz  ≤  예산       ⟹  h는 이를 만족하는 «가장 작은» 값
```
패딩이 h에 의존해 닫힌 형식이 아니므로 이분 탐색으로 푼다 — 값은 여전히
**예산·두께·몸 크기에서만** 나온다.

**예산의 근거**: 이미 상주하는 GLB 자산이 **36.8 MB**다. 그것이 근사하는 대상보다 더 큰
SDF는 정당화되지 않으므로 **그 위의 2의 거듭제곱 = 64 MB**를 상한으로 둔다.

```
⟹ h = 3.951 mm · band = 8.902 mm · 격자 459×456×80 = 1.674e7 복셀 · 63.9 MB
```

**h ≤ 두께는 원리적으로 불가능하다** — 이 몸 bbox(0.886 m³)에서 h=1mm면 **3.30 GB**다.

```
 h[mm]     복셀수      메모리
  1.00   8.860e+08   3379.7 MB   ← 불가능
  2.00   1.107e+08    422.5 MB
  3.95   1.674e+07     63.9 MB   ← 채택
  8.00   1.730e+06      6.6 MB
```

**그러므로 h > 두께를 받아들이고 «오차를 정량화»한다.** 삼선형 보간의 주 오차항은
곡률에서 온다: `ε ≈ h²/(4R)`.

```
 h=3.951mm  R=40mm → 0.098mm (두께의 9.8%)   R=20mm → 0.195mm   R=10mm → 0.390mm
```
티셔츠의 접촉 대역은 몸통·어깨·상완(R ≳ 40mm)이다. 손가락·코(R≈10mm)는 접촉 대역이 아니다.

### 3. 부호 결정 — **레이 패리티**

§1이 원본을 **수밀**로 실측했으므로(χ=2 · 종수 0) 패리티가 **원리적으로 정확**하다 —
근사가 아니다. 최근접 면 법선(모서리에서 뒤집힘)도 winding number(전 삼각형 순회로
가장 비쌈)도 필요 없다. v3-10 §1-1 표의 후보 3종 중 **레이 패리티의 실패 조건(비수밀)이
이 입력에서 성립하지 않는다**는 것이 §1의 결과다.

**이 몸에서의 실패 위험**: 격자 노드가 메시의 정점·엣지에 «정확히» 얹히면 교차가 두 번
세어져 패리티가 뒤집힌다. 측도 0이지만 격자는 규칙적이라 무시할 수 없다 ⟹ §3-③이
독립 방법과 교차 검증한다.

### 4. 몸이 바뀌면 — **재굽기 비용만 잰다**(캐시 정책 미정)

§3-⑤에서 굽기 시간을 잰다. **이 판에서 캐시 정책은 정하지 않는다**(프롬프트 조항).

### 5. 격자 밖 — **「밖(자유 공간)」으로 간주** · 외삽 0

`sampleSdf`가 격자 밖에서 `band`(양수)를 돌려준다 ⟹ `d ≥ 0`이라 해소가 **아무 일도
하지 않는다.** **흡착이 아니라 «충돌 없음»이므로 절대 조항에 저촉되지 않는다** —
`resolveCollisions`의 `if (d >= 0) continue;` 가드가 그것을 값으로 보장한다.

---

## §3 시험 6종

### ① 수밀성 — §1 원문. **수밀**(χ=2 · 종수 0 · 열린 0 · 비다양체 0 · 와인딩 0 · 성분 1)

### ② SDF 자기검사 — **PASS**  [문턱 = 격자 간격의 5% = 0.198mm · 실행 «전» 고정]

**문턱의 도출**: 주 오차항 `h²/(4R)`를 h에 대한 «비»로 쓰면 `h/(4R)`이고, 시험 반지름
R≥50mm에서 **1.88%**다. 삼각화 새기타와 띠 가장자리 효과에 **2.6배 여유**를 두어 5%.

```
 형상                          삼각형   표본   평균[mm]   p99[mm]  최대[mm]  최대/h   판정
 구 R=50mm                     25280  20000    0.0627    0.0920   0.1029   2.60%   PASS
 원기둥 R=40mm(무한 대역)         512  20000    0.0401    0.0630   0.0655   1.66%   PASS
 원기둥 뚜껑 모서리(참고·게이트 밖)  512  20000    0.0516    0.5457   1.1181  28.30%   참고
```

> **자기검사가 «시험 도구»의 결함 2건을 잡았다** — v3-12 승격 규범(「기댓값이 아니라
> 구현을 고칠 수 있는지부터 본다」)을 그대로 적용했다:
> - **구 메시가 비수밀**이었다(극을 «고리»로 두어 퇴화 삼각형 발생 · 열린 160 · **χ=161**).
>   패리티는 수밀에서만 정확하므로 시험 자체가 무효였다 ⟹ 극을 «한 점»으로 고쳐 χ=2.
> - **원기둥 뚜껑 둘이 역배향**이었다(와인딩 불일치 **256**) ⟹ 감는 순서를 고쳐 0.
>
> **정의역도 고쳤다**: 초판은 «뚜껑 있는» 원기둥을 재고 28.30%로 FAIL을 냈는데,
> **S3의 `Collider`는 «무한» 원기둥이라 뚜껑이 원래 없다.** 시험이 S3의 정의역 밖을
> 재고 있었다. 뚜껑 모서리 대역은 게이트에서 내리고 **참고로 남긴다** — 삼선형은
> 크레이스에서 O(h²)가 아니라 **O(h)**라 원리적으로 뭉개지고, 사람 몸에는 그런 예리한
> 볼록 모서리가 없다.

### ③ 부호 일관성 — **불일치 0.000%** (v1 Stage 1a 2.95% 대비)

독립 구현으로 교차 검증했다: **임의 방향 광선 3개의 다수결**을 전 삼각형(28880) 브루트
포스로 돌린다 — 격자도, x축도, 버킷도 쓰지 않는다(구운 경로와 «공유하는 코드가 없다»).

```
 표본 1200 · 불일치 0 (0.000%) · 표면 ±h 대역 표본 20 · 그중 불일치 0
 표면 ±h를 «뺀» 불일치 = 0 (0.000%)      ← v1 2.95%와 대조할 값
```

**§2-3이 등재한 실패 위험(격자 노드가 정점·엣지에 얹혀 패리티가 뒤집힘)이 이 몸·이
격자에서 «관측되지 않았다».**

### ④ S3 시험을 이 경로로 재실행 — **관통 0 실패 · 마찰 완전 보존** [갈래 C]

같은 장면·같은 문턱으로 `SDFPATH=1`만 켰다. 해석 충돌체를 **같은 해석 함수로 채운 격자**로
바꾸는 것 말고는 아무것도 바꾸지 않는다(메시→SDF 정확도는 ②가 «따로» 검증했다 —
두 오차원을 섞지 않는다). **관통·미끄러짐 판정은 «해석» 형상 기준**이다.

```
 형상    정점   sub    관통정점   최대깊이[mm]   예측 곡률오차[mm]   판정
   구    625  1211        1      0.076686      h²/(4R) = 0.078    FAIL
 원기둥   625  1211       24      0.038324      h²/(8R) = 0.039    FAIL
  평면    625  1211        0      0.000000               0         PASS

  μ     θ_c[°]   tanθ_c   tanθ_c/μ    오차    판정
 0.20   11.321   0.2002    1.0010   0.10%   PASS
 0.40   21.807   0.4001    1.0003   0.03%   PASS
 0.80   38.660   0.8000    1.0000   0.00%   PASS      ⟹ ② PASS (±10%)
```

**마찰은 «완전히» 보존된다 — 해석 경로와 소수점까지 같은 값이다.**

**관통은 0이 아니다. 그러나 원인이 특정됐다**: 실측 관통이 **예측 곡률 오차와 두 자리까지
일치**한다. 구는 두 주곡률이 모두 1/R이라 `h²/(4R)`, 원기둥은 하나뿐이라 그 절반
`h²/(8R)`, 평면은 곡률 0이라 **정확히 0**이다(선형장은 삼선형으로 정확히 재현된다).
⟹ **미지의 결함이 아니라 §2-2가 미리 정량화한 «그 오차»가 그대로 나타난 것이다.**

### ⑤ 비용 — 굽기 294ms · 63.9MB · 질의 175ns

```
 몸 SDF 굽기   294 ms   (삼각형 28880 · 격자 459×456×80 = 1.674e7 복셀)
 메모리        63.9 MB  (예산 64 MB 안)
 안쪽 복셀     988,094 (5.90%)
 질의          2e6회 351 ms = 175.4 ns/질의
```
**부분과 전체를 함께 찍었다**(v3-10 승격 규범): 굽기·메모리·질의를 따로 낸다.
**해석 SDF 대비 질의 비용**: 격자 질의는 값 1회 + 중심차분 법선 6회 = `sampleSdf` **7회**다.
④의 벽시계가 해석 경로보다 크게 늘어난 것이 그 대가다(정확한 배수는 이 판에서 안 쟀다 —
④는 정확도 판정이었고 시간 열을 등록하지 않았다. **추정치로 채우지 않는다**).

**재굽기**: 294ms는 치수 슬라이더 1회당 비용으로 **감당 가능하다**(v1이 불가로 판정한
5.3초와 18배 차이). 다만 **캐시 정책은 이 판에서 정하지 않는다**(§2-4).

### ⑥ S1·S2·S3·S3b 게이트 전량 회귀 — **전부 «같은 값»**

```
S1  ① 4.915219m 0.2083% · ② Δx=0.0196200032m 절대차 3.16e-9m · ③ 0.0000%
    ③′ substepsFor 산정=103 r=0.95250                              → PASS [갈래 A]
S2  ∇θ 12성분 최대상대오차 1.22e-9 · ① 정지 이면각 0.00e+0 rad(힌지 73) → [갈래 B 유지]
S3  ① 관통 0(구·원기둥·평면) · h·|v|max 0.0056/0.0008/0.0000 µm
    ② tanθ_c/μ 1.0010/1.0003/1.0000 · 오차 0.10%/0.03%/0.00%        → PASS
S3b G 6/6 · 초기 적법 6.0479mm · ① ON 교차 0 / OFF 대조 16
    ② 최소 거리 1.9909mm · 위반 0 · ⑤ selfStats 7칸 0 · 비트 동일     → PASS
```

**⟹ 갈래 F(기존 제약 오염) 불성립.** 솔버에 `Collider` 갈래를 «순수 추가»했고 기존
충돌체의 코드 경로는 한 줄도 바뀌지 않았다.

---

## §4 갈래 판정

| 갈래 | 성립? | 근거 |
|---|---|---|
| A ①~⑥ 통과 | **불성립** | ④의 관통이 0이 아니다 |
| B ③ 불일치율이 크다 | 불성립 | 0.000% (v1 2.95% 대비) |
| **C ④가 깨진다** | **성립(주)** | 구 0.0767mm · 원기둥 0.0383mm 관통. **표현이 물리를 바꿨다** |
| D 원본도 비수밀 | 불성립 | χ=2 · 종수 0 · 열린 엣지 0 |
| E ⑤ 비용 실행 불가 | 불성립 | 굽기 294ms · 63.9MB · 질의 175ns |
| F ⑥이 깨진다 | 불성립 | S1·S2·S3·S3b 전부 같은 값 |
| G ② 자기검사 실패 | 불성립 | 6/6 PASS(시험 도구 결함 2건은 «구현»을 고쳐 통과 · 기댓값 무조정) |
| H 판정창 이탈 | **부분 성립(명시)** | 원기둥 «뚜껑 모서리» 28.30%는 S3 정의역 밖이라 게이트에서 내리고 참고로 남겼다 |
| **I 부위별로 갈린다** | **성립(부수)** | 대역이 **곡률**로 갈린다: 평면 0.000000(정확) · 원기둥 0.038 · 구 0.077 |
| J 판정 불가 | — | C로 적힌다 |

**주 판정 = C. #25는 해소되지 않았다 — S4 착수 조건 미충족 ⟹ 정지.**

---

## §5 회귀

```
v3 → v2 임포트 : 0건        v2 → v3 임포트 : 0건
(GLB는 «데이터»로 읽는다 — glTF 2.0 규격 파싱 · meshCollision.ts 호출 0)

git diff 경로
 M package.json          (+1줄: "v3:body")
 M src/v3/solver.ts      (**순수 추가** — Collider에 {kind:'grid'} 1갈래 + sdf() 분기)
?? src/v3/bodySdf.ts     신규 (deriveSpacing · bakeSdf · sampleSdf · gridFromFn)
?? scripts/v3Body.ts     신규 (§1 수밀성 · §2 도출 · ②③⑤)
 M scripts/v3S3.ts       (SDFPATH=1 경로 + signedDist의 grid 갈래 — 기본 경로 값 무변경)
```

**솔버를 고쳤으므로 «재실행»했다** — 결과는 §3-⑥에 있고 **전부 이전 판과 같은 값**이다.
`kind:'grid'`가 없는 기존 호출은 새 분기를 **한 줄도 밟지 않는다**(`if (c.kind === 'grid')`
가드가 함수 첫 줄이고, 기존 세 갈래의 코드는 이동 없이 그대로다).

**v1 게이트 4종 · v2 체커 반팔 B-3 18채널 — 재실행하지 않았다.** 대상 코드
(`src/lib/**`·`src/components/**`·`src/workers/**`) **0줄 변경** · 임포트 양방향 **0건**
⟹ v2 경로가 새 코드에 도달할 수 없다. `npx tsc -b` 통과 · `npm run build` 통과.

---

## §6 잔여 갱신

| # | 항목 | v3-12 | v3-13 |
|---|---|---|---|
| **25** | 메시 몸 부호 판정 | 신설 · S4 선행 | **부분 해소 · 미종결.** 원본 **수밀 확정**(χ=2 · 종수 0) ⟹ 부호는 «원리적으로» 정의되고 레이 패리티 **불일치 0.000%**(v1 2.95% 대비). **그러나 ④에서 관통이 0이 아니다**(구 0.077mm · 원기둥 0.038mm) ⟹ **S4 착수 조건 미충족** |
| **29** | **격자 SDF의 곡률 오차** *(신설 · #25에서 갈라짐)* | — | 관통 = 삼선형 곡률 오차 `h²/(4R)`(구)·`h²/(8R)`(원기둥)·0(평면)과 **두 자리 일치**. 원인 특정 완료. 후보 3종: (a) h 축소(메모리 — 0.077mm→10% 줄이려면 h를 √10배 줄여야 하고 메모리는 **31배**) (b) 두께에 곡률 보정 오프셋 (c) 고차 보간. **이 판은 고르지 않는다** |
| **15** | 비용 예산 | 입력 4건 | **입력 추가 5** — 몸 SDF 굽기 **294ms** · **63.9MB** · 질의 **175ns**(해석 대비 `sampleSdf` **7회** = 값 1 + 중심차분 6). 재굽기는 v1이 불가로 본 5.3초의 **1/18** |
| **24** | ④ 총 ms 계기 이상 | 「재현 불가 · 후보 2건」 | **종결(코드 무관).** 후보 ① 반증이 **독립 2회차 재현**(20.7/20.0/19.9 vs 20.5/19.8/19.8). 남은 후보 ②(측정 환경)는 **코드 밖이라 더 좁히지 않는다.** `v3-10 §3-④ 총 ms 무효 유지 · 주기 N 미정` · **#15는 나중에 새로 측정**한다. **잔여 목록에서 내린다** |
| 19·23·26·27·28 | — | — | 변화 없음 |

**신규 등재 2건 — 계기 규범**

> **① 위상 계기는 «용접 기준»을 함께 내지 않으면 정반대 결론을 낸다.** 이 판에서 원시
> 인덱스는 열린 엣지 **2774** · 연결 성분 **61**로 「비수밀」을 가리켰고, 위치 용접은
> **0 · 1**로 「수밀」을 가리켰다. 참인 쪽은 후자다(χ=2가 독립으로 확인). glTF는 UV·법선
> 이음매에서 같은 위치의 정점을 쪼개 저장하므로, 인덱스 기준 위상은 **저장 형식의
> 성질**이지 기하의 성질이 아니다.
>
> **② 자기검사가 실패하면 «시험 도구»와 «정의역»을 먼저 본다.** 이 판의 ② 초판 FAIL은
> 세 겹이었다 — 시험 메시가 비수밀(구 χ=161) · 시험 메시가 역배향(원기둥 256) ·
> **시험 형상이 S3의 정의역 밖**(뚜껑 있는 원기둥 vs S3의 «무한» 원기둥). 셋 다 고치자
> 통과했다. 기댓값은 한 번도 건드리지 않았다.

---

## §7 다음 수 (제안 — 판단은 전략 세션)

1. **#29를 먼저 닫는다** — S4는 «옷이 몸에 걸리는가»를 묻는데, 0.077mm 관통이 그 판정을
   교란하는지부터 값으로 봐야 한다. 두께 1mm 대비 7.7%이므로 **교란하지 않을 수도 있다** —
   그러면 ④의 문턱(1µm)이 이 표현에 대해 과도한 것이고, **문턱을 표현의 정확도에서
   재도출**하는 것이 옳은 수다(결과에 맞춘 완화가 아니라 도출의 정정).
2. **또는 (b) 곡률 보정** — 접촉면을 `thickness + h²/(4R_local)`로 두면 원리적으로 상쇄된다.
   R_local은 SDF의 라플라시안에서 나온다(추가 저장 0).
3. **S4는 #29가 닫힌 뒤**. 물리 코어(S1·S2·S3·S3b)와 몸 부호(§3-③)는 이미 서 있다.

---

**호출한 ecc 에이전트·스킬: 0.**

## 출처

- 저장소 `public/models/mannequin.glb` — glTF 2.0 · 실측 대상 **[1차 · 로컬]**
- 저장소 `src/lib/meshCollision.ts:53,113` — `excludeArms`·`splitFrontBack`(구멍을 내는 쪽) **[1차 · 로컬]**
- 저장소 `docs/v3/10-S3충돌.md` §1-1·§1-2 — 부호 후보 3종과 실패 조건 · 「원본은 수밀」 인용 **[1차 · 로컬]**
- 저장소 `CLAUDE.md` Stage 1a 동결 — v1 부호 모순 2.95% · 복셀 8mm 굽기 5.3초 **[1차 · 로컬]**
- Ericson, *Real-Time Collision Detection* §5.1.5 — 점–삼각형 거리 **[2차 · 교재]**
- Möller–Trumbore 선분/광선–삼각형 교차 **[2차 · 교재]**
- 오일러 지표 χ = V − E + F, 닫힌 유향 곡면에서 χ = 2 − 2g **[2차 · 교재]**

---
---

# v3-14 — #29 문턱 재도출 + 실제 몸 검증 (2026-08-18)

> **git 스탬프** — 브랜치 `v2-stage2a` · 기점 `3fc6a80`(v3-13 metrics) · 커밋 해시는 이 파일이 담긴 커밋.
> **호출한 ecc 에이전트·스킬: 0.**
>
> **갈래 판정 = B + D (주 · 조건별 동시 성립) + F·I (부수)**
> - **B**: 해석 형상은 새 문턱을 **통과**(실측/예측 = 0.982)하는데 **실제 몸에서 벗어난다**(2.211).
> - **D**: 일치 조건 [0.5, 1.25]를 **위반** ⟹ **곡률 말고 «다른 오차원»이 더 있다.**
> - **F(부수)**: 뾰족 특징(R_eff < 20mm)이 **티셔츠 접촉 대역 안에 153개** 있다(정지 아님 · 한계 등재).
> - **I(부수)**: 대역이 **「매끈한 해석면」 vs 「면분할된 실제 메시」**로 갈린다.
> - **#25·#29 미종결. S4 착수 조건 미충족 ⟹ 정지.**
>
> **§2-② 크레비스 메움은 0건**(갈래 C 불성립) — 이것은 «얻은 것»이다.

---

## §1 문턱 재도출 — 유도에서 · v3-13 실측을 «보지 않고» 고정

**유도**: [0,h]에서 f의 선형 보간은 `L(t) − f(th) = (f''h²/2)·t(1−t)`이고 t=½에서 최대
`f''h²/8`이다(f''>0이면 보간이 «과대평가»). 삼선형은 축 3개의 1차 오차 합이므로 최악
셀 위상에서 `d̃ − d = (h²/8)·∇²d`. 표면에서 `∇²d = κ₁+κ₂` 이므로

```
P_pred = (h²/8)·(κ₁+κ₂)        구 h²/(4R) · 원기둥 h²/(8R) · 평면 0
```

천 정점은 `d̃ = 두께`인 자리에 앉으므로 **관통 = 보간 오차 그 자체**다. 즉 P_pred는 이
표현의 **원리적 하한**이고, **1µm 문턱은 해석 SDF(오차 0)에서 나온 값**이라 이 표현에
요구할 수 없는 정확도였다. 문턱 재도출은 «완화»가 아니라 **정의역 정정**이다.

**사전 등록**(순서: 아래를 «먼저» 고정하고 §3이 판정한다):

```
문턱      관통 ≤ P_pred × (1 + 0.25)          — 고정 상수 아님. 예측값의 함수다
여유근거  버린 다음 항이 P_pred 대비 O(h·κ) = h/R. h=3.951mm에서 h/R ≤ 0.20 ⟺ R ≥ 19.8mm
유효범위  R ≥ 20mm (더 뾰족한 특징은 §2-③ 소관)
일치조건  실측 / P_pred ∈ [0.5, 1.25]         — 「작다」가 아니라 「설명된다」
  상한 = 문턱과 같다(그 위는 설명되지 않는 초과분)
  하한 0.5 = 접촉 정점이 수백 개면 셀 위상 t_k가 조밀히 표집되므로 최댓값이 최악
            위상값의 절반에는 도달해야 한다. 훨씬 작으면 곡률 기전이 지배적이지
            않다는 뜻이고 「설명된다」를 주장할 근거가 없다(작다고 통과시키지 않는다)
평면      P_pred = 0 ⟹ 비가 정의되지 않는다 ⟹ 관통 = 0 을 «정확히» 요구
```

**미채택 등재(전략 세션 처분)**: (b) 곡률 보정 — `R_local`을 격자 라플라시안에서 뽑으면
잡음이 크고 오목부에서 부호가 뒤집혀 **천을 밖으로 미는 인위 항**이 된다. (a) h 축소 —
오차 1/10에 메모리 31배. 둘 다 이 판에서 **하지 않는다.**

---

## §2 실제 몸에서의 정량화

### ① 접촉 대역 곡률 분포 — 부위는 **높이 × |x| 로 «두 축» 모두** 가른다

> **계기 정의역 정정 2건**: 초판은 «높이»만으로 갈랐는데 **T포즈는 팔이 수평**이라
> 「가슴·등」 대역의 최소 R_eff가 4.5mm로 나왔다 — 그것은 가슴이 아니라 **손가락**이었다.
> |x|를 함께 넣자 갈렸다. 그 다음엔 「상완」의 최악이 y=0.007(**발목**)이었다 — |x|만으로는
> 벌어진 두 다리가 들어온다. 높이 하한(f < 0.5 ⟹ 다리)을 넣어 고쳤다.

```
 부위        정점   P_pred중앙[mm]  p95[mm]  최대[mm]  최소R_eff[mm]
 머리·목      674       0.0844    0.1581   0.3325       11.7
 어깨         548       0.0861    0.2528   0.4720        8.3
 가슴·등      721       0.0641    0.1966   0.5325        7.3
 상완         458       0.0986    0.1757   0.2298       17.0
 허리·배      744       0.0430    0.1075   0.1746       22.4
 엉덩이       821       0.0600    0.1941   0.3626       10.8
 전완·손     4441       0.2091    0.6181   0.8602        4.5
 다리·발     5466       0.0852    0.3289   0.9238        4.2

 티셔츠 대역(|x| ≤ 350mm · 0.55~0.92 H · 3136정점):
   P_pred 중앙 0.0630 · p95 0.1935 · 최대 0.5325 mm · 최소 R_eff 7.3mm
```

### ② 오목 크레비스 — **메움 0건** [갈래 C 불성립]

메시 정점에서 무작위 방향 U(0, 2h) 떨어진 점을 뽑아(표면 근방을 빠짐없이 덮고, 크레비스는
정점 밀도가 높아 오히려 더 촘촘히 표집된다) 격자와 **브루트포스 정확값**을 대조했다.

> **판정 기준을 먼저 고쳤다**: 초판은 「참=밖·격자=안」을 전부 «메움»으로 셌는데,
> 그 18건의 참 거리가 **11~39µm**였다 — 표면에 붙은 점의 부호 모호이지 크레비스가 아니다.
> 「메움」은 **참 거리가 보간 오차 규모를 넘을 때**로 정의해야 한다.

```
 표본 4000 · 부호 불일치 178 (4.450%) · 띠 안 크기오차 최대 1.7859mm

 부호 불일치를 «참 거리»로 가르면:
   0.00~0.99mm :  177 / 1491  (11.87%)
   0.99~1.98mm :    1 /  858  ( 0.12%)
   1.98~3.95mm :    0 / 1051  ( 0.00%)
   3.95~7.90mm :    0 /  600  ( 0.00%)

 「참=밖·격자=안」 30건 · 참 거리 최대 1.0779mm  <  h/2 = 1.976mm
```

**부호 오차가 표면 «한 셀» 껍질 안에 갇혀 있다** — 1.98mm 밖에서는 0.00%다.
⟹ **격자가 메운 크레비스가 없다.** §2-②는 이 판이 «얻은 것»이다.

### ③ 뾰족 볼록 특징 — **티셔츠 대역 안에 153개** [갈래 F 성립]

```
 부위        R_eff<20mm 정점   최악 P_pred[mm]  R_eff[mm]   위치(x, y)
 전완·손           2402            0.8602        4.5    −0.875, 1.402
 다리·발            603            0.9238        4.2     0.204, 0.007
 어깨                72            0.4720        8.3     0.176, 1.421
 엉덩이              40            0.3626       10.8    −0.040, 0.983
 가슴·등             37            0.5325        7.3     0.176, 1.398
 머리·목             11            0.3325       11.7     0.085, 1.636
 상완                 6            0.2298       17.0     0.299, 1.426
 (전체 3171 / 15882 = 19.97%)

 티셔츠 대역 안 153개 · 최악 P_pred 0.5325mm (R_eff 7.3mm) @ x 0.176 y 1.398 (겨드랑이·어깨 접합)
```
**§1 문턱의 유효 범위(R ≥ 20mm) 밖이 접촉 대역에 존재한다** ⟹ 갈래 **F 성립**(정지 아님).

---

## §3 재판정

### ③-1 해석 형상 — **전부 PASS**(문턱 · 일치 조건 둘 다)

```
 형상        P_pred[mm]  문턱[mm]  실측[mm]  실측/예측   문턱  일치
 구 R=50       0.0781    0.0976   0.0767     0.982   PASS  PASS
 원기둥 R=50   0.0390    0.0488   0.0383     0.982   PASS  PASS
 평면          0         0(정확)  0.000000     —     PASS  PASS
 마찰 tanθ_c/μ = 1.0010 / 1.0003 / 1.0000 (오차 0.10 / 0.03 / 0.00%) — 재확인 PASS
```
**유도가 실측을 2% 안에서 맞힌다.** §1의 문턱이 우연히 통과시킨 것이 아니라
**예측이 실측과 일치**한다 — 「설명된다」가 성립한다.

### ③-2 실제 몸 SDF 낙하 — **FAIL(문턱 · 일치 조건 둘 다)** [갈래 B·D]

**장면(먼저 정의)**: 수평으로 뻗은 **상완**(x 0.30~0.40에서 y 1.373~1.457 · z −0.087~0.012 ·
반지름 ≈45mm)에 100×240mm 천을 «가로질러» 덮는다. 양쪽으로 늘어져 미끄러져 나갈 수 없고,
상완은 반팔 소매 접촉 대역이며 원기둥형이라 **S3 ①의 원기둥 시험을 실제 몸에서 반복**하는 셈이다.

> 장면을 두 번 고쳤다(둘 다 실측으로): 어깨 «위»에 얹은 판은 **미끄러져 떨어졌다**
> (접촉 0 · |v| 1765mm/s — 어깨 경사가 μ=0.3의 임계각보다 가파르다). 그 다음 상완 판은
> z 중심을 +0.02로 잡아 **팔 옆으로 흘렀다**(접촉 0) — 메시에서 팔의 실제 z 중심이
> −0.038임을 확인하고 맞췄다. **초기 적법성은 매번 확인**했다(정확한 메시 거리 최소 62.47mm).

```
 정점 225 · sub 236 · T 3s · |v|max 0.12mm/s(정착) · 접촉 정점 135
 관통 정점 69 · 최대 관통 0.3581mm
 P_pred(접촉점 최대) 0.1620mm · 문턱 0.2025mm       ⟹ 문턱 FAIL
 실측 / 예측 = 2.211  ∉ [0.5, 1.25]                 ⟹ 일치 FAIL
```

**해석 0.982 vs 실제 몸 2.211.** 같은 h·같은 코드·같은 문턱인데 **2.25배 갈린다.**

**차이의 크기와 부호**: 실측이 예측보다 **크다**(+0.196mm = 예측의 +121%). 부호가 «크다»
쪽이므로 **알려진 기전으로 설명되지 않는 초과분이 존재**한다.

**후보(단정 0)** — 해석면과 실제 메시의 차이는 «면분할»이다. 몸 메시는 엣지 중앙 12.43mm의
**평면 조각들**이고, 조각 경계마다 거리장에 **크레이스(기울기 불연속)**가 생긴다. 삼선형은
크레이스에서 O(h²)가 아니라 **O(h)**다 — v3-13이 뚜껑 있는 원기둥 모서리에서 **28.30% of h**로
이미 특성화한 바로 그 기전이다. 매끈한 해석 구·원기둥에는 크레이스가 없고 실제 메시에는
있다는 점이 **대역 갈림(갈래 I)의 방향과 일치**한다. **확인은 다음 판**(#30).

---

## §4 갈래 판정

| 갈래 | 성립? | 근거 |
|---|---|---|
| A 전부 통과 | 불성립 | ③-2가 문턱·일치 둘 다 FAIL |
| **B 해석은 통과·실제 몸에서 벗어남** | **성립(주)** | 0.982 vs 2.211 |
| C 크레비스 메움 | **불성립** | 「참=밖·격자=안」 참 거리 최대 1.0779mm < h/2 · 1.98mm 밖 0.00% |
| **D 일치 조건 위반** | **성립(주)** | 2.211 ∉ [0.5, 1.25] · 초과분 +121% |
| E 문턱이 유도에서 안 나온다 | 불성립 | 닫힌 형식으로 유도됐고 해석 형상에서 2% 안에 맞는다 |
| **F 뾰족 특징이 접촉 대역** | **성립(부수)** | 티셔츠 대역 안 153개 · 최악 R_eff 7.3mm(겨드랑이) |
| G 회귀 깨짐 | 불성립 | §5 — 전량 불변 |
| H 판정창 이탈 | 불성립 | 네 시험 모두 창 안 |
| **I 대역별로 갈린다** | **성립(부수)** | 「매끈한 해석면」 vs 「면분할된 실제 메시」로 갈린다 |
| J 판정 불가 | — | B·D로 적힌다 |

**주 판정 = B + D. #25·#29 미종결 ⟹ 정지.**

---

## §5 회귀

**솔버를 고치지 않았다** — v3-13 커밋 `ec8aac4` 이후 `src/v3/solver.ts`·`src/v3/bodySdf.ts`
**0줄 변경**(이 판의 변경은 `scripts/v3Body.ts` 계기 추가뿐). 가드 근거를 값으로:

```
git diff ec8aac4..HEAD --stat  ⟹  scripts/v3Body.ts 만
v3 → v2 임포트 0건 · v2 → v3 임포트 0건 (GLB는 «데이터»로 읽는다)
```
해석 형상 재실행(`SDFPATH=1`)이 v3-13과 **같은 값**(구 0.076686 · 원기둥 0.038324 · 평면 0 ·
마찰 1.0010/1.0003/1.0000)을 냈다 — 솔버 무변경을 «값으로» 확인한 것이다.
`npx tsc -b` 통과 · `npm run build` 통과.

---

## §6 잔여 갱신

| # | 항목 | v3-13 | v3-14 |
|---|---|---|---|
| **29** | 격자 SDF 곡률 오차 | 신설 · 후보 3종 | **문턱 재도출 완료**(P_pred×1.25 + 일치조건). **해석 형상에서 유도가 2% 안에 맞는다**(0.982). **그러나 실제 몸에서 2.211** ⟹ 미종결. (a)h축소·(b)곡률보정 **미채택 등재** |
| **30** | **면분할 크레이스 오차** *(신설)* | — | 실제 몸의 초과분 +121%의 **후보**. 몸 메시는 중앙 12.43mm 평면 조각이고 조각 경계마다 거리장에 크레이스가 생긴다. 삼선형은 크레이스에서 **O(h)**(v3-13이 28.30% of h로 특성화). **단정 0 · 확인은 다음 판** |
| **25** | 메시 몸 부호 | 부분 해소 | **변화 없음(미종결)**. 부호 자체는 여전히 건강하다 — §2-②가 **표면 한 셀 밖 부호 오차 0.00%**를 추가 확인 |
| **15** | 비용 예산 | 입력 5건 | 변화 없음 |
| 19·23·26·27·28 | — | — | 변화 없음 |

**신규 등재 1건 — 계기 규범**

> **「작다」와 「설명된다」는 다른 판정이다.** 이 판의 실제 몸 관통 0.358mm는 두께의 36%로
> «작다»고 넘길 수도 있었다. 그러나 **예측의 2.21배**라 알려진 기전으로 설명되지 않고,
> 설명되지 않는 초과분에는 **미지의 오차원이 숨는다**. 문턱을 「예측값의 함수 + 일치 구간」
> 으로 세우면 그 자리가 없어진다 — 완화가 아니라 **강화**다.

---

## §7 다음 수 (제안 — 판단은 전략 세션)

1. **#30(면분할 크레이스)을 값으로 가른다** — 같은 몸 메시를 **세분(subdivide)**해 조각을
   잘게 만들면 크레이스 각이 줄어든다. 세분 전후로 실측/예측 비가 2.211 → 1에 가까워지면
   후보 확정이다. 몸 메시를 바꾸지 않는 대조로는 **해석 구를 «면분할»해** 굽는 방법도 있다
   (매끈한 구 vs 12mm 면분할 구 — 이 판의 대조와 정확히 같은 축).
2. **F(뾰족 특징 153개)** 는 S4에 한계로 넘긴다 — 겨드랑이·어깨 접합의 R_eff 7.3mm는
   문턱 유효 범위 밖이고, 그 대역의 관통은 §1 문턱으로 판정하지 않는다.
3. **S4는 #29·#30이 닫힌 뒤.**

---

**호출한 ecc 에이전트·스킬: 0.**

---
---

# v3-15 — #30 면분할 크레이스 검증 (2026-08-18)

> **git 스탬프** — 브랜치 `v2-stage2a` · 기점 `e4d55d2`(v3-14 metrics). **ecc 에이전트·스킬 0.**
>
> **갈래 판정 = B (주) + I (부수)**
> - **§2가 가설을 지지한다**: 비가 엣지 길이와 함께 단조 증가(1.032 → 1.474 → 3.067 → 4.405)하고,
>   **e=2mm(0.5h)에서 1.032로 대조군 0.982를 재현**한다 ⟹ 갈래 C·D 불성립.
> - **§3은 여전히 벗어난다**(등록 통계 기준 실측/P_ext = **2.078**) ⟹ **B**.
> - **★ 진짜 소득**: **「θ의 어느 «통계»를 쓰는가」가 사전 등록되지 않았고, 그것이 판정을
>   2.5배 흔든다** — 접촉점에서 삼각형 3엣지의 «중앙» 0.84° vs «최대» 21.53°(팔 메시가
>   강한 비등방). 중앙이면 2.078(FAIL), 최대면 0.834(PASS). **#31로 신설.**
> - **#25·#29·#30 미종결 · 정지.**

## §1 크레이스 항 — 형태·계수를 실측 «전»에 등록

**유도**: 볼록 크레이스의 부채꼴(결손각 θ) 안에서 참 거리는 모서리까지 반경 r이고
`∇²r = 1/r`이다. 삼선형 오차 `(h²/8)∇²d`는 `r ~ h/2`에서 **≈ h/4로 포화**하며 θ와 무관하다.
다만 반경 r에서 부채꼴 폭이 `r·θ`이므로 θ가 작으면 셀의 «일부»만 부채꼴에 들고 오차가 그
분율만큼 준다 ⟹ **θ에 선형 · 1 rad 부근 포화**.

```
P_crease   = c·f(θ)·h,   f(θ) = min(1, θ/(π/2)),   c = 0.283   (v3-13 뚜껑 90° 실측 28.30% of h)
P_pred_ext = P_smooth + P_crease
유효 범위  e ≳ h  (e < h면 크레이스가 셀 밑으로 들어가 평균화된다 — §2의 대조점)
문턱·일치  v3-14 §1 등록값 «그대로»: ×(1+0.25) · 실측/예측 ∈ [0.5, 1.25]  (§2를 보고 고치지 않았다)
```

## §2 면분할 대조 — 같은 구(R=50mm)·엣지 길이«만» 바꾼다

```
 목표e[mm] 실제중앙e 삼각형  θ중앙[°] 수밀  관통[mm] /P_smooth  /P_ext
       2      1.99  24492    1.67   OK   0.0805    1.032    0.815
       6      6.04   2600    4.90   OK   0.1151    1.474    0.828
      12     12.05    624   10.39   OK   0.2394    3.067    1.156
      20     19.51    224   18.78   OK   0.3438    4.405    1.104
 대조군(면분할 0 · 해석 함수로 채운 격자) = 0.982      P_smooth = h²/(4R) = 0.0781mm
```
네 메시 전부 **수밀·배향 일치**(열린 0 · 비다양체 0 · 와인딩 0 · χ=2) — 초기 적법성도 매번 확인.

**판정**: 비가 엣지 길이와 함께 **단조 증가**한다 ⟹ **크레이스가 지배 기전**이다.
**e=2mm에서 1.032**로 대조군 0.982를 재현한다 ⟹ **대조가 성립**(갈래 D 불성립).
확장 항을 넣으면 4.4배 흩어져 있던 비가 **0.815~1.156**으로 모인다 — 형태가 대역을 가로질러 작동한다.

## §3 실제 몸 재판정 — 같은 장면(상완 · 100×240mm · 장면 변경 0)

```
 접촉점 θ 분포[°]  «삼각형 3엣지 중앙»: 중앙 0.84 · p95 2.62 · 최대 3.05
                   «삼각형 3엣지 최대»: 중앙 21.53 · p95 25.93 · 최대 26.54

 등록·구현된 통계(중앙)  θ 0.84° → P_crease 0.0104 · P_pred_ext 0.1724 · 문턱 0.2155
                        실측 0.3581 ⟹ 문턱 FAIL · 실측/P_ext = 2.078 ⟹ 일치 FAIL
 감도(최대)             θ 21.53° → P_pred_ext 0.4294 · 실측/P_ext = 0.834 ⟹ 둘 다 PASS일 값
```
**주 판정은 등록·구현된 «중앙»으로 낸다 ⟹ §3 FAIL.** 감도는 «참고»로만 적는다 —
결과를 보고 통계를 바꾸는 것이 금지 조항이다.

**왜 갈리는가**: 팔 메시가 **비등방**이다(팔 축 방향으로 긴 삼각형). 축과 나란한 엣지의
이면각은 거의 0이고 원주 방향 엣지만 크다 ⟹ 3엣지의 «중앙»은 평평한 쪽을 집는다.
§2의 구 메시는 등방이라 중앙과 최대가 가깝고, 그래서 §2에서는 이 자유도가 드러나지 않았다.

## §4 갈래 판정

| 갈래 | 성립? | 근거 |
|---|---|---|
| A 전부 통과 | 불성립 | §3이 등록 통계로 FAIL |
| **B §2 지지 · §3 벗어남** | **성립(주)** | §2 단조 증가 + e=2mm 1.032 · §3 2.078 |
| C 비가 엣지와 무관 | 불성립 | 1.032 → 4.405 단조 |
| D e=2mm가 대조군 재현 실패 | 불성립 | 1.032 vs 0.982 |
| E c·f를 유도에서 못 정함 | 불성립 | 부채꼴 폭 논증으로 유도·등록 |
| F 시험 구 메시 결함 | 불성립 | 4종 전부 χ=2 · 와인딩 0 |
| G 회귀 깨짐 | 불성립 | §5 |
| H 판정창 이탈 | 불성립 | — |
| **I 엣지 대역별로 갈린다** | **성립(부수)** | e < h(평균화)와 e ≳ h(크레이스 지배)가 다른 체제다 |
| J 판정 불가 | — | B로 적힌다 |

**주 판정 = B. 남은 초과분: 실측 0.3581 − P_ext(중앙) 0.1724 = +0.1857mm.**

## §5 회귀

**솔버 0줄 변경** — v3-13 `ec8aac4` 이후 `src/v3/**` 무변경(이 판도 `scripts/v3Body.ts` 계기만).
`git diff ec8aac4..HEAD --stat` ⟹ `scripts/v3Body.ts` 단독. v3↔v2 임포트 양방향 0건.
해석 형상 재실행이 v3-14와 같은 값(구 0.076686 · 원기둥 0.038324 · 평면 0 · 마찰
1.0010/1.0003/1.0000)을 냈다 — 솔버 무변경을 값으로 확인. `tsc -b` · `build` 통과.

## §6 잔여 갱신

| # | 항목 | v3-14 | v3-15 |
|---|---|---|---|
| **30** | 면분할 크레이스 | 신설 · 단정 0 | **지배 기전으로 «지지»됨**(§2 단조 + e=2mm 대조 재현 + 확장항이 4.4배→0.82~1.16으로 수렴). **다만 실제 몸은 미종결** |
| **31** | **θ의 «통계» 미등록** *(신설)* | — | 접촉점 삼각형 3엣지의 중앙(0.84°) vs 최대(21.53°)가 **25배** 다르고 판정을 2.5배 흔든다(2.078 vs 0.834). **비등방 메시에서만 드러난다** — 등방 구에서는 안 보였다. **다음 판이 «유도»로 등록**해야 한다(셀이 straddle하는 크레이스가 무엇인가) |
| **29·25** | — | 미종결 | **변화 없음(미종결)** · S4 착수 조건 미충족 |
| 15·19·23·26·27·28 | — | — | 변화 없음 |

**신규 등재 1건 — 계기 규범**

> **모형의 «입력 통계»도 사전 등록 대상이다.** 이 판은 형태(f)와 계수(c)를 등록했으나
> **θ를 어느 통계로 뽑는지**는 등록하지 않았고, 그 한 자유도가 판정을 2.5배 흔들었다.
> 등방 시험체(구)에서는 중앙≈최대라 드러나지 않고 **비등방 실물에서만** 드러난다 ⟹
> **시험체가 등방이면 통계 자유도를 검출하지 못한다.**

## §7 다음 수 (제안 — 판단은 전략 세션)

1. **#31을 유도로 닫는다** — 셀 하나가 straddle하는 크레이스는 «그 셀 안의» 엣지들이고,
   오차는 그중 가장 가파른 것이 지배한다는 논증이 자연스럽다. 그렇다면 통계는 «최대»여야
   하고, 그 경우 §3은 0.834로 통과한다. **그러나 유도를 먼저 쓰고 그 다음 판정한다.**
2. **비등방 시험체를 §2에 추가**한다 — 축방향으로 늘린 타원체/원기둥을 등방 구와 나란히
   두면 통계 자유도가 대조로 드러난다(이 판에서 구만 썼기에 놓쳤다).
3. **S4는 #29·#30·#31이 닫힌 뒤.**

**호출한 ecc 에이전트·스킬: 0.**

---
---

# v3-16 — #31 θ 통계 확정 + 최종 재판정 (2026-08-18)

> **git 스탬프** — 브랜치 `v2-stage2a` · 기점 `30bac10`(v3-15 metrics). **ecc 에이전트·스킬 0.**
>
> **갈래 판정 = A. #31 · #30 · #29 · #25 «전부 종결» · S4 착수 가능.**
> §2 안전장치 통과(구 계열 전 대역 [0.5,1.25]) + §3 실제 몸 **0.766** 통과.

## §1 통계 정의 — 구조에서 도출하고 «먼저» 등록

문턱이 비교하는 것은 **최대 관통**이고 매끈한 항 `P_smooth`는 이미 «점별 → 최대»였다.
크레이스 항만 «전역 중앙»이어서 **두 항의 통계가 어긋난 채** 최댓값과 비교되고 있었다.
⟹ **완화가 아니라 통계 불일치의 정정.**

```
점별:  P_pred_ext(i) = P_smooth(i) + c·f(θ_i)·h
근방:  θ_i = 점 i 에서 «h 이내»의 메시 엣지  — 삼선형 스텐실이 한 변 h이므로
       그 안의 기울기 불연속만 재구성에 관여한다
축약:  **최대**. 크레이스가 여러 개 겹쳐도 셀의 선형 재구성 편차는 h/4로 «포화»하므로
       합이 아니라 «가장 가파른 하나»가 지배한다
c·f·문턱·일치 구간: v3-15 등록분 «그대로»(c=0.283 · f=min(1,θ/(π/2)) · ×1.25 · [0.5,1.25])
```

## §2 안전장치 — 새 통계를 v3-15 구 계열에 그대로 적용

구 계열은 **재실행했다**(v3-15가 접촉점 데이터를 저장하지 않았다 — 그 사실을 적는다).

```
 목표e[mm]  실제중앙e  θ중앙[°]  관통[mm]  /P_smooth   /P_ext(전역중앙)  /P_ext(점별최대·신)
       2       1.99     1.67    0.0805     1.032           0.815              0.753
       6       6.04     4.90    0.1151     1.474           0.828              0.620
      12      12.05    10.39    0.2394     3.067           1.156              0.732
      20      19.51    18.78    0.3438     4.405           1.104              0.669
```
- **e=2mm가 대조군(0.982) 근방** ✓ · **전 대역이 일치 구간 [0.5,1.25] 안** ✓ · **단조성 유지** ✓
- 원 신호(/P_smooth 1.032→4.405 단조)는 그대로 보존된다 — 새 통계가 기전을 지우지 않았다.
- 비가 0.62~0.75로 «1보다 작은» 쪽에 모인다 = 모형이 약간 **보수적**(과대예측)이다.
  천이 항상 최악 셀 위상에 앉지는 않으므로 예상되는 방향이고, 등록 구간 안이다.

**⟹ 안전장치 통과. 이 정의는 결과에 맞춘 선택이 아니다**(구 계열을 깨지 않았다).

## §3 실제 몸 최종 재판정 — 같은 장면(상완 100×240mm · **장면 변경 0**)

```
 점별 P_ext[mm]  중앙 0.2248 · p95 0.4144 · 최대 0.4675   (접촉 표본 135)
 최대 관통 0.3581mm · 최대 관통이 난 점의 θ_i = 26.01°
 문턱 0.5844mm ⟹ PASS      실측/P_ext = 0.766 ∈ [0.5, 1.25] ⟹ PASS(설명된다)
 정착 |v|max 0.12mm/s · 관통 정점 69 · 초기 적법 62.47mm
```
**최댓값 하나에 의존하는 판정의 대표성**: 점별 P_ext의 p95(0.4144)가 최대(0.4675)에 가깝고
최대 관통점의 θ_i = 26.01°가 접촉 대역의 상위 대역에 정상적으로 놓인다 — 특이점 하나가
판정을 끌고 간 것이 아니다.

## §4 갈래 판정

| 갈래 | 성립? | 근거 |
|---|---|---|
| **A 안전장치 + §3 둘 다 통과** | **성립(주)** | §2 전 대역 [0.5,1.25]·단조 보존 · §3 0.766 |
| B §2 통과·§3 벗어남 | 불성립 | §3 PASS |
| C §2가 깨진다 | 불성립 | e=2mm 대조군 근방 · 전 대역 구간 안 |
| D 유도에서 못 정함 | 불성립 | 스텐실 폭(근방) + h/4 포화(축약)로 도출 |
| E 회귀 깨짐 | 불성립 | §6 |
| F 판정창 이탈 | 불성립 | — |
| G 대역별 갈림 | 불성립 | 구 4대역 + 실제 몸이 같은 구간 안 |
| H 판정 불가 | — | A로 적힌다 |

**주 판정 = A.** 격자 SDF의 관통이 **곡률항 + 크레이스항으로 «설명된다»**:
매끈한 해석면(0.982) · 면분할 구 4대역(0.620~0.753) · 실제 몸(0.766)이 **하나의 모형**으로
[0.5, 1.25] 안에 들어온다. 「작다」가 아니라 「설명된다」가 성립했다.

## §5 한계 — 등재하되 «미해소»가 아니다

갈래 A이므로 §5 한계 등재(미해소 확정)는 **수행하지 않는다.** 다만 **정량화된 성질**로 남긴다:
- 이 표현의 관통은 **0 이 아니다** — 실제 몸 접촉에서 **최대 0.358mm(두께의 36%)**이고
  그 크기는 `P_smooth + c·f(θ)·h`로 **예측 가능**하다(비 0.766).
- **S4가 물을 수 있는 것**: 「흡착 없이 옷이 걸리는가」 · 이음선이 남는가 · 원주가 유지되는가.
- **S4가 못 묻는 것**: **접촉 간극의 절대값**(0.36mm 규모의 주장). 그 규모의 결론이 필요하면
  h를 줄이거나(메모리 31배) 표현을 바꿔야 한다.
- **재개 조건**: S4에서 관통이 예측(`P_pred_ext`)을 **일치 구간 밖으로** 벗어나면 다시 연다.

## §6 회귀

**솔버 0줄 변경** — v3-13 `ec8aac4` 이후 `src/v3/**` 무변경(이 축 4개 판 전부 계기만).
`git diff ec8aac4..HEAD --stat` ⟹ `scripts/v3Body.ts` 단독. v3↔v2 임포트 양방향 0건.
해석 형상 재실행이 v3-14와 같은 값(구 0.076686 · 원기둥 0.038324 · 평면 0 · 마찰
1.0010/1.0003/1.0000). `tsc -b` · `build` 통과.

## §7 잔여 갱신

| # | 항목 | v3-15 | v3-16 |
|---|---|---|---|
| **31** | θ 통계 미등록 | 신설 | **종결.** 구조에서 도출(근방 = 스텐실 폭 h · 축약 = 최대·포화 논증). **안전장치가 구 계열을 보존**함을 확인 |
| **30** | 면분할 크레이스 | 지지·미종결 | **종결.** 점별 통계로 구 4대역(0.620~0.753)과 실제 몸(0.766)이 한 모형에 들어온다 |
| **29** | 격자 SDF 곡률 오차 | 미종결 | **종결(정량화된 성질로).** 관통 ≠ 0이지만 **예측 가능**하고 일치 구간 안. 한계는 §5 |
| **25** | 메시 몸 부호 판정 | 미종결 | **종결. S4 착수 가능** — 수밀 χ=2 · 부호 불일치 0.000% · 크레비스 메움 0 · 마찰 완전 보존 · 관통 설명됨 |
| **F(뾰족 특징)** | 등재 | — | **S4 한계로 이월** — 티셔츠 대역 153정점(R_eff 7.3mm)은 문턱 유효 범위(R≥20mm) 밖 |
| 15·19·23·26·27·28 | — | — | 변화 없음 |

**신규 등재 1건 — 계기 규범**

> **더하는 두 항은 «같은 통계»로 맞춰야 한다.** 이 축은 매끈한 항을 「점별→최대」로,
> 크레이스 항을 「전역 중앙」으로 낸 채 **최댓값과 비교**하고 있었고 그 불일치가
> 2.5배로 나타났다. 모형이 여러 항의 합일 때 **각 항의 통계와 비교 대상의 통계가
> 일치하는지**를 등록 시점에 확인한다.

## §8 다음 수

**S4 — 봉제 + 실제 패턴.** 물리 코어(S1·S2·S3·S3b)와 몸 충돌체(#25)가 전부 섰다.
S4는 §5의 한계 위에서 「흡착 없이 옷이 걸리는가」를 묻는다.

**호출한 ecc 에이전트·스킬: 0.**
