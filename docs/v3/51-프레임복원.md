# 51 — 커넥터 «프레임 복원» + 동기 굽기 경로 (v3-72 · **물리 0프레임 · 손 상수 0**)

v3-72. 브랜치 `v3-72-frame-restore`(사전 등록 · **이 CC 세션 전용**). 호출한 ecc 에이전트/스킬: **0 / 0**

## git 스탬프

```
 브랜치  v3-72-frame-restore        워크트리  /Users/banshk/projects/fit-simulator
 기점    0da90af (v3-71 HEAD)       직전 회차  0da90af
 커밋    (§0 → §1 → §2 → §3 순서로 채운다)
```

---

## §0 판정 수령 — **집행 «전» 커밋**

### §0-1 **CC 귀책 1건 등재** (v3-70 ㉮ «공허 통과»)

```
 **내용**: v3-70 §2-㉮ 의 「비트 동일」은 **커넥터를 통과하지 않은 값**이었다 —
   마네킹 부재 시 커넥터가 **조용히 항등 통과**(`skinned:false` ⟹ `base` 반환)했기 때문이다.
 **원문 무삭제** · **v3-71 §3-㉰ 이 «정정의 자리»**다.
 ★ **대칭 기준**(어느 쪽 귀책인지 규칙으로 가른다):
   · **폴백 «설계» = CC** — 「전제가 없으면 조용히 항등을 돌려준다」를 코드에 넣은 것은 CC 다.
   · **공허 가능성을 못 본 «검사 설계» = 전략 세션 «병기»** — ㉮ 를 「비트 대조」로만 적고
     **「커넥터를 실제로 통과했는가」를 요구하지 않았다**.
   ⟹ **둘 다 적는다.** 한쪽으로 몰지 않는다.
```

### §0-2 신설 함정 (번호 **CC 배정**)

```
 **#121 「전제 부재 시 «던져라» — 조용한 항등 폴백은 자기검사를 «공허»하게 만든다.
        「통과」와 「미집행」을 구별 못 하는 검사는 «없는 것»이다.」**
 실례 = v3-70 ㉮(마네킹 부재 ⟹ 항등 통과 ⟹ 비트 동일 ⟹ 「성립」으로 읽혔다).
 적용 = 이 판에서 커넥터의 항등 폴백을 **폐기**하고 **던지도록** 고친다(§0-5).
```

### §0-3 백화 **종결 재분류**

```
 **분류**: **「환경 조건」** — 비가시 탭 ⟹ `ResizeObserver` 미발화 ⟹ **R3F 루트 미생성**.
 **v2 · `Mannequin` 혐의 «해제»**(v3-71 §2 실측 사슬 ①~⑥).
 **미확인 1건 병기**: **사용자의 «가시 탭»에서 재현되는지는 확인하지 않았다**
   (CC 는 확장 구동이라 항상 비가시다). ⟹ **「해결」이라 적지 않는다.**
 `?v2=1` 항목을 위 문언으로 **갱신**한다.
```

### §0-4 무인 굽기 처분 수령

```
 **rAF 의존 «제거» = 「동기 굽기」**.
 **P26 「도착점 «유일»」 조항의 «등가 구현»**이지 **재구현이 아니다** —
 **v2 `Mannequin` 객체·배율 로직을 «호출»**한다(**`Mannequin` 로직 0줄**).
```

### §0-5 폴백 **정정 수령**

```
 커넥터는 **마네킹 부재 시 «던진다»**. **항등 통과 폐기**(#121 적용).
```

### §0-6 §1~§3 사전 등록

```
 **§1 프레임 복원** — 복원 변환의 **출처를 «씬에서» 도출**한다(**스칼라 수기 입력 금지** ·
   **채택안과 «행렬 출처 파일:줄» 등재**).
   **자기검사 ㉮″(«진짜» 연결부 등가)**: 기본 슬라이더 몸 · **커넥터 «실통과»**
   (**`skinned:true` 확인 값 등재**) → `parseGlb` 대비 **max|Δ| 실측 등재**
   (**문턱 창작 0** · 판정은 전략 세션). **조립 통과(목선 도출 성립 · throw 0)가 «필수 조건»**.
 **§2 동기 굽기** — `bakeBodySync` 신설(**rAF·가시성·R3F 루프 불요** · **새 문턱 0**).
   **등가 측정 1회**: 가시화(스크린샷) rAF 정착 굽기 ↔ 동기 굽기 · **max|Δ| 등재**.
 **§3 ㉱ 재집행** — v3-71 §0-7 문언 **그대로** · **동기 굽기 경로로**.
   ⓐ 가슴 대역 실측 둘레 배율(**기대 1.10 병기**) ⓑ 도출값 7종(기본 몸 병기) ⓒ **방향 판정만**
   (**허용 오차 창작 0**).
```

### §0-7 §4 갈래 (사전 등록)

| 갈래 | 조건 | 귀결 |
|---|---|---|
| **A** | ㉮″ **조립 통과** · §2 등가 성립(또는 **Δ 상신**) · ㉱ **방향 전량 성립** | **완료 상신**(민감도 판 준비) |
| **B** | 프레임 복원 «후»에도 **조립 throw** | 정지 · **값 사실만** |
| **E** | 판정 불가 | 정지 · 사실만 |

### 이 판에서 바꾸지 않는 것

**물리 0프레임** · **`solver` / `garmentScene` / `s4Gate` diff 0** · **정본 0** · **v2 화면 0줄** ·
**문턱 0** · **`Mannequin` 로직 0줄**(**호출만**).

### 순서

```
 §0 커밋 → §1 → ㉮″ → §2 → §3 → §4 판정 · 상신
```

---

## §1 프레임 복원 — **씬에서 도출 · 손 상수 0**

### §1-1 채택안과 **행렬 «출처» 파일:줄**

```
 근거 two lines(three 소스 · **스칼라 수기 입력 0**)
   `node_modules/three/src/objects/SkinnedMesh.js:319-345`
      `applyBoneTransform` = **`bindMatrixInverse · Σ(boneMatrix · bindMatrix · p)`**
   `node_modules/three/src/objects/SkinnedMesh.js:230-240`
      `bind(skeleton, bindMatrix)` — 인자가 없으면 **`this.matrixWorld`**(= **바인드 «시점»** 월드)를 쓴다
 ⟹ 렌더가 그리는 월드 = `matrixWorld · applyBoneTransform(p)` 이고,
    `parseGlb` 의 POSITION 프레임 = **바인드 시점의 국소 프레임**(= `bindMatrix` 가 월드로 보내는 공간)
 ⟹ **채택식**  **`p_glb = inverse(bindMatrix) · matrixWorld · applyBoneTransform(p)`**
 구현 `src/components/bodyInjectBake.ts` —
    `const restore = new THREE.Matrix4().copy(sk.bindMatrix).invert().multiply(sk.matrixWorld);`
    **두 행렬 모두 씬(three `SkinnedMesh` 필드)에서 읽는다.**
 **항등에서 항등**: 바인드 이후 조상 변환이 안 바뀌면 `matrixWorld === bindMatrix` ⟹ 복원 행렬 = 단위행렬.
 **키(조상 균등 스케일)는 살아남는다**: 조상이 바뀌면 두 행렬의 비가 남는다.
 참고 사실(코드에 값 0) — 이 GLB 는 루트 `Armature` 노드가 **scale 0.01 · x축 −90° 회전**이고
    메시 `Ch36` 이 그 자식이다 ⟹ **조인트 세계와 POSITION 세계가 100배·회전만큼 어긋나 있다**.
    **v3-71 의 「정확히 100배」가 이것이다.**
```

### §1-2 항등 폴백 **폐기**(#121 적용)

```
 커넥터는 `root` 부재 · 스킨드 메시 부재 시 **던진다**(구 「항등 통과」 폐기).
 호출부는 **`skinned:true` 를 «확인 값»으로 찍는다** ⟹ 「통과」와 「미집행」이 **구별된다**.
```

### §1-3 ★ 자기검사 ㉮″ — **커넥터 «실통과» · 조립 «통과»**

```
 [v3-72 굽기] 가슴 100cm · **rAF** · 정착 **성립** · 프레임 **7** · 잔차 **0.000e+0**
 [v3-72 굽기] **skinned:true**(실통과 확인) · 정점 **15882**
              · `parseGlb` 배열과 **비트 상이** · **max|Δ| 5.9187e+2 mm**
 [v3-72 도출] BEXT [**1.088971**, **1.700000**, **0.271880**]m · h **3.260478mm** · band **7.520955mm**
              · ③b 문턱 **0.815119mm** · Y_TOP **137.9934cm** · Y_NECK **144.2316cm**
              · AXIS_Z **1.5426cm** · 목선둘레 **46.9149cm** · **조립 정점 7224**
 ⟹ **필수 조건 «충족»** — **목선 도출 성립 · throw 0**(v3-71 의 「갈래 D」가 **사라졌다**).
```

**★ max|Δ| 591.87mm 의 «해석»** — 문턱을 만들지 않는다. **값이 스스로 설명한다**:

```
 BEXT y = **1.700000 m**(정확히 1.70) — `Mannequin` 이 **기준 신장으로 정규화**한 결과다
   (`Mannequin.tsx:299` `targetHeight = DEFAULT_BODY_SIZE.height / 100`). `parseGlb` 원본은 **1.765 m**.
 BEXT x = **1.088971 m** ↔ 원본 **1.779605 m** — **팔이 T포즈에서 A포즈로 내려갔다**(x 범위 축소).
 ⟹ **두 배열은 «다른 몸»이다** — 하나는 원본 GLB(T포즈·미정규화), 하나는 **화면에 보이는 마네킹**.
    **차이가 나는 것이 «맞다»**. 비트 동일을 요구할 대상이 아니다.
 ⟹ **판정 문턱은 만들지 않는다**(사전 등록대로 · **판정은 전략 세션**).
```

---

## §2 동기 굽기 경로 — **등가 «비트 동일»**

```
 `stepFrames(n, dtMs = 1/60초)` 신설(`BakeMount.tsx`) — R3F 공개 API `advance(t)` 로 프레임을 «민다».
 정착 판정은 **기존 채널 그대로**(`poseStopped()` ∧ `maxScaleResidual ≤ POSE_SETTLE_EPS`) — **새 문턱 0**.
 상한 **600프레임**에 걸리면 **굽되 사실을 남긴다**(함정 25).
 남는 환경 의존 **1건**: **루트 «생성» 자체는 가시화 계기 1회를 요구한다**(v3-71 §2 ⑥) —
   `advance` 는 루트가 있어야 의미가 있다. **이 판이 그 조건을 없애지는 못했다.**
```

### §2-1 ★ 조기 종료 **정정**(같은 판 안에서 값으로 잡았다 · 은폐 0)

```
 1차 구현은 「가슴 110」에서 **전진 1프레임 · 잔차 0 · 도출값이 기본과 «완전히 동일»」**을 냈다
   ⟹ **몸이 안 바뀐 채로 「정착 성립」**이었다. 원인 2건:
   ① 스토어를 바꿔도 **React 재렌더가 비동기**라, 곧바로 `advance` 하면 `useFrame` 이
      **옛 `bodySize` 클로저**를 본다 ⟹ **한 번 양보**(`setTimeout 0`)를 넣었다.
   ② `maxScaleResidual` 은 **직전 프레임** 값이라 변경 직후엔 **낡은 0**이다 ⟹
      **연속 2프레임** 충족을 요구했다(P26 의 「target 이 직전 프레임과 같을 것」과 같은 성질).
 정정 후: 「가슴 110 · **전진 4프레임** · 잔차 0」이고 **도출값이 실제로 갈린다**(§3).
 **#121 계열의 재발이었다** — 「통과」와 「미집행」이 다시 붙을 뻔했다. **사실로 남긴다.**
```

### §2-2 등가 측정 (Node · 배열 비트 대조)

| 대조 | 상이 성분 | max&#124;Δ&#124; | 판정 |
|---|---|---|---|
| **rAF 정착 굽기 ↔ 동기 굽기**(가슴 100) | **0 / 47,646** | **0.0000e+0 mm** | **비트 동일** ✅ |
| (참고) rAF 굽기 ↔ `parseGlb` | 47,646 / 47,646 | 5.9187e+2 mm | §1-3 해석 |

⟹ **동기 굽기는 rAF 정착 굽기와 «같은 배열»을 낸다**. **rAF·가시성 의존이 굽기 «판정»에서 사라졌다**
(루트 생성 1회 조건은 남는다).

---

## §3 ㉱ 재집행 — **가슴둘레 100 → 110cm**(동기 경로)

### ⓑ 도출값 — **기본 몸과 병기**

| 채널 | 가슴 **100** | 가슴 **110** | 방향 |
|---|---|---|---|
| BEXT x [m] | 1.088971 | **1.231474** | ↑ (+13.09%) |
| BEXT y [m] | 1.700000 | **1.700878** | ↑ (+0.05%) |
| BEXT z [m] | 0.271880 | **0.277341** | ↑ (+2.01%) |
| `h` [mm] | 3.260478 | **3.426315** | ↑ (+5.09%) |
| `band` [mm] | 7.520955 | **7.852630** | ↑ |
| **③b 문턱** [mm] | 0.815119 | **0.856579** | ↑ · **정확히 0.25 × h** |
| `Y_TOP` [cm] | 137.9934 | **140.3546** | ↑ |
| `Y_NECK` [cm] | 144.2316 | 144.1477 | ↓(미세) |
| `AXIS_Z` [cm] | 1.5426 | **1.8301** | ↑ |
| 목선둘레 [cm] | 46.9149 | **50.8478** | ↑ |
| 조립 정점 | 7224 | **7306** | ↑ |

### ⓐ 가슴 대역 **실측 둘레 배율** — 기대 **1.10** 병기

```
 밴드 정의(**손 상수 0**): y = **그 몸의 «도출» Y_TOP** × **등재 비율 0.92164**
   (= 등재 chestY 135.362 / 등재 Y_TOP 146.8696 · 둘 다 기본 몸 등재값) · 반폭 = **그 몸의 도출 h**
 가슴100  밴드 y **127.181cm** ±3.260mm · 표본 **69** · 폭 x 56.3560 · 폭 z 22.4204 · 평균반지름 **17.2933cm**
 가슴110  밴드 y **129.357cm** ±3.426mm · 표본 **60** · 폭 x 61.0087 · 폭 z 23.6959 · 평균반지름 **20.0223cm**
 **배율**  폭 x **1.0826** · 폭 z **1.0569** · **평균반지름 1.1578**      (**기대 1.10**)
 **허용 오차는 정하지 않는다**(사전 등록). 세 채널이 **1.06~1.16 대역**이고 **기대 1.10 을 둘러싼다**.
 **정확 일치를 요구할 수 없는 이유도 값으로 적는다**: 두 밴드가 **다른 높이**(127.18 vs 129.36cm)에서
 잡히고 **표본 수도 다르다**(69 vs 60) — 밴드가 «그 몸의» 도출값을 따라가기 때문이다.
```

### ⓒ **방향 판정** — 전량 성립

```
 ① **둘레 증가** — 폭 x · 폭 z · 평균반지름 **셋 다 > 1** ✅
 ② **BEXT y·z 증가** — y +0.05% · z +2.01% ✅
 ③ **문턱이 그 h 를 따라감** — h +5.09% · ③b 문턱 +5.09%(**정확히 0.25 × h**) ✅
```

---

## §4 판정 — **갈래 A**

| 사전 등록 조건 | 실측 | 충족 |
|---|---|---|
| ㉮″ **조립 통과** | 조립 정점 **7224** · **throw 0** · 목선 도출 성립 · **skinned:true** | ✅ |
| §2 **등가 성립**(또는 Δ 상신) | **비트 동일**(상이 성분 0 / 47,646) | ✅ |
| ㉱ **방향 전량 성립** | ⓒ ①②③ 전량 | ✅ |

⟹ **갈래 A — 완료 상신**(민감도 판 준비).

**회귀 · 게이트**(정본 무변화 확인)

```
 blob sha 3종 `8dec6eca…` / `786f3fc0…` / `dcaceda3…` **등재값 일치** ·
 스트립 **258 / 258 / 290** · G7 **3/3 통과** · **정본 0**
 diff 0 확인 11파일 — solver · garmentScene · s4Gate · **dressRun** · printUv · printComposite ·
   **Mannequin** · **FitCanvas** · **Controls** · V3Product · App ⟹ **v2 화면 0줄 · Mannequin 로직 0줄**
 tsc -b rc=0 · build rc=0 · **물리 0프레임**
```

**★ 상신하는 «사실» 3건**
① **max|Δ| 591.87mm 는 결함이 아니다** — `parseGlb`(원본 T포즈·1.765m)와 **화면 마네킹**
   (정규화 1.700m · A포즈)이 **서로 다른 몸**이기 때문이다. **문턱 창작 0 · 판정은 전략 세션.**
② **동기 굽기 = rAF 굽기와 «비트 동일»** — 굽기 판정에서 rAF·가시성 의존이 사라졌다.
   **다만 «루트 생성» 1회 조건은 남는다**(가시화 계기 1회 · v3-71 §2 ⑥). **무인 굽기의 잔여 결손.**
③ **§2-1 조기 종료 정정** — 1차 구현이 「가슴 110」을 **안 바뀐 채로 «성립»**으로 읽었다(#121 계열 재발).
   **같은 판 안에서 값으로 잡아 고쳤다.** 원인 2건(React 재렌더 비동기 · 잔차 1프레임 지연) 등재.

**다음 판 입력**: 몸 축 1개(가슴)의 **도출 전파가 값으로 확인**됐다 ⟹ **민감도 판 발주 가능**.
**CC 화면 판정 0.**
