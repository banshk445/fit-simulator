# 27 — 시접 «횡단» 굽힘 (재봉선에 회전 연속성을 · 물리 판 · 실패 시 의무 원복)

v3-46. 브랜치 `v3-46-seam-bend`(사전 등록). 호출한 ecc 에이전트/스킬: **0 / 0**

---

## §0 판정 기준 — 실행 «전»에 등록

> 이 절을 커밋한 «뒤»에만 ㉠ 확인으로 간다. 이후 결과를 보고 어떤 수도 고치지 않는다.

### §0-1 배경 (전략 세션 판정 수령분)

```
 사용자 재판정: **(나) 끊김 어느 정도 해소 · (가) 어깨 «집힘» 잔존** ⟹ **물리 판**.

 **결핍 가설(사전 등록)**: 시접은 현재 «**자유 경첩**»이다 — 굽힘 제약이 패널 «내부»에만 있고
 시접을 «가로지르는» 굽힘 결합이 0 이라면, 두 패널은 rest 2mm 만 지킨 채 **자유 회전**한다.
 실물 재봉선의 **회전 연속성이 결핍** ⟹
   · 시접선 집힘(농축 **1.6~2.4배** · v3-44 ㉡4)
   · 소매 텐팅(상위 5%의 **50~58%** · v3-44 ㉡2)
   · 뒤 암홀 쐐기(**6~9mm** · v3-45 §2-1)
   · 코너 확폭(**8.09mm** · v3-44 ㉠1)
 가 **한 결핍으로 설명**된다.
 이는 **보조 힘이 아니라 «결핍 물리의 보충»**이며, 상수는 전부 기존 등재분에서 온다
 (**원단 자신의 B · 휴지 기하 — 새 수 0**).
```

### §0-2 ㉠ 가설의 «코드 확인» — 선행 · **반증 시 정지**

```
 `garmentScene` 의 굽힘 요소 생성이 **시접 쌍을 가로지르는 요소를 만드는지** 확인한다.
   **이미 있다** ⟹ 가설 **반증 · 갈래 F 정지 · 상신**(구현 착수 **0**).
   **없다**   ⟹ 그 사실(**코드 줄 지목**)을 등재하고 진행.
```

### §0-3 ㉡ 구현 — 시접 «횡단» 굽힘 요소

```
 **전 시접**(특정 시접만 특별 취급 **0** — 기전은 일반이다)에 대해, 시접 쌍이 만드는
 사변형 `(a[k], b[k], a[k+1], b[k+1])` 마다 기존 `makeBend` «**와 같은 형식**»의 굽힘 요소를
 **패널 경계를 가로질러** 추가한다.
   강성  = **그 원단의 B**(등재 물성 · v3-13 원칙 재사용: 「이음선을 원단보다 뻣뻣하게 만들지 않는다」)
   휴지각 = **휴지 기하에서 도출**(평평한 패턴 두 장 ⟹ 패널 내부와 «같은 도출식»)
   **새 수 0.** 시접 rest(SEP)·자기충돌·기타 물리 전 항목 **무변경**.
 **조립 결정성**: 변경 후 조립 **2회** — n · 삼각형 · sub · 램프 **동일** 확인.
 **sub 가 변하면 그 사실과 새 값을 등재**(`substepsForBending` 경로 — B·ℓ 불변이므로
 불변 «예상»이나, **예상을 판정에 쓰지 않는다**).
```

### §0-4 ㉢ 프로브 — 벽 판정 (§1′ 조항 그대로)

```
 3원단 «각각» **단독 20프레임** 프로브 → **s/f 실측** → 예상 = s/f × **정착 프레임**(등재분).
 **어느 원단이든 예상 > 60분 ⟹ 전량 착수 0 · 갈래 B 정지 · 상신.**
 (참고 등재분: **gray 53.7분이 벽에 근접** — 제약 추가로 넘을 수 있다)
```

### §0-5 ㉣ 전량 + 게이트 — 하드 판정

```
 3원단 **전량 재실행**(정착 프레임 등재분 · **단독**) → C-브라우저 정착 상태 «**재수립 후보**».

 **하드 게이트** (하나라도 실패 ⟹ **갈래 C: «의무 원복»** — revert 커밋 · 등재 · 상신):
   자기교차 **0** · 시접 교차 **0** · 발산 **0** · ③a 관통 **≤ 0.5mm** ·
   목선 ringExcess **≤ 1** · 각 실행 **실측 ≤ 60분**

 **S5 재확인** (실패 ⟹ **갈래 E: 의무 원복**):
   O4 인접 차가 **잡음 바닥 0.25pp 이상** · **순서 유지**
   (물리 변경이 재료 구분을 뭉개지 않았는가 — 4종이나 **denim 은 제외 확정**이므로
    «**3종 인접 차**»로 판정하고 **그 사실을 명기**)
```

### §0-6 ㉤ 개선 채널 — **방향 술어**(문턱 없음 · 방향만 사전 등록)

```
 아래 4채널이 3원단 «**전부 개선 방향**»이어야 진행(어긋나면 **갈래 D: 의무 원복 · 상신**):
   ① 시접 ±2열 **농축비**            (v3-44 ㉡4: 2.42 / 2.37 / 1.56)      — **감소**
   ② 상위 5% **소매 점유**            (v3-44 ㉡2: 58 / 50 / 52%)          — **감소**
   ③ sideXplus 잔여 끊김(브리지 on · 팔 숨김) (v3-45: 28.5 / 31.0 / 5.5‰) — **감소**
   ④ **코너 확폭 최대**               (v3-44 ㉠1: 5.13 / 4.58 / 8.09mm)   — **감소**
 **개선 «양»은 판정하지 않는다** — 좋고 나쁨은 사용자 화면 판정 몫.
```

### §0-7 ㉥ 캡처 + 상신

```
 게이트·S5·개선 방향이 서면: 제품 **9장** + 대조 **3장**(v3-45 규약 · **브리지 on**) →
 **사용자 화면 판정 상신**. **CC 자체 화면 판정 0.**
```

### §0-8 ㉦ 갈래 (사전 등록)

| 갈래 | 조건 | 귀결 |
|---|---|---|
| **A** | ㉣ 전량 통과 · S5 유지 · ㉤ 4채널 개선 | 캡처 상신. **기준선 «후보»** — 정본 승격은 **사용자 합격 + 전략 선언 뒤**. 그 전까지 **구 기준선 유지 · 신 상태는 후보 표기** |
| **B** | 프로브 벽 초과 | **착수 0 · 상신** |
| **C** | 하드 게이트 실패 | **의무 원복** · 실패 조항 등재 · 상신 |
| **D** | 개선 방향 미충족 | **의무 원복** · 등재 · 상신 (기전이 답이 아니었다는 실측) |
| **E** | S5 붕괴 | **의무 원복** · 상신 |
| **F** | ㉠ 가설 반증(횡단 굽힘이 이미 있음) | **구현 0 · 상신** |
| **G** | 판정 불가 | 정지 · 사실만 |

### §0-9 부채 등재 (이 판이 하지 «않는» 것)

```
 · **Node B-v3 기준선 재수립** — 물리 변경으로 무효화(별도 판)
 · **denim 검증 경로 재측정**
 · **v3-40 목선 문턱의 `C_body` 재확인** — 정착 위치가 바뀔 수 있음.
   게이트는 이 판에서 재실행되므로 **판정은 유효** · **등재값 갱신만**
```

### 이 판에서 바꾸지 않는 것

`SEP`·시접 rest·자기충돌 분리 **0** · 문턱 전 종목 **0** · 게이트 조항 **0** ·
**브리지 유지** · 표시 층 **0** · v2 경로 · **CC 자체 화면 판정 0** · **원단 물성(MAT) 0**.

### 순서

```
 §0 커밋 → ㉠ 확인 → ㉡ 구현 + 조립 2회 → ㉢ 프로브 → ㉣ 전량 + 게이트 + S5 →
 ㉤ 개선 채널 → ㉥ 캡처 → 등재
```

---

## §1 ㉠ 가설의 «코드 확인» — **반증 아님 · 갈래 F 불발화**

`src/v3/solver.ts` `makeBend`:

```ts
  for (const e of edges.values()) {
    if (e.wings.length !== 2) continue; // 경계 엣지 — 힌지 없음
```

```
 힌지는 **두 삼각형이 «공유»하는 엣지**에만 생긴다. 그런데 패널은 각자 `base` 로 시작하는
 **분리된 정점 범위**를 쓰고(`at(p,i,j) = p.base + j*(p.nu+1) + i`), 시접 쌍은 «서로 다른 패널»의
 정점이라 **엣지를 공유할 수 없다**.
 ⟹ **시접을 가로지르는 굽힘 결합은 원리적으로 0.** 가설 성립 — 진행한다.
```

---

## §2 ㉡ 구현 — `makeSeamBend` (solver.ts · `makeBend` 와 «같은 형식»)

```
 축   = 시접 구간 `(a[k], a[k+1])`. 휴지 패턴에서 `(b[k], b[k+1])` 과 **동일시**되는 엣지다
        (봉제 = 두 가장자리를 붙이는 것 · **이즈 0.000%** — v3-44 ㉡3 실측)
 날개 = 양쪽 패널의 **실제** 경계 삼각형 두 개(경계 엣지의 유일한 날개) ⟹ `a = areaA + areaB`
        내부 힌지와 **같은 차수**다(퇴화 슬리버를 만들지 않는다)
 정지각 = **0** — 평평한 패턴 두 장을 시접에서 이으면 공면. `makeBend` 의 도출과 «같은 줄»
 강성 = **그 원단의 `MAT.B`** — v3-13 §3 원칙(「이음선을 원단보다 뻣뻣하게 만들지 않는다」)
 **새 수 0.**
```

### §2-1 조립 결정성 + 계수 (스텝 **0회**)

| 채널 | gray d9 | swim d10 | sweat d9 |
|---|---|---|---|
| 조립 2회 동일 | **예** | **예** | **예** |
| 정점 / 삼각형 | 10402 / 20012 | 8676 / 16636 | 10402 / 20012 |
| 램프 | 82 | 81 | 82 |
| 시접 쌍 | 268 | 242 | 268 |
| **시접 굽힘 요소** | **258** | **232** | **258** |
| **건너뜀** | **0** | **0** | **0** |
| 굽힘 전체 (내부 + 시접) | 29884 = 29626 + 258 | 24832 = 24600 + 232 | 29884 = 29626 + 258 |
| **sub** (멤 / 굽) | **226** (226 / 110) | **345** (345 / 138) | **160** (125 / 160) |
| **v3-38 등재 sub** | **226** | **345** | **160** |

```
 **sub 불변 3/3** — 예상이 아니라 실측이다. 이유도 값으로 나온다:
 `shape = 4a/l²` 가 **시접 3.08~6.86 · 내부 0.70~12.38**(gray) ⟹ 시접 요소가 내부 분포 «안»에 있고
 «가장 빠른 제약»이 아니다(`substepsForBending` 은 최대 ω 를 본다).
```

---

## §3 ㉢ 프로브 — 벽 판정 **통과**

3원단 «각각» **단독 20프레임** · `scripts/v3Run.ts`.
**변경 «전» 프로브도 같은 방식으로 쟀다** — 비용 비를 «추정»하지 않기 위해서다(#81 재발 방지).
변경 전은 `git worktree` 로 §0 커밋(`2b83148`)을 꺼내 같은 기계·같은 하네스에서 돌렸다.

| 원단 | 정착 프레임(등재) | 변경 «전» s/f | 변경 «후» s/f | 비 | **예상 = s/f × 정착** | 벽 |
|---|---|---|---|---|---|---|
| gray d9 | 220 | 10.21 | **9.26** | 0.907 | **34.0분** | ✓ |
| swim d10 | 180 | 12.93 | **12.92** | 0.999 | **38.8분** | ✓ |
| sweat d9 | 190 | 7.98 | **7.26** | 0.910 | **23.0분** | ✓ |

```
 **시접 굽힘은 측정 가능한 비용을 더하지 않는다** — 비 0.907 / 0.999 / 0.910(≤ 1).
 근거: **sub 불변** + 제약 수 **+0.9%**(258 / 29626). 세 값이 1 아래인 것은 실행 간 요동이다
 (CLAUDE.md: 물리 ms 는 같은 코드·같은 fixture 에서 2배까지 흔들린다 — **속도 개선으로 읽지 않는다**).
 **하네스 주의**: 프로브는 `v3Run`(계기 없음)이고 ㉣ 전량은 «브라우저 게이트»(계기 포함)다.
 절대값을 섞어 쓰지 않았다 — 벽 판정은 프로브로, ㉣ 의 「실측 ≤ 60분」은 전량 실행 자체로 냈다.
```

---

## §4 ㉣ 전량 + 게이트 — **gray 실패 ⟹ 갈래 C**

v3-38 과 **같은 경로**(브라우저 워커 + `s4Gate.ts`) · **단독** · 정착 프레임 220.

### §4-1 gray d9 — 하드 게이트

| 채널 | 문턱 | v3-38 등재 | **v3-46 실측** | 판정 |
|---|---|---|---|---|
| **③a 관통 `penMaxM`** | ≤ 0.5mm | **0.3768mm ✓** | **0.7460mm** | **✗ 실패** |
| 자기교차 `crossings` | 0 | 0 | **0** | ✓ |
| 발산 | 0 | 0 | **false** | ✓ |
| 목선 `ringExcess` | ≤ 1 | — | **0.9877** | ✓ |
| (구 채널) `ringRestRatio` | 병기 | 1.0933 | **1.0930** | 병기 |
| 정착 `settleNetM` | ≤ 0.1mm | — | **0.0160mm** (settled) | ✓ |
| 보조 장치 `pinned` | 0 | — | **0** | ✓ |
| **실측 시간** | ≤ 60분 | 53.7분 | **41.0분** | ✓ |
| `lambdaMax` | 참고 | 1.4499 | 1.4525 | — |
| 시접 갭 중앙 / 최대 | 참고 | — | 2.142 / **5.187mm** | — |
| 관통 정점 수 `penCnt` | 참고 | — | 1049 | — |
| 최악 정점 | 참고 | — | v4520 @ (0.1768, 1.4820, −0.0196) | — |

```
 **`fails: ["③a 관통 0.7460mm > 0.5mm"]`** — 단 한 채널이 실패했고, **그 하나로 갈래 C 가 확정**된다.
 등재 대비 **1.98배 악화**(0.3768 → 0.7460). 최악 정점은 **y 1.482m** — 목선~어깨 대역이다.
 상태 페이로드 sha **`633dbfa1 7c7b62ff …`** · 41.0분 · 220프레임 완주.
```

### §4-2 남은 2원단을 «돌리지 않은» 이유 — 명기

```
 §0-5 는 「**하나라도 실패 ⟹ 갈래 C**」다. gray 실패로 **판정은 이미 확정**되고, swim·sweat 를
 더 돌아도(≈ 62분) **갈래가 바뀌지 않는다.**
 CLAUDE.md 규범(「반증에 걸리면 계기를 돌리지 말고 정지·보고한다 — 회차를 아낀다」)에 따라
 **미실행**으로 두고 그 사실을 여기 적는다. **추정치를 적지 않는다** — swim·sweat 의
 ③a 값은 **모른다**.
```

### §4-3 S5 재확인 — **미실행**(판정 불가로 적는다)

```
 S5 는 **3원단 전량**이 있어야 「3종 인접 차」를 낼 수 있다. gray 하나로는 인접 차가 성립하지 않는다.
 ⟹ **S5 판정 미실행.** 갈래 E 는 발화하지도 기각되지도 않았다 — **판정 불가**로 적는다.
 (§0-5 의 「denim 제외 ⟹ 3종 인접 차」 조항은 그대로 유효하고, 다음 판이 쓰면 된다)
```

---

## §5 ㉤ 개선 채널 — **기록**(갈래 C 가 먼저 발화했으므로 판정 아님)

원복 «전»에 실패 상태(sha `633dbfa1…`)를 확보해 v3-44/45 계기로 **같은 4채널**을 쟀다.
**gray 만** 있다(§4-2).

| # | 채널 | v3-44/45 등재(gray) | **v3-46 실측(gray)** | 방향 |
|---|---|---|---|---|
| ① | 시접 ±2열 **농축비** | 2.42 | **2.32** | **감소 ✓** |
| ② | 상위 5% **소매 점유** | 58% | **54%** (27+27) | **감소 ✓** |
| ③ | sideXplus 잔여 끊김(브리지 on · 팔 숨김) | 28.5‰ | **23.9‰** | **감소 ✓** |
| ④ | **코너 확폭 최대** | 5.13mm | **5.19mm** | **증가 ✗** |

```
 ①②③ 은 개선 방향이고 **④ 는 어긋났다**(+0.06mm). 세 채널의 개선 폭도 크지 않다
 (농축비 −4% · 소매 점유 −4pp · 끊김 −16%).
 **이것으로 갈래 D 를 판정하지 않는다** — 순서상 ㉣ 가 먼저 발화했고, ㉤ 는 3원단이 아니라 1원단뿐이다.
 **기전이 어깨 집힘을 «조금» 움직이긴 했으나 관통을 «두 배» 악화시켰다**는 것이 이 판의 실측이다.
```

---

## §6 ㉦ 갈래 판정 — **C (하드 게이트 실패 · 의무 원복)**

| 갈래 | 조건 | 실측 | 성립 |
|---|---|---|---|
| A | 전량 통과 · S5 유지 · ㉤ 4채널 개선 | gray ③a 실패 · ④ 어긋남 | 아니오 |
| B | 프로브 벽 초과 | 34.0 / 38.8 / 23.0분 — 전부 통과 | 아니오 |
| **C** | **하드 게이트 실패** | **gray ③a 0.7460mm > 0.5mm** | **예** |
| D | 개선 방향 미충족 | ④ 어긋남이나 **㉣ 가 먼저 발화** · 1원단뿐 | (선점됨) |
| E | S5 붕괴 | **미실행 — 판정 불가**(§4-3) | 판정 불가 |
| F | ㉠ 반증 | 횡단 굽힘 **원리적으로 0** — 반증 아님 | 아니오 |
| G | 판정 불가 | — | 아니오 |

### §6-1 의무 원복 — **실행 완료**

```
 revert 커밋 **`ae0fc0c`**(= `Revert "v3-46 ㉡: makeSeamBend …"`) — `ba8c13d` 를 되돌린다.
 원복 후 **물리 diff 0줄**(v3-45 HEAD `182426f` 대비 solver · garmentScene · consts · dressRun ·
 wangStretch · bodySdf **전부 0**).
 **유지한 것 1건**: `V3Panel` 의 `__v3s4` 게이트 채널(`dbd416f`) — **계기이고 물리 무관**이다.
 다음 판이 같은 게이트를 읽을 때 다시 만들 필요가 없으므로 남긴다. **그 사실을 여기 적는다.**
```

### §6-2 이 판이 «닫은» 것

```
 · **시접 횡단 굽힘 결합이 0 이라는 사실** — 코드 줄로 확정(§1). 「자유 경첩」 서술은 **참**이다
 · **그 결핍을 «원단 자신의 B · 정지각 0»으로 메우면 ③a 관통이 1.98배 악화된다** — 실측
 · **비용은 문제가 아니었다** — sub 불변 · 프로브 비 0.907~0.999 · 전량 41.0분(등재 53.7분보다 짧다)
 · ㉤ ①②③ 은 «조금» 개선되고 ④ 는 악화 — **기전이 어깨 집힘의 답은 아니다**(1원단 기준)
```

---

## §7 부채 등재 (§0-9 그대로 · 이 판이 하지 «않은» 것)

```
 · **Node B-v3 기준선 재수립** — 원복으로 물리가 되돌아왔으므로 **무효화되지 않았다**. 부채 해소
 · **denim 검증 경로 재측정** — 미실행. 부채 유지
 · **v3-40 목선 문턱 `C_body` 재확인** — 원복으로 정착 위치가 되돌아왔으므로 **갱신 불필요**. 부채 해소
 **이 판이 «새로» 남기는 부채**
 · **swim · sweat 의 시접 굽힘 ③a 값 미측정**(§4-2) — 다음 판이 같은 기전을 다시 볼 때 필요
 · **S5 3종 인접 차 미실행**(§4-3)
```

---

## §8 회귀

```
 원복 후 물리 diff **0줄** — v3-45 HEAD(`182426f`) 대비 solver · garmentScene · consts ·
   dressRun · wangStretch · bodySdf **전부 0**
 npx tsc -b rc=0 · npm run build 통과 · v2 경로 무영향
 **브리지 off 진단 벌 = v3-44/45 값 재현**(구 기준선 상태 `settled-gray-d9.bin`):
   끊김 front/side/back **7 / 118 / 7** · 팔 숨김 후 side **76 (65.6‰)** · 실루엣 **9735** — 전부 일치
 **하네스 비트 동일 — 통과**: 원복 후 20프레임 상태 blob ↔ 변경 «전» 워크트리(`2b83148`) 20프레임 blob
   `cmp` **차이 0바이트**(499,378B) ⟹ **원복이 완전하다.** 프로브 시간도 8.92 s/f 로 같은 대역
```

---

## §9 자기 점검

| # | 항목 | 답 |
|---|---|---|
| 1 | §0 커밋이 ㉠보다 앞서는가 | **예** — `2b83148`(§0) → `ba8c13d`(㉡ 구현) |
| 2 | 새 수 0인가 — 도출 근거 | **예**. 강성 = `MAT.B`(v3-26 등재 물성 · v3-13 §3 원칙) · 정지각 = **0**(`makeBend` 와 «같은 줄»의 도출: 평면 제도) · `l`·`a` = 휴지 패턴 좌표(`uv`) · 축 동일시 근거 = **이즈 0.000%**(v3-44 ㉡3). **상수 신설 0건** |
| 3 | 하드 게이트 판정에 쓴 실행이 전부 단독·실측인가 | **예** — gray 220프레임 **단독** 브라우저 실행 1회 · 41.0분 **실측**. 병렬 0 |
| 4 | 실패 갈래에서 원복 커밋이 실제로 찍혔는가 | **예** — `ae0fc0c`. 물리 diff 0줄로 확인 |
| 5 | S5 판정이 3종 인접 차 기준임을 명기했는가 | **예 · 다만 미실행** — §4-3. 1원단으로는 인접 차가 성립하지 않아 **판정 불가**로 적었다 |
| 6 | 부채 3건이 등재됐는가 | **예** — §7. 2건(Node 재수립 · C_body)은 **원복으로 해소**, 1건(denim)은 유지, **신규 2건** 추가 |
| 7 | #102 신설 | **예** — 「시접 자유 경첩 결핍」(§1 사실) + 그 보충이 **관통을 악화시킨다**는 실측을 같은 항목에 묶었다 |
| 8 | 이 판에서 호출한 ecc 에이전트·스킬 | **0 / 0** |
