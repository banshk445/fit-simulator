# v3-84 — unitScale 멱등화 · 계산 출처 계기 · 게이트 통과 증명 · c87.5-h155-s40 재굽기

| | |
|---|---|
| 브랜치 | `v3-84-unitscale-idempotent` |
| 기점 | **`8d2ae06`** (= v3-83 HEAD) |
| §0 커밋 | **`4a8ac19`** — 집행 «전» 별도 커밋(sha 만 그 뒤 한 줄로 채웠다) |
| 호출 ecc 에이전트/스킬 | **0** |

---

## §0 수령 (집행 «전» 별도 커밋)

### §0-1 판정문 (전략 세션 · v3-83 §4 수령 · 원문)

```
 「갈래 A 수령 · ①(나) = **공유 캐시 씬 «현장 변조»** · 「항등 폴백 유력」 = **전략 세션 귀책** ·
   ②(가) 「첫 칸」 확정 · ㉮ 재시도 = **처방으로 유지** · 제품 v1 **무영향**(blob 주입) ·
   함정 후보 ㄱ·ㄴ **채택** · 처방 = **rawHeight 를 지오메트리에서(멱등)** · **계산 횟수 불변** ·
   **sha 64자** · 1몸 재굽기 = ④(승인 시)」
```

★ **귀책 등재 1건** — v3-83 ① 의 「항등 폴백 유력」은 **전략 세션 귀책**으로 수령한다.
  (v3-81·v3-82 의 CC 귀책 2건과 «별개»이고, 어느 쪽도 상쇄하지 않는다.)
★ **④ 는 이 발주에 «포함된 채»로 왔다** ⟹ 승혁 승인분으로 받는다(미승인이면 삭제하고 발주하기로 돼 있었다).

### §0-2 잠금 «해제» 범위

| 파일 | 허용 |
|---|---|
| `Mannequin.tsx` | `:295-301` **rawHeight 산출 «대상» 교체** + **계기 1줄**(인스턴스 id) · 그 외 diff 0 · `lerp`/`snap`/`EPS` **0** |
| `V3Panel.tsx` | 게이트 = **v3-83 그대로** · 기록 **sha 64자만** 수정 |

### §0-3 바꾸지 않는 것

```
 물리 코어 4파일 · `framing`/`match`/`provide` · **108 · 35 · 37** · `body-index-27.json` · **문턱 0** ·
 몸 blob 27 **무변조**(④ 에서 **1개 교체**만 · **원본 보관**)
```

### §0-4 사전 등재 기대

**① 멱등화**

```
 새 적재 1회에 계산 **4회 전부** rawHeight **1.765052** · unitScale **0.963145** ·
 최종 `Scene.scale.x` **0.963145** · 두 구성(`?v2=1&v3=1` · `?canon=1&v3=1`) **동일**.
 정본 3종(`?canon=1`) **sha·정점 «불변»**(parseGlb 경로는 별개 — **불변이 회귀 증명**).
```

**② 출처 계기** — 계산마다 **인스턴스 id**(`useRef` 카운터) + StrictMode 여부 인쇄 · **4회의 (id, 회차) 표** 등재 · **판정 0**.

**③ 게이트 통과 증명** (`?bakedry=1` · 기본 순서 · 1회)

```
 본순회 27행 중 **첫 칸 `c87.5-h155-s40` 만 ㉮ 실패** · **재시도 1행 통과** ·
 그 외 **26행 ㉯㉰ 통과** · 높이 잔차 26행 **−0.0014 ~ +0.0020m** 안(v3-82 등재 범위 «인용») ·
 **최종 통과 27/27** · `unitScale` 실측 **전 행 0.963145**.
 ★ **통과가 27/27 이 아니면 ④ 착수 0.**
```

**④ 재굽기**(승인분) — ㄱ 몸 1개만 저장(나머지 26 blob 쓰기 0 · sha 전후 동일 증명) · 원본은
`public/v3diag/body-c87.5-h155-s40.v3-77.bin` 으로 보관 · 새 blob 높이 기대 **1.5489 ± 0.0020** ·
ㄴ 옷 4칸(맥 1호기 · 시간 상한 = v3-78 실측 최대 × 2 · 프레임 상한 400 · **기존 게이트·분류 규칙 그대로**) ·
ㄷ **정본 «수정 0» · 신설**(`index-merged-108.v3-84.json` 4행 교체 · `v1-provide-<n>.json`) ·
ㄹ 캡처 3장 · ㅁ **굽기 «결과값»은 사전 등재하지 않는다 — 규칙만**.

### §0-5 갈래 (집행 «전» 등재)

| 갈래 | 조건 | 처분 |
|---|---|---|
| **A** | ①②③(+④) 전량 | 상신 |
| B | ① 기대 불일치(4회 중 하나라도 ≠ 0.963145) | **정지** · 값 상신 |
| C | ③ 27/27 미달 | ④ 착수 0 · 상신 |
| D | ④ 몸 게이트 실패 2회 | 옷 착수 0 · 상신 |
| E | 그 외 | 정지 |

**사후 갈래 신설 0.**

---

## §1′ 집행

### §1-①′ 멱등화 — **성립**(단, 기대의 «한 항목»은 **읽는 대상을 정정**해야 참이 된다)

**바꾼 것** — `Mannequin.tsx` 삭제 줄은 **3줄**뿐이고 그중 동작 줄은 **1줄**이다:

```
 -    const box = new THREE.Box3().setFromObject(scene);      ← 월드 공간(씬 배율 포함)
 +    지오메트리 공간 bbox 합집합(`geometry.boundingBox` · 바인드 포즈 · 배율 «무관»)
   (나머지 삭제 2줄 = v3-83 계기 문자열 · 계기 교체분)
 `rawHeight` · `targetHeight` · `scale` 계산식과 `lerp`/`snap`/`EPS` 는 **한 글자도 안 바뀌었다.**
 계산 «횟수»도 손대지 않았다(판정문 조항).
```

**실측 — `?v2=1&v3=1` 새 적재 1회**

| 회차 | inst | rawHeight | 계산 시점 GLTF 씬 배율 | unitScale |
|---|---|---|---|---|
| 1 | **#1** | **1.765052** | 1.000000 | **0.963145** |
| 2 | **#1** | 1.765052 | 1.000000 | 0.963145 |
| 3 | **#2** | **1.765052** | **0.963145** | **0.963145** |
| 4 | **#2** | 1.765052 | 0.963145 | 0.963145 |

```
 ⟹ **4회 전부 rawHeight 1.765052 · unitScale 0.963145** — 사전 등재 기대 그대로.
   3·4회가 **이미 배율이 걸린 상태에서도 같은 값**을 낸다 ⟹ **멱등**이다(자기 상쇄 소멸).
 최종 **GLTF 씬(`Scene#10`) 배율 = 0.963145** · 스킨드 메시 월드 배율 = 0.009631(= 0.963145 × 0.01)
 `?canon=1&v3=1` — 계산 **2회**(inst#1 ×2) · 같은 값 · 최종 배율 **0.963145** ⟹ **두 구성 동일**.
```

```
 ★★ **측정 대상 정정 1건 — CC 귀책**
   v3-83 이 「최종 `Scene.scale.x` = 1」로 적은 값은 **R3F 루트 씬(`Scene#89`)** 을 읽은 것이다.
   `unitScale` 이 실제로 걸리는 곳은 **GLTF 씬(`Scene#10`)** 이고, 그 노드는
   `mannequinRootRef.current` 의 **부모가 아니라 자손**이다(사슬: `Ch36 ← Armature(0.01) ←
   **Scene#10(0.963145)** ← Group ← Group ← Scene#89(1)`).
   ⟹ **v3-83 §1-①′ 의 「자리」 결론(자기 상쇄)은 유지된다** — 그 근거는 계산 3·4회의
     `unitScale = 1` 과 굽기 산출물의 **1.038266배**였고, 둘 다 잘못 읽은 그 한 줄과 «무관»하다.
     정정되는 것은 **인용한 노드 이름 하나**다. 이 판의 기록은 정정된 대상으로 다시 쟀다.
```

**정본 3종 회귀 — sha·정점 «불변»**

| 원단 | sha | 정점 |
|---|---|---|
| gray d9 | `faf4873f…` | 10402 |
| swim d9 | `557bc446…` | 10402 |
| sweat d8 | `51d74c66…` | 13248 |

### §1-②′ 출처 계기 — **인스턴스 2 × StrictMode 2** 확정

| 구성 | 계산 횟수 | (inst, 회차) |
|---|---|---|
| `?v2=1&v3=1` | **4** | (#1, 1) (#1, 2) (#2, 3) (#2, 4) |
| `?canon=1&v3=1` | **2** | (#1, 1) (#1, 2) |

```
 ⟹ **같은 인스턴스가 두 번** 계산한다(= StrictMode 이중 호출) · **인스턴스 수는 화면 구성이 정한다**
   (v2 `FitCanvas` + `BakeMount` = 2 / canon + `BakeMount` = 1).
 ⟹ v3-82 의 「인스턴스 2개 가설 **기각**」은 **틀렸다** — 여기서 **정정**한다(CC 귀책).
   v3-83 이 「4회 계산이 그 가설을 되살린다(단정 0)」로 유보한 것이 맞았다. **판정 0**(사실만).
```

### §1-③′ 게이트 통과 증명 — **24/27** ⟹ 기대 **미달**

**드라이런(기본 순서 · blob 다운로드 0 · 기록 파일만 산출)**

| 항목 | 값 |
|---|---|
| 행 | **31**(본순회 27 + 재시도 4) |
| **통과 몸** | **24 / 27** |
| 실패 몸 | **3** — `c122.5-h185-s40` · `-s45` · `-s50` |
| `unitScale` 실측 | **전 행 0.963145** ✅ |
| 높이 잔차(첫 칸 제외) | **−0.0014 ~ +0.0020 m** — **v3-82 등재 범위와 «동일»** |
| 첫 칸 `c87.5-h155-s40` | 본순회 ㉮·㉯·㉰ 실패 → **재시도 통과**(전진 3 · 잔차 0) ✅ |

```
 ★ **실패 3칸의 사유는 하나뿐이다**: 「㉰ 높이 잔차 **0.0020m > 0.002m**」 — **경계 등호**다.
   실측 높이 1.8520 · 목표 1.85 ⟹ 잔차가 **문턱과 같은 자리**이고, 표시는 4자리 반올림이라
   0.0020 으로 찍히지만 게이트는 **반올림 «전»** 값을 비교한다 ⟹ 미세 초과로 떨어진다.
 ★ **문턱의 출처가 원인이다**: v3-82 문턱 = 「26몸 |잔차| **최대값**」 = **바로 그 칸의 값**을
   4자리로 «반올림»해 얻은 0.0020 이다. 최대값을 문턱으로 쓰면 **그 최대값 자신이 경계에 선다**.
 ★ **문턱을 움직이지 않는다**(함정 14). 결과에 맞춰 0.0021 로 올리는 것은 이 판의 정의역 밖이고,
   §0-5 는 이 경우를 **C** 로 미리 정해 뒀다.
 ⟹ **높이 재현 자체는 성공**이다 — 재생성 높이가 저장 자산의 값(1.5517 / 1.7019 / 1.8520 / 1.5500 /
   1.7000 / 1.8500 …)과 **같아졌다**. 떨어진 것은 «문턱 정의»이지 «몸»이 아니다.
```

**기록 파일**(§0-2 「sha 64자」 조항 충족)

| 파일 | 성격 |
|---|---|
| `public/v3diag/body-index-2026-08-30.json` | **정본** — 계기 정정 «후» · `unitScale` **0.963145** · sha **64자** |
| `public/v3diag/body-index-2026-08-30-pre.json` | 보관 — 계기 정정 «전»(그 필드가 1 로 찍힘 · 한계 명기) |

```
 ★ 계기 결함 1건 «수리»: 기록의 `unitScale실측` 이 **R3F 루트 씬**을 읽어 늘 1 이었다 ⟹
   **GLTF 씬**을 읽도록 고쳤다(`V3Panel.tsx` · 커밋 `947f33b`). §1-①′ 의 정정과 **같은 뿌리**다.
 ★ 사소한 사실 1건: 파일명의 날짜는 `toISOString()`(**UTC**)이라 로컬 자정 직후엔 하루 늦다.
```

### §1-④ 재굽기 — **착수 0**

```
 §0-5 **C**: 「③ 27/27 미달 ⟹ ④ 착수 0 · 상신」. **몸도 옷도 굽지 않았다** ·
 원본 blob 보관도 하지 않았다(교체 대상이 없다) · 정본 신설 0 · 캡처 0.
```

---

## §2′ 자기검사

| # | 항목 | 결과 |
|---|---|---|
| ㉠ | ① 표 | 4회 전부 1.765052 / 0.963145 · 최종 GLTF 씬 0.963145 · 두 구성 동일 |
| ㉡ | ② 표 | (#1,1)(#1,2)(#2,3)(#2,4) · canon 2회 |
| ㉢ | ③ 표 | **24/27** · 실패 3(경계 등호) |
| ㉣ | 정본 3종(화면) 불변 | gray/swim/sweat **sha·정점 전부 일치** |
| ㉤ | `Mannequin.tsx` diff | **삭제 3줄**(동작 1 + 계기 2) — 원문 위 §1-①′ |
| ㉥ | 물리 코어 · `framing`/`match`/`provide` diff | **0 · 0** |
| ㉦ | 108 · 35 · 37 · `body-index-27.json` diff | **0** |
| ㉧ | 몸 blob 27 변경 | **0건** |
| ㉨ | 회귀 | `tsc` 통과 · v3-81 **전량 통과** · v3-80 프레이밍 **통과** · v3-81 canon 잘림 **0/3** |

**§3 캡처 — 0장**(④ 미착수 ⟹ 사전 등재대로).

---

## §4′ 판정 — **갈래 C**

```
 §0-5 **C = ③ 27/27 미달 ⟹ ④ 착수 0 · 상신**. 실측 **24/27** 이므로 C 다.
 ①·② 는 기대대로 성립했고 회귀도 전량이지만, **③ 의 문면이 갈래를 가른다.**
 사후 갈래 신설 0 · **문턱 이동 0** · 기대 사후 변경 0.
```

### §4-1 넘기는 것 — ★ ㉠ 이 ④ 재개의 «유일한» 관문

| # | 항목 | 몫 |
|---|---|---|
| **㉠** | **㉰ 문턱의 정의** — 「26몸 |잔차| **최대값**」은 그 최대값 자신을 경계에 세운다. 재정의(예: 최대값 + 여유 / 반올림 자리 명시 / 다른 도출)는 **전략 세션 몫**이고 CC 는 움직이지 않았다 | 전략 세션 |
| ㉡ | 그 재정의가 서면 **④ 는 그대로 집행 가능**하다(①③ 의 나머지는 전부 성립) | 전략 세션 → 승혁 |
| ㉢ | **CC 귀책 2건 등재** — v3-82 「인스턴스 2개 가설 기각」 · v3-83 「Scene.scale.x = 1」(노드 오독) | 등재 |
| ㉣ | 전략 세션 귀책 1건(v3-83 ① 「항등 폴백 유력」)은 §0-1 로 수령 완료 | 등재 |
