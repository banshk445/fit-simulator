# 19 — 장면 조립 추출 (부분 접속 2/5)

v3-34. `scripts/v3S4.ts`의 **장면 조립**(제도·배치·봉제·물성·파라미터 산정)을
`src/v3/`의 **순수 모듈**로 옮긴다. **판정 기준은 「비트 동일」 하나다.**

호출한 ecc 에이전트/스킬: **0**

---

## §0 판정 기준 — **추출 «전»에 등록**

> **이 절은 추출 코드를 «한 줄도 쓰기 전»에 커밋한다.** git 이력이 순서의 증거다.
> §1~§3 결과를 보고 이 절의 어떤 수도 고치지 않는다.

### ㉠ 주 기준 — **비트 동일**

추출 «전»과 «후»가 **같은 입력에서 같은 출력**이어야 한다. 대조 채널 전량:

```
 ㉠-1  정점 위치 해시      전체 정점 좌표의 바이트 해시(Float64 원본 · 반올림 0)
 ㉠-2  O1 밑단둘레 · O2 파장(영교차 수 포함) · O3 간극중앙 · O4 접촉
 ㉠-3  ③a 절대 관통 · ③b 비 · P_ext
 ㉠-4  자기관통(삼각형 교차 수) · 비인접 최소거리
 ㉠-5  시접 간극 중앙 · λmax · 목선 원주비
 ㉠-6  정착 판정 상태(창 순변위) · 프레임 번호
 ㉠-7  제도·배치 산출(정점 수 · 삼각형 수 · 제약 수 · 이음선 쌍 수 · 최소각 · 종횡비 ·
        DELTA · GAP_SIDE · SLV_X0 · SLV_R · 파생 치수 4종 · 서브스텝 수 · RAMP_N)
 **「거의 같다」는 통과가 아니다.** 한 채널이라도 다르면 갈래 B.
```

### ㉡ 순수성

```
 추출된 모듈에  `node:` 임포트 **0** · 파일 시스템 접근 **0** · `process.env` 직독 **0**
 설정은 전부 **인자로** 받는다. **grep으로 값을 낸다**(주장하지 않는다).
```

### ㉢ v2 무영향

```
 v2 코드 임포트 **0** · v1/v2 게이트 경로 무영향(diff로 확인) · 구 경로 비트 동일성 유지
```

### ㉣ 검증 비용 — **N을 «지금» 도출한다**

**전량 정착(4종 136.2분 · gray 1점 18분)을 돌리지 않는다.** 근거와 N:

```
 근거  솔버는 결정적이고(난수 0 · v3-22~33 확인분) 발산이 0이다. 상태가 프레임 N에서
       «비트 동일»이면 이후 프레임은 **같은 순수 함수의 반복**이므로 영원히 같다.
       따라서 N은 「조립이 만드는 것이 «전부» 소비되는」 지점이면 족하다.
 조립의 시간 의존 산출은 **하나뿐이다** — 점진 봉제 램프(`RAMP_N` 프레임 동안
       이음선 정지 길이가 매 프레임 바뀐다). 그 밖(메시·배치·제약·물성·서브스텝)은
       **f = 0에 한 번** 만들어지고 변하지 않는다.
 ⟹ **N = RAMP_N + N_WIN**
       RAMP_N  d11에서 **76**프레임 (v3-33 실행 헤더 등재분)
       N_WIN   **10**프레임 = round(1/(DAMP·DT)) — 정착 판정 창(v3-22 등록분)
       ⟹ **N = 86** — 램프 전량 + 램프 «후» 정착 창 한 바퀴를 지난다
 비용  d11 gray 4.90 s/f × 86 ≈ **421초 ≈ 7분** · 전후 2회 = **약 14분**
 전량 1회 확인 여부는 **§3이 판단**하고 근거를 적는다(비용 18분)
```

### ㉤ 순서

```
 이 커밋(§0 · 추출 코드 0줄)  →  §1 판독  →  §2 추출  →  §3 검증
 §3 결과를 보고 ㉠~㉣을 고치지 않는다.
```

### 이 판에서 바꾸지 않는 것

로직 · 연산 순서(배열 순회·누적) · 이름 · 문턱 · 해상도 d11 · 잡음 바닥 ·
v2 코드(임포트 0) · UI(3/5 몫) · CC 자체 화면 판정 0.

---

## §1 판독 — 무엇이 「조립」이고 무엇이 「측정」인가

`scripts/v3S4.ts` **1,992줄**(v3-34 시작 시점 2,196줄)을 역할별로 분류했다.

### §1-1 분류표

| 역할 | 줄 범위(추출 «전») | 내용 | 처분 |
|---|---|---|---|
| **조립** | 134–411 | 몸 링 계기(`planeSection`·`ringOf`·`wallFracAt`) · 어깨끝 · 목 밑동 · 파생 치수 4종 · 곡선족 · 경계 곡선 | **추출** |
| **조립** | 488–922 | `Panel`/`coons`/`build`(메시) · 배치면(`supportAt`·`boundaryOf`·`arcOn`·`fitDelta`) · `fitSleeve` · `place` · `assemble`(봉제) · 옆 틈 G 고정점 | **추출** |
| **조립** | 924–947 | `meshQuality` · `substepsOf`(파라미터 산정) | **추출** |
| 측정·인쇄 | 413–438 · 471–486 · 949–962 | §1 · §1-B · §2 인쇄 블록 | 남김 |
| 측정·인쇄 | 439–469 | `slice` · `armAxis`(§1-B 전용 진단) | 남김 |
| 측정·인쇄 | 964–1110 | §0 해상도 도출 인쇄(`run('0')`) | 남김 |
| 측정 | 1112–1389 | 계기 전량 — `nearestBodyPoint` · `exactBodyDist` · `bodyClearance` · `minPairDist` · `edgeDihedrals` · `thetaAt` · `predExt` · `triTriHit` | 남김 |
| 실행 제어 | 1391–2196 | 체크포인트 · 진행 로그 · 착장 루프 · 시험 7종 · 캡처 | 남김 |
| 실행 제어 | 65–131 | env 읽기 · GLB 읽기 · SDF 굽기 · `V2REF` | 남김 |

### §1-2 하네스가 조립부에서 쓰는 식별자 — **28개뿐**

기계적으로 세었다(최상위 선언 중 1391줄 이후에 등장하는 것):
`run G DT KMEM MAT THICK SEP MU DAMP TOL_SELF V_SETTLE prim0 bodyIdx bodyG V2REF
Y_TOP ARM_D neckF LEN_NECK slice at Y_HEM AXIS_Z PROBE PLACE_SIG assemble substepsOf D_CHOSEN`
⟹ **경계가 좁다.** 추출이 성립할 근거다(갈래 E 불성립).

### §1-3 `process.env` 직독 위치 — 전량 열거

```
 조립부 안(추출 대상)  DIAG=1 · DIAG=2 두 블록 — **진단 인쇄**다. 조립이 아니다
                       ⟹ 하네스로 «되돌렸다»(계기 정의 뒤에 둔다 — `minPairDist`를 쓴다)
 조립부가 «값»으로 쓰던 env  GLB · MATK · MATRHO · MATB · D_MM · GL/GW/GSW/GSLEN/GARMG · V2DIMS · BSX/BSY/BSZ
                       ⟹ 전부 **하네스가 읽어 `cfg` 로 넘긴다**(모듈의 `process.env` 직독 0)
 추출 후 모듈의 `process` 등장  **0**(주석 2곳뿐 — §3-2 grep)
```

### §1-4 **모호분 2건** — 어느 쪽으로 넣었는지와 근거

```
 ① `minPairDistLite` / `minPairDist`
    조립(옆 틈 G 고정점이 «판정 조건»으로 쓴다) ↔ 측정(S4 ③ 자기관통 판정 채널) **양쪽**이다.
    처분: **계기는 하네스에 남기고, 조립은 그것을 «인자로» 받는다**(`cfg.minPairDistLite`).
    근거: 정의를 둘로 복제하면 «같은 이름의 다른 계기»가 생긴다 — 함정 13 계열이다.
    복제 0 · 순수성 유지(함수 인자는 I/O가 아니다).
 ② `meshQuality`
    측정 이름이지만 `assemble`(890·893줄)과 `substepsOf`(943줄)가 **조립 안에서** 쓴다.
    처분: **모듈로 넣고 export** 한다. 하네스의 §0 인쇄 블록이 그것을 import 해 쓴다.
    근거: 조립의 산출(최소각·종횡비)이 서브스텝 산정에 들어가므로 조립 경로에 «있어야» 한다.
 ③ SDF 굽기(`deriveSpacing` + `bakeSdf`)
    조립의 «입력»이지만 GLB 읽기(`node:fs`)와 붙어 있다.
    처분: **하네스에 남기고 `bodyG`를 인자로 넘긴다.** 두 함수는 이미 `src/v3/bodySdf.ts`의
    순수 코드라 3/5(브라우저)에서 그대로 부를 수 있다 — 추출의 장애가 아니다.
```

---

## §2 추출 — **동작 변경 0**

신설 `src/v3/garmentScene.ts` (**776줄**) · `scripts/v3S4.ts` 2,196 → **1,516줄**.

### §2-1 「로직 변경 0」의 근거

```
 옮긴 줄은 **원문 그대로**다. 파이썬으로 줄 범위(134–411 · 488–947)를 «잘라» 붙였고
 편집한 것은 들여쓰기 2칸(함수 본문이 됐다)뿐이다.
 바뀐 것은 «닫는 방식»뿐:
   모듈 최상위 `const` → `createScene(cfg)` 의 «지역» `const`
   `process.env` 직독   → `cfg` 인자
   `prim0` `bodyIdx` `bodyG` `sdfSpec` → `cfg` 인자(같은 이름으로 바인딩해 본문 무수정)
 **하지 않은 것**: 최적화 0 · 정리 0 · 이름 개선 0 · 죽은 코드 제거 0.
```

### §2-2 부동소수 «연산 순서» 보존

```
 배열 순회 순서 · 누적 순서 · 반복 횟수 · 분기 순서 전부 원문 그대로다
 (줄을 옮겼을 뿐 재배치하지 않았다).
 새로 «계산»하는 코드는 0줄이다 — 추가된 것은 `cfg` 구조분해 1줄과 `return {…}` 뿐이다.
 ⟹ 비트 동일이 «성립할 수 있는» 구조다. 실제 성립 여부는 §3이 값으로 낸다.
```

### §2-3 하네스에 남은 것

```
 GLB 읽기(`node:fs`) · 몸 축 비례(BSX/BSY/BSZ) · SDF 굽기 · env → cfg 조립
 계기 전량 · 시험 7종 · 체크포인트 · 진행 로그 · 캡처 · 인쇄 블록 전량 · DIAG 블록 2종
 하네스는 조립을 **`createScene(cfg)` 한 번 호출**하고 결과를 구조분해해 쓴다.
```

