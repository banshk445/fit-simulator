# 56 — 본 그리드 파이프라인 · 차트 정본 · 무인 순회 · 스모크 3칸 (v3-77)

v3-77. 브랜치 `v3-77-grid-pipeline`(사전 등록 · **이 CC 세션 전용**). 호출한 ecc 에이전트/스킬: **0 / 0**

## git 스탬프

```
 브랜치  v3-77-grid-pipeline        워크트리  /Users/banshk/projects/fit-simulator
 기점    4d9d443 (v3-76 HEAD)       직전 회차  4d9d443
```

---

## §0 판정·사무 수령 — **집행 «전» 커밋**

### §0-1 **전략 귀책 6건째** 등재

```
 **내용**: v3-76 §0-3 ① 이 「사이즈 차트 8행 «정본 등재»」를 지시하면서 **실측표의 값을 함께 보내지 않았다**.
 ⟹ CC 는 **등재 불가**로 명시하고 갈래 E 로 처리했다(**차트를 만들지 않았다**).
 **귀책 = 전략 세션** · 계열 = **「인용·등재 «가정»」**(전달됐다고 가정하고 지시문을 썼다).
 **#82 확장 병기** — 「자기검사 «기준값» 인용은 «정본 sha 붙은 셀»만 · 회차 «번호»로 인용할 때는
   그 회차가 정본을 승격·강등하지 않았는지 확인을 함께 등재」와 **같은 뿌리**다:
   **«무엇이 이미 등재됐는지»를 확인하지 않고 인용/지시하는 것**.
 **선행 5건**: #117 · #118 · #82확장 · #119·#120 · v3-67 경미. ⟹ **6건째.**
```

### §0-2 ★★ 사이즈 차트 8행 — **정본 등재**(값 수령 · 출처 = 사용자 실측표 · cm)

| 사이즈 | **L** 총장 | **SW** 어깨 | **W** 품 | **화장** | **SLEN = 화장 − SW/2** |
|---|---|---|---|---|---|
| **XS** | 63 | 40 | 45.5 | 39 | **19.0** |
| **S** | 65 | 41.5 | 48.5 | 40 | **19.25** |
| **M** | 68 | 43 | 51.5 | 42 | **20.5** |
| **L** | 71 | 44.5 | 54.5 | 44 | **21.75** |
| **XL** | 74 | 46.5 | 58.5 | 46 | **22.75** |
| **XXL** | 76 | 48.5 | 62.5 | 47 | **22.75** |
| **3XL** | 76 | 50.5 | 66.5 | 48 | **22.75** |
| **4XL** | 76.5 | 52.5 | 70.5 | 49.5 | **23.25** |

```
 **도출식 병기**: **`SLEN = 화장 − SW/2`**(v3-73 §0-3 등재분 그대로).
 **`ARM_G` = 제도 기본값 `0.4439` · «전 사이즈 공통»** — v3-73 §0 처분 **유지**.
 ★ **한계 등재(재확인)**: **차트가 암홀 둘레를 주지 않는다**(v3-69 결손 3 ㉡) ⟹
   `ARM_G` 는 **차트에서 도출한 값이 아니다**. 사이즈가 커져도 **암홀은 그대로**다.
   **이 한계가 v3-75 `chest140` 의 「암홀 반쪽 22.20 < 소매산 반폭 27.31cm」와 같은 자리에 있다**
   (**단정 0** — 다만 두 사실이 같은 축을 가리킨다는 것은 적어 둔다).
 ★ **관측 1건**(사실만): **XXL·3XL 은 총장 76 으로 같고 SLEN 도 22.75 로 XL 과 같다** —
   차트가 그 대역에서 **총장·화장을 거의 고정**하고 **폭만 늘린다**.
```

### §0-3 비대칭 단서 **수용**

```
 **가슴둘레 = «전이 축»으로 그리드에 포함**한다.
 근거: v3-76 §1-(b′) 의 **크기 순위 5.8%(꼴찌)는 «편측 측정»**이고(70만 쟀다),
   반대쪽 140 은 **착용 불가로 전이**했다 ⟹ **전이 기준에서는 가장 센 축 후보**.
```

### §0-4 예산 **승인 수령**(사용자)

```
 **안 B** — **몸 27칸 × 4사이즈(S / M / L / XL) = 108칸 · 약 58시간**.
 ⟹ **XS · XXL · 3XL · 4XL 은 이번 굽기 대상이 «아니다»**(차트에는 정본으로 남는다).
```

### §0-5 ★ 그리드 «도출» — 규칙과 값

```
 **축 = 가슴둘레 · 키 · 어깨너비**. 포함 근거 **각 1줄**:
   **가슴둘레** — 전이 축(140에서 착용 불가) · 크기 순위는 편측이라 과소평가돼 있다(§0-3)
   **키**       — 크기 순위 **1위 283.5%**(v3-76) · 전이도 있다(200 착용 불가)
   **어깨너비** — **유일하게 양끝이 살아 있는 축**이고 크기 순위 **2위 118.2%**
 **제외 축**: **다리 길이 · 팔 길이 = 기본값 «고정»**(매칭 미사용 · v3-76 §0-3 ③ 기등재 규칙).
 **점 규칙** = **{ min + ¼R · 기본값 · max − ¼R }** (R = `Controls.tsx:128-137` 등재 범위)
 **끝값 회피 근거**: ① `height140` **182.0분 · 정착 미도달**(v3-76 §1-(c)) ②
   **4/5 축이 끝값에서 착용 불가**(v3-76 §1-(a)) ⟹ 끝값은 **비싸고 자주 못 쓴다**.
```

| 축 | min | max | R | ¼R | **도출 점 3** | 참고 기대값 | 일치 |
|---|---|---|---|---|---|---|---|
| 가슴둘레 | 70 | 140 | 70 | 17.5 | **87.5 / 100 / 122.5** | 87.5/100/122.5 | ✅ |
| 키 | 140 | 200 | 60 | 15 | **155 / 170 / 185** | 155/170/185 | ✅ |
| 어깨너비 | 35 | 55 | 20 | 5 | **40 / 45 / 50** | 40/45/50 | ✅ |

```
 ⟹ **몸 3 × 3 × 3 = 27칸** · **× 사이즈 4(S/M/L/XL) = 108칸**. **예산 안 B 와 일치.**
 다리 길이 **85**(기본) · 팔 길이 **60**(기본) 고정.
```

### §0-6 v1 **정본 편입 기준** 등재

```
 **편입** — **게이트 «전량» 통과**(③a · **③b = «그 칸» h × 25%** · 교차 · settleNet · ringExcess)
            **+ blob sha 등재** ⟹ **v1 정본으로 편입**.
 **착용 불가** — **조립 불능** ⟹ 「이 몸에는 착용 불가」 **상태로 등재**(**사유 값 병기** ·
            v3-76 §0-2 대로 **제품의 정당한 상태**).
 **보류** — **정착 «미도달»** ⟹ **보류 목록**(**처방 0** · 그 칸을 버리지도 편입하지도 않는다).
 ⟹ **세 상태는 «배타»**다. 칸마다 정확히 하나로 분류된다.
```

### §0-7 §1·§2 사전 등록

```
 **§1 무인 순회**(v3-69 **결손 ④ 해소**)
   · **108칸 목록 «사전 생성»** · **몸 «우선» 순회**(몸 굽기 **1회 재사용** — 몸 1개당 사이즈 4)
   · 칸별 **체크포인트**(blob + 게이트 결과 + sha) · **재시작 시 완료 칸 «건너뜀»**(재개)
   · **칸 실패가 순회를 죽이지 않는다**(등재 후 계속) · **진행 로그**(칸/시각/결과 1줄)
   · **개시 시퀀스 준수**(v3-73 §0-2): **가시화 계기 1회 → 루트 생성 → 동기 굽기 «반복»**
 **§2 스모크 3칸**(**본 굽기 아님**)
   ㉠ **기본 몸(100/170/45) × M** — 정상 경로(v3-74 기준 칸과 **옷만 다르다**)
   ㉡ **작은 몸(87.5/155/40) × XL** — 여유 «극단»
   ㉢ **큰 몸(122.5/185/50) × S** — **착용 불가 경로 «예상»**(그 상태 등재가 작동하는지)
   각 칸: **편입 기준 적용 · 체크포인트 기록** · **중단 후 재시작 1회 실험**(㉡ 도중 kill ⟹ 재개 검증)
```

### §0-8 §4 갈래 (사전 등록)

| 갈래 | 조건 | 귀결 |
|---|---|---|
| **A** | 스모크 3칸 처리 완료(**편입/착용불가/보류 «정확» 분류**) · **재개 검증 통과** | **상신 · 본 굽기 대기** |
| **B** | 오케스트레이터 **결함**(재개 실패 · 분류 오류) | 정지 · 사실만 |
| **E** | 판정 불가 | 정지 · 사실만 |

### 이 판에서 바꾸지 않는 것

**`solver` / `garmentScene` / `s4Gate` / `bodyLevels` diff 0** · **문턱·정본 0** · **정본 3종 불변** ·
**제품 화면 0줄** · **`Mannequin` 로직 0줄**.

---

## §1′ 무인 순회 구현 — **결손 ④ 해소**

### §1′-1 변경 파일 **3개**

```
 **신설** `src/v3/grid.ts` — **순수 모듈**(node: 0 · 파일 0 · three 0 · **v2 임포트 0**).
   차트 8행 · 축 범위 · 점 규칙 · `garmentOf(size)` · `bodies()` 27 · `cells()` 108.
   ⟹ **브라우저(굽기 계기)와 Node(오케스트레이터)가 «같은 목록»을 만든다**(#65 계열).
   **이 파일이 새로 정하는 수 0** — 전부 등재분 인용.
 **신설** `scripts/v3GridRun.ts` — 오케스트레이터.
 **수정** `src/components/V3Panel.tsx` — 「그리드 몸 굽기(27칸)」 계기(하네스).
```

**기점 `4d9d443` 대비 diff 0 확인**: `solver` **0** · `garmentScene` **0** · `s4Gate` **0** ·
`bodyLevels` **0** · `dressRun` **0** · `Mannequin` **0** · `V3Product` **0** · `App` **0**.
`tsc -b` rc=0 · `build` rc=0.

### §1′-2 결손 ④ 세 항목 — **어떻게 메웠는가**

```
 ㉠ **재개** — 칸별 결과를 `public/v3diag/v3-77/index.json` 에 **즉시 기록**(`save()` 매 칸).
    재시작하면 **`idx[c.id]` 가 있는 칸을 건너뛴다**.
 ㉡ **칸 실패가 순회를 죽이지 않는다** — `prepare` throw 를 **잡아서 «착용 불가»로 분류**하고
    **`continue`** 한다(v3-76 §0-2 「제품의 정당한 상태」 그대로).
 ㉢ **순회 드라이버** — 목록·명명·인덱스를 **순수 모듈**이 준다.
    **몸 «우선» 순회** + `bodyCache` ⟹ **몸 하나를 읽어 사이즈 4에 재사용**(파일 읽기 1/4).
 **정지 규칙**은 v3-75 정정분 그대로 — `pass || settleNet ≤ 등재 문턱`(**새 문턱 0**).
```

### §1′-3 그리드 몸 굽기 — **27칸 전량**

```
 개시 시퀀스(v3-73 §0-2) 준수 — **가시화 계기 1회 → 루트 생성 → 동기 굽기 «반복»**.
 팔 **60** · 다리 **85** 고정(`FIXED`) · **T포즈** · 자세 되돌린 본 **2개**(전 칸 동일).
 ★ **상한 초과 1칸**(굽되 사실 남김 · 함정 25):
   **`c87.5-h155-s40` — 전진 600프레임 · 잔차 8.60e-2** ⟹ **그 몸은 슬라이더 목표에
   «완전히» 도달하지 않았을 수 있다**. **공교롭게 그 몸이 스모크 ㉡ 의 몸이다**(사실 병기).
   나머지 26칸은 4~64프레임에 잔차 0.
```

---

## §2′ 스모크 3칸 — **집행**

| # | 칸 | 몸(가슴/키/어깨) | 사이즈 | **분류** | f | 초 | 게이트 · 사유 |
|---|---|---|---|---|---|---|---|
| **㉡** | `c87.5-h155-s40_XL` | 87.5 / 155 / 40 | **XL** | **편입** | 200 | **2318.8**(38.6분) | **pass** |
| **㉠** | `c100-h170-s45_M` | 100 / 170 / 45 | **M** | **편입** | 180 | **1705.7**(28.4분) | **pass** |
| **㉢** | `c122.5-h185-s50_S` | 122.5 / 185 / 50 | **S** | **착용불가** | — | ~0 | 「소매산 실수해 없음 — **암홀 반쪽 22.20cm < 소매산 반폭 28.37cm** — 갈래 D」 |

```
 **분류 = 편입 2 · 착용불가 1 · 보류 0.** **사전 등록 예상과 일치**한다:
   ㉠ 정상 경로 ⟹ **편입** ✅   ㉢ 착용 불가 «예상» ⟹ **착용불가** ✅
   ㉡ 「여유 극단」은 **예상보다 좋게 나왔다** — **게이트 전량 통과 · 편입**.
      (작은 몸에 XL 을 입혀도 물리·게이트가 성립한다는 **관측**이다. **기전 귀속 0.**)
 ★ **㉢ 의 값이 v3-75 `chest140` 과 «같은 자리»**다 — 암홀 반쪽 **22.20cm**(전 사이즈 공통 `ARM_G`
   에서 나온다) 대 소매산 반폭 **28.37cm**(S 의 SW 41.5 에서 나온다).
   **§0-2 의 「ARM_G 한계」가 착용 불가의 «직접 원인 자리»에 있다**(단정 0 · 값 병기).
```

### §2′-2 blob sha — **파일 ↔ 인덱스 대조**

| 칸 | 파일 sha | 인덱스 sha | 헤더 | 판정 |
|---|---|---|---|---|
| `c87.5-h155-s40_XL` | `5012b82b3a11863a…` | `5012b82b3a11863a…` | f=200 · n=**11118** | **일치** |
| `c100-h170-s45_M` | `9d3b07e45b67e0c3…` | `9d3b07e45b67e0c3…` | f=180 · n=**9388** | **일치** |
| `c122.5-h185-s50_S` | — | — | — | blob 없음(**착용불가** · 정상) |

### §2′-3 ★ 재개 검증 — **2단계 «양방향» 통과**

```
 **1단계 — 미완 칸은 «기록되지 않는다»**
   ㉡ 을 돌던 중 **5분 지점에서 kill**했다. 그 시점 실측:
     `index.json` **미생성** · 완료 칸 **0** · `settled-*.bin` **0개**
   재시작 로그가 **「완료분 0칸」**으로 시작해 **㉡ 을 처음부터** 다시 돌았다.
   ⟹ **반쯤 된 칸을 «완료»로 오인하지 않는다.**
 **2단계 — 완료 칸은 «건너뛴다»**
   3칸이 끝난 뒤 **같은 명령을 다시** 실행:
     `[그리드] 대상 3칸 · 완료분 **3칸**`
     `c87.5-h155-s40_XL  **건너뜀**(편입)` / `c100-h170-s45_M  **건너뜀**(편입)` /
     `c122.5-h185-s50_S  **건너뜀**(착용불가)`
     `[그리드] 종료 — 편입 2 · 착용불가 1 · 총 3칸`
   ⟹ **즉시 종료**(물리 0프레임). **착용 불가 칸도 «완료»로 취급돼 다시 돌지 않는다.**
 ⟹ **재개가 양방향으로 성립한다.**
```

---

## §3′ 판정 — **갈래 A**

| 사전 등록 조건 | 실측 | 충족 |
|---|---|---|
| 스모크 3칸 처리 완료 · **편입/착용불가/보류 «정확» 분류** | 편입 2 · 착용불가 1 · **예상과 일치** · sha 대조 일치 | ✅ |
| **재개 검증 통과** | **1단계**(미완 미기록) · **2단계**(완료 건너뜀) **양방향** | ✅ |

⟹ **갈래 A — 상신 · 본 굽기 «대기»**.

**본 굽기 입력 — 지금 서 있는 것**

```
 · 차트 **8행 정본** + `garmentOf(size)` 도출식
 · 그리드 **27 몸 × 4 사이즈 = 108칸** 목록(순수 모듈 · 브라우저/Node 공용)
 · 그리드 몸 **27칸 전량 구움**(`public/v3diag/v3-77/body-*.bin`)
 · 오케스트레이터(체크포인트 · 재개 · 실패 계속 · 몸 우선) — **검증 완료**
 · 편입 기준 3상태 — **스모크에서 두 상태가 실제로 나왔다**(편입 · 착용불가)
 · **이미 처리된 3칸**은 본 굽기에서 **자동으로 건너뛴다**(105칸 남는다)
 **예산 참고 실측**(이 판 2칸): **28.4분 · 38.6분** — 착용 불가 칸은 **≈0분**.
 **칸 수 × 시간 산정은 하지 않는다**(#81).
```

**★ 상신하는 «사실» 4건**

```
 ① **㉡ 「여유 극단」이 편입됐다** — 작은 몸(87.5/155/40) × XL 이 **게이트 전량 통과**.
    사전 예상(극단)보다 **좋은 결과**다. **기전 귀속 0.**
 ② **㉢ 착용 불가의 값이 v3-75 `chest140` 과 같은 자리** — **암홀 반쪽 22.20cm**(전 사이즈 공통
    `ARM_G` 에서 나온다) 대 **소매산 반폭 28.37cm**. **§0-2 의 ARM_G 한계가 그 자리에 있다**(단정 0).
 ③ **그리드 몸 굽기 상한 초과 1칸** — `c87.5-h155-s40`(잔차 8.60e-2) · **스모크 ㉡ 의 몸**.
    그 칸이 **편입됐다는 사실과 함께** 적는다.
 ④ **재개가 양방향으로 검증됐다** — 미완 미기록 · 완료 건너뜀.
```

**바꾸지 않은 것**: `solver` / `garmentScene` / `s4Gate` / `bodyLevels` **diff 0** ·
**문턱·정본 0** · **정본 3종 불변** · **제품 화면 0줄** · **`Mannequin` 로직 0줄**.
