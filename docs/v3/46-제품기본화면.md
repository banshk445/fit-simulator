# 46 — 5/5-β-1 제품 «기본» 화면 = v3 정본 재생 (v3-66 · **물리 0프레임 · v2 코드 무삭제**)

v3-66. 브랜치 `v3-66-product-default`(사전 등록 · **이 CC 세션 전용**). 호출한 ecc 에이전트/스킬: **0 / 0**

## git 스탬프

```
 브랜치  v3-66-product-default      워크트리  /Users/banshk/projects/fit-simulator
 기점    f42ec75 (v3-65 HEAD)       직전 회차  f42ec75
 커밋    (§0 → §1 → §2 → §3 순서로 채운다)
```

---

## §0 판정 수령 — **집행 «전» 커밋**

### §0-1 ★ **5/5-α 완결 «선언» 등재** — CC 는 **등재만** 한다

```
 **선언**: **5/5-α(프린트 축) 완결.**
 **귀속**: **사용자 화면 합격** + **전략 세션 판정**. **CC 는 선언 주체가 아니다**(화면 판정 0 규약).
 **완결의 내용**(회차별 근거):
   v3-59  프린트 UV 정규화 층 신설 · 패널 분리 렌더 · **G7 신설**(①②③)
   v3-60  합성 층 신설(대표색 + 가슴 프린트 + 브리지 복원) — 갈래 **E → A**(v3-63 전환)
   v3-62  알파 합성 «구조적» 확인 · 자산 선별 규칙 ㉠㉡㉢ 신설
   v3-63  사무 이월(v3-61 결번 · 기준값 재등재 · #82 확장)
   v3-64  자산 통과·합성 완주 · **대표색 폴백** 구조 사실 · back 반점 판정 불가
   v3-65  **상하 반전 해소**(부호표 3층 · ㉮ 단독) · **G7 ③ 정정** · 방향 자기검사 신설
 **귀책 4건 병기**(전부 **전략 세션** · 은폐 0):
   ① **#117** 게이트 문턱이 판정 정의역과 겹쳤다
   ② **#118** 상관을 재려는 두 채널이 독립이 아니었다
   ③ **#82 확장** 자기검사 기준값을 «셀» 없이 인용해 v3-60 을 갈래 E 로 오정지시켰다
   ④ **#119 · #120** G7 ③ 부등식을 v2 유도와 **정반대**로 등록했고(#120),
      **방향이 모호한 자산**으로 화면 판정을 해 반전을 못 걸렀다(#119 · v3-59 「정립 확인」 무효)
 **미해소로 «남기고» 넘어가는 것 1건**: **대표색 = 폴백 rgb(128,128,128)**(v3-64 §2-3).
   규칙 ㉢(테두리 투명)과 「대표색이 옷 색을 준다」는 **양립 불가**다. **α 정의역 밖**으로 등재됐고
   **처방 0** — 「옷 색을 어디서 받는가」는 **별도 축**이다.
```

### §0-2 β-1 범위 수령

```
 **제품 «기본» 화면(쿼리 플래그 «없음»)을 v3 정본 재생으로 전환**한다.
 **기확정 처분 인용**(이 판이 새로 정하지 않는다):
   **Q3 = 최종 «대체»** — 이 판은 **이관 1차**다. **v2 코드 무삭제**.
     화면 소비자 이관이 «완료»된 시점에 v2 를 **하네스 전용으로 강등**한다 — **그 시점은 이 판이 아니다**.
   **R5 / R8** — 원단 UI 는 **v3 프리셋 3종 직접 노출** · **denim 칸 제외**(#78) · **프리셋 코드 무삭제**.
```

### §0-3 이 판이 **하지 않는** 것

```
 **새 물리 실행 0**(재생·주입만 · **Q2 미결**) · **핏 리포트 «표» 이관 0** ·
 **프린트 UI 통합 0**(표시 토글은 **v3 패널 것 재사용**) · **v2 화면 제거 0**.
```

### §0-4 §1 전환 (사전 등록)

```
 ① `App.tsx` 기본 렌더 = **v3 productView**(blob 재생 · v3-41~43 검증 경로 그대로)
    구 v2 씬은 **`?v2=1` 플래그로 «보존»** — **플래그 신설 = 「이관 중」 상태의 «명시»** · 기본값 아님
 ② 원단 선택 UI = **gray / swim / sweat 3종**(라벨 = **등재 물성 명** · `fabricPresets` 매핑 경유 **금지** ·
    **denim 항목 렌더 제외** · **프리셋 코드 무삭제**)
 ③ 선택 → 해당 **정본 blob** 로드 · **sha 검증 후 표시**(불일치 시 **오류 표시** · **폴백 0**)
 ④ 핏 맵 색 / 프린트 토글 — **V3Panel 기존 구현 재사용**(**신규 0**)
```

### §0-5 §2 자기검사 (사전 등록)

```
 ㉠ 3종 각각: **로드 blob 페이로드 sha = 등재 정본 sha** · **표시 정점 수 = 정본 셀 값**
 ㉡ **G7 ③ 방향 자기검사** — **v3-65 계기 재실행**(프린트 on)
 ㉢ **`?v2=1` 경로**: v2 씬이 **v3-65 이전과 동일 렌더**(회귀 확인 · **수정 0**)
 ㉣ **G1 정의역 «갱신» 적용** — `src/v3` **+ 신규 v3 소비 화면 코드**에서 **v2 임포트 0**.
    **`App.tsx` 의 «플래그 분기»는 «경계»로 등재**한다 — **분기문 자체는 위반 아님**(문언 등재).
```

### §0-6 §3 캡처 · §4 갈래

**§3** 기본 화면 **3종 × front 3장** + **원단 전환 UI 1장** + **`?v2=1` 대조 1장** = **5장**.

| 갈래 | 조건 | 귀결 |
|---|---|---|
| **A** | 전환 성립 · 자기검사 전량 · 게이트 전량 | **캡처 상신**(β-1 완결 «후보») |
| **B** | v2 소비자 중 기본 화면이 **«잃으면 안 되는» 기능** 발견(**사전 목록 밖**) | 정지 · **목록 상신** |
| **E** | 판정 불가 | 정지 · 사실만 |

### 이 판에서 바꾸지 않는 것

**물리 0프레임** · **`solver` / `garmentScene` / `s4Gate` / `printUv` / `printComposite` diff 0** ·
문턱·정본 **0** · **v2 «코드» 0줄 삭제** · **`V2DIMS` off**.

### 순서

```
 §0 커밋 → §1 → §2 → §3 → §4 판정 · 상신
```
