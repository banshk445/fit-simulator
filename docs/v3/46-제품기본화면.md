# 46 — 5/5-β-1 제품 «기본» 화면 = v3 정본 재생 (v3-66 · **물리 0프레임 · v2 코드 무삭제**)

v3-66. 브랜치 `v3-66-product-default`(사전 등록 · **이 CC 세션 전용**). 호출한 ecc 에이전트/스킬: **0 / 0**

## git 스탬프

```
 브랜치  v3-66-product-default      워크트리  /Users/banshk/projects/fit-simulator
 기점    f42ec75 (v3-65 HEAD)       직전 회차  f42ec75
 커밋    (§0 → §1 → §2 → §3 순서로 채운다)
```

---

## §0 판정 수령 — **집행 «전» 커밋**

### §0-1 ★ **5/5-α 완결 «선언» 등재** — CC 는 **등재만** 한다

```
 **선언**: **5/5-α(프린트 축) 완결.**
 **귀속**: **사용자 화면 합격** + **전략 세션 판정**. **CC 는 선언 주체가 아니다**(화면 판정 0 규약).
 **완결의 내용**(회차별 근거):
   v3-59  프린트 UV 정규화 층 신설 · 패널 분리 렌더 · **G7 신설**(①②③)
   v3-60  합성 층 신설(대표색 + 가슴 프린트 + 브리지 복원) — 갈래 **E → A**(v3-63 전환)
   v3-62  알파 합성 «구조적» 확인 · 자산 선별 규칙 ㉠㉡㉢ 신설
   v3-63  사무 이월(v3-61 결번 · 기준값 재등재 · #82 확장)
   v3-64  자산 통과·합성 완주 · **대표색 폴백** 구조 사실 · back 반점 판정 불가
   v3-65  **상하 반전 해소**(부호표 3층 · ㉮ 단독) · **G7 ③ 정정** · 방향 자기검사 신설
 **귀책 4건 병기**(전부 **전략 세션** · 은폐 0):
   ① **#117** 게이트 문턱이 판정 정의역과 겹쳤다
   ② **#118** 상관을 재려는 두 채널이 독립이 아니었다
   ③ **#82 확장** 자기검사 기준값을 «셀» 없이 인용해 v3-60 을 갈래 E 로 오정지시켰다
   ④ **#119 · #120** G7 ③ 부등식을 v2 유도와 **정반대**로 등록했고(#120),
      **방향이 모호한 자산**으로 화면 판정을 해 반전을 못 걸렀다(#119 · v3-59 「정립 확인」 무효)
 **미해소로 «남기고» 넘어가는 것 1건**: **대표색 = 폴백 rgb(128,128,128)**(v3-64 §2-3).
   규칙 ㉢(테두리 투명)과 「대표색이 옷 색을 준다」는 **양립 불가**다. **α 정의역 밖**으로 등재됐고
   **처방 0** — 「옷 색을 어디서 받는가」는 **별도 축**이다.
```

### §0-2 β-1 범위 수령

```
 **제품 «기본» 화면(쿼리 플래그 «없음»)을 v3 정본 재생으로 전환**한다.
 **기확정 처분 인용**(이 판이 새로 정하지 않는다):
   **Q3 = 최종 «대체»** — 이 판은 **이관 1차**다. **v2 코드 무삭제**.
     화면 소비자 이관이 «완료»된 시점에 v2 를 **하네스 전용으로 강등**한다 — **그 시점은 이 판이 아니다**.
   **R5 / R8** — 원단 UI 는 **v3 프리셋 3종 직접 노출** · **denim 칸 제외**(#78) · **프리셋 코드 무삭제**.
```

### §0-3 이 판이 **하지 않는** 것

```
 **새 물리 실행 0**(재생·주입만 · **Q2 미결**) · **핏 리포트 «표» 이관 0** ·
 **프린트 UI 통합 0**(표시 토글은 **v3 패널 것 재사용**) · **v2 화면 제거 0**.
```

### §0-4 §1 전환 (사전 등록)

```
 ① `App.tsx` 기본 렌더 = **v3 productView**(blob 재생 · v3-41~43 검증 경로 그대로)
    구 v2 씬은 **`?v2=1` 플래그로 «보존»** — **플래그 신설 = 「이관 중」 상태의 «명시»** · 기본값 아님
 ② 원단 선택 UI = **gray / swim / sweat 3종**(라벨 = **등재 물성 명** · `fabricPresets` 매핑 경유 **금지** ·
    **denim 항목 렌더 제외** · **프리셋 코드 무삭제**)
 ③ 선택 → 해당 **정본 blob** 로드 · **sha 검증 후 표시**(불일치 시 **오류 표시** · **폴백 0**)
 ④ 핏 맵 색 / 프린트 토글 — **V3Panel 기존 구현 재사용**(**신규 0**)
```

### §0-5 §2 자기검사 (사전 등록)

```
 ㉠ 3종 각각: **로드 blob 페이로드 sha = 등재 정본 sha** · **표시 정점 수 = 정본 셀 값**
 ㉡ **G7 ③ 방향 자기검사** — **v3-65 계기 재실행**(프린트 on)
 ㉢ **`?v2=1` 경로**: v2 씬이 **v3-65 이전과 동일 렌더**(회귀 확인 · **수정 0**)
 ㉣ **G1 정의역 «갱신» 적용** — `src/v3` **+ 신규 v3 소비 화면 코드**에서 **v2 임포트 0**.
    **`App.tsx` 의 «플래그 분기»는 «경계»로 등재**한다 — **분기문 자체는 위반 아님**(문언 등재).
```

### §0-6 §3 캡처 · §4 갈래

**§3** 기본 화면 **3종 × front 3장** + **원단 전환 UI 1장** + **`?v2=1` 대조 1장** = **5장**.

| 갈래 | 조건 | 귀결 |
|---|---|---|
| **A** | 전환 성립 · 자기검사 전량 · 게이트 전량 | **캡처 상신**(β-1 완결 «후보») |
| **B** | v2 소비자 중 기본 화면이 **«잃으면 안 되는» 기능** 발견(**사전 목록 밖**) | 정지 · **목록 상신** |
| **E** | 판정 불가 | 정지 · 사실만 |

### 이 판에서 바꾸지 않는 것

**물리 0프레임** · **`solver` / `garmentScene` / `s4Gate` / `printUv` / `printComposite` diff 0** ·
문턱·정본 **0** · **v2 «코드» 0줄 삭제** · **`V2DIMS` off**.

### 순서

```
 §0 커밋 → §1 → §2 → §3 → §4 판정 · 상신
```

---

## §1 전환 — **`V3Product` 신설 + `App.tsx` 분기**

### §1-1 변경 파일 **2개**

```
 **신설** `src/components/V3Product.tsx` — 제품 «기본» 화면. **렌더 로직 0줄**:
   핏 맵 색·프린트 둘 다 `productView` / `printUv` / `printComposite` 의 **같은 계약을 같은 인자로**
   소비한다(V3Panel 과 **동일 모듈**). **신규 표시 구현 0**(§0-4 ④).
 **수정** `src/App.tsx` — 기본 = `V3Product`, **`?v2=1` = 구 v2 화면**.
   구 화면은 `V2Screen()` 안에 **원문 그대로** 옮겼다. **v2 코드 0줄 삭제.**
```

**기점 `f42ec75` 대비 diff 0 확인**(전량): `solver.ts` **0** · `garmentScene.ts` **0** ·
`s4Gate.ts` **0** · `printUv.ts` **0** · `printComposite.ts` **0** · `V3Panel.tsx` **0** ·
`Controls.tsx` **0** · `FitCanvas.tsx` **0**.
`npx tsc -b` **rc=0** · `npm run build` **rc=0**.

### §1-2 원단 UI — **v3 프리셋 3종 «직접» 노출**(R5 / R8)

```
 목록 = **gray / swim / sweat**. **denim 항목은 «렌더»에서 제외**(#78) —
   `consts.ts` 의 `FABRICS.denim` **프리셋 코드는 무삭제**(값은 그대로 남는다).
 라벨은 **등재 물성에서 «뜬다»** — `FABRICS[fab]` 의 `k` · `ρ` · `B` 를 그대로 찍는다.
   **`fabricPresets`(v2) 경유 0** · **G1 위반 0**(아래 §2-㉣).
   화면 실측: gray **k 69 · ρ 0.187 · B 2.319e-5** / swim **k 209.2 · ρ 0.204 · B 6.024e-5** /
             sweat **k 25 · ρ 0.224 · B 5.947e-5**
```

### §1-3 정본 재생 — **대조 «후» 표시 · 폴백 0**

```
 워커 인자 = `frames: **0**` + `injectStateUrl` ⟹ **물리 0프레임**(v3-41~43 검증 경로 그대로).
 로드 후 **페이로드 sha**(헤더 제외)와 **정점 수**를 **등재 정본값과 대조**한다.
 **어긋나면 «표시하지 않고» 오류를 띄운다** — **폴백 0 · 대체 정본 0 · 조용한 성공 0**.
 `CANON` 의 sha·정점·프레임은 **등재값의 «전사»**이고 출처는 `docs/metrics-log.md` v3-49 ㉢ 표다.
 **이 판이 새로 정한 값은 0개.**
```

---

## §2 자기검사

### §2-㉠ 3종 대조 — **3/3 일치**

| 원단 | 정착 프레임 | 페이로드 sha(화면 표시) | 등재 정본 sha | 정점(화면) | 정본 셀 | 판정 |
|---|---|---|---|---|---|---|
| gray d9 | 220 | `faf4873fb33cfa37…` | `faf4873f…` | **10402** | 10402 | ✅ |
| swim d9 | 260 | `557bc4469a0f1b57…` | `557bc446…` | **10402** | 10402 | ✅ |
| sweat d8 | 330 | `51d74c668fa570f4…` | `51d74c66…` | **13248** | 13248 | ✅ |

### §2-㉡ G7 ③ 방향 자기검사 — **v3-65 계기 «재실행»**(프린트 on) · **3/3 정립**

계기 `scripts/v3PrintDir.ts` **diff 0**(v3-65 신설 그대로). 대상 = **제품 기본 화면의 캔버스 PNG**.

| 기본 화면 캡처(480×672) | 상반부색 y 중앙 | 하반부색 y 중앙 | 판정 |
|---|---|---|---|
| gray | **239** | 275 | **대응 일치 = 정립** |
| swim | **236** | 274 | **대응 일치 = 정립** |
| sweat | **242** | 283 | **대응 일치 = 정립** |

노드 쪽 **G7 재판정**(gray d9) — ① 위반 **0** · ② **3.924e-8** ≤ 1.429e-4 · ③ 불성립 **0**
⟹ **3/3 통과**(`printUv.ts` **diff 0** 이므로 v3-65 판정이 그대로 유효하다는 값 근거).

### §2-㉢ `?v2=1` 경로 — **동일 조건 대조로 «회귀 아님» 확정**

```
 관측: `?v2=1` 에서 **v2 컨트롤은 전부 뜨고**(슬라이더·업로드·소매 토글 · 값 전량 동일)
       **3D 캔버스는 «흰 화면»** 이다(캔버스 자체는 존재 · 972×853 · **WebGL 컨텍스트 손실 없음** ·
       콘솔 오류 **0건** · 경고 1건은 three 의 `Clock` deprecated 안내).
 **판별을 «추정»으로 끝내지 않았다** — `git show f42ec75:src/App.tsx` 로 **v3-65 의 App.tsx 를
 그대로 되돌려** 기본 경로를 다시 띄웠다. 결과: **같은 흰 화면**(픽셀 수준 동일 구도).
 ⟹ **이 판의 회귀가 «아니다»**. 원복 후 `git diff` **0**(임시 교체분 잔류 0).
 **원인 귀속 0 · 처방 0** — 이 판의 정의역 밖이고, **갈래 B 의 「잃으면 안 되는 기능 발견」에도
 해당하지 않는다**(전환으로 «잃은» 것이 아니라 **전환 전부터 같았다**).
 **부기**: 같은 저장소를 이 세션 «초반»(v3-65 · 다른 창 1525×784 · `?v3=1`)에 띄웠을 때는
 v2 씬이 **보였다**. 창·컨텍스트 차이로 보이나 **단정하지 않는다**(사실만).
```

### §2-㉣ G1 정의역 «갱신» 적용 — **위반 0**

```
 `src/v3/**` + **신규 v3 소비 화면**(`V3Product.tsx`)에서 **v2 임포트 0건**
   (`lib/` · `fabricPresets` · v2 store 검색 ⟹ **주석 2줄 외 코드 0**).
 **`App.tsx` 의 «플래그 분기»는 «경계»로 등재**한다 — 이 파일은 v2 3종과 v3 1종을 **둘 다 임포트**한다.
   §0-5 ㉣ 대로 **분기문 자체는 위반이 아니다**: 금지되는 것은 **v3 코드가 v2 를 «소비»하는 것**이고,
   여기서 두 화면은 **서로를 소비하지 않는다**(공유 상태 0 · 공유 모듈 0).
```

### §2-㉤ 그 밖의 게이트

| 게이트 | 값 | 판정 |
|---|---|---|
| G4 물리 프레임 | **0**(워커 `frames: 0` · 주입만) | ✅ |
| blob 무변조 | gray `8dec6eca…` · swim `786f3fc0…` · sweat `dcaceda3…` | ✅ 등재값 일치 |
| 스트립 자기검사 | **258 / 258 / 290**(idx 1548 / 1548 / 1740) | ✅ 기준 일치 |

---

## §3 캡처 — **10장**(요구 5장 상회)

`docs/captures/v3-66-제품기본화면/`

```
 기본 화면 front — **핏 맵 색**   `v3-66-{gray,swim,sweat}-fit.png`     (480×672 · 캔버스 원본 PNG)
 기본 화면 front — **프린트**     `v3-66-{gray,swim,sweat}-print.png`   (480×672 · §2-㉡ 판정 대상)
 **원단 전환 UI** 3장             `v3-66-UI-원단전환-{gray,swim,sweat}.jpg`
   (원단 버튼 · **등재 물성 라벨** · 정착 프레임 · sha · 정점 대조줄이 **한 화면에** 들어 있다)
 **`?v2=1` 대조** 1장             `v3-66-대조-v2플래그.jpg`
```

### §3-1 계기 사실 1건 (집행 경로 · 판정 아님)

```
 제품 화면에는 **캡처 버튼을 «넣지 않았다»**(§0-3 「프린트 UI 통합 0」 · 신규 UI 0).
 ⟹ 캔버스 PNG 는 **브라우저 콘솔에서 `canvas.toBlob` 다운로드**로 받았다
    (**제품 코드 0줄** · 화면이 이미 그린 것을 그대로 저장 · 값 조작 0).
 UI 캡처는 **뷰포트 스크린샷**(jpeg)이다 — 픽셀 판정 대상이 아니라 **UI 증거**다.
```

---

## §4 판정 — **갈래 A**

| 조건 | 실측 | 충족 |
|---|---|---|
| 전환 성립 | 기본 경로(플래그 없음) = v3 정본 재생 · `?v2=1` 로 v2 보존 · **v2 0줄 삭제** | ✅ |
| 자기검사 전량 | ㉠ 3/3 sha·정점 일치 · ㉡ 3/3 정립 + G7 3/3 · ㉢ 회귀 아님(동일 조건 대조) · ㉣ G1 0건 | ✅ |
| 게이트 전량 | G4 물리 0프레임 · blob 무변조 · 스트립 258/258/290 · tsc·build rc=0 | ✅ |

⟹ **갈래 A** — **캡처 10장 상신**(β-1 완결 «후보»).

**갈래 B 불발화 근거**: `?v2=1` 흰 화면은 **전환으로 «잃은» 기능이 아니다** —
**v3-65 의 App.tsx 로 되돌려도 같았다**(§2-㉢). 「사전 목록 밖의 잃으면 안 되는 기능」에 해당하지 않는다.

**함께 상신하는 «사실» 2건**(판정을 바꾸지 않는다):
① §2-㉢ `?v2=1` 3D 캔버스 흰 화면 — **회귀 아님이 값으로 확정**됐으나 **상태 자체는 미해소**
  (원인 귀속 0 · 처방 0 · 정의역 밖). 세션 초반에는 보였다는 부기 포함.
② §3-1 계기 사실(콘솔 `toBlob` 다운로드 · 제품 코드 0줄).

**β-1 완결 선언은 전략 세션이 한다** — 이 판은 선언하지 않는다. **CC 화면 판정 0.**
