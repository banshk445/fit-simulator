# 49 — 몸 정점 주입 경로 신설 + BEXT·③b 도출 복원 + 5/5 사무 이월 (v3-70 · **물리 0프레임**)

v3-70. 브랜치 `v3-70-body-inject`(사전 등록 · **이 CC 세션 전용**). 호출한 ecc 에이전트/스킬: **0 / 0**

## git 스탬프

```
 브랜치  v3-70-body-inject          워크트리  /Users/banshk/projects/fit-simulator
 기점    268a795 (v3-69 HEAD)       직전 회차  268a795
 커밋    (§0 → §1·§2등재 → §2집행 → §3 순서로 채운다)
```

---

## §0 판정·사무 수령 — **집행 «전» 커밋**

### §0-A 사무 이월 — **규약 승격 수령**

```
 **v3-68 결번이 «2건째»**(v3-61 에 이어). ⟹ **순수 사무는 «별도 판» 없이 다음 «작업 판» §0 에 병합**한다.
 이 조항을 **상시 규약으로 승격**해 수령한다. 아래 ①②③ 이 그 병합분이다.
```

#### ① **5/5 「제품화」 완결 «선언» 등재** — CC 는 **등재만**

```
 **선언**: **β 완결** 및 **5/5(제품화 축) 완결**.
 **귀속**: **사용자 화면 합격 + 전략 세션**. **CC 는 선언 주체가 아니다.**
 **근거 판 번호 전량**
   v3-58  5/5 개시 정찰(제품 경로 · 프린트 UV · 성능)
   v3-59  프린트 UV 정규화 층 · 패널 분리 렌더 · **G7 신설**
   v3-60  프린트 «합성 층»(대표색 + 가슴 프린트 + 브리지 복원) — 갈래 **E → A**(v3-63 전환)
   v3-62  알파 합성 «구조» 확인 · 자산 규칙 ㉠㉡㉢ 신설      v3-63  사무 이월(#82 확장)
   v3-64  자산 통과 · 합성 완주 · **대표색 폴백** 구조 사실   v3-65  **상하 반전 해소** · G7 ③ 정정
   v3-66  **β-1** 제품 «기본» 화면 = v3 정본 재생(이관 1차)   v3-67  **β-2** 핏 리포트 표 이관
 **★ 등재 «한계» 병기**(완결이 «전부 풀렸다»는 뜻이 아니다 · 은폐 0)
   ㉠ **`?v2=1` 백화** — 미해소 · 저우선(v3-67 §0-2 처분) · **발생 시점 미상**
   ㉡ **스트립(브리지) = 단색** — 프린트 모드에서 텍스처 0 · UV 0(v3-60 설계 · 대체 미검토)
   ㉢ **#104** — 어깨 대역 e 분포 · **상관 판정 «불가»**(v3-57 갈래 E · 미해소)
   ㉣ **뒤 암홀** — 등재 잔존분(해소 0)
   ㉤ **Q2(성능 목표) 종속** — 성능 문턱이 없어 **「제품으로 충분한가」는 미판정**이다
   ㉥ **대표색 = 폴백 rgb(128,128,128)** — 규칙 ㉢ 과 「대표색 = 옷 색」 **양립 불가**(v3-64 §2-3)
```

#### ② **v2 경로 「하네스·참고 전용」 강등 등재**

```
 **강등**: v2 렌더 경로(`Controls` · `FitCanvas` · `DressButton` · `PatternPreview` 계열)는
   **제품 경로가 아니다**. **「하네스·참고 전용」**으로 분류한다.
 **코드 무삭제** · **`?v2=1` 유지**(v3-66 §1 이 만든 보존 경로 그대로).
 **★ 백화 사실 병기**: `?v2=1` 의 **3D 캔버스는 흰 화면**이다(v3-66 §2-㉢ · **회귀 아님**이
   동일 조건 대조로 확정됨 · **원인 귀속 0 · 처방 0**). **강등이 이 사실을 지우지 않는다.**
```

#### ③ **v3-67 «경미» 귀책 등재 — 5건째**

```
 **내용**: v3-67 §0-5 ③ 이 「표 셀 값 = **metrics-log 등재값**(3종 × 5행)」을 사전 등록했으나,
   **신 채널 표는 gray 만 5행 전량 등재**이고 **swim·sweat 는 부분 등재**였다 ⟹
   **사전 등록 문언이 «집행 불가»**였다(v3-67 §1-4 가 그 자리에서 사실로 등재).
 **귀책 = 전략 세션**(등재 «상태»를 확인하지 않고 문언을 썼다). **경미** — 불일치 0 이었고
   판정이 바뀌지 않았다. **#117·#118·#82확장·#119·#120 에 이은 5건째.**
```

### §0-B 결손 처분 수령 (v3-69 §5 상신분)

```
 ① **경로 = (a) «구운 몸 정점 배열» 주입.**
    **(b) v3 안에 본 스케일 재구현 = 기각** — 사유 **이중 구현 «발산» 회피** · **v2 검증 재사용**.
 ② **BEXT = «주입 몸 실측 바운딩»에서 도출.**
    하드코딩 `[1.78, 1.765, 0.282]` 은 **「기본 몸 캐시」로 강등**하고 **무삭제**로 남긴다.
 ③ **③b = 「기본 몸 «전용»」 한정 재등재 + 일반형 `h × 25%` «도출식» 복원.**
    **완화가 아니라 «도출 복원»** — 근거 v3-69 §1-5 인용(「④ 가 칸마다 다른 수가 된다」).
```

### §0-C 민감도 산출물 «분류» 수령

```
 민감도 판의 산출물은 **「측정 «전용» · «정본» 아님」** 으로 분류한다.
 ⟹ **Node 채널 허용**. **20프레임 브라우저↔Node 비트 대조는 «정본 편입»에만** 걸린다 —
    `docs/v3/부록-상태재현.md §5` **문언은 그대로 유지**한다(고치지 않는다).
```

### §0-D §1 전환 (사전 등록)

```
 ① `v3DressWorker` 입력에 **`bodyVerts?: Float32Array`** 신설 (+ **정점 수·법선 규약 명기**) —
    **존재 시 `parseGlb` 몸 «대신»** 쓴다. **`glb` 경로는 유지**(기본값 · **무삭제**).
 ② `dressRun`: **BEXT = 주입 정점 «실측 바운딩»에서 도출**.
    `bodyScale` 곱 형태는 **주입 경로에서 폐기**(스케일이 이미 «정점»에 반영돼 있다) ·
    **`glb` 경로는 기존 그대로**.
 ③ 메인 스레드: **«현 슬라이더 몸»을 굽고 워커에 넘기는 연결부**.
    **v3 패널에 «몸 주입» 경로**(제품 화면 노출 **0** · **계기 성격**).
```

### §0-E §2 자기검사 (사전 등록 · **0프레임**)

```
 **㉮ 등가** — **스케일 1(기본 몸)** 주입 정점 배열 **≡** `parseGlb` 경로 정점 배열 · **비트 대조**
    **일치 ⟹ 물리 재실행 없이 «경로 등가» 성립** · **불일치 ⟹ 정지 · 차이 «사실만»**(갈래 B)
 **㉯ 도출 스모크**(조립까지만 · **물리 0프레임**) — **본 스케일 몸 1개**(**가슴둘레 +10%** ·
    **v2 슬라이더 값으로 명기**) 주입 → **BEXT · h · band · ③b 문턱 · Y_TOP · 목선 · AXIS_Z** 가
    전부 **«그 몸의» 도출값**으로 나오는지 **값 등재**(**기본 몸 값과 병기**).
    **판정은 「도출식이 «돌았는가»」만** — 값의 옳고 그름은 이 판의 정의역 밖이다.
 **㉰ 기본 몸 «회귀»** — 기존 **정본 3종** 재생·게이트 **값 채널 무변화**
    (**blob sha · 스트립 · G7**).
```

### §0-F §3·§4

```
 **§3 등재** `docs/v3/49`(이 노트) · **#116 갱신**(③b **한정 재등재** · **구 문언 무삭제**) ·
    **BEXT 강등** · **색인**.
```

| 갈래 | 조건 | 귀결 |
|---|---|---|
| **A** | ㉮ **비트 동일** · ㉯ 도출 **전량 성립** · ㉰ 회귀 **무변화** | **완료 상신**(민감도 판 준비 완료) |
| **B** | **㉮ 불일치** | 정지 · **차이 사실만** |
| **E** | 판정 불가 | 정지 · 사실만 |

### 이 판에서 바꾸지 않는 것

**물리 0프레임** · **`solver.ts` diff 0** · **`s4Gate` 판정 로직 0줄**(**문턱 «도출부»만** · diff 등재) ·
**색·표시·표 채널 0줄** · **정본 0** · **v2 코드 무삭제** · 문턱 **«③b 도출 복원 외» 0**.

### 순서

```
 §0 커밋 → §1 → §2 «등재» 커밋 → §2 집행 → §3 → §4 판정 · 상신
```

---

## §1 주입 경로 신설 — 변경 파일 **5개**

```
 **수정** `src/v3/dressRun.ts`
   · `RunInput.bodyVerts?: Float32Array` 신설 — **규약 3항을 주석에 «정본»으로 적었다**
       ① 길이 = `parseGlb(glb).prims[0].pos.length` — 어긋나면 **던진다**(조용한 성공 0)
       ② 정점 **순서** 동일 — **위상(`idx`)은 GLB 것을 쓰기 때문**이다
       ③ **법선은 넘기지 않는다** — v3 는 몸 법선을 쓰지 않는다(충돌 = `bodySdf` 격자 ·
          표시 = `productView` 가 위치에서 다시 만든다) ⟹ 넘기면 **두 벌이 되어 갈릴 수 있다**
   · 주입 시 **`bodyScale` 을 곱하지 않는다**(스케일이 이미 정점에 반영돼 있다)
   · **BEXT 분기** — 주입이면 `measureExt(정점)`, `glb` 경로면 **구 식 그대로**(§1-2)
   · `prepare` 반환에 `bext` 추가(인쇄용)
 **수정** `src/workers/v3DressWorker.ts` — `bodyVerts` 통과 + **`derived` 인쇄 채널**
   (BEXT · h · band · ③b 문턱 · Y_TOP · Y_NECK · AXIS_Z · 주입 여부). **판정 로직 0줄.**
 **신설** `src/components/bodyInjectBake.ts` — 메인 스레드 «몸 굽기» 연결부
 **수정** `src/components/V3Panel.tsx` — «몸 주입 스모크» 버튼 2개(계기 · **제품 화면 노출 0**)
 **수정** `src/v3/s4Gate.ts` · `scripts/v3Gate3b.ts` — **③b 문언 한정 재등재**(§3)
```

**기점 `268a795` 대비 diff 0 확인**: `solver.ts` **0** · `garmentScene.ts` **0** · `printUv.ts` **0** ·
`printComposite.ts` **0** · `productView.ts` **0** · `fitReport.ts` **0** · `V3Product.tsx` **0** ·
`FitReportTable.tsx` **0** · `App.tsx` **0**. `tsc -b` **rc=0** · `build` **rc=0**.

### §1-1 연결부 설계 — **판독기를 «하나»로 유지한다**

```
 `src/v3/glb.ts` 머리주석이 이미 못 박았다: 「브라우저가 `useGLTF`(three)로 몸을 읽으면
 **정점 순서·병합이 달라질 수 있고**, 그러면 Node ↔ 브라우저 대조가 «판독기 차이»에 오염된다」.
 ⟹ 연결부는 **기준 위치를 `parseGlb` 에서 뜨고**, three 에서는 **«스킨 변환»만** 가져온다
    (`applyBoneTransform(i, v)`). 이렇게 해야 규약 ①②(길이·순서)가 **구조적으로** 만족된다.
 본 스케일은 `skeleton.boneMatrices` 로, 루트 균등 스케일(키)은 `bone.matrixWorld` 로 들어온다
 ⟹ **키·팔·다리·어깨·가슴둘레 5축이 모두 담긴다**. 좌표계는 **GLB 프레임 그대로**다.
 **v3 안에 본 스케일을 재구현하지 않았다**(§0-B ① 기각 조항 준수).
```

### §1-2 ★ BEXT 강등 — **분기로 «둘 다» 남긴다**

```
 `dressRun.ts` — 구 식 `[1.78·BS, 1.765·BS, 0.282·BS]` 은 **삭제하지 않았다**.
 **`glb` 경로 전용 = 「기본 몸 «캐시»」**로 강등하고 주석에 그 지위를 적었다.
 **주입 경로만** 실측 바운딩을 쓴다.
 **이 분기가 정본 3종 «비트» 회귀의 근거다** — 구 식을 통일했다면 h 가 바뀌어 정본이 깨진다(§2-㉰).
```

---

## §2 자기검사 — 집행

### §2-㉮ 경로 등가 — **정점 배열 «비트 동일» ✅**

Node 에서 **실제 주입 경로**를 통과시켜 잰다(`prepare(bodyVerts=parseGlb 사본)` ↔ `prepare(glb)`).

| 채널 | `glb` 경로(비주입) | 주입 경로(항등 정점) | 판정 |
|---|---|---|---|
| 몸 정점 배열(n=15882) | — | — | **비트 동일** ✅ |
| 조립 정점 | 10402 | 10402 | 같다 |
| `Y_TOP` | 146.8696cm | 146.8696cm | 같다 |
| `AXIS_Z` | 1.6049cm | 1.6049cm | 같다 |
| SDF 복셀 | 16,744,320 | 16,744,320 | 같다 |

⟹ **㉮ 는 사전 등록 문언대로 «성립»한다.**

### §2-㉮′ ★★ 그러나 — **㉮ 가 «잡지 못한» 차이가 있다**(은폐 0)

```
 같은 정점인데 **도출값이 갈린다** — BEXT 분기 때문이다.
   BEXT   하드코딩 [**1.780000**, **1.765000**, **0.282000**]
          실측     [**1.779605**, **1.765052**, **0.282219**]   ← x 는 작고 y·z 는 크다
   h      3.951220mm → **3.950345mm**   (차 **−8.750e-4 mm**)
   band   8.902439mm → **8.900689mm**
   ③b 문턱 0.987805mm → **0.987586mm**  (차 **−2.187e-4 mm**)
 ⟹ 「**물리 재실행 없이 경로 등가**」는 **«정점 배열 수준»에서만** 성립한다.
    **«굽기 수준»에서는 성립하지 않는다** — 주입 경로로 정본을 다시 구우면 **다른 h 로 굽는다**.
 **처분**: §0-B ② 대로 **분기를 유지**한다(구 식 = 기본 몸 캐시 · 무삭제).
    구 식으로 «통일»하면 주입 몸에서 틀리고, 실측으로 «통일»하면 **정본 3종이 깨진다**
    ⟹ **어느 쪽 통일도 하지 않는다.** **차이를 값으로 등재하는 것이 이 판의 처분이다.**
 **하드코딩 값의 성격이 이 값으로 확정된다** — [1.78, 1.765, 0.282] 은 **실측이 아니라 «어림»**이었다
    (실측과 최대 **3.95e-4 m** 어긋난다). **귀책 등재 0**(v3-13 시점의 예산 계산용 어림값이다).
```

### §2-㉯ 도출 스모크 — ★ **집행 «불가»**

```
 사전 등록: **본 스케일 몸 1개**(**가슴둘레 100 → 110cm · +10%**)를 주입해 도출값을 낸다.
 **실측**: 브라우저 `?v2=1&v3=1` 에서 «몸 주입 스모크(현 슬라이더)» 를 눌렀다 —
   `[v3-70] 굽기 — 살아있는 마네킹 **없다** · 스킨드 메시 **못 찾음** · 정점 15882
            · parseGlb 배열과 **비트 동일**`
   ⟹ `mannequinRootRef.current` 가 **null** 이다. 연결부는 그 경우 **항등 통과**로 정의돼 있어
      «본 스케일 몸»이 만들어지지 않았다. **가슴둘레 +10% 몸을 얻지 못했다.**
 **부분 성립분**(주입 경로 자체는 돌았다) — 같은 클릭의 워커 로그:
   `[v3-70] 도출(주입 **예**) — BEXT [1.7796, 1.7651, 0.2822]m · h 3.9503mm · band 8.9007mm
            · ③b 문턱 0.9876mm · Y_TOP 146.870cm · Y_NECK 150.045cm · AXIS_Z 1.6049cm
            · 목선둘레 49.322cm · 정점 6848`
   ⟹ **주입 정점에서 BEXT·h·band·③b 가 «도출식으로» 나온다는 것은 확인됐다**(항등 몸 기준).
      **확인 못 한 것은 「«다른 몸»에서도 그 값이 그 몸을 따라가는가」**다.
```

### §2-㉯′ ★ `?v2=1` 백화의 **재개 조건이 «발화»했다**

```
 v3-67 §0-2 처분: 재개 조건 = 「**이관 항목이 v2 화면을 «참조 기준»으로 필요로 할 때**」.
 **이 판의 몸 주입이 정확히 그 경우다** ⟹ **조건 발화 · 저우선 해제 사유 발생**(판정은 전략 세션 몫).
 **한 번만** 본 진단 사실(원인 귀속 0 · 처방 0 · 수정 0줄):
   · `models/mannequin.glb` **GET 200** — 파일은 받는다
   · **WebGL2 사용 가능**(프로브 캔버스로 확인) · 컨텍스트 손실 없음 · **콘솔 오류 0건**
   · r3f 캔버스가 **300×150(기본값)에 머문다** — 부모 상자는 **972×853** 인데 캔버스 CSS 에
     크기 지정이 없다(`style="display: block;"` 뿐) ⟹ **리사이즈가 반영되지 않았다**
   · `mannequinRootRef.current` **null** ⟹ `Mannequin` 의 등록 effect 가 서지 않았다
 **단정하지 않는다** — 위는 «관측»이고 기전 귀속은 하지 않는다.
```

### §2-㉰ 기본 몸 회귀 — **무변화 ✅**

| 채널 | 값 | 판정 |
|---|---|---|
| blob sha(3종) | `8dec6eca…` · `786f3fc0…` · `dcaceda3…` | ✅ 등재값 일치 |
| 스트립 자기검사 | **258 / 258 / 290**(idx 1548 / 1548 / 1740) | ✅ 기준 일치 |
| G7 ①②③ | 위반 0 · 3.924e-8 ≤ 1.429e-4 · 불성립 0 ⟹ **3/3** | ✅ |
| ③b(gray d9) | 문턱 **0.9878mm** · max **0.9429mm** ⟹ 통과(여유 4.5%) | ✅ |
| G1 **제품 경로** | `src/v3/**` · `V3Product` · `FitReportTable` v2 임포트 **0건** | ✅ |

### §2-㉱ ★ G1 «경계» 등재 — 정의역을 **조용히 넓히지 않았다**

```
 `V3Panel.tsx` 가 **`src/lib/mannequinRef`(v2 자산)를 임포트한다** — **신설 1건**.
 사유: 살아있는 마네킹은 v2 씬에만 있고, 본 스케일 재구현은 **§0-B ① 이 기각**했다.
 **V3Panel 은 v3-67 §0-3 이 «하네스·플래그 전용»으로 분류한 층**이고,
 **v3-70 §0-A ② 가 v2 를 «하네스·참고 전용»으로 강등**했다 ⟹ **하네스 ↔ 하네스** 참조다.
 **제품 경로는 오염되지 않았다**(위 표 마지막 행 · 실측 0건).
 **이 줄이 등재다** — 게이트 문언을 고치지 않았고, **판정은 전략 세션 몫**으로 남긴다.
```

---

## §3 등재 — ③b 한정 재등재 · **★ 코드 변경 0줄**

```
 `src/v3/s4Gate.ts`(문언) · `scripts/v3Gate3b.ts`(문언) — **구 문언 무삭제 · 병기**.
   구 「v3-56 «고정 승격» — `h × 25%`(현행 h 3.951mm ⟹ **0.9877mm**)」
   신 「**일반형 = `h × 25%`(도출식)**. **0.9877mm 는 «기본 몸 전용» 예시값**이다 —
       `h` 는 `deriveSpacing(BEXT, …)` 의 출력이고 `BEXT` 는 몸에서 온다 ⟹ **몸이 바뀌면 문턱도 바뀐다**」
 ★★ **실측 1건 — «복원할 것이 없었다»**
   `scripts/v3Gate3b.ts:51` 이 이미 `const TH = TH_FRAC * P.sdfSpec.h * 1000` 이다.
   ⟹ **코드는 처음부터 도출식**이었고, **고정돼 있던 것은 «문언의 예시 수»뿐**이다.
   ⟹ **「도출 복원」에 코드 변경 «0줄»** — 고친 것은 **문언**이다. **완화 0 · 새 수 0.**
   (v3-69 §1-5 가 「도출식으로 되돌려야 한다」고 적은 것은 **문언 기준으로는 맞고 코드 기준으로는
    이미 그랬다** — 그 자리에 이 실측을 병기한다. **귀책 등재 0** · 정찰 판독의 정상 범위다.)
 **BEXT 강등** — `dressRun.ts` 주석에 「기본 몸 «캐시»」 지위를 적었다(§1-2 · 무삭제).
```

---

## §4 판정 — **갈래 E**(판정 불가 · 정지 · 사실만)

| 사전 등록 조건 | 실측 | 충족 |
|---|---|---|
| **㉮** 주입 정점 배열 ≡ `parseGlb` 배열 · 비트 대조 | **비트 동일** | ✅ |
| **㉯** 본 스케일 몸(**가슴둘레 +10%**) 주입 → 도출 전량 | **집행 불가** — 살아있는 마네킹 **부재** | ❌ |
| **㉰** 기본 몸 회귀 무변화 | blob·스트립·G7·③b **전량 무변화** | ✅ |

```
 **갈래 A 불성립** — ㉯ 가 «불일치»가 아니라 **«집행 불가»**다.
 **갈래 B 불성립** — B 의 조건은 「**㉮ 불일치**」인데 **㉮ 는 일치**했다.
 ⟹ **어느 갈래에도 정확히 해당하지 않는다** ⟹ **갈래 E · 정지 · 사실만**(상시 규약).
```

**정지 시점에 «서 있는» 것**(다음 판이 이어받을 수 있다):
① 주입 경로 **동작 확인**(워커 → `prepare` → BEXT 실측 도출 → h·band·③b 산출) ·
② **정점 배열 비트 등가** · ③ **기본 몸 회귀 무변화** · ④ ③b 문언 한정 재등재 · ⑤ BEXT 강등.

**★ 상신하는 «사실» 4건**
① **㉮′ 도출값 차이** — 같은 정점인데 h 가 **−8.750e-4mm**, ③b 문턱이 **−2.187e-4mm** 갈린다.
   「물리 재실행 없는 경로 등가」는 **정점 수준에서만** 성립한다. **분기 유지가 처분이다.**
② **하드코딩 BEXT 는 «어림»이었다** — 실측과 최대 **3.95e-4 m** 차(귀책 등재 0).
③ **`?v2=1` 백화의 «재개 조건 발화»** — 이 판의 이관 항목이 v2 마네킹을 필요로 했고 **막혔다**.
   진단 관측 4건 병기(GLB 200 · WebGL2 가용 · r3f 캔버스 300×150 고착 · 마네킹 ref null) ·
   **원인 귀속 0 · 처방 0 · 수정 0줄**.
④ **③b 는 코드가 이미 도출식이었다** — 복원 대상은 문언뿐이었다(**코드 변경 0줄**).

**다음 판이 먼저 풀어야 하는 것**: ③ 이 풀리지 않으면 **㉯ 도 민감도 판도 설 수 없다**
(살아있는 몸이 있어야 «다른 몸»이 만들어진다). **우선순위 판정은 전략 세션 몫.**
