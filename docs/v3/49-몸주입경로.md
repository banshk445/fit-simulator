# 49 — 몸 정점 주입 경로 신설 + BEXT·③b 도출 복원 + 5/5 사무 이월 (v3-70 · **물리 0프레임**)

v3-70. 브랜치 `v3-70-body-inject`(사전 등록 · **이 CC 세션 전용**). 호출한 ecc 에이전트/스킬: **0 / 0**

## git 스탬프

```
 브랜치  v3-70-body-inject          워크트리  /Users/banshk/projects/fit-simulator
 기점    268a795 (v3-69 HEAD)       직전 회차  268a795
 커밋    (§0 → §1·§2등재 → §2집행 → §3 순서로 채운다)
```

---

## §0 판정·사무 수령 — **집행 «전» 커밋**

### §0-A 사무 이월 — **규약 승격 수령**

```
 **v3-68 결번이 «2건째»**(v3-61 에 이어). ⟹ **순수 사무는 «별도 판» 없이 다음 «작업 판» §0 에 병합**한다.
 이 조항을 **상시 규약으로 승격**해 수령한다. 아래 ①②③ 이 그 병합분이다.
```

#### ① **5/5 「제품화」 완결 «선언» 등재** — CC 는 **등재만**

```
 **선언**: **β 완결** 및 **5/5(제품화 축) 완결**.
 **귀속**: **사용자 화면 합격 + 전략 세션**. **CC 는 선언 주체가 아니다.**
 **근거 판 번호 전량**
   v3-58  5/5 개시 정찰(제품 경로 · 프린트 UV · 성능)
   v3-59  프린트 UV 정규화 층 · 패널 분리 렌더 · **G7 신설**
   v3-60  프린트 «합성 층»(대표색 + 가슴 프린트 + 브리지 복원) — 갈래 **E → A**(v3-63 전환)
   v3-62  알파 합성 «구조» 확인 · 자산 규칙 ㉠㉡㉢ 신설      v3-63  사무 이월(#82 확장)
   v3-64  자산 통과 · 합성 완주 · **대표색 폴백** 구조 사실   v3-65  **상하 반전 해소** · G7 ③ 정정
   v3-66  **β-1** 제품 «기본» 화면 = v3 정본 재생(이관 1차)   v3-67  **β-2** 핏 리포트 표 이관
 **★ 등재 «한계» 병기**(완결이 «전부 풀렸다»는 뜻이 아니다 · 은폐 0)
   ㉠ **`?v2=1` 백화** — 미해소 · 저우선(v3-67 §0-2 처분) · **발생 시점 미상**
   ㉡ **스트립(브리지) = 단색** — 프린트 모드에서 텍스처 0 · UV 0(v3-60 설계 · 대체 미검토)
   ㉢ **#104** — 어깨 대역 e 분포 · **상관 판정 «불가»**(v3-57 갈래 E · 미해소)
   ㉣ **뒤 암홀** — 등재 잔존분(해소 0)
   ㉤ **Q2(성능 목표) 종속** — 성능 문턱이 없어 **「제품으로 충분한가」는 미판정**이다
   ㉥ **대표색 = 폴백 rgb(128,128,128)** — 규칙 ㉢ 과 「대표색 = 옷 색」 **양립 불가**(v3-64 §2-3)
```

#### ② **v2 경로 「하네스·참고 전용」 강등 등재**

```
 **강등**: v2 렌더 경로(`Controls` · `FitCanvas` · `DressButton` · `PatternPreview` 계열)는
   **제품 경로가 아니다**. **「하네스·참고 전용」**으로 분류한다.
 **코드 무삭제** · **`?v2=1` 유지**(v3-66 §1 이 만든 보존 경로 그대로).
 **★ 백화 사실 병기**: `?v2=1` 의 **3D 캔버스는 흰 화면**이다(v3-66 §2-㉢ · **회귀 아님**이
   동일 조건 대조로 확정됨 · **원인 귀속 0 · 처방 0**). **강등이 이 사실을 지우지 않는다.**
```

#### ③ **v3-67 «경미» 귀책 등재 — 5건째**

```
 **내용**: v3-67 §0-5 ③ 이 「표 셀 값 = **metrics-log 등재값**(3종 × 5행)」을 사전 등록했으나,
   **신 채널 표는 gray 만 5행 전량 등재**이고 **swim·sweat 는 부분 등재**였다 ⟹
   **사전 등록 문언이 «집행 불가»**였다(v3-67 §1-4 가 그 자리에서 사실로 등재).
 **귀책 = 전략 세션**(등재 «상태»를 확인하지 않고 문언을 썼다). **경미** — 불일치 0 이었고
   판정이 바뀌지 않았다. **#117·#118·#82확장·#119·#120 에 이은 5건째.**
```

### §0-B 결손 처분 수령 (v3-69 §5 상신분)

```
 ① **경로 = (a) «구운 몸 정점 배열» 주입.**
    **(b) v3 안에 본 스케일 재구현 = 기각** — 사유 **이중 구현 «발산» 회피** · **v2 검증 재사용**.
 ② **BEXT = «주입 몸 실측 바운딩»에서 도출.**
    하드코딩 `[1.78, 1.765, 0.282]` 은 **「기본 몸 캐시」로 강등**하고 **무삭제**로 남긴다.
 ③ **③b = 「기본 몸 «전용»」 한정 재등재 + 일반형 `h × 25%` «도출식» 복원.**
    **완화가 아니라 «도출 복원»** — 근거 v3-69 §1-5 인용(「④ 가 칸마다 다른 수가 된다」).
```

### §0-C 민감도 산출물 «분류» 수령

```
 민감도 판의 산출물은 **「측정 «전용» · «정본» 아님」** 으로 분류한다.
 ⟹ **Node 채널 허용**. **20프레임 브라우저↔Node 비트 대조는 «정본 편입»에만** 걸린다 —
    `docs/v3/부록-상태재현.md §5` **문언은 그대로 유지**한다(고치지 않는다).
```

### §0-D §1 전환 (사전 등록)

```
 ① `v3DressWorker` 입력에 **`bodyVerts?: Float32Array`** 신설 (+ **정점 수·법선 규약 명기**) —
    **존재 시 `parseGlb` 몸 «대신»** 쓴다. **`glb` 경로는 유지**(기본값 · **무삭제**).
 ② `dressRun`: **BEXT = 주입 정점 «실측 바운딩»에서 도출**.
    `bodyScale` 곱 형태는 **주입 경로에서 폐기**(스케일이 이미 «정점»에 반영돼 있다) ·
    **`glb` 경로는 기존 그대로**.
 ③ 메인 스레드: **«현 슬라이더 몸»을 굽고 워커에 넘기는 연결부**.
    **v3 패널에 «몸 주입» 경로**(제품 화면 노출 **0** · **계기 성격**).
```

### §0-E §2 자기검사 (사전 등록 · **0프레임**)

```
 **㉮ 등가** — **스케일 1(기본 몸)** 주입 정점 배열 **≡** `parseGlb` 경로 정점 배열 · **비트 대조**
    **일치 ⟹ 물리 재실행 없이 «경로 등가» 성립** · **불일치 ⟹ 정지 · 차이 «사실만»**(갈래 B)
 **㉯ 도출 스모크**(조립까지만 · **물리 0프레임**) — **본 스케일 몸 1개**(**가슴둘레 +10%** ·
    **v2 슬라이더 값으로 명기**) 주입 → **BEXT · h · band · ③b 문턱 · Y_TOP · 목선 · AXIS_Z** 가
    전부 **«그 몸의» 도출값**으로 나오는지 **값 등재**(**기본 몸 값과 병기**).
    **판정은 「도출식이 «돌았는가»」만** — 값의 옳고 그름은 이 판의 정의역 밖이다.
 **㉰ 기본 몸 «회귀»** — 기존 **정본 3종** 재생·게이트 **값 채널 무변화**
    (**blob sha · 스트립 · G7**).
```

### §0-F §3·§4

```
 **§3 등재** `docs/v3/49`(이 노트) · **#116 갱신**(③b **한정 재등재** · **구 문언 무삭제**) ·
    **BEXT 강등** · **색인**.
```

| 갈래 | 조건 | 귀결 |
|---|---|---|
| **A** | ㉮ **비트 동일** · ㉯ 도출 **전량 성립** · ㉰ 회귀 **무변화** | **완료 상신**(민감도 판 준비 완료) |
| **B** | **㉮ 불일치** | 정지 · **차이 사실만** |
| **E** | 판정 불가 | 정지 · 사실만 |

### 이 판에서 바꾸지 않는 것

**물리 0프레임** · **`solver.ts` diff 0** · **`s4Gate` 판정 로직 0줄**(**문턱 «도출부»만** · diff 등재) ·
**색·표시·표 채널 0줄** · **정본 0** · **v2 코드 무삭제** · 문턱 **«③b 도출 복원 외» 0**.

### 순서

```
 §0 커밋 → §1 → §2 «등재» 커밋 → §2 집행 → §3 → §4 판정 · 상신
```
