# 45 — G7 ③ 정정 + 프린트 «상하 반전» 해소 (v3-65 · **물리 0프레임**)

v3-65. 브랜치 `v3-65-print-flip`(사전 등록 · **이 CC 세션 전용**). 호출한 ecc 에이전트/스킬: **0 / 0**

## git 스탬프

```
 브랜치  v3-65-print-flip           워크트리  /Users/banshk/projects/fit-simulator
 기점    70d8cd0 (v3-64 HEAD)       직전 회차  70d8cd0
 커밋    (§0 → §1 → §2 → §3 → §4 순서로 채운다)
```

---

## §0 판정 수령 — **집행 «전» 커밋**

### §0-1 화면 판정 수령

```
 **불합격 1건 = 프린트 «상하 반전»**(**좌우 보존** · **3종 동일**).
 **합격 성분** = 알파 합성 · 배치 · 3종 동일 · 드레이프 추종 ⟹ **잔여 결함은 «반전» 하나**.
 ⟹ v3-64 가 「CC 화면 판정 0」으로 남긴 자리를 전략 세션이 채웠다. **이 판은 그 판정을 전제로 집행한다.**
```

### §0-2 귀책 수령 **2건** — 둘 다 **전략 세션**(#117 · #118 · #82확장에 이어 **연속 4건**)

```
 ① **G7 ③ 부등식 «반전» 등록** — v2 계약 «유도»(어깨 v > 밑단 v · v2 패턴 y 하향 증가가 근거)와
    **정반대**인 「어깨 v **<** 밑단 v」를 v3-59 에 등록했다.
    **함정 14(결과에 맞춰 문턱 조정) 아님**의 근거 2건:
      ⓐ 정정 방향이 **「통과 → 실패」**다(느슨하게 만드는 방향이 아니다)
      ⓑ 근거가 **결과가 아니라** «계약 문언 유도» + «방향이 자명한 자산의 픽셀»이다
 ② **v3-59 화면 판정 「정립 확인」 무효** — **방향이 모호한 자산**이라 반전을 못 걸렀다.
    ⟹ 그 회차의 「정립」 문장은 **근거를 잃는다**(원문 무삭제 · 이 노트가 정정의 자리).
```

### §0-3 back 반점 — **조명 하이라이트 귀속 수용 · 종결**

```
 v3-64 §3-2 는 **판정 불가**로 남겼다. 전략 세션이 **조명 하이라이트 귀속**을 수용·확정했다.
 ⟹ **종결 등재** · **#89 렌더 기여 목록 갱신**. **이 판의 처방 0.**
```

### §0-4 신설 함정 (번호 **CC 배정**)

```
 **#119 「화면 판정용 자산은 방향·대칭이 «자명»해야 한다」** — 귀책 **전략 세션** · 실례 **v3-59**.
 **#120 G7 ③ 정정 이력** — **구 문언 무삭제** · 정정 문언과 유도를 병기한다.
```

### §0-5 §1 반전 지점 측정 — **고치기 «전에» 값으로**(추정 0)

```
 세 층의 부호를 **각각** 등재하고 **«곱»이 화면 반전을 설명하는지** 확인한다 —
 **한 층만 고쳐 우연히 맞는 «이중 반전»을 남기지 않기 위함**이다.
   ㉮ `printUv` 의 v 부호 (**v3 제도 y 축 방향을 «좌표값»으로 실측**)
   ㉯ `productView`/텍스처의 **flipY 규약**
   ㉰ 캔버스 합성의 **y 방향**
```

### §0-6 §2·§3·§4

```
 **§2** G7 ③ **문언 정정**(유도 병기 · 구 문언 무삭제) + **§1 이 가리킨 «그 층» 하나만** 수정
        (**실제 수정 위치를 diff 로 등재**) + **G7 재판정 3종 × 3건**(①·② **불변 확인** 포함).
 **§3** **방향 자기검사 «신설»**(픽셀 수준 · 사전 등재) — 자산 원본 불투명 영역을 상/하로 갈라
        평균색 2값 → 렌더 캡처의 가슴 프린트 영역에서 **같은 절차** → **대응 일치 판정**
        (**반전이면 교차**) · **손 상수 0**.
 **§4** 캡처 **3종 × front/side/back-p 9장** + 스트립 자기검사 **258/258/290** + blob 무변조.
```

### §0-7 §5 갈래 (사전 등록)

| 갈래 | 조건 | 귀결 |
|---|---|---|
| **A** | §1 특정 · §2 정정 · §3 대응 일치 · 게이트 전량 | **캡처 상신**(α 완결 «후보») |
| **B** | §1 에서 **층 특정 불가** 또는 **이중 반전** 발견 | 정지 · **부호표 사실만** 상신 |
| **E** | 판정 불가 | 정지 · 사실만 |

### 이 판에서 바꾸지 않는 것

**물리 0프레임** · **`solver` / `garmentScene` / `s4Gate` diff 0 «기본»**(§2 예외는 **diff 등재**) ·
**핏 색 채널 0줄** · 문턱·정본 **«G7 ③ 정정 외» 0** · **v2 경로 0줄** · **`V2DIMS` off**.

### 순서

```
 §0 커밋 → §1 측정 → §2 정정 → §3 등재·집행 → §4 → §5 판정 · 상신
```

---

## §1 반전 «지점» 측정 — **고치기 전에 값으로**(추정 0 · 수정 0줄)

계기 **`scripts/v3PrintFlip.ts` 신설**(제품 코드 0줄 · 물리 0프레임 · 주입만).

### ㉮ `printUv` 의 v 부호 — **v3 패턴 y 는 «위»로 증가한다**

**어깨/밑단을 «패턴 y» 로 가르지 않는다** — 그것이 정정 대상이라 **순환**이다(#118).
**독립 채널 = 정착 «세계 y»**(blob 주입분). 세계에서 위에 있는 쪽이 어깨다.

| 패널 | 패턴 y 범위 | 어깨측(세계 y) | → v | 밑단측(세계 y) | → v | 결과 |
|---|---|---|---|---|---|---|
| front | 0.00~70.00cm | 패턴 y **최대**(**149.30cm**) | **0.0000** | 패턴 y 최소(85.69cm) | **1.0000** | 어깨 v **<** 밑단 v |
| back | 0.00~70.00cm | 패턴 y **최대**(149.25cm) | 0.0000 | 패턴 y 최소(84.25cm) | 1.0000 | 어깨 v **<** 밑단 v |
| sleeveR | −9.08~12.92cm | 패턴 y **최대**(149.49cm) | 0.6857 | 패턴 y 최소(138.93cm) | 1.0000 | 어깨 v **<** 밑단 v |
| sleeveL | −9.08~12.92cm | 패턴 y **최대**(148.80cm) | 0.6857 | 패턴 y 최소(138.97cm) | 1.0000 | 어깨 v **<** 밑단 v |

**소스 근거**(`garmentScene.ts:388-389`): 어깨 `topB` 를 **y = L**, 밑단 `botB` 를 **y = 0** 에 둔다
⟹ **v3 패턴 y 는 위로 증가**. **v2 는 아래로 증가**했고, `printUv` 의 `1 −` 는 **v2 의 사실**을
v3 에 그대로 옮긴 것이었다.

### ㉯ 텍스처 flipY 규약 — **`true`(three 기본값) · 제품 코드 설정 «0건»**

```
 node_modules/three/src/textures/Texture.js:281   this.flipY = true;
 제품 코드(V3Panel · productView)의 flipY 설정: **0건 ⟹ 기본값 그대로**
 ⟹ **캔버스 «위»(y=0) ↔ v = 1**
```

### ㉰ 캔버스 합성 y 방향 — **프린트는 «위»에 놓인다**

```
 printComposite.ts:47   const PRINT_TOP_FRACTION = 0.25;
 printComposite.ts:191  const destY = OUTPUT_SIZE * PRINT_TOP_FRACTION;
 printComposite.ts:198  ctx.drawImage(img, box.x, box.y, box.w, box.h, destX, destY, destW, destH);
 ⟹ 캔버스 y 는 «아래»로 증가(2D 규격) · 프린트는 위에서 25% 지점 ⟹ **v ≈ 0.75 대역**
```

### ★ 곱 — **틀린 층은 «하나»다**(이중 반전 **아님**)

```
 ㉰ 프린트를 캔버스 «위»에 → ㉯ 캔버스 «위» = v 1 → **프린트는 v 큰 쪽에 놓인다**
 ㉮ 는 **v 큰 쪽 = «밑단»** 으로 보낸다
 ⟹ 프린트가 **밑단 방향으로 · 상하 뒤집혀** 나온다. u 에는 뒤집기가 **없다** ⟹ **좌우 보존**.
 **화면 증상(상하 반전 · 좌우 보존 · 3종 동일)과 «정확히» 일치한다.**
 두 층이 동시에 틀렸다면 화면은 **정립으로 보였을 것**이다 ⟹ **㉮ 단독** · **갈래 B 불발화**.
```

---

## §2 정정

### §2-1 G7 ③ 문언 정정 (#120 · **구 문언 무삭제**)

```
 구  「어깨측 정점의 v **<** 밑단측 정점의 v」   ← v2 계약 «유도»와 정반대(귀책 = 전략 세션)
 신  **「어깨측 정점의 v > 밑단측 정점의 v」**   ← 텍스처 «위»(v=1) = 어깨
 유도  ㉯ flipY=true 가 캔버스 «위»를 v=1 로 올린다 ⟹ 어깨가 v 큰 쪽이어야 프린트가 정립한다.
 **함정 14 아님** ⓐ 정정 방향이 **「통과 → 실패」** ⓑ 근거가 결과가 아니라 **계약 문언 유도 + 픽셀**.
 **가르는 채널도 정정** — 어깨/밑단을 «패턴 y» 가 아니라 **정착 세계 y**(독립 채널)로 가른다(#118).
```

### §2-2 수정 위치 — **`src/v3/printUv.ts` «한 파일»**

```
 구  uv[i+1] = 1 − (uvM[i+1] − b.yMin) / scaleM      ← 어깨(y 최대) → v 0
 신  uv[i+1] = 1 − (b.yMax − uvM[i+1]) / scaleM      ← **어깨측 끝(y 최대) → v 1**
 v2 계약의 «문언»은 그대로다(어깨 끝 → v=1 · 공통 scale · 축척 보존). **y 축 방향만** v3 것으로 읽었다.
```

**기점 `70d8cd0` 대비 diff 0 확인**(전량):
`garmentScene.ts` **0** · `solver.ts` **0** · `s4Gate.ts` **0** · `printComposite.ts` **0** ·
`productView.ts` **0** · `V3Panel.tsx` **0**.
변경 = `printUv.ts`(**제품 코드 유일**) + 계기 2개(`v3PrintUvCheck.ts` 문언·구현 정정 ·
`v3PrintFlip.ts` 신설). `npx tsc -b` **rc=0** · `npm run build` **rc=0**.

### §2-3 G7 재판정 — **3종 × 3건 전량 통과**

| 정본 | ① [0,1] 위반 | ② 축척 편차 max | ③ 어깨 v > 밑단 v |
|---|---|---|---|
| gray d9 `faf4873f` | **0** / 10402 | **3.924e-8** ≤ 1.429e-4 | **4/4 성립**(어깨 v 1.0000 · 밑단 0.0000/0.6857) |
| swim d9 `557bc446` | **0** / 10402 | **3.924e-8** ≤ 1.429e-4 | **4/4 성립** |
| sweat d8 `51d74c66` | **0** / 13248 | **3.924e-8** ≤ 1.429e-4 | **4/4 성립** |

**★ ① 은 불변 · ② 는 «불변이 아니다» — 사실대로 적는다**

```
 ① 위반 **0** — v3-59 와 **동일**(범위는 안 건드렸다).
 ② v3-59 **2.036e-8** → v3-65 **3.924e-8**. **「불변」이라 적지 않는다.**
    사유: 식이 `(y − yMin)` 에서 `(yMax − y)` 로 바뀌었으므로 **float32 반올림 경로가 다르다**.
    두 값 모두 **float32 half-ulp(5.96e-8) 미만**이고 문턱(1.429e-4)의 **약 1/3600** 이다
    ⟹ **축척 보존 «판정»은 불변**, **값은 반올림 수준에서 이동**. (관측 사실 · 처방 0)
```

---

## §3 방향 자기검사 «신설» — 픽셀 수준 · **사전 등재** · 손 상수 0

계기 **`scripts/v3PrintDir.ts` 신설**. 절차는 §0-6 에 적은 그대로다.
**상수는 전부 «소스에서 뜬다»**: 계약 문턱 **55**(`printComposite.ts:44`) ·
불투명 기준 **α ≥ 250**(v3-62 규칙의 여집합 · `v3AssetScan.ts`). **새 수 0.**

**① 자산 원본** `print-graphic.png` 1200×1200 · 불투명 bbox (230,210)~(970,1025)

```
 **상반부** rgb(253.6, 136.9, 69.3) · n=199,262      **하반부** rgb(89.4, 77.4, 62.7) · n=173,282
 두 색 사이 거리 **174.78** — 계약 문턱 55 의 **3.2배** ⟹ 분류자로 쓸 만하다(값 근거).
```

**② 계기 «자체» 검증 — 알려진 반전 사례에서 반전이 나오는가**(먼저 확인했다)

| 캡처(정정 **전** · v3-64) | 상반부색 y 중앙 | 하반부색 y 중앙 | 판정 |
|---|---|---|---|
| gray `faf4873f` front | 533 | 505 | **교차 = 반전** |
| swim `557bc446` front | 526 | 506 | **교차 = 반전** |
| sweat `51d74c66` front | 544 | 521 | **교차 = 반전** |

⟹ **3/3 반전**. 전략 세션의 화면 판정(§0-1)과 **독립 채널에서 일치**한다 — 계기가 유효하다.

**③ 정정 «후» 판정**

| 캡처(정정 **후** · v3-65) | 상반부색 y 중앙 | 하반부색 y 중앙 | 판정 |
|---|---|---|---|
| gray `faf4873f` front | **448** | 517 | **대응 일치 = 정립** |
| swim `557bc446` front | **443** | 516 | **대응 일치 = 정립** |
| sweat `51d74c66` front | **455** | 534 | **대응 일치 = 정립** |

⟹ **3/3 정립**. **전후가 같은 계기·같은 자산·같은 문턱**에서 뒤집혔다.

---

## §4 캡처 · 게이트

`docs/captures/v3-65-반전해소/` — **9장**(3 정본 × `front` / `side` / `back-p` · 정본 sha 셀 병기).

| 게이트 | 값 | 판정 |
|---|---|---|
| G1 v3 → `lib/` 임포트 | **0건** | ✅ |
| G2 `tsc -b` | rc=0 | ✅ |
| G3 build | rc=0 | ✅ |
| G4 물리 프레임 | **0** | ✅ |
| G7 ①②③ | **3종 × 3/3** (§2-3) | ✅ |
| blob 무변조 | gray `8dec6eca…` · swim `786f3fc0…` · sweat `dcaceda3…` | ✅ **등재값 일치** |
| 스트립 자기검사 | **258 / 258 / 290**(idx 1548 / 1548 / 1740) | ✅ **기준 일치** |

### §4-1 계기 사실 1건 (집행 경로 · 판정 아님)

```
 상태 선택 `<select>` 가 **합성 키 입력에 반응하지 않았다**(Down/Return 3회 무반응).
 ⟹ 브라우저 콘솔에서 **`change` 이벤트를 발화**시켜 정본을 바꿨다(**값 조작 0** ·
    선택지는 화면에 이미 있는 것 · 전후 라벨을 캡처로 확인: 「swim d9 정본 (정착 260)」·
    「sweat d8 정본 (정착 330)」 · 정착 프레임 **260 / 330** 화면 확인).
 부수: `~/Downloads` 에 **v3-64 잔류 캡처 3장**(sweat · 8/24 23:59)이 있어 **먼저 삭제**했다
    — 정정 «전» 산출물이 «후» 산출물로 섞이는 것을 막기 위함(오염 0).
```

---

## §5 판정 — **갈래 A**

| 조건 | 실측 | 충족 |
|---|---|---|
| §1 특정 | ㉮ 단독 · 곱이 증상을 정확히 설명 · **이중 반전 아님** | ✅ |
| §2 정정 | `printUv.ts` **한 파일** · 나머지 6파일 diff 0 · G7 3종 3/3 | ✅ |
| §3 대응 일치 | **3/3 정립**(정정 전 **3/3 반전**과 대조) | ✅ |
| 게이트 전량 | G1~G4 · G7 · blob · 스트립 258/258/290 | ✅ |

⟹ **갈래 A** — **캡처 9장 상신**(α 완결 «후보»).

**함께 상신하는 «사실» 2건**(판정을 바꾸지 않는다):
① §2-3 **G7 ② 는 «불변이 아니다»**(2.036e-8 → 3.924e-8 · float32 반올림 경로 변경 · 판정 불변)
② §4-1 계기 사실(`select` 조작 경로 · 잔류 캡처 삭제).

**α 완결 선언 여부는 전략 세션이 한다** — 이 판은 **선언하지 않는다**.
**CC 화면 판정 0** — §3 은 픽셀 채널의 «방향» 판정이고, 보기 판단이 아니다.
