# v3-83 — unitScale 폴백 분리 · 순회 순서 교란 · 몸 굽기 게이트 승격

**물리 0프레임 · 옷 재생 0 · 몸 굽기 = 측정용 산출만(다운로드 0 · blob 27 무변조)**

| | |
|---|---|
| 브랜치 | `v3-83-body-bake-gate` |
| 기점 | **`331897d`** (= v3-82 HEAD) |
| §0 커밋 | **`4d596c8`** — 집행 «전» 별도 커밋(sha 만 그 뒤 한 줄로 채웠다) |
| 호출 ecc 에이전트/스킬 | **0** |

---

## §0 수령 (집행 «전» 별도 커밋 — v3-82 §5 이탈 재발 금지)

### §0-1 판정문 (전략 세션 · v3-82 §4 수령 · 원문)

```
 「갈래 A 수령 · 기전 성립(첫 칸 미수렴 굽기 · **순서 교란만 미검정**) ·
   unitScale=1 유력 = **① 항등 폴백**(② 는 Scene.scale.x=1 과 불일치 · **단정 0**) ·
   v3-81 「재현 불가」 정정 = **CC 귀책** · v3-82 §0 생략 = **CC 귀책·판정 유지**
   (발주 프롬프트가 기대를 담음) · 1몸 재굽기 = 권고 «한다» · 승인 승혁 ·
   **이 판 재굽기 0** · 몸 인덱스 생산 시점 기록 = 한다」
```

★ **귀책 2건을 수령한다** — v3-81 「재현 불가」 조기 단정 · v3-82 §0 생략. 둘 다 CC 귀책이고 다투지 않는다.

### §0-2 잠금 «해제» 범위

| 파일 | 허용 |
|---|---|
| `Mannequin.tsx` | **계기(인쇄)만** · **동작 변경 0** · `lerp`/`snap`/`EPS` **0** |
| `V3Panel.tsx` 그리드 굽기 | 게이트·기록 추가 허용 · 순회 순서는 **측정용 env 플래그로만**(기본 순서 불변) |

### §0-3 바꾸지 않는 것

```
 물리 코어 4파일 · `framing`/`match`/`provide` · 정본 3파일(108 · 35 · 37) ·
 `body-index-27.json`(**v3-82분 무삭제** · 생산 기록은 «별도 파일») · **문턱 신설 0** · **재굽기 0**
```

### §0-4 사전 등재 기대 (집행 «전»)

**① unitScale 폴백 분리**

| 갈래 | 조건 |
|---|---|
| (가) | `rawHeight ≤ 0.001` ⟹ **폴백 발화** · `unitScale = 1` (조용한 폴백 · 자리 `:301`) |
| (나) | `rawHeight ≈ 1.70` ⟹ 이미 배율 걸린 대상을 잰다(**어느 객체인지 등재**) |
| (다) | `rawHeight ≈ 1.7651` 인데 `unitScale = 1` ⟹ 계산 **뒤** 어딘가가 되돌린다(자리 등재) |
| (라) | 그 외 |

**② 순회 순서 교란** (`BAKE_ORDER=rev` · 측정 전용 · 기본 불변)

| 갈래 | 조건 |
|---|---|
| (가) | 새 첫 칸(`c122.5-h185-s50`)이 600·잔차>1e-4 이고 `c87.5-h155-s40` 은 3·0 ⟹ **「첫 칸」 확정** |
| (나) | 여전히 `c87.5-h155-s40` 만 600 ⟹ **「그 몸」** · 기전 재개 |
| (다) | 둘 다 또는 둘 다 아님 |

**③ 몸 굽기 게이트 — 규칙(새 수 0)**

```
 ㉮ 상한 600 도달 칸 = **몸 굽기 실패** — blob 저장 0 · 인덱스에 실패 행 ·
    **재시도 1회**(그 칸을 순회 «끝»에 다시) · 재시도도 실패면 **칸 실패 확정**
 ㉯ 통과 문턱 = `POSE_SETTLE_EPS` **1e-4**(기존 상수 그대로 · 신설 0)
 ㉰ 높이 검사 = |실측 − 목표| ≤ **0.0020m**(v3-82 `body-index-27.json` 등재 문턱 «인용» · 신설 0)
 ㉱ 생산 시점 기록 = `public/v3diag/body-index-<날짜>.json` —
    id · 목표 · 실측 높이 · 잔차 · 전진 프레임 · 굽기 잔차 · sha256 · **unitScale 실측값** · 순회 위치
 검증 = **다운로드 0 드라이런 1회**로 27행 산출.
 기대: ① 이 (가)면 27행 전부 `unitScale = 1` 이라 **높이 검사 ㉰ 에서 27/27 «실패»**
      (폴백 상태 굽기를 게이트가 **잡는다**는 증명) · **첫 칸은 ㉮ 로도 실패**.
```

**④ 사실 확인** — 「정본 3종 몸 **1.7651** ≠ 그리드 기본몸 **1.7000**」이 볼트·노트에 등재된 적 있는가(grep) ·
있으면 자리 · 없으면 **신규 사실로 등재**(판정 0).

### §0-5 갈래 (집행 «전» 등재)

| 갈래 | 조건 | 처분 |
|---|---|---|
| **A** | ①②③④ 전량 | 상신 |
| B | ① (라) | 사실 · ②③ 집행 |
| C | ③ 드라이런이 폴백을 못 잡음(27행 통과) | 게이트 정의 재검 · 상신 |
| D | 그 외 | 정지 |

**사후 갈래 신설 0.**

---

## §1′ 집행

### §1-①′ `unitScale` 폴백 분리 — **갈래 (나)** ★ 전략 세션 추정과 «다르다»

**계기**(`Mannequin.tsx:295-307` · **추가만 · 삭제 0줄** · 동작 변경 0 · `lerp`/`snap`/`EPS` 손대지 않음)

**실측 — `?v2=1&v3=1` 새 적재 1회에 계산 «4회»**

| # | rawHeight | 계산 시점 `scene.scale.x` | unitScale | 폴백 |
|---|---|---|---|---|
| 1 | **1.765052** | 1.000000 | **0.963145** | 미발화 |
| 2 | 1.765052 | 1.000000 | 0.963145 | 미발화 |
| 3 | **1.700000** | **0.963145** | **1.000000** | 미발화 |
| 4 | 1.700000 | 0.963145 | 1.000000 | 미발화 |
| 최종 | | **`Scene.scale.x = 1`** | | |

```
 ⟹ **갈래 (나)**: `rawHeight ≈ 1.70` — **이미 배율이 걸린 «그 씬 객체»를 다시 잰다.**
   어느 객체인가: **`useGLTF` 가 캐시해 «공유»하는 GLTF `scene`** 이다.
   ① 이 0.963145 를 계산해 `setScalar` 로 적용하고 → ③ 이 그 «적용된» 씬을 재서 몫 1 을 얻고
   → `setScalar(1)` 로 **되돌린다**. 마지막에 남는 값이 1 이다.
 ★ **폴백은 한 번도 발화하지 않았다**(`rawHeight` 는 늘 ≫ 0.001) ⟹
   전략 세션의 「**① 항등 폴백 유력**」 추정은 **실측으로 기각**된다. 판정문 원문은 「단정 0」이었고,
   이 판이 값으로 갈랐다. **처방 0 · 수정 0**(자리와 갈래만 등재).
 ★ v3-82 의 「인스턴스 2개 가설 기각」도 **정정**된다 — 계산이 «4회» 일어난다는 사실이
   그 가설을 되살린다(2 인스턴스 × StrictMode 이중 호출로 설명 가능). **단정하지 않는다**:
   확정하려면 인스턴스별 식별자를 찍어야 하고 그것은 이 판의 계기 범위 밖이다.
```

**v3-77 때와 지금의 차이 — 코드 사실**(`git log` · v3-77 이후 두 파일)

```
 `Mannequin.tsx` — v3-77 이후 **이 판의 계기 삽입이 유일한 변경**이다(그 전 마지막 변경은 v3-72 이전).
 `V3Panel.tsx`  — v3-79(라벨) · v3-83(게이트) 변경. **굽기 로직은 v3-77 이후 이 판까지 불변**이었다.
 ⟹ **코드는 안 바뀌었다.** 바뀐 것은 **적재 순서·타이밍**(어느 화면이 먼저 마운트되는가)이고,
   그 축은 v3-79 의 **기본 라우팅 교체**(v2 → v1)로 실제로 움직였다. **단정 0 · 귀속 0.**
 ★ 교차 증거: v3-72 metrics 는 「**BEXT y = 정확히 1.700000m** = Mannequin 기준 신장 정규화」를 등재했다
   ⟹ 그때는 **unitScale 이 실려 있었다**. 지금은 1.7651 이다.
```

### §1-②′ 순회 순서 교란 — **갈래 (가)** · 「첫 칸」 **확정**

**`?bakeorder=rev` · 새 적재 1회 · 드라이런(다운로드 0)**

| 칸 | 순회 위치 | 전진 | 굽기 잔차 |
|---|---|---|---|
| **`c122.5-h185-s50`**(새 첫 칸) | **0** | **600** | **6.82e-2** ( > 1e-4 ) |
| **`c87.5-h155-s40`**(옛 첫 칸) | 26 | **3** | **0** |

```
 본순회 전진 분포(rev) — **3: 25칸 · 141: 1칸 · 600: 1칸**. 600 은 **새 첫 칸 하나뿐**이다.
 ⟹ **갈래 (가)**: 미수렴은 **「그 몸」이 아니라 「순회의 첫 칸」**의 성질이다. **확정.**
 ★ v3-82 가 「관측된 정합」으로만 남긴 것을 **교란 실험으로 확정**했다.
```

### §1-③′ 몸 굽기 게이트 승격 — **드라이런이 폴백 상태를 «잡는다»**

**규칙 구현**(`V3Panel.tsx` 그리드 굽기 · 새 수 0 · 문턱은 전부 인용)

```
 ㉮ 상한 600 도달 ⟹ 실패 · blob 저장 0 · **재시도 1회(순회 끝)** · 재시도도 실패면 확정
 ㉯ 굽기 잔차 ≤ `POSE_SETTLE_EPS` = 1e-4   ㉰ |실측 높이 − 목표| ≤ 0.0020m
 ㉱ 기록 = `public/v3diag/body-index-2026-08-29.json`(신설 · **`body-index-27.json` 무삭제**)
 순서 플래그 `?bakeorder=rev` / `VITE_BAKE_ORDER=rev` — **기본 순서 불변** · 드라이런 `?bakedry=1`
```

**드라이런 결과 — 기본 순서 · 다운로드 0**

| 항목 | 값 |
|---|---|
| 행 | **54**(본순회 27 + 재시도 27) |
| **통과** | **0** |
| 실패 | **54** |
| `unitScale` 실측 | **전 행 1** (유일값) |
| 높이 잔차 범위 | **+0.0581 ~ +0.1483 m** (문턱 0.0020m 의 **29~74배**) |
| 본순회 전진 분포 | 3: 25 · 121: 1 · **600: 1**(첫 칸) |
| 첫 칸 `c87.5-h155-s40` | **㉮ · ㉯(5.09e-2) · ㉰(0.1483m)** — **세 조항 모두 실패** |

```
 ⟹ **사전 등재 기대와 «일치»**: 「① 이 (가)면 27행 전부 unitScale=1 ⟹ ㉰ 에서 27/27 실패 ·
   첫 칸은 ㉮ 로도 실패」. ①이 (가)가 아니라 (나)였지만 **귀결(unitScale=1)은 같았고**,
   게이트는 **폴백/미적용 상태 굽기를 27/27 잡았다**. §0-5 의 C(게이트가 못 잡음)는 **불성립**.
 ★ 부수 사실: 첫 칸의 **재시도**는 순회 끝이라 **전진 3 · 잔차 0 으로 수렴**했다 —
   즉 **재시도는 ㉮·㉯ 를 해소하지만 ㉰ 는 못 해소한다**(unitScale 은 칸의 문제가 아니다).
   ⟹ 게이트 두 층이 **서로 다른 결함을 잡는다**는 것이 값으로 보인다.
```

### §1-④′ 사실 확인 — **이미 등재돼 있다**(신규 아님)

| 자리 | 원문 |
|---|---|
| `.obsidian-log/00-현재상태.md:231-232` | 「`parseGlb`(원본 T포즈 **1.765m**) ↔ 화면 마네킹(정규화 **1.700m** · A포즈)은 **다른 몸**이다」 |
| `docs/metrics-log.md:25341` | 「BEXT y = **정확히 1.700000 m** = Mannequin 의 기준 신장 정규화(원본 parseGlb 는 **1.765 m**)」 |

```
 ⟹ **v3-72 시점에 이미 등재**됐다. 이 판은 **자리만 가리키고 판정하지 않는다**.
   다만 그 등재가 **지금은 성립하지 않는다**는 사실을 병기한다 — 오늘 화면 마네킹은 **1.7651m** 다.
```

---

## §2′ 자기검사

| # | 항목 | 결과 |
|---|---|---|
| ㉠ | ① 갈래·표 | **(나)** · 계산 4회 표 |
| ㉡ | ② 갈래·표 | **(가)** · 새 첫 칸 600/6.82e-2 · 옛 첫 칸 3/0 |
| ㉢ | ③ 드라이런 27행 | **54행**(27+재시도 27) · 통과 0 · 실패 54 |
| ㉣ | 몸 blob 27 sha 전후 동일 | **변경 0건** |
| ㉤ | 정본 3파일 diff · `body-index-27.json` diff | **0 · 0** |
| ㉥ | `Mannequin.tsx` diff = 인쇄 줄만 | **삭제 줄 0** — 순수 추가(diff 원문은 커밋 `249c8e0`) |
| ㉦ | 물리 코어 · `framing`/`match`/`provide` diff | **0 · 0** |
| ㉧ | `tsc -b` · v3-81 회귀 · v3-80 프레이밍 회귀 | 통과 · **전량 통과** · **통과** |

**§3 캡처 — 0장**(사전 등재 그대로).

---

## §4′ 판정 — **갈래 A**

```
 A = ①②③④ 전량 ⟹ 상신
   ① **(나)** · ② **(가)** · ③ 게이트 승격 + 드라이런 54행(폴백 상태 27/27 포착) · ④ 기등재 확인
 B(① 라) 불성립 · C(게이트가 못 잡음) 불성립. 사후 갈래 신설 0 · 기대 사후 변경 0.
 ★ §0 은 집행 «전» 별도 커밋 `4d596c8` 로 남겼다(v3-82 §5 이탈 **재발 없음**).
```

### §4-1 함정 후보 2건 — **등재 «요청»**(번호 부여는 대장 몫)

```
 후보 ㄱ 「**순회의 첫 항목은 직전 상태가 없어 다른 체제를 탄다**」
   근거: v3-77(첫 칸 600) · v3-82(재현) · **v3-83 순서 교란으로 «첫 칸» 확정**.
   일반형: **직전 프레임/직전 항목에 의존하는 수렴 루프는 «순회 첫 항목»에서 다른 값을 낸다.**
   방어: 순회 전 «워밍업» 또는 첫 항목을 **버리고 끝에 다시** 굽는다(이 판의 ㉮ 재시도가 그 형태다).

 후보 ㄴ **#121 계열 「조용한 항등 폴백」 사례 추가** — ★ 단, 이 사례의 실체는 폴백이 «아니었다»:
   `Mannequin.tsx:299` 의 `rawHeight > 0.001 ? … : 1` 은 **발화하지 않았고**,
   1 을 만든 것은 **「이미 적용된 상태를 다시 재서 몫이 1 이 되는」 자기 상쇄**다.
   일반형: **정규화 계수를 «대상 자신»에서 재 «대상 자신»에 적용하면, 두 번 계산될 때 항등이 된다.**
   ⟹ #121 의 이웃 함정으로 «따로» 세우는 편이 맞다는 의견을 붙인다(**번호·채택은 대장 몫**).
```

### §4-2 넘기는 것

| # | 항목 | 몫 |
|---|---|---|
| ㉠ | **1몸 재굽기 권고** — 다만 지금 굽으면 **27칸 전부 게이트 실패**다(unitScale=1) ⟹ **①을 먼저 처분해야 재굽기가 가능하다** | 승혁 · 전략 세션 |
| ㉡ | ① 의 **처방**(계산을 1회로 묶을지 · 계수를 씬 밖에 둘지) — 이 판은 처방 0 | 전략 세션 |
| ㉢ | 4회 계산의 **출처 확정**(인스턴스 2 × StrictMode 2 인지) — 계기 범위 밖 | 전략 세션 |
| ㉣ | 기록 파일의 **sha 16자 한계**(드라이런이라 산출물 없음 · 저장 모드 재실행이 정본) | 전략 세션 |
