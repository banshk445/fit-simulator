# v3-83 — unitScale 폴백 분리 · 순회 순서 교란 · 몸 굽기 게이트 승격

**물리 0프레임 · 옷 재생 0 · 몸 굽기 = 측정용 산출만(다운로드 0 · blob 27 무변조)**

| | |
|---|---|
| 브랜치 | `v3-83-body-bake-gate` |
| 기점 | **`331897d`** (= v3-82 HEAD) |
| §0 커밋 | **집행 «전» 별도 커밋** — sha 는 이 절 끝에 적는다 |
| 호출 ecc 에이전트/스킬 | **0** |

---

## §0 수령 (집행 «전» 별도 커밋 — v3-82 §5 이탈 재발 금지)

### §0-1 판정문 (전략 세션 · v3-82 §4 수령 · 원문)

```
 「갈래 A 수령 · 기전 성립(첫 칸 미수렴 굽기 · **순서 교란만 미검정**) ·
   unitScale=1 유력 = **① 항등 폴백**(② 는 Scene.scale.x=1 과 불일치 · **단정 0**) ·
   v3-81 「재현 불가」 정정 = **CC 귀책** · v3-82 §0 생략 = **CC 귀책·판정 유지**
   (발주 프롬프트가 기대를 담음) · 1몸 재굽기 = 권고 «한다» · 승인 승혁 ·
   **이 판 재굽기 0** · 몸 인덱스 생산 시점 기록 = 한다」
```

★ **귀책 2건을 수령한다** — v3-81 「재현 불가」 조기 단정 · v3-82 §0 생략. 둘 다 CC 귀책이고 다투지 않는다.

### §0-2 잠금 «해제» 범위

| 파일 | 허용 |
|---|---|
| `Mannequin.tsx` | **계기(인쇄)만** · **동작 변경 0** · `lerp`/`snap`/`EPS` **0** |
| `V3Panel.tsx` 그리드 굽기 | 게이트·기록 추가 허용 · 순회 순서는 **측정용 env 플래그로만**(기본 순서 불변) |

### §0-3 바꾸지 않는 것

```
 물리 코어 4파일 · `framing`/`match`/`provide` · 정본 3파일(108 · 35 · 37) ·
 `body-index-27.json`(**v3-82분 무삭제** · 생산 기록은 «별도 파일») · **문턱 신설 0** · **재굽기 0**
```

### §0-4 사전 등재 기대 (집행 «전»)

**① unitScale 폴백 분리**

| 갈래 | 조건 |
|---|---|
| (가) | `rawHeight ≤ 0.001` ⟹ **폴백 발화** · `unitScale = 1` (조용한 폴백 · 자리 `:301`) |
| (나) | `rawHeight ≈ 1.70` ⟹ 이미 배율 걸린 대상을 잰다(**어느 객체인지 등재**) |
| (다) | `rawHeight ≈ 1.7651` 인데 `unitScale = 1` ⟹ 계산 **뒤** 어딘가가 되돌린다(자리 등재) |
| (라) | 그 외 |

**② 순회 순서 교란** (`BAKE_ORDER=rev` · 측정 전용 · 기본 불변)

| 갈래 | 조건 |
|---|---|
| (가) | 새 첫 칸(`c122.5-h185-s50`)이 600·잔차>1e-4 이고 `c87.5-h155-s40` 은 3·0 ⟹ **「첫 칸」 확정** |
| (나) | 여전히 `c87.5-h155-s40` 만 600 ⟹ **「그 몸」** · 기전 재개 |
| (다) | 둘 다 또는 둘 다 아님 |

**③ 몸 굽기 게이트 — 규칙(새 수 0)**

```
 ㉮ 상한 600 도달 칸 = **몸 굽기 실패** — blob 저장 0 · 인덱스에 실패 행 ·
    **재시도 1회**(그 칸을 순회 «끝»에 다시) · 재시도도 실패면 **칸 실패 확정**
 ㉯ 통과 문턱 = `POSE_SETTLE_EPS` **1e-4**(기존 상수 그대로 · 신설 0)
 ㉰ 높이 검사 = |실측 − 목표| ≤ **0.0020m**(v3-82 `body-index-27.json` 등재 문턱 «인용» · 신설 0)
 ㉱ 생산 시점 기록 = `public/v3diag/body-index-<날짜>.json` —
    id · 목표 · 실측 높이 · 잔차 · 전진 프레임 · 굽기 잔차 · sha256 · **unitScale 실측값** · 순회 위치
 검증 = **다운로드 0 드라이런 1회**로 27행 산출.
 기대: ① 이 (가)면 27행 전부 `unitScale = 1` 이라 **높이 검사 ㉰ 에서 27/27 «실패»**
      (폴백 상태 굽기를 게이트가 **잡는다**는 증명) · **첫 칸은 ㉮ 로도 실패**.
```

**④ 사실 확인** — 「정본 3종 몸 **1.7651** ≠ 그리드 기본몸 **1.7000**」이 볼트·노트에 등재된 적 있는가(grep) ·
있으면 자리 · 없으면 **신규 사실로 등재**(판정 0).

### §0-5 갈래 (집행 «전» 등재)

| 갈래 | 조건 | 처분 |
|---|---|---|
| **A** | ①②③④ 전량 | 상신 |
| B | ① (라) | 사실 · ②③ 집행 |
| C | ③ 드라이런이 폴백을 못 잡음(27행 통과) | 게이트 정의 재검 · 상신 |
| D | 그 외 | 정지 |

**사후 갈래 신설 0.**
