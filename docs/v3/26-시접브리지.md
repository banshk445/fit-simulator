# 26 — 시접 브리지 (표시 층 · 물리 0프레임 · 모델 두께를 화면에 그린다)

v3-45. 브랜치 `v3-45-seam-bridge`(사전 등록). 호출한 ecc 에이전트/스킬: **0 / 0**

---

## §0 판정 기준 — 실행 «전»에 등록

> 이 절을 커밋한 «뒤»에만 구현으로 간다. 이후 결과를 보고 어떤 수도 고치지 않는다.

### §0-1 배경 (전략 세션 판정 수령분)

```
 v3-44 후보표 판정: **(나) 끊김 = #89·#94 족 — 물리 결함 0.**
 (가) 어깨는 이 판에서 **수정 0** — 브리지 재캡처에 대한 사용자 재판정 뒤 결정.
 후보 ④(대역 바닥 15~17mm) · sweat 어긋남은 **관측 등재 유지 · 손대지 않는다.**
```

### §0-2 근거 논증 — **이 판이 문서에 등재한다** (#100 후보)

```
 ① `SEP = 2 × THICK = 2mm` 는 **시접 rest 이자 자기충돌 «분리 거리»와 «같은 값»**이다
    (`src/v3/consts.ts:8` · `garmentScene.ts:710` · 원 커밋 `88c52ab`).
 ② 따라서 **이 모델에서 중심면 2mm 의 두 겹은 «표면이 맞닿은» 상태다.**
    v3-13 §3 원문: 「두 겹 원단이 이음선에서 갖는 **실제 간격이 정확히 두 두께**이므로
    **rest = 2×두께**가 옳고」 — 즉 2mm 는 «벌어짐»이 아니라 «닿음»의 좌표다.
 ③ 그런데 **렌더는 두께 0 중심면을 그린다**(#89 렌더 기여 목록 · v3-42 §0-2 ㄱ).
    ⟹ **모델이 «닫았다»고 간주하는 접촉을 화면이 «벌어진 갭»으로 그린다.**
 ④ v3-44 ㉠2 실측과 정합: sideXplus 시접 대역의 **배경 픽셀이 0~1** 이고 보이는 것은 **몸**이다
    — 「뚫린 구멍」이 아니라 «두 겹 사이로 몸이 비치는» 형태. ③ 이 예측하는 바로 그 형태다.
 ⟹ **시접 브리지는 «결핍 물리의 대체품»이 아니라 «물리가 이미 지고 있는 두께(THICK)의 표시»다.**
    보조 힘 원칙 **비저촉** — 힘을 더하지 않고 **그리기만** 한다(물리 로직 0줄 · 0프레임).
```

### §0-3 ㉠ 브리지 — 표시 전용 기하

```
 시접 쌍(**268 / 242 / 268** · v3-44 계기 재사용) 정점열을 따라 두 가장자리를 잇는
 **삼각형 스트립**을 «표시 사본»에만 생성한다. **blob 무변조.** 재질·색 = **옷과 동일**.
 `productView`(제품 씬)와 진단 래스터 **«브리지 벌»** 양쪽에 단다 —
 **진단 래스터의 등재 3뷰 프리셋은 무변경**, 브리지 on/off 를 **벌로 구분**한다(**off 가 등재 기준**).
```

### §0-4 ㉡ 투명성 — **가리는 것을 값으로**

```
 브리지가 덮는 **갭 분포를 v3-44 ㉠1 표에서 재인용**해 병기한다.
 **코너 확폭(최대 8.09mm)** 은 «브리지가 가리는 물리 관측»으로 **명시 등재 — 삭제 0**.
 **#89 갱신**: 렌더 기여 목록에 「**시접 브리지(표시 전용 · 스트립 n개)**」 추가.
```

### §0-5 ㉢ 검증 — v3-44 ㉠2 채널 재측정 (같은 계기 · 브리지 on)

```
 **성공 판정(사전 등록 · 참조값은 전부 v3-44 등재분)**
   sideXplus 끊김(**전방 폐색 제외 · 팔 숨김 열**) **≤ 4.9‰**
   — 4.9‰ 는 **등재된 front/back 끊김의 최대값**이다(v3-44 ㉠2 표: gray front/back 4.9 ·
     swim front 4.9 / back 1.4 · sweat front 4.7 / back 4.0). **손 상수 아님 · 도출 참조 명기.**
   **3원단 «전부» 충족해야 성공.**
   front/back 도 재측정해 **악화 없음**(등재값 ±) 확인.
 **무변조**: 표시 전후 payload·pos sha 동일(v3-43 §3-1 형식).
```

### §0-6 ㉣ 재캡처 + 상신

```
 제품 **9장** + 대조 **3장**(v3-43 규약 · `-p` 뷰 · 900×1260) — **브리지 on**.
 **CC 자체 화면 판정 0.** 캡처 · 재현 절차 · 검증 수치만 낸다.
 사용자 재판정 대상 = **(나) 끊김 해소 여부 + (가) 어깨 인상 변화.**
```

### §0-7 ㉤ 갈래 (사전 등록)

| 갈래 | 조건 | 귀결 |
|---|---|---|
| **A** | ㉢ 성공(3/3) · 무변조 · 캡처 12장 | 사용자 화면 판정 **상신** |
| **B** | ㉢ 실패(어느 원단이든 > 4.9‰) | **잔여의 자리·크기 등재 · 브리지 유지한 채 정지 · 상신**(표시로 안 닫히는 성분 = 물리 판 후보의 입력) |
| **C** | 무변조 실패 | 정지 · 상신 |
| **D** | 환경 제약 | 막힌 지점 명기 |
| **E** | 판정 불가 | 정지 · 사실만 등재 |

### 이 판에서 바꾸지 않는 것

물리 로직 **0줄** · 물리 실행 **0프레임** · blob **무변조** · 문턱·게이트·기준선 **0** ·
**`SEP`·시접 제약 0**(물리는 옳다 — 그리기만 바꾼다) · **진단 래스터 프리셋 0**(브리지는 벌) ·
v2 경로 · **CC 자체 화면 판정 0** · **(가) 관련 수정 0**.

### 순서

```
 §0 커밋(논증 포함) → 브리지 구현 → ㉢ 검증 → ㉡ 투명성 등재 → ㉣ 캡처 → 등재
```

