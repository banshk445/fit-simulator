# P29 — 매듭 갱신 (배포 + README · 새 기능 0)

**데모**: https://banshk445.github.io/fit-simulator/?patterncore=1
**계기 URL**: https://banshk445.github.io/fit-simulator/?patterncore=1&ringtotal=0

**실행**: §1 회귀 · §2 배포 갱신(P18~P28 반영) · §3 배포 URL 실측(**반팔 18/18 · 긴팔
17/17 비트 동일** · 루프 판독) · §4 README 갱신 + **대표 이미지 교체** · §5 회귀(인용).
**미실행**: 새 기능 · 셔츠 · 밴드 on · fixture 재생성 · 관통 처방 · 어깨너비 범위 ·
물리 개선 · UI 변경 · **§3-7 루프 게이트 수리(P30 몫)** — 회차 조항대로 착수 0.
**수리 0 · 값 변경 0 · 새 문턱 0 · 새 프리셋 0 · 기전 귀속 0.**

**코드 0줄.** 이 판이 바꾼 소스 파일은 `README.md` 하나다(그 외는 `docs/`·`.obsidian-log/`).

**이 판에서 호출한 ecc 에이전트: 0.**

---

## §1 회귀 — 배포 «전» 로컬에서 전량 확인

| 채널 | 결과 |
|---|---|
| `npx tsc -b` · `npm run build` | **통과** · index 568.68kB / three 725.72kB / fixture 별도 청크 1,789.33kB |
| v1 게이트 4종 | `check:weld` · `check:sleeve` · `check:seambridge` · `check:demo` **PASS** |
| v2 게이트 3종 | `check:boundary` · `check:outline` · `check:topology` **PASS** |
| v2 `check:pattern` | **실패 1건 — 최소각(sleeve) 24.74° < 25°.** 기존 등재분과 동일(신규 0) |
| 하네스 `RINGTOTAL=0 PATTERNCORE=1` | **934줄 · 정규화 후 쌍 diff 0줄**(`P2d-log-normalize.py`) |
| `dress-state` md5 | `2501239ab06a678cd956d73be8e14159` **불변**(정본 일치 · 2회 실행 후에도 동일) |
| 하네스 하드 게이트 | 자기교차 **1889**(엣지-삼각형) — metrics 기존 블록과 같은 값. 그 외 PASS |

**코드 0줄이므로 위 전부는 「안 바뀐 것을 확인」한 결과다.**

## §2 배포 갱신 — gh-pages

```
gh-pages  54697d3  「배포: P29 정적 빌드 (P18~P28 반영 — 위상 일반화 · 커프 밴드(기본 off) ·
                    조립층 수리 · 결정성 완성(A포즈 절대 구성) · 기준선 B-3)」
직전       d139ddc  「배포: P17b …」 ⟹ **배포본이 P17b에서 P28까지 왔다**
번들       assets/index-DGT6qIo7.js
```

**서빙 확인(추측 0)**: 배포 URL에서 `index.html`을 `cache:'no-store'`로 다시 받아
참조 자산 목록을 뽑았다 — `index-DGT6qIo7.js` · `three.module-Ds95rE7w.js` ·
`meshCollision-DYo7Kp9H.js` · `index-DIZzIPdr.css`. **로컬 빌드 산출 해시와 일치**하고,
실제 로드된 스크립트도 같은 파일이다.

## §3 배포 URL 실측 — **재개 후 완료. 반팔 18/18 · 긴팔 17/17 «비트 동일»**

> **§3-1~§3-3은 1차 시도의 기록이다(원문 무삭제).** 결론은 §3-4·§3-5에 있다.

### §3-1 계기 정의역을 틀렸다 (자체 판정 · 함정 13 계열)

첫 실행을 **`?patterncore=1`** 로 했다. B-3의 계기 URL은 **`?patterncore=1&ringtotal=0`**
이다(P28 §조건 · P27c §공통 · P17과 같은 계기). 목선 링 총 길이 상한이 켜진 채로 잰
값이므로 **B-3과 비교할 수 없다.** 값을 버리지 않고 아래에 원문으로 남기되
**판정에 쓰지 않는다.**

이 어긋남 자체가 값으로 확인된다 — **제도·배치까지는 비트 동일하고 정착만 갈린다**:

| 단계 해시 | B-3 정본 | 이번 실행 | |
|---|---|---|---|
| `bodyPos` | `8051f71f` | `8051f71f` | 동일 |
| `armsAxis` | `c4b8ef51` | `c4b8ef51` | 동일 |
| 배치 `placed` | `032ac5e2` | `032ac5e2` | 동일 |
| 정착 `settled` | `2555c894` | `5f76f690` | **상이** |

`ringtotal`은 정착 단계의 제약이므로 이 분포와 정합한다. **기전 귀속은 하지 않는다** —
같은 URL로 재측정해 확인할 몫이다.

포즈 해시는 착장 «전»에 확인했고 **B-3 정본과 일치**했다:
`arm18 b34c4f2d` · `scaleDigest 408ff818` · `targetKey 1,1,1,1,1` ·
`maxArmDeltaM 0` · `maxScaleResidual 0` (P24·P27 고정이 배포본에서 작동한다).

### §3-2 실측 원문 (`?patterncore=1` · **대조 불가** · 기록만)

```
DONE f=514 · 벽시계 41.8s
cov 6.60% (114/1728) · maxStrain 2.3937566065572597(정점 2146) ·
maxSeamGap 10.693305362602892mm · Δ20 5.495569743820469mm ·
자기교차 1748 · 관통 56/5204 · 목선 링 54.05483692944385cm ·
밑단 앞 55.8782536170382 / 뒤 58.15825531048523 / 합 114.03650892752343cm ·
소맷부리 35.28302008311891cm(제도 33.18583665720241 · 여유 8.825583921474456) ·
정점 5204 · 제약 29392 · 시접 240쌍
```

핏 리포트 5행(중앙값 mm): 목선 **11.99**(n62) · 가슴 **14.24**(n291) · 허리 **15.44**(n288) ·
밑단 **14.60**(n70) · 소매 **12.83**(n908 · 표본 908/908) · **소매 부호 일치율 100.0%** ·
판정 경계 marginMm **15**.
관통 분해: 패널별 앞 0 / 뒤 6 / 소매L 24 / 소매R 26 · 소매 교차표 **50/50/50 · 차집합 0**
(P14가 등재한 「눌림과 관통이 같은 집합」이 배포본에서도 유지된다).
**JS 에러 0건**(콘솔 조회 결과 경고 1 = THREE.Clock deprecation · 로그 3 = 텍스처 판별).

### §3-3 재측정이 막힌 자리 — **계기 사실 1건 등재 · 수리 0**

`?patterncore=1&ringtotal=0`으로 새 로드한 뒤 **착장 버튼이 영영 안 켜진다.**
원인은 값으로 확인됐다: **`document.hidden === true`** — 탭이 백그라운드면 브라우저가
`requestAnimationFrame`을 멎히므로 마네킹 포즈 루프가 **한 프레임도 안 돈다**
(`__fitDebug.poseState().frames === 0` · `canvas` 300×150 기본 크기 그대로 ·
GLB는 캐시에서 75ms에 도착해 **로딩 문제가 아니다**). 화면은 「마네킹 불러오는 중 —
준비되면 «착장하기»가 켜집니다」에서 멈춘다.

이는 P17 ㉯(마네킹 준비 전 실행 차단)가 **의도대로 작동한 결과**이고 결함이 아니다.
**등재 대상은 절차 쪽이다**: 배포 URL 실측은 **탭이 전경일 때만 성립한다.**
탭 3개(재사용 1 · 신규 2)에서 같은 상태였고, 스크린샷·클릭·JS 주입으로는 전경 전환이
안 된다. **사용자 조치 대기 1건.**

첫 실행이 성공한 것은 그 탭이 전경이었기 때문이다 — 즉 **같은 배포본·같은 브라우저에서
전경/배경이 실측 가능 여부를 가른다.** 「배포가 깨졌다」로 읽지 말 것.

### §3-4 재개 실측 — 반팔 기본값 · **B-3과 18/18 일치**

조건: 배포 URL `?patterncore=1&ringtotal=0` · **새 로드** · 슬라이더 기본값 ·
탭 **전경**(측정 전 구간 `document.hidden === false` · `visibilitychange` 감시 결과
백그라운드 전환 **0회**) · 포즈 정착 대기 2,154ms · 벽시계 34.2s.

**`DONE f=369` — B-3과 같다.**

| 채널 | B-3 정본 | 배포 실측 | |
|---|---|---|---|
| cov | 6.94%(120/1728) | 6.94%(120/1728) | 일치 |
| maxStrain | 2.6504517308513718 | 2.6504517308513718 | 일치(정점 2146) |
| maxSeamGap | 10.515096008260077mm | 10.515096008260077mm | 일치 |
| Δ20 | 5.42878104356224mm | 5.42878104356224mm | 일치 |
| 자기교차 | 1777 | 1777 | 일치 |
| 관통 | 51/5204 | 51/5204 | 일치 |
| 밑단 합 | 114.14605334420155cm | 114.14605334420155cm | 일치(앞 55.752577129200674 / 뒤 58.393476215000874) |
| 목선 링 | 60.69840566032626cm | 60.69840566032626cm | 일치 |
| 소맷부리 | 35.28367929384613cm | 35.28367929384613cm | 일치(제도 33.18583665720241 · 여유 8.825583921474456) |
| 정점·제약 | 5204 · 29392 | 5204 · 29392 | 일치(시접 240쌍) |
| DONE f | 369 | **369** | 일치 |
| `bodyPos` | `8051f71f` | `8051f71f` | 일치 |
| `armsAxis` | `c4b8ef51` | `c4b8ef51` | 일치 |
| 배치 `placed` | `032ac5e2` | `032ac5e2` | 일치 |
| 정착 `settled` | `2555c894` | **`2555c894`** | 일치 |

**18/18 전량 일치 · 갈린 채널 0.** 정착 해시까지 같으므로 **프로덕션 번들이 dev와 같은
물리를 돈다**는 것이 P9 §3 이후 P18~P27의 코드 변경을 거치고도 유지된다.

같은 실행의 나머지 해시(정본 미등재분 · 기록용):
`arms 979fb4ff` · `armsDir 578256cb` · `armProfile 3da6976a` · `bodyIdx dfa0a0e2` ·
`draftDims 32063381` · `slices cb5262ad` · `ridge b2ff7556` · `segCounts bcc230f4` ·
`segLens 861ba5c0` · `pos2 3e81d034` · `tris 56f0a51f` · `edges e45ef9a9`.

핏 리포트 5행 중앙값(mm): 목선 14.24(n62) · 가슴 14.49(n306) · 허리 15.33(n288) ·
밑단 14.03(n70) · 소매 12.92(n908) · **소매 부호 일치율 100.0%**.
관통 분해: 앞 0 / 뒤 4 / 소매L 23 / 소매R 24 · 소매 교차표 **47/47/47 · 차집합 0**.

### §3-5 재개 실측 — 긴팔 · **P28 §2 정본과 17/17 일치 · DONE f 신규 기록**

조건: 새 로드 → **긴팔** 전환(소매길이 58cm 자동) → 착장. 탭 전경 유지(전환 0회) ·
포즈 정착 대기 734.5ms · 벽시계 35.1s.

**`DONE f=322` — P28 §2가 「미기록」으로 남긴 값이다. 이 판이 기록한다(P28 §7-3 해소).**

| 채널 | P28 §2 정본 | 배포 실측 | |
|---|---|---|---|
| cov | 6.54%(114/1744) | 6.54%(114/1744) | 일치 |
| maxStrain | 2.6488423766847906 | 2.6488423766847906 | 일치(정점 2146) |
| maxSeamGap | 9.428393670688457mm | 9.428393670688457mm | 일치 |
| Δ20 | 5.4839351047511835mm | 5.4839351047511835mm | 일치 |
| 자기교차 | 1969 | 1969 | 일치 |
| 관통 | 107/6294 | 107/6294 | 일치 |
| 밑단 합 | 112.97725132730494cm | 112.97725132730494cm | 일치(앞 55.50364794751126 / 뒤 57.47360337979367) |
| 목선 링 | 61.41350500927022cm | 61.41350500927022cm | 일치 |
| 소맷부리 | 25.810068656617407cm | 25.810068656617407cm | 일치(제도 23.706884804658863 · 여유 8.825583921474456) |
| 정점·제약 | 6294 · 35738 | 6294 · 35738 | 일치(시접 286쌍) |
| `bodyPos` | `8051f71f` | `8051f71f` | 일치 |
| 배치 `placed` | `ce6aaa5e` | `ce6aaa5e` | 일치 |
| 정착 `settled` | `8f988b66` | **`8f988b66`** | 일치 |
| DONE f | **미기록** | **322** | 신규 기록 |

**17/17 전량 일치 · 갈린 채널 0.**

핏 리포트 5행 중앙값(mm): 목선 14.28(n62) · 가슴 14.53(n305) · 허리 15.44(n290) ·
밑단 14.12(n70) · 소매 15.49(n1998) · **소매 부호 일치율 100.0%**.
관통 분해: 앞 0 / 뒤 6 / 소매L 52 / 소매R 49 · 소매 교차표 **101/101/101 · 차집합 0**
(반팔과 마찬가지로 눌림·관통이 같은 집합 — P14 유지).

**부수 관찰 1건(등재만 · 귀속 0)**: 긴팔의 `armsAxis`가 **`cd1b8c39`** 로 반팔
(`c4b8ef51`)과 **다르다.** P28 §2는 긴팔 `armsAxis`를 적지 않았으므로 대조 상대가 없다.
같은 몸·같은 로드에서 옷만 바꿨는데 「팔 축」 해시가 갈리는 것이므로 **그 채널의 정의역이
몸에 한정되지 않는다**는 뜻이다(P28 부수 등재 ① `bodyPos` 정의역 건과 같은 계열 ·
함정 13 계열). 나머지 17채널이 정본과 비트 동일하므로 **비결정성이 아니다.**
**수리 0 · 기전 귀속 0 · 다음 판 몫.**

## §3-6 1차 시도의 §3-1이 남긴 값 — 이제 해석된다

1차(§3-2)는 `ringtotal` 없이 재서 정착 해시만 `5f76f690`으로 갈렸다. 재개 실측에서
**같은 URL 조건이면 정착 해시가 `2555c894`로 정확히 재현**되므로, 그 차이는
**`ringtotal` 정의역 차이 하나로 설명된다.** 1차의 「입력·배치는 같고 정착만 다르다」는
관찰이 그대로 확인됐다. **여전히 기전 귀속은 하지 않는다**(목선 링 상한이 정착 단계에서
무엇을 하는지는 이 판의 정의역 밖).

## §3-7 착장 루프를 «무엇이 모는가» — **워커가 몬다** (코드 판독 · 재현 실험 0)

| 자리 | 파일:줄 | 내용 |
|---|---|---|
| 워커 생성 | `DressButton.tsx:221` | `new Worker(new URL("../workers/patternDressWorker.ts", …), {type:"module"})` |
| 착수 | `DressButton.tsx:308` | `worker.postMessage({ringTotal, garmentDims, fixture})` |
| 워커 진입 | `patternDressWorker.ts:29` | `self.onmessage = async (ev) => …` |
| 코어 호출 | `patternDressCore.ts:800` | `runDressing(sim, …)` |
| **루프 본체** | `dressingMachine.ts:175` | **`while (frame < maxFrames && cur() !== "DONE" && cur() !== "ABORT")`** — 동기 while |
| rAF 유무 | 위 4파일 | `requestAnimationFrame` · `useFrame` **0건**(grep) |

⟹ **「백그라운드 탭에서 착장이 멎는다」는 성립하지 않는다.** 착장은 워커 스레드의 동기
루프이고 탭 가시성과 무관하다. 진행 보고만 `PROGRESS_EVERY` 주기로 `postMessage` 된다.

**그런데 «시작 게이트»는 rAF에 묶여 있다** — 이쪽이 실제 자리다:

```
Mannequin.tsx:236 / :311   useFrame(...)  ← r3f = requestAnimationFrame
      └─ mannequinPoseRef.frames / scaleDigest / armDigest 를 매 프레임 쓴다
DressButton.tsx:78         base = mannequinPoseRef.frames >= 1 && 잔차 ≤ eps && 본 준비
DressButton.tsx:79         if (!base) return;          ← 여기서 되돌아간다
DressButton.tsx:89         else if (waited > POSE_STOP_TIMEOUT_MS) { …그대로 굽는다 }
```

**대기 상한 탈출(`:89`)이 `base` 가드(`:79`) «뒤»에 있다.** 백그라운드 탭은 rAF가 멎어
`frames === 0`이므로 `base`가 영원히 거짓이고, **상한에 도달하는 경로 자체에 못 간다.**
`setInterval` 폴링은 백그라운드에서도(느리게) 돌지만 매번 `:79`에서 되돌아간다.
관측과 일치한다 — 93분을 둬도 버튼이 안 켜졌다.

**판정**: 이 사고는 **측정·촬영 채널만의 문제가 아니다.** P23 §1이 상한을 둔 취지가
「말없이 막아 두지 않는다」(함정 25)인데, **이 경로에서는 정확히 말없이 막힌다.**
다만 탭을 전경으로 되돌리면 rAF가 재개되어 정상 진행되므로 **영구 갇힘은 아니다**
(재개 실측이 그 복구를 실증했다). ⟹ **제품 결함 후보로 등재**하고 **수리는 P30 몫**이다.
이 판은 **수리 0 · 값 변경 0.**

## §4 README 갱신

### §4-1 추가한 것

| 자리 | 내용 |
|---|---|
| 「지금 되는 것」 | **결정성 항목 신설** — P27 27회 비트 동일 · 착장 실패 0/27 · 슬라이더 왕복 후 복귀 |
| 데모 콜아웃 | **계기 URL `&ringtotal=0` 조항** — 이 판이 §3-1에서 밟은 것을 다음 사람이 안 밟게 |
| 검증 방식 | **단계별 비트 해시 채널**(`diag.bits` · P21) · **기준선 B-3 수치 전량** |
| 실행 블록 | v2 게이트 4종 전량 + **`check:pattern` 실패 1건이 서 있다는 고지** |
| 파이프라인 표 | 배치 행에 **역할 질의**(`topology.roleOf` · P18) |
| 한계 표 | **신규 2행** — `round()` 경계 증폭기(P21·P22) · 조립 층 잔여 고정 `necklineRing`(P21) |
| 제품 축 색인 | P1~P16 → **P1~P29** |
| 전제 뒤집힘 | 셋 → **넷**(P17 추가) + **「판정 불가」 2건 절 신설**(P20·P21) |

### §4-2 고친 수치

| 항목 | 전 | 후 | 근거 |
|---|---|---|---|
| 하네스 로그 줄 수 | 938 | **934** | 이번 판 실측 2회 |
| 반팔 관통 | 54/5204 | **51/5204** | B-3(P28) |
| 긴팔 관통 | 118/6320 | **107/6294** | B-3 긴팔 정본(P28 · 정점이 6320→6294로 줄었다) |
| 캠페인 회차 | 109 | **108** | `.obsidian-log/회차/` 108개 |
| 함정 수 | 32 | **33** | `.obsidian-log/함정/` 33개 |
| 함정 절 제목 | 「13가지」(본문은 15건) | **「15건 발췌 · 볼트 정본 33건」** | 제목이 본문과 어긋나 있었다 |

### §4-3 상태가 바뀐 한계 3행

- **긴팔 커프 밴드 미완** → **커프 밴드 기본 off**. 「위상 리팩터가 선행한다」는 끝났고
  (P18·P19 §1) 밴드는 선다. 남은 것은 **긴팔 착장 실패**와 **밴드 on 경로의 비재현성**이다.
- **몸 좌표 민감도** → 민감도 자체는 유지, **P27 이후 실사용에서 안 드러난다**로 정정.
  B-3의 재현 조건이 B-2보다 넓다는 것(슬라이더 왕복 후에도 재현)을 병기했다.
- **옷 타입 1종** → 선행 리팩터가 **소비 층은 끝났고 생산 층 9곳이 남았다**로 구체화.

### §4-4 대표 이미지 — **교체 완료**

`docs/captures/P17-핏맵렌더수리/배포-핏맵+리포트.jpg`
→ **`docs/captures/P29-매듭갱신/배포-핏맵+리포트.jpg`**(1440×723 · md5 `cb760a082e4289c86e38d4f01a30351d`).

**§3-5의 긴팔 실행 화면 그대로다** — 즉 **그림과 숫자가 같은 실행에서 나왔다**(DONE f=322가
캡처에 보인다 · P27c 조항 충족). 이 상태는 그 자리에서 정본과 17/17 대조를 마쳤으므로
P16이 깨진 화면을 올린 사고(P17 정정)가 구조적으로 반복될 수 없다.

**⚠ 회차 지시 중 「체커 원단 · 핏맵 off」는 「핏 리포트가 함께 보이게」와 현 UI에서
양립하지 않는다** — 핏 리포트 표는 **핏 맵 토글 뒤에** 있다(토글을 끄면 표도 사라진다 ·
P17 §①이 「토글은 표와 범례를 켠다」로 등재한 그 구조). 「핏 리포트가 함께 보이는」 쪽을
택했고, **핏맵 off·체커 화면은 같은 실행에서 따로 찍어 보존했다**:
`docs/captures/P29-매듭갱신/배포-체커-핏맵off.jpg`(md5 `fff3acf59815172d5e8bf467d0283eab`).
**구도 임의 변경 0**(창 1440×900 · 좌측 패널 스크롤로 리포트를 올린 것이 전부).

### §4-5 재개 후 추가한 것

- **긴팔 정본 수치**(cov 6.54% · 자기교차 1969 · 관통 107/6294 · 링 61.414 ·
  소맷부리 25.810 · 정점·제약 6294·35738 · **DONE f=322**)
- **「배포된 사이트에서 다시 잰다」 조항** — 반팔 18/18 · 긴팔 17/17 비트 동일 실증 병기
- **한계 표 신규 2행** — 「세션 누적 미재측정」(P28 §7-1 · **「완화됐다」가 아니라 «미측정»**) ·
  「백그라운드 탭에서는 시작할 수 없다」(§3-7 · 갇히지는 않는다는 것까지)
- `check:topology` 주석에 통과 구성 3종(티셔츠 4패널 · 셔츠형 5패널 · 커프형 6패널)

**옛 수치 잔존 검사**: README에서 B-2·P13 계열 값(`61.5` · `113.9` · `112.97` · `25.86` ·
`292` · `287` · `2002` · `1979` · `14e50b89` · `938` · `6320` · `54/5204`)을 grep으로
훑었고 **잔존 0**이다(유일한 일치는 이 판이 새로 쓴 B-3 행이었다).

## §5 회귀 (재개분)

**코드 0줄이므로 §1을 인용한다** — 재개 구간에서 바꾼 파일은 `README.md`와
`docs/captures/P29-매듭갱신/` 2장뿐이고 `src/`는 **0줄**이다(`git status` 확인).
따라서 §1의 게이트 8종·하네스 934줄 쌍 diff·tsc·build 결과가 **그대로 유효하다**
(P28 §6 형식 — 인용임을 명기한다 · **재실행 0**).

**재개 구간에서 실제로 다시 본 것 2건**: `public/dress-state.json` md5
`2501239ab06a678cd956d73be8e14159` **불변** · `git status`가 `src/` 변경 **0**.
`patternHash 9f7ba80b3497`는 **P28 §5 인용**(재실행 0).

## §6 다음 판이 이어받을 것

1. **§3-7 루프 게이트 수리(P30)** — `DressButton.tsx:79`의 `if (!base) return;`이
   상한 탈출을 삼킨다. 「말없이 막아 두지 않는다」(함정 25 · P23 §1)를 이 경로에서도
   성립시키는 것이 표적. **이 판은 등재만 했다.**
2. **긴팔 `armsAxis` 정의역**(§3-5 부수 관찰) — 옷을 바꾸면 「팔 축」 해시가 갈린다.
   비결정성은 아니고 **채널명이 주장하는 정의역 문제**다(함정 13 계열).
   P28 부수 등재 ①(`bodyPos`)과 함께 볼 것.
3. 이월(순서 없음): 밴드 on 결정화 · 셔츠(생산 층 9곳) · 세션 누적 재측정 ·
   증폭기 `round(total)+1` · 관통 1~2% · 어깨너비 상한 62cm ·
   `necklineRing` 패널 0·1 고정 · `pairKey` 1e6 천장 · P23b 이월분.

**이 판이 해소한 이월 2건**: 긴팔 DONE f 미기록(→ **322**) · 정착 해시 대조(→ 반팔·긴팔
모두 배포본에서 정본과 비트 동일).

---

- 옛 정본: [[P16-매듭갱신]](배포 P10~P15 · 이 판이 잇는다) · [[P9-매듭]](§1 새 로드 조건)
- 관련: [[P28-기준선재수립]](B-3 정본) · [[P27-절대회전]](결정성) · [[P21-조립층과비결정성]] ·
  [[P18-위상일반화]] · [[P17-핏맵렌더수리]](대표 이미지 · 같은 계기)
