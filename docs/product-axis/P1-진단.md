# P1 — 치수 조정 경로 진단

> **제품 축 첫 판.** 드레이프 캠페인(2b · 107회차까지)은 보류. 목표 4개 중 ①「옷 치수 조정」의
> 착수 지점을 정하기 위한 **진단**이다. **코드 0줄** — 읽고 확인만 했다.
> 캠페인 규율(사전등록·갈래·회차 노트) **미적용**.

**스탬프** — HEAD `b4175e9`(진단 시작) · 브랜치 `v2-stage2a` · 워크트리
`/Users/banshk/projects/fit-simulator` · 2026-08-10.

**기준선 A 불변 확인** — 진단 종료 시 `dress:pattern` 1회:
`pattern 9f7ba80b3497 · fixture 9e8b2bf13925` · dress-state md5
`14e50b8919a63a7f9799220b86e508a9`. **코드 변경 0줄이므로 기준선을 깬 시점 없음.**

---

## §1 이미 확인된 코드 사실 — 재확인 결과

| # | 진술 | 판정 | 근거 |
|---|---|---|---|
| ① | `garmentSize.width` 슬라이더가 v2 제도 경로에서 **읽히지 않는다** | **참** | `patternDraft.ts`에서 `g.widthM`을 읽는 코드 **0곳**(`:206` 타입 선언 · `:229` 주석뿐). 104회차가 `halfWidthM = body.chestGirthM/4 + WIDTH_EASE_M`로 항등 재표현하면서 끊겼다 |
| ② | `PatternPreview`의 `g.widthM`은 **죽은 입력** → 슬라이더가 미리보기 폭을 못 바꾼다 | **참** | `PatternPreview.tsx:224-230`이 `garmentSize` 5종을 전부 넘기지만 그중 `widthM`만 소비처가 없다. §2에서 실측 확인 |
| ③ | v2 경로가 **`wearable` 게이트를 통과하지 않는다** | **참** | `FitCanvas.tsx:75-77`이 `wearable`을 계산하지만 렌더 분기 `:104-111`의 `patternCore &&` 쪽은 그것을 **안 본다**(v1 `Garment` 분기만 본다) |
| ④ | 같은 슬라이더가 소비처에 따라 **폐둘레 ↔ 반둘레**로 2배 다르게 해석 | **참** | `FitCanvas.tsx:48` `radiusM = garmentSize.width / (2π) / 100` = **품을 폐둘레로** 읽는다(v1 원통 오버레이 `GarmentMesh`). v1 물리·v2 제도는 품 = 패널 1매 전폭(폐둘레 = 2×품) |
| ⑤ | 몸 fixture가 **고정 로드**된다 | **참** | `PatternPreview.tsx:192` `import("../../scripts/fixtures/collision-fixture.json")` · `measureBody(...)`가 그 fixture 좌표로만 돈다 |
| ⑥ | 폭 «이외» 치수는 제도가 실제로 읽는다 | **참 — 폭만 끊겼다** | `patternDraft.ts:245 g.shoulderWidthM` · `:294-295 g.lengthM` · `:341 g.sleeveWidthM` · `:360 g.sleeveLengthM`. **4종 전부 살아 있다** |

**§1 결론**: 6항 전부 지금도 참이고, ⑥이 범위를 좁힌다 — **끊긴 것은 「품」 하나**다.

---

## §2 실제 UI 동작 (브라우저에서 직접 조작 · 추측 0)

`http://localhost:5173/?patterncore=1` · 슬라이더를 실제로 드래그하고 콘솔 로그
(`[patternPreview] 정적 배치 렌더 — 정점 N …`)의 **정점 수 변화**로 재제도 발화를 판정했다.

| 슬라이더 | 범위 | 조작 | 미리보기(정적 배치) | 3D 착장 결과 | 재시뮬 | 콘솔 |
|---|---|---|---|---|---|---|
| **옷 총장** | 40~120 | 70 → 120 | **바뀐다** · 정점 5244 → 5845 → 7699 → **8221** | — | **자동**(드래그 중 매 스텝) | 정상 |
| **옷 품** | 35~90 | 55 → 90 | **안 바뀐다** · 정점 **5244 불변**(재제도는 돌지만 산출이 같다) | — | 자동(무의미) | 정상 |
| **옷 어깨너비** | — | 45 → 61 | **바뀐다** · 8653 → **8815** | — | 자동 | 정상 |
| **옷 소매길이** | — | 22 → 31 | **바뀐다** · 8221 → 8489 → **8551** | — | 자동 | 정상 |
| **옷 소매통** | — | 18 → 20 | **바뀐다**(소매가 굵어지는 것이 화면에 보인다) | — | 자동 | 정상 |
| **몸 가슴둘레** | 70~140 | 100 → 140 | **마네킹은 굵어진다** · **패턴은 무반응**(재제도 로그 **0건**) | — | **안 돈다** | 정상 |
| 몸 키·팔 길이·다리 길이·어깨너비 | — | 미조작 | (같은 구조 — `bodySize`는 v2 패턴 입력에 배선 0) | — | 안 돈다 | — |

**콘솔 에러·경고 0건**(전 조작 구간).

**「3D 착장 결과」 열이 전부 «—»인 이유 = §3의 핵심 발견**이다.

**부수 관측(해석 아님)**: 가슴둘레 140cm에서 마네킹 가슴이 옷을 뚫고 나온다 —
**마네킹은 슬라이더를 따라가고 옷은 fixture 몸으로 제도돼 있어 둘이 서로 다른 몸을 본다.**

---

## §3 경로 지도 — **브라우저에 착장 시뮬이 없다**

```
[슬라이더]                    [상태]                [제도]                     [패턴]              [시뮬]            [렌더]
Controls.tsx:196 총장    ─→ useFitStore        ─→ patternDraft :294-295 ─→ buildPatternGarment ─→   ✗           ─→ PatternPreview
Controls.tsx:197 품      ─╳  garmentSize.width ─→ patternDraft «읽지 않음» ─→ (영향 0)          ─→   ✗           ─→ (불변)
Controls.tsx:198 어깨너비 ─→ useFitStore        ─→ patternDraft :245       ─→ buildPatternGarment ─→   ✗           ─→ PatternPreview
Controls.tsx:232 소매길이 ─→ useFitStore        ─→ patternDraft :360       ─→ buildPatternGarment ─→   ✗           ─→ PatternPreview
Controls.tsx:239 소매통  ─→ useFitStore        ─→ patternDraft :341       ─→ buildPatternGarment ─→   ✗           ─→ PatternPreview
Controls.tsx:119 가슴둘레 ─→ useFitStore        ─╳ (fixture 고정)          ─→ (영향 0)          ─→   ✗           ─→ Mannequin만
```

| 구간 | 상태 | 끊긴 이유 |
|---|---|---|
| 슬라이더 → 상태(전 항목) | **연결됨** | — |
| 상태 → 제도 · **품** | **끊김** | **죽은 입력** — 104회차 항등 재표현이 `halfWidthM`을 `body.chestGirthM/4 + e`로 바꾸면서 `g.widthM` 소비처가 사라졌다(`patternDraft.ts:231-244`) |
| 상태 → 제도 · 총장·어깨너비·소매길이·소매통 | **연결됨** | — |
| 상태 → 제도 · **`bodySize` 전량** | **끊김** | **미배선** — `PatternPreview.tsx:192`가 `collision-fixture.json`을 고정 로드하고 `measureBody`가 그 좌표로만 돈다. 슬라이더 몸은 `Mannequin`(렌더)만 바꾼다 |
| 제도 → 패턴 | **연결됨** | 드래그 중 매 스텝 재제도(정점 수가 실시간으로 바뀐다) |
| 패턴 → **시뮬** | **끊김(전 경로)** | **미배선** — `PatternPreview.tsx:1-5` 원문: 「**물리 없음.** … 워커·시뮬 배선은 2b에서 온다」. 2b는 스크립트(`dress:pattern`)에서만 돈다 |
| 시뮬 → 렌더 | **조건부** | `?patternstate=1`이면 `PatternPreview.tsx:245` `fetch("/dress-state.json")`으로 **스크립트가 남긴 파일**을 그린다. 없으면 `:247` 경고 후 정적 배치. **슬라이더와 무관한 «옛 결과»다** |
| 착용 가능 게이트 → v2 | **끊김** | `FitCanvas.tsx:104-111`의 `patternCore` 분기가 `wearable`을 안 읽는다 |
| 품 → v1 원통 오버레이 | **연결됨(규약 불일치)** | `FitCanvas.tsx:48`이 품을 **폐둘레**로 읽는다 — 제도의 **반둘레** 규약과 2배 차 |

---

## §4 재시뮬 비용

| 경로 | 비용 | 산출물 |
|---|---|---|
| `dress:pattern`(스크립트) | **벽시계 42.1s** · 내부 19.1s · 정착 260프레임 · 물리 73.6ms/프레임 | 실제 착장 결과(`dress-state.json`) |
| 브라우저 v2(`PatternPreview`) | **즉시**(드래그 중 실시간 재제도) | **정적 배치**(물리 0) |

**설계 제약(사실 등재 · 해결책 제시 0)**
1. **42초는 실시간 UI에서 감당되지 않는다** — 슬라이더 1틱마다 이 비용을 낼 수 없다.
2. 그러나 그보다 앞선 제약이 있다 — **브라우저에는 착장 경로가 아예 없다.**
   즉 「42초를 어떻게 줄이나」 이전에 「브라우저에서 착장을 어떻게 돌리나」가 미해결이다.
3. 현재 `?patternstate=1` 화면은 **슬라이더와 무관한 파일 재생**이다.

---

## 막고 있는 것 — P2 이후 작업 자리

| # | 막힌 것 | 자리 | 성격 |
|---|---|---|---|
| **1** | **품 슬라이더가 제도에 안 닿는다** | `patternDraft.ts:231-244` | 죽은 입력(104회차 항등 재표현의 부수) · **①의 최소 착수점** |
| **2** | **브라우저에 착장 시뮬이 없다** | `PatternPreview.tsx:1-5`(예고만) · 워커 배선 부재 | 미배선 · **①③의 전제** |
| **3** | **`bodySize`가 v2 패턴에 안 닿는다** | `PatternPreview.tsx:192` fixture 고정 로드 | 미배선 · **②의 본체** |
| **4** | **재시뮬 42초** | `dress:pattern` | 설계 제약 · 2가 풀린 뒤에야 실체가 드러난다 |
| **5** | `wearable` 게이트가 v2에 미적용 | `FitCanvas.tsx:104-111` | 게이트 미통과 |
| **6** | 품의 폐둘레↔반둘레 2배 해석 차 | `FitCanvas.tsx:48` vs `patternDraft.ts:206` | 규약 불일치 · UI 라벨은 한 개다 |

**의존**: 1은 단독으로 풀린다(제도만 고치면 미리보기가 즉시 반응). 3도 단독으로 풀린다.
**2가 풀리기 전에는 어떤 슬라이더도 «착장 결과»를 못 바꾼다** — 미리보기만 바뀐다.
4는 2의 하위 문제다. 5·6은 독립이고 작다.
